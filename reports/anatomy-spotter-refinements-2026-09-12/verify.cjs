const fs=require('node:fs'),assert=require('node:assert/strict'),crypto=require('node:crypto'),parser=require('@babel/parser'),traverse=require('@babel/traverse').default;
const source=fs.readFileSync('stem_lab/stem_tool_anatomy.js','utf8');assert.equal(source,fs.readFileSync('desktop/web-app/public/stem_lab/stem_tool_anatomy.js','utf8'));
const english=JSON.parse(fs.readFileSync(__dirname+'/english.json','utf8')),registry=JSON.parse(fs.readFileSync('dev-tools/i18n/stem_anatomy_en.json','utf8')),table=JSON.parse(fs.readFileSync('dev-tools/i18n/handtl_anatomy_spotter_20260912.json','utf8')),seen=new Set(),vars=s=>(s.match(/\{\w+\}/g)||[]).sort();
traverse(parser.parse(source,{sourceType:'script'}),{CallExpression(p){const key=p.node.arguments[0]?.value;if(typeof key==='string'&&key.startsWith('stem.anatomy.spot_ref_')){assert.equal(p.node.arguments[1]?.value,english[key.slice(13)]);seen.add(key.slice(13));}}});assert.equal(seen.size,34);
for(const [key,value]of Object.entries(english)){assert.equal(registry[key],value);for(const variable of vars(value))assert.match(variable,/^\{[a-z]+\}$/);}
for(const [lang,dict]of Object.entries(table)){const packs=['','desktop/web-app/public/'].map(prefix=>JSON.parse(fs.readFileSync(prefix+'lang/'+lang+'.js','utf8')).stem.anatomy);assert.equal(Object.keys(dict).length,34);for(const [key,value]of Object.entries(dict)){assert.ok(value);assert.ok(value!==english[key]||(lang==='french'&&key==='spot_ref_correct'));assert.deepEqual(vars(value),vars(english[key]));for(const pack of packs)assert.equal(pack[key],value);}}
const b=JSON.parse(fs.readFileSync(__dirname+'/browser-results.json','utf8')),t=JSON.parse(fs.readFileSync(__dirname+'/test-results.json','utf8'));
assert.equal(t.success,true);assert.equal(t.numFailedTests,0);assert.ok(t.testResults.every(f=>f.status==='passed'));assert.equal(b.axe.length,20);assert.equal(b.screens.length,16);assert.deepEqual(b.errors,[]);assert.deepEqual(b.axe.flatMap(r=>r.violations),[]);assert.deepEqual(b.axe.flatMap(r=>r.incomplete),[]);
const v={syntax:'pass',sha256:crypto.createHash('sha256').update(source).digest('hex'),mirrorIdentical:true,localizedStrings:34,validatedPacks:6,testFiles:t.testResults.length,passedTests:t.numPassedTests,failedTests:t.numFailedTests,axeScans:b.axe.length,axeViolations:0,axeIncomplete:0,browserErrors:0,screenshots:b.screens.length};
fs.writeFileSync(__dirname+'/verification.json',JSON.stringify(v,null,2)+'\n');console.log(JSON.stringify(v,null,2));
const reportFile=__dirname+'/README.md';let report=fs.readFileSync(reportFile,'utf8').split('\n## Final verification')[0];report+='\n## Final verification\n\n- '+v.passedTests+' tests passed across '+v.testFiles+' anatomy test files.\n- 20 scoped axe scans: zero violations and zero incomplete checks.\n- 16 screenshots with no horizontal page overflow at the tested phone widths.\n- No browser errors; keyboard focus, hidden-answer speech, timing choice, recovery, and saved-round restoration passed.\n- Both active source copies match; syntax, all 34 translation keys, six packs, and placeholder compatibility passed.\n- Active source SHA-256: `'+v.sha256+'`.\n';fs.writeFileSync(reportFile,report);
