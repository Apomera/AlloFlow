const fs = require('fs');
const file='behavior_lens_module.js';
let source=fs.readFileSync(file,'utf8');
const eol=source.includes('\r\n')?'\r\n':'\n';
for (const [before,after] of [["        const [practiceScenarioName, setPracticeScenarioName] = useState('');","        const [practiceScenarioName, setPracticeScenarioName] = useState('');\n        const [practiceReturnStudent, setPracticeReturnStudent] = useState('');\n        const PRACTICE_WORKSPACE_ID = 'behavior-lens-practice';"],["            setAiAnalysis(null);\n            setAnalyzing(false);\n            setSessionNotes([]);","            setAiAnalysis(null);\n            setAnalyzing(false);\n            setIsPracticeMode(false);\n            setPracticeScenarioName('');\n            setPracticeReturnStudent('');\n            setSessionNotes([]);"],["            setAbcEntries(workspace.abcEntries);\n            setObservationSessions(workspace.observationSessions);","            setAbcEntries(workspace.abcEntries);\n            setObservationSessions(workspace.observationSessions);\n            setIsPracticeMode(data && data.isPracticeMode === true);\n            setPracticeScenarioName(data && data.isPracticeMode === true ? String(data.practiceScenarioName || 'Practice Scenario') : '');\n            setPracticeReturnStudent(data && data.isPracticeMode === true ? String(data.practiceReturnStudent || '') : '');"],["JSON.stringify(studentRoster.slice(0, 20))","JSON.stringify(studentRoster)"],["{ roster: studentRoster.slice(0, 20) }","{ roster: studentRoster }"],["return [{ id: uid(), name: selectedStudent, lastAccessed: new Date().toISOString() }, ...prev].slice(0, 20);","return [{ id: uid(), name: selectedStudent, lastAccessed: new Date().toISOString() }, ...prev];"],["        }, [selectedStudent, studentRoster]);\n\n        // Build a storage key","        }, [selectedStudent, studentRoster]);\n        const workspaceSelectionRef = useRef(null);\n        const workspaceSelectionIdentity = activeStudentId || 'name:' + selectedStudent;\n        if (!workspaceSelectionRef.current || workspaceSelectionRef.current.identity !== workspaceSelectionIdentity) {\n            workspaceSelectionRef.current = { identity: workspaceSelectionIdentity };\n        }\n\n        // Build a storage key"],["                aiAnalysis: aiAnalysis || null,\n                targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics,","                aiAnalysis: aiAnalysis || null,\n                isPracticeMode, practiceScenarioName, practiceReturnStudent,\n                targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics,"],["            aiAnalysis, targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics,\n            dismissedAlerts, visitedPanels, buildWorkspaceSummary]);","            aiAnalysis, isPracticeMode, practiceScenarioName, practiceReturnStudent, targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics,\n            dismissedAlerts, visitedPanels, buildWorkspaceSummary]);"],["            aiAnalysis, fullSummary, favorites,\n            targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics,","            aiAnalysis, fullSummary, favorites,\n            isPracticeMode, practiceScenarioName, practiceReturnStudent,\n            targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics,"],["            aiAnalysis, fullSummary, favorites, targetBehaviors, toolState, deletedAbcEntries,\n            auditLog, workflowDiagnostics, dismissedAlerts, visitedPanels]);","            aiAnalysis, fullSummary, favorites, isPracticeMode, practiceScenarioName, practiceReturnStudent,\n            targetBehaviors, toolState, deletedAbcEntries, auditLog, workflowDiagnostics, dismissedAlerts, visitedPanels]);"],["            if (isCanvasEnv || !firestore || !_cloudUserId || !studentId) return false;","            if (isCanvasEnv || !firestore || !_cloudUserId || !studentId || (data && data.isPracticeMode === true)) return false;"],["            if (cloudSync.userId && !isCanvasEnv && selectedStudent) {\n                cloudSync.saveToCloud(activeStudentId || selectedStudent, workspace)","            if (cloudSync.userId && !isCanvasEnv && selectedStudent && !isPracticeMode) {\n                cloudSync.saveToCloud(activeStudentId || selectedStudent, workspace)"],["            const shouldLoadCloud = !!(cloudSync.userId && !isCanvasEnv && !cloudLoadAttempted.current[loadKey]);","            const shouldLoadCloud = !!(cloudSync.userId && !isCanvasEnv && activeStudentId !== PRACTICE_WORKSPACE_ID && !cloudLoadAttempted.current[loadKey]);"],["            const localOnly = !!isCanvasEnv;","            const localOnly = !!isCanvasEnv || isPracticeMode || activeStudentId === PRACTICE_WORKSPACE_ID;"],["            if (cloudSync.userId && !isCanvasEnv && !suppressDirtyMark) {","            if (cloudSync.userId && !localOnly && !suppressDirtyMark) {"],["            const pendingWorkspace = pendingWorkspaceRef.current && pendingWorkspaceRef.current.student === selectedStudent\n                ? pendingWorkspaceRef.current.data : null;","            const pendingWorkspace = pendingWorkspaceRef.current && pendingWorkspaceRef.current.student === selectedStudent && pendingWorkspaceRef.current.studentId === activeStudentId\n                ? pendingWorkspaceRef.current.data : null;"],["                pendingWorkspaceRef.current = null;\n                applyStudentWorkspace(pendingWorkspace);","                pendingWorkspaceRef.current = null;\n                suppressNextWorkspacePersistRef.current = true;\n                latestLocalSnapshotRef.current = pendingWorkspace;\n                applyStudentWorkspace(pendingWorkspace);"],["            const identity = activeStudentId || 'name:' + selectedStudent;\n            if (localWorkspaceSaveSchedulerRef.current.hasPending())","            if (!activeStudentId) { abcHydratedRef.current = false; return undefined; }\n            const identity = activeStudentId;\n            if (localWorkspaceSaveSchedulerRef.current.hasPending())"],["            if (!selectedStudent || !cloudSync.userId) return;\n            const remote = await cloudSync.loadFromCloud(activeStudentId || selectedStudent);","            if (!selectedStudent || !cloudSync.userId || isPracticeMode) return;\n            const selection = workspaceSelectionRef.current;\n            const remote = await cloudSync.loadFromCloud(activeStudentId || selectedStudent, { reconcile: true });\n            if (workspaceSelectionRef.current !== selection) return;"],["        }, [selectedStudent, activeStudentId, cloudSync, applyStudentWorkspace, addToast, blAnnounceToSR]);","        }, [selectedStudent, activeStudentId, isPracticeMode, cloudSync, applyStudentWorkspace, addToast, blAnnounceToSR]);"]]) { const needle=before.replaceAll('\n',eol), replacement=after.replaceAll('\n',eol); const i=source.indexOf(needle); if(i<0 || source.indexOf(needle,i+needle.length)>=0) throw Error('Nonunique or missing anchor: '+before.slice(0,90)); source=source.slice(0,i)+replacement+source.slice(i+needle.length); }
fs.writeFileSync(file,source);