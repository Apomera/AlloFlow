const fs=require('fs'),p='reports/geometry-world-creative-tools-2026-09-12/selection-ui-functions.txt';let s=fs.readFileSync(p,'utf8');const a="'Remove saved stamp'))))";if(!s.includes(a))throw Error('Missing draft anchor');s=s.replace(a,"'Remove saved stamp')))");const f=fs.openSync(p,'r+');try{fs.writeFileSync(f,s);fs.ftruncateSync(f,Buffer.byteLength(s));}finally{fs.closeSync(f);}
