const {session}=require('./science_refinement_session.cjs');exports.refine=p=>{const {E,get,replaceAll,finish}=session(p);
E('reading','data',`## What Counts as Typical

A **statistical** question anticipates variation in the data used to answer it. Heights across a class or waits across bus arrivals can vary. A single specified person's height on one occasion asks for one value; studying that person's height across time is a different question.

A **data** set records observations. Before calculating, ask what was measured, who or what was included, and in which units. Plots can reveal patterns, gaps and unusual values.

The **mean** is the sum of the values divided by the number of values. For 2,3,3,4,8, the total is 20 and there are five values, so the mean is 4. An equal-sharing picture divides twenty squares into five groups of four.

The **median** comes from ordered data. With five values it is the third. For 2,3,3,4,8, the median is 3. Values can equal the median, so it is not always true that exactly half are strictly above and half strictly below. With an even count, average the two middle values: 3,5,8,10 has median (5 + 8) ÷ 2 = 6.5.

The **mode** is the most frequent value. Three is the mode of 2,3,3,4,8. More than one value can tie. Under our classroom convention, a set in which every value appears once has no mode. Modes also summarize categories, such as most common book genre.

Now consider 4,5,6,7,100. The mean is 24.4 and median is 6. The distant 100 is an **outlier** in this example. Changing only 100 to 1000 leaves the middle position unchanged, but increases the mean. The median is less sensitive to extreme magnitudes; adding, deleting or reordering observations can still change it.

Neither summary is automatically more honest. For a value near the cluster here, median 6 is useful. For equal sharing of the total, mean 24.4 answers another legitimate question. Check the distribution and purpose; a gap between mean and median does not by itself prove an error or a single outlier.

**Spread** describes variation. Two sets, 49,50,50,51 and 5,20,80,95, both have mean 50. Their **ranges**, largest minus smallest, are 2 and 90. Range uses only extremes; the plots reveal more.

A **sample** is the subset observed from a wider group. More observations do not automatically remove selection bias. Report how data were chosen and handle unusual values transparently.

Ask: what was measured, which summary is reported, and what does the spread show? One number cannot replace the full story.`);
E('glossary','data.3.def','The middle of ordered numerical data; for an even count, the mean of the two middle values.');E('glossary','data.4.def','A value with the highest frequency; several can tie.');E('glossary','data.8.def','A summary of location, such as mean or median; it does not represent every feature of a set.');
E('outline','data.branches.0.items.1','One specified height asks for one value; heights across a group or time can anticipate variation');E('outline','data.branches.0.items.2','Define the question and variation, not just the number of people');E('outline','data.branches.1.items.0','Plotting data helps reveal patterns, gaps and unusual values');E('outline','data.branches.2.items.2','Mean and median can differ; inspect the distribution and purpose');E('outline','data.branches.3.title','Different sensitivity to extremes');E('outline','data.branches.3.items.1','The median depends on ordered middle values and can change when the data change');
const M=(k,v)=>E('memory','data.cards.0.'+k,v);M('target','Compare how mean and median respond to this extreme value');M('aiExample','Place equal-mass markers at the data values along a number line. The mean is their balance point: moving one marker farther right shifts that point. In 4,5,6,7,100, replacing only 100 with 1000 keeps the third value at 6, so this median stays unchanged.');M('mapping','Markers must have equal mass; their positions represent the values. The unchanged median is specific to moving the already-largest value farther out, not a promise that a median never changes.');M('essentialFacts.1','The median depends on the ordered middle value or values; some changes affect it and some do not.');M('scaffoldStarter','In this replacement example, the mean ____ and the median ____ because ____.');M('scaffoldSteps.0','Draw equal-mass markers at the numerical values, not heavier markers for bigger values.');
E('sort','data.categories.0.label','STATISTICAL');E('sort','data.categories.1.label','ONE VALUE');E('sort','data.items.5.content','How tall am I at this specified time, using one measurement?');E('sort','data.items.7.content','What is the scheduled first departure for this route on this date?');
E('math','data.problems.3.question','For 4,5,6,7,100, find mean and median. Which is nearer the cluster of four values?');E('math','data.problems.3.answer','Mean 24.4, median 6; median is nearer the cluster');E('math','data.problems.3.steps.2.explanation','Median 6 is near the cluster from 4 to 7. Mean 24.4 describes equal sharing of all 122 across five observations. The useful summary depends on the question.');E('math','data.problems.5.steps.2.explanation','The third value is 155, so the median is 155. Mean and median agree in this particular symmetric set; lack of outliers alone does not guarantee equality.');
const q=get('quiz').data.questions;q[2].options[3]='It is 13, because you add the middle values without dividing';q[3].question='In 4,5,6,7,100, why does replacing 100 with 1000 change the mean but not the median?';q[3].options[3]=q[3].correctAnswer='The sum changes, while the third ordered value stays 6';q[4].options[0]=q[4].correctAnswer='The mean alone does not determine how spread out either group is';q[6].expectedAnswer='The sum is 122 and dividing by five gives mean 24.4. The largest value, 100, exceeds the other four combined (22) and raises the mean. The third ordered value is 6, so the median is 6. This example shows different sensitivity to an extreme; a gap alone is not proof of one particular cause in every data set.';
E('faq','data.3.answer','Values can tie for the highest frequency. In this lesson, when every value occurs once we say there is no mode; some conventions instead list all tied values. State the convention you use.');E('faq','data.4.answer','A larger well-designed sample can improve precision, but increasing the size of a biased sample does not remove the bias. Describe the target group and how participants or objects were selected.');E('faq','data.5.answer','Graph design can mislead. Check labels, units, axis limits and spacing. A nonzero axis start can be useful for some plots if shown clearly; bar charts need special care because bar length encodes magnitude.');
E('challenge','data.brief.context','Collect at least fifteen nonpersonal measurements, such as lengths of classroom pencils or letter counts in teacher-selected book titles. Alternatively use anonymous voluntary responses to an agreed question. No one must disclose sleep, family or travel information.');E('challenge','data.brief.seedDirection','Choose a clear question that anticipates variation. A prediction is welcome; collect data to test it rather than selecting values that support it.');E('challenge','data.brief.lockedLessonFacts.1','The mean responds to all values; the median is less sensitive to extreme magnitudes but can also change.');E('challenge','data.brief.constraints.2','Report the data and any corrections or exclusions transparently, without identifying participants');E('challenge','data.supports.coachPrompts.2','Is an unusual value an error or a real observation? Explain how you checked without identifying a person.');E('challenge','data.supports.parallelExample.context','An invented practice set of seven object lengths is 5,7,9,11,13,18,70 centimetres.');E('challenge','data.supports.parallelExample.move','The total is 133, so the mean is 19 cm. The fourth ordered value is 11 cm, the median. The range is 65 cm. The student reported both centres and the distant 70, explaining that a central value hides the spread. These are illustrative numbers, not collected class data.');E('challenge','data.supports.parallelExample.whyItHelps','The full data support every reported calculation, and no individual is identified by an unusual personal measurement.');
return finish();};
