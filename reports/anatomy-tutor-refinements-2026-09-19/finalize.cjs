const fs=require('fs'),file=__dirname+'/README.md';let s=fs.readFileSync(file,'utf8');s=s.replace('Final results and the source hash are in `verification.json`.','**936 tests pass across 47 anatomy test files.** A separate final run of all 42 tutor-specific checks also passes. Final results and the source hash are in `verification.json`.');s=s.replace('seven screenshots are retained.','seven screenshots are retained. The desktop, Arabic phone, and dark-mode phone layouts were also visually inspected.');fs.writeFileSync(file,s);
