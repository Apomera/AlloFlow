DRAFT: Combined gates are still running on helper4cbe3774. The final one-line inline visibility:collapse guard and its two focused regressions are pending source release. Final counts, snapshot links and latest replay filename will be finalized afterward.

Round 2 calibration fixed four reproduced bug classes.

- Pending agent replies could keep a deadline-aborted driver waiting forever. The driver now uses its existing abortable promise wrapper; each completed/failed folder file settles abandoned requests, and the existing per-file answer-cache reset remains.
- Generated documents reached their first audits before the detected source language was applied. The existing language helper now runs before both baseline audits.
- Bare-title predicates missed valid attributed titles and could append duplicates. Recognition and explicit replacement now handle attributes consistently across initial, repair, inventory, and requested-fix modes.
- The coverage checker treated native ordered-list labels as missing source numbers. It now uses source-linked semantic marker credits while retaining numeric-fact, custom-CSS, hidden-content, nesting, start/value/reversed, and duplicate-credit protections.

Final maintained verification:

| Selection | Passed | Files | Failed |
| --- | ---: | ---: | ---: |
| Current combined run | Running | Pending | Pending |
| Prior calibration snapshot | 158 | 17 | 0 |

Exact commands, counts, and evidence links are in [checks-summary.json](checks-summary.json). The capability inventory parity check also passed. Source Babel parsing, generated syntax, root/desktop module equality, and pipeline integrity passed in [language-title-companion-verification.json](language-title-companion-verification.json). Its 53 focused tests overlap the maintained selections and are not added to their totals. Final file hashes are recorded separately. The unchanged broader browser recovery suite was not repeated.

The real Chromium/production-transport terminal matrix covers cancellation, a natural public one-minute deadline, and a two-file folder deadline followed by resume/reuse. It verifies pending settlement, durable status, lane release, stale-answer rejection, fresh request IDs, batch atomicity, and preservation of the completed second file. Before the fix, a confirmed silent-client run remained running at 80,965 ms, with three pending requests, 75 seconds after the first request was published. The corrected born-digital matrix passed 3/3; its deadline settled at 60,379 ms with zero pending requests. Final gate observations are retained in bridge-terminal-matrix-post-semantic-final-gate.json. These transport tests use scripted fixture replies, never a live provider.

All earlier attempts remain visible. The first post-fix matrix passed 2/3; one tools/call timed out before any request was published, with insufficient stage evidence to attribute the cause. An unchanged focused retry proved deadline settlement at 60,860 ms, then its tiny 37-character fixture invoked real OCR during resume and exhausted its separate one-minute budget. The test-only fixture was replaced with a >100-character born-digital PDF and matching fixture replies; no production deadline was increased. The first later parity gate passed 72/73, with one adapter case exceeding its existing 60-second budget. A target-only retry lacked the suite-required initialization and failed before exercising the adapter. Initialization plus the unchanged case passed in 10.3 seconds; the final full parity gate above is separate evidence.

The full nine-page Spanish UDHR live agent-bridge pilot completed at 02:29:47.741 UTC, within its original 45-minute wall bound. It used no Gemini key, answered 18 actual current model requests, and retained raw prompts/images/replies and frozen source/options/engine identities. Options were target 95, two fix passes, no polish, auto-continue up to two rounds, tagged PDF requested, PDF/UA validation requested, and no narration. Its contextual score changed from 58 to 96, with zero confirmed axe/EqualAccess failures and a review verdict. One initial operator audit wrongly credited PDF language metadata; the run was cancelled before remediation output and resumed with a corrected source-grounded audit. That correction and the original answer remain explicit evidence, not a hidden successful first attempt.

The official live HTML and report are immutable. Root independently matched all 30 ordered article headings, all 13 ordered lists/32 clauses, the preamble, and all 10,101 non-whitespace source characters including punctuation and reconstructed list labels. Root reviewed four actual screenshots at widths 320/1000: readable text, intact grouping, no visible clipping or overlap. Chromium found no horizontal overflow or page errors and confirmed keyboard-visible skip-link focus. [root-final-source-qa.json](root-final-source-qa.json) and [spanish-final-rendered-facts.json](spanish-final-rendered-facts.json) bind these checks to the actual HTML hash.

The live engine still exhibited the language timing and duplicate-title defects now fixed after the pilot. It also reported 32 missing source tokens; the independent investigation showed every one was a semantic list number. The final guarded helper matched all 1,959 source tokens on the same unchanged artifact in [spanish-content-coverage-after-semantic-marker-final-guards.json](spanish-content-coverage-after-semantic-marker-final-guards.json). This is a targeted deterministic post-fix replay, not a new live verdict or replacement report. No global numeric suppression or scoring-threshold change was made.

Tagged PDF delivery remained blocked by active_content_scan_unavailable; no PDF was produced and PDF/UA was not applicable. The PDF artifact-operation marker helper succeeded before the original run. The pilot, model audits, deterministic checks, and screenshots do not establish blinded human, screen-reader, or PDF/UA acceptance. No scanner security exception was introduced.

Original MCP extraction checks returned 10,113 characters for the native form, 0 for the 1954 image-only scan, 12,067 for Spanish, and 13,989 for the text-bearing 1913 scan. Each reported method:null and mediaImages:0. These are extraction payload observations, not image inventories or proof of OCR: the 1954 source has four page images. Source metadata and frozen text/boundary oracles are retained. Follow-up calibration targets are image-only OCR, native form semantics, a full RTL document, and safe PDF export with independent PDF/UA validation.

Owned pilot servers and browser processes were stopped, local connection credentials removed, and test servers recorded bounded cleanup. Prior round-1 reports, original pilot outputs, and intermediate failed/provisional evidence were preserved.
