const fs = require('node:fs');
const path = require('node:path');
const builder = JSON.parse(fs.readFileSync(path.join(__dirname, 'builder-flow-fix-tests.json'), 'utf8'));
if (!builder.success || builder.numPassedTests !== 14) throw new Error('Final selection-summary regressions are not complete');
require('./verify-enhancement-review.cjs');
const result = JSON.parse(fs.readFileSync(path.join(__dirname, 'enhancement-review-summary.json'), 'utf8'));
const reportPath = path.join(__dirname, 'ENHANCEMENT-REVIEW.md');
let report = fs.readFileSync(reportPath, 'utf8').split('\n## Final verification')[0];
report = report.replace('## Corrections being verified in this pass', '## Corrections completed in this pass');
report = report.replace('Final verification results are recorded below after the changes complete. These are bounded corrections to the current experience; the larger proposals below remain recommendations.', 'The corrections above are implemented and verified. They improve the current experience; the larger proposals below remain recommendations.');
const selectionRow = '| Removing the final selected block leaves its measurement in the UI. | A nonexistent selection still offers Showcase and measurement actions. | Clear only the stale student-selection result when that selection disappears. |';
const dockRow = '| Measuring the ground can replace the dock summary while a student creation stays selected. | Displayed size can describe the ground while Print and Showcase act on the creation. | Use the retained creation for dock count, print envelope, and Showcase caption; preserve the independent measurement inspector. |';
if (!report.includes(dockRow)) report = report.replace(selectionRow, selectionRow + '\n' + dockRow);
const torchNote = 'A further bounded graphics candidate is a quality-dependent budget for active torch lights. Each torch currently creates its own PointLight even in Saver. Retain each torch\'s visible emissive appearance while measuring a limited set of active lights; verify the result before choosing numerical caps.';
if (!report.includes(torchNote)) report = report.replace('Print size, profile fit, topology feedback,', torchNote + '\n\nPrint size, profile fit, topology feedback,');
const base = __dirname.replace(/\\/g, '/');
const link = (name, label = name) => `[${label}](<${base}/${name}>)`;
report += `\n## Final verification\n\n${result.passedTests} tests passed across ${Object.keys(result.testsByFile).length} focused files. Both Geometry World source files and their desktop copies parse and match byte-for-byte. The Print Lab source also matches its desktop copy.\n\nThe actual WebGL comparison held the camera, quality setting, and scene constant and toggled only floor edges: **${result.browser.drawsBefore.toLocaleString('en-US')} → ${result.browser.drawsAfter.toLocaleString('en-US')} draw calls**, saving **${result.browser.savedDraws} (${result.browser.savedPercent}%)**. The scene retained ${result.browser.trianglesUnchanged.toLocaleString('en-US')} rendered triangles. This is a draw-call result for this view, not an FPS claim.\n\nThe browser verified unchanged block/edge buffers and object identities, placement raycast, edit history, and exact STL through Saver/Balanced changes inside and outside Studio. The 31,784-byte pavilion STL also matches the preceding art pass's SHA-256. No browser, console, or shader errors were reported. Root visually inspected the final Saver screenshot.\n\nStandalone STL tests verify default 5 mm and retained 12.5 mm scales, vertex-by-vertex, while preserving normals and the unscaled editable handoff. Selection tests cover removal, partial edits, unrelated measurements, changed selection, and dock/export consistency. Lifecycle tests cover delayed effects, reset, teardown, Studio restoration, dust, Photo rendering, and floor-edge scope.\n\n${link('saver-floor-edges-off.png', 'Final Saver preview')} · ${link('saver-ground-edge-results.json', 'Browser evidence')} · ${link('enhancement-review-summary.json', 'Test counts and source hashes')}\n\nThe earlier ground-edge test report records a test-fixture syntax failure. It is superseded by ${link('ground-edge-tests-final.json', 'the successful final ground-edge run')}; the final aggregation rejects failed or incomplete runs.\n\nSupporting reviews: ${link('VISUAL-STATE-LIFECYCLE.md', 'Visual lifecycle audit')} and the supporting flow/performance notes in this directory.\n`;
const fd = fs.openSync(reportPath, 'r+');
try { fs.writeFileSync(fd, report); fs.ftruncateSync(fd, Buffer.byteLength(report)); } finally { fs.closeSync(fd); }
console.log('Final review written: ' + reportPath);
