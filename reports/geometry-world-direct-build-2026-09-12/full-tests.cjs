const fs=require('fs'),{spawnSync}=require('child_process');const files=fs.readdirSync('tests').filter(f=>/^geometry_world.*\.test\.js$/.test(f)).map(f=>'tests/'+f);const r=spawnSync(process.execPath,['node_modules/vitest/vitest.mjs','run',...files,'--maxWorkers=2','--reporter=json','--outputFile=reports/geometry-world-direct-build-2026-09-12/full-tests.json'],{stdio:'inherit'});process.exitCode=r.status;
