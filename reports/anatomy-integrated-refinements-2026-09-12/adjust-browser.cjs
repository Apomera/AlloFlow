const fs=require('fs');const file=__dirname+'/browser.cjs';let s=fs.readFileSync(file,'utf8');s=s.replace('assert.deepEqual(results.retainedAnswer.after,results.retainedAnswer.before);',"for(const key of ['_retrievalEvidence','_structureConfidence','quizFeedback','_quizAttempts','quizScore'])assert.deepEqual(results.retainedAnswer.after[key],results.retainedAnswer.before[key]);");fs.writeFileSync(file,s);
