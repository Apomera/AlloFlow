// ── Reduced motion CSS (WCAG 2.3.3) — shared across all STEAM Lab tools ──
(function() {
  if (typeof document === 'undefined') return;
  if (document.getElementById('allo-stem-motion-reduce-css')) return;
  var st = document.createElement('style');
  st.id = 'allo-stem-motion-reduce-css';
  st.textContent = '@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important; } }';
  if (document.head) document.head.appendChild(st);
})();

// Scoped Virtual Dissection Lab UI shell.
(function() {
  if (typeof document === 'undefined' || document.getElementById('allo-dissection-ui-css')) return;
  var st = document.createElement('style');
  st.id = 'allo-dissection-ui-css';
  st.textContent = `
[data-dissection-root] {
  --diss-ink: #10233f;
  --diss-muted: #52647d;
  --diss-line: #cbd7e7;
  --diss-blue: #1766d2;
  --diss-teal: #087f78;
  --diss-gold: #f6c65b;
  color: var(--diss-ink);
  min-width: 0;
}
[data-dissection-root] *, [data-dissection-root] *::before, [data-dissection-root] *::after { box-sizing: border-box; }
[data-dissection-root] button, [data-dissection-root] input, [data-dissection-root] summary { font: inherit; }
[data-dissection-root] button { min-height: 2.75rem; }
[data-dissection-root] summary { min-height: 2.75rem; }
[data-dissection-root] [class*="text-[10px]"] { font-size: .75rem !important; line-height: 1.4 !important; }
[data-dissection-root] [class*="text-[11px]"] { font-size: .8125rem !important; line-height: 1.4 !important; }
[data-dissection-root] :where(button, input, textarea, summary, [tabindex]) { scroll-margin-block: 5rem; }
[data-dissection-root] button:focus-visible,
[data-dissection-root] input:focus-visible,
[data-dissection-root] textarea:focus-visible,
[data-dissection-root] summary:focus-visible,
[data-dissection-root] a:focus-visible,
[data-dissection-root] [tabindex]:focus-visible {
  outline: 3px solid #111827;
  outline-offset: 3px;
  box-shadow: 0 0 0 6px #facc15;
}
[data-dissection-root][data-reduced-motion="true"] *,
[data-dissection-root][data-reduced-motion="true"] *::before,
[data-dissection-root][data-reduced-motion="true"] *::after {
  animation-duration: .01ms !important;
  animation-iteration-count: 1 !important;
  transition-duration: .01ms !important;
  scroll-behavior: auto !important;
}
[data-dissection-root][data-text-size="large"] :where(button, input, textarea, summary, p, li, label, span) {
  font-size: max(1em, .9rem) !important;
  line-height: 1.5 !important;
}
[data-dissection-root][data-high-contrast="true"] {
  --diss-ink: #000;
  --diss-muted: #172033;
  --diss-line: #111827;
  --diss-blue: #003c99;
  --diss-teal: #005a55;
}
[data-dissection-root][data-high-contrast="true"] :where(button:not(:disabled), input, textarea, summary, .diss-stage, .diss-tool-panel, .diss-selection-card) {
  border-color: #111827 !important;
}
.diss-skip-link {
  position: absolute;
  z-index: 1000;
  top: .5rem;
  left: .5rem;
  padding: .65rem .8rem;
  border: 2px solid #111827;
  border-radius: .5rem;
  background: #fff;
  color: #003c99;
  font-weight: 900;
  transform: translateY(-180%);
}
.diss-skip-link:focus { transform: translateY(0); }
.diss-skip-return { display: inline-flex; width: max-content; min-height: 2.75rem; align-items: center; padding: .55rem .7rem; border: 1px solid #1d4ed8; border-radius: .55rem; background: #eff6ff; color: #1e3a8a; font-size: .75rem; font-weight: 900; }
.diss-save-status { display: flex; align-items: center; gap: .45rem; min-height: 2.5rem; padding: .55rem .75rem; border: 1px solid #6ee7b7; border-radius: .7rem; background: #ecfdf5; color: #065f46; font-size: .73rem; font-weight: 850; line-height: 1.35; }
.diss-save-status[data-state="warning"] { border-color: #f59e0b; background: #fffbeb; color: #78350f; }
.diss-save-status[data-state="error"] { border-color: #fb7185; background: #fff1f2; color: #881337; }
.diss-save-status__glyph { display: inline-grid; width: 1.25rem; height: 1.25rem; flex: 0 0 1.25rem; place-items: center; border: 1px solid currentColor; border-radius: 999px; font-size: .68rem; font-weight: 950; line-height: 1; }
.diss-reset-confirm { flex: 1 0 100%; display: grid; grid-template-columns: minmax(0, 1fr) auto auto; align-items: center; gap: .45rem; padding: .65rem; border: 2px solid #b91c1c; border-radius: .7rem; background: #fff7ed; color: #7f1d1d; }
.diss-reset-confirm p { margin: 0; font-size: .72rem; line-height: 1.45; }
.diss-reset-confirm button { min-height: 2.75rem !important; margin: 0 !important; border: 1px solid #991b1b !important; background: #fff !important; color: #7f1d1d !important; }
.diss-reset-confirm button[data-confirm="true"] { background: #991b1b !important; color: #fff !important; }
.diss-directory-status { margin: -.1rem 0 .45rem; color: #475569; font-size: .68rem; font-weight: 800; }
.diss-directory-empty { padding: .8rem; border: 1px dashed #94a3b8; border-radius: .6rem; background: #f8fafc; color: #334155; font-size: .75rem; line-height: 1.45; }
.diss-directory-index { display: inline-grid; width: 1.6rem; height: 1.6rem; flex: 0 0 1.6rem; place-items: center; border: 2px solid #f8fafc; border-radius: 999px; background: #1e293b; color: #fff; box-shadow: 0 0 0 2px var(--diss-system-color, #64748b); font-size: .7rem; font-weight: 900; line-height: 1; }
@media (max-width: 620px) { .diss-reset-confirm { grid-template-columns: 1fr 1fr; } .diss-reset-confirm p { grid-column: 1 / -1; } }
.diss-sr-only {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important;
  white-space: nowrap !important;
  border: 0 !important;
}
.diss-pan-controls { display: grid; grid-template-columns: repeat(3, 2.75rem); grid-template-areas: ". up ." "left center right" ". down ."; justify-content: center; gap: .25rem; }
.diss-pan-controls button { min-width: 2.75rem; padding: .25rem !important; }
.diss-pan-controls button:nth-child(1) { grid-area: up; }
.diss-pan-controls button:nth-child(2) { grid-area: left; }
.diss-pan-controls span { grid-area: center; align-self: center; color: #334155; font-size: .72rem; font-weight: 900; text-align: center; }
.diss-pan-controls button:nth-child(4) { grid-area: right; }
.diss-pan-controls button:nth-child(5) { grid-area: down; }
@media (forced-colors: active) {
  [data-dissection-root] :focus-visible { outline: 3px solid Highlight !important; box-shadow: none !important; forced-color-adjust: auto; }
  [data-dissection-root] button, [data-dissection-root] input, [data-dissection-root] textarea, [data-dissection-root] canvas { border: 1px solid CanvasText !important; }
}
.diss-mission {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(15rem, 19rem);
  gap: 1rem;
  padding: 1.15rem;
  overflow: hidden;
  position: relative;
  border: 1px solid rgba(125, 211, 252, .35);
  border-radius: 1.25rem;
  background:
    radial-gradient(circle at 90% 0%, rgba(45, 212, 191, .26), transparent 38%),
    linear-gradient(135deg, #10233f 0%, #123d6c 58%, #0b6967 100%);
  box-shadow: 0 18px 45px rgba(15, 23, 42, .16);
  color: #fff;
}
.diss-mission::after {
  content: "";
  position: absolute;
  width: 12rem;
  height: 12rem;
  right: -5rem;
  bottom: -7rem;
  border: 1.5rem solid rgba(255, 255, 255, .06);
  border-radius: 999px;
  pointer-events: none;
}
.diss-mission__main, .diss-mission__action, .diss-mission__stats { position: relative; z-index: 1; }
.diss-mission__eyebrow {
  margin: 0 0 .3rem;
  color: #99f6e4;
  font-size: .68rem;
  font-weight: 900;
  letter-spacing: .16em;
  text-transform: uppercase;
}
.diss-mission h2 { margin: 0; color: #fff; font-size: clamp(1.25rem, 2.6vw, 1.8rem); line-height: 1.08; font-weight: 900; }
.diss-mission__copy { max-width: 46rem; margin: .45rem 0 0; color: #dbeafe; font-size: .84rem; line-height: 1.5; }
.diss-workflow { display: flex; flex-wrap: wrap; gap: .45rem; margin: .8rem 0 0; padding: 0; list-style: none; }
.diss-workflow li { display: inline-flex; align-items: center; gap: .35rem; padding: .35rem .58rem; border: 1px solid rgba(255,255,255,.2); border-radius: 999px; background: rgba(15,23,42,.24); color: #f8fafc; font-size: .7rem; font-weight: 800; }
.diss-workflow__number { display: inline-grid; width: 1.3rem; height: 1.3rem; place-items: center; border-radius: 999px; background: #ccfbf1; color: #115e59; font-size: .65rem; }
.diss-mission__action { align-self: stretch; padding: .85rem; border: 1px solid rgba(255,255,255,.2); border-radius: 1rem; background: rgba(3, 20, 40, .34); backdrop-filter: blur(8px); }
.diss-mission__action-label { color: #bae6fd; font-size: .66rem; font-weight: 900; letter-spacing: .12em; text-transform: uppercase; }
.diss-mission__action h3 { margin: .25rem 0 .2rem; color: #fff; font-size: 1rem; font-weight: 900; }
.diss-mission__action p { margin: 0; min-height: 2.6em; color: #dbeafe; font-size: .74rem; line-height: 1.35; }
.diss-primary-action {
  width: 100%;
  min-height: 2.75rem !important;
  margin-top: .7rem;
  padding: .65rem .85rem;
  border: 1px solid #fde68a;
  border-radius: .8rem;
  background: linear-gradient(135deg, #fef3c7, #f6c65b);
  box-shadow: 0 8px 20px rgba(246,198,91,.2);
  color: #422006;
  font-size: .78rem;
  font-weight: 900;
  cursor: pointer;
}
.diss-primary-action:hover:not(:disabled) { transform: translateY(-1px); filter: brightness(1.03); }
.diss-primary-action:disabled { cursor: default; opacity: .72; }
.diss-mission__stats { grid-column: 1 / -1; display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .55rem; }
.diss-stat { min-width: 0; padding: .58rem .68rem; border: 1px solid rgba(255,255,255,.18); border-radius: .8rem; background: rgba(15,23,42,.25); }
.diss-stat__label { display: block; color: #a5f3fc; font-size: .62rem; font-weight: 900; letter-spacing: .08em; text-transform: uppercase; }
.diss-stat__value { display: block; margin-top: .15rem; overflow: hidden; color: #fff; font-size: .78rem; font-weight: 850; text-overflow: ellipsis; white-space: nowrap; }
.diss-workspace-mode {
  display: flex;
  min-width: 0;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: .65rem;
  padding: .65rem .75rem;
  border: 1px solid #bfdbfe;
  border-radius: 1rem;
  background: linear-gradient(135deg, #eff6ff, #f0fdfa);
}
.diss-workspace-mode__copy { min-width: min(100%, 18rem); flex: 1 1 28rem; }
.diss-workspace-mode__copy strong { display: block; color: #143b65; font-size: .78rem; }
.diss-workspace-mode__copy span { display: block; margin-top: .16rem; color: #48647f; font-size: .68rem; line-height: 1.4; }
.diss-workspace-mode__choices { display: inline-flex; gap: .25rem; padding: .25rem; border: 1px solid #a9c9ee; border-radius: .75rem; background: #fff; }
.diss-workspace-mode__choices button { min-height: 2.5rem; padding: .48rem .72rem; border: 1px solid transparent; border-radius: .55rem; background: transparent; color: #36506f; font-size: .72rem; font-weight: 900; cursor: pointer; }
.diss-workspace-mode__choices button[aria-pressed="true"] { border-color: #1766d2; background: #1766d2; color: #fff; }
.diss-mode-rail {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: .5rem;
  padding: .45rem;
  border: 1px solid var(--diss-line);
  border-radius: 1rem;
  background: linear-gradient(180deg, #f8fbff, #eef6ff);
}
.diss-route-button {
  min-height: 2.75rem !important;
  border: 1px solid transparent;
  border-radius: .75rem;
  background: transparent;
  color: #36506f;
  font-size: .76rem;
  font-weight: 900;
  cursor: pointer;
}
.diss-route-button[data-active="true"] { border-color: #8ab7ed; background: #fff; box-shadow: 0 5px 16px rgba(23,102,210,.12); color: #0b57b3; }
[data-dissection-root][data-workspace-mode="essentials"] .diss-advanced-only,
[data-dissection-root][data-workspace-mode="essentials"] .diss-shortcuts,
[data-dissection-root][data-workspace-mode="essentials"] .diss-procedure__mode,
[data-dissection-root][data-workspace-mode="essentials"] .diss-procedure__metrics,
[data-dissection-root][data-workspace-mode="essentials"] .diss-advanced-row,
[data-dissection-root][data-workspace-mode="essentials"] .diss-scenario-console,
[data-dissection-root][data-workspace-mode="essentials"] .diss-attempt-comparison,
[data-dissection-root][data-workspace-mode="essentials"] .diss-instructor,
[data-dissection-root][data-workspace-mode="essentials"] .diss-overlay-actions { display: none !important; }
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle layer cross-section"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Specimen condition"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle curated anatomical relationships"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Rendering quality"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle detailed pointer-following"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle specimen-specific surface"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle anatomical depth atlas"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle responsive tissue relief"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle animated direction markers"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle selected-structure focus"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle subtle pointer-responsive"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle live and reference split"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Lab light"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Laboratory light direction"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Generate another deterministic"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Toggle before and after"],
[data-dissection-root][data-workspace-mode="essentials"] #diss-view-tools button[aria-label^="Animation speed"] { display: none !important; }
.diss-objective-list { display: grid; gap: .5rem; margin: 0; padding: 0; list-style: none; }
.diss-objective-item { padding: .6rem .65rem; border: 1px solid #a7f3d0; border-radius: .75rem; background: rgba(255,255,255,.78); }
.diss-objective-item[data-objective-status="demonstrated"] { border-color: #34d399; background: #ecfdf5; }
.diss-objective-item__head { display: flex; align-items: flex-start; justify-content: space-between; gap: .65rem; }
.diss-objective-item__head strong { color: #254b45; font-size: .72rem; line-height: 1.45; }
.diss-objective-item__status { flex: 0 0 auto; color: #47635f; font-size: .62rem; font-weight: 900; }
.diss-objective-item[data-objective-status="demonstrated"] .diss-objective-item__status { color: #047857; }
.diss-objective-item__meter { overflow: hidden; height: .4rem; margin-top: .45rem; border-radius: 999px; background: #d1fae5; }
.diss-objective-item__meter span { display: block; height: 100%; border-radius: inherit; background: #0f8f86; }
.diss-objective-item p { margin: .38rem 0 0; color: #47635f; font-size: .65rem; line-height: 1.45; }
.diss-picker, .diss-layer-stepper { padding: .75rem; border: 1px solid var(--diss-line); border-radius: 1rem; background: #fff; box-shadow: 0 8px 24px rgba(15,23,42,.05); }
.diss-section-heading { display: flex; align-items: end; justify-content: space-between; gap: .75rem; margin-bottom: .55rem; }
.diss-section-heading h3 { margin: 0; color: #193454; font-size: .8rem; font-weight: 900; }
.diss-section-heading p { margin: 0; color: var(--diss-muted); font-size: .68rem; }
.diss-specimen-rail { display: flex; gap: .45rem; overflow-x: auto; padding: .15rem .05rem .35rem; overscroll-behavior-inline: contain; scroll-snap-type: x proximity; scrollbar-width: thin; }
.diss-specimen-tab {
  min-height: 2.75rem !important;
  flex: 0 0 auto;
  scroll-snap-align: start;
  padding: .6rem .85rem;
  border: 1px solid #c6d4e6;
  border-radius: .8rem;
  background: #f8fafc;
  color: #435771;
  font-size: .74rem;
  font-weight: 850;
  cursor: pointer;
}
.diss-specimen-tab[aria-selected="true"] { border-color: #1766d2; background: linear-gradient(135deg, #1766d2, #0b7a77); box-shadow: 0 7px 18px rgba(23,102,210,.2); color: #fff; }
.diss-layer-list { display: grid; grid-template-columns: repeat(auto-fit, minmax(7.25rem, 1fr)); gap: .45rem; }
.diss-layer-button {
  min-height: 3rem !important;
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: .45rem;
  padding: .55rem .65rem;
  border: 1px solid #cbd5e1;
  border-radius: .75rem;
  background: #f8fafc;
  color: #435771;
  text-align: left;
  cursor: pointer;
}
.diss-layer-button[data-state="current"] { border-color: #0f8f86; background: #ecfdf5; box-shadow: inset 0 0 0 1px #5eead4; color: #115e59; }
.diss-layer-button[data-state="revealed"] { border-color: #bbd5f5; background: #eff6ff; color: #174d86; }
.diss-layer-button:disabled { cursor: not-allowed; opacity: .5; }
.diss-layer-index { display: grid; width: 1.4rem; height: 1.4rem; place-items: center; border-radius: 999px; background: #e2e8f0; color: #334155; font-size: .64rem; font-weight: 900; }
.diss-layer-name { overflow: hidden; font-size: .7rem; font-weight: 850; text-overflow: ellipsis; white-space: nowrap; }
.diss-layer-state { color: #078174; font-size: .7rem; font-weight: 900; }
.diss-toolbar { gap: .45rem !important; padding: .5rem !important; border-radius: 1rem !important; background: #f8fafc !important; }
.diss-toolbar > button, .diss-tool-panel > button { min-height: 2.75rem !important; }
.diss-tool-panel { gap: .45rem !important; padding: .65rem !important; }
.diss-light-intensity { display: grid; min-width: min(100%, 15rem); min-height: 2.75rem; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: .18rem .55rem; padding: .38rem .55rem; border: 1px solid #bfdbfe; border-radius: .55rem; background: #fff; color: #1e3a8a; font-size: .75rem; font-weight: 800; }
.diss-light-intensity[data-tone="glare"], .diss-light-intensity[data-tone="dim"] { border-color: #d97706; background: #fffbeb; color: #78350f; }
.diss-light-intensity input { grid-column: 1 / -1; width: 100%; min-height: 1.5rem; margin: 0; accent-color: #1766d2; cursor: pointer; }
.diss-light-intensity output { font-variant-numeric: tabular-nums; font-weight: 900; }
.diss-next-action {
  position: sticky;
  z-index: 30;
  top: .5rem;
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto;
  align-items: center;
  gap: .75rem;
  padding: .72rem;
  border: 1px solid #8fb4dc;
  border-radius: 1rem;
  background: rgba(248, 251, 255, .97);
  box-shadow: 0 12px 30px rgba(15, 23, 42, .14);
  color: #163a63;
  backdrop-filter: blur(12px);
}
.diss-next-action[data-tone="ready"] { border-color: #34a68f; background: rgba(240, 253, 250, .98); color: #115e59; }
.diss-next-action[data-tone="caution"] { border-color: #d9a03b; background: rgba(255, 251, 235, .98); color: #78350f; }
.diss-next-action[data-tone="assessment"] { border-color: #c0841a; background: rgba(255, 251, 235, .98); color: #78350f; }
.diss-next-action__step {
  display: grid;
  width: 3.45rem;
  height: 3.45rem;
  place-items: center;
  border: 1px solid currentColor;
  border-radius: .8rem;
  background: #fff;
  font-size: .72rem;
  font-weight: 900;
  line-height: 1.15;
  text-align: center;
}
.diss-next-action__body { min-width: 0; }
.diss-next-action__eyebrow { display: block; font-size: .68rem; font-weight: 900; letter-spacing: .09em; text-transform: uppercase; }
.diss-next-action h3 { margin: .12rem 0 0; color: currentColor; font-size: .95rem; font-weight: 900; line-height: 1.25; }
.diss-next-action p { margin: .16rem 0 0; color: currentColor; font-size: .76rem; line-height: 1.42; opacity: .88; }
.diss-next-action__meta { display: flex; flex-wrap: wrap; gap: .35rem .7rem; margin-top: .3rem; color: currentColor; font-size: .68rem; font-weight: 800; opacity: .82; }
.diss-next-action__actions { display: grid; min-width: 10.5rem; gap: .35rem; }
.diss-next-action__primary, .diss-next-action__secondary {
  min-height: 2.75rem !important;
  padding: .55rem .72rem;
  border-radius: .65rem;
  font-size: .74rem;
  font-weight: 900;
  cursor: pointer;
}
.diss-next-action__primary { border: 1px solid #0f766e; background: #0f766e; color: #fff; box-shadow: 0 6px 14px rgba(15,118,110,.18); }
.diss-next-action__primary:hover:not(:disabled) { background: #115e59; }
.diss-next-action__secondary { border: 1px solid #94a3b8; background: #fff; color: #334155; }
.diss-next-action__primary:disabled { cursor: not-allowed; opacity: .56; }
.diss-next-action__actions > :only-child { grid-column: 1 / -1; }
.diss-learning-check {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: .65rem 1rem;
  padding: .8rem .9rem;
  border: 1px solid #7aa6cf;
  border-radius: 1rem;
  background: linear-gradient(135deg, #f0f9ff, #f8fbff 58%, #ecfdf5);
  color: #163a63;
  box-shadow: 0 8px 22px rgba(15, 73, 120, .09);
}
.diss-learning-check[data-phase="reflect"] { border-color: #a78bfa; background: linear-gradient(135deg, #f5f3ff, #faf5ff 58%, #f0fdfa); color: #4c1d95; }
.diss-learning-check[data-phase="perform"] { border-color: #34a68f; background: linear-gradient(135deg, #ecfdf5, #f0fdfa); color: #115e59; }
.diss-learning-check__head { min-width: 0; }
.diss-learning-check__eyebrow { margin: 0; font-size: .67rem; font-weight: 900; letter-spacing: .09em; text-transform: uppercase; }
.diss-learning-check h3 { margin: .15rem 0 0; color: currentColor; font-size: .94rem; font-weight: 900; line-height: 1.3; }
.diss-learning-check__prompt { margin: .3rem 0 0; max-width: 64rem; color: currentColor; font-size: .79rem; line-height: 1.5; }
.diss-learning-check__phases { display: flex; flex-wrap: wrap; align-self: start; justify-content: flex-end; gap: .3rem; }
.diss-learning-check__phase { min-width: 4.7rem; padding: .3rem .42rem; border: 1px solid #a9bdd3; border-radius: 999px; background: #fff; color: #405a77; font-size: .64rem; font-weight: 900; text-align: center; }
.diss-learning-check__phase[data-state="current"] { border-color: currentColor; background: #e0f2fe; color: #075985; box-shadow: inset 0 0 0 1px currentColor; }
.diss-learning-check[data-phase="reflect"] .diss-learning-check__phase[data-state="current"] { background: #ede9fe; color: #5b21b6; }
.diss-learning-check__phase[data-state="complete"] { border-color: #5eb69d; background: #ecfdf5; color: #065f46; }
.diss-learning-check__options { display: grid; grid-column: 1 / -1; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .48rem; }
.diss-learning-check__option { min-height: 3.35rem !important; padding: .58rem .65rem; border: 1px solid #8ba7c4; border-radius: .68rem; background: #fff; color: #173f69; font-size: .72rem; font-weight: 800; line-height: 1.38; text-align: left; cursor: pointer; }
.diss-learning-check__option:hover { border-color: #1766d2; background: #eff6ff; }
.diss-learning-check__option[aria-pressed="true"] { border-color: #1766d2; box-shadow: inset 0 0 0 1px #1766d2; }
.diss-learning-check__option[data-result="incorrect"] { border-color: #d97706; background: #fffbeb; color: #78350f; }
.diss-learning-check__option[data-result="correct"] { border-color: #0f766e; background: #ecfdf5; color: #065f46; }
.diss-learning-check__feedback { grid-column: 1 / -1; margin: 0; padding: .48rem .58rem; border-left: .25rem solid currentColor; border-radius: .3rem; background: rgba(255,255,255,.72); color: currentColor; font-size: .72rem; font-weight: 750; line-height: 1.45; }
.diss-learning-check__perform { grid-column: 1 / -1; display: flex; align-items: center; gap: .55rem; padding: .5rem .6rem; border: 1px solid #75c9ae; border-radius: .65rem; background: rgba(255,255,255,.78); color: #065f46; font-size: .74rem; font-weight: 800; line-height: 1.4; }
.diss-learning-check__perform::before { content: "2"; display: grid; width: 1.6rem; height: 1.6rem; flex: 0 0 auto; place-items: center; border-radius: 999px; background: #0f766e; color: #fff; font-size: .7rem; font-weight: 900; }
.diss-pointer-guide { display: flex; min-width: 0; align-items: center; gap: .5rem; margin: 0 0 .5rem; padding: .48rem .58rem; border: 1px solid #89a8c8; border-radius: .7rem; background: #eff6ff; color: #174d86; }
.diss-pointer-guide[data-tone="ready"] { border-color: #34a68f; background: #ecfdf5; color: #065f46; }
.diss-pointer-guide[data-tone="caution"], .diss-pointer-guide[data-tone="blocked"] { border-color: #d9a03b; background: #fffbeb; color: #78350f; }
.diss-pointer-guide[data-tone="assessment"] { border-color: #8b5cf6; background: #f5f3ff; color: #5b21b6; }
.diss-pointer-guide__cursor { display: grid; width: 1.8rem; height: 1.8rem; flex: 0 0 auto; place-items: center; border: 1px solid currentColor; border-radius: .48rem; background: #fff; font-size: 1rem; font-weight: 900; }
.diss-pointer-guide__body { display: grid; min-width: 0; gap: .08rem; }
.diss-pointer-guide__body strong { color: currentColor; font-size: .72rem; font-weight: 900; }
.diss-pointer-guide__body span { color: currentColor; font-size: .68rem; line-height: 1.35; opacity: .9; }
.diss-workflow li[data-state="complete"] { border-color: rgba(153,246,228,.55); background: rgba(13,148,136,.35); }
.diss-workflow li[data-state="current"] { border-color: #fde68a; background: #fef3c7; color: #422006; box-shadow: 0 0 0 2px rgba(246,198,91,.22); }
.diss-workflow li[data-state="upcoming"] { opacity: .72; }
[data-dissection-root][data-assessment-mode="true"] :where(.diss-stage__handoff, .diss-field-monitor, .diss-procedure, .diss-overlay-actions) { display: none !important; }
@media (max-width: 700px) {
  .diss-next-action { position: static; grid-template-columns: auto minmax(0, 1fr); }
  .diss-next-action__actions { grid-column: 1 / -1; grid-template-columns: 1fr 1fr; min-width: 0; }
  .diss-stage__telemetry-grid, .diss-field-readiness__checks { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-learning-check { grid-template-columns: 1fr; }
  .diss-learning-check__phases { justify-content: flex-start; }
  .diss-learning-check__options { grid-template-columns: 1fr; }
}
@media (max-height: 500px) {
  .diss-next-action { position: static; }
}
@media (max-width: 430px) {
  .diss-next-action { grid-template-columns: 1fr; }
  .diss-next-action__step { width: auto; height: auto; min-height: 2.5rem; padding: .35rem .55rem; justify-self: start; }
  .diss-next-action__actions { grid-column: auto; grid-template-columns: 1fr; }
}
.diss-workspace { display: grid !important; grid-template-columns: minmax(0, 1fr) minmax(18rem, 21rem); align-items: start; gap: 1rem !important; }
.diss-primary-column, .diss-sidebar { min-width: 0; }
.diss-primary-column { display: grid; gap: .85rem; }
.diss-stage { overflow: hidden; padding: .8rem; border: 1px solid #becde0; border-radius: 1.15rem; background: linear-gradient(180deg, #ffffff, #f5f9ff); box-shadow: 0 12px 30px rgba(15,23,42,.08); }
.diss-stage__header { display: flex; align-items: center; justify-content: space-between; gap: .8rem; margin-bottom: .65rem; }
.diss-stage__eyebrow { color: #0f766e; font-size: .62rem; font-weight: 900; letter-spacing: .1em; text-transform: uppercase; }
.diss-stage__title { margin: .14rem 0 0; color: #10233f; font-size: .92rem; font-weight: 900; }
.diss-stage__status { max-width: 50%; padding: .42rem .6rem; border: 1px solid #cbdcf0; border-radius: 999px; background: #eef6ff; color: #24517f; font-size: .68rem; font-weight: 850; text-align: right; }
.diss-stage__handoff { display: flex; min-width: 0; flex-wrap: wrap; align-items: center; gap: .28rem .55rem; margin: -.1rem 0 .58rem; padding: .48rem .58rem; border: 1px solid #93b4d7; border-radius: .7rem; background: #eff6ff; color: #174d86; }
.diss-stage__handoff[data-tone="ready"], .diss-stage__handoff[data-tone="complete"] { border-color: #059669; background: #ecfdf5; color: #065f46; }
.diss-stage__handoff[data-tone="caution"] { border-color: #d97706; background: #fffbeb; color: #78350f; }
.diss-stage__handoff[data-tone="restricted"] { border-color: #b91c1c; background: #fff1f2; color: #881337; }
.diss-stage__handoff-glyph { display: inline-grid; width: 1.35rem; height: 1.35rem; flex: 0 0 auto; place-items: center; border: 1px solid currentColor; border-radius: 999px; font-size: .72rem; font-weight: 900; }
.diss-stage__handoff strong { min-width: 0; font-size: .72rem; }
.diss-stage__handoff-tool { color: inherit; font-size: .66rem; font-weight: 800; opacity: .9; }
.diss-stage__handoff-progress { margin-left: auto; font-size: .64rem; font-weight: 900; white-space: nowrap; }
.diss-stage__telemetry { display: grid; gap: .46rem; margin: -.04rem 0 .65rem; padding: .58rem .64rem; border: 1px solid #9fc5d8; border-radius: .82rem; background: linear-gradient(110deg, #f0f9ff 0%, #f8fbff 54%, #ecfeff 100%); box-shadow: inset 0 1px 0 rgba(255,255,255,.9), 0 5px 14px rgba(14,116,144,.08); color: #164e63; }
.diss-stage__telemetry[data-tone="caution"] { border-color: #d6a34b; background: linear-gradient(110deg, #fffbeb, #fff7ed); color: #78350f; }
.diss-stage__telemetry[data-tone="danger"] { border-color: #e08b8b; background: linear-gradient(110deg, #fff1f2, #fff7ed); color: #881337; }
.diss-stage__telemetry-head { display: flex; min-width: 0; align-items: center; justify-content: space-between; gap: .65rem; }
.diss-stage__telemetry-head > div { display: grid; min-width: 0; gap: .08rem; }
.diss-stage__telemetry-kicker { color: currentColor; font-size: .56rem; font-weight: 900; letter-spacing: .1em; opacity: .72; text-transform: uppercase; }
.diss-stage__telemetry-head strong { overflow: hidden; color: currentColor; font-size: .75rem; font-weight: 900; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-phase { flex: 0 1 auto; max-width: 48%; overflow: hidden; color: currentColor; font-size: .62rem; font-weight: 850; text-align: right; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: .38rem; }
.diss-stage__telemetry-metric { min-width: 0; padding: .34rem .42rem; border: 1px solid rgba(125,174,194,.64); border-radius: .55rem; background: rgba(255,255,255,.72); }
.diss-stage__telemetry-metric[data-risk="true"] { border-color: #d6a34b; background: rgba(255,251,235,.9); color: #78350f; }
.diss-stage__telemetry-metric-head { display: flex; align-items: baseline; justify-content: space-between; gap: .3rem; }
.diss-stage__telemetry-metric-head span { overflow: hidden; color: currentColor; font-size: .57rem; font-weight: 850; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-metric-head b { color: currentColor; font-size: .64rem; font-variant-numeric: tabular-nums; }
.diss-stage__telemetry-metric progress { display: block; width: 100%; height: .32rem; margin: .24rem 0 .2rem; accent-color: #0f766e; }
.diss-stage__telemetry-metric[data-risk="true"] progress { accent-color: #d97706; }
.diss-stage__telemetry-metric-note { display: block; overflow: hidden; color: currentColor; font-size: .53rem; font-weight: 750; opacity: .78; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-foot { display: flex; min-width: 0; align-items: baseline; justify-content: space-between; gap: .55rem; color: currentColor; font-size: .58rem; line-height: 1.35; }
.diss-stage__telemetry-foot span { min-width: 0; }
.diss-stage__telemetry-action { overflow: hidden; font-weight: 850; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-priority { flex: 0 1 48%; overflow: hidden; opacity: .78; text-align: right; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-prepare { min-height: 2.25rem; flex: 0 0 auto; padding: .32rem .5rem; border: 1px solid #0f766e; border-radius: .5rem; background: #0f766e; color: #fff; font-size: .6rem; font-weight: 900; cursor: pointer; }
.diss-stage__telemetry-prepare:hover { background: #115e59; }
.diss-stage__telemetry-prepare:disabled { border-color: #94a3b8; background: #e2e8f0; color: #475569; cursor: not-allowed; }
.diss-field-monitor { margin: -.05rem 0 .65rem; border: 1px solid #b8cde3; border-radius: .8rem; background: #f8fbff; color: #294b70; }
.diss-field-monitor__summary { display: grid; min-height: 2.8rem; grid-template-columns: auto auto minmax(0, 1fr); align-items: center; gap: .35rem .6rem; padding: .55rem .65rem; cursor: pointer; list-style-position: inside; }
.diss-field-monitor__summary strong { color: #173f68; font-size: .75rem; font-weight: 900; }
.diss-field-monitor__summary span { padding: .2rem .38rem; border-radius: 999px; background: #e0f2fe; color: #075985; font-size: .7rem; font-weight: 900; }
.diss-field-monitor__summary small { min-width: 0; overflow: hidden; color: #36506f; font-size: .7rem; font-weight: 700; text-overflow: ellipsis; white-space: nowrap; }
.diss-field-monitor[open] > .diss-field-monitor__summary { border-bottom: 1px solid #b8cde3; }
.diss-field-monitor__body { display: grid; gap: .55rem; padding: .6rem; }
.diss-field-monitor__body > .diss-stage__telemetry, .diss-field-monitor__body > .diss-field-readiness { margin: 0; }
.diss-field-monitor__body > .diss-stage__telemetry-detail { padding: .45rem .55rem; border: 1px solid #b8cde3; border-radius: .65rem; background: #fff; }
.diss-field-readiness { display: grid; gap: .45rem; margin: -.05rem 0 .65rem; padding: .52rem .62rem; border: 1px solid #b8cde3; border-radius: .78rem; background: #f8fbff; color: #294b70; }
.diss-field-readiness[data-tone="ready"] { border-color: #75c9ae; background: #ecfdf5; color: #065f46; }
.diss-field-readiness[data-tone="caution"] { border-color: #e4b05a; background: #fffbeb; color: #78350f; }
.diss-field-readiness[data-tone="restricted"] { border-color: #e08b8b; background: #fff1f2; color: #881337; }
.diss-field-readiness__header { display: flex; min-width: 0; align-items: baseline; justify-content: space-between; gap: .6rem; }
.diss-field-readiness__header strong { color: currentColor; font-size: .68rem; font-weight: 900; }
.diss-field-readiness__score { color: currentColor; font-size: .62rem; font-variant-numeric: tabular-nums; font-weight: 900; }
.diss-field-readiness__cue { margin: 0; color: currentColor; font-size: .59rem; line-height: 1.35; opacity: .84; }
.diss-field-readiness__checks { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: .32rem; }
.diss-field-readiness__check { min-width: 0; padding: .3rem .38rem; border: 1px solid #b8cde3; border-radius: .48rem; background: #fff; color: #36506f; }
.diss-field-readiness__check[data-ready="true"] { border-color: #75c9ae; background: #f0fdf4; color: #065f46; }
.diss-field-readiness__check[data-ready="false"] { border-color: #e4b05a; background: #fffbeb; color: #92400e; }
.diss-field-readiness__check-head { display: flex; align-items: center; gap: .24rem; }
.diss-field-readiness__check-head b { display: inline-grid; width: .95rem; height: .95rem; flex: 0 0 auto; place-items: center; border: 1px solid currentColor; border-radius: 999px; font-size: .55rem; }
.diss-field-readiness__check-head span { overflow: hidden; font-size: .57rem; font-weight: 900; text-overflow: ellipsis; white-space: nowrap; }
.diss-field-readiness__check-detail { display: block; margin-top: .16rem; overflow: hidden; font-size: .53rem; line-height: 1.25; opacity: .78; text-overflow: ellipsis; white-space: nowrap; }
.diss-field-readiness__actions { display: flex; flex-wrap: wrap; gap: .32rem; }
.diss-field-readiness__action { min-height: 2rem; padding: .28rem .48rem; border: 1px solid currentColor; border-radius: .45rem; background: transparent; color: currentColor; font-size: .58rem; font-weight: 900; cursor: pointer; }
.diss-field-readiness__action:hover { background: rgba(255,255,255,.62); }
.diss-field-readiness__action:disabled { cursor: not-allowed; opacity: .68; }
.diss-stage__telemetry-detail { border-top: 1px solid rgba(125,174,194,.58); padding-top: .34rem; }
.diss-stage__telemetry-detail summary { display: flex; min-height: 2rem; align-items: center; cursor: pointer; color: currentColor; font-size: .6rem; font-weight: 900; }
.diss-stage__telemetry-detail[open] summary { margin-bottom: .35rem; }
.diss-stage__telemetry-detail-body { display: grid; gap: .35rem; }
.diss-stage__telemetry-detail-body > p { margin: 0; color: currentColor; font-size: .59rem; line-height: 1.4; }
.diss-stage__telemetry-replay { justify-self: start; min-height: 2rem; padding: .28rem .48rem; border: 1px solid currentColor; border-radius: .45rem; background: transparent; color: currentColor; font-size: .58rem; font-weight: 900; cursor: pointer; }
.diss-stage__telemetry-replay:hover { background: rgba(255,255,255,.62); }
.diss-stage__telemetry-deltas { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: .3rem; }
.diss-stage__telemetry-delta { min-width: 0; padding: .3rem .36rem; border: 1px solid rgba(125,174,194,.62); border-radius: .48rem; background: rgba(255,255,255,.68); }
.diss-stage__telemetry-delta[data-direction="improve"] { border-color: #75c9ae; background: #ecfdf5; color: #065f46; }
.diss-stage__telemetry-delta[data-direction="caution"] { border-color: #e4b05a; background: #fffbeb; color: #92400e; }
.diss-stage__telemetry-delta > span, .diss-stage__telemetry-delta > strong, .diss-stage__telemetry-delta > small { display: block; overflow: hidden; color: currentColor; text-overflow: ellipsis; white-space: nowrap; }
.diss-stage__telemetry-delta > span { font-size: .54rem; font-weight: 850; }
.diss-stage__telemetry-delta > strong { margin-top: .12rem; font-size: .62rem; font-variant-numeric: tabular-nums; }
.diss-stage__telemetry-delta > small { margin-top: .08rem; font-size: .53rem; font-weight: 850; opacity: .78; }
.diss-canvas-layout { display: grid; gap: .65rem; }
.diss-canvas-layout[data-split="true"] { grid-template-columns: minmax(0, 1fr) minmax(11rem, .56fr); align-items: stretch; }
.diss-canvas-frame { overflow: hidden; padding: .48rem; border: 1px solid #71849c; border-radius: 1rem; background: linear-gradient(145deg, #64748b 0%, #26364b 16%, #0f172a 78%, #475569 100%); box-shadow: inset 0 0 0 1px rgba(255,255,255,.16), inset 0 -8px 18px rgba(2,6,23,.34), 0 14px 28px rgba(15,23,42,.16); }
.diss-canvas { display: block; width: min(100%, 650px) !important; height: auto; margin: 0 auto; border: 0 !important; border-radius: .75rem !important; box-shadow: 0 14px 32px rgba(2,6,23,.24); }
.diss-split-reference { display: flex; min-width: 0; flex-direction: column; padding: .5rem; border: 1px solid #91a8c4; border-radius: 1rem; background: #0f172a; color: #e2e8f0; }
.diss-split-reference__header { display: flex; align-items: flex-start; justify-content: space-between; gap: .4rem; margin-bottom: .45rem; }
.diss-split-reference__header > div { display: grid; min-width: 0; }
.diss-split-reference__header button { min-height: 2rem !important; padding: .3rem .45rem; border: 1px solid #64748b; border-radius: .45rem; background: #1e293b; color: #e2e8f0; font-size: .58rem; font-weight: 850; cursor: pointer; }
.diss-split-reference img { display: block; width: 100%; min-height: 0; flex: 1 1 auto; border-radius: .7rem; object-fit: contain; background: #020617; }
.diss-split-reference strong { margin-top: .45rem; font-size: .68rem; }
.diss-split-reference span { margin-top: .12rem; color: #bfdbfe; font-size: .61rem; line-height: 1.35; }
.diss-evidence { margin-top: .55rem; border-top: 1px solid #cbdcf0; padding-top: .45rem; }
.diss-evidence summary { cursor: pointer; color: #24517f; font-size: .69rem; font-weight: 850; }
.diss-evidence__capacity { margin: .45rem 0 0; padding: .42rem .55rem; border: 1px solid #bfdbfe; border-radius: .55rem; background: #eff6ff; color: #244b75; font-size: .64rem; font-weight: 750; line-height: 1.4; }
.diss-evidence__capacity[data-full="true"] { border-color: #f59e0b; background: #fffbeb; color: #78350f; }
.diss-evidence__manage { display: flex; flex-wrap: wrap; align-items: center; gap: .4rem; margin-top: .45rem; }
.diss-evidence__manage > button { min-height: 2.75rem; padding: .4rem .58rem; border: 1px solid #b91c1c; border-radius: .5rem; background: #fff; color: #991b1b; font-size: .65rem; font-weight: 850; cursor: pointer; }
.diss-evidence__confirm { flex: 1 0 100%; display: flex; flex-wrap: wrap; align-items: center; gap: .4rem; padding: .5rem; border: 1px solid #f59e0b; border-radius: .55rem; background: #fffbeb; }
.diss-evidence__confirm p { flex: 1 0 100%; margin: 0; color: #78350f; font-size: .65rem; font-weight: 750; line-height: 1.4; }
.diss-evidence__confirm button { min-height: 2.75rem; padding: .4rem .58rem; border: 1px solid #92400e; border-radius: .45rem; background: #fff; color: #78350f; font-size: .65rem; font-weight: 850; cursor: pointer; }
.diss-evidence__confirm button[data-confirm="true"] { background: #991b1b; color: #fff; }
.diss-evidence__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(8rem, 1fr)); gap: .45rem; margin-top: .5rem; }
.diss-evidence__item { min-width: 0; padding: .4rem; border: 1px solid #cbdcf0; border-radius: .65rem; background: #fff; }
.diss-evidence__item[data-reference="true"] { border-color: #0f766e; box-shadow: inset 0 0 0 1px #0f766e; }
.diss-evidence__item img { display: block; width: 100%; aspect-ratio: 5/6; border-radius: .45rem; background: #0f172a; object-fit: cover; }
.diss-evidence__item strong, .diss-evidence__item span { display: block; margin-top: .28rem; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.diss-evidence__item strong { color: #10233f; font-size: .68rem; }
.diss-evidence__item span { color: #526b87; font-size: .64rem; }
.diss-evidence__item .diss-evidence__confirm-copy { margin: .35rem 0 0; color: #78350f; font-size: .64rem; font-weight: 750; line-height: 1.4; }
.diss-evidence__actions { display: flex; flex-wrap: wrap; gap: .3rem; margin-top: .35rem; }
.diss-evidence__actions button { min-height: 2.75rem !important; padding: .35rem .46rem; border: 1px solid #91a8c4; border-radius: .45rem; background: #f8fafc; color: #24517f; font-size: .65rem; font-weight: 850; cursor: pointer; }
.diss-evidence__actions button[data-danger="true"] { border-color: #fda4af; color: #9f1239; }
.diss-canvas:fullscreen { width: auto !important; height: min(96vh, 1100px) !important; height: min(96dvh, 1100px) !important; max-width: 96vw !important; margin: auto; border-radius: 0 !important; background: #0f172a !important; object-fit: contain; }
.diss-fullscreen-dock { display: none; }
.diss-canvas-frame:fullscreen, .diss-canvas-frame[data-allo-fullscreen-active="true"] { display: grid; width: 100vw; width: 100dvw; height: 100vh; height: 100dvh; grid-template-rows: auto minmax(0, 1fr); gap: .5rem; padding: max(.65rem, env(safe-area-inset-top)) max(.65rem, env(safe-area-inset-right)) max(.65rem, env(safe-area-inset-bottom)) max(.65rem, env(safe-area-inset-left)); border: 0; border-radius: 0; background: #07111f; }
.diss-canvas-frame:fullscreen .diss-fullscreen-dock, .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock { display: flex; min-width: 0; flex-wrap: wrap; align-items: center; gap: .4rem; padding: .5rem; border: 1px solid #71849c; border-radius: .7rem; background: #10233f; color: #fff; }
.diss-canvas-frame:fullscreen .diss-fullscreen-dock button, .diss-canvas-frame:fullscreen .diss-fullscreen-dock select, .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock button, .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock select { min-height: 2.5rem !important; padding: .42rem .58rem; border: 1px solid #9fb3ca; border-radius: .5rem; background: #fff; color: #174d86; font-size: .7rem; font-weight: 850; }
.diss-canvas-frame:fullscreen .diss-fullscreen-dock label, .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock label { display: inline-flex; min-height: 2.5rem; align-items: center; gap: .4rem; padding: .35rem .5rem; border: 1px solid #71849c; border-radius: .5rem; background: #172f51; color: #fff; font-size: .7rem; font-weight: 800; }
.diss-canvas-frame:fullscreen .diss-fullscreen-dock input[type="range"], .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock input[type="range"] { width: min(19vw, 9rem); accent-color: #5eead4; }
.diss-canvas-frame:fullscreen .diss-fullscreen-dock__exit, .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock__exit { border-color: #fecaca !important; background: #991b1b !important; color: #fff !important; }
.diss-fullscreen-dock__handoff { margin-left: auto; padding: .38rem .5rem; border: 1px solid #5eead4; border-radius: .5rem; background: #0f3f46; color: #ccfbf1; font-size: .68rem; font-weight: 850; }
.diss-canvas-frame:fullscreen .diss-canvas, .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-canvas { width: auto !important; max-width: 100% !important; height: 100% !important; min-height: 0; object-fit: contain; }
.diss-canvas-frame:fullscreen .diss-fullscreen-dock,
.diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock {
  max-height: min(34vh, 18rem);
  overflow-y: auto;
  align-content: flex-start;
}
@media (max-width: 640px) {
  .diss-canvas-frame:fullscreen,
  .diss-canvas-frame[data-allo-fullscreen-active="true"] {
    grid-template-rows: minmax(0, 36vh) minmax(0, 1fr);
    gap: .35rem;
    padding: .4rem;
  }
  .diss-canvas-frame:fullscreen .diss-fullscreen-dock,
  .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock {
    gap: .3rem;
    padding: .4rem;
    max-height: 36vh;
  }
  .diss-canvas-frame:fullscreen .diss-fullscreen-dock button,
  .diss-canvas-frame:fullscreen .diss-fullscreen-dock select,
  .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock button,
  .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock select {
    flex: 1 1 calc(50% - .3rem);
    min-width: 7.25rem;
  }
  .diss-canvas-frame:fullscreen .diss-fullscreen-dock label,
  .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock label {
    flex: 1 1 100%;
    justify-content: space-between;
  }
  .diss-canvas-frame:fullscreen .diss-fullscreen-dock input[type="range"],
  .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock input[type="range"] {
    width: min(42vw, 12rem);
  }
  .diss-canvas-frame:fullscreen .diss-fullscreen-dock__handoff,
  .diss-canvas-frame[data-allo-fullscreen-active="true"] .diss-fullscreen-dock__handoff {
    flex: 1 1 100%;
    margin-left: 0;
  }
}
@media (max-width: 460px) {
  .diss-stage__telemetry-head { align-items: flex-start; flex-direction: column; gap: .18rem; }
  .diss-stage__telemetry-phase { max-width: 100%; text-align: left; }
  .diss-stage__telemetry-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-stage__telemetry-deltas { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-field-readiness__checks { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-stage__telemetry-foot { align-items: flex-start; flex-direction: column; gap: .2rem; }
  .diss-stage__telemetry-priority { flex-basis: auto; text-align: left; }
}
.diss-stage__live { margin: .55rem .1rem 0; color: #36506f; font-size: .72rem; line-height: 1.4; }
.diss-stage__live[data-tool-status="true"] { display: flex; align-items: flex-start; gap: .38rem; padding: .42rem .55rem; border: 1px solid #b8cde3; border-radius: .6rem; background: #f8fbff; color: #294b70; font-weight: 750; }
.diss-stage__live[data-tool-status="true"]::before { content: ""; width: .58rem; height: .58rem; flex: 0 0 auto; margin-top: .28rem; border-radius: 999px; background: #64748b; box-shadow: 0 0 0 3px rgba(100,116,139,.16); }
.diss-stage__live[data-tool-status="true"][data-tone="ready"]::before,
.diss-stage__live[data-tool-status="true"][data-tone="complete"]::before { background: #0f766e; box-shadow: 0 0 0 3px rgba(15,118,110,.16); }
.diss-stage__live[data-tool-status="true"][data-tone="working"]::before { background: #2563eb; box-shadow: 0 0 0 3px rgba(37,99,235,.16); }
.diss-stage__live[data-tool-status="true"][data-tone="caution"]::before { background: #d97706; box-shadow: 0 0 0 3px rgba(217,119,6,.16); }
.diss-stage__live[data-tool-status="true"][data-tone="restricted"]::before { background: #b91c1c; box-shadow: 0 0 0 3px rgba(185,28,28,.16); }
.diss-shortcuts { margin-top: .45rem; border: 1px solid #cbdcf0; border-radius: .7rem; background: #f8fbff; color: #36506f; }
.diss-shortcuts summary { min-height: 2.25rem; display: flex; align-items: center; cursor: pointer; padding: .45rem .65rem; color: #24517f; font-size: .68rem; font-weight: 850; }
.diss-shortcuts__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(8.5rem, 1fr)); gap: .35rem; padding: 0 .65rem .65rem; }
.diss-shortcuts__grid span { padding: .34rem .45rem; border: 1px solid #dbe6f3; border-radius: .5rem; background: #fff; font-size: .62rem; line-height: 1.35; }
.diss-shortcuts kbd { display: inline-block; min-width: 1.35rem; margin-right: .25rem; padding: .1rem .28rem; border: 1px solid #9fb3ca; border-bottom-width: 2px; border-radius: .28rem; background: #eef6ff; color: #174d86; font: 800 .58rem/1.2 Inter, system-ui; text-align: center; }
.diss-procedure { margin-top: .7rem; padding: .75rem; border: 1px solid #99c7be; border-radius: .9rem; background: linear-gradient(145deg, #f0fdfa, #f8fbff); }
.diss-procedure__summary { display: flex; min-height: 2.5rem; align-items: center; justify-content: space-between; gap: .65rem; cursor: pointer; color: #134e4a; font-size: .78rem; font-weight: 900; }
.diss-procedure__summary span { color: #426568; font-size: .72rem; font-weight: 800; text-align: right; }
.diss-procedure:not([open]) { padding-block: .45rem; }
.diss-procedure[open] > .diss-procedure__summary { margin-bottom: .65rem; padding-bottom: .55rem; border-bottom: 1px solid #b9d8d2; }
.diss-procedure__header { display: flex; align-items: flex-start; justify-content: space-between; gap: .75rem; }
.diss-procedure__header h4 { margin: 0; color: #134e4a; font-size: .82rem; font-weight: 900; }
.diss-procedure__header p { margin: .2rem 0 0; color: #426568; font-size: .7rem; line-height: 1.4; }
.diss-protocol { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .38rem; margin: .58rem 0 0; }
.diss-protocol div { min-width: 0; padding: .48rem .55rem; border: 1px solid #b9d8d2; border-radius: .6rem; background: rgba(255,255,255,.82); }
.diss-protocol dt { color: #47706c; font-size: .56rem; font-weight: 900; letter-spacing: .055em; text-transform: uppercase; }
.diss-protocol dd { margin: .14rem 0 0; color: #173f3c; font-size: .64rem; font-weight: 750; line-height: 1.35; }
.diss-protocol div[data-aligned="false"] { border-color: #d97706; background: #fffbeb; }
.diss-protocol__align { min-height: 2rem !important; margin-top: .35rem; padding: .28rem .48rem; border: 1px solid #0f766e; border-radius: .45rem; background: #0f766e; color: #fff; font-size: .6rem; font-weight: 900; cursor: pointer; }
.diss-procedure__mode { display: flex; flex: 0 0 auto; gap: .25rem; padding: .2rem; border: 1px solid #b9d8d2; border-radius: .65rem; background: #fff; }
.diss-procedure__mode button { min-height: 2rem !important; padding: .35rem .55rem; border: 0; border-radius: .45rem; background: transparent; color: #466a68; font-size: .66rem; font-weight: 850; cursor: pointer; }
.diss-procedure__mode button[aria-pressed="true"] { background: #0f766e; color: #fff; }
.diss-instruments { display: grid; grid-template-columns: repeat(auto-fit, minmax(5.2rem, 1fr)); gap: .35rem; margin-top: .65rem; }
.diss-instrument { min-height: 3.25rem !important; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: .12rem; padding: .35rem .25rem; border: 1px solid #b8ccc9; border-radius: .65rem; background: #fff; color: #315b58; font-size: .67rem; font-weight: 850; cursor: pointer; }
.diss-instrument[aria-checked="true"] { border-color: #0f766e; background: #0f766e; box-shadow: 0 5px 14px rgba(15,118,110,.18); color: #fff; }
.diss-instrument[data-next="true"]:not([aria-checked="true"]) { border-style: dashed; border-color: #0f766e; background: #f0fdfa; box-shadow: 0 0 0 2px rgba(15,118,110,.08); }
.diss-instrument[data-readiness="waiting"]:not([aria-checked="true"]) { border-color: #d97706; background: #fffbeb; color: #78350f; }
.diss-instrument[data-readiness="complete"]:not([aria-checked="true"]) { border-color: #2563eb; background: #eff6ff; color: #1d4ed8; }
.diss-instrument:disabled { opacity: 1; border-color: #b91c1c; background: #fff1f2; color: #881337; cursor: not-allowed; }
.diss-instrument:disabled .diss-instrument__state { color: #9f1239; }
.diss-instrument__state { font-size: .65rem; font-weight: 900; letter-spacing: .035em; opacity: 1; text-transform: uppercase; }
.diss-instrument__hint { color: #52647d; font-size: .56rem; font-weight: 850; letter-spacing: .04em; line-height: 1.15; text-transform: uppercase; }
.diss-instrument[aria-checked="true"] .diss-instrument__hint { color: #ccfbf1; }
.diss-instrument:disabled .diss-instrument__hint { color: #9f1239; }
.diss-instrument[data-readiness="ready"] .diss-instrument__state { color: #047857; }
.diss-instrument[data-readiness="complete"] .diss-instrument__state { color: #2563eb; }
.diss-instrument[aria-checked="true"] .diss-instrument__state { color: #fff; opacity: 1; }
.diss-active-tool { display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: .55rem; margin-top: .65rem; padding: .58rem .65rem; border: 1px solid #99c7be; border-radius: .7rem; background: #fff; color: #315b58; }
.diss-active-tool__icon { display: grid; width: 2rem; height: 2rem; place-items: center; border-radius: 999px; background: #ccfbf1; color: #0f766e; font-size: 1rem; font-weight: 900; }
.diss-active-tool strong, .diss-active-tool span { display: block; }
.diss-active-tool strong { color: #134e4a; font-size: .72rem; }
.diss-active-tool span { margin-top: .1rem; color: #496966; font-size: .64rem; line-height: 1.35; }
.diss-active-tool__badge { padding: .25rem .42rem; border-radius: 999px; background: #fef3c7; color: #92400e; font-size: .56rem; font-weight: 900; letter-spacing: .045em; text-transform: uppercase; white-space: nowrap; }
.diss-active-tool[data-readiness="ready"] .diss-active-tool__badge { background: #d1fae5; color: #065f46; }
.diss-active-tool[data-readiness="complete"] .diss-active-tool__badge { background: #dbeafe; color: #1d4ed8; }
.diss-readiness { display: grid; gap: .45rem; margin-top: .5rem; padding: .58rem .65rem; border: 1px solid #94a3b8; border-radius: .7rem; background: #f8fafc; color: #334155; }
.diss-readiness[data-tone="ready"], .diss-readiness[data-tone="complete"] { border-color: #059669; background: #ecfdf5; color: #065f46; }
.diss-readiness[data-tone="caution"] { border-color: #d97706; background: #fffbeb; color: #78350f; }
.diss-readiness[data-tone="restricted"] { border-color: #b91c1c; background: #fff1f2; color: #881337; }
.diss-readiness__header { display: flex; min-width: 0; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: .35rem; }
.diss-readiness__header strong { font-size: .72rem; }
.diss-readiness__score { font-variant-numeric: tabular-nums; font-size: .66rem; font-weight: 900; }
.diss-readiness__meter { overflow: hidden; height: .55rem; border: 1px solid currentColor; border-radius: 999px; background: rgba(255,255,255,.82); }
.diss-readiness__meter > span { display: block; height: 100%; border-radius: inherit; background: currentColor; transition: width .2s ease; }
[data-dissection-root][data-reduced-motion="true"] .diss-readiness__meter > span { transition: none; }
.diss-readiness__checks { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: .3rem; }
.diss-readiness__check { display: flex; min-width: 0; align-items: center; gap: .25rem; padding: .3rem .35rem; border: 1px solid currentColor; border-radius: .45rem; background: rgba(255,255,255,.72); font-size: .6rem; font-weight: 800; }
.diss-readiness__check span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.diss-readiness__cue { margin: 0; font-size: .65rem; line-height: 1.4; }
@media (max-width: 520px) { .diss-readiness__checks { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
.diss-procedure__steps { display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); gap: .28rem; margin-top: .6rem; }
.diss-procedure__step { min-width: 0; padding: .3rem .2rem; border-radius: .45rem; background: #dfeae9; color: #3f5f5c; font-size: .61rem; font-weight: 800; text-align: center; }
.diss-procedure__step[data-complete="true"] { background: #ccfbf1; color: #0f766e; }
.diss-procedure__step[data-current="true"] { box-shadow: inset 0 0 0 2px #f59e0b; background: #fffbeb; color: #92400e; }
.diss-procedure__controls { display: flex; flex-wrap: wrap; align-items: center; gap: .45rem; margin-top: .65rem; }
.diss-procedure__controls button { min-height: 2.4rem !important; padding: .5rem .7rem; border: 1px solid #9dbab6; border-radius: .65rem; background: #fff; color: #315b58; font-size: .7rem; font-weight: 850; cursor: pointer; }
.diss-procedure__controls .diss-procedure__next { border-color: #0f766e; background: #0f766e; color: #fff; }
.diss-procedure__controls button:disabled { cursor: not-allowed; opacity: .55; }
.diss-calibration { display: grid; grid-template-columns: minmax(7rem, auto) minmax(9rem, 1fr) auto; align-items: center; gap: .42rem .65rem; margin-top: .65rem; padding: .62rem .68rem; border: 1px solid #9fcac2; border-radius: .72rem; background: linear-gradient(135deg, rgba(240,253,250,.96), rgba(255,255,255,.94)); }
.diss-calibration[data-tone="adjust"] { border-color: #f59e0b; background: linear-gradient(135deg, #fffbeb, #fff); }
.diss-calibration[data-tone="caution"] { border-color: #fb7185; background: linear-gradient(135deg, #fff1f2, #fff); }
.diss-calibration__title { min-width: 0; }
.diss-calibration__title strong, .diss-calibration__title span { display: block; }
.diss-calibration__title strong { color: #134e4a; font-size: .69rem; }
.diss-calibration__title span { margin-top: .12rem; color: #526b68; font-size: .58rem; line-height: 1.35; }
.diss-calibration input[type="range"] { width: 100%; min-height: 2rem; accent-color: #0f766e; cursor: pointer; }
.diss-calibration output { min-width: 3.15rem; padding: .3rem .42rem; border-radius: .48rem; background: #0f766e; color: #fff; font-size: .7rem; font-weight: 900; text-align: center; }
.diss-calibration[data-tone="adjust"] output { background: #b45309; }
.diss-calibration[data-tone="caution"] output { background: #be123c; }
.diss-calibration__status { grid-column: 1 / -1; display: flex; align-items: center; gap: .38rem; margin: 0; color: #315b58; font-size: .62rem; line-height: 1.4; }
.diss-calibration__status b { padding: .17rem .34rem; border-radius: 999px; background: #ccfbf1; color: #0f766e; font-size: .56rem; letter-spacing: .04em; text-transform: uppercase; white-space: nowrap; }
.diss-calibration[data-tone="adjust"] .diss-calibration__status b { background: #fef3c7; color: #92400e; }
.diss-calibration[data-tone="caution"] .diss-calibration__status b { background: #ffe4e6; color: #9f1239; }
@media (max-width: 520px) { .diss-calibration { grid-template-columns: 1fr auto; } .diss-calibration__title { grid-column: 1 / -1; } }
.diss-procedure__feedback { margin: .55rem 0 0; padding: .5rem .6rem; border-left: 4px solid #0f766e; border-radius: .45rem; background: #fff; color: #355754; font-size: .7rem; line-height: 1.4; }
.diss-procedure__feedback[data-tone="caution"] { border-left-color: #f59e0b; background: #fffbeb; color: #7c3f10; }
.diss-procedure__feedback[data-tone="working"] { border-left-color: #2563eb; background: #eff6ff; color: #1e40af; }
.diss-procedure__notice { margin: .5rem 0 0; color: #5a6d73; font-size: .64rem; line-height: 1.4; }
.diss-procedure__metrics { display: grid; grid-template-columns: repeat(auto-fit, minmax(6.2rem, 1fr)); gap: .35rem; margin-top: .6rem; }
.diss-procedure__metric { min-width: 0; padding: .45rem; border: 1px solid #c3d8d4; border-radius: .55rem; background: rgba(255,255,255,.84); }
.diss-procedure__metric span { display: block; color: #5b7471; font-size: .58rem; font-weight: 850; letter-spacing: .04em; text-transform: uppercase; }
.diss-procedure__metric strong { display: block; margin-top: .12rem; overflow: hidden; color: #134e4a; font-size: .76rem; text-overflow: ellipsis; white-space: nowrap; }
.diss-procedure__timeline { display: flex; flex-wrap: wrap; gap: .3rem; margin-top: .55rem; }
.diss-procedure__timeline > span { display: inline-flex; align-items: center; gap: .25rem; padding: .28rem .45rem; border: 1px solid #b9d8d2; border-radius: 999px; background: #fff; color: #3c625f; font-size: .62rem; font-weight: 800; }
.diss-procedure__timeline > span[data-tone="success"] { border-color: #6ee7b7; background: #ecfdf5; color: #065f46; }
.diss-procedure__timeline > span[data-tone="caution"] { border-color: #fbbf24; background: #fffbeb; color: #92400e; }
.diss-procedure__timeline > button { display: inline-flex; align-items: center; gap: .25rem; padding: .28rem .45rem; border: 1px solid #b9d8d2; border-radius: 999px; background: #fff; color: #3c625f; font: inherit; font-size: .62rem; font-weight: 800; text-align: left; cursor: pointer; transition: border-color .16s ease, background .16s ease, box-shadow .16s ease, transform .16s ease; }
.diss-procedure__timeline > button:hover { transform: translateY(-1px); border-color: #0f766e; }
.diss-procedure__timeline > button[data-tone="success"] { border-color: #6ee7b7; background: #ecfdf5; color: #065f46; }
.diss-procedure__timeline > button[data-tone="caution"] { border-color: #fbbf24; background: #fffbeb; color: #92400e; }
.diss-procedure__timeline > button[data-selected="true"] { border-color: #0f766e; background: #ccfbf1; color: #134e4a; box-shadow: 0 0 0 2px rgba(15,118,110,.16); }
.diss-procedure__timeline > button:focus-visible { outline: 3px solid rgba(14,116,144,.48); outline-offset: 2px; }
.diss-procedure__timeline-index { color: currentColor; font-variant-numeric: tabular-nums; }
.diss-procedure__timeline-label { font-weight: 900; letter-spacing: .02em; text-transform: uppercase; }
.diss-procedure__timeline-outcome { color: currentColor; font-size: .56rem; font-weight: 850; opacity: .86; }
.diss-advanced-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(9.5rem, 1fr)); gap: .4rem; margin-top: .6rem; }
.diss-advanced-card { min-width: 0; padding: .55rem .6rem; border: 1px solid #b9d8d2; border-radius: .65rem; background: rgba(255,255,255,.88); color: #315b58; }
.diss-advanced-card strong { display: block; color: #134e4a; font-size: .7rem; }
.diss-tissue-monitor[data-tone="stable"] { border-color: #5eead4; background: linear-gradient(145deg, #f0fdfa, #ffffff); }
.diss-tissue-monitor[data-tone="caution"] { border-color: #fbbf24; background: linear-gradient(145deg, #fffbeb, #ffffff); }
.diss-tissue-monitor[data-tone="danger"] { border-color: #fb7185; background: linear-gradient(145deg, #fff1f2, #ffffff); }
.diss-tissue-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .35rem; margin-top: .45rem; }
.diss-tissue-meter { min-width: 0; }
.diss-tissue-meter span { display: flex; justify-content: space-between; gap: .25rem; color: #52647d; font-size: .56rem; font-weight: 800; }
.diss-tissue-meter progress { display: block; width: 100%; height: .42rem; margin-top: .18rem; accent-color: #0f8f86; }
.diss-tissue-meter[data-risk="true"] progress { accent-color: #e11d48; }
.diss-tissue-consequence { margin: .45rem 0 0; padding: .4rem .48rem; border-left: 3px solid #0f8f86; border-radius: .35rem; background: rgba(255,255,255,.76); color: #36506f; font-size: .61rem; line-height: 1.4; }
.diss-advanced-card p { margin: .2rem 0 0; font-size: .66rem; line-height: 1.4; }
.diss-field-quality { border-color: #7dd3fc; background: linear-gradient(145deg, #f0f9ff, #fff); }
.diss-field-quality[data-tone="developing"] { border-color: #fbbf24; background: linear-gradient(145deg, #fffbeb, #fff); }
.diss-field-quality[data-tone="restricted"] { border-color: #fb7185; background: linear-gradient(145deg, #fff1f2, #fff); }
.diss-field-quality progress { display: block; width: 100%; height: .55rem; margin-top: .45rem; accent-color: #0284c7; }
.diss-field-quality[data-tone="developing"] progress { accent-color: #d97706; }
.diss-field-quality[data-tone="restricted"] progress { accent-color: #e11d48; }
.diss-field-quality__metrics { display: flex; flex-wrap: wrap; gap: .25rem; margin-top: .4rem; }
.diss-field-quality__metrics span { padding: .2rem .34rem; border-radius: 999px; background: rgba(255,255,255,.82); color: #36506f; font-size: .56rem; font-weight: 800; }
.diss-optics { border-color: #a78bfa; background: linear-gradient(145deg, #f5f3ff, #fff); }
.diss-optics[data-tone="usable"] { border-color: #38bdf8; background: linear-gradient(145deg, #f0f9ff, #fff); }
.diss-optics[data-tone="soft"] { border-color: #fbbf24; background: linear-gradient(145deg, #fffbeb, #fff); }
.diss-optics[data-tone="unresolved"] { border-color: #fb7185; background: linear-gradient(145deg, #fff1f2, #fff); }
.diss-optics__meter { display: grid; grid-template-columns: 1fr auto; align-items: center; gap: .4rem; margin-top: .42rem; }
.diss-optics__meter progress { width: 100%; height: .55rem; accent-color: #7c3aed; }
.diss-optics__meter b { color: #5b21b6; font-size: .64rem; }
.diss-optics__controls { display: flex; flex-wrap: wrap; gap: .28rem; margin-top: .42rem; }
.diss-optics__controls span { align-self: center; color: #64748b; font-size: .56rem; font-weight: 900; text-transform: uppercase; }
.diss-optics__controls button { min-height: 2rem !important; padding: .3rem .45rem; border: 1px solid #c4b5fd; border-radius: .48rem; background: #fff; color: #5b21b6; font-size: .59rem; font-weight: 850; cursor: pointer; }
.diss-optics__controls button[aria-pressed="true"] { border-color: #6d28d9; background: #6d28d9; color: #fff; }
.diss-optics__plane-key { display: flex; flex-wrap: wrap; align-items: center; gap: .34rem .62rem; margin-top: .48rem; padding-top: .42rem; border-top: 1px solid #ddd6fe; color: #334155; font-size: .69rem; font-weight: 800; }
.diss-optics__plane-key > strong { color: #4c1d95; font-size: .69rem; }
.diss-optics__plane-item { display: inline-flex; align-items: center; gap: .28rem; white-space: nowrap; }
.diss-focus-symbol { display: inline-block; width: .78rem; height: .78rem; border: 2px solid #6d28d9; border-radius: 50%; background: transparent; }
.diss-focus-symbol[data-focus="resolved"] { box-shadow: inset 0 0 0 2px #fff; }
.diss-focus-symbol[data-focus="soft"] { border-style: dashed; }
.diss-focus-symbol[data-focus="unresolved"] { border-style: dotted; box-shadow: 0 0 0 2px #c4b5fd; }
[data-dissection-root][data-high-contrast="true"] .diss-optics__plane-key { border-color: #000; color: #000; }
[data-dissection-root][data-high-contrast="true"] .diss-optics__plane-key > strong { color: #000; }
[data-dissection-root][data-high-contrast="true"] .diss-focus-symbol { border-color: #000; box-shadow: none; }
.diss-advanced-card button { margin-top: .42rem; padding: .38rem .5rem; border: 1px solid #8b5cf6; border-radius: .5rem; background: #fff; color: #5b21b6; font-size: .62rem; font-weight: 850; cursor: pointer; }
.diss-scenario-console { grid-column: span 2; min-width: 0; padding: .7rem; border: 2px solid #7c3aed; border-radius: .75rem; background: linear-gradient(135deg, rgba(245,243,255,.96), rgba(237,233,254,.92)); color: #3b1d68; }
.diss-scenario-console__header { display: flex; min-width: 0; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: .45rem; }
.diss-scenario-console__header strong { color: #4c1d95; font-size: .78rem; }
.diss-scenario-console__state { display: inline-flex; min-height: 1.65rem; align-items: center; padding: .2rem .45rem; border: 1px solid #7c3aed; border-radius: 999px; background: #fff; color: #4c1d95; font-size: .64rem; font-weight: 900; }
.diss-scenario-console__state[data-state="complete"] { border-color: #047857; color: #065f46; }
.diss-scenario-console__state[data-state="expired"] { border-color: #b91c1c; color: #991b1b; }
.diss-scenario-console__objective { margin: .35rem 0; font-size: .7rem; line-height: 1.45; }
.diss-scenario-console__progress { overflow: hidden; height: .65rem; border: 1px solid #8b5cf6; border-radius: 999px; background: #fff; }
.diss-scenario-console__progress > span { display: block; height: 100%; border-radius: inherit; background: #6d28d9; transition: width .25s ease; }
[data-dissection-root][data-reduced-motion="true"] .diss-scenario-console__progress > span { transition: none; }
.diss-scenario-console__detail { margin: .38rem 0 0; color: #4c1d95; font-size: .68rem; line-height: 1.45; }
.diss-scenario-console__controls { display: flex; min-width: 0; flex-wrap: wrap; align-items: end; gap: .45rem; margin-top: .5rem; }
.diss-scenario-console__controls label { display: grid; min-width: min(100%, 13rem); flex: 1 1 13rem; gap: .2rem; color: #4c1d95; font-size: .64rem; font-weight: 850; }
.diss-scenario-console__controls select { min-height: 2.75rem; width: 100%; border: 1px solid #7c3aed; border-radius: .5rem; background: #fff; color: #3b1d68; font: inherit; }
.diss-scenario-console__controls button { min-height: 2.75rem; margin: 0; padding: .45rem .65rem; border: 1px solid #6d28d9; border-radius: .5rem; background: #fff; color: #4c1d95; font-size: .68rem; font-weight: 900; }
.diss-scenario-console__timer { font-variant-numeric: tabular-nums; }
@media (max-width: 520px) { .diss-scenario-console { grid-column: 1 / -1; } }
.diss-attempt-comparison { grid-column: 1 / -1; border-color: #c4b5fd; background: linear-gradient(135deg, rgba(245,243,255,.96), rgba(253,242,248,.9)); }
.diss-attempt-comparison__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(7.2rem, 1fr)); gap: .35rem; margin-top: .45rem; }
.diss-attempt-comparison__metric { padding: .4rem .45rem; border: 1px solid #ddd6fe; border-radius: .5rem; background: rgba(255,255,255,.78); }
.diss-attempt-comparison__metric span { display: block; color: #6d5b8c; font-size: .57rem; font-weight: 850; text-transform: uppercase; }
.diss-attempt-comparison__metric strong { display: block; margin-top: .12rem; color: #4c1d95; font-size: .72rem; }
.diss-attempt-comparison__legend { display: flex; flex-wrap: wrap; gap: .3rem .55rem; margin-top: .45rem; color: #4c1d95; font-size: .61rem; font-weight: 850; }
.diss-attempt-comparison__legend-item { display: inline-flex; align-items: center; gap: .28rem; }
.diss-attempt-comparison__swatch { display: inline-block; width: 1.25rem; height: .35rem; border-top: 3px solid #22d3ee; border-radius: 999px; }
.diss-attempt-comparison__swatch[data-style="saved"] { border-top-style: dashed; border-top-color: #ec4899; }
.diss-attempt-comparison__swatch[data-style="caution"] { width: .7rem; height: .7rem; border: 2px solid #f59e0b; border-radius: 50%; transform: rotate(45deg); }
.diss-attempt-comparison__scrub { display: grid; grid-template-columns: auto minmax(0, 1fr) auto auto auto; align-items: center; gap: .35rem .5rem; margin-top: .55rem; padding-top: .45rem; border-top: 1px solid #ddd6fe; color: #4c1d95; }
.diss-attempt-comparison__scrub label { font-size: .62rem; font-weight: 900; white-space: nowrap; }
.diss-attempt-comparison__scrub input[type="range"] { width: 100%; min-width: 8rem; accent-color: #7c3aed; cursor: pointer; }
.diss-attempt-comparison__scrub output { min-width: 4.2rem; color: #4c1d95; font-size: .62rem; font-variant-numeric: tabular-nums; font-weight: 900; text-align: right; }
.diss-attempt-comparison__scrub button { min-height: 2.2rem; margin: 0; padding: .3rem .5rem; border: 1px solid #8b5cf6; border-radius: .45rem; background: #fff; color: #5b21b6; font-size: .62rem; font-weight: 900; cursor: pointer; }
.diss-attempt-comparison__scrub button:hover { background: #f5f3ff; }
@media (max-width: 620px) { .diss-attempt-comparison__scrub { grid-template-columns: minmax(0, 1fr) auto; } .diss-attempt-comparison__scrub label { grid-column: 1 / -1; } .diss-attempt-comparison__scrub input[type="range"] { min-width: 0; } }
.diss-debrief { margin-top: .55rem; padding: .6rem; border: 1px solid #93c5fd; border-radius: .65rem; background: #eff6ff; color: #24486e; font-size: .69rem; line-height: 1.45; }
.diss-debrief strong { color: #123b67; }
.diss-instructor { margin-top: .55rem; border-top: 1px solid #b9d8d2; padding-top: .45rem; }
.diss-instructor summary { cursor: pointer; color: #315b58; font-size: .68rem; font-weight: 850; }
.diss-zoom-bar { min-height: 2.7rem; margin-top: .6rem !important; background: #edf4fb !important; }
.diss-zoom-bar button { min-width: 2.5rem; min-height: 2.25rem !important; }
.diss-depth-key { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: .42rem .72rem; margin-top: .48rem; padding: .48rem .62rem; border: 1px solid #b8cbe1; border-radius: .72rem; background: linear-gradient(135deg, #f8fbff, #eef6ff); color: #243b59; }
.diss-depth-key__title { color: #123b67; font-size: .67rem; font-weight: 900; letter-spacing: .06em; text-transform: uppercase; }
.diss-depth-key__item { display: inline-flex; align-items: center; gap: .3rem; min-width: 0; font-size: .65rem; font-weight: 800; }
.diss-depth-key__count { color: #526b87; font-variant-numeric: tabular-nums; }
.diss-depth-symbol { position: relative; display: inline-block; width: .8rem; height: .8rem; flex: 0 0 auto; border: 2px solid #1d4ed8; background: #fff; }
.diss-depth-symbol[data-depth="surface"] { border-radius: 50%; }
.diss-depth-symbol[data-depth="structure"] { width: .7rem; height: .7rem; margin: .05rem; transform: rotate(45deg); border-color: #a16207; }
.diss-depth-symbol[data-depth="deep"] { border-radius: 50%; border-color: #6d28d9; box-shadow: inset 0 0 0 2px #fff, inset 0 0 0 3px #6d28d9; }
[data-dissection-root][data-high-contrast="true"] .diss-depth-key { border-width: 2px; background: #fff; color: #000; }
[data-dissection-root][data-high-contrast="true"] .diss-depth-key__title, [data-dissection-root][data-high-contrast="true"] .diss-depth-key__count { color: #000; }
[data-dissection-root][data-high-contrast="true"] .diss-depth-symbol { border-color: #000 !important; box-shadow: inset 0 0 0 2px #fff, inset 0 0 0 3px #000; }
.diss-overlay-actions { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .5rem; }
.diss-overlay-actions > summary { grid-column: 1 / -1; min-height: 2.5rem; padding: .5rem .65rem; border: 1px solid #cbd5e1; border-radius: .7rem; background: #f8fafc; color: #334155; cursor: pointer; font-size: .75rem; font-weight: 900; }
.diss-overlay-actions:not([open]) { display: block; }
.diss-overlay-actions button { min-height: 2.55rem !important; margin-top: .55rem !important; }
.diss-system-playback { grid-column: 1 / -1; display: grid; grid-template-columns: minmax(0, 1fr) auto auto; align-items: center; gap: .4rem; margin-top: .1rem; padding: .5rem; border: 1px solid #99c7be; border-radius: .7rem; background: linear-gradient(135deg, #f0fdfa, #eff6ff); }
.diss-system-playback__status { display: grid; min-width: 0; }
.diss-system-playback__status strong { color: #134e4a; font-size: .7rem; }
.diss-system-playback__status span { overflow: hidden; color: #496966; font-size: .61rem; text-overflow: ellipsis; white-space: nowrap; }
.diss-system-playback button { min-height: 2rem !important; margin-top: 0 !important; padding: .35rem .5rem; border: 1px solid #0f766e; border-radius: .5rem; background: #fff; color: #0f766e; font-size: .64rem; font-weight: 850; cursor: pointer; }
.diss-system-playback button[aria-pressed="true"] { background: #0f766e; color: #fff; }
.diss-system-playback button:disabled { cursor: not-allowed; opacity: .48; }
.diss-living-function { grid-column: 1 / -1; display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: .55rem; padding: .65rem; border: 1px solid #93c5fd; border-radius: .75rem; background: linear-gradient(135deg, #eff6ff, #f0fdfa); }
.diss-living-function[data-active="true"] { border-color: #14b8a6; box-shadow: inset 0 0 0 1px rgba(20,184,166,.2); }
.diss-living-function__copy { min-width: 0; }
.diss-living-function__copy strong, .diss-living-function__copy span { display: block; }
.diss-living-function__copy strong { color: #164e63; font-size: .71rem; }
.diss-living-function__copy span { margin-top: .15rem; color: #496966; font-size: .61rem; line-height: 1.4; }
.diss-living-function__badge { display: inline-flex !important; width: max-content; margin-top: .3rem !important; padding: .18rem .34rem; border-radius: 999px; background: #dbeafe; color: #1e40af !important; font-size: .54rem !important; font-weight: 900; letter-spacing: .04em; text-transform: uppercase; }
.diss-living-function[data-active="true"] .diss-living-function__badge { background: #ccfbf1; color: #115e59 !important; }
.diss-living-function__controls { display: flex; flex-wrap: wrap; justify-content: flex-end; gap: .3rem; }
.diss-living-function__controls button { min-height: 2.1rem !important; padding: .35rem .5rem; border: 1px solid #0f766e; border-radius: .5rem; background: #fff; color: #0f766e; font-size: .61rem; font-weight: 850; cursor: pointer; }
.diss-living-function__controls button[aria-pressed="true"] { background: #0f766e; color: #fff; }
.diss-living-function__controls button:disabled { cursor: not-allowed; opacity: .48; }
@media (max-width: 620px) { .diss-living-function { grid-template-columns: 1fr; } .diss-living-function__controls { justify-content: flex-start; } }
.diss-sidebar { display: grid; width: 100% !important; gap: .8rem; }
.diss-selection-card { border-color: #9db4cf !important; box-shadow: 0 10px 26px rgba(15,23,42,.07); }
.diss-selection-nav button { min-width: 2.75rem !important; min-height: 2.75rem !important; }
.diss-selection-position { display: inline-block; margin-top: .12rem; color: #64748b; font-size: .7rem; font-weight: 800; }
.diss-observe-first { margin: .65rem 0; padding: .65rem .7rem; border: 1px solid #5eead4; border-left: 4px solid #0f766e; border-radius: .65rem; background: #f0fdfa; }
.diss-observe-first strong { display: block; color: #115e59; font-size: .75rem; }
.diss-observe-first p { margin: .22rem 0 0; color: #315f5b; font-size: .73rem; line-height: 1.45; }
.diss-selection-reference { margin: .65rem 0; border: 1px solid #bfdbfe; background: #f8fbff; }
.diss-selection-reference.diss-disclosure > summary { min-height: 2.75rem; color: #174d86; font-size: .75rem; }
.diss-selection-reference__intro { margin: 0 0 .55rem; color: #48647f; font-size: .72rem; line-height: 1.45; }
.diss-selection-reference__layer { display: flex; flex-wrap: wrap; gap: .25rem; margin-top: .45rem; color: #52677f; font-size: .7rem; }
.diss-selection-summary { margin: .65rem 0; padding: .7rem; border-left: 4px solid #f59e0b; border-radius: .65rem; background: #fff7ed; }
.diss-selection-summary__label { display: block; color: #9a4d08; font-size: .62rem; font-weight: 900; letter-spacing: .09em; text-transform: uppercase; }
.diss-selection-summary p { margin: .2rem 0 0; color: #4a3b2b; font-size: .78rem; line-height: 1.48; }
.diss-evidence-note { width: 100%; min-height: 5rem; margin-top: .4rem; padding: .65rem; border: 1px solid #aebed2; border-radius: .65rem; background: #fff; color: #24364d; resize: vertical; }
.diss-evidence-coach { margin-top: .65rem; padding: .65rem; border: 1px solid #93c5fd; border-radius: .7rem; background: rgba(255,255,255,.72); }
.diss-evidence-coach__head { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: .6rem; color: #173f6d; font-size: .75rem; }
.diss-evidence-coach__score { flex: 0 0 auto; padding: .16rem .38rem; border-radius: 999px; background: #dbeafe; color: #1e40af; font-size: .7rem; font-weight: 900; }
.diss-evidence-coach[data-ready="true"] { border-color: #6ee7b7; background: rgba(236,253,245,.82); }
.diss-evidence-coach[data-ready="true"] .diss-evidence-coach__score { background: #d1fae5; color: #047857; }
.diss-evidence-starters { display: flex; flex-wrap: wrap; gap: .35rem; margin-top: .5rem; }
.diss-evidence-starters button { min-height: 2.75rem !important; padding: .45rem .6rem; border: 1px solid #93c5fd; border-radius: .5rem; background: #fff; color: #174d86; font-size: .72rem; font-weight: 850; cursor: pointer; }
.diss-evidence-starters button:hover { border-color: #2563eb; background: #eff6ff; }
.diss-confidence-scale { min-width: 0; margin: .65rem 0 0; padding: 0; border: 0; }
.diss-confidence-scale__label { width: 100%; margin: 0; padding: 0; color: #173f6d; font-size: .75rem; font-weight: 900; line-height: 1.4; }
.diss-confidence-scale__choices { display: flex; flex-wrap: wrap; gap: .4rem; margin-top: .4rem; }
.diss-confidence-cue { margin: .45rem 0 0; color: #315d88; font-size: .75rem; line-height: 1.45; }
.diss-confidence-cue strong { color: #173f6d; }
.diss-confidence-scale__option { display: flex; min-height: 2.75rem !important; align-items: center; gap: .4rem; padding: .45rem .6rem; border: 1px solid #93c5fd; border-radius: .55rem; background: #fff; color: #174d86; font-size: .72rem; font-weight: 850; cursor: pointer; }
.diss-confidence-scale__option:hover { border-color: #2563eb; background: #eff6ff; }
.diss-confidence-scale__option[data-selected="true"] { border-color: #1d4ed8; background: #1d4ed8; color: #fff; }
.diss-confidence-scale__option input { width: 1rem; height: 1rem; margin: 0; accent-color: #1d4ed8; }
.diss-evidence-meter { overflow: hidden; height: .38rem; margin-top: .55rem; border-radius: 999px; background: #dbeafe; }
.diss-evidence-meter span { display: block; height: 100%; border-radius: inherit; background: #2563eb; transition: width .2s ease; }
.diss-evidence-coach[data-ready="true"] .diss-evidence-meter span { background: #059669; }
.diss-evidence-checks { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .3rem .55rem; margin: .55rem 0 0; padding: 0; list-style: none; }
.diss-evidence-checks li { color: #52677f; font-size: .72rem; line-height: 1.4; }
.diss-evidence-checks li[data-complete="true"] { color: #047857; font-weight: 800; }
.diss-evidence-coach__cue { margin: .5rem 0 0; color: #244b75; font-size: .75rem; font-weight: 750; line-height: 1.45; }
.diss-evidence-coach__note { margin: .3rem 0 0; color: #64748b; font-size: .7rem; line-height: 1.4; }
.diss-countercheck { margin-top: .65rem; padding: 0; border: 1px dashed #7c3aed; border-radius: .7rem; background: #f5f3ff; color: #4c1d95; }
.diss-countercheck.diss-disclosure > summary { min-height: 2.75rem; color: #4c1d95; }
.diss-countercheck p { margin: .25rem 0 .55rem; color: #5b4a78; font-size: .72rem; line-height: 1.45; }
.diss-countercheck button { min-height: 2.75rem !important; padding: .45rem .6rem; border: 1px solid #8b5cf6; border-radius: .5rem; background: #fff; color: #6d28d9; font-size: .72rem; font-weight: 850; cursor: pointer; }
.diss-countercheck button:hover { background: #ede9fe; }
.diss-countercheck button:disabled { cursor: default; opacity: .68; }
.diss-countercheck__status { margin: .45rem 0 0 !important; color: #5b21b6 !important; font-weight: 750; }
@media (forced-colors: active) { .diss-confidence-scale__option[data-selected="true"] { border: 3px solid Highlight; background: Canvas; color: CanvasText; } }
@media (max-width: 460px) { .diss-evidence-checks { grid-template-columns: 1fr; } }
.diss-science-scope { margin: .55rem 0 0; padding: .55rem .65rem; border: 1px solid #bfdbfe; border-radius: .65rem; background: #eff6ff; color: #294f79; font-size: .75rem; line-height: 1.45; }
.diss-structure-list button { min-height: 2.7rem !important; }
.diss-study-card { min-width: 0; }
.diss-flashcard { width: 100%; min-height: 7rem !important; border: 2px solid #c4b5fd; cursor: pointer; }
.diss-disclosure { overflow: hidden; border-radius: .9rem; }
.diss-disclosure > summary { min-height: 2.65rem; display: flex; align-items: center; gap: .4rem; padding: .7rem .8rem; cursor: pointer; font-size: .72rem; font-weight: 900; list-style: none; }
.diss-disclosure > summary::-webkit-details-marker { display: none; }
.diss-disclosure > summary::after { content: "＋"; margin-left: auto; font-size: 1rem; }
.diss-disclosure[open] > summary::after { content: "−"; }
.diss-disclosure__body { padding: 0 .8rem .8rem; }
.diss-disclosure--inset { padding: 0 .8rem .8rem; }
.diss-disclosure--inset > summary { margin: 0 -.8rem .7rem; }
/* Keep critical workflow copy comfortably readable across compact layouts. */
.diss-stage__telemetry-kicker,
.diss-stage__telemetry-phase,
.diss-stage__telemetry-metric-head span,
.diss-stage__telemetry-metric-head b,
.diss-stage__telemetry-metric-note,
.diss-stage__telemetry-foot,
.diss-stage__telemetry-detail summary,
.diss-stage__telemetry-detail-body > p,
.diss-stage__telemetry-replay,
.diss-stage__telemetry-delta > span,
.diss-stage__telemetry-delta > strong,
.diss-stage__telemetry-delta > small,
.diss-field-readiness__score,
.diss-field-readiness__cue,
.diss-field-readiness__check-head span,
.diss-field-readiness__check-detail,
.diss-field-readiness__action,
.diss-instrument__hint,
.diss-readiness__score,
.diss-readiness__check,
.diss-readiness__cue,
.diss-procedure__step,
.diss-procedure__controls button,
.diss-procedure__feedback,
.diss-procedure__notice,
.diss-procedure__metric span,
.diss-procedure__metric strong,
.diss-procedure__timeline > span,
.diss-procedure__timeline > button,
.diss-procedure__timeline-outcome {
  font-size: .75rem;
}
.diss-stage__telemetry-metric-head span,
.diss-stage__telemetry-metric-note,
.diss-field-readiness__check-head span,
.diss-field-readiness__check-detail {
  line-height: 1.35;
  overflow: visible;
  text-overflow: clip;
  white-space: normal;
}

@media print {
  [data-dissection-root] { color: #000 !important; background: #fff !important; }
  [data-dissection-root] .diss-mode-rail, [data-dissection-root] .diss-picker, [data-dissection-root] .diss-layer-stepper,
  [data-dissection-root] .diss-toolbar, [data-dissection-root] .diss-tool-panel, [data-dissection-root] .diss-zoom-bar,
  [data-dissection-root] .diss-overlay-actions, [data-dissection-root] .diss-primary-action, [data-dissection-root] .diss-next-action { display: none !important; }
  [data-dissection-root] .diss-mission { display: block; padding: .6rem; border: 1px solid #777; background: #fff !important; box-shadow: none; color: #000 !important; }
  [data-dissection-root] .diss-mission * { color: #000 !important; }
  [data-dissection-root] .diss-workspace { grid-template-columns: 58% 40% !important; gap: 2% !important; }
  [data-dissection-root] .diss-stage, [data-dissection-root] .diss-selection-card, [data-dissection-root] .diss-disclosure { break-inside: avoid; box-shadow: none !important; }
  [data-dissection-root] .diss-selection-reference > summary { display: none !important; }
  [data-dissection-root] .diss-selection-reference > .diss-disclosure__body { display: block !important; }
}
@media (max-width: 980px) {
  .diss-mission { grid-template-columns: 1fr; }
  .diss-mission__stats { grid-column: auto; }
  .diss-workspace { grid-template-columns: 1fr; }
}
@media (max-width: 640px) {
  .diss-mission { padding: .9rem; border-radius: 1rem; }
  .diss-mission__stats { grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .35rem; }
  .diss-stat { padding: .48rem .45rem; }
  .diss-mode-rail { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .35rem; }
  .diss-section-heading { align-items: start; flex-direction: column; }
  .diss-layer-list { display: flex; overflow-x: auto; padding-bottom: .25rem; scroll-snap-type: x proximity; }
  .diss-layer-button { min-width: 8.6rem; scroll-snap-align: start; }
  .diss-stage { padding: .55rem; }
  .diss-stage__header { align-items: flex-start; flex-direction: column; }
  .diss-stage__status { max-width: 100%; text-align: left; }
  .diss-depth-key { justify-content: flex-start; }
  .diss-depth-key__title { flex: 1 0 100%; }
  .diss-procedure__header { flex-direction: column; }
  .diss-protocol { grid-template-columns: 1fr; }
  .diss-instruments { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-canvas-layout[data-split="true"] { grid-template-columns: 1fr; }
  .diss-procedure__steps { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  .diss-procedure__metrics { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-overlay-actions { grid-template-columns: 1fr; }
}
@media (max-width: 430px) {
  .diss-mission__stats { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .diss-mission__stats .diss-stat:last-child { grid-column: 1 / -1; }
  .diss-stat__value { overflow: visible; text-overflow: clip; white-space: normal; }
  .diss-field-monitor__summary { grid-template-columns: 1fr auto; }
  .diss-field-monitor__summary small { grid-column: 1 / -1; white-space: normal; }
}
`;
  if (document.head) document.head.appendChild(st);
})();

// stem_tool_dissection.js — Virtual Dissection Lab
// Extracted from stem_tool_science.js as a standalone module
// Uses window.StemLab.registerTool() plugin architecture

  // ═══ 🔬 dissection (dissection) ═══
  // ── Dissection Lab Audio System ──
  var DISSECTION_SAVE_SCHEMA_VERSION = 21;
  var _disAC = null;
  var _disSaveQueue = Object.create(null);
  var _disSaveAccess = Object.create(null);
  var _disSaveOwnerCounter = 0;
  var _disSaveOwnerBySetter = typeof WeakMap === 'function' ? new WeakMap() : null;
  var _disSaveStatus = {
    state: 'ready',
    message: 'Progress saves locally on this device.',
    at: 0
  };
  var _disSaveStatusByKey = Object.create(null);
  function dissectionSaveOwnerFor(identity) {
    var identityType = typeof identity;
    if (!_disSaveOwnerBySetter || !identity || (identityType !== 'function' && identityType !== 'object')) return 'default';
    var owner = _disSaveOwnerBySetter.get(identity);
    if (!owner) {
      owner = 'lab-' + (++_disSaveOwnerCounter);
      _disSaveOwnerBySetter.set(identity, owner);
    }
    return owner;
  }
  function dissectionSaveSlot(key, owner) {
    return String(owner || 'default') + '::' + String(key || '');
  }
  function clearDissectionCanvasTimer(canvas, name) {
    if (!canvas || !name || !canvas._dissTimers) return;
    var timers = canvas._dissTimers;
    if (!Object.prototype.hasOwnProperty.call(timers, name)) return;
    var timer = timers[name];
    delete timers[name];
    if (timer != null) clearTimeout(timer);
    if (!Object.keys(timers).length) canvas._dissTimers = null;
  }
  function scheduleDissectionCanvasTimer(canvas, name, callback, delay) {
    if (!canvas || !name || typeof callback !== 'function') return null;
    clearDissectionCanvasTimer(canvas, name);
    var timers = canvas._dissTimers || Object.create(null);
    canvas._dissTimers = timers;
    var timer = setTimeout(function () {
      var liveTimers = canvas._dissTimers;
      if (!liveTimers || liveTimers[name] !== timer) return;
      delete liveTimers[name];
      if (!Object.keys(liveTimers).length) canvas._dissTimers = null;
      callback();
    }, Math.max(0, Number(delay) || 0));
    timers[name] = timer;
    return timer;
  }
  function clearAllDissectionCanvasTimers(canvas) {
    if (!canvas || !canvas._dissTimers) return;
    Object.keys(canvas._dissTimers).forEach(function (name) {
      clearDissectionCanvasTimer(canvas, name);
    });
    canvas._dissTimers = null;
  }
  function cancelDissectionLayerTimers(canvas) {
    clearDissectionCanvasTimer(canvas, 'layerPeel');
    clearDissectionCanvasTimer(canvas, 'layerTransition');
  }
  function isPlainDissectionRecord(value) {
    return !!value && typeof value === 'object' && !Array.isArray(value);
  }
  function dissectionUndoFieldsForAction(action) {
    return ({
      inspect: ['inspected'],
      scalpel: ['incisionStarted', 'incisionPath', 'incisionMetrics', 'incisionSamples'],
      scissors: ['incisionExtended', 'extensionPath', 'extensionMetrics', 'extensionSamples'],
      forceps: ['retracted', 'forcepsPoint', 'forcepsDragMetrics'],
      pin: ['pins', 'pinDragMetrics'],
      dropper: ['surfaceCleared', 'dropperPoint', 'dropperDragMetrics'],
      wick: ['wickPoint', 'fieldWicked', 'wickDragMetrics'],
      probe: ['probed', 'probedOrganId', 'probeDragMetrics']
    })[action] || [];
  }
  function dissectionUndoDefaultValue(field) {
    if (['inspected', 'incisionStarted', 'incisionExtended', 'retracted', 'surfaceCleared', 'fieldWicked', 'probed'].indexOf(field) >= 0) return false;
    if (field === 'pins') return [];
    return null;
  }
  function unsafeSavedScalarTree(value, prefix, depth) {
    depth = Number(depth) || 0;
    if (value == null || typeof value === 'string' || typeof value === 'boolean') return null;
    if (typeof value === 'number') return isFinite(value) ? null : prefix;
    if (depth >= 6) return prefix;
    if (Array.isArray(value)) {
      if (value.length > 512) return prefix;
      for (var arrayIndex = 0; arrayIndex < value.length; arrayIndex++) {
        var unsafeArrayValue = unsafeSavedScalarTree(value[arrayIndex], prefix, depth + 1);
        if (unsafeArrayValue) return unsafeArrayValue;
      }
      return null;
    }
    if (!isPlainDissectionRecord(value)) return prefix;
    var keys = Object.keys(value);
    if (keys.length > 128) return prefix;
    for (var keyIndex = 0; keyIndex < keys.length; keyIndex++) {
      var unsafeObjectValue = unsafeSavedScalarTree(value[keys[keyIndex]], prefix, depth + 1);
      if (unsafeObjectValue) return unsafeObjectValue;
    }
    return null;
  }
  function unsafeSavedPoint(value, prefix) {
    if (value == null) return null;
    if (!isPlainDissectionRecord(value)) return prefix;
    if (value.x != null && (typeof value.x !== 'number' || !isFinite(value.x))) return prefix;
    if (value.y != null && (typeof value.y !== 'number' || !isFinite(value.y))) return prefix;
    return unsafeSavedScalarTree(value, prefix, 0);
  }
  function unsafeSavedPointArray(value, prefix) {
    if (value == null) return null;
    if (!Array.isArray(value)) return prefix;
    for (var pointIndex = 0; pointIndex < value.length; pointIndex++) {
      var unsafePoint = unsafeSavedPoint(value[pointIndex], prefix);
      if (unsafePoint) return unsafePoint;
    }
    return null;
  }
  function unsafeSavedTissueSnapshot(value, prefix) {
    if (value == null) return null;
    if (!isPlainDissectionRecord(value)) return prefix;
    var numericFields = ['moisture', 'tension', 'exposure', 'trauma', 'clarity', 'stability', 'risk', 'salineDrops', 'lastUpdatedAt'];
    for (var numericIndex = 0; numericIndex < numericFields.length; numericIndex++) {
      var numericField = numericFields[numericIndex];
      if (value[numericField] != null && (typeof value[numericField] !== 'number' || !isFinite(value[numericField]))) return prefix + '.' + numericField;
    }
    var stringFields = ['variantId', 'variantLabel', 'lastAction'];
    for (var stringIndex = 0; stringIndex < stringFields.length; stringIndex++) {
      var stringField = stringFields[stringIndex];
      if (value[stringField] != null && typeof value[stringField] !== 'string') return prefix + '.' + stringField;
    }
    if (value.consequences != null && !Array.isArray(value.consequences)) return prefix + '.consequences';
    if (Array.isArray(value.consequences)) {
      for (var consequenceIndex = 0; consequenceIndex < value.consequences.length; consequenceIndex++) {
        var consequence = value.consequences[consequenceIndex];
        if (!isPlainDissectionRecord(consequence)) return prefix + '.consequences';
        var consequenceTextFields = ['action', 'message', 'label', 'tone'];
        for (var textIndex = 0; textIndex < consequenceTextFields.length; textIndex++) {
          var consequenceField = consequenceTextFields[textIndex];
          if (consequence[consequenceField] != null && typeof consequence[consequenceField] !== 'string') return prefix + '.consequences.' + consequenceField;
        }
      }
    }
    return unsafeSavedScalarTree(value, prefix, 0);
  }
  function unsafeSavedProcedureField(record, prefix) {
    var knownActions = ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'dropper', 'wick', 'probe'];
    var history = Array.isArray(record.history) ? record.history : [];
    if (history.some(function (action) { return typeof action !== 'string' || knownActions.indexOf(action) < 0; })) return prefix + '.history';
    if (record.learningChecks != null) {
      if (!isPlainDissectionRecord(record.learningChecks)) return prefix + '.learningChecks';
      var learningActions = Object.keys(record.learningChecks);
      var allowedLearningFields = ['predictionChoice', 'predictionCorrect', 'predictionAttempts', 'predictionFeedback', 'predictionAnsweredAt', 'reflectionChoice', 'reflectionCorrect', 'reflectionAttempts', 'reflectionFeedback', 'reflectionAnsweredAt', 'legacy'];
      for (var learningIndex = 0; learningIndex < learningActions.length; learningIndex++) {
        var learningAction = learningActions[learningIndex];
        if (knownActions.indexOf(learningAction) < 0) return prefix + '.learningChecks.' + learningAction;
        var learningEntry = record.learningChecks[learningAction];
        if (!isPlainDissectionRecord(learningEntry)) return prefix + '.learningChecks.' + learningAction;
        var learningFields = Object.keys(learningEntry);
        for (var learningFieldIndex = 0; learningFieldIndex < learningFields.length; learningFieldIndex++) {
          var learningField = learningFields[learningFieldIndex];
          var learningValue = learningEntry[learningField];
          if (allowedLearningFields.indexOf(learningField) < 0) return prefix + '.learningChecks.' + learningAction + '.' + learningField;
          if (/Choice$|Feedback$/.test(learningField) && learningValue != null && typeof learningValue !== 'string') return prefix + '.learningChecks.' + learningAction + '.' + learningField;
          if (/Correct$/.test(learningField) && learningValue != null && typeof learningValue !== 'boolean') return prefix + '.learningChecks.' + learningAction + '.' + learningField;
          if (/Attempts$/.test(learningField) && learningValue != null && (typeof learningValue !== 'number' || !isFinite(learningValue) || learningValue < 0 || Math.floor(learningValue) !== learningValue)) return prefix + '.learningChecks.' + learningAction + '.' + learningField;
          if (/AnsweredAt$/.test(learningField) && learningValue != null && (typeof learningValue !== 'number' || !isFinite(learningValue) || learningValue < 0)) return prefix + '.learningChecks.' + learningAction + '.' + learningField;
          if (learningField === 'legacy' && learningValue != null && typeof learningValue !== 'boolean') return prefix + '.learningChecks.' + learningAction + '.legacy';
        }
      }
    }
    var actionStringFields = ['inputType', 'assistance', 'action', 'label', 'outcome', 'outcomeDetail', 'tone'];
    var actionLog = Array.isArray(record.actionLog) ? record.actionLog : [];
    for (var actionIndex = 0; actionIndex < actionLog.length; actionIndex++) {
      var actionEntry = actionLog[actionIndex];
      for (var stringIndex = 0; stringIndex < actionStringFields.length; stringIndex++) {
        var stringField = actionStringFields[stringIndex];
        if (actionEntry[stringField] != null && typeof actionEntry[stringField] !== 'string') return prefix + '.actionLog.' + stringField;
      }
      if (actionEntry.action != null && knownActions.indexOf(actionEntry.action) < 0) return prefix + '.actionLog.action';
      if (actionEntry.at != null && (typeof actionEntry.at !== 'number' || !isFinite(actionEntry.at))) return prefix + '.actionLog.at';
      if (actionEntry.calibrationScore != null && (typeof actionEntry.calibrationScore !== 'number' || !isFinite(actionEntry.calibrationScore))) return prefix + '.actionLog.calibrationScore';
      var unsafeActionPoint = unsafeSavedPoint(actionEntry.point, prefix + '.actionLog.point');
      if (unsafeActionPoint) return unsafeActionPoint;
      var actionTissueFields = ['tissueBefore', 'tissueAfter'];
      for (var actionTissueIndex = 0; actionTissueIndex < actionTissueFields.length; actionTissueIndex++) {
        var actionTissueField = actionTissueFields[actionTissueIndex];
        var unsafeActionTissue = unsafeSavedTissueSnapshot(actionEntry[actionTissueField], prefix + '.actionLog.' + actionTissueField);
        if (unsafeActionTissue) return unsafeActionTissue;
      }
      var actionRecordFields = ['toolCalibration', 'interactionMetrics', 'calibrationScoresBefore'];
      for (var actionRecordIndex = 0; actionRecordIndex < actionRecordFields.length; actionRecordIndex++) {
        var actionRecordField = actionRecordFields[actionRecordIndex];
        if (actionEntry[actionRecordField] != null && !isPlainDissectionRecord(actionEntry[actionRecordField])) return prefix + '.actionLog.' + actionRecordField;
        var unsafeActionRecord = unsafeSavedScalarTree(actionEntry[actionRecordField], prefix + '.actionLog.' + actionRecordField, 0);
        if (unsafeActionRecord) return unsafeActionRecord;
      }
      if (actionEntry.undoState != null) {
        if (!isPlainDissectionRecord(actionEntry.undoState)) return prefix + '.actionLog.undoState';
        var allowedUndoFields = dissectionUndoFieldsForAction(actionEntry.action);
        var undoFields = Object.keys(actionEntry.undoState);
        for (var undoIndex = 0; undoIndex < undoFields.length; undoIndex++) {
          var undoField = undoFields[undoIndex];
          var undoValue = actionEntry.undoState[undoField];
          if (allowedUndoFields.indexOf(undoField) < 0) return prefix + '.actionLog.undoState.' + undoField;
          if (['inspected', 'incisionStarted', 'incisionExtended', 'retracted', 'surfaceCleared', 'fieldWicked', 'probed'].indexOf(undoField) >= 0 && undoValue != null && typeof undoValue !== 'boolean') return prefix + '.actionLog.undoState.' + undoField;
          if (['forcepsPoint', 'dropperPoint', 'wickPoint'].indexOf(undoField) >= 0) {
            var unsafeUndoPoint = unsafeSavedPoint(undoValue, prefix + '.actionLog.undoState.' + undoField);
            if (unsafeUndoPoint) return unsafeUndoPoint;
          }
          if (['pins', 'incisionPath', 'incisionSamples', 'extensionPath', 'extensionSamples'].indexOf(undoField) >= 0) {
            var unsafeUndoPoints = unsafeSavedPointArray(undoValue, prefix + '.actionLog.undoState.' + undoField);
            if (unsafeUndoPoints) return unsafeUndoPoints;
          }
          if (undoField === 'probedOrganId' && undoValue != null && typeof undoValue !== 'string') return prefix + '.actionLog.undoState.probedOrganId';
          if (/Metrics$/.test(undoField)) {
            if (undoValue != null && !isPlainDissectionRecord(undoValue)) return prefix + '.actionLog.undoState.' + undoField;
            var unsafeUndoMetrics = unsafeSavedScalarTree(undoValue, prefix + '.actionLog.undoState.' + undoField, 0);
            if (unsafeUndoMetrics) return unsafeUndoMetrics;
          }
        }
      }
    }
    var cautionLog = Array.isArray(record.cautionLog) ? record.cautionLog : [];
    for (var cautionIndex = 0; cautionIndex < cautionLog.length; cautionIndex++) {
      var cautionEntry = cautionLog[cautionIndex];
      if (cautionEntry.message != null && typeof cautionEntry.message !== 'string') return prefix + '.cautionLog.message';
      if (cautionEntry.at != null && (typeof cautionEntry.at !== 'number' || !isFinite(cautionEntry.at))) return prefix + '.cautionLog.at';
      if (cautionEntry.protocolDeviation != null && typeof cautionEntry.protocolDeviation !== 'boolean') return prefix + '.cautionLog.protocolDeviation';
      var unsafeCautionPoint = unsafeSavedPoint(cautionEntry.point, prefix + '.cautionLog.point');
      if (unsafeCautionPoint) return unsafeCautionPoint;
    }
    var pointArrays = ['pins', 'incisionPath', 'extensionPath', 'incisionSamples', 'extensionSamples'];
    for (var pointArrayIndex = 0; pointArrayIndex < pointArrays.length; pointArrayIndex++) {
      var points = Array.isArray(record[pointArrays[pointArrayIndex]]) ? record[pointArrays[pointArrayIndex]] : [];
      if (points.some(function (point) {
        return point.x != null && (typeof point.x !== 'number' || !isFinite(point.x)) || point.y != null && (typeof point.y !== 'number' || !isFinite(point.y));
      })) return prefix + '.' + pointArrays[pointArrayIndex];
    }
    var procedurePointFields = ['forcepsPoint', 'dropperPoint', 'wickPoint'];
    for (var procedurePointIndex = 0; procedurePointIndex < procedurePointFields.length; procedurePointIndex++) {
      var procedurePointField = procedurePointFields[procedurePointIndex];
      var unsafeProcedurePoint = unsafeSavedPoint(record[procedurePointField], prefix + '.' + procedurePointField);
      if (unsafeProcedurePoint) return unsafeProcedurePoint;
    }
    var procedureMetricFields = ['incisionMetrics', 'extensionMetrics', 'forcepsDragMetrics', 'pinDragMetrics', 'dropperDragMetrics', 'wickDragMetrics', 'probeDragMetrics', 'calibrationScores'];
    for (var metricIndex = 0; metricIndex < procedureMetricFields.length; metricIndex++) {
      var metricField = procedureMetricFields[metricIndex];
      if (record[metricField] != null && !isPlainDissectionRecord(record[metricField])) return prefix + '.' + metricField;
      var unsafeMetric = unsafeSavedScalarTree(record[metricField], prefix + '.' + metricField, 0);
      if (unsafeMetric) return unsafeMetric;
    }
    var unsafeTissueState = unsafeSavedTissueSnapshot(record.tissueState, prefix + '.tissueState');
    if (unsafeTissueState) return unsafeTissueState;
    return null;
  }
  function inspectDissectionStoredValue(key) {
    var raw;
    try {
      raw = localStorage.getItem(key);
    } catch (e) {
      return { ok: false, kind: 'storage-unavailable', error: e };
    }
    if (raw == null) return { ok: true, kind: 'empty', data: {}, raw: raw };
    var data;
    try {
      data = JSON.parse(raw);
    } catch (e) {
      return { ok: false, kind: 'malformed', error: e, raw: raw };
    }
    if (!isPlainDissectionRecord(data)) return { ok: false, kind: 'unsafe-shape', raw: raw };
    if (data.schemaVersion != null) {
      var version = data.schemaVersion;
      if (typeof version !== 'number' || !isFinite(version) || Math.floor(version) !== version || version <= 0) {
        return { ok: false, kind: 'invalid-version', version: version, raw: raw };
      }
      if (version > DISSECTION_SAVE_SCHEMA_VERSION) return { ok: false, kind: 'future-version', version: version, raw: raw };
    }
    var mapFields = ['exploredOrgans', 'verifiedIdentifications', 'revealedLayers', 'assessmentEvidence', 'completedObjectives', 'organNotes', 'organConfidence', 'procedureByLayer', 'attemptArchive'];
    for (var mapIndex = 0; mapIndex < mapFields.length; mapIndex++) {
      var mapField = mapFields[mapIndex];
      if (data[mapField] != null && !isPlainDissectionRecord(data[mapField])) return { ok: false, kind: 'unsafe-shape', field: mapField, raw: raw };
    }
    var arrayFields = ['annotations', 'quizReviewQueue', 'visualEvidence'];
    for (var arrayIndex = 0; arrayIndex < arrayFields.length; arrayIndex++) {
      var arrayField = arrayFields[arrayIndex];
      if (data[arrayField] != null && !Array.isArray(data[arrayField])) return { ok: false, kind: 'unsafe-shape', field: arrayField, raw: raw };
    }
    if (Array.isArray(data.annotations) && data.annotations.some(function (annotation) { return !isPlainDissectionRecord(annotation); })) {
      return { ok: false, kind: 'unsafe-shape', field: 'annotations', raw: raw };
    }
    if (Array.isArray(data.visualEvidence) && data.visualEvidence.some(function (entry) { return !isPlainDissectionRecord(entry); })) {
      return { ok: false, kind: 'unsafe-shape', field: 'visualEvidence', raw: raw };
    }
    var procedureLayers = data.procedureByLayer || {};
    var procedureArrayFields = ['history', 'actionLog', 'cautionLog', 'pins', 'incisionPath', 'extensionPath', 'incisionSamples', 'extensionSamples'];
    var procedureRecordArrayFields = ['actionLog', 'cautionLog', 'pins', 'incisionPath', 'extensionPath', 'incisionSamples', 'extensionSamples'];
    var procedureLayerIds = Object.keys(procedureLayers);
    for (var procedureIndex = 0; procedureIndex < procedureLayerIds.length; procedureIndex++) {
      var procedureLayer = procedureLayers[procedureLayerIds[procedureIndex]];
      if (!isPlainDissectionRecord(procedureLayer)) return { ok: false, kind: 'unsafe-shape', field: 'procedureByLayer', raw: raw };
      for (var procedureArrayIndex = 0; procedureArrayIndex < procedureArrayFields.length; procedureArrayIndex++) {
        var procedureArrayField = procedureArrayFields[procedureArrayIndex];
        if (procedureLayer[procedureArrayField] != null && !Array.isArray(procedureLayer[procedureArrayField])) return { ok: false, kind: 'unsafe-shape', field: 'procedureByLayer.' + procedureArrayField, raw: raw };
      }
      for (var procedureRecordIndex = 0; procedureRecordIndex < procedureRecordArrayFields.length; procedureRecordIndex++) {
        var procedureRecordField = procedureRecordArrayFields[procedureRecordIndex];
        if (Array.isArray(procedureLayer[procedureRecordField]) && procedureLayer[procedureRecordField].some(function (entry) { return !isPlainDissectionRecord(entry); })) return { ok: false, kind: 'unsafe-shape', field: 'procedureByLayer.' + procedureRecordField, raw: raw };
      }
      var unsafeProcedureField = unsafeSavedProcedureField(procedureLayer, 'procedureByLayer');
      if (unsafeProcedureField) return { ok: false, kind: 'unsafe-shape', field: unsafeProcedureField, raw: raw };
    }
    var attemptArchive = data.attemptArchive || {};
    var attemptLayerIds = Object.keys(attemptArchive);
    for (var attemptIndex = 0; attemptIndex < attemptLayerIds.length; attemptIndex++) {
      var attempts = attemptArchive[attemptLayerIds[attemptIndex]];
      if (!Array.isArray(attempts) || attempts.some(function (attempt) { return !isPlainDissectionRecord(attempt); })) return { ok: false, kind: 'unsafe-shape', field: 'attemptArchive', raw: raw };
      for (var archivedAttemptIndex = 0; archivedAttemptIndex < attempts.length; archivedAttemptIndex++) {
        var archivedAttempt = attempts[archivedAttemptIndex];
        for (var archivedArrayIndex = 0; archivedArrayIndex < procedureArrayFields.length; archivedArrayIndex++) {
          var archivedArrayField = procedureArrayFields[archivedArrayIndex];
          if (archivedAttempt[archivedArrayField] != null && !Array.isArray(archivedAttempt[archivedArrayField])) return { ok: false, kind: 'unsafe-shape', field: 'attemptArchive.' + archivedArrayField, raw: raw };
        }
        for (var archivedRecordIndex = 0; archivedRecordIndex < procedureRecordArrayFields.length; archivedRecordIndex++) {
          var archivedRecordField = procedureRecordArrayFields[archivedRecordIndex];
          if (Array.isArray(archivedAttempt[archivedRecordField]) && archivedAttempt[archivedRecordField].some(function (entry) { return !isPlainDissectionRecord(entry); })) return { ok: false, kind: 'unsafe-shape', field: 'attemptArchive.' + archivedRecordField, raw: raw };
        }
        var unsafeArchivedField = unsafeSavedProcedureField(archivedAttempt, 'attemptArchive');
        if (unsafeArchivedField) return { ok: false, kind: 'unsafe-shape', field: unsafeArchivedField, raw: raw };
      }
    }
    return { ok: true, kind: 'valid', data: data, raw: raw };
  }
  function currentDissectionSaveAccess(key, owner) {
    return _disSaveAccess[dissectionSaveSlot(key, owner)] || { state: 'unknown', issue: null, key: key, owner: owner || 'default' };
  }
  function allowDissectionSave(key, baselineRaw, specimenName, owner) {
    var saveOwner = owner || 'default';
    _disSaveAccess[dissectionSaveSlot(key, saveOwner)] = { state: 'writable', issue: null, baselineRaw: baselineRaw == null ? null : String(baselineRaw), specimenName: specimenName || 'specimen', key: key, owner: saveOwner };
  }
  function refreshDissectionSaveAccess(key, owner) {
    setTimeout(function () {
      try {
        if (typeof document === 'undefined') return;
        var roots = document.querySelectorAll('[data-dissection-root][data-dissection-save-key]');
        for (var rootIndex = 0; rootIndex < roots.length; rootIndex++) {
          var root = roots[rootIndex];
          if (root.getAttribute('data-dissection-save-key') !== key) continue;
          if (owner && root.getAttribute('data-dissection-save-owner') !== owner) continue;
          if (typeof root._alloDissectionRefreshSaveAccess === 'function') root._alloDissectionRefreshSaveAccess();
        }
      } catch (e) {}
    }, 0);
  }
  function rememberDissectionSaveBaseline(key, raw, owner) {
    var access = currentDissectionSaveAccess(key, owner);
    if (access.state === 'writable') access.baselineRaw = raw == null ? null : String(raw);
  }
  function dissectionSaveIssueMessage(issue, specimenName) {
    var label = specimenName || 'specimen';
    if (issue && issue.kind === 'future-version') return 'This ' + label + ' save was created by a newer lab version and was preserved unchanged. Saving is paused; use Reset specimen only if you want to replace it.';
    if (issue && issue.kind === 'concurrent-change') return 'Saved ' + label + ' progress changed in another lab, tab, or window and was preserved unchanged. Saving is paused until you safely reload that record or confirm a reset.';
    if (issue && issue.kind === 'storage-unavailable') return 'Saved ' + label + ' progress could not be accessed and was left unchanged. Saving is paused in this temporary session.';
    return 'Saved ' + label + ' data could not be read safely and was preserved unchanged. Saving is paused; use Reset specimen to start fresh.';
  }
  function blockDissectionSave(key, issue, specimenName, owner) {
    var saveOwner = owner || 'default';
    cancelDissectionSave(key, saveOwner);
    _disSaveAccess[dissectionSaveSlot(key, saveOwner)] = { state: 'blocked', issue: issue || { kind: 'unsafe-shape' }, specimenName: specimenName || 'specimen', key: key, owner: saveOwner };
    publishDissectionSaveStatus(issue && (issue.kind === 'future-version' || issue.kind === 'concurrent-change') ? 'warning' : 'error', dissectionSaveIssueMessage(issue, specimenName), key, saveOwner);
    refreshDissectionSaveAccess(key, saveOwner);
  }
  function prepareDissectionSaveAccess(key, specimenName, owner) {
    var inspection = inspectDissectionStoredValue(key);
    if (inspection.ok) {
      allowDissectionSave(key, inspection.raw, specimenName, owner);
      publishDissectionSaveStatus('ready', 'Progress saves locally on this device.', key, owner);
    } else {
      blockDissectionSave(key, inspection, specimenName, owner);
    }
    return inspection;
  }
  function disSoundEnabled() { try { return window.__alloDissectionSoundEnabled !== false; } catch (e) { return true; } }
  function currentDissectionSaveStatus(key, owner) {
    var slot = key ? dissectionSaveSlot(key, owner) : null;
    return slot && _disSaveStatusByKey[slot] ? _disSaveStatusByKey[slot] : _disSaveStatus;
  }
  function publishDissectionSaveStatus(state, message, key, owner) {
    var previousStatus = currentDissectionSaveStatus(key, owner);
    if (previousStatus.state === state && previousStatus.message === message) return;
    var nextStatus = { state: state, message: message, at: Date.now() };
    if (key) _disSaveStatusByKey[dissectionSaveSlot(key, owner)] = nextStatus;
    else _disSaveStatus = nextStatus;
    try {
      if (typeof document === 'undefined') return;
      var statusNodes = document.querySelectorAll('[data-dissection-save-status]');
      for (var statusIndex = 0; statusIndex < statusNodes.length; statusIndex++) {
        var statusNode = statusNodes[statusIndex];
        var statusRoot = statusNode.closest ? statusNode.closest('[data-dissection-root]') : null;
        if (key && (!statusRoot || statusRoot.getAttribute('data-dissection-save-key') !== key)) continue;
        if (owner && (!statusRoot || statusRoot.getAttribute('data-dissection-save-owner') !== owner)) continue;
        statusNode.setAttribute('data-state', state);
        var statusMessage = statusNode.querySelector('[data-dissection-save-message]');
        var statusGlyph = statusNode.querySelector('[data-dissection-save-glyph]');
        if (statusMessage) statusMessage.textContent = message;
        if (statusGlyph) statusGlyph.textContent = state === 'error' ? '\u00D7' : (state === 'warning' ? '!' : '\u2713');
      }
    } catch (e) {}
  }
  function tryDissectionStorageWrite(key, data) {
    try {
      var raw = JSON.stringify(data);
      localStorage.setItem(key, raw);
      return { ok: true, error: null, raw: raw };
    } catch (e) {
      return { ok: false, error: e };
    }
  }
  function writeDissectionSave(key, data, owner) {
    var saveOwner = owner || 'default';
    var saveAccess = currentDissectionSaveAccess(key, saveOwner);
    if (saveAccess.state !== 'writable') return { durable: false, complete: false };
    // Re-check the bytes at the last responsible moment. Another tab or a
    // newer app build may have replaced this record after the initial load.
    var writeInspection = inspectDissectionStoredValue(key);
    if (!writeInspection.ok) {
      blockDissectionSave(key, writeInspection, key.indexOf('dissection_progress_') === 0 ? key.slice('dissection_progress_'.length) : 'specimen', saveOwner);
      return { durable: false, complete: false };
    }
    if (writeInspection.raw !== saveAccess.baselineRaw) {
      var targetRaw = null;
      try { targetRaw = JSON.stringify(data); } catch (e) {}
      // Two live roots can legitimately prepare the same empty specimen at the
      // same time. If another owner already wrote these exact bytes, adopt that
      // baseline instead of reporting a false conflict.
      if (targetRaw != null && writeInspection.raw === targetRaw) {
        rememberDissectionSaveBaseline(key, targetRaw, saveOwner);
        publishDissectionSaveStatus('saved', 'Progress saved locally on this device.', key, saveOwner);
        return { durable: true, complete: true };
      }
      blockDissectionSave(key, { kind: 'concurrent-change', raw: writeInspection.raw }, saveAccess.specimenName, saveOwner);
      return { durable: false, complete: false };
    }
    var fullWrite = tryDissectionStorageWrite(key, data);
    if (fullWrite.ok) {
      rememberDissectionSaveBaseline(key, fullWrite.raw, saveOwner);
      var previousSaveStatus = currentDissectionSaveStatus(key, saveOwner);
      var wasLimited = previousSaveStatus.state === 'warning' || previousSaveStatus.state === 'error';
      publishDissectionSaveStatus(wasLimited ? 'recovered' : 'saved', wasLimited
        ? 'Full progress saving has been restored.'
        : 'Progress saved locally on this device.', key, saveOwner);
      return { durable: true, complete: true };
    }

    // Image data is the largest part of a lab record. If the origin is near its
    // storage limit, preserve all text/procedure evidence before giving up on
    // the entire record, and tell the learner exactly what was retained.
    var evidence = data && Array.isArray(data.visualEvidence) ? data.visualEvidence.filter(function (entry) { return entry && entry.image; }) : [];
    if (evidence.length > 1) {
      var newestEvidence = evidence[evidence.length - 1];
      var reducedData = Object.assign({}, data, {
        visualEvidence: [newestEvidence],
        referenceEvidenceId: newestEvidence && newestEvidence.id != null ? newestEvidence.id : null,
        splitComparison: false
      });
      var reducedWrite = tryDissectionStorageWrite(key, reducedData);
      if (reducedWrite.ok) {
        rememberDissectionSaveBaseline(key, reducedWrite.raw, saveOwner);
        publishDissectionSaveStatus('warning', 'Progress saved, but storage is nearly full; only the newest evidence frame was kept. Download important frames and remove older notebook frames to restore full saving.', key, saveOwner);
        return { durable: true, complete: false };
      }
    }
    if (evidence.length) {
      var textOnlyData = Object.assign({}, data, {
        visualEvidence: [],
        referenceEvidenceId: null,
        splitComparison: false
      });
      var textOnlyWrite = tryDissectionStorageWrite(key, textOnlyData);
      if (textOnlyWrite.ok) {
        rememberDissectionSaveBaseline(key, textOnlyWrite.raw, saveOwner);
        publishDissectionSaveStatus('warning', 'Progress and notes saved, but evidence images could not be stored. Download important frames and remove notebook frames to restore full saving.', key, saveOwner);
        return { durable: true, complete: false };
      }
    }

    publishDissectionSaveStatus('error', 'Progress is not saved in this browser. Keep the lab open; copy the lab report or print it to PDF before closing.', key, saveOwner);
    return { durable: false, complete: false };
  }
  function scheduleDissectionSave(key, data, owner) {
    var saveOwner = owner || 'default';
    var saveSlot = dissectionSaveSlot(key, saveOwner);
    if (currentDissectionSaveAccess(key, saveOwner).state !== 'writable') {
      cancelDissectionSave(key, saveOwner);
      return false;
    }
    try {
      var previous = _disSaveQueue[saveSlot];
      if (previous && previous.timer) clearTimeout(previous.timer);
      var pending = { key: key, owner: saveOwner, data: data, timer: null };
      pending.timer = setTimeout(function () {
        if (_disSaveQueue[saveSlot] !== pending) return;
        var saveResult = writeDissectionSave(key, pending.data, saveOwner);
        if (_disSaveQueue[saveSlot] !== pending) return;
        pending.timer = null;
        // A transient storage failure must not discard the only copy of the
        // latest snapshot. Keep it available for an explicit flush, page-hide
        // retry, or a later specimen-switch attempt while access is writable.
        pending.durable = !!(saveResult && saveResult.durable);
        if ((saveResult && saveResult.complete) || currentDissectionSaveAccess(key, saveOwner).state !== 'writable') delete _disSaveQueue[saveSlot];
      }, 120);
      _disSaveQueue[saveSlot] = pending;
      return true;
    } catch (e) {}
    return false;
  }
  function flushDissectionSave(key, requireComplete, owner) {
    var saveOwner = owner || 'default';
    var saveSlot = dissectionSaveSlot(key, saveOwner);
    if (currentDissectionSaveAccess(key, saveOwner).state !== 'writable') {
      cancelDissectionSave(key, saveOwner);
      return false;
    }
    try {
      var pending = _disSaveQueue[saveSlot];
      if (!pending) return false;
      if (pending.timer) clearTimeout(pending.timer);
      pending.timer = null;
      var saveResult = writeDissectionSave(key, pending.data, saveOwner);
      pending.durable = !!(saveResult && saveResult.durable);
      if (_disSaveQueue[saveSlot] === pending && ((saveResult && saveResult.complete) || currentDissectionSaveAccess(key, saveOwner).state !== 'writable')) delete _disSaveQueue[saveSlot];
      return requireComplete ? !!(saveResult && saveResult.complete) : !!(saveResult && saveResult.durable);
    } catch (e) {
      return false;
    }
  }
  function cancelDissectionSave(key, owner) {
    try {
      var saveSlot = dissectionSaveSlot(key, owner);
      var pending = _disSaveQueue[saveSlot];
      if (!pending) return false;
      if (pending.timer) clearTimeout(pending.timer);
      if (_disSaveQueue[saveSlot] === pending) delete _disSaveQueue[saveSlot];
      return true;
    } catch (e) {
      return false;
    }
  }
  function flushAllDissectionSaves() {
    var allSaved = true;
    try {
      Object.keys(_disSaveQueue).forEach(function (slot) {
        var pending = _disSaveQueue[slot];
        if (pending && !flushDissectionSave(pending.key, false, pending.owner)) allSaved = false;
      });
    } catch (e) {
      allSaved = false;
    }
    return allSaved;
  }
  function releaseDissectionSaveOwner(owner) {
    var saveOwner = owner || 'default';
    if (saveOwner === 'default') return;
    try {
      if (typeof document !== 'undefined') {
        var connectedRoots = document.querySelectorAll('[data-dissection-save-owner]');
        for (var rootIndex = 0; rootIndex < connectedRoots.length; rootIndex++) {
          if (connectedRoots[rootIndex].isConnected && connectedRoots[rootIndex].getAttribute('data-dissection-save-owner') === saveOwner) return;
        }
      }
      Object.keys(_disSaveQueue).forEach(function (slot) {
        var pending = _disSaveQueue[slot];
        if (!pending || pending.owner !== saveOwner) return;
        if (currentDissectionSaveAccess(pending.key, saveOwner).state === 'writable') flushDissectionSave(pending.key, false, saveOwner);
        cancelDissectionSave(pending.key, saveOwner);
      });
      Object.keys(_disSaveAccess).forEach(function (slot) {
        if (_disSaveAccess[slot] && _disSaveAccess[slot].owner === saveOwner) delete _disSaveAccess[slot];
      });
      var ownerPrefix = saveOwner + '::';
      Object.keys(_disSaveStatusByKey).forEach(function (slot) {
        if (slot.indexOf(ownerPrefix) === 0) delete _disSaveStatusByKey[slot];
      });
    } catch (e) {}
  }
  function bindDissectionSaveLifecycle() {
    try {
      if (typeof window === 'undefined' || typeof window.addEventListener !== 'function') return;
      var previousHandler = window.__alloDissectionSavePagehideHandler;
      if (previousHandler && typeof window.removeEventListener === 'function') window.removeEventListener('pagehide', previousHandler);
      window.__alloDissectionSavePagehideHandler = flushAllDissectionSaves;
      window.addEventListener('pagehide', flushAllDissectionSaves);
      var previousStorageHandler = window.__alloDissectionSaveStorageHandler;
      if (previousStorageHandler && typeof window.removeEventListener === 'function') window.removeEventListener('storage', previousStorageHandler);
      window.__alloDissectionSaveStorageHandler = function (event) {
        var changedKey = event ? event.key : null;
        Object.keys(_disSaveAccess).forEach(function (slot) {
          var access = _disSaveAccess[slot];
          var key = access && access.key;
          if (!key || key.indexOf('dissection_progress_') !== 0) return;
          if (changedKey != null && changedKey !== key) return;
          if (access.state !== 'writable') return;
          var inspection = inspectDissectionStoredValue(key);
          if (!inspection.ok) blockDissectionSave(key, inspection, access.specimenName, access.owner);
          else if (inspection.raw !== access.baselineRaw) blockDissectionSave(key, { kind: 'concurrent-change', raw: inspection.raw }, access.specimenName, access.owner);
        });
      };
      window.addEventListener('storage', window.__alloDissectionSaveStorageHandler);
    } catch (e) {}
  }
  bindDissectionSaveLifecycle();
  function clampDissectionInquiryValue(value, min, max, fallback) {
    var numeric = Number(value);
    if (!isFinite(numeric)) numeric = fallback;
    return Math.max(min, Math.min(max, Math.round(numeric)));
  }
  function defaultDissectionInquiry() {
    return { specimenSize: 8, layerDepth: 1, careLevel: 5, timePress: 5, hypothesis: '', stuckRevealed: false, understood: false, explanation: '', log: [] };
  }
  function dissectionInquiryMetrics(inquiry) {
    var damage = (inquiry.layerDepth * inquiry.timePress) / Math.max(1, inquiry.careLevel * (inquiry.specimenSize / 8));
    var insight = (inquiry.layerDepth * 1.5 + inquiry.careLevel * 0.8 - inquiry.timePress * 0.3) * (inquiry.specimenSize / 8);
    return {
      damage: damage,
      insight: insight,
      state: damage > 8 ? 'destroyed' : damage > 5 ? 'damaged' : damage > 3 ? 'compromised' : insight > 8 ? 'excellent' : insight > 4 ? 'good' : 'surface'
    };
  }
  function dissectionInquiryStateLabel(state) {
    return ({
      destroyed: 'High damage risk',
      damaged: 'Elevated damage risk',
      compromised: 'Compromised visibility',
      surface: 'Surface-only access',
      good: 'Balanced progress',
      excellent: 'High modeled insight'
    })[state] || 'Modeled outcome';
  }
  function normalizeDissectionInquiry(value) {
    var source = value && typeof value === 'object' ? value : {};
    var normalized = {
      specimenSize: clampDissectionInquiryValue(source.specimenSize, 2, 30, 8),
      layerDepth: clampDissectionInquiryValue(source.layerDepth, 1, 5, 1),
      careLevel: clampDissectionInquiryValue(source.careLevel, 1, 10, 5),
      timePress: clampDissectionInquiryValue(source.timePress, 1, 10, 5),
      hypothesis: typeof source.hypothesis === 'string' ? source.hypothesis.slice(0, 2000) : '',
      stuckRevealed: !!source.stuckRevealed,
      understood: !!source.understood,
      explanation: typeof source.explanation === 'string' ? source.explanation.slice(0, 3000) : '',
      log: []
    };
    normalized.log = (Array.isArray(source.log) ? source.log : []).filter(function (entry) {
      return entry && typeof entry === 'object';
    }).slice(-20).map(function (entry) {
      var point = {
        specimenSize: clampDissectionInquiryValue(entry.sz, 2, 30, normalized.specimenSize),
        layerDepth: clampDissectionInquiryValue(entry.dp, 1, 5, normalized.layerDepth),
        careLevel: clampDissectionInquiryValue(entry.c, 1, 10, normalized.careLevel),
        timePress: clampDissectionInquiryValue(entry.tp, 1, 10, normalized.timePress)
      };
      var metrics = dissectionInquiryMetrics(point);
      return {
        t: typeof entry.t === 'string' ? entry.t.slice(0, 20) : '',
        sz: point.specimenSize,
        dp: point.layerDepth,
        c: point.careLevel,
        tp: point.timePress,
        dmg: metrics.damage.toFixed(1),
        ins: metrics.insight.toFixed(1),
        state: dissectionInquiryStateLabel(metrics.state)
      };
    });
    return normalized;
  }
  function getDisAC() { if (!_disAC) { try { _disAC = new (window.AudioContext || window.webkitAudioContext)(); } catch(e) {} } if (_disAC && _disAC.state === 'suspended') { try { _disAC.resume(); } catch(e) {} } return _disAC; }
  function disTone(f,d,tp,v) { if (!disSoundEnabled()) return; var ac = getDisAC(); if (!ac) return; try { var o = ac.createOscillator(); var g = ac.createGain(); o.type = tp||'sine'; o.frequency.value = f; g.gain.setValueAtTime(v||0.07, ac.currentTime); g.gain.exponentialRampToValueAtTime(0.001, ac.currentTime+(d||0.1)); o.connect(g); g.connect(ac.destination); o.start(); o.stop(ac.currentTime+(d||0.1)); } catch(e) {} }
  function disNoise(dur,vol,hz) { if (!disSoundEnabled()) return; var ac = getDisAC(); if (!ac) return; try { var bs = Math.floor(ac.sampleRate*(dur||0.05)); var b = ac.createBuffer(1,bs,ac.sampleRate); var dd = b.getChannelData(0); for(var i=0;i<bs;i++) dd[i]=(Math.random()*2-1)*(1-i/bs); var s = ac.createBufferSource(); s.buffer=b; var f = ac.createBiquadFilter(); f.type='lowpass'; f.frequency.value=hz||600; var g = ac.createGain(); g.gain.setValueAtTime(vol||0.04,ac.currentTime); g.gain.exponentialRampToValueAtTime(0.001,ac.currentTime+(dur||0.05)); s.connect(f); f.connect(g); g.connect(ac.destination); s.start(); } catch(e) {} }
  function sfxDisCut() { disNoise(0.06, 0.05, 800); disTone(300, 0.08, 'sawtooth', 0.05); }
  function sfxDisProbe() { disTone(800, 0.04, 'sine', 0.05); setTimeout(function() { disTone(1000, 0.03, 'sine', 0.04); }, 30); }
  function sfxDisReveal() { disTone(440, 0.06, 'sine', 0.06); setTimeout(function() { disTone(554, 0.06, 'sine', 0.06); }, 50); setTimeout(function() { disTone(659, 0.08, 'sine', 0.07); }, 100); }
  function sfxDisPin() { disTone(1200, 0.03, 'square', 0.04); }
  function sfxDisLabel() { disTone(600, 0.04, 'sine', 0.05); }

  console.log('[Dissection Plugin] Registering dissection tool...');
  window.StemLab.registerTool('dissection', {
    icon: "🔪",
    label: "Virtual Dissection Lab",
    desc: "Peel anatomical layers and click organs across specimens (frog, fetal pig, earthworm, perch and more) with quizzes and flashcards.",
    color: 'slate',
    category: 'science',
    questHooks: [
      { id: 'reveal_3_layers', label: 'Reveal 3 layers of a specimen', icon: '\uD83D\uDD2C', check: function(d) { return Object.keys(d.revealedLayers || {}).length >= 3; }, progress: function(d) { return Object.keys(d.revealedLayers || {}).length + '/3 layers'; } },
      { id: 'complete_guided', label: 'Complete a guided dissection', icon: '\uD83D\uDCDA', check: function(d) { return d.guidedComplete || false; }, progress: function(d) { return d.guidedComplete ? 'Done!' : 'In progress'; } },
      { id: 'quiz_3_correct', label: 'Answer 3+ dissection quiz questions correctly on the first attempt', icon: '\uD83E\uDDE0', check: function(d) { return (d.assessmentRecordedScore || d.quizFirstAttemptScore || 0) >= 3; }, progress: function(d) { return (d.assessmentRecordedScore || d.quizFirstAttemptScore || 0) + '/3 first-attempt'; } },
      { id: 'try_2_specimens', label: 'Explore 2 different specimens', icon: '\uD83D\uDC38', check: function(d) { return Object.keys(d.specimensViewed || {}).length >= 2; }, progress: function(d) { return Object.keys(d.specimensViewed || {}).length + '/2'; } }
    ],
    render: function(ctx) {
      var __alloT = function (k, fb) { var v; try { v = (typeof ctx.t === "function") ? ctx.t(k, fb) : null; } catch (e) { v = null; } return (v == null) ? (fb != null ? fb : k) : v; };
      console.log('[Dissection Plugin] render() called, has React:', !!ctx.React, 'has toolData:', !!ctx.toolData);
      // Aliases — maps ctx properties to original variable names
      var React = ctx.React;
      var h = React.createElement;
      var labToolData = ctx.toolData;
      var setLabToolData = ctx.setToolData;
      // The host's deferred setter wrapper may be recreated on every parent
      // render. Prefer the bridge's stable per-fiber token so one mounted lab
      // retains one persistence owner for its full lifetime.
      var dissectionSaveOwner = dissectionSaveOwnerFor(ctx.pluginInstanceToken || setLabToolData);
      var setStemLabTool = ctx.setStemLabTool;
      var setStemLabTab = ctx.setStemLabTab;
      var stemLabTab = ctx.stemLabTab || 'explore';
      var stemLabTool = ctx.stemLabTool;
      var toolSnapshots = ctx.toolSnapshots;
      var setToolSnapshots = ctx.setToolSnapshots;
      var addToast = ctx.addToast;
      var t = ctx.t;
      var callGemini = ctx.callGemini;
      var ArrowLeft = ctx.icons.ArrowLeft;
      var Calculator = ctx.icons.Calculator;
      var Sparkles = ctx.icons.Sparkles;
      var X = ctx.icons.X;
      var GripVertical = ctx.icons.GripVertical;
      var announceToSR = ctx.announceToSR;
      var awardStemXP = ctx.awardXP;
      var getStemXP = ctx.getXP;
      var stemCelebrate = ctx.celebrate;
      var stemBeep = ctx.beep;
      var callGemini = ctx.callGemini;
      var callTTS = ctx.callTTS;
      var callImagen = ctx.callImagen;
      var callGeminiVision = ctx.callGeminiVision;
      var gradeLevel = ctx.gradeLevel;
      var srOnly = ctx.srOnly;
      var a11yClick = ctx.a11yClick;
      var canvasA11yDesc = ctx.canvasA11yDesc;
      var canvasNarrate = ctx.canvasNarrate;
      var props = ctx.props;

      // ── Tool body (dissection) ──
      return (function() {
var d = labToolData.dissection || {};

          // ── Specimen-specific trivia for "Did You Know?" panel ──
          var SPECIMEN_TRIVIA = {
            frog: [
              'A frog can absorb water through its skin \u2014 it never needs to drink.',
              'The golden poison dart frog has enough toxin to kill 10 adults.',
              'Frogs were the first land animals with true vocal cords.',
              'Wood frogs can survive being frozen solid and thaw back to life.',
              'A group of frogs is called an "army."'
            ],
            earthworm: [
              'Earthworms have 5 pairs of aortic arches that act as hearts.',
              'Charles Darwin studied earthworms for 39 years.',
              'A single acre can contain over 1 million earthworms.',
              'Earthworms can regenerate lost segments in some species.',
              'They eat their own weight in soil every single day.'
            ],
            pig: [
              'Processed porcine tissue is used in some bioprosthetic heart valves; that use does not make the whole pig heart identical to a human heart.',
              'Porcine insulin treated millions of diabetics before synthetic versions existed.',
              'Pigs have more taste buds (15,000) than humans (9,000).',
              'Fetal pigs share many mammalian organ systems with humans, but organ shape, proportions, vessels, and development are species-specific.',
              'Pig skin is used as temporary grafts for human burn victims.'
            ],
            perch: [
              'Fish gills extract proportionally more O\u2082 from water than lungs from air.',
              'The swim bladder evolved from the same structure that became lungs in land animals.',
              'Perch scale rings can reveal the fish\u2019s exact age, like tree rings.',
              'A perch\u2019s lateral line can detect prey movement in complete darkness.',
              'Fish have been on Earth for over 500 million years \u2014 before trees existed.'
            ],
            crayfish: [
              'Crayfish can regenerate lost claws over several molts.',
              'Their tail flip escape is one of the fastest movements in the animal kingdom.',
              'A crayfish has teeth inside its stomach (the gastric mill).',
              'They can walk forward but swim backward.',
              'Crayfish establish dominance hierarchies and remember social status.'
            ],
            sheepEye: [
              'The human eye can distinguish about 10 million different colors.',
              'Sheep have a tapetum lucidum \u2014 their eyes literally glow in the dark.',
              'The cornea is the only body part with no blood supply \u2014 it gets oxygen from air.',
              'Your retina contains 120 million rod cells for night vision.',
              'The eye\u2019s lens is made of transparent crystallin proteins that never turn over.'
            ],
            sheepHeart: [
              'Your heart beats about 100,000 times per day \u2014 3 billion times in a lifetime.',
              'The left ventricle wall is 3x thicker than the right.',
              'The heart creates enough pressure to squirt blood 30 feet.',
              'A sheep heart shares the four-chamber mammalian plan, while size, vessel branching, rate, and pressure differ by species.',
              'The SA node fires 60-100 times per minute with zero input from the brain.'
            ]
          };


          var upd = function (k, v) { setLabToolData(function (p) { return Object.assign({}, p, { dissection: Object.assign({}, p.dissection, (function () {
  // WCAG 4.1.3: Status live region for dynamic content announcements
  (function() {
    if (document.getElementById('allo-live-dissection')) return;
    var liveRegion = document.createElement('div');
    liveRegion.id = 'allo-live-dissection';
    liveRegion.setAttribute('aria-live', 'polite');
    liveRegion.setAttribute('aria-atomic', 'true');
    liveRegion.setAttribute('role', 'status');
    liveRegion.className = 'sr-only';
    liveRegion.style.cssText = 'position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);border:0';
    document.body.appendChild(liveRegion);
  })();
 var o = {}; o[k] = v; return o; })()) }); }); };
          var updMany = function (patch) {
            setLabToolData(function (p) {
              return Object.assign({}, p, { dissection: Object.assign({}, p.dissection || {}, patch || {}) });
            });
          };
          var updManyForSpecimen = function (expectedSpecimen, patch) {
            setLabToolData(function (p) {
              var currentDissection = p.dissection || {};
              if ((currentDissection.specimen || 'frog') !== expectedSpecimen) return p;
              return Object.assign({}, p, { dissection: Object.assign({}, currentDissection, patch || {}) });
            });
          };
          function clearOwnedTimedDissectionState(field, startedAt, expectedSpecimen) {
            setLabToolData(function (p) {
              var currentDissection = p.dissection || {};
              if ((currentDissection.specimen || 'frog') !== expectedSpecimen) return p;
              var currentTimedState = currentDissection[field];
              if (!currentTimedState || Number(currentTimedState.startedAt) !== Number(startedAt)) return p;
              var timedPatch = {}; timedPatch[field] = null;
              return Object.assign({}, p, { dissection: Object.assign({}, currentDissection, timedPatch) });
            });
          }
          try { window.__alloDissectionSoundEnabled = d.soundEnabled !== false; } catch (e) {}

          // Canvas Narration: Dissection Lab init
          if (typeof canvasNarrate === 'function') canvasNarrate('dissection', 'init', {
            first: 'Virtual Dissection Lab loaded. Choose a specimen such as frog, earthworm, fetal pig, perch, crayfish, sheep eye, or sheep heart. Peel layers to reveal anatomy, click organs for detail, and use study tools for quizzes and flashcards.',
            repeat: 'Dissection Lab ready.',
            terse: 'Dissection Lab ready.'
          });



          // ════════ SPECIMEN DATABASE ════════

          var SPECIMENS = {

            frog: {

              name: 'Frog (Rana)', icon: '\uD83D\uDC38',

              desc: 'Classic vertebrate — 3-chambered heart, cutaneous respiration, metamorphosis.',

              objectives: [
                'Identify the 3-chambered heart and trace blood flow',
                'Compare cutaneous vs pulmonary respiration',
                'Distinguish mesonephric kidneys from mammalian metanephric',
                'Locate the cloaca and explain shared-opening anatomy'
              ],
              objectiveTargets: [['heart'], ['dorsal_skin', 'lungs'], ['kidneys'], ['cloaca']],
              specTerms: [
                { term: 'Cutaneous respiration', def: 'Gas exchange through moist, permeable skin.' },
                { term: 'Buccal pumping', def: 'Throat-based breathing mechanism; frogs push air into lungs by raising the floor of the mouth.' },
                { term: 'Mesonephric kidney', def: 'Intermediate kidney type between fish pronephros and mammalian metanephros.' },
                { term: 'Cloaca', def: 'Shared exit chamber for digestive, urinary, and reproductive systems.' },
                { term: 'Nictitating membrane', def: 'Transparent third eyelid that protects the eye underwater.' }
              ],
              bodyShape: 'frog',

              layers: [

                { id: 'skin', name: 'Skin', icon: '\uD83D\uDFE2', color: '#4ade80', accent: '#16a34a', desc: 'Moist permeable integument with chromatophores.' },

                { id: 'muscle', name: 'Muscle', icon: '\uD83D\uDCAA', color: '#f87171', accent: '#dc2626', desc: 'Skeletal muscles for jumping and swimming.' },

                { id: 'organs', name: 'Organs', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Digestive, respiratory, circulatory, and urogenital organs.' },

                { id: 'skeleton', name: 'Skeleton', icon: '\uD83E\uDDB4', color: 'var(--allo-stem-text, #e2e8f0)', accent: '#94a3b8', desc: 'Endoskeleton adapted for jumping.' },

                { id: 'nervous', name: 'Nervous', icon: '\u26A1', color: '#c084fc', accent: '#7c3aed', desc: 'CNS and peripheral nerves.' }

              ],

              organs: {

                skin: [

                  { id: 'dorsal_skin', name: 'Dorsal Skin', x: 0.50, y: 0.38, fn: 'Green-brown pigmented surface with chromatophores. Mucous glands keep skin moist for cutaneous respiration (up to 50% of gas exchange).', clinical: 'Chytrid fungus attacks frog skin keratin, causing global amphibian declines.' },

                  { id: 'ventral_skin', name: 'Ventral Skin', x: 0.50, y: 0.58, fn: 'Lighter, thinner ventral surface. Highly vascularized \u2014 frogs absorb water through skin, not by drinking.', clinical: 'Permeable skin makes frogs sensitive bioindicators of environmental pollution.' },

                  { id: 'tympanum', name: 'Tympanic Membrane', x: 0.65, y: 0.24, fn: 'External eardrum behind eye. Transmits sound to columella and inner ear. Size indicates sex (larger = male).', clinical: 'Used for species and sex identification in field studies.' },

                  { id: 'nictitating', name: 'Nictitating Membrane', x: 0.60, y: 0.18, fn: 'Transparent third eyelid protecting eye underwater. Homologous to human plica semilunaris (vestigial).', clinical: 'Present in many vertebrates \u2014 vestigial in humans as plica semilunaris.' }

                ],

                muscle: [

                  { id: 'pectoralis', name: 'Pectoralis', x: 0.50, y: 0.36, fn: 'Chest muscle adducting forelimb. Assists landing absorption after jumps. Homologous to human pectoralis major.', clinical: 'Comparative anatomy: same muscle, different function \u2014 landing in frogs vs pushing in humans.' },

                  { id: 'rectus_abd', name: 'Rectus Abdominis', x: 0.50, y: 0.52, fn: 'Ventral abdominal muscle from sternum to pelvis. Flexes trunk and compresses cavity for buccal pump breathing.', clinical: 'Frogs use buccal pumping (throat) rather than diaphragmatic breathing like mammals.' },

                  { id: 'gastrocnemius', name: 'Gastrocnemius', x: 0.24, y: 0.69, fn: 'Large calf muscle powering jumps. Can generate 2\u00D7 body weight force. Galvani used frog legs to discover bioelectricity (1780s).', clinical: 'Classic muscle for physiology experiments \u2014 Galvani\'s frog leg experiments founded bioelectricity.' },

                  { id: 'triceps_fem', name: 'Triceps Femoris', x: 0.31, y: 0.57, fn: 'Three-headed thigh muscle (homolog of quadriceps). Primary knee extensor during jumping.', clinical: 'Frogs can jump 20\u00D7 body length due to elastic energy storage in tendons.' },

                  { id: 'sartorius', name: 'Sartorius', x: 0.37, y: 0.60, fn: 'Thin strap on medial thigh. Flexes and rotates hip. Assists drawing legs into swimming position.', clinical: 'Longest muscle in both frogs and humans \u2014 a homologous structure across vertebrates.' },

                  { id: 'deltoid', name: 'Deltoideus', x: 0.35, y: 0.36, fn: 'Shoulder muscle elevating/rotating forelimb. Smaller than hindlimb muscles \u2014 frogs are hindlimb-dominant.' }

                ],

                organs: [

                  { id: 'heart', name: 'Heart (3-chamber)', x: 0.50, y: 0.35, fn: '2 atria + 1 ventricle. Spiral valve in conus arteriosus separates blood with ~90% efficiency despite single ventricle.', clinical: 'Frog hearts beat without neural input (myogenic) and can beat in saline for hours \u2014 used in cardiac physiology research.' },

                  { id: 'lungs', name: 'Lungs', x: 0.43, y: 0.35, fn: 'Simple thin-walled sacs (no alveoli). Supplemented by cutaneous respiration. Inflated by buccal pumping \u2014 not diaphragm.', clinical: 'The Bornean flat-headed frog is completely lungless \u2014 breathes entirely through skin.' },

                  { id: 'liver', name: 'Liver (3 lobes)', x: 0.50, y: 0.44, fn: 'Large three-lobed organ. Produces bile, detoxifies blood, stores glycogen. Largest internal organ.', clinical: 'Liver color/size indicates environmental contamination in ecotoxicology studies.' },

                  { id: 'gallbladder', name: 'Gallbladder', x: 0.53, y: 0.45, fn: 'Small green sac between liver lobes. Stores/concentrates bile. Bright green color \u2014 key dissection landmark.', clinical: 'Bright green color makes it one of the easiest organs to identify in dissection.' },

                  { id: 'stomach', name: 'Stomach', x: 0.41, y: 0.51, fn: 'J-shaped muscular organ. Frogs swallow prey whole and use eye retraction to push food down.', clinical: 'Frogs push food into stomach by retracting eyes into mouth roof \u2014 unique swallowing mechanism.' },

                  { id: 'sm_intestine', name: 'Small Intestine', x: 0.50, y: 0.59, fn: 'Coiled tube (duodenum + ileum). Primary nutrient absorption site. Shorter than in herbivorous tadpoles.', clinical: 'Tadpoles (herbivores) have much longer intestines than adult frogs (carnivores) \u2014 diet drives gut length.' },

                  { id: 'lg_intestine', name: 'Large Intestine', x: 0.50, y: 0.66, fn: 'Short wide tube to cloaca. Absorbs water. Opens into cloaca (shared digestive/urinary/reproductive exit).', clinical: 'The cloaca \u2014 shared exit for 3 systems \u2014 is the ancestral vertebrate condition; separate openings evolved later.' },

                  { id: 'spleen', name: 'Spleen', x: 0.41, y: 0.55, fn: 'Small reddish organ near stomach. Filters blood, removes old RBCs, immune function.', clinical: 'Spleen plus antimicrobial skin peptides form a dual immune defense system.' },

                  { id: 'kidneys', name: 'Kidneys', x: 0.50, y: 0.62, fn: 'Elongated, dorsal organs. Mesonephric kidneys \u2014 intermediate between fish and mammal kidney types. Drains to cloaca.', clinical: 'Frog kidneys (mesonephric) are ancestral \u2014 mammals evolved more advanced metanephric kidneys.' },

                  { id: 'fat_bodies', name: 'Fat Bodies', x: 0.43, y: 0.42, fn: 'Yellow finger-like structures on kidneys/gonads. Energy reserves for hibernation and reproduction.', clinical: 'Fat body size indicates nutritional status \u2014 shrunken = environmental stress.' },

                  { id: 'pancreas', name: 'Pancreas', x: 0.53, y: 0.54, fn: 'Thin pale organ between stomach and duodenum. Produces digestive enzymes and insulin/glucagon.', clinical: 'Frog pancreatic islets used in early insulin research.' },

                  { id: 'cloaca', name: 'Cloaca', x: 0.50, y: 0.72, fn: 'Common chamber for digestive, urinary, and reproductive output. Present in amphibians, reptiles, birds.', clinical: 'Represents ancestral vertebrate design \u2014 separate openings evolved independently in mammals.' }

                ],

                skeleton: [

                  { id: 'skull', name: 'Skull', x: 0.50, y: 0.17, fn: 'Broad flat skull with large orbits. Frontoparietal bone fused (unique to frogs). Maxillary + vomerine teeth.', clinical: 'Frogs have teeth on upper jaw only \u2014 toads have no teeth at all.' },

                  { id: 'vertebral_col', name: 'Vertebral Column', x: 0.50, y: 0.44, fn: 'Only 9 presacral vertebrae (mammals have 24+). Short rigid spine for jumping.', clinical: 'Fewest vertebrae of any tetrapod \u2014 extreme spinal reduction for jumping.' },

                  { id: 'urostyle', name: 'Urostyle', x: 0.50, y: 0.63, fn: 'Fused caudal vertebrae forming rod-like tailbone. Absorbs landing shock. Unique to frogs/toads.', clinical: 'The urostyle is found only in anurans \u2014 a defining skeletal feature of frogs and toads.' },

                  { id: 'pelvic_girdle', name: 'Pelvic Girdle', x: 0.50, y: 0.59, fn: 'Elongated ilium creates lever for powerful jumps. Highly modified compared to other vertebrates.', clinical: 'Elongated pelvic girdle is the key anatomical adaptation enabling the frog jump.' },

                  { id: 'pectoral_gird', name: 'Pectoral Girdle', x: 0.50, y: 0.34, fn: 'Supports forelimbs. Acts as shock absorber during landing. Clavicle, coracoid, scapula, suprascapula.', clinical: 'The pectoral girdle absorbs impact forces that would fracture bones in most other animals.' },

                  { id: 'femur', name: 'Femur', x: 0.32, y: 0.59, fn: 'Long thigh bone. Proportionally longer than most vertebrates for jumping leverage.', clinical: 'Frog femur is proportionally longer than human femur relative to body size.' },

                  { id: 'tibiofibula', name: 'Tibio-fibula', x: 0.24, y: 0.67, fn: 'Fused tibia + fibula (single bone). Reduces weight while maintaining strength. 5 digits with webbing.', clinical: 'Bone fusion reduces weight \u2014 an adaptation for efficient jumping.' },

                  { id: 'radioulna', name: 'Radio-ulna', x: 0.29, y: 0.43, fn: 'Fused radius + ulna. 4 digits on forelimb (digit I lost). Simplified limb for landing.', clinical: 'Loss of digit I and bone fusion are weight reduction adaptations.' },

                  { id: 'astragalus', name: 'Elongated Ankle Bones', x: 0.27, y: 0.78, fn: 'Astragalus and calcaneus elongated to add extra leg segment \u2014 increases jump distance. Unique to frogs.', clinical: 'Elongated ankles function as an extra leg segment \u2014 key innovation for saltatory locomotion.' }

                ],

                nervous: [

                  { id: 'brain', name: 'Brain', x: 0.50, y: 0.17, fn: 'Small brain with prominent optic lobes (largest region). Olfactory lobes, cerebrum, cerebellum (small), medulla.', clinical: 'Vision dominates \u2014 classic Lettvin 1959 study: "What the Frog\'s Eye Tells the Frog\'s Brain."' },

                  { id: 'spinal_cord', name: 'Spinal Cord', x: 0.50, y: 0.44, fn: '10 spinal nerve pairs. Ends with filum terminale. Classic spinal frog preparation demonstrates reflexes.', clinical: 'A "spinal frog" (brain destroyed) still shows coordinated reflex responses \u2014 foundational neuroscience.' },

                  { id: 'sciatic_n', name: 'Sciatic Nerve', x: 0.32, y: 0.63, fn: 'Largest nerve. Runs along posterior thigh. Branches from sacral plexus (spinal nerves 8-9).', clinical: 'Frog sciatic nerve was the model system for early electrophysiology experiments.' },

                  { id: 'brachial_n', name: 'Brachial Nerves', x: 0.36, y: 0.36, fn: 'Spinal nerves 2-3 forming brachial plexus for forelimb. Smaller than sciatic due to smaller forelimb.', clinical: 'Smaller brachial vs larger sciatic reflects hindlimb dominance in frogs.' },

                  { id: 'cranial_n', name: 'Cranial Nerves', x: 0.57, y: 0.20, fn: '10 pairs (mammals have 12). Key: optic (II, large), trigeminal (V), vagus (X, viscera).', clinical: 'Frogs have 10 cranial nerve pairs vs 12 in mammals \u2014 lacking spinal accessory (XI) and hypoglossal (XII).' },

                  { id: 'optic_lobe', name: 'Optic Lobes (Tectum)', x: 0.52, y: 0.16, fn: 'Largest brain region processing visual "bug detector" neurons \u2014 respond to small moving dark objects.', clinical: 'Optic tectum "bug detectors" inspired early computer vision and AI motion detection algorithms.' }

                ]

              }

            },



            earthworm: {

              name: 'Earthworm (Lumbricus)', icon: '\uD83E\uDEB1',

              desc: 'Annelid \u2014 segmented body, closed circulation, 5 aortic arches, ventral nerve cord.',

              objectives: [
                'Trace the closed circulatory system through aortic arches',
                'Explain peristaltic locomotion via antagonistic muscles',
                'Identify nephridia and compare to vertebrate kidneys',
                'Describe the role of the clitellum in reproduction'
              ],
              objectiveTargets: [['aortic_arches'], ['circular_m', 'longitudinal_m'], ['nephridia'], ['clitellum']],
              specTerms: [
                { term: 'Peristalsis', def: 'Wave-like contraction of circular and longitudinal muscles for locomotion.' },
                { term: 'Nephridium', def: 'Excretory tubule in each segment; filters coelomic fluid similar to a kidney nephron.' },
                { term: 'Aortic arch', def: 'One of five paired muscular vessels that pump blood; analogous to a heart.' },
                { term: 'Clitellum', def: 'Glandular band that secretes a cocoon for egg deposition.' },
                { term: 'Setae', def: 'Chitinous bristles providing traction during locomotion.' }
              ],
              bodyShape: 'worm',

              layers: [

                { id: 'skin', name: 'Integument', icon: '\uD83D\uDFE4', color: '#a78bfa', accent: '#7c3aed', desc: 'Moist cuticle with setae for locomotion.' },

                { id: 'muscle', name: 'Body Wall', icon: '\uD83D\uDCAA', color: '#f87171', accent: '#dc2626', desc: 'Circular and longitudinal muscles for peristalsis.' },

                { id: 'organs', name: 'Internal Organs', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Digestive tube, aortic arches, nephridia.' },

                { id: 'nervous', name: 'Nervous System', icon: '\u26A1', color: '#c084fc', accent: '#7c3aed', desc: 'Ventral nerve cord with cerebral ganglia.' }

              ],

              organs: {

                skin: [

                  { id: 'cuticle', name: 'Cuticle', x: 0.52, y: 0.30, fn: 'Transparent outer covering. Keeps skin moist for gas exchange \u2014 earthworms breathe through skin.', clinical: 'Earthworms die if skin dries out \u2014 cutaneous respiration requires moisture.' },

                  { id: 'setae', name: 'Setae', x: 0.46, y: 0.55, fn: 'Tiny chitinous bristles (4 pairs/segment). Grip soil during peristaltic locomotion.', clinical: 'Setae are homologous to marine polychaete parapodia.' },

                  { id: 'clitellum', name: 'Clitellum', x: 0.51, y: 0.33, fn: 'Glandular band (segments 32-37). Secretes cocoon for eggs. Indicates sexual maturity.', clinical: 'Clitellum position varies by species \u2014 used for identification.' },

                  { id: 'prostomium', name: 'Prostomium', x: 0.51, y: 0.07, fn: 'Fleshy lip over mouth. Sensory: detects light, chemicals, vibrations. Not a true segment.', clinical: '"Worm grunting" exploits vibration sensitivity to harvest bait worms.' }

                ],

                muscle: [

                  { id: 'circular_m', name: 'Circular Muscles', x: 0.52, y: 0.40, fn: 'Outer layer running around each segment. Contraction = longer/thinner (elongation phase).', clinical: 'Same peristaltic mechanism humans use for intestinal movement.' },

                  { id: 'longitudinal_m', name: 'Longitudinal Muscles', x: 0.48, y: 0.50, fn: 'Inner layer along body length. Contraction = shorter/fatter (anchoring phase). Antagonistic to circular.', clinical: 'Hydrostatic skeleton (fluid-filled coelom) transmits force between the two layers.' },

                  { id: 'septa', name: 'Septa', x: 0.51, y: 0.45, fn: 'Muscular partitions between segments. Each segment = independent hydraulic unit.', clinical: 'Segmentation allows independent control \u2014 damage to one segment doesn\'t disable others.' }

                ],

                organs: [

                  { id: 'pharynx', name: 'Pharynx', x: 0.52, y: 0.15, fn: 'Muscular pump (segments 1-5). Sucks in soil and organic matter.', clinical: 'Earthworms eat their own weight in soil daily, aerating tons of soil per acre.' },

                  { id: 'crop', name: 'Crop', x: 0.51, y: 0.31, fn: 'Thin-walled storage chamber (segments 15-16). Temporary food storage.', clinical: 'Similar function to a bird\'s crop \u2014 convergent evolution of food storage.' },

                  { id: 'gizzard', name: 'Gizzard', x: 0.51, y: 0.36, fn: 'Thick muscular grinder (segments 17-18). Uses sand grains to crush food. No teeth.', clinical: 'Like a bird\'s gizzard \u2014 independent evolution of grit-grinding organs.' },

                  { id: 'intestine', name: 'Intestine', x: 0.50, y: 0.58, fn: 'Straight tube with typhlosole (dorsal fold increasing surface area). Digestion and absorption.', clinical: 'Typhlosole is a simple version of intestinal villi \u2014 same principle, different solution.' },

                  { id: 'aortic_arches', name: 'Aortic Arches (5 Hearts)', x: 0.52, y: 0.23, fn: '5 pairs of contractile vessels (segments 7-11). Pump blood in closed circulatory system.', clinical: 'Often called "5 hearts" \u2014 actually muscular blood vessels. Among first studied for closed circulation.' },

                  { id: 'nephridia', name: 'Nephridia', x: 0.54, y: 0.52, fn: 'Paired excretory organs per segment. Filter coelomic fluid. Equivalent of kidneys.', clinical: 'Segmentally repeated kidneys \u2014 unique annelid design with one pair per segment.' },

                  { id: 'seminal_v', name: 'Seminal Vesicles', x: 0.50, y: 0.25, fn: 'White organs (segments 9-12). Store sperm. Earthworms are hermaphrodites but cross-fertilize.', clinical: 'Despite being hermaphrodites, self-fertilization is rare \u2014 they mate with partners.' }

                ],

                nervous: [

                  { id: 'cerebral_g', name: 'Cerebral Ganglia', x: 0.51, y: 0.12, fn: 'Paired ganglia above pharynx (segment 3). Process sensory input. "Brain."', clinical: 'Very simple \u2014 a headless earthworm can still burrow, eat, and mate.' },

                  { id: 'ventral_cord', name: 'Ventral Nerve Cord', x: 0.50, y: 0.58, fn: 'Runs entire ventral length. Giant fibers enable rapid escape contraction (20-45 m/s).', clinical: 'Giant axons transmit signals fast \u2014 enabling rapid withdrawal when disturbed.' },

                  { id: 'segmental_g', name: 'Segmental Ganglia', x: 0.51, y: 0.44, fn: 'Paired ganglia per segment. Control local reflexes independently.', clinical: 'Each ganglion controls its segment \u2014 why cut segments still move.' }

                ]

              }

            },

            pig: {

              name: 'Fetal Pig (Sus scrofa)', icon: '\uD83D\uDC37',

              desc: 'Mammalian specimen with a four-chambered heart and diaphragm. Major organ systems support comparison with humans, while anatomy, proportions, and development remain species-specific.',

              bodyShape: 'pig',
              objectives: ['Compare the 4-chambered pig heart to a human heart','Identify the diaphragm and explain negative-pressure breathing','Trace the fetal circulatory pathway through the umbilical cord','Locate the spiral colon and compare to human large intestine'],
              objectiveTargets: [['heart_p'], ['diaphragm_p', 'lungs_p'], ['umbilical'], ['lg_int_p']],
              specTerms: [
                { term: 'Diaphragm', def: 'Dome-shaped respiratory muscle separating thorax from abdomen; mammalian innovation.' },
                { term: 'Umbilical vein', def: 'Carries oxygenated blood from placenta to fetus through the umbilical cord.' },
                { term: 'Spiral colon', def: 'Uniquely porcine coiled large intestine resembling a watch spring.' },
                { term: 'Urachus', def: 'Fetal canal connecting the bladder to the umbilicus; becomes the median umbilical ligament.' },
                { term: 'Xenotransplantation', def: 'Transplanting cells, tissues, or organs between species; pigs are studied as donor sources, but immune, infectious, and physiological barriers remain.' }
              ],

              layers: [

                { id: 'skin', name: 'Skin', icon: '\uD83E\uDDB4', color: '#fda4af', accent: '#e11d48', desc: 'Thin skin with hair follicles and umbilical cord.' },

                { id: 'muscle', name: 'Musculature', icon: '\uD83D\uDCAA', color: '#f87171', accent: '#dc2626', desc: 'Mammalian muscle groups useful for comparison; attachments, proportions, and posture-related organization differ from humans.' },

                { id: 'organs', name: 'Visceral Organs', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Mammalian visceral organs with important porcine features, including distinct lobation, vessels, and a spiral colon.' },

                { id: 'skeleton', name: 'Skeleton', icon: '\uD83E\uDDB4', color: 'var(--allo-stem-text, #e2e8f0)', accent: '#94a3b8', desc: 'Largely cartilaginous fetal skeleton.' },

                { id: 'nervous', name: 'Nervous', icon: '\u26A1', color: '#c084fc', accent: '#7c3aed', desc: 'Complex mammalian CNS with cerebral cortex.' }

              ],

              organs: {

                skin: [

                  { id: 'epidermis_p', name: 'Epidermis', x: 0.53, y: 0.34, fn: 'Stratified squamous epithelium. Pig skin is widely used as a comparative model, while structure and healing remain species-specific.', clinical: 'Pig skin is used in burn-treatment research and graft studies; model suitability does not imply identity with human skin.' },

                  { id: 'umbilical', name: 'Umbilical Cord', x: 0.51, y: 0.58, fn: 'Two umbilical arteries and one umbilical vein connect fetal circulation to the placenta. Interpret the surrounding cord and fetal vessels as porcine anatomy.', clinical: 'Humans also typically have two umbilical arteries and one vein, but comparative interpretation must retain species and developmental context.' },

                  { id: 'mammary', name: 'Mammary Papillae', x: 0.55, y: 0.56, fn: '6-7 pairs along ventral surface (vs 1 pair in humans). Along embryonic "milk line."', clinical: 'Supernumerary nipples occur in 1-5% of humans along the vestigial milk line.' }

                ],

                muscle: [

                  { id: 'diaphragm_p', name: 'Diaphragm', x: 0.47, y: 0.43, fn: 'Dome-shaped muscle separating thorax and abdomen and driving ventilation. Attachment pattern and proportions are porcine, not human.', clinical: 'The diaphragm supports negative-pressure breathing in both species, while its anatomy must be interpreted in the specimen\'s own body plan.' },

                  { id: 'masseter_p', name: 'Masseter', x: 0.26, y: 0.42, fn: 'Powerful jaw-closing muscle shaped by the pig\'s skull and feeding mechanics.', clinical: 'Homologous to the human masseter, while size, orientation, and mechanical demands differ between species.' },

                  { id: 'ext_oblique_p', name: 'External Oblique', x: 0.58, y: 0.46, fn: 'Broad abdominal-wall muscle that supports and compresses the viscera and contributes to trunk movement.', clinical: 'Useful for comparing mammalian body-wall organization; fiber direction, attachments, and innervation should be treated as porcine anatomy.' }

                ],

                organs: [

                  { id: 'heart_p', name: 'Heart (4-chamber)', x: 0.44, y: 0.44, fn: 'Four chambers separate pulmonary and systemic circulation. The porcine heart shares this mammalian plan with humans, while atrial, venous, ventricular, and great-vessel anatomy differs.', clinical: 'Processed porcine valve tissue is used in bioprosthetic valves, and genetically modified pig hearts are studied for xenotransplantation; species differences remain clinically important.' },

                  { id: 'lungs_p', name: 'Lungs', x: 0.41, y: 0.42, fn: 'Alveolated, lobed lungs. The right lung has cranial, middle, caudal, and accessory lobes, and a tracheal bronchus supplies the cranial lobe; human lobation and branching differ.', clinical: 'Pig lungs are used in research and training, but their lobation and bronchial tree must not be treated as human-identical.' },

                  { id: 'liver_p', name: 'Liver', x: 0.55, y: 0.44, fn: 'Five gross lobes, with a more visibly lobated form than the human liver. Core roles include bile production, metabolism, protein synthesis, and glycogen storage.', clinical: 'Pig liver is studied as a comparative and transplantation model; shared roles do not imply identical anatomy or physiology.' },

                  { id: 'stomach_p', name: 'Stomach', x: 0.54, y: 0.49, fn: 'Monogastric stomach with cardiac, fundic, and pyloric regions that produces acid and pepsin. Shape and mucosal regions remain species-specific.', clinical: 'The porcine stomach is a useful monogastric comparison, not a direct substitute for human gastric anatomy.' },

                  { id: 'sm_int_p', name: 'Small Intestine', x: 0.59, y: 0.55, fn: 'Long (~15m adult). Duodenum, jejunum, ileum. Villi for nutrient absorption.', clinical: 'Proportionally longer than human \u2014 used for surgical anastomosis training.' },

                  { id: 'lg_int_p', name: 'Spiral Colon', x: 0.65, y: 0.53, fn: 'Distinctive spiral colon (coiled). Cecum present. Absorbs water.', clinical: 'Spiral colon is uniquely porcine \u2014 coiled like a watch spring.' },

                  { id: 'kidneys_p', name: 'Kidneys', x: 0.62, y: 0.39, fn: 'Bean-shaped, retroperitoneal, multipyramidal kidneys that filter blood and regulate fluid and electrolytes. Vascular and collecting-system details are porcine.', clinical: 'Pig kidneys are studied for xenotransplantation, but immune compatibility, infection risk, and physiological fit require dedicated evaluation.' },

                  { id: 'thymus_p', name: 'Thymus', x: 0.36, y: 0.39, fn: 'Enormous in fetus (much larger than adult). T-cell maturation. Extends from mediastinum into neck.', clinical: 'Fetal thymus demonstrates critical early immune development role.' },

                  { id: 'pancreas_p', name: 'Pancreas', x: 0.60, y: 0.53, fn: 'Produces exocrine digestive enzymes and the endocrine hormones insulin and glucagon. Gross form, ducts, and tissue properties are porcine.', clinical: 'Porcine insulin treated human diabetes before recombinant human insulin; shared hormone function does not make the organs anatomically identical.' },

                  { id: 'bladder_p', name: 'Urinary Bladder', x: 0.67, y: 0.53, fn: 'Large distensible organ. Allantoic bladder with urachus in fetus.', clinical: 'Patent urachus is a congenital anomaly in both pigs and humans.' }

                ],

                skeleton: [

                  { id: 'skull_p', name: 'Skull', x: 0.24, y: 0.40, fn: 'Elongated snout and a largely cartilaginous fetal skeleton. Cranial proportions and developmental landmarks differ substantially from humans.', clinical: 'Use the skull to compare homologous mammalian bones, not to transfer human measurements or spatial relationships directly.' },

                  { id: 'vert_col_p', name: 'Vertebral Column', x: 0.52, y: 0.35, fn: '7C, 14-15T, 6-7L, 4S, 20-23 caudal. More vertebrae than human. 7 cervical constant across mammals.', clinical: 'Cervical count (7) is constant across nearly all mammals \u2014 giraffe to mouse.' },

                  { id: 'ribs_p', name: 'Ribs', x: 0.43, y: 0.39, fn: '14-15 pairs (vs 12 human). Cartilaginous in fetus. Protect thoracic organs.', clinical: '"Spare ribs" in cooking come from this ventral rib region.' },

                  { id: 'pelvis_p', name: 'Pelvis', x: 0.68, y: 0.43, fn: 'Ilium, ischium, pubis — largely cartilaginous in fetus. Same tripartite structure as human pelvis but horizontally oriented for quadrupedal stance.', clinical: 'Pig pelvis is oriented horizontally vs vertically in bipedal humans — key comparative anatomy difference.' },

                  { id: 'scapula_p', name: 'Scapula', x: 0.36, y: 0.39, fn: 'Triangular shoulder blade with prominent spine and acromion. Cartilaginous in fetus. Attachment for supraspinatus and infraspinatus muscles.', clinical: 'Pig scapula is more vertically oriented than human — adaptation for quadrupedal weight bearing.' },

                  { id: 'humerus_p', name: 'Forelimb Bones', x: 0.37, y: 0.55, fn: 'Humerus, radius, and ulna articulate with the shoulder and carpus; the ulna has a prominent olecranon for triceps attachment.', clinical: 'These are homologous mammalian bone elements, while proportions, loading, and joint orientation reflect quadrupedal locomotion.' },

                  { id: 'femur_p', name: 'Hindlimb Bones', x: 0.68, y: 0.55, fn: 'Femur (thigh), tibia and fibula (leg). Terminates in cloven hoof (digits III and IV). Other digits are vestigial dewclaws.', clinical: 'Pig walks on digits III-IV — an even-toed ungulate (Artiodactyla). Humans walk on entire foot sole.' },

                  { id: 'sternum_p', name: 'Sternum', x: 0.45, y: 0.47, fn: 'Segmented breastbone with 6-7 sternebrae. Largely cartilaginous in fetus with ossification centers. Ribs articulate laterally.', clinical: 'Segmented pig sternum reveals the fetal ossification process — each sternebra starts as cartilage.' }

                ],

                nervous: [

                  { id: 'brain_p', name: 'Brain', x: 0.24, y: 0.39, fn: 'Mammalian brain with sulci, gyri, and prominent olfactory bulbs. Regional proportions and organization differ from humans.', clinical: 'Pig brains are used in some research and procedural training because of useful scale and folding, but they are not human anatomical substitutes.' },

                  { id: 'spinal_p', name: 'Spinal Cord', x: 0.50, y: 0.36, fn: 'Extends along the fetal vertebral canal with cervical and lumbar enlargements and the shared vertebrate organization of gray and white matter. Proportions and tract anatomy are species-specific.', clinical: 'Dorsal sensory and ventral motor organization is a broad vertebrate plan; do not transfer human levels or measurements directly.' },

                  { id: 'vagus_p', name: 'Vagus Nerve (CN X)', x: 0.35, y: 0.40, fn: 'Longest cranial nerve. Heart, lungs, GI innervation. Runs with carotid/jugular.', clinical: 'Pig vagus nerve studies led to human vagus nerve stimulator implants for epilepsy.' }

                ]

              }

            },



            perch: {

              name: 'Perch (Perca)', icon: '\uD83D\uDC1F',

              desc: 'Bony fish \u2014 gills, swim bladder, lateral line, 2-chambered heart.',

              bodyShape: 'fish',
              objectives: ['Explain countercurrent flow in gill respiration','Trace single-circuit circulation through the 2-chambered heart','Identify the swim bladder and explain buoyancy regulation','Describe the lateral line sensory system'],
              objectiveTargets: [['gills'], ['heart_f', 'gills'], ['swim_bladder'], ['lat_line']],
              specTerms: [
                { term: 'Operculum', def: 'Bony gill cover that protects gills and actively pumps water for respiration.' },
                { term: 'Countercurrent exchange', def: 'Blood flows opposite to water across gills, maximizing O\u2082 extraction (~80%).' },
                { term: 'Swim bladder', def: 'Gas-filled organ homologous to lungs; provides neutral buoyancy without effort.' },
                { term: 'Lateral line', def: 'Sensory system detecting water pressure changes for navigation and predator detection.' },
                { term: 'Pyloric ceca', def: 'Finger-like pouches at stomach-intestine junction unique to fish; increase absorption area.' }
              ],

              layers: [

                { id: 'skin', name: 'Scales & Skin', icon: '\uD83D\uDFE1', color: '#fde68a', accent: '#d97706', desc: 'Ctenoid scales with mucus coating and lateral line.' },

                { id: 'muscle', name: 'Musculature', icon: '\uD83D\uDCAA', color: '#f87171', accent: '#dc2626', desc: 'W-shaped myomeres for undulatory swimming.' },

                { id: 'organs', name: 'Internal Organs', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Swim bladder, gills, 2-chambered heart, pyloric ceca.' },

                { id: 'skeleton', name: 'Skeleton', icon: '\uD83E\uDDB4', color: 'var(--allo-stem-text, #e2e8f0)', accent: '#94a3b8', desc: 'Ossified skeleton with fin rays and operculum.' }

              ],

              organs: {

                skin: [

                  { id: 'scales', name: 'Ctenoid Scales', x: 0.52, y: 0.43, fn: 'Overlapping bony scales with growth rings \u2014 used to determine fish age like tree rings.', clinical: 'Scale annuli counting is the standard method for aging fish in fisheries biology.' },

                  { id: 'lat_line', name: 'Lateral Line', x: 0.53, y: 0.445, fn: 'Sensory system detecting water pressure changes. Enables schooling, predator detection, murky-water navigation.', clinical: 'A "sixth sense" unique to fish/aquatic amphibians \u2014 no equivalent in terrestrial vertebrates.' },

                  { id: 'operculum', name: 'Operculum', x: 0.31, y: 0.44, fn: 'Bony gill cover. Protects gills and pumps water for respiration.', clinical: 'Only bony fish have opercula \u2014 sharks have exposed gill slits.' },

                  { id: 'fins_ext', name: 'Fins (7 types)', x: 0.58, y: 0.27, fn: 'Dorsal (spiny + soft), caudal, anal, pelvic, pectoral. Pectoral/pelvic are homologous to tetrapod limbs.', clinical: 'Fish fins are evolutionary precursors of tetrapod limbs \u2014 pectoral=arms, pelvic=legs.' }

                ],

                muscle: [

                  { id: 'myomeres', name: 'Myomeres', x: 0.53, y: 0.43, fn: 'W-shaped muscle blocks separated by myosepta. Contract in waves for swimming. White (fast) + red (slow) fibers.', clinical: 'Visible as "flakes" in cooked fish \u2014 each flake is one myomere.' },

                  { id: 'epaxial', name: 'Epaxial Muscles', x: 0.52, y: 0.36, fn: 'Dorsal muscle mass above lateral septum. Main swimming power. Bulk of body musculature.', clinical: 'This is the "fillet" \u2014 mostly epaxial muscle, the main edible portion.' }

                ],

                organs: [

                  { id: 'gills', name: 'Gills', x: 0.31, y: 0.44, fn: '4 gill arches with filaments/lamellae. Countercurrent flow extracts 80% of dissolved O\u2082.', clinical: 'Fish gills extract proportionally more oxygen from water than lungs from air.' },

                  { id: 'heart_f', name: 'Heart (2-chamber)', x: 0.32, y: 0.51, fn: '1 atrium + 1 ventricle. Single-circuit circulation: heart\u2192gills\u2192body\u2192heart.', clinical: 'Simplest vertebrate heart. Evolution: 2 (fish) \u2192 3 (amphibian) \u2192 4 (mammal/bird).' },

                  { id: 'swim_bladder', name: 'Swim Bladder', x: 0.51, y: 0.40, fn: 'Gas-filled sac for buoyancy. Closed type \u2014 gas secreted/absorbed via rete mirabile. Neutral buoyancy without effort.', clinical: 'Homologous to the tetrapod lung \u2014 both evolved from pharyngeal outpocketing in ancestral fish.' },

                  { id: 'liver_f', name: 'Liver', x: 0.40, y: 0.47, fn: 'Large lobed organ. Bile production, glycogen/lipid storage.', clinical: 'Fish liver oil is a concentrated energy reserve \u2014 cod liver oil rich in vitamins A and D.' },

                  { id: 'stomach_f', name: 'Stomach', x: 0.46, y: 0.50, fn: 'J-shaped. HCl + pepsin. Expandable for large prey.', clinical: 'Not all fish have stomachs \u2014 carp and minnows lack them entirely.' },

                  { id: 'pyloric_ceca', name: 'Pyloric Ceca', x: 0.50, y: 0.53, fn: 'Finger-like pouches at stomach-intestine junction (3-5 in perch). Increase absorption area.', clinical: 'Unique to fish \u2014 no mammalian homolog. Number used in species identification.' },

                  { id: 'kidneys_f', name: 'Kidneys', x: 0.52, y: 0.37, fn: 'Dark elongated organs along dorsal wall. Head kidney (immune) + trunk kidney (excretion).', clinical: 'Freshwater fish excrete dilute urine \u2014 constantly fighting water influx through gills.' },

                  { id: 'gonads_f', name: 'Gonads', x: 0.59, y: 0.46, fn: 'Paired dorsal organs. External fertilization. Ovaries can be 20-30% of body weight when full.', clinical: 'Enormous reproductive investment \u2014 some fish produce millions of eggs per spawning.' }

                ],

                skeleton: [

                  { id: 'skull_f', name: 'Skull', x: 0.25, y: 0.44, fn: 'Complex with 60+ separate bones \u2014 more than any other vertebrate class.', clinical: 'Fish skulls have the most individual bones of any vertebrate group.' },

                  { id: 'vert_col_f', name: 'Vertebral Column', x: 0.54, y: 0.43, fn: 'Neural arches (spinal cord) + hemal arches (caudal vessels). Trunk + caudal regions only.', clinical: 'No distinct cervical/thoracic/lumbar regions \u2014 the "neck" is a tetrapod innovation.' },

                  { id: 'fin_rays', name: 'Fin Rays', x: 0.58, y: 0.27, fn: 'Spiny (hard, sharp, defense) and soft (segmented, flexible) rays support fin membranes.', clinical: 'Perch spiny rays are sharp enough to puncture skin \u2014 defensive adaptation.' }

                ]

              }

            },

            crayfish: {

              name: 'Crayfish (Cambarus)', icon: '\uD83E\uDD9E',

              desc: 'Crustacean \u2014 exoskeleton, compound eyes, gills, open circulatory system, gastric mill.',

              bodyShape: 'crayfish',
              objectives: ['Compare open circulatory system to closed circulation in vertebrates','Identify the gastric mill and explain post-ingestion grinding','Explain ecdysis (molting) and the role of gastroliths','Describe the compound eye structure and motion detection'],
              objectiveTargets: [['heart_c'], ['gastric_mill'], ['carapace', 'gastric_mill'], ['compound_eye']],
              specTerms: [
                { term: 'Hemolymph', def: 'Open circulatory fluid combining blood and interstitial fluid; flows through sinuses, not vessels.' },
                { term: 'Ecdysis', def: 'Periodic molting of the exoskeleton to allow growth; animal is vulnerable during hardening.' },
                { term: 'Gastrolith', def: 'Calcium carbonate deposit in the stomach; dissolved during molting to harden new exoskeleton.' },
                { term: 'Ommatidium', def: 'Individual visual unit in compound eyes; ~3,000 per eye, excellent at motion detection.' },
                { term: 'Statocyst', def: 'Balance organ at the base of the antennules containing a sand grain that shifts with gravity.' }
              ],

              layers: [

                { id: 'skin', name: 'Exoskeleton', icon: '\uD83D\uDEE1\uFE0F', color: '#ef4444', accent: '#b91c1c', desc: 'Chitinous shell hardened with calcium carbonate.' },

                { id: 'muscle', name: 'Musculature', icon: '\uD83D\uDCAA', color: '#f87171', accent: '#dc2626', desc: 'Muscles attached inside exoskeleton.' },

                { id: 'organs', name: 'Internal Organs', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Open circulatory system, gastric mill, green glands.' },

                { id: 'nervous', name: 'Nervous System', icon: '\u26A1', color: '#c084fc', accent: '#7c3aed', desc: 'Ventral nerve cord, compound eyes, antennae.' }

              ],

              organs: {

                skin: [

                  { id: 'carapace', name: 'Carapace', x: 0.47, y: 0.37, fn: 'Fused dorsal shell covering cephalothorax. Chitin + CaCO\u2083. Must molt (ecdysis) to grow.', clinical: 'Soft-shell stage after molting makes them vulnerable \u2014 many crustaceans hide while hardening.' },

                  { id: 'telson', name: 'Telson & Uropods', x: 0.89, y: 0.45, fn: 'Tail fan for escape response ("tail flip"). Swims backward at 2 m/s in milliseconds.', clinical: 'Tail flip is one of the fastest animal movements \u2014 mediated by giant nerve fibers.' },

                  { id: 'chelipeds', name: 'Chelipeds (Claws)', x: 0.19, y: 0.62, fn: 'First walking legs modified into pincers. Defense, feeding, territorial displays. Can regenerate if lost.', clinical: 'Lost claws regenerate over several molts \u2014 remarkable crustacean regenerative ability.' },

                  { id: 'compound_eye', name: 'Compound Eyes', x: 0.27, y: 0.35, fn: '~3,000 ommatidia per eye on moveable stalks. Motion detection specialist.', clinical: 'Compound eyes excel at motion detection but have lower resolution than vertebrate eyes.' }

                ],

                muscle: [

                  { id: 'flexor_m', name: 'Abdominal Flexors', x: 0.72, y: 0.48, fn: 'Powerful ventral muscles for escape tail-flip. Among fastest muscle contractions in animal kingdom.', clinical: 'This is the edible "crawfish tail" \u2014 these flexor muscles are the commercial meat.' },

                  { id: 'extensor_m', name: 'Abdominal Extensors', x: 0.72, y: 0.41, fn: 'Dorsal muscles returning abdomen to rest position. Slower than flexors.', clinical: 'Antagonistic flexor/extensor system works same as in vertebrate limbs.' },

                  { id: 'cheliped_m', name: 'Cheliped Muscles', x: 0.24, y: 0.58, fn: 'Closer (adductor) much larger than opener. Crushing force >50N in large specimens.', clinical: 'Claw joint leverages amplify muscle force \u2014 a biological lever system.' }

                ],

                organs: [

                  { id: 'heart_c', name: 'Heart', x: 0.54, y: 0.40, fn: 'Single-chambered dorsal heart. Open circulatory system \u2014 hemolymph flows through sinuses, not vessels.', clinical: 'Low-pressure open circulation limits crustacean maximum body size.' },

                  { id: 'gills_c', name: 'Gills', x: 0.47, y: 0.35, fn: 'Feather-like gills in branchial chamber. Gill bailer creates water current. Attached to leg bases.', clinical: 'Walking legs ventilate gills \u2014 movement and breathing are linked.' },

                  { id: 'gastric_mill', name: 'Gastric Mill', x: 0.38, y: 0.45, fn: '3 calcified teeth (ossicles) inside stomach. Grind food after swallowing. Gastroliths store calcium for molting.', clinical: '"Teeth in the stomach" \u2014 crustaceans chew food after eating it.' },

                  { id: 'green_gland', name: 'Green Glands', x: 0.30, y: 0.39, fn: 'Excretory organs at antenna base. Filter hemolymph, produce urine. Equivalent of kidneys.', clinical: 'Excrete ammonia directly \u2014 possible because aquatic environments flush waste.' },

                  { id: 'hepato', name: 'Hepatopancreas', x: 0.48, y: 0.47, fn: 'Combined liver + pancreas. Digestive enzymes, nutrient absorption, energy storage. Largest internal organ.', clinical: 'Called "tomalley" in lobster cuisine. Accumulates toxins in polluted waters.' },

                  { id: 'gonads_c', name: 'Gonads', x: 0.57, y: 0.44, fn: 'Dorsal to hepatopancreas. Females carry eggs on swimmerets ("berried" females).', clinical: 'Males identified by modified first swimmerets (gonopods) for sperm transfer.' }

                ],

                nervous: [

                  { id: 'brain_c', name: 'Supraesophageal Ganglion', x: 0.34, y: 0.44, fn: 'Fused ganglia above esophagus. Processes eyes, antennae input. Supports learning and social hierarchies.', clinical: 'Simple brain but complex behavior \u2014 crayfish establish dominance hierarchies.' },

                  { id: 'ventral_c', name: 'Ventral Nerve Cord', x: 0.68, y: 0.48, fn: 'Double cord with giant fibers mediating escape. Segmental ganglia control appendages.', clinical: 'Crayfish giant axons were foundational to neuroscience \u2014 among first where action potentials recorded.' },

                  { id: 'antennae_n', name: 'Antennae & Antennules', x: 0.19, y: 0.32, fn: 'Long pair: touch/taste. Short pair: chemoreception + balance (statocyst).', clinical: 'Classic experiment: iron filings in statocyst + magnet = inverted orientation.' }

                ]

              }

            },

            sheepEye: {

              name: 'Sheep Eye', icon: '\uD83D\uDC41\uFE0F',

              desc: 'Mammalian eye with cornea, lens, retina, and vitreous humor. Sheep-specific features include a tapetum lucidum and a horizontal retinal specialization rather than a human fovea.',

              bodyShape: 'eye',
              objectives: ['Trace the path of light through the eye from cornea to retina','Explain accommodation and the role of the ciliary body','Compare the tapetum lucidum in sheep to the human eye','Identify the blind spot and explain why it exists'],
              objectiveTargets: [['cornea', 'lens', 'retina'], ['lens', 'ciliary_body'], ['tapetum', 'retina'], ['optic_nerve']],
              specTerms: [
                { term: 'Accommodation', def: 'Process by which the ciliary muscle changes lens shape to focus on near or far objects.' },
                { term: 'Tapetum lucidum', def: 'Reflective layer behind the retina enhancing night vision; absent in humans.' },
                { term: 'Human fovea comparison', def: 'Humans use a cone-dense fovea for sharp central vision; sheep retinal specialization differs and should not be treated as identical.' },
                { term: 'Aqueous humor', def: 'Clear fluid in the anterior chamber; produced by ciliary body, drains via trabecular meshwork.' },
                { term: 'Vitreous humor', def: 'Clear gel filling the posterior 80% of the eye; maintains shape, does not regenerate.' }
              ],

              layers: [

                { id: 'skin', name: 'External', icon: '\uD83D\uDC41\uFE0F', color: '#93c5fd', accent: '#2563eb', desc: 'Outer structures: cornea, sclera, muscles, optic nerve.' },

                { id: 'organs', name: 'Internal', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Internal structures: lens, iris, retina, humors.' }

              ],

              organs: {

                skin: [

                  { id: 'cornea', name: 'Cornea', x: 0.20, y: 0.45, fn: 'Transparent anterior surface. Provides 2/3 of refractive power. Avascular \u2014 nourished by aqueous humor and tears. 5 layers.', clinical: 'LASIK reshapes the cornea with laser. Corneal transplants are the most common transplant surgery worldwide.' },

                  { id: 'sclera', name: 'Sclera', x: 0.72, y: 0.28, fn: 'Tough white outer coat. Dense connective tissue protecting eye contents. Attachment for extraocular muscles. "White of the eye."', clinical: 'Yellow sclera (scleral icterus) = jaundice from liver disease. Blue sclera = osteogenesis imperfecta.' },

                  { id: 'optic_nerve', name: 'Optic Nerve', x: 0.84, y: 0.51, fn: '~1.2 million retinal ganglion cell axons. Exits at optic disc (blind spot). Surrounded by meninges extension.', clinical: 'Optic disc has no photoreceptors = blind spot. Papilledema (optic disc swelling) = increased intracranial pressure.' },

                  { id: 'ext_muscles', name: 'Extraocular Muscles', x: 0.67, y: 0.17, fn: '6 muscles control eye movement: 4 rectus (sup/inf/med/lat) + 2 oblique (sup/inf). Cranial nerves III, IV, VI.', clinical: 'CN III palsy: eye "down and out," ptosis. CN IV: difficulty looking down stairs. CN VI: medial deviation.' },

                  { id: 'conjunctiva', name: 'Conjunctiva', x: 0.27, y: 0.30, fn: 'Thin mucous membrane lining eyelids (palpebral) and covering sclera (bulbar). Produces mucin for tear film.', clinical: 'Conjunctivitis ("pink eye") = inflamed conjunctiva. Subconjunctival hemorrhage looks alarming but is usually benign.' },

                  { id: 'fat_pad', name: 'Orbital Fat', x: 0.72, y: 0.68, fn: 'Cushions and insulates the eye within the orbit. Acts as shock absorber.', clinical: 'Orbital fat atrophy causes sunken eyes (enophthalmos). Graves disease causes fat expansion \u2192 proptosis.' }

                ],

                organs: [

                  { id: 'iris', name: 'Iris & Pupil', x: 0.33, y: 0.43, fn: 'Pigmented muscular diaphragm. Dilator (sympathetic) and sphincter (parasympathetic) muscles control pupil size. Regulates light entry.', clinical: 'Anisocoria (unequal pupils): may indicate CN III palsy, Horner syndrome, or elevated ICP. Iris color from melanin amount.' },

                  { id: 'lens', name: 'Crystalline Lens', x: 0.39, y: 0.45, fn: 'Biconvex, transparent, avascular. Changes shape for focusing (accommodation). Held by zonular fibers attached to ciliary body. Contains crystallin proteins.', clinical: 'Cataracts = lens clouding (most common surgery worldwide). Presbyopia = lens stiffening with age.' },

                  { id: 'ciliary_body', name: 'Ciliary Body', x: 0.35, y: 0.31, fn: 'Ring of muscle + epithelium. Ciliary muscle changes lens shape for focusing. Epithelium produces aqueous humor.', clinical: 'Glaucoma: excess aqueous humor \u2192 increased IOP \u2192 optic nerve damage. Treated with drugs reducing production.' },

                  { id: 'retina', name: 'Retina', x: 0.69, y: 0.35, fn: 'Neural sensory tissue containing rods and cones. In the sheep specimen, observe the retina as a delicate layer against the choroid; avoid applying human fovea and macula measurements directly to sheep anatomy.', clinical: 'Retinal detachment = surgical emergency. Diabetic retinopathy. Macular degeneration = leading cause of blindness in elderly.' },

                  { id: 'tapetum', name: 'Tapetum Lucidum', x: 0.67, y: 0.27, fn: 'Reflective layer behind retina in sheep (absent in humans). Reflects light back through retina for enhanced night vision. Causes "eyeshine."', clinical: 'Present in many animals (cats, dogs, sheep) but not humans or pigs. This is why animal eyes glow in headlights.' },

                  { id: 'vitreous', name: 'Vitreous Humor', x: 0.57, y: 0.47, fn: 'Clear gel filling posterior 80% of eye. Maintains eye shape. 99% water + collagen + hyaluronic acid. Does not regenerate.', clinical: 'Floaters = collagen clumps in vitreous. Posterior vitreous detachment common with aging.' },

                  { id: 'aqueous', name: 'Aqueous Humor', x: 0.28, y: 0.49, fn: 'Clear fluid in anterior/posterior chambers (in front of lens). Produced by ciliary body, drains via trabecular meshwork at angle.', clinical: 'Blocked drainage \u2192 increased IOP \u2192 glaucoma. Acute angle-closure = emergency.' },

                  { id: 'choroid', name: 'Choroid', x: 0.73, y: 0.57, fn: 'Vascular layer between sclera and retina. Blood supply for outer retina. Heavily pigmented to absorb stray light.', clinical: 'Choroidal melanoma is the most common primary intraocular malignancy in adults.' }

                ]

              }

            },

            sheepHeart: {

              name: 'Sheep Heart', icon: '\u2764\uFE0F',

              desc: 'Organ dissection \u2014 a four-chambered mammalian heart with a body plan useful for comparison to humans. Vessel branching, size, rate, and pressure remain species-specific.',

              bodyShape: 'heart',
              objectives: ['Trace blood flow through all four chambers and valves','Distinguish the coronary arteries and explain myocardial blood supply','Compare wall thickness of left vs right ventricles','Identify the conduction system pathway from SA node to Purkinje fibers'],
              objectiveTargets: [['ra', 'tricuspid', 'rv', 'pulm_v', 'la', 'mitral', 'lv', 'aortic_v'], ['coronary_aa'], ['rv', 'lv'], ['conduction']],
              specTerms: [
                { term: 'Chordae tendineae', def: 'Fibrous cords connecting AV valve leaflets to papillary muscles, preventing prolapse.' },
                { term: 'Coronary sinus', def: 'Large venous channel collecting deoxygenated blood from the heart muscle itself.' },
                { term: 'Purkinje fibers', def: 'Specialized conduction cells distributing electrical impulse to ventricular myocardium.' },
                { term: 'Papillary muscle', def: 'Muscular projections anchoring chordae tendineae; contract during systole to keep valves closed.' },
                { term: 'Pericardium', def: 'Double-walled sac enclosing the heart; fibrous outer layer and serous inner layer with lubricating fluid.' }
              ],

              layers: [

                { id: 'skin', name: 'External', icon: '\u2764\uFE0F', color: '#fca5a5', accent: '#dc2626', desc: 'Pericardium, great vessels, coronary arteries, surface anatomy.' },

                { id: 'organs', name: 'Internal', icon: '\uD83E\uDEC1', color: '#fbbf24', accent: '#d97706', desc: 'Chambers, valves, septum, chordae tendineae.' }

              ],

              organs: {

                skin: [

                  { id: 'pericardium', name: 'Pericardium', x: 0.50, y: 0.32, fn: 'Double-walled sac enclosing heart. Fibrous (outer, tough) and serous (inner, 2 layers with fluid). Anchors heart. 15-50mL pericardial fluid reduces friction.', clinical: 'Pericarditis: inflammation causing chest pain. Cardiac tamponade: fluid compresses heart = emergency. Beck triad: hypotension, JVD, muffled sounds.' },

                  { id: 'aorta_h', name: 'Aorta', x: 0.54, y: 0.12, fn: 'Largest artery. The ascending aorta exits the left ventricle and distributes oxygenated blood to the body. Aortic-arch branch patterns differ between sheep and humans.', clinical: 'Aortic aneurysm: >5.5cm \u2192 surgical repair risk. Aortic dissection: tearing pain, emergency surgery.' },

                  { id: 'pulm_trunk', name: 'Pulmonary Trunk', x: 0.62, y: 0.20, fn: 'Exits RV, bifurcates into R and L pulmonary arteries carrying deoxygenated blood to lungs. Only arteries carrying deoxy blood.', clinical: 'Pulmonary embolism: clot lodges here. Saddle PE across bifurcation is life-threatening.' },

                  { id: 'coronary_aa', name: 'Coronary Arteries', x: 0.49, y: 0.45, fn: 'Left main \u2192 LAD + circumflex. Right coronary artery (RCA). Supply myocardium with oxygenated blood. First aortic branches.', clinical: 'LAD = "widow maker." Coronary artery disease is #1 cause of death. CABG bypasses blockages using vein/artery grafts.' },

                  { id: 'sup_vena_h', name: 'Cranial (Superior) Vena Cava', x: 0.69, y: 0.14, fn: 'Returns deoxygenated blood from upper body to RA. Formed by brachiocephalic veins.', clinical: 'SVC syndrome from lung cancer: facial swelling and dyspnea.' },

                  { id: 'inf_vena_h', name: 'Caudal (Inferior) Vena Cava', x: 0.69, y: 0.66, fn: 'Returns blood from lower body to RA. Largest vein.', clinical: 'IVC filter prevents PE from DVT. Compression in pregnancy causes supine hypotension.' },

                  { id: 'apex', name: 'Apex', x: 0.45, y: 0.73, fn: 'Inferior tip of heart formed by LV. Points left and anterior. PMI (point of maximum impulse) at 5th intercostal space, midclavicular line.', clinical: 'Displaced PMI = ventricular enlargement. PMI palpation is key clinical exam finding.' }

                ],

                organs: [

                  { id: 'ra', name: 'Right Atrium', x: 0.59, y: 0.35, fn: 'Receives deoxygenated blood from SVC (upper body), IVC (lower body), and coronary sinus (heart). Thin-walled. SA node here sets heart rhythm.', clinical: 'SA node = "pacemaker of the heart" \u2014 sets sinus rhythm at 60-100 bpm. Atrial fibrillation: chaotic atrial activity.' },

                  { id: 'rv', name: 'Right Ventricle', x: 0.57, y: 0.54, fn: 'Pumps blood to the lungs through the pulmonary trunk. Its wall is thinner than the left ventricle because it works against lower pulmonary resistance; exact pressures vary by species and condition.', clinical: 'RV failure from pulmonary hypertension or massive PE. RV infarction from RCA occlusion.' },

                  { id: 'la', name: 'Left Atrium', x: 0.43, y: 0.34, fn: 'Receives oxygenated blood from 4 pulmonary veins. Smooth walled. Left atrial appendage is common site of thrombus formation in AFib.', clinical: 'Atrial appendage clots in AFib = stroke risk. Anticoagulation or appendage occlusion devices prevent this.' },

                  { id: 'lv', name: 'Left Ventricle', x: 0.43, y: 0.55, fn: 'The thick-walled systemic pump. It sends oxygenated blood through the aorta; its muscular wall is substantially thicker than the right ventricle. Exact pressure values vary by species and condition.', clinical: 'LV hypertrophy from chronic hypertension or aortic stenosis. LV ejection fraction (normal 55-70%) = key cardiac metric.' },

                  { id: 'tricuspid', name: 'Tricuspid Valve', x: 0.57, y: 0.43, fn: 'AV valve with 3 cusps between RA and RV. Chordae tendineae attach to papillary muscles preventing prolapse during systole.', clinical: 'Tricuspid regurgitation: blood leaks backward. Endocarditis in IV drug users often affects tricuspid.' },

                  { id: 'mitral', name: 'Mitral (Bicuspid) Valve', x: 0.44, y: 0.43, fn: 'AV valve with 2 cusps between LA and LV. Most commonly affected valve in rheumatic heart disease. "Bicuspid" = 2 leaflets.', clinical: 'Mitral stenosis from rheumatic fever. Mitral valve prolapse (MVP) in 2-3% of population. "Lub" = AV valves closing.' },

                  { id: 'aortic_v', name: 'Aortic Valve', x: 0.48, y: 0.29, fn: '3 semilunar cusps at LV-aorta junction. Opens during systole for ejection. Coronary ostia just above valve.', clinical: 'Aortic stenosis: calcified valve \u2192 syncope, angina, HF. "Dub" = semilunar valves closing. Bicuspid aortic valve (1-2% prevalence).' },

                  { id: 'pulm_v', name: 'Pulmonary Valve', x: 0.57, y: 0.30, fn: '3 semilunar cusps at RV-pulmonary trunk junction. Prevents backflow into RV during diastole.', clinical: 'Pulmonary stenosis: congenital narrowing (part of Tetralogy of Fallot). Least commonly affected valve.' },

                  { id: 'septum', name: 'Interventricular Septum', x: 0.50, y: 0.53, fn: 'Muscular wall separating L and R ventricles. Thick muscular portion + thin membranous portion. LAD supplies anterior septum.', clinical: 'VSD (ventricular septal defect): most common congenital heart defect. Septal MI from LAD occlusion.' },

                  { id: 'chordae', name: 'Chordae Tendineae', x: 0.50, y: 0.49, fn: '"Heart strings" \u2014 fibrous cords connecting AV valve leaflets to papillary muscles. Prevent valve prolapse during ventricular contraction.', clinical: 'Ruptured chordae = sudden severe valve regurgitation = acute heart failure. Can occur in endocarditis or MI.' },

                  { id: 'conduction', name: 'Conduction System', x: 0.52, y: 0.42, fn: 'SA node \u2192 AV node \u2192 atrioventricular bundle \u2192 right and left bundle branches \u2192 Purkinje fibers. This pathway coordinates contraction; normal rates differ between sheep and humans.', clinical: 'Heart blocks: 1st degree (delayed), 2nd degree (dropped beats), 3rd degree (complete dissociation). Pacemaker implantation.' }

                ]

              }

            }

          };





          // ════════ ACTIVE STATE ════════

          var specimen = d.specimen || 'frog';

          var spec = SPECIMENS[specimen];

          if (!spec) { specimen = 'frog'; spec = SPECIMENS['frog']; }

          var activeLayer = d.activeLayer || (spec.layers[0] || {}).id || 'skin';

          var revealedLayers = d.revealedLayers || {};

          var currentLayerIdx = spec.layers.findIndex(function (l) { return l.id === activeLayer; });

          if (currentLayerIdx < 0) currentLayerIdx = 0;

          var organs = (spec.organs[activeLayer] || []);
          var organSearchText = String(d.organSearch || '').trim().toLowerCase();
          var filteredOrgans = organs.filter(function (org) {
            if (!organSearchText) return true;
            return org.name.toLowerCase().indexOf(organSearchText) >= 0 || org.fn.toLowerCase().indexOf(organSearchText) >= 0;
          });

          var sel = d.selectedOrgan ? organs.find(function (o) { return o.id === d.selectedOrgan; }) : null;
          var selectedOrganIndex = sel ? organs.findIndex(function (o) { return o.id === sel.id; }) : -1;

          var guidedStep = d.guidedStep || 0;

          var guidedMode = d.guidedMode || false;

          var PROCEDURE_INSTRUMENTS = [
            { id: 'probe', label: 'Probe', icon: '\u2022', inputHint: 'Press · drag', help: 'Trace and identify a visible structure.' },
            { id: 'forceps', label: 'Forceps', icon: '\u2195', inputHint: 'Press · drag', help: 'Lift and retract the opened layer.' },
            { id: 'scalpel', label: 'Scalpel', icon: '\u2712', inputHint: 'Stroke', help: 'Create the first shallow guided incision.' },
            { id: 'scissors', label: 'Scissors', icon: '\u2702', inputHint: 'Stroke', help: 'Extend an opening that has already been started.' },
            { id: 'pin', label: 'Pins', icon: '\u25C6', inputHint: 'Press · drag', help: 'Hold retracted tissue so structures remain visible.' },
            { id: 'dropper', label: 'Dropper', icon: '\u25C9', inputHint: 'Press · drag', help: 'Restore specimen moisture with a controlled saline dose.' },
            { id: 'wick', label: 'Absorbent Wick', icon: '\u25A7', inputHint: 'Press · drag', help: 'Lift excess saline from a pooled field without disturbing exposed anatomy.' }
          ];
          var specimenTrayNote = spec.bodyShape === 'eye'
            ? 'Eye tray: probe, forceps, shallow cutting tools, pins, a viewing-surface dropper, and an absorbent wick for excess saline.'
            : (spec.bodyShape === 'heart'
              ? 'Heart tray: blunt probing is emphasized before opening a layer; saline and an absorbent wick support field moisture control.'
              : 'Comparative tray: inspect and probe before shallow access; saline and an absorbent wick support field moisture control.');
          // Each organism carries an explicit access route, orientation, protected landmarks,
          // and purpose-built step language instead of sharing one generalized incision script.
          var SPECIMEN_PROCEDURE_PROFILES = {
            frog: {
              title: 'Frog ventral midline access', recommendedView: 'ventral', corridorLabel: 'VENTRAL MIDLINE',
              order: ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'], cutResistance: 28,
              failureMode: 'A deep midline stroke can damage the liver, heart, or lungs beneath the body wall.',
              orientation: 'Ventral surface centered with the limbs stabilized symmetrically.',
              route: 'shallow ventral midline corridor', protected: ['heart', 'liver', 'lungs'],
              steps: ['Orient ventral', 'Skin opening', 'Extend midline', 'Reflect wall', 'Anchor flaps', 'Trace organ'],
              actions: ['Inspect ventral landmarks and confirm symmetry', 'Begin the shallow ventral midline opening', 'Extend the opening along the ventral midline', 'Reflect the body wall with forceps', 'Anchor the reflected body-wall flaps', 'Trace a visible organ boundary'],
              pinFirst: 'Place the first body-wall anchor', pinSecond: 'Place the opposing body-wall anchor',
              completion: 'Complete the frog ventral-access sequence'
            },
            worm: {
              title: 'Earthworm dorsal longitudinal access', recommendedView: 'dorsal', corridorLabel: 'DORSAL LONGITUDINAL',
              order: ['inspect', 'pin', 'scalpel', 'scissors', 'forceps', 'probe'], preStabilize: true, cutResistance: 18,
              failureMode: 'Poor stabilization can twist segments or obscure the dorsal vessel and digestive tract.',
              orientation: 'Dorsal side up with the anterior-posterior axis straight and the surface kept moist.',
              route: 'shallow dorsal longitudinal corridor', protected: ['aortic_arches', 'cerebral_g', 'intestine'],
              steps: ['Orient dorsal', 'Stabilize ends', 'Dorsal entry', 'Extend lengthwise', 'Reflect wall', 'Trace tract'],
              actions: ['Confirm the dorsal surface and anterior-posterior orientation', 'Stabilize the anterior and posterior ends before opening', 'Begin a shallow dorsal midline opening', 'Extend the opening longitudinally', 'Reflect the body wall without twisting segments', 'Trace the digestive tract or a visible vessel'],
              pinFirst: 'Place the first body-wall anchor', pinSecond: 'Place the opposing body-wall anchor',
              completion: 'Complete the earthworm dorsal-access sequence'
            },
            pig: {
              title: 'Fetal pig ventral body-cavity access', recommendedView: 'ventral', corridorLabel: 'UMBILICAL DETOUR',
              order: ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'], cutResistance: 52,
              failureMode: 'An unplanned midline extension can cross the umbilical vessels or damage the urinary bladder.',
              orientation: 'Ventral surface up with limbs stabilized and the umbilical region identified before cutting.',
              route: 'ventral midline corridor with an umbilical detour', protected: ['umbilical', 'heart_p', 'bladder_p'],
              steps: ['Orient ventral', 'Midline entry', 'Detour cord', 'Reflect wall', 'Anchor flaps', 'Trace organ'],
              actions: ['Inspect ventral landmarks and locate the umbilical region', 'Begin a shallow ventral midline opening', 'Extend around the umbilical region', 'Reflect the body wall with forceps', 'Anchor the body-wall flaps', 'Trace a thoracic or abdominal organ boundary'],
              pinFirst: 'Place the first body-wall flap anchor', pinSecond: 'Place the opposing body-wall flap anchor',
              completion: 'Complete the fetal-pig ventral-access sequence'
            },
            fish: {
              title: 'Fish lateral abdominal-window access', recommendedView: 'lateral', corridorLabel: 'LATERAL WINDOW',
              order: ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'], cutResistance: 45,
              failureMode: 'Cutting too far anteriorly can damage the gills or heart behind the operculum.',
              orientation: 'Lateral side centered with the operculum, ventral edge, and body axis identified.',
              route: 'shallow lateral body-wall window posterior to the operculum', protected: ['gills', 'heart_f', 'liver_f'],
              steps: ['Orient lateral', 'Body-wall entry', 'Extend window', 'Reflect wall', 'Anchor window', 'Trace organ'],
              actions: ['Inspect lateral landmarks and locate the operculum', 'Begin a shallow lateral body-wall opening', 'Extend the window posterior to the operculum', 'Reflect the body wall with forceps', 'Anchor the lateral viewing window', 'Trace a gill or visceral structure'],
              pinFirst: 'Place the first viewing-window anchor', pinSecond: 'Place the opposing viewing-window anchor',
              completion: 'Complete the fish lateral-window sequence'
            },
            crayfish: {
              title: 'Crayfish dorsal carapace-window access', recommendedView: 'dorsal', corridorLabel: 'CARAPACE MARGIN',
              order: ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'], cutResistance: 88,
              failureMode: 'Excess force through the rigid carapace can penetrate the heart, gills, or supraesophageal ganglion.',
              orientation: 'Dorsal side up with the cephalothorax centered and carapace margins identified.',
              route: 'dorsal carapace-margin window', protected: ['heart_c', 'gills_c', 'brain_c'],
              steps: ['Orient dorsal', 'Score margin', 'Extend window', 'Lift carapace', 'Anchor flap', 'Trace organ'],
              actions: ['Inspect dorsal carapace landmarks', 'Score the dorsal carapace margin', 'Extend the dorsal carapace window', 'Lift the carapace section with forceps', 'Anchor the viewing flap', 'Trace a gill or digestive structure'],
              pinFirst: 'Place the first carapace-flap anchor', pinSecond: 'Place the opposing carapace-flap anchor',
              completion: 'Complete the crayfish carapace-window sequence'
            },
            eye: {
              title: 'Sheep eye equatorial access', recommendedView: 'lateral', corridorLabel: 'CORNEOSCLERAL EQUATOR',
              order: ['inspect', 'pin', 'scalpel', 'scissors', 'forceps', 'probe'], preStabilize: true, cutResistance: 36,
              failureMode: 'An unstable eye cup or deep entry can displace the lens or tear the retina.',
              orientation: 'Eye cup stabilized with cornea, sclera, and optic-nerve pole identified.',
              route: 'shallow equatorial corneoscleral opening', protected: ['lens', 'retina', 'optic_nerve'],
              steps: ['Orient globe', 'Stabilize cup', 'Equator entry', 'Extend equator', 'Reflect coat', 'Trace pathway'],
              actions: ['Inspect corneoscleral landmarks and identify the optic-nerve pole', 'Stabilize the eye cup before opening', 'Create a shallow equatorial entry', 'Extend the equatorial opening', 'Reflect the outer coat with forceps', 'Trace the lens-to-retina pathway'],
              pinFirst: 'Place the first eye-cup stabilizer', pinSecond: 'Place the opposing eye-cup stabilizer',
              completion: 'Complete the sheep-eye equatorial-access sequence'
            },
            heart: {
              title: 'Sheep heart chamber-window access', recommendedView: 'ventral', corridorLabel: 'CHAMBER WINDOW',
              order: ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'], cutResistance: 58,
              failureMode: 'A poorly placed wall opening can sever coronary vessels, chordae tendineae, or conduction tissue.',
              orientation: 'External surface centered with apex, base, coronary vessels, and major vessels identified.',
              route: 'guided surface-to-chamber window', protected: ['coronary_aa', 'conduction', 'chordae'],
              steps: ['Orient heart', 'Wall entry', 'Extend window', 'Reflect wall', 'Anchor window', 'Trace flow'],
              actions: ['Inspect the apex, base, coronary vessels, and major vessels', 'Create a shallow myocardial-wall entry', 'Extend the chamber window', 'Reflect the myocardial flap with forceps', 'Stabilize the chamber window', 'Trace a valve-to-vessel route'],
              pinFirst: 'Place the first chamber-window anchor', pinSecond: 'Place the opposing chamber-window anchor',
              completion: 'Complete the sheep-heart chamber-window sequence'
            }
          };
          var procedureProtocol = SPECIMEN_PROCEDURE_PROFILES[spec.bodyShape] || SPECIMEN_PROCEDURE_PROFILES.frog;
          function anatomicalOrientationData() {
            // The longitudinal axis uses specimen-specific anatomical vocabulary and follows mirrored ventral views.
            var profiles = {
              frog: { start: 'ANTERIOR', end: 'POSTERIOR', axis: 'vertical' },
              worm: { start: 'ANTERIOR', end: 'POSTERIOR', axis: 'vertical' },
              pig: { start: 'CRANIAL', end: 'CAUDAL', axis: 'horizontal' },
              fish: { start: 'ANTERIOR', end: 'POSTERIOR', axis: 'horizontal' },
              crayfish: { start: 'ANTERIOR', end: 'POSTERIOR', axis: 'horizontal' },
              eye: { start: 'CORNEA', end: 'OPTIC NERVE', axis: 'horizontal' },
              heart: { start: 'BASE', end: 'APEX', axis: 'vertical' }
            };
            var profile = profiles[spec.bodyShape] || profiles.frog;
            var view = d.anatomicalView || anatomicalView || 'dorsal';
            return {
              start: profile.start,
              end: profile.end,
              axis: profile.axis,
              mirrored: profile.axis === 'horizontal' && view === 'ventral',
              view: view,
              targetView: procedureProtocol.recommendedView,
              aligned: view === procedureProtocol.recommendedView
            };
          }
          var PROCEDURE_STEPS = procedureProtocol.order.map(function (stepId, stepIndex) {
            return { id: stepId, label: procedureProtocol.steps[stepIndex] };
          });
          function procedureProtectedLandmarks() {
            var protectedIds = procedureProtocol.protected || [];
            return protectedIds.map(function (structureId) {
              return allSpecimenStructures().find(function (structure) { return structure.id === structureId; });
            }).filter(Boolean);
          }
          var activeInstrument = d.activeInstrument || 'probe';
          var livingFunctionEnabled = !!d.livingFunctionEnabled;
          var livingFunctionPaused = !!d.livingFunctionPaused;
          var livingFunctionSpeed = ['slow', 'normal', 'fast'].indexOf(d.livingFunctionSpeed) >= 0 ? d.livingFunctionSpeed : 'normal';
          var storedAccessibilityPreferences = {};
          try { storedAccessibilityPreferences = JSON.parse(localStorage.getItem('dissection_accessibility_preferences') || '{}') || {}; } catch (e) { storedAccessibilityPreferences = {}; }
          var highContrastEnabled = !!d.highContrast || !!storedAccessibilityPreferences.highContrast;
          var reducedMotionEnabled = !!d.reducedMotion || !!storedAccessibilityPreferences.reducedMotion;
          var largeTextEnabled = !!d.largeText || !!storedAccessibilityPreferences.largeText;
          var simplifiedInstructions = !!d.simplifiedInstructions || !!storedAccessibilityPreferences.simplifiedInstructions;
          var storedWorkspaceMode = 'essentials';
          try { storedWorkspaceMode = localStorage.getItem('dissection_workspace_mode') || 'essentials'; } catch (e) { storedWorkspaceMode = 'essentials'; }
          var workspaceMode = d.workspaceMode === 'advanced' || d.workspaceMode === 'essentials'
            ? d.workspaceMode
            : (storedWorkspaceMode === 'advanced' ? 'advanced' : 'essentials');
          var advancedWorkspace = workspaceMode === 'advanced';
          function setDissectionWorkspaceMode(mode) {
            var nextMode = mode === 'advanced' ? 'advanced' : 'essentials';
            var endedTimedPractical = nextMode === 'essentials' && !!d.practicalMode;
            try { localStorage.setItem('dissection_workspace_mode', nextMode); } catch (e) {}
            if (endedTimedPractical) closeTimedPractical();
            updMany(Object.assign({ workspaceMode: nextMode }, nextMode === 'essentials' ? {
              toolbarToolsOpen: false,
              compareTechniqueAttempts: false,
              compareReplayPlaying: false,
              showProcedureDebrief: false,
              procedureScenario: 'precision-access',
              scenarioStartedAt: 0,
              scenarioTimeRemaining: 0,
              scenarioCompletedAt: 0
            } : {}));
            var modeMessage = nextMode === 'advanced'
              ? 'Advanced workspace enabled. Scenario, instructor, comparison, and detailed simulation controls are available.'
              : 'Essentials workspace enabled. Core procedure, observation, evidence, and accessibility controls remain available.';
            if (endedTimedPractical) modeMessage += ' The timed practical ended before its Advanced controls were hidden.';
            setProcedureFeedback(modeMessage, 'working');
          }
          function setAccessibilityPreference(key, value, label) {
            var nextPreferences = Object.assign({}, storedAccessibilityPreferences);
            nextPreferences[key] = !!value;
            try { localStorage.setItem('dissection_accessibility_preferences', JSON.stringify(nextPreferences)); } catch (e) {}
            var preferencePatch = {}; preferencePatch[key] = !!value; updMany(preferencePatch);
            var preferenceMessage = label + (value ? ' enabled.' : ' disabled.');
            setProcedureFeedback(preferenceMessage, 'working');
          }
          function normalizeToolCalibration(value) {
            value = value || {};
            return {
              bladeAngle: Math.max(10, Math.min(60, Number(value.bladeAngle) || 25)),
              scissorsAperture: Math.max(15, Math.min(70, Number(value.scissorsAperture) || 35)),
              forcepsGrip: Math.max(15, Math.min(90, Number(value.forcepsGrip) || 40)),
              pinAngle: Math.max(35, Math.min(90, Number(value.pinAngle) || 65)),
              probePressure: Math.max(10, Math.min(80, Number(value.probePressure) || 30)),
              salineDose: Math.max(1, Math.min(3, Math.round(Number(value.salineDose) || 1)))
            };
          }
          function normalizeInstructorMaxCautions(value) {
            if (value == null || value === '' || typeof value === 'boolean' || Array.isArray(value)) return 1;
            var numericValue = Number(value);
            return isFinite(numericValue) ? Math.max(0, Math.min(2, Math.round(numericValue))) : 1;
          }
          var toolCalibration = normalizeToolCalibration(d.toolCalibration);
          var procedureMode = advancedWorkspace && d.procedureMode === 'independent' ? 'independent' : 'guided';
          livingFunctionEnabled = advancedWorkspace && livingFunctionEnabled;
          var visualRealism = advancedWorkspace ? (d.visualRealism || 'guided') : 'guided';
          var labLight = advancedWorkspace ? (d.labLight || 'neutral') : 'neutral';
          var lightIntensity = Math.max(20, Math.min(100, Number(d.lightIntensity) || 68));
          var inspectionLens = advancedWorkspace && !!d.inspectionLens;
          var lensPinned = advancedWorkspace && !!d.lensPinned;
          var lensMagnification = [2, 4, 6].indexOf(Number(d.lensMagnification)) >= 0 ? Number(d.lensMagnification) : 2;
          var lensFocusDepth = ['surface', 'structure', 'deep'].indexOf(d.lensFocusDepth) >= 0 ? d.lensFocusDepth : 'structure';
          var instrumentVisuals = d.instrumentVisuals !== false;
          var macroInset = advancedWorkspace && d.macroInset !== false;
          var sceneDetail = advancedWorkspace ? d.sceneDetail !== false : true;
          var depthAtlasEnabled = advancedWorkspace && d.depthAtlas !== false;
          var tissueReliefEnabled = d.tissueRelief !== false;
          var relationshipMotion = advancedWorkspace && d.relationshipMotion !== false;
          var focusMode = d.focusMode !== false;
          var parallaxDepth = advancedWorkspace && d.parallaxDepth !== false;
          var evidenceFrameLimit = 6;
          var visualEvidence = Array.isArray(d.visualEvidence) ? d.visualEvidence.filter(function (entry) { return entry && entry.image; }).slice(-evidenceFrameLimit) : [];
          var splitComparison = advancedWorkspace && !!d.splitComparison;
          var lightDirection = advancedWorkspace && ['overhead', 'left', 'right', 'raking'].indexOf(d.lightDirection) >= 0 ? d.lightDirection : 'overhead';
          var variationSeed = Number(d.variationSeed) || 1;
          var anatomicalView = ['dorsal', 'ventral', 'lateral', 'internal'].indexOf(d.anatomicalView) >= 0 ? d.anatomicalView : 'dorsal';
          var crossSectionMode = advancedWorkspace && !!d.crossSectionMode;
          var specimenCondition = advancedWorkspace && ['standard', 'preserved', 'dehydrated', 'cloudy', 'swollen'].indexOf(d.specimenCondition) >= 0 ? d.specimenCondition : 'standard';
          var relationshipMode = advancedWorkspace && !!d.relationshipMode;
          var renderQuality = advancedWorkspace && ['auto', 'high', 'balanced'].indexOf(d.renderQuality) >= 0 ? d.renderQuality : 'auto';
          var procedureScenario = advancedWorkspace ? (d.procedureScenario || 'precision-access') : 'precision-access';
          var instructorTarget = Math.max(60, Math.min(95, Number(d.instructorTarget) || 75));
          var instructorMaxCautions = normalizeInstructorMaxCautions(d.instructorMaxCautions);
          var instructorRequiredStructures = Math.max(1, Math.min(5, Number(d.instructorRequiredStructures) || 2));
          var procedureByLayer = d.procedureByLayer || {};
          var currentProcedure = Object.assign({
            inspected: false, incisionStarted: false, incisionExtended: false,
            retracted: false, pins: [], probed: false, errors: 0, history: [], actionLog: [], cautionLog: [],
            tissueState: defaultTissueState()
          }, procedureByLayer[activeLayer] || {});
          currentProcedure.tissueState = normalizeTissueState(currentProcedure.tissueState);
          if (procedureScenario === 'restricted-tray' && (activeInstrument === 'dropper' || activeInstrument === 'wick')) {
            var restrictedFallbackAction = nextProcedureInfo().action;
            activeInstrument = procedureInstrumentForAction(restrictedFallbackAction === 'complete' ? 'probe' : restrictedFallbackAction);
          }
          var activeFunctionalTraceKey = advancedWorkspace ? (d.traceCirculation ? 'circulation' : (d.traceDigestion ? 'digestion' : (d.traceRespiration ? 'respiration' : (d.traceExcretory ? 'excretory' : null)))) : null;
          var activeFunctionalTraceLabel = { circulation: 'Circulatory', digestion: 'Digestive', respiration: 'Respiratory', excretory: 'Excretory' }[activeFunctionalTraceKey] || '';

          // Nearest-target hit testing also treats laid-out labels as interactive targets.
          // The owning canvas is passed explicitly so normalized label boxes remain clickable
          // without relying on an out-of-scope identifier.
          function closestVisibleOrganAt(x, y, radius, canvasEl) {
            var labelHit = (canvasEl && canvasEl._hotspotLabelBoxes || []).find(function (box) {
              return x >= box.x && x <= box.x + box.width && y >= box.y && y <= box.y + box.height;
            });
            if (labelHit) {
              var labelOrgan = organs.find(function (organ) { return organ.id === labelHit.id; });
              if (labelOrgan && structureExposureState(labelOrgan, currentProcedure) === 'visible') return labelOrgan;
            }
            var nearestOrgan = null, nearestDistance = radius;
            organs.forEach(function (organ) {
              if (structureExposureState(organ, currentProcedure) !== 'visible') return;
              var point = variedOrganPoint(organ), dx = x - point.x, dy = y - point.y;
              var distance = Math.sqrt(dx * dx + dy * dy);
              if (distance <= nearestDistance) { nearestDistance = distance; nearestOrgan = organ; }
            });
            return nearestOrgan;
          }

          // Contact context separates tissue interaction from tray contact and identifies anatomy under the tip.
          function specimenContactContext(point) {
            if (!point) return { onSpecimen: false, organ: null, edgeDistance: 1 };
            var contactProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
            var marginX = spec.bodyShape === 'worm' ? 0.055 : (spec.bodyShape === 'eye' || spec.bodyShape === 'heart' ? 0.035 : 0.075);
            var marginY = spec.bodyShape === 'worm' ? 0.035 : 0.065;
            var normalizedX = (point.x - contactProfile.cx) / Math.max(0.02, contactProfile.rx + marginX);
            var normalizedY = (point.y - contactProfile.cy) / Math.max(0.02, contactProfile.ry + marginY);
            var ellipseDistance = Math.sqrt(normalizedX * normalizedX + normalizedY * normalizedY);
            var nearbyOrgan = null;
            var nearbyRadius = spec.bodyShape === 'crayfish' || spec.bodyShape === 'frog' ? 0.105 : 0.082;
            organs.forEach(function (organ) {
              if (viewOrganVisibility(organ) !== 'visible') return;
              var organPoint = variedOrganPoint(organ);
              var organDX = point.x - organPoint.x, organDY = point.y - organPoint.y;
              var organDistance = Math.sqrt(organDX * organDX + organDY * organDY);
              if (organDistance <= nearbyRadius) { nearbyRadius = organDistance; nearbyOrgan = organ; }
            });
            return { onSpecimen: ellipseDistance <= 1 || !!nearbyOrgan, organ: nearbyOrgan, edgeDistance: ellipseDistance };
          }

          // canvasEl is a PARAMETER, not a free variable. It used to read an undeclared `canvas`,
          // which throws ReferenceError the first time a cut is evaluated — every caller already
          // had the element in hand, so passing it costs nothing. Falls back to the element lookup
          // this file uses elsewhere so a future call site that forgets the argument degrades
          // instead of crashing the tool.
          function cuttingTrajectorySafety(point, vector, canvasEl) {
            if (!point) return null;
            var safetyCanvas = canvasEl || queryDissectionNode('[data-diss-canvas]');
            if (!safetyCanvas) return null;
            // Predictive cutting safety evaluates the instrument's forward envelope before the tip reaches protected anatomy.
            var safetyWidth = Number(safetyCanvas._logicalW) || safetyCanvas.width || 500;
            var safetyHeight = Number(safetyCanvas._logicalH) || safetyCanvas.height || 600;
            var guide = procedureGuidePoints();
            var rawVector = vector && Math.abs(vector.x) + Math.abs(vector.y) > 0.0015
              ? vector
              : { x: guide[guide.length - 1].x - guide[0].x, y: guide[guide.length - 1].y - guide[0].y };
            var vectorX = rawVector.x * safetyWidth, vectorY = rawVector.y * safetyHeight;
            var vectorLength = Math.sqrt(vectorX * vectorX + vectorY * vectorY) || 1;
            var unitX = vectorX / vectorLength, unitY = vectorY / vectorLength;
            var maxForward = Math.min(safetyWidth, safetyHeight) * (spec.bodyShape === 'worm' ? 0.20 : 0.16);
            var lateralLimit = Math.max(20, Math.min(safetyWidth, safetyHeight) * (spec.bodyShape === 'worm' ? 0.055 : 0.065));
            var nearestSafety = null, nearestScore = Infinity;
            organs.forEach(function (organ) {
              if (viewOrganVisibility(organ) !== 'visible') return;
              var organPoint = variedOrganPoint(organ);
              var organDX = (organPoint.x - point.x) * safetyWidth, organDY = (organPoint.y - point.y) * safetyHeight;
              var forwardDistance = organDX * unitX + organDY * unitY;
              var lateralDistance = Math.abs(organDX * unitY - organDY * unitX);
              if (forwardDistance < 7 || forwardDistance > maxForward || lateralDistance > lateralLimit) return;
              var safetyScore = lateralDistance + forwardDistance * 0.16;
              if (safetyScore >= nearestScore) return;
              var organDepth = structureOpticalDepth(organ);
              var protocolProtected = (procedureProtocol.protected || []).indexOf(organ.id) >= 0;
              var critical = protocolProtected || (d.incisionDepth || 'shallow') === 'deep' || lateralDistance < lateralLimit * 0.48 || forwardDistance < maxForward * 0.34;
              nearestScore = safetyScore;
              nearestSafety = {
                organ: organ,
                point: organPoint,
                depth: organDepth,
                forward: forwardDistance,
                lateral: lateralDistance,
                critical: critical,
                protocolProtected: protocolProtected,
                clearance: Math.max(0, Math.min(100, Math.round((1 - lateralDistance / lateralLimit) * 100)))
              };
            });
            return nearestSafety;
          }

          var CURATED_ANATOMY_RELATIONSHIPS = {
            frog: [
              { from: 'heart', to: 'lungs', type: 'circulation', forward: 'pumps blood toward', reverse: 'return blood toward' },
              { from: 'liver', to: 'gallbladder', type: 'biliary', forward: 'produces bile stored by', reverse: 'stores bile produced by' },
              { from: 'stomach', to: 'sm_intestine', type: 'digestive flow', forward: 'empties into', reverse: 'receives digesta from' },
              { from: 'sm_intestine', to: 'lg_intestine', type: 'digestive flow', forward: 'continues into', reverse: 'receives material from' },
              { from: 'pancreas', to: 'sm_intestine', type: 'secretion', forward: 'delivers digestive secretions to', reverse: 'receives digestive secretions from' },
              { from: 'kidneys', to: 'cloaca', type: 'excretion', forward: 'drain waste toward', reverse: 'receives urinary waste from' },
              { from: 'brain', to: 'spinal_cord', type: 'neural continuity', forward: 'continues into', reverse: 'continues cranially into' },
              { from: 'spinal_cord', to: 'sciatic_n', type: 'neural branching', forward: 'gives rise to pathways including', reverse: 'connects proximally toward' },
              { from: 'skull', to: 'brain', type: 'protection', forward: 'encloses and protects', reverse: 'is enclosed by' },
              { from: 'femur', to: 'tibiofibula', type: 'joint', forward: 'articulates with', reverse: 'articulates with' }
            ],
            worm: [
              { from: 'pharynx', to: 'crop', type: 'digestive flow', forward: 'moves food toward', reverse: 'receives food from' },
              { from: 'crop', to: 'gizzard', type: 'digestive flow', forward: 'passes stored food to', reverse: 'receives stored food from' },
              { from: 'gizzard', to: 'intestine', type: 'digestive flow', forward: 'passes ground food to', reverse: 'receives ground food from' },
              { from: 'cerebral_g', to: 'ventral_cord', type: 'neural continuity', forward: 'connects with', reverse: 'connects anteriorly with' },
              { from: 'ventral_cord', to: 'segmental_g', type: 'neural branching', forward: 'links the repeated', reverse: 'connect with' },
              { from: 'circular_m', to: 'longitudinal_m', type: 'antagonistic muscles', forward: 'works antagonistically with', reverse: 'works antagonistically with' },
              { from: 'setae', to: 'longitudinal_m', type: 'locomotion', forward: 'anchor movement generated with', reverse: 'generates movement stabilized by' },
              { from: 'clitellum', to: 'seminal_v', type: 'reproduction', forward: 'functions reproductively with', reverse: 'functions reproductively with' }
            ],
            pig: [
              { from: 'heart_p', to: 'lungs_p', type: 'pulmonary circulation', forward: 'pumps blood toward', reverse: 'return oxygenated blood toward' },
              { from: 'diaphragm_p', to: 'lungs_p', type: 'respiration', forward: 'changes thoracic pressure around', reverse: 'expand as pressure changes from' },
              { from: 'stomach_p', to: 'sm_int_p', type: 'digestive flow', forward: 'empties into', reverse: 'receives digesta from' },
              { from: 'sm_int_p', to: 'lg_int_p', type: 'digestive flow', forward: 'continues into', reverse: 'receives material from' },
              { from: 'pancreas_p', to: 'sm_int_p', type: 'secretion', forward: 'delivers digestive secretions to', reverse: 'receives digestive secretions from' },
              { from: 'kidneys_p', to: 'bladder_p', type: 'urinary flow', forward: 'drain urine toward', reverse: 'stores urine produced by' },
              { from: 'brain_p', to: 'spinal_p', type: 'neural continuity', forward: 'continues into', reverse: 'continues cranially into' },
              { from: 'brain_p', to: 'vagus_p', type: 'autonomic nerve', forward: 'gives rise to pathways including', reverse: 'connects to the brainstem within' },
              { from: 'ribs_p', to: 'sternum_p', type: 'thoracic support', forward: 'attach ventrally toward', reverse: 'anchors anterior portions of' },
              { from: 'scapula_p', to: 'humerus_p', type: 'joint', forward: 'articulates with', reverse: 'articulates with' },
              { from: 'pelvis_p', to: 'femur_p', type: 'joint', forward: 'articulates with', reverse: 'articulates with' }
            ],
            fish: [
              { from: 'heart_f', to: 'gills', type: 'circulation', forward: 'pumps blood toward', reverse: 'oxygenate blood returning toward' },
              { from: 'operculum', to: 'gills', type: 'protection and ventilation', forward: 'covers and helps ventilate', reverse: 'are covered by' },
              { from: 'stomach_f', to: 'pyloric_ceca', type: 'digestion', forward: 'passes digesta near', reverse: 'receive digesta near' },
              { from: 'liver_f', to: 'stomach_f', type: 'digestive support', forward: 'supports digestion adjacent to', reverse: 'lies functionally near' },
              { from: 'scales', to: 'lat_line', type: 'sensory surface', forward: 'surround the sensory pathway of', reverse: 'passes through the scale field of' },
              { from: 'vert_col_f', to: 'fin_rays', type: 'axial support', forward: 'supports movement coordinated with', reverse: 'transmit forces toward' },
              { from: 'swim_bladder', to: 'kidneys_f', type: 'dorsal adjacency', forward: 'lies ventral to', reverse: 'lie dorsal to' }
            ],
            crayfish: [
              { from: 'heart_c', to: 'gills_c', type: 'circulation', forward: 'circulates hemolymph associated with', reverse: 'oxygenate hemolymph returning toward' },
              { from: 'gastric_mill', to: 'hepato', type: 'digestion', forward: 'processes food before support from', reverse: 'provides digestive support after' },
              { from: 'brain_c', to: 'ventral_c', type: 'neural continuity', forward: 'connects with', reverse: 'connects anteriorly with' },
              { from: 'ventral_c', to: 'antennae_n', type: 'sensory neural pathway', forward: 'integrates sensory signals including', reverse: 'carry sensory signals toward' },
              { from: 'flexor_m', to: 'extensor_m', type: 'antagonistic muscles', forward: 'works antagonistically with', reverse: 'works antagonistically with' },
              { from: 'cheliped_m', to: 'chelipeds', type: 'attachment', forward: 'moves', reverse: 'are moved by' },
              { from: 'carapace', to: 'gills_c', type: 'protection', forward: 'covers and protects', reverse: 'lie protected beneath' }
            ],
            eye: [
              { from: 'cornea', to: 'aqueous', type: 'optical pathway', forward: 'refracts light into', reverse: 'lies immediately behind' },
              { from: 'aqueous', to: 'lens', type: 'optical pathway', forward: 'transmits light toward', reverse: 'receives light through' },
              { from: 'iris', to: 'lens', type: 'aperture control', forward: 'regulates light reaching', reverse: 'receives regulated light through' },
              { from: 'ciliary_body', to: 'lens', type: 'accommodation', forward: 'changes the shape of', reverse: 'is shaped by' },
              { from: 'lens', to: 'retina', type: 'optical pathway', forward: 'focuses light onto', reverse: 'receives focused light from' },
              { from: 'retina', to: 'optic_nerve', type: 'neural transmission', forward: 'sends visual signals through', reverse: 'carries signals from' },
              { from: 'choroid', to: 'retina', type: 'vascular support', forward: 'supports and nourishes', reverse: 'is supported by' },
              { from: 'sclera', to: 'ext_muscles', type: 'attachment', forward: 'provides attachment for', reverse: 'attach to' },
              { from: 'vitreous', to: 'retina', type: 'structural support', forward: 'supports the inner surface near', reverse: 'is supported internally by' },
              { from: 'tapetum', to: 'retina', type: 'light reflection', forward: 'reflects light back through', reverse: 'receives reflected light from' }
            ],
            heart: [
              { from: 'sup_vena_h', to: 'ra', type: 'blood flow', forward: 'drains into', reverse: 'receives blood from' },
              { from: 'inf_vena_h', to: 'ra', type: 'blood flow', forward: 'drains into', reverse: 'receives blood from' },
              { from: 'ra', to: 'tricuspid', type: 'blood flow', forward: 'passes blood through', reverse: 'controls flow from' },
              { from: 'tricuspid', to: 'rv', type: 'blood flow', forward: 'opens into', reverse: 'receives blood through' },
              { from: 'rv', to: 'pulm_v', type: 'blood flow', forward: 'ejects blood through', reverse: 'controls outflow from' },
              { from: 'pulm_v', to: 'pulm_trunk', type: 'blood flow', forward: 'opens into', reverse: 'receives blood through' },
              { from: 'la', to: 'mitral', type: 'blood flow', forward: 'passes blood through', reverse: 'controls flow from' },
              { from: 'mitral', to: 'lv', type: 'blood flow', forward: 'opens into', reverse: 'receives blood through' },
              { from: 'lv', to: 'aortic_v', type: 'blood flow', forward: 'ejects blood through', reverse: 'controls outflow from' },
              { from: 'aortic_v', to: 'aorta_h', type: 'blood flow', forward: 'opens into', reverse: 'receives blood through' },
              { from: 'aorta_h', to: 'coronary_aa', type: 'coronary supply', forward: 'gives rise to', reverse: 'originate from' },
              { from: 'chordae', to: 'mitral', type: 'valve support', forward: 'stabilize', reverse: 'is stabilized by' },
              { from: 'chordae', to: 'tricuspid', type: 'valve support', forward: 'stabilize', reverse: 'is stabilized by' },
              { from: 'septum', to: 'rv', type: 'separation', forward: 'separates from the left ventricle', reverse: 'is separated from the left ventricle by' },
              { from: 'conduction', to: 'ra', type: 'electrical control', forward: 'initiates coordinated activity near', reverse: 'contains the primary pacemaker region of' }
            ]
          };
          var VIEW_OCCLUSIONS = {
            frog: { dorsal: ['ventral_skin', 'rectus_abd', 'liver', 'stomach', 'sm_intestine', 'lg_intestine'], ventral: ['dorsal_skin', 'kidneys', 'brain', 'spinal_cord', 'vertebral_col'], lateral: ['rectus_abd', 'pectoral_gird', 'pelvic_girdle'], internal: ['dorsal_skin', 'ventral_skin', 'tympanum', 'nictitating'] },
            worm: { dorsal: ['setae'], ventral: ['cerebral_g'], lateral: ['aortic_arches', 'seminal_v'], internal: ['cuticle', 'setae'] },
            pig: { dorsal: ['umbilical', 'mammary', 'stomach_p', 'sm_int_p', 'lg_int_p', 'bladder_p'], ventral: ['vert_col_p', 'brain_p', 'spinal_p'], lateral: ['kidneys_p', 'thymus_p'], internal: ['epidermis_p', 'mammary'] },
            fish: { dorsal: ['gills', 'heart_f', 'liver_f', 'stomach_f'], ventral: ['swim_bladder', 'kidneys_f', 'vert_col_f'], lateral: [], internal: ['scales', 'lat_line', 'operculum'] },
            crayfish: { dorsal: ['gills_c', 'green_gland', 'ventral_c'], ventral: ['heart_c', 'carapace'], lateral: ['green_gland', 'gonads_c'], internal: ['carapace', 'telson'] },
            eye: { dorsal: ['retina', 'choroid', 'tapetum'], ventral: ['optic_nerve', 'retina'], lateral: ['fat_pad', 'conjunctiva'], internal: ['ext_muscles', 'conjunctiva', 'fat_pad'] },
            heart: { dorsal: ['rv', 'tricuspid', 'pulm_v'], ventral: ['la', 'mitral'], lateral: ['septum', 'conduction'], internal: ['pericardium', 'coronary_aa'] }
          };
          var VIEW_LANDMARK_OFFSETS = {
            frog: { ventral: { liver: [-0.015, 0.025], heart: [0, 0.018], stomach: [0.018, 0.02] }, lateral: { lungs: [0.035, -0.025], heart: [-0.018, 0], kidneys: [0.02, 0.025] }, internal: { brain: [0, -0.018], liver: [-0.02, 0.012], sm_intestine: [0.025, 0.025] } },
            worm: { ventral: { ventral_cord: [0, 0.018], segmental_g: [0.018, 0] }, lateral: { crop: [-0.025, 0], gizzard: [0.025, 0] }, internal: { aortic_arches: [-0.02, -0.012], intestine: [0.02, 0.012] } },
            pig: { ventral: { heart_p: [0, 0.018], liver_p: [-0.02, 0.025], stomach_p: [0.02, 0.025] }, lateral: { lungs_p: [0.025, -0.02], kidneys_p: [0.03, 0.025] }, internal: { brain_p: [0, -0.02], sm_int_p: [0.025, 0.02] } },
            fish: { ventral: { heart_f: [-0.02, 0.018], liver_f: [0.02, 0.02] }, lateral: { gills: [-0.025, 0], swim_bladder: [0.025, -0.02] }, internal: { stomach_f: [-0.02, 0.02], pyloric_ceca: [0.025, 0.015] } },
            crayfish: { ventral: { ventral_c: [0, 0.02], green_gland: [-0.018, 0] }, lateral: { heart_c: [0.02, -0.02], gills_c: [-0.02, 0.02] }, internal: { gastric_mill: [-0.02, 0], hepato: [0.025, 0.02] } },
            eye: { ventral: { optic_nerve: [0.025, 0], retina: [-0.015, 0.018] }, lateral: { lens: [0.025, 0], retina: [-0.025, 0] }, internal: { lens: [-0.02, 0], retina: [0.025, 0], optic_nerve: [0.03, 0.01] } },
            heart: { ventral: { rv: [0.02, 0.018], lv: [-0.018, 0.018] }, lateral: { ra: [-0.025, 0], la: [0.025, 0], septum: [0, 0.02] }, internal: { tricuspid: [-0.02, 0.015], mitral: [0.02, 0.015], chordae: [0, 0.03] } }
          };
          // Material response values drive incision width, flap movement, and live tool-contact deformation.
          var SPECIMEN_MATERIAL_PROFILES = {
            frog: { pattern: 'chromatophores', label: 'moist chromatophore surface', response: 'elastic skin', compliance: 0.78, moisture: 0.90, accent: '#86efac', cx: 0.50, cy: 0.49, rx: 0.25, ry: 0.35, density: 34 },
            worm: { pattern: 'segments', label: 'segmented iridescent cuticle', response: 'segment compression', compliance: 0.92, moisture: 0.72, accent: '#f9a8d4', cx: 0.50, cy: 0.49, rx: 0.095, ry: 0.39, density: 28 },
            pig: { pattern: 'follicles', label: 'fine follicle and connective surface', response: 'dermal tension', compliance: 0.62, moisture: 0.42, accent: '#fecdd3', cx: 0.50, cy: 0.49, rx: 0.35, ry: 0.23, density: 38 },
            fish: { pattern: 'scales', label: 'overlapping reflective scales', response: 'scale displacement', compliance: 0.38, moisture: 0.65, accent: '#bae6fd', cx: 0.50, cy: 0.48, rx: 0.37, ry: 0.19, density: 42 },
            crayfish: { pattern: 'facets', label: 'jointed chitin microfacets', response: 'rigid shell contact', compliance: 0.18, moisture: 0.18, accent: '#fdba74', cx: 0.50, cy: 0.48, rx: 0.35, ry: 0.21, density: 32 },
            eye: { pattern: 'radial', label: 'translucent corneal and scleral layers', response: 'corneal ripple', compliance: 0.58, moisture: 0.96, accent: '#a5f3fc', cx: 0.50, cy: 0.48, rx: 0.26, ry: 0.25, density: 24 },
            heart: { pattern: 'fibers', label: 'directional myocardial fibers', response: 'fiber compression', compliance: 0.84, moisture: 0.58, accent: '#fda4af', cx: 0.50, cy: 0.50, rx: 0.23, ry: 0.28, density: 30 }
          };          function allSpecimenStructures() {
            return Object.keys(spec.organs || {}).reduce(function (all, layerId) { return all.concat(spec.organs[layerId] || []); }, []);
          }
          function viewOrganVisibility(org) {
            var shapeViews = VIEW_OCCLUSIONS[spec.bodyShape] || {};
            var occluded = shapeViews[d.anatomicalView || anatomicalView] || [];
            return occluded.indexOf(org.id) >= 0 ? 'occluded' : 'visible';
          }
          function alternateVisibleViewForOrgan(org) {
            if (!org || viewOrganVisibility(org) === 'visible') return null;
            var shapeViews = VIEW_OCCLUSIONS[spec.bodyShape] || {};
            var currentView = d.anatomicalView || anatomicalView;
            return ['dorsal', 'ventral', 'lateral', 'internal'].find(function (view) {
              return view !== currentView && (shapeViews[view] || []).indexOf(org.id) < 0;
            }) || null;
          }
          function viewSpecificOrganPoint(org, point) {
            var view = d.anatomicalView || anatomicalView;
            var next = { x: point.x, y: point.y };
            if (view === 'lateral') next.y += (point.x - 0.5) * 0.045;
            else if (view === 'internal') { next.x = 0.5 + (point.x - 0.5) * 1.06; next.y = 0.45 + (point.y - 0.45) * 1.04; }
            var shapeOffsets = VIEW_LANDMARK_OFFSETS[spec.bodyShape] || {};
            var offset = ((shapeOffsets[view] || {})[org.id]) || [0, 0];
            next.x += offset[0]; next.y += offset[1];
            return { x: Math.max(0.04, Math.min(0.96, next.x)), y: Math.max(0.04, Math.min(0.96, next.y)) };
          }
          function specimenVariationValue(salt) {
            var liveVariationSeed = Number(d.variationSeed) || variationSeed;
            return (dissHash(specimen + '|' + liveVariationSeed + '|' + salt) % 1001) / 1000;
          }
          function variedOrganPoint(org) {
            var variedMode = d.visualRealism || visualRealism;
            var amount = variedMode === 'accessible' ? 0 : (variedMode === 'realistic' ? 0.016 : 0.007);
            return viewSpecificOrganPoint(org, {
              x: Math.max(0.04, Math.min(0.96, org.x + (specimenVariationValue(org.id + '|x') - 0.5) * amount * 2)),
              y: Math.max(0.04, Math.min(0.96, org.y + (specimenVariationValue(org.id + '|y') - 0.5) * amount * 2))
            });
          }
          function specimenScaleFactors() {
            var scaleMode = d.visualRealism || visualRealism;
            var amount = scaleMode === 'accessible' ? 0 : (scaleMode === 'realistic' ? 0.045 : 0.018);
            var view = d.anatomicalView || anatomicalView;
            var viewScaleX = view === 'ventral' ? -1 : (view === 'lateral' ? 0.66 : (view === 'internal' ? 1.04 : 1));
            var viewScaleY = view === 'lateral' ? 1.04 : (view === 'internal' ? 1.03 : 1);
            return {
              x: (1 + (specimenVariationValue('body-scale-x') - 0.5) * amount * 2) * viewScaleX,
              y: (1 + (specimenVariationValue('body-scale-y') - 0.5) * amount * 2) * viewScaleY
            };
          }
          function inverseSpecimenVariation(point) {
            var factors = specimenScaleFactors();
            return { x: (point.x - 0.5) / factors.x + 0.5, y: (point.y - 0.45) / factors.y + 0.45 };
          }
          function setVisualRealism(mode) {
            var patch = { visualRealism: mode };
            if (mode === 'accessible') { patch.highContrast = true; patch.animSpeed = 'slow'; }
            updMany(patch);
            setProcedureFeedback(mode === 'realistic'
              ? 'Realistic visuals enabled: richer materials, natural landmark variation, and reduced teaching emphasis.'
              : (mode === 'accessible'
                ? 'Accessible visuals enabled: high contrast, larger visual targets, and slower motion.'
                : 'Guided visuals enabled: balanced materials and visible technique targets.'));
          }

          function structureOpticalDepth(organ) {
            if (!organ) return 'surface';
            if (viewOrganVisibility(organ) === 'occluded') return 'deep';
            var layerId = String(activeLayer || '').toLowerCase();
            var organText = String((organ.id || '') + ' ' + (organ.name || '')).toLowerCase();
            if (/skin|surface|external|cornea|iris|scale|segment|setae|tympan|membrane|follicle/.test(layerId + ' ' + organText)) return 'surface';
            if (/organ|interior|internal|chamber|skeleton|nervous|system|brain|kidney|stomach|liver|heart/.test(layerId + ' ' + organText) || currentLayerIdx >= 2) return 'deep';
            return 'structure';
          }
          var depthAtlasCounts = organs.reduce(function (counts, organ) {
            var depth = structureOpticalDepth(organ);
            counts[depth] = (counts[depth] || 0) + 1;
            return counts;
          }, { surface: 0, structure: 0, deep: 0 });
          function anatomicalDepthLabel(depth) {
            return depth === 'surface' ? 'surface' : (depth === 'deep' ? 'deep' : 'mid-depth');
          }
          function opticalPlaneStatus(organ, focusDepth) {
            var rank = { surface: 0, structure: 1, deep: 2 };
            var idealDepth = structureOpticalDepth(organ);
            var distance = Math.abs(rank[idealDepth] - rank[focusDepth || lensFocusDepth]);
            return distance === 0 ? 'resolved' : (distance === 1 ? 'soft' : 'unresolved');
          }
          var opticalPlaneCounts = organs.reduce(function (counts, organ) {
            if (viewOrganVisibility(organ) !== 'visible') return counts;
            var status = opticalPlaneStatus(organ, lensFocusDepth);
            counts[status] += 1;
            return counts;
          }, { resolved: 0, soft: 0, unresolved: 0 });
          var opticalPlaneMapActive = inspectionLens || lensPinned;
          function illuminationAssessmentData(procedureState, intensity) {
            intensity = Math.max(20, Math.min(100, Number(intensity) || lightIntensity));
            var tissue = normalizeTissueState((procedureState || currentProcedure).tissueState || procedureState);
            var reflectivity = { eye: 0.95, fish: 0.82, worm: 0.72, crayfish: 0.68, frog: 0.65, heart: 0.58, pig: 0.45 }[spec.bodyShape] || 0.6;
            var idealCenter = 68 - reflectivity * 6 - Math.max(0, tissue.moisture - 82) * 0.08;
            var glareRisk = clampTissueMetric(Math.max(0, intensity - idealCenter) * 2.2 * reflectivity + Math.max(0, tissue.moisture - 82) * 0.65);
            var shadowLoss = clampTissueMetric(Math.max(0, 48 - intensity) * 2);
            var score = clampTissueMetric(100 - glareRisk * 0.58 - shadowLoss * 0.72);
            var label = glareRisk >= 45 ? 'Glare limited' : (shadowLoss >= 45 ? 'Underlit' : (score >= 88 ? 'Balanced illumination' : 'Usable light'));
            var tone = glareRisk >= 45 ? 'glare' : (shadowLoss >= 45 ? 'dim' : (score >= 88 ? 'balanced' : 'usable'));
            var suggestion = glareRisk >= 45
              ? 'Reduce illumination or use a raking angle to recover wet-surface detail.'
              : (shadowLoss >= 45
                ? 'Increase illumination until boundaries are visible without flattening relief.'
                : (score >= 88 ? 'Intensity preserves relief while keeping reflective glare controlled.' : 'Fine-tune intensity for a clearer balance of boundaries and surface relief.'));
            return { intensity: intensity, score: score, label: label, tone: tone, glareRisk: glareRisk, shadowLoss: shadowLoss, reflectivity: Math.round(reflectivity * 100), suggestion: suggestion };
          }
          function opticalInspectionData(target, focusDepth, magnification, pinned, procedureState) {
            focusDepth = ['surface', 'structure', 'deep'].indexOf(focusDepth) >= 0 ? focusDepth : lensFocusDepth;
            magnification = [2, 4, 6].indexOf(Number(magnification)) >= 0 ? Number(magnification) : lensMagnification;
            var idealDepth = structureOpticalDepth(target);
            var depthRank = { surface: 0, structure: 1, deep: 2 };
            var depthDistance = Math.abs(depthRank[focusDepth] - depthRank[idealDepth]);
            var focusScore = depthDistance === 0 ? 100 : (depthDistance === 1 ? 66 : 34);
            var magnificationScores = {
              surface: { 2: 100, 4: 88, 6: 62 },
              structure: { 2: 72, 4: 100, 6: 86 },
              deep: { 2: 58, 4: 88, 6: 100 }
            };
            var magnificationScore = magnificationScores[idealDepth][magnification];
            var field = observationFieldData(procedureState || currentProcedure);
            var targetScore = target ? 100 : 52;
            var pinScore = pinned ? 100 : 72;
            var illumination = illuminationAssessmentData(procedureState || currentProcedure, lightIntensity);
            var quality = clampTissueMetric(focusScore * 0.34 + magnificationScore * 0.16 + field.quality * 0.20 + targetScore * 0.10 + pinScore * 0.08 + illumination.score * 0.12);
            var label = quality >= 82 ? 'Resolved' : (quality >= 65 ? 'Usable focus' : (quality >= 45 ? 'Soft focus' : 'Unresolved'));
            var tone = quality >= 82 ? 'resolved' : (quality >= 65 ? 'usable' : (quality >= 45 ? 'soft' : 'unresolved'));
            var suggestion = !target ? 'Select or pin a structure to establish a stable inspection target.'
              : (focusScore < 90 ? 'Set focus depth to ' + idealDepth + ' for this structure.'
                : (illumination.score < 72 ? illumination.suggestion
                  : (field.quality < 58 ? field.guidance
                    : (magnificationScore < 75 ? 'Use lower magnification for a wider, steadier field.' : 'Focus, magnification, and illumination support reliable visual evidence.'))));
            var detailByDepth = {
              surface: { frog: 'pigment and mucous texture', worm: 'annular grooves and setae', fish: 'scale margins', crayfish: 'chitin facets', pig: 'follicle texture', eye: 'corneal surface', heart: 'epicardial texture' },
              structure: { frog: 'structure boundaries', worm: 'segment boundaries', fish: 'tissue interfaces', crayfish: 'joint membranes', pig: 'fascia and connective tissue', eye: 'iris and lens boundary', heart: 'muscle fiber direction' },
              deep: { frog: 'subsurface tissue plane', worm: 'internal septa', fish: 'deep organ plane', crayfish: 'internal attachment plane', pig: 'deep fascial plane', eye: 'retinal and optic layers', heart: 'chamber-wall depth' }
            };
            return {
              quality: quality,
              label: label,
              tone: tone,
              idealDepth: idealDepth,
              focusDepth: focusDepth,
              focusScore: focusScore,
              magnification: magnification,
              magnificationScore: magnificationScore,
              illumination: illumination,
              targetName: target ? target.name : 'Unlabeled tissue field',
              detail: (detailByDepth[focusDepth] || {})[spec.bodyShape] || 'tissue detail',
              suggestion: suggestion
            };
          }

          function specimenLivingFunctionProfile() {
            return {
              frog: {
                title: 'Buccal-pump ventilation', motion: 'frog-buccal', color: '#38bdf8',
                summary: 'The throat floor lowers to draw air in, then rises to push air into paired lungs.',
                phases: ['Buccal intake', 'Lung inflation']
              },
              worm: {
                title: 'Peristaltic locomotion', motion: 'worm-peristalsis', color: '#f59e0b',
                summary: 'Circular and longitudinal muscles contract in alternating waves while setae anchor segments.',
                phases: ['Circular contraction', 'Longitudinal contraction']
              },
              fish: {
                title: 'Opercular ventilation', motion: 'fish-operculum', color: '#22d3ee',
                summary: 'Mouth and operculum alternate pressure to maintain one-way water flow across the gills.',
                phases: ['Buccal intake', 'Opercular outflow']
              },
              crayfish: {
                title: 'Gill-bailer ventilation', motion: 'crayfish-gills', color: '#67e8f9',
                summary: 'The scaphognathite drives water across feather-like gills inside the branchial chamber.',
                phases: ['Water intake', 'Gill-chamber outflow']
              },
              pig: {
                title: 'Diaphragmatic ventilation', motion: 'pig-diaphragm', color: '#60a5fa',
                summary: 'Diaphragm descent expands the thorax; elastic recoil supports expiration.',
                phases: ['Inspiration', 'Expiration']
              },
              eye: {
                title: 'Pupillary light reflex', motion: 'eye-pupil', color: '#a78bfa',
                summary: 'Iris sphincter activity constricts the pupil in brighter light; dilation increases light entry.',
                phases: ['Constriction', 'Dilation']
              },
              heart: {
                title: 'Coordinated cardiac cycle', motion: 'heart-cycle', color: '#fb7185',
                summary: 'Atrial contraction precedes ventricular contraction; relaxation permits refilling.',
                phases: ['Atrial filling and contraction', 'Ventricular contraction']
              }
            }[spec.bodyShape] || {
              title: 'Living function model', motion: 'generic', color: '#2dd4bf',
              summary: 'A simplified physiological motion model connects structure with function.',
              phases: ['Active phase', 'Recovery phase']
            };
          }

          function toggleInspectionPin() {
            if (d.lensPinned) {
              updMany({ lensPinned: false, lensPinnedPoint: null, lensPinnedOrganId: null });
              setProcedureFeedback('Inspection lens released. It now follows the pointer.');
              return;
            }
            var pinTarget = sel ? variedOrganPoint(sel) : null;
            var pinOrganId = sel ? sel.id : null;
            if (!pinTarget) {
              try {
                var lensCanvas = queryDissectionNode('[data-diss-canvas]');
                if (lensCanvas && lensCanvas._toolPointer) pinTarget = { x: lensCanvas._toolPointer.x, y: lensCanvas._toolPointer.y };
              } catch (e) {}
            }
            if (!pinTarget) {
              setProcedureFeedback('Move over the specimen or select a structure before pinning the lens.', 'caution');
              return;
            }
            updMany({ inspectionLens: true, lensPinned: true, lensPinnedPoint: pinTarget, lensPinnedOrganId: pinOrganId });
            var pinnedInspection = opticalInspectionData(sel, lensFocusDepth, lensMagnification, true, currentProcedure);
            setProcedureFeedback('Inspection lens pinned' + (sel ? ' to ' + sel.name : ' at the current field') + '. ' + pinnedInspection.label + ' at ' + pinnedInspection.quality + '%. ' + pinnedInspection.suggestion);
          }
          function cycleLensMagnification() {
            var levels = [2, 4, 6];
            var next = levels[(levels.indexOf(lensMagnification) + 1) % levels.length];
            upd('lensMagnification', next);
            var magnificationInspection = opticalInspectionData(sel, lensFocusDepth, next, lensPinned, currentProcedure);
            setProcedureFeedback('Inspection magnification set to ' + next + 'x. ' + magnificationInspection.label + ' at ' + magnificationInspection.quality + '%. ' + magnificationInspection.suggestion, magnificationInspection.quality >= 65 ? 'success' : 'caution');
          }
          function cycleLensFocusDepth() {
            var depths = ['surface', 'structure', 'deep'];
            var next = depths[(depths.indexOf(lensFocusDepth) + 1) % depths.length];
            upd('lensFocusDepth', next);
            var depthInspection = opticalInspectionData(sel, next, lensMagnification, lensPinned, currentProcedure);
            setProcedureFeedback('Inspection focus depth set to ' + next + '. ' + depthInspection.label + ' at ' + depthInspection.quality + '%. ' + depthInspection.suggestion, depthInspection.quality >= 65 ? 'success' : 'caution');
          }

          // Persistent tissue dynamics turn isolated tool actions into connected, reversible consequences.
          function clampTissueMetric(value) {
            return Math.max(0, Math.min(100, Math.round(Number(value) || 0)));
          }
          function tissueVariantDefinition(layerId, options) {
            options = options || {};
            var seed = Math.abs(Number(options.variationSeed || d.variationSeed || variationSeed || 1));
            var variantLayerIdx = currentLayerIdx;
            if (layerId) {
              var resolvedLayerIdx = spec.layers.findIndex(function (layer) { return layer.id === layerId; });
              if (resolvedLayerIdx >= 0) variantLayerIdx = resolvedLayerIdx;
            }
            var variants = [
              { id: 'balanced', label: 'Balanced preparation', moisture: 0, tension: 0, clarity: 0, risk: 0 },
              { id: 'firm', label: 'Firm preservation', moisture: -8, tension: 14, clarity: -4, risk: 4 },
              { id: 'dry', label: 'Reduced surface moisture', moisture: -22, tension: 9, clarity: -8, risk: 8 },
              { id: 'narrow', label: 'Narrow working field', moisture: -4, tension: 18, clarity: -5, risk: 12 }
            ];
            return variants[(seed + specimen.length + variantLayerIdx * 3) % variants.length];
          }
          function defaultTissueState(layerId, options) {
            options = options || {};
            var condition = ['standard', 'preserved', 'dehydrated', 'cloudy', 'swollen'].indexOf(options.specimenCondition) >= 0 ? options.specimenCondition : (specimenCondition || 'standard');
            var variant = tissueVariantDefinition(layerId, options);
            var moisture = spec.bodyShape === 'worm' ? 86 : (spec.bodyShape === 'eye' ? 82 : 76);
            var tension = spec.bodyShape === 'heart' ? 54 : (spec.bodyShape === 'pig' ? 48 : 40);
            var clarity = spec.bodyShape === 'eye' ? 88 : 84;
            if (condition === 'preserved') { moisture -= 16; tension += 18; clarity -= 7; }
            if (condition === 'dehydrated') { moisture -= 40; tension += 28; clarity -= 16; }
            if (condition === 'cloudy') clarity -= spec.bodyShape === 'eye' ? 43 : 28;
            if (condition === 'swollen') { tension += 25; clarity -= 9; }
            return {
              moisture: clampTissueMetric(moisture + variant.moisture),
              tension: clampTissueMetric(tension + variant.tension),
              exposure: 8,
              trauma: 0,
              clarity: clampTissueMetric(clarity + variant.clarity),
              stability: 18,
              risk: clampTissueMetric(8 + variant.risk),
              salineDrops: 0,
              variantId: variant.id,
              variantLabel: variant.label,
              lastAction: 'prepared',
              lastUpdatedAt: Date.now(),
              consequences: []
            };
          }
          function normalizeTissueState(state, layerId, options) {
            var baseline = defaultTissueState(layerId, options);
            state = state || {};
            return {
              moisture: clampTissueMetric(state.moisture == null ? baseline.moisture : state.moisture),
              tension: clampTissueMetric(state.tension == null ? baseline.tension : state.tension),
              exposure: clampTissueMetric(state.exposure == null ? baseline.exposure : state.exposure),
              trauma: clampTissueMetric(state.trauma == null ? baseline.trauma : state.trauma),
              clarity: clampTissueMetric(state.clarity == null ? baseline.clarity : state.clarity),
              stability: clampTissueMetric(state.stability == null ? baseline.stability : state.stability),
              risk: clampTissueMetric(state.risk == null ? baseline.risk : state.risk),
              salineDrops: Math.max(0, Math.min(4, Number(state.salineDrops) || 0)),
              variantId: state.variantId || baseline.variantId,
              variantLabel: state.variantLabel || baseline.variantLabel,
              lastAction: state.lastAction || baseline.lastAction,
              lastUpdatedAt: Number(state.lastUpdatedAt) || baseline.lastUpdatedAt,
              consequences: Array.isArray(state.consequences) ? state.consequences.slice(-12) : []
            };
          }
          function normalizeSavedProcedureByLayer(map, options) {
            var normalizedMap = {};
            Object.keys(map || {}).forEach(function (layerId) {
              var procedureState = map[layerId];
              if (!procedureState || typeof procedureState !== 'object' || Array.isArray(procedureState)) return;
              normalizedMap[layerId] = Object.assign({}, procedureState, {
                tissueState: normalizeTissueState(procedureState.tissueState, layerId, options)
              });
            });
            return normalizedMap;
          }
          function interpolateTissueState(beforeState, afterState, progress) {
            var before = normalizeTissueState(beforeState);
            var after = normalizeTissueState(afterState);
            var amount = Math.max(0, Math.min(1, Number(progress) || 0));
            var interpolated = Object.assign({}, after);
            ['moisture', 'tension', 'exposure', 'trauma', 'clarity', 'stability', 'risk'].forEach(function (metric) {
              interpolated[metric] = before[metric] + (after[metric] - before[metric]) * amount;
            });
            interpolated.salineDrops = amount < 0.68 ? before.salineDrops : after.salineDrops;
            return interpolated;
          }
          function procedureMeanPressure(samples) {
            var pressures = (samples || []).map(function (sample) { return Number(sample && sample.pressure) || 0; }).filter(function (pressure) { return pressure > 0; });
            return pressures.length ? pressures.reduce(function (sum, pressure) { return sum + pressure; }, 0) / pressures.length : 0.34;
          }
          function compactProcedureSamples(samples) {
            // Bounded pressure and cadence samples preserve technique replay detail without retaining raw pointer events.
            var sourceSamples = (samples || []).filter(Boolean).slice(-64);
            var firstAt = sourceSamples.length ? Number(sourceSamples[0].at) || 0 : 0;
            return sourceSamples.map(function (sample) {
              return {
                pressure: Math.round(Math.max(0.02, Math.min(1, Number(sample.pressure) || 0.34)) * 1000) / 1000,
                dt: Math.max(0, Math.min(12000, sample.dt != null ? Number(sample.dt) || 0 : (Number(sample.at) || firstAt) - firstAt)),
                pointerType: String(sample.pointerType || 'unknown').slice(0, 12)
              };
            });
          }
          function calibrationControlDefinition(toolId) {
            return {
              scalpel: { key: 'bladeAngle', label: 'Blade angle', min: 10, max: 60, step: 1, unit: '\u00B0', ideal: '20\u201335\u00B0 protects deeper structures.' },
              scissors: { key: 'scissorsAperture', label: 'Scissors aperture', min: 15, max: 70, step: 1, unit: '%', ideal: '25\u201345% supports a controlled extension.' },
              forceps: { key: 'forcepsGrip', label: 'Forceps grip', min: 15, max: 90, step: 1, unit: '%', ideal: '25\u201350% lifts tissue without crushing it.' },
              pin: { key: 'pinAngle', label: 'Pin angle', min: 35, max: 90, step: 1, unit: '\u00B0', ideal: '55\u201375\u00B0 provides stable anchoring.' },
              probe: { key: 'probePressure', label: 'Probe pressure', min: 10, max: 80, step: 1, unit: '%', ideal: '18\u201338% traces anatomy gently.' },
              dropper: { key: 'salineDose', label: 'Saline dose', min: 1, max: 3, step: 1, unit: ' drop', ideal: 'One drop restores moisture without flooding the field.' }
            }[toolId] || null;
          }
          function calibrationRangeScore(value, idealMin, idealMax, outerMin, outerMax) {
            value = Number(value);
            if (value >= idealMin && value <= idealMax) return 100;
            if (value < idealMin) return Math.max(20, Math.round(100 - (idealMin - value) / Math.max(1, idealMin - outerMin) * 80));
            return Math.max(20, Math.round(100 - (value - idealMax) / Math.max(1, outerMax - idealMax) * 80));
          }
          function toolCalibrationScore(toolId, calibration) {
            calibration = normalizeToolCalibration(calibration);
            if (toolId === 'scalpel') return calibrationRangeScore(calibration.bladeAngle, 20, 35, 10, 60);
            if (toolId === 'scissors') return calibrationRangeScore(calibration.scissorsAperture, 25, 45, 15, 70);
            if (toolId === 'forceps') return calibrationRangeScore(calibration.forcepsGrip, 25, 50, 15, 90);
            if (toolId === 'pin') return calibrationRangeScore(calibration.pinAngle, 55, 75, 35, 90);
            if (toolId === 'probe') return calibrationRangeScore(calibration.probePressure, 18, 38, 10, 80);
            if (toolId === 'dropper') return calibration.salineDose === 1 ? 100 : (calibration.salineDose === 2 ? 72 : 45);
            return 100;
          }
          function instrumentCalibrationAssessment(toolId, calibration) {
            var score = toolCalibrationScore(toolId, calibration);
            var control = calibrationControlDefinition(toolId);
            if (!control) return { score: 100, label: 'Ready', tone: 'ready', detail: 'No additional calibration is needed.' };
            if (score >= 85) return { score: score, label: 'Calibrated', tone: 'ready', detail: control.ideal };
            if (score >= 60) return { score: score, label: 'Adjust', tone: 'adjust', detail: 'Close to the safe working range. ' + control.ideal };
            return { score: score, label: 'Caution', tone: 'caution', detail: 'This setting increases simulated tissue risk. ' + control.ideal };
          }
          function calibratedActionPatch(toolId, patch) {
            var scoreMap = Object.assign({}, currentProcedure.calibrationScores || {});
            scoreMap[toolId] = toolCalibrationScore(toolId, toolCalibration);
            return Object.assign({}, patch || {}, {
              toolCalibration: Object.assign({}, toolCalibration),
              calibrationScore: scoreMap[toolId],
              calibrationScores: scoreMap
            });
          }
          function evolveTissueState(action, patch, baseState) {
            patch = patch || {};
            var next = normalizeTissueState(baseState);
            var consequence = '';
            var tone = 'stable';
            var metrics = patch.extensionMetrics || patch.incisionMetrics || {};
            var calibration = normalizeToolCalibration(patch.toolCalibration || toolCalibration);
            var calibrationScore = toolCalibrationScore(action, calibration);
            var precision = Number(metrics.precision) || 75;
            var control = Number(metrics.control == null ? metrics.smoothness : metrics.control) || 75;
            var pressure = Number(patch.probePressure || patch.extensionPressure || patch.incisionPressure) || 0.34;
            var elapsedMinutes = Math.max(0, (Date.now() - next.lastUpdatedAt) / 60000);
            next.moisture = clampTissueMetric(next.moisture - Math.min(5, elapsedMinutes * 0.7));
            if (action === 'inspect') {
              next.clarity += 5; next.risk -= 3;
              consequence = 'Orientation improved field clarity before contact.';
            } else if (action === 'scalpel') {
              var bladePenalty = Math.max(0, calibration.bladeAngle - 35) + Math.max(0, 20 - calibration.bladeAngle);
              next.exposure += 16 + precision * 0.1;
              next.trauma += Math.max(1, (68 - precision) * 0.18 + Math.max(0, pressure - 0.52) * 28 + bladePenalty * 0.24);
              next.risk += Math.max(1, (65 - control) * 0.12 + Math.max(0, pressure - 0.58) * 22 + bladePenalty * 0.32);
              next.moisture -= 3;
              consequence = precision >= 78 && calibrationScore >= 85 ? 'A clean, safely angled shallow separation increased exposure with minimal tissue trauma.' : 'The incision opened the field, but stroke or blade calibration added visible edge stress.';
              tone = precision >= 78 && calibrationScore >= 85 ? 'stable' : 'caution';
            } else if (action === 'scissors') {
              var apertureHigh = Math.max(0, calibration.scissorsAperture - 45);
              var apertureLow = Math.max(0, 25 - calibration.scissorsAperture);
              next.exposure += 20 + precision * 0.12 - apertureLow * 0.32;
              next.trauma += Math.max(1, (66 - precision) * 0.16 + Math.max(0, pressure - 0.55) * 24 + apertureHigh * 0.22);
              next.risk += Math.max(1, (62 - control) * 0.1 + apertureHigh * 0.26);
              next.tension += 4;
              consequence = precision >= 76 && calibrationScore >= 85 ? 'Controlled aperture widened the access window along the existing opening.' : 'The extension widened access, but aperture or stroke control stressed the tissue edges.';
              tone = precision >= 76 && calibrationScore >= 85 ? 'stable' : 'caution';
            } else if (action === 'forceps') {
              var excessGrip = Math.max(0, calibration.forcepsGrip - 50);
              var weakGrip = Math.max(0, 25 - calibration.forcepsGrip);
              var liftMetrics = patch.forcepsDragMetrics || {};
              var liftControl = isFinite(Number(liftMetrics.control)) ? Number(liftMetrics.control) : 86;
              var liftPercent = isFinite(Number(liftMetrics.liftPercent)) ? Number(liftMetrics.liftPercent) : 72;
              var controlPenalty = Math.max(0, 72 - liftControl);
              next.exposure += 12 + liftPercent * 0.085 - weakGrip * 0.28;
              next.tension += 16 + liftPercent * 0.09 + excessGrip * 0.34 + controlPenalty * 0.12;
              next.stability -= 4 + weakGrip * 0.18 + controlPenalty * 0.08;
              next.trauma += excessGrip * 0.2 + controlPenalty * 0.1;
              next.risk += (next.moisture < 42 ? 8 : 2) + excessGrip * 0.22 + controlPenalty * 0.12;
              consequence = calibrationScore < 85 ? 'Retraction increased exposure, but grip calibration added slip or compression stress.' : (next.moisture < 42 ? 'Retraction increased exposure, but the dry tissue is under elevated tension.' : (liftControl >= 82 ? 'A controlled direct lift increased exposure while preserving the opened corridor.' : 'Retraction increased exposure with mild tension from variable pull control.'));
              tone = next.moisture < 42 || calibrationScore < 85 || liftControl < 72 ? 'caution' : 'stable';
            } else if (action === 'pin') {
              var pinDeviation = calibration.pinAngle < 55 ? 55 - calibration.pinAngle : Math.max(0, calibration.pinAngle - 75);
              var pinDragMetrics = patch.pinDragMetrics || {};
              var pinControl = isFinite(Number(pinDragMetrics.control)) ? Number(pinDragMetrics.control) : 86;
              var pinInsertion = isFinite(Number(pinDragMetrics.insertionPercent)) ? Number(pinDragMetrics.insertionPercent) : 72;
              var pinAlignment = isFinite(Number(pinDragMetrics.alignment)) ? Number(pinDragMetrics.alignment) : 86;
              var pinControlPenalty = Math.max(0, 74 - pinControl);
              var pinAlignmentPenalty = Math.max(0, 76 - pinAlignment);
              next.stability += 27 + Math.min(15, pinInsertion * 0.12) - pinDeviation * 0.7 - pinControlPenalty * 0.08 - pinAlignmentPenalty * 0.08;
              next.tension += 6 + pinDeviation * 0.2 + pinControlPenalty * 0.1 + pinAlignmentPenalty * 0.08;
              next.risk += (next.tension > 82 ? 5 : 0) + pinDeviation * 0.32 + pinControlPenalty * 0.12 + pinAlignmentPenalty * 0.1;
              consequence = calibrationScore < 85 ? 'The anchor engaged, but its angle reduced stability and increased edge stress.' : (pinControl >= 82 && pinAlignment >= 82 ? 'A controlled direct insertion stabilized the flap while preserving the observation field.' : 'The anchor stabilized the flap with mild control or alignment variability.');
              tone = next.tension > 82 || calibrationScore < 85 || pinControl < 74 || pinAlignment < 76 ? 'caution' : 'stable';
            } else if (action === 'probe') {
              var probeDragMetrics = patch.probeDragMetrics || {};
              var probeControl = isFinite(Number(probeDragMetrics.control)) ? Number(probeDragMetrics.control) : 84;
              var probeContact = isFinite(Number(probeDragMetrics.contactPercent)) ? Number(probeDragMetrics.contactPercent) : 88;
              var probeControlPenalty = Math.max(0, 76 - probeControl);
              var probeContactPenalty = Math.max(0, 72 - probeContact);
              var probePressurePenalty = Math.max(0, pressure - 0.38) * 22;
              next.clarity += 4 + Math.min(4, probeContact * 0.035); next.exposure += 3;
              next.trauma += Math.max(1, pressure > 0.62 ? 4 : 1 + probePressurePenalty * 0.12 + probeControlPenalty * 0.08);
              next.risk += Math.max(1, pressure > 0.62 ? 5 : 1 + probePressurePenalty * 0.18 + probeControlPenalty * 0.1 + probeContactPenalty * 0.08);
              consequence = calibrationScore < 85 || probePressurePenalty > 0 || probeControl < 72 ? 'The structure was traced, but pressure or control added simulated contact stress.' : (patch.probeDragMetrics ? 'A controlled direct palpation clarified the selected structure while maintaining contact and respecting tissue resistance.' : 'Gentle probing clarified the selected structure without obscuring adjacent anatomy.');
              tone = pressure > 0.62 || calibrationScore < 85 || probeControl < 72 || probeContact < 58 ? 'caution' : 'stable';
            } else if (action === 'dropper') {
              var dose = calibration.salineDose;
              var dropperDragMetrics = patch.dropperDragMetrics || {};
              var dropperDose = isFinite(Number(dropperDragMetrics.dose)) ? Number(dropperDragMetrics.dose) : dose;
              var dropperContact = isFinite(Number(dropperDragMetrics.contactPercent)) ? Number(dropperDragMetrics.contactPercent) : 100;
              var dropperControl = isFinite(Number(dropperDragMetrics.control)) ? Number(dropperDragMetrics.control) : 86;
              var dropperPoolingRisk = isFinite(Number(dropperDragMetrics.poolingRisk)) ? Number(dropperDragMetrics.poolingRisk) : (dropperDose > 1 ? 42 : 12);
              next.moisture += next.salineDrops >= 2 ? 5 * dropperDose : 18 + dropperDose * 4;
              next.clarity += next.salineDrops >= 2 ? 3 : (dropperDose === 1 ? 18 : 10);
              next.tension -= 7; next.trauma -= 2 + Math.max(0, 78 - dropperControl) * 0.03;
              next.risk += dropperDose === 1 ? -4 : dropperDose * 2;
              next.risk += dropperPoolingRisk * 0.06 + Math.max(0, 72 - dropperContact) * 0.04;
              next.salineDrops += dropperDose;
              consequence = dropperDragMetrics && dropperDragMetrics.contactPercent != null
                ? (dropperPoolingRisk >= 64 ? 'The saline path contacted the specimen, but pooling risk increased; reduce dose and keep the drag short.' : (dropperControl >= 78 && dropperContact >= 72 ? 'A controlled direct hydration pass followed the specimen flow and restored a thin viewing film.' : 'The saline pass restored moisture, but contact or flow control was variable.'))
                : (next.salineDrops > 3 || dose > 1 ? 'The surface was hydrated, but the larger dose reduced field control and added little clarity.' : 'A controlled saline drop restored moisture and improved surface clarity.');
              tone = dropperPoolingRisk >= 64 || dropperControl < 72 || next.salineDrops > 3 || dropperDose > 1 ? 'caution' : 'stable';
            } else if (action === 'wick') {
              var pooledDrops = Math.max(0, next.salineDrops - 2);
              var wickDragMetrics = patch.wickDragMetrics || {};
              var wickRecovery = isFinite(Number(wickDragMetrics.recoveryPercent)) ? Number(wickDragMetrics.recoveryPercent) : 72;
              var wickControl = isFinite(Number(wickDragMetrics.control)) ? Number(wickDragMetrics.control) : 84;
              var wickContact = isFinite(Number(wickDragMetrics.contactPercent)) ? Number(wickDragMetrics.contactPercent) : 100;
              next.salineDrops = Math.max(1, next.salineDrops - Math.max(1, pooledDrops));
              next.moisture -= 8 + pooledDrops * 5 + Math.max(0, 70 - wickControl) * 0.04;
              next.clarity += 7 + pooledDrops * 2 + Math.min(6, wickRecovery * 0.04);
              next.stability += 3 + Math.min(3, wickContact * 0.025);
              next.risk -= 7 + pooledDrops * 2 - Math.max(0, 68 - wickControl) * 0.04;
              next.trauma += Math.max(0, 72 - wickContact) * 0.03;
              consequence = wickDragMetrics && wickDragMetrics.recoveryPercent != null
                ? (wickControl >= 78 && wickContact >= 72 ? 'A controlled direct wick pass recovered saline from the pool edge without disturbing exposed anatomy.' : 'The wick recovered saline, but contact or control was variable near the pool edge.')
                : (pooledDrops > 0
                  ? 'The absorbent wick lifted excess saline from the edge of the pool and restored a controlled viewing film.'
                  : 'The absorbent wick reduced the surface film; avoid further wicking so the specimen does not dry.');
              tone = wickControl < 72 || wickContact < 58 || pooledDrops <= 0 ? 'caution' : 'stable';
            } else if (action === 'mistake') {
              next.trauma += 8; next.risk += 12; next.clarity -= 4; next.moisture -= 2;
              consequence = patch.message || 'Unsafe sequencing increased simulated tissue risk.';
              tone = 'caution';
            }
            ['moisture','tension','exposure','trauma','clarity','stability','risk'].forEach(function (key) { next[key] = clampTissueMetric(next[key]); });
            next.lastAction = action;
            next.lastUpdatedAt = Date.now();
            if (consequence) next.consequences = next.consequences.concat([{ action: action, message: consequence, tone: tone, at: next.lastUpdatedAt }]).slice(-12);
            return next;
          }
          function tissuePreservationScore(state) {
            var tissue = normalizeTissueState(state && state.tissueState ? state.tissueState : state);
            return clampTissueMetric((100 - tissue.trauma) * 0.45 + (100 - tissue.risk) * 0.25 + tissue.clarity * 0.18 + tissue.moisture * 0.12);
          }
          function tissueStatusData(state) {
            var tissue = normalizeTissueState(state);
            var preservation = tissuePreservationScore(tissue);
            var label = tissue.trauma >= 60 ? 'Compromised' : (tissue.risk >= 55 ? 'At risk' : (tissue.moisture < 38 ? 'Drying' : (tissue.tension > 82 ? 'High tension' : (preservation >= 82 ? 'Well preserved' : 'Stable'))));
            var priority = tissue.moisture < 38 ? 'Restore surface moisture before further manipulation.' : (tissue.tension > 82 ? 'Reduce handling and avoid adding tension near the flap edge.' : (tissue.trauma > 42 ? 'Use gentler pressure and protect the current access corridor.' : (tissue.stability < 45 && tissue.exposure > 35 ? 'Stabilize the retracted tissue before probing.' : 'The field is ready for the next controlled action.')));
            return { label: label, preservation: preservation, priority: priority };
          }

          function tissuePhysicsData(state) {
            var tissue = normalizeTissueState(state);
            var compliance = clampTissueMetric(tissue.moisture * 0.5 + (100 - tissue.tension) * 0.28 + (100 - tissue.trauma) * 0.22);
            var damping = clampTissueMetric(tissue.stability * 0.62 + (100 - compliance) * 0.24 + tissue.tension * 0.14);
            var sag = clampTissueMetric(compliance * 0.55 + tissue.moisture * 0.25 - tissue.stability * 0.35);
            var recoil = clampTissueMetric((100 - tissue.tension) * 0.52 + compliance * 0.32 - tissue.trauma * 0.18);
            var label = tissue.stability >= 72 ? 'Anchored and damped' : (tissue.tension >= 82 ? 'Taut with limited compliance' : (tissue.moisture < 38 ? 'Dry and stiff' : (compliance >= 68 ? 'Supple with visible settling' : 'Firm with controlled recoil')));
            return { compliance: compliance, damping: damping, sag: sag, recoil: recoil, label: label };
          }

          // One observation-field model keeps visual aperture, clarity, coaching, evidence, and reporting consistent.
          function observationFieldData(state) {
            state = state || {};
            var tissue = normalizeTissueState(state.tissueState || state);
            var quality = clampTissueMetric(tissue.clarity * 0.36 + tissue.exposure * 0.32 + tissue.stability * 0.22 + (100 - tissue.risk) * 0.10);
            if (state.probed) quality = clampTissueMetric(quality + 5);
            var label = quality >= 80 ? 'High-definition field' : (quality >= 65 ? 'Clear field' : (quality >= 48 ? 'Developing field' : 'Limited observation field'));
            var tone = quality >= 65 ? 'clear' : (quality >= 48 ? 'developing' : 'restricted');
            var limiter = tissue.exposure < 45 ? 'Access width'
              : (tissue.stability < 45 ? 'Field stability'
                : (tissue.clarity < 58 ? 'Surface clarity'
                  : (tissue.trauma > 42 ? 'Edge trauma'
                    : (tissue.risk > 55 ? 'Tissue risk' : 'No major limiter'))));
            var guidance = limiter === 'Access width' ? 'Extend and retract only along the established corridor.'
              : (limiter === 'Field stability' ? 'Stabilize the retracted tissue with well-spaced anchors.'
                : (limiter === 'Surface clarity' ? 'Restore moisture with one controlled saline drop.'
                  : (limiter === 'Edge trauma' ? 'Avoid widening the opening and reduce contact pressure.'
                    : (limiter === 'Tissue risk' ? 'Pause manipulation and protect the current corridor.' : 'The field supports confident structure observation.'))));
            return {
              quality: quality,
              label: label,
              tone: tone,
              limiter: limiter,
              guidance: guidance,
              aperture: tissue.exposure,
              clarity: tissue.clarity,
              stability: tissue.stability
            };
          }

          // Exposure is earned through access, retraction, stability, field clarity, and protocol-aligned orientation.
          function procedureExposureData(state) {
            state = state || {};
            var tissue = normalizeTissueState(state.tissueState || state);
            var orderLength = Math.max(1, (procedureProtocol.order || []).length);
            var protocolProgress = procedureStepIndex(state) / orderLength;
            var access = (state.inspected ? 8 : 0) + (state.incisionStarted ? 16 : 0) + (state.incisionExtended ? 20 : 0) + (state.retracted ? 25 : 0) + (Math.min(2, (state.pins || []).length) * 8) + (state.probed ? 7 : 0);
            var field = tissue.clarity * 0.20 + tissue.stability * 0.14 + tissue.exposure * 0.22 + (100 - tissue.risk) * 0.08;
            var orientation = (d.anatomicalView || anatomicalView) === procedureProtocol.recommendedView ? 10 : 0;
            var illumination = Math.max(0, 10 - Math.abs((Number(d.lightIntensity) || lightIntensity) - 68) * 0.16);
            var score = clampTissueMetric(access * 0.56 + field * 0.44 + orientation + illumination + protocolProgress * 8);
            var label = score >= 78 ? 'Fully exposed field' : (score >= 58 ? 'Working exposure' : (score >= 36 ? 'Emerging anatomy' : 'Covered anatomy'));
            var next = score < 36 ? 'Complete orientation and establish the planned opening.' : (tissue.exposure < 55 ? 'Increase exposure only through the current protocol step.' : (tissue.stability < 52 ? 'Stabilize the field before tracing structures.' : 'The field supports relationship tracing and evidence capture.'));
            return { score: score, label: label, next: next, protocolProgress: Math.round(protocolProgress * 100), aperture: tissue.exposure, clarity: tissue.clarity, stability: tissue.stability };
          }
          function structureExposureState(org, state) {
            var orientationState = viewOrganVisibility(org);
            if (orientationState !== 'visible') return orientationState;
            if (currentLayerIdx === 0) return 'visible';
            var exposure = procedureExposureData(state || currentProcedure);
            var depth = structureOpticalDepth(org);
            var threshold = depth === 'deep' ? 64 : (depth === 'structure' ? 48 : 34);
            threshold += dissHash(specimen + '|' + activeLayer + '|' + org.id + '|exposure') % 11;
            if (exposure.score >= threshold) return 'visible';
            if (exposure.score >= threshold - 18) return 'emerging';
            return 'obscured';
          }
          function procedureMaterialInteractionData(toolId, state) {
            var resistance = Math.max(0, Math.min(100, Number(procedureProtocol.cutResistance) || 40));
            var condition = specimenCondition;
            if (condition === 'dehydrated') resistance += 12;
            else if (condition === 'preserved') resistance += 7;
            else if (condition === 'swollen') resistance += 5;
            resistance = Math.max(0, Math.min(100, resistance));
            var calibration = toolCalibrationScore(toolId, (state && state.toolCalibration) || toolCalibration);
            var margin = calibration - Math.round(54 + resistance * 0.28);
            var label = resistance >= 76 ? 'High-resistance material' : (resistance >= 48 ? 'Moderate-resistance material' : 'Low-resistance material');
            var response = toolId === 'scalpel' || toolId === 'scissors'
              ? (margin >= 12 ? 'Calibration supports a controlled opening.' : (margin >= 0 ? 'Proceed slowly at the current calibration.' : 'Recalibrate before cutting this material.'))
              : activeMaterialProfile.response + ' expected at contact.';
            return { resistance: resistance, calibration: calibration, margin: margin, label: label, response: response, failureMode: procedureProtocol.failureMode };
          }
          // Procedure credit follows a predict -> perform -> explain cycle. Physical
          // specimen state and learning evidence remain separate so hints and motor-neutral
          // controls stay available without turning a single Next click into mastery.
          function procedureLearningDefinition(action) {
            var route = procedureProtocol.route || 'planned access corridor';
            var protectedNames = procedureProtectedLandmarks().map(function (landmark) { return landmark.name; });
            var protectedText = protectedNames.length ? protectedNames.slice(0, 3).join(', ') : 'underlying landmarks';
            var definitions = {
              inspect: {
                title: 'Plan the orientation check',
                predictionPrompt: 'What must be confirmed before any instrument contacts this specimen?',
                predictionCorrectId: 'confirm-route',
                predictionOptions: [
                  { id: 'confirm-route', label: 'Confirm the ' + procedureProtocol.recommendedView + ' view, center the ' + route + ', and account for ' + protectedText + '.' },
                  { id: 'deep-view', label: 'Choose the deepest-looking region first; orientation can be corrected after opening.' },
                  { id: 'tool-first', label: 'Select a cutting tool first, then use the incision to determine orientation.' }
                ],
                predictionCoaching: 'Look for the recommended view, planned route, and protected landmarks before contact.',
                reflectionPrompt: 'Which observation shows that orientation was recorded successfully?',
                reflectionCorrectId: 'aligned-landmarks',
                reflectionOptions: [
                  { id: 'largest-area', label: 'The largest possible surface area is visible, even if the access route is off-center.' },
                  { id: 'aligned-landmarks', label: 'The recommended view and access route are centered, with protected landmarks accounted for.' },
                  { id: 'tool-selected', label: 'A cutting instrument is selected and visible above the tray.' }
                ],
                reflectionCoaching: 'Base the conclusion on view alignment and identifiable landmarks, not tool selection.'
              },
              scalpel: {
                title: 'Plan the first opening',
                predictionPrompt: 'Which plan best protects anatomy during the initial entry?',
                predictionCorrectId: 'shallow-corridor',
                predictionOptions: [
                  { id: 'wide-entry', label: 'Open the widest area in one stroke so every structure becomes visible immediately.' },
                  { id: 'shallow-corridor', label: 'Use shallow depth and follow the ' + route + '; stop if alignment or resistance changes.' },
                  { id: 'deep-shortcut', label: 'Use a deeper short stroke so the scissors have less tissue to extend.' }
                ],
                predictionCoaching: 'The first entry should be shallow, corridor-aligned, and reversible before it is wide.',
                reflectionPrompt: 'What outcome is evidence of a controlled initial incision?',
                reflectionCorrectId: 'aligned-entry',
                reflectionOptions: [
                  { id: 'aligned-entry', label: 'A shallow entry follows the planned corridor while underlying landmarks remain protected.' },
                  { id: 'maximum-exposure', label: 'Maximum exposure is achieved even if the stroke leaves the corridor.' },
                  { id: 'high-resistance', label: 'Resistance rises sharply, showing the tool reached deeper tissue.' }
                ],
                reflectionCoaching: 'Use alignment, depth, and tissue preservation as evidence.'
              },
              scissors: {
                title: 'Plan the extension',
                predictionPrompt: 'How should scissors be used at this point in the protocol?',
                predictionCorrectId: 'extend-opening',
                predictionOptions: [
                  { id: 'new-opening', label: 'Start a separate opening beside the first incision to increase exposure.' },
                  { id: 'cross-landmark', label: 'Cut toward the nearest landmark regardless of the established corridor.' },
                  { id: 'extend-opening', label: 'Extend the existing opening along the planned corridor with controlled coverage.' }
                ],
                predictionCoaching: 'Scissors extend an established opening; they do not create an unrelated path.',
                reflectionPrompt: 'Which result best supports a safe extension?',
                reflectionCorrectId: 'longer-controlled',
                reflectionOptions: [
                  { id: 'longer-controlled', label: 'The established opening is longer and usable without a new tear or corridor deviation.' },
                  { id: 'more-pressure', label: 'More pressure was required than before, regardless of the tissue response.' },
                  { id: 'new-window', label: 'A second window appeared away from the initial entry.' }
                ],
                reflectionCoaching: 'Look for controlled coverage of the same opening and no new tissue damage.'
              },
              forceps: {
                title: 'Plan tissue retraction',
                predictionPrompt: 'Where and how should forceps contact the opened layer?',
                predictionCorrectId: 'edge-low-tension',
                predictionOptions: [
                  { id: 'edge-low-tension', label: 'Grip near the opened edge and lift gradually with low, controlled tension.' },
                  { id: 'organ-grip', label: 'Grip the nearest visible structure so the layer moves out of the way.' },
                  { id: 'maximum-lift', label: 'Pull to maximum height quickly so the field stays open.' }
                ],
                predictionCoaching: 'Use the opened edge, gradual lift, and bounded tension.',
                reflectionPrompt: 'What change indicates that retraction helped rather than harmed the field?',
                reflectionCorrectId: 'exposure-stable',
                reflectionOptions: [
                  { id: 'trauma-rise', label: 'The edge becomes more irregular and tissue risk rises.' },
                  { id: 'exposure-stable', label: 'Exposure and stability improve without a sharp increase in trauma or risk.' },
                  { id: 'field-covered', label: 'The lifted layer crosses the observation field.' }
                ],
                reflectionCoaching: 'Compare exposure and stability with trauma and risk.'
              },
              pin: {
                title: 'Plan stabilization',
                predictionPrompt: 'Which pin arrangement creates a stable, observable field?',
                predictionCorrectId: 'opposing-anchors',
                predictionOptions: [
                  { id: 'center-pins', label: 'Place both pins near the center of the observation field.' },
                  { id: 'opposing-anchors', label: 'Place well-spaced anchors at opposing ends of the opened corridor, away from structures.' },
                  { id: 'same-side', label: 'Place both pins together on the easiest side to reach.' }
                ],
                predictionCoaching: 'Stability comes from spacing and opposition while preserving the observation field.',
                reflectionPrompt: 'What evidence shows that the anchoring step is complete?',
                reflectionCorrectId: 'stable-open-field',
                reflectionOptions: [
                  { id: 'single-anchor', label: 'One anchor is present anywhere on the specimen.' },
                  { id: 'pins-near-target', label: 'Both anchors are close to the structure that will be probed.' },
                  { id: 'stable-open-field', label: 'Two separated anchors hold the opening stable without obstructing visible anatomy.' }
                ],
                reflectionCoaching: 'Check anchor count, spacing, field stability, and obstruction.'
              },
              probe: {
                title: 'Plan structure tracing',
                predictionPrompt: 'Which probe plan produces useful anatomical evidence?',
                predictionCorrectId: 'visible-low-pressure',
                predictionOptions: [
                  { id: 'hidden-target', label: 'Trace the named structure even if it is still covered or obscured.' },
                  { id: 'firm-contact', label: 'Use firm pressure so resistance is unmistakable.' },
                  { id: 'visible-low-pressure', label: 'Choose a visible exposed boundary and trace it with calibrated, controlled pressure.' }
                ],
                predictionCoaching: 'A valid trace begins on visible anatomy and uses controlled contact.',
                reflectionPrompt: 'Which result supports a successful probe trace?',
                reflectionCorrectId: 'boundary-preserved',
                reflectionOptions: [
                  { id: 'boundary-preserved', label: 'The visible boundary is traced and identified without increasing tissue stress.' },
                  { id: 'deep-displacement', label: 'The structure shifts deeply under pressure during the trace.' },
                  { id: 'name-only', label: 'The structure name appears, even though no boundary was followed.' }
                ],
                reflectionCoaching: 'Use boundary contact, identification, and preserved tissue condition as evidence.'
              }
            };
            return definitions[action] || null;
          }
          function procedureLearningEntry(action, state) {
            state = state || currentProcedure || {};
            var checks = isPlainDissectionRecord(state.learningChecks) ? state.learningChecks : null;
            if (!checks && action && procedureActionComplete(state, action)) {
              return { predictionCorrect: true, reflectionCorrect: true, legacy: true };
            }
            return checks && isPlainDissectionRecord(checks[action]) ? checks[action] : {};
          }
          function pendingProcedureReflectionAction(state) {
            state = state || currentProcedure || {};
            var ordered = procedureProtocol.order || [];
            for (var learningIndex = 0; learningIndex < ordered.length; learningIndex++) {
              var learningAction = ordered[learningIndex];
              if (procedureActionComplete(state, learningAction) && !procedureLearningEntry(learningAction, state).reflectionCorrect) return learningAction;
            }
            return null;
          }
          function procedureLearningGateFor(action, state) {
            state = state || currentProcedure || {};
            var pendingReflection = pendingProcedureReflectionAction(state);
            if (pendingReflection) {
              return { required: true, phase: 'reflect', action: pendingReflection, definition: procedureLearningDefinition(pendingReflection), entry: procedureLearningEntry(pendingReflection, state) };
            }
            var definition = procedureLearningDefinition(action);
            if (!definition) return { required: false, phase: action === 'complete' ? 'complete' : 'perform', action: action, definition: null, entry: {} };
            var entry = procedureLearningEntry(action, state);
            if (!entry.predictionCorrect) return { required: true, phase: 'predict', action: action, definition: definition, entry: entry };
            return { required: false, phase: procedureActionComplete(state, action) ? 'verified' : 'perform', action: action, definition: definition, entry: entry };
          }
          function procedureLearningSummary(state) {
            state = state || currentProcedure || {};
            var ordered = procedureProtocol.order || [];
            var verified = ordered.filter(function (action) {
              var entry = procedureLearningEntry(action, state);
              return procedureActionComplete(state, action) && !!entry.predictionCorrect && !!entry.reflectionCorrect;
            }).length;
            return { verified: verified, total: ordered.length, score: ordered.length ? Math.round(verified / ordered.length * 100) : 100 };
          }
          function recordProcedureLearningChoice(action, phase, choiceId) {
            var definition = procedureLearningDefinition(action);
            if (!definition || (phase !== 'prediction' && phase !== 'reflection')) return false;
            if (phase === 'reflection' && !procedureActionComplete(currentProcedure, action)) {
              setProcedureFeedback('Perform the planned action before explaining its observed result.', 'caution');
              return false;
            }
            var correctId = definition[phase + 'CorrectId'];
            var isCorrect = choiceId === correctId;
            var checks = Object.assign({}, currentProcedure.learningChecks || {});
            var entry = Object.assign({}, checks[action] || {});
            entry[phase + 'Choice'] = choiceId;
            entry[phase + 'Correct'] = isCorrect;
            entry[phase + 'Attempts'] = (Number(entry[phase + 'Attempts']) || 0) + 1;
            entry[phase + 'Feedback'] = isCorrect
              ? (phase === 'prediction' ? 'Plan confirmed. Demonstrate the action on the specimen; the action will not run automatically.' : 'Interpretation verified. The next protocol decision is now available.')
              : definition[phase + 'Coaching'];
            entry[phase + 'AnsweredAt'] = Date.now();
            checks[action] = entry;
            updateProcedure({ learningChecks: checks }, entry[phase + 'Feedback'], null, isCorrect ? 'success' : 'caution');
            if (isCorrect) {
              setTimeout(function () {
                var nextFocusId = phase === 'prediction' ? 'diss-canvas' : 'diss-next-action-title';
                var focusTarget = dissectionNodeById(nextFocusId);
                if (focusTarget && focusTarget.focus) focusTarget.focus();
              }, 0);
            }
            return isCorrect;
          }
          function beginGuidedObservation(org) {
            if (!org) return false;
            updMany({ guidedObservationPending: org.id, guidedObservationFeedback: null, selectedOrgan: null });
            setProcedureFeedback('Target located. Now choose the specimen observation that best supports this identification; the Guided step has not advanced yet.', 'working');
            focusDissectionTarget('diss-guided-observation-check');
            return true;
          }
          function recordGuidedObservationChoice(choiceId) {
            var pendingId = d.guidedObservationPending;
            var pendingOrgan = pendingId ? organs.find(function (organ) { return organ.id === pendingId; }) : null;
            if (!pendingOrgan || !currentGuided || pendingOrgan.id !== currentGuided.organId) {
              setProcedureFeedback('Return to the current Guided target before recording an observation.', 'caution');
              return false;
            }
            var isCorrect = choiceId === pendingOrgan.id;
            if (!isCorrect) {
              upd('guidedObservationFeedback', { correct: false, choiceId: choiceId, message: 'That observation belongs to a different structure. Compare position, tissue appearance, and function, then try again.' });
              setProcedureFeedback('Observation does not yet support the identification. Compare the visible structure with the choices and try again.', 'caution');
              return false;
            }
            var verified = Object.assign({}, d.verifiedIdentifications || {});
            verified[specimen + '|' + pendingOrgan.id] = { status: 'verified', guided: true, at: Date.now(), layer: activeLayer };
            var nextGuidedStep = guidedStep + 1;
            var guidedFinished = nextGuidedStep >= guidedSteps.length;
            updMany({ verifiedIdentifications: verified, guidedObservationPending: null, guidedObservationFeedback: { correct: true, choiceId: choiceId, message: 'Observation verified. The identification is supported by specimen evidence.' }, selectedOrgan: pendingOrgan.id, guidedStep: nextGuidedStep, guidedMode: guidedFinished ? false : true, guidedComplete: guidedFinished ? true : !!d.guidedComplete, guidedTargetIds: guidedFinished ? [] : guidedTargetIds });
            awardStemXP('dissection', 2, 'Verified ' + pendingOrgan.name + ' with specimen evidence');
            sfxDisProbe();
            if (addToast) addToast('\u2713 Observation verified for ' + pendingOrgan.name, 'success');
            if (guidedFinished) {
              awardStemXP('dissection', 10, 'Completed evidence-based guided investigation');
              if (addToast) addToast('Guided investigation complete!', 'success');
            }
            return true;
          }
          function procedureToolReadinessData(toolId, state) {
            state = state || currentProcedure;
            var tool = PROCEDURE_INSTRUMENTS.find(function (entry) { return entry.id === toolId; }) || { id: toolId, label: toolId };
            var toolState = procedureInstrumentStatus(toolId);
            var next = nextProcedureInfo();
            var learningGate = procedureLearningGateFor(next.action, state);
            var calibration = instrumentCalibrationAssessment(toolId, (state && state.toolCalibration) || toolCalibration);
            var material = procedureMaterialInteractionData(toolId, state);
            var field = observationFieldData(state);
            var tissue = normalizeTissueState(state.tissueState);
            var isHydrationTool = toolId === 'dropper' || toolId === 'wick';
            var restricted = currentScenarioDefinition().id === 'restricted-tray' && isHydrationTool;
            var protocolComplete = next.action === 'complete' && !learningGate.required;
            var completed = toolState.readiness === 'complete';
            var toolWaiting = toolState.readiness === 'waiting';
            var sequenceReady = !protocolComplete && (isHydrationTool || next.instrument === toolId);
            var learningReady = isHydrationTool || !sequenceReady || !learningGate.required;
            var calibrationReady = calibration.score >= 60;
            var preOpeningTool = toolId === 'scalpel' || toolId === 'scissors';
            var fieldReady = isHydrationTool || next.action === 'inspect' || (preOpeningTool ? tissue.clarity >= 45 && tissue.risk < 70 : field.quality >= 45);
            var viewReady = isHydrationTool || (d.anatomicalView || anatomicalView) === procedureProtocol.recommendedView;
            var materialReady = (toolId !== 'scalpel' && toolId !== 'scissors') || material.margin >= 0;
            var depthReady = toolId !== 'scalpel' || (d.incisionDepth || 'shallow') === 'shallow';
            var contactReady = materialReady && depthReady;
            var score = Math.round((sequenceReady ? 24 : 0) + (learningReady ? 20 : 0) + (calibrationReady ? 20 : Math.max(0, calibration.score / 100 * 20)) + (fieldReady ? 14 : Math.max(0, field.quality / 45 * 14)) + (viewReady ? 10 : 0) + (contactReady ? 12 : 0));
            score = Math.max(0, Math.min(100, score));
            var tone = restricted ? 'restricted' : (protocolComplete ? 'complete' : (!learningReady && sequenceReady ? 'waiting' : (completed ? 'complete' : (toolWaiting ? 'waiting' : (!sequenceReady ? 'waiting' : ((!calibrationReady || !fieldReady || !viewReady || !contactReady) ? 'caution' : 'ready'))))));
            var label = restricted ? 'Unavailable' : (protocolComplete ? 'Technique complete' : (!learningReady && sequenceReady ? (learningGate.phase === 'reflect' ? 'Explain the result' : 'Prediction required') : (completed ? 'Completed' : (toolWaiting ? toolState.label : (tone === 'ready' ? 'Ready to act' : (tone === 'waiting' ? 'Out of sequence' : 'Adjust before acting'))))));
            var cue = restricted ? 'This scenario removes ' + tool.label + ' from the tray.'
              : protocolComplete ? 'Technique protocol complete. Continue with visible structure identification and evidence.'
              : !learningReady && sequenceReady ? (learningGate.phase === 'reflect' ? 'Explain what changed after ' + procedureActionLabel(learningGate.action).toLowerCase() + ' before beginning another step.' : 'Complete the prediction checkpoint before this instrument can contact the specimen.')
              : completed ? tool.label + ' has completed its protocol role.'
              : toolWaiting ? toolState.instruction
              : !sequenceReady ? 'Use ' + ((PROCEDURE_INSTRUMENTS.find(function (entry) { return entry.id === next.instrument; }) || {}).label || next.instrument) + ' first: ' + next.label + '.'
              : !viewReady ? 'Align the specimen to the recommended ' + procedureProtocol.recommendedView + ' view.'
              : !depthReady ? 'Switch to shallow depth to protect underlying landmarks.'
              : !calibrationReady ? 'Adjust calibration into the safe working range before contact.'
              : !materialReady ? material.response + ' ' + material.failureMode
              : !fieldReady ? 'Improve field clarity before using this instrument.'
              : tool.label + ' is ready to ' + next.label.toLowerCase() + '.';
            return {
              tool: tool, score: score, tone: tone, label: label, cue: cue,
              target: sequenceReady ? next.label : 'Waiting for ' + next.label,
              safeToAct: tone === 'ready',
              checks: [
                { id: 'sequence', label: 'Sequence', ready: sequenceReady },
                { id: 'learning', label: 'Predict + explain', ready: learningReady },
                { id: 'calibration', label: 'Calibration', ready: calibrationReady },
                { id: 'field', label: 'Field + view', ready: fieldReady && viewReady },
                { id: 'contact', label: 'Contact', ready: contactReady && !restricted }
              ],
              calibration: calibration, material: material, field: field
            };
          }

          function procedureDecisionScore(state) {
            state = state || {};
            var aligned = (d.anatomicalView || anatomicalView) === procedureProtocol.recommendedView ? 1 : 0;
            var deviations = Number(state.protocolDeviations) || 0;
            var exposure = procedureExposureData(state).score / 100;
            var preservation = tissuePreservationScore(state) / 100;
            var understanding = procedureLearningSummary(state).score / 100;
            return Math.max(0, Math.min(100, Math.round(aligned * 15 + Math.max(0, 1 - deviations * 0.22) * 25 + exposure * 20 + preservation * 20 + understanding * 20)));
          }
          function procedureActionLabel(action) {
            return {
              inspect: 'Inspected', scalpel: 'Incised', scissors: 'Extended',
              forceps: 'Retracted', pin: 'Pinned', probe: 'Probed', dropper: 'Rehydrated surface', wick: 'Wicked excess saline'
            }[action] || action;
          }
          function procedureTactile(kind) {
            if (d.tactileFeedback === false) return;
            try {
              if (window._alloHaptic) {
                window._alloHaptic(kind === 'warning' ? 'error' : (kind === 'firm' ? 'break' : 'selection'));
                return;
              }
              if (navigator.vibrate) navigator.vibrate(kind === 'warning' ? [18, 28, 18] : (kind === 'firm' ? 24 : 8));
            } catch (e) {}
          }
          function procedureCoachingCue(metrics) {
            if (!metrics) return '';
            if (metrics.alignment < 72) return ' Keep the instrument tip closer to the access corridor.';
            if (metrics.coverage < 68) return ' Carry the stroke farther toward both endpoints.';
            if (metrics.control < 72) return ' Use a steadier pace and smoother hand motion.';
            return ' The stroke showed balanced alignment, coverage, and control.';
          }
          function selectProcedureTimelineEntry(entry) {
            if (!entry) return;
            upd('selectedProcedureActionAt', entry.at != null ? entry.at : Date.now());
            setProcedureFeedback('Reviewing ' + (entry.label || procedureActionLabel(entry.action)) + '. The response panel now reflects this recorded step.', 'working');
          }
          function showProcedureLastResponse() {
            var entries = (currentProcedure.actionLog || []).slice();
            var selectedAt = d.selectedProcedureActionAt;
            var entry = selectedAt != null ? (entries.find(function (candidate) { return String(candidate.at) === String(selectedAt); }) || null) : null;
            if (!entry) entry = entries.length ? entries[entries.length - 1] : null;
            if (!entry) { setProcedureFeedback('Complete at least one technique action before replaying a response.'); return; }
            var action = entry.action || 'inspect';
            var replayTool = action === 'mistake' ? activeInstrument : procedureInstrumentForAction(action);
            var options = {};
            var replayPath = action === 'scalpel' ? currentProcedure.incisionPath : (action === 'scissors' ? currentProcedure.extensionPath : null);
            if (Array.isArray(replayPath) && replayPath.length) options.path = replayPath;
            else options.point = procedureOutcomePoint(action, currentProcedure);
            queueProcedureInstrumentReplay(replayTool, options);
            setProcedureFeedback((selectedAt != null ? 'Selected response replay started: ' : 'Last response replay started: ') + (entry.outcome || procedureActionLabel(action)) + '. Watch the contact point and compare it with the recorded tissue response.', 'working');
          }
          function showProcedureReplay() {
            var actions = (currentProcedure.actionLog || []).slice();
            if (!actions.length) actions = (currentProcedure.history || []).map(function (action, idx) {
              return { action: action, label: procedureActionLabel(action), at: idx };
            });
            if (!actions.length) { setProcedureFeedback('Complete at least one technique action before replaying the attempt.'); return; }
            var duration = Math.max(2800, actions.length * 650);
            var replayCanvas = canvasRefNode;
            var replayStartedAt = Date.now();
            scheduleDissectionCanvasTimer(replayCanvas, 'procedureReplay', function () {
              clearOwnedTimedDissectionState('_procedureReplay', replayStartedAt, specimen);
            }, duration + 250);
            upd('_procedureReplay', { layer: activeLayer, startedAt: replayStartedAt, duration: duration, actions: actions });
            setProcedureFeedback('Attempt replay started. The canvas will retrace each action with pressure width, path alignment, and numbered progress.');
          }

          function procedureActionComplete(state, action) {
            state = state || {};
            if (action === 'inspect') return !!state.inspected && !(currentScenarioDefinition().id === 'orientation-rescue' && state.orientationRecheckRequired);
            if (action === 'scalpel') return !!state.incisionStarted;
            if (action === 'scissors') return !!state.incisionExtended;
            if (action === 'forceps') return !!state.retracted;
            if (action === 'pin') return (state.pins || []).length >= 2;
            if (action === 'probe') return !!state.probed;
            return false;
          }
          function procedureInstrumentForAction(action) {
            return action === 'inspect' ? 'probe' : action;
          }
          function procedureStepIndex(state) {
            var orderedActions = procedureProtocol.order || ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'];
            for (var stepIndex = 0; stepIndex < orderedActions.length; stepIndex++) {
              if (!procedureActionComplete(state, orderedActions[stepIndex])) return stepIndex;
            }
            return orderedActions.length;
          }
          function procedureInstrumentStatus(toolId) {
            if (currentScenarioDefinition().id === 'restricted-tray' && (toolId === 'dropper' || toolId === 'wick')) return { readiness: 'waiting', label: 'Unavailable', instruction: 'The restricted-tray scenario removes hydration tools; preserve the existing field.' };
            if (revealedLayers[activeLayer]) return { readiness: 'complete', label: 'Layer complete', instruction: 'Continue to the next available layer.' };
            if (toolId === 'dropper') {
              var liveTissue = normalizeTissueState(currentProcedure.tissueState);
              if (liveTissue.moisture >= 92) return { readiness: 'complete', label: 'Hydrated', instruction: 'Surface moisture is already in the optimal simulation range.' };
              return { readiness: 'ready', label: liveTissue.salineDrops ? liveTissue.salineDrops + ' applied' : 'Ready', instruction: 'Apply a controlled saline drop to restore moisture and improve visibility.' };
            }
            if (toolId === 'wick') {
              var wickTissue = normalizeTissueState(currentProcedure.tissueState);
              if (wickTissue.salineDrops > 2) return { readiness: 'ready', label: 'Pool detected', instruction: 'Touch the edge of the visible saline pool to lift excess fluid.' };
              if (currentProcedure.fieldWicked) return { readiness: 'complete', label: 'Field balanced', instruction: 'The excess film has been removed. Resume the planned technique.' };
              return { readiness: 'waiting', label: 'No excess fluid', instruction: 'Use the wick only after a broad saline film or pooling event.' };
            }
            var next = nextProcedureInfo();
            if (next.action !== 'complete' && next.instrument === toolId) {
              var nextInstruction = next.label + '.';
              if (toolId === 'scalpel') nextInstruction = 'Follow the ' + procedureProtocol.route + ' from its start marker to its finish marker, or use the action button.';
              else if (toolId === 'scissors') nextInstruction = next.label + ' through the existing opening, or use the action button.';
              else if (toolId === 'forceps') nextInstruction = 'Press on the opened tissue edge, drag away to lift within the safe envelope, then release; or use the action button.';
              else if (toolId === 'pin') nextInstruction = ((currentProcedure.pins || []).length ? procedureProtocol.pinSecond : procedureProtocol.pinFirst) + ' at the indicated endpoint.';
              else if (next.action === 'inspect') nextInstruction = procedureProtocol.actions[0] + ' before using a cutting instrument.';
              else if (toolId === 'probe') nextInstruction = next.label + ' by selecting a currently exposed structure.';
              return { readiness: 'ready', label: toolId === 'pin' && (currentProcedure.pins || []).length ? '1 of 2' : 'Ready', instruction: nextInstruction };
            }
            if ((toolId === 'probe' && currentProcedure.probed) || (toolId !== 'probe' && procedureActionComplete(currentProcedure, toolId))) {
              return { readiness: 'complete', label: toolId === 'pin' ? '2 placed' : 'Complete', instruction: 'This instrument has completed its role in the current specimen protocol.' };
            }
            var nextTool = (PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === next.instrument; }) || {}).label || next.instrument;
            return { readiness: 'waiting', label: 'Later step', instruction: 'Current protocol step: ' + next.label + '. Use ' + nextTool + ' next.' };
          }          function procedureGuidePoints() {
            var view = d.anatomicalView || anatomicalView;
            var paths = {
              frog: { dorsal: [[0.50,0.27],[0.50,0.49],[0.50,0.72]], ventral: [[0.49,0.29],[0.50,0.50],[0.51,0.70]], lateral: [[0.47,0.31],[0.51,0.50],[0.54,0.68]], internal: [[0.50,0.30],[0.50,0.49],[0.50,0.68]] },
              worm: { dorsal: [[0.50,0.18],[0.50,0.49],[0.50,0.80]], ventral: [[0.51,0.19],[0.51,0.49],[0.51,0.79]], lateral: [[0.48,0.20],[0.50,0.49],[0.52,0.78]], internal: [[0.50,0.22],[0.50,0.49],[0.50,0.76]] },
              pig: { dorsal: [[0.30,0.47],[0.50,0.47],[0.70,0.47]], ventral: [[0.30,0.50],[0.50,0.49],[0.70,0.48]], lateral: [[0.34,0.45],[0.50,0.48],[0.66,0.51]], internal: [[0.33,0.48],[0.50,0.48],[0.67,0.48]] },
              fish: { dorsal: [[0.29,0.47],[0.50,0.47],[0.71,0.47]], ventral: [[0.29,0.51],[0.50,0.50],[0.71,0.49]], lateral: [[0.28,0.49],[0.50,0.48],[0.73,0.47]], internal: [[0.32,0.49],[0.50,0.48],[0.68,0.47]] },
              crayfish: { dorsal: [[0.31,0.46],[0.50,0.47],[0.69,0.48]], ventral: [[0.31,0.50],[0.50,0.50],[0.69,0.50]], lateral: [[0.33,0.46],[0.50,0.48],[0.67,0.51]], internal: [[0.34,0.48],[0.50,0.48],[0.66,0.48]] },
              eye: { dorsal: [[0.36,0.53],[0.50,0.47],[0.64,0.53]], ventral: [[0.37,0.50],[0.50,0.46],[0.63,0.50]], lateral: [[0.40,0.45],[0.50,0.48],[0.60,0.55]], internal: [[0.40,0.51],[0.50,0.47],[0.60,0.51]] },
              heart: { dorsal: [[0.43,0.33],[0.50,0.49],[0.55,0.67]], ventral: [[0.57,0.34],[0.50,0.49],[0.45,0.67]], lateral: [[0.46,0.34],[0.50,0.49],[0.53,0.66]], internal: [[0.48,0.35],[0.50,0.49],[0.52,0.64]] }
            };
            var shapePaths = paths[spec.bodyShape] || {};
            var raw = shapePaths[view] || shapePaths.dorsal || [[0.5,0.32],[0.5,0.5],[0.5,0.68]];
            var layerOffset = Math.min(0.018, currentLayerIdx * 0.004);
            return raw.map(function (pair, idx) { return { x: pair[0] + (idx === 1 ? layerOffset : 0), y: pair[1] }; });
          }
          var ADVANCED_SCENARIOS = [
            { id: 'precision-access', label: 'Precision access', difficulty: 'Core', prompt: 'Complete the specimen protocol at or above the instructor score target.' },
            { id: 'structure-trace', label: 'Structure trace', difficulty: 'Core', prompt: 'Identify the required number of currently exposed structures.' },
            { id: 'preservation', label: 'Preservation challenge', difficulty: 'Advanced', prompt: 'Complete the protocol within the caution limit while preserving the field.' },
            { id: 'orientation-rescue', label: 'Orientation rescue', difficulty: 'Core', prompt: 'Recognize the incorrect starting view, align the specimen, and record orientation.' },
            { id: 'obscured-field', label: 'Obscured field recovery', difficulty: 'Advanced', prompt: 'Recover a clear, stable field from low clarity and excess surface fluid.' },
            { id: 'relationship-pathway', label: 'Relationship pathway', difficulty: 'Advanced', prompt: 'Trace from the selected structure to an exposed connected structure.' },
            { id: 'restricted-tray', label: 'Restricted tray', difficulty: 'Challenge', prompt: 'Complete the protocol without dropper or absorbent-wick tools.' },
            { id: 'timed-practical', label: 'Timed practical', difficulty: 'Challenge', prompt: 'Complete the specimen protocol within four minutes while maintaining safe decisions.' }
          ];
          function currentScenarioDefinition() {
            return ADVANCED_SCENARIOS.find(function (scenario) { return scenario.id === procedureScenario; }) || ADVANCED_SCENARIOS[0];
          }
          function formatScenarioTime(seconds) {
            seconds = Math.max(0, Math.round(Number(seconds) || 0));
            return Math.floor(seconds / 60) + ':' + String(seconds % 60).padStart(2, '0');
          }
          function formatAccessibleDuration(seconds) {
            seconds = Math.max(0, Math.round(Number(seconds) || 0));
            var minutes = Math.floor(seconds / 60);
            var remainder = seconds % 60;
            var parts = [];
            if (minutes) parts.push(minutes + ' minute' + (minutes === 1 ? '' : 's'));
            if (remainder || !minutes) parts.push(remainder + ' second' + (remainder === 1 ? '' : 's'));
            return parts.join(' ') + ' remaining';
          }
          function activateProcedureScenario(scenario, restartAttempt) {
            scenario = scenario || ADVANCED_SCENARIOS[0];
            var patch = {
              procedureScenario: scenario.id,
              scenarioStartedAt: Date.now(),
              scenarioTimeRemaining: scenario.id === 'timed-practical' ? 240 : 0,
              scenarioCompletedAt: 0
            };
            if (scenario.id === 'restricted-tray' && (activeInstrument === 'dropper' || activeInstrument === 'wick')) {
              patch.activeInstrument = procedureInstrumentForAction(nextProcedureInfo().action === 'complete' ? 'probe' : nextProcedureInfo().action);
            }
            var map = Object.assign({}, d.procedureByLayer || {});
            var scenarioProcedure = restartAttempt ? {
              inspected: false, incisionStarted: false, incisionExtended: false,
              retracted: false, pins: [], probed: false, errors: 0, protocolDeviations: 0,
              history: [], actionLog: [], cautionLog: [], learningChecks: {}, tissueState: defaultTissueState()
            } : Object.assign({}, currentProcedure);
            if (restartAttempt) {
              var explored = Object.assign({}, d.exploredOrgans || {});
              Object.keys(explored).forEach(function (key) { if (key.indexOf(specimen + '|') === 0) delete explored[key]; });
              patch.exploredOrgans = explored;
              patch.selectedOrgan = null;
              patch.relationshipTraceIndex = 0;
              patch.relationshipTraceCount = 0;
              patch.compareTechniqueAttempts = false;
            }
            if (scenario.id === 'orientation-rescue') {
              var orientationLearningChecks = Object.assign({}, scenarioProcedure.learningChecks || {});
              delete orientationLearningChecks.inspect;
              patch.anatomicalView = procedureProtocol.recommendedView === 'dorsal' ? 'ventral' : 'dorsal';
              // Preserve existing technique evidence while requiring a fresh,
              // scenario-specific observation after the learner corrects the view.
              scenarioProcedure = Object.assign({}, scenarioProcedure, {
                orientationRecheckRequired: true,
                learningChecks: orientationLearningChecks
              });
              map[activeLayer] = scenarioProcedure;
              patch.procedureByLayer = map;
            }
            if (scenario.id === 'relationship-pathway') patch.relationshipMode = true;
            if (scenario.id === 'obscured-field') {
              var obscuredTissue = normalizeTissueState(scenarioProcedure.tissueState);
              obscuredTissue.clarity = 32; obscuredTissue.moisture = 78; obscuredTissue.salineDrops = 3; obscuredTissue.stability = Math.min(obscuredTissue.stability, 44); obscuredTissue.risk = Math.max(obscuredTissue.risk, 48);
              scenarioProcedure = Object.assign({}, scenarioProcedure, { tissueState: obscuredTissue, dropperPoint: procedureGuidePoints()[1], fieldWicked: false });
            }
            if (restartAttempt || scenario.id === 'obscured-field') {
              map[activeLayer] = scenarioProcedure;
              patch.procedureByLayer = map;
            }
            updMany(patch);
            setProcedureFeedback((restartAttempt ? 'Scenario restarted: ' : 'Scenario selected: ') + scenario.label + '. ' + scenario.prompt, 'working');
          }
          function procedureScenarioStatus(procedureState) {
            procedureState = procedureState || currentProcedure;
            var scenario = currentScenarioDefinition();
            var stepCount = Math.max(1, (procedureProtocol.order || []).length);
            var completedSteps = procedureStepIndex(procedureState);
            var layerExplored = organs.filter(function (org) { return !!(d.exploredOrgans || {})[specimen + '|' + org.id]; }).length;
            var metrics = procedureState.extensionMetrics || procedureState.incisionMetrics;
            var control = metrics ? (metrics.control == null ? metrics.smoothness || 0 : metrics.control) : 0;
            if (scenario.id === 'structure-trace') return { complete: layerExplored >= instructorRequiredStructures, expired: false, progress: Math.min(100, Math.round(layerExplored / instructorRequiredStructures * 100)), detail: layerExplored + '/' + instructorRequiredStructures + ' exposed structures identified' };
            if (scenario.id === 'orientation-rescue') {
              var orientationRecorded = !!procedureState.inspected && !procedureState.orientationRecheckRequired;
              var orientationComplete = anatomicalView === procedureProtocol.recommendedView && orientationRecorded;
              return { complete: orientationComplete, expired: false, progress: (anatomicalView === procedureProtocol.recommendedView ? 55 : 0) + (orientationRecorded ? 45 : 0), detail: 'Current view ' + anatomicalView + '; target ' + procedureProtocol.recommendedView + '; orientation ' + (orientationRecorded ? 'recorded' : 'not recorded') };
            }
            if (scenario.id === 'obscured-field') {
              var recoveredField = observationFieldData(procedureState);
              var recoveredTissue = normalizeTissueState(procedureState.tissueState);
              var recovered = recoveredField.quality >= 65 && recoveredTissue.salineDrops <= 2 && recoveredTissue.stability >= 48;
              return { complete: recovered, expired: false, progress: Math.min(100, Math.round(recoveredField.quality * 0.7 + (recoveredTissue.salineDrops <= 2 ? 15 : 0) + recoveredTissue.stability * 0.15)), detail: 'Field ' + recoveredField.quality + '%, saline ' + recoveredTissue.salineDrops + ', stability ' + recoveredTissue.stability + '%' };
            }
            if (scenario.id === 'relationship-pathway') {
              var relationships = anatomicalRelationships();
              var traced = relationships.filter(function (item) { return !!(d.exploredOrgans || {})[specimen + '|' + item.organ.id]; }).length;
              var relationshipComplete = !!d.selectedOrgan && (traced > 0 || (Number(d.relationshipTraceCount) || 0) > 0);
              return { complete: relationshipComplete, expired: false, progress: relationshipComplete ? 100 : (d.selectedOrgan ? 45 : 0), detail: d.selectedOrgan ? traced + ' connected structures traced from the selected landmark' : 'Select a landmark to begin' };
            }
            if (scenario.id === 'restricted-tray') {
              var hydrationActions = (procedureState.actionLog || []).filter(function (entry) { return entry.action === 'dropper' || entry.action === 'wick'; }).length;
              var restrictedComplete = completedSteps >= stepCount && hydrationActions === 0;
              return { complete: restrictedComplete, expired: false, progress: Math.min(100, Math.round(completedSteps / stepCount * 90 + (hydrationActions === 0 ? 10 : 0))), detail: hydrationActions ? 'Hydration tool used; restart this attempt for the restriction' : 'No hydration tools used' };
            }
            if (scenario.id === 'timed-practical') {
              var elapsed = Math.max(0, Math.round((Date.now() - (Number(d.scenarioStartedAt) || Date.now())) / 1000));
              var remaining = d.scenarioTimeRemaining == null ? Math.max(0, 240 - elapsed) : Math.max(0, Number(d.scenarioTimeRemaining) || 0);
              var timedComplete = completedSteps >= stepCount && remaining > 0;
              var timedExpired = remaining <= 0 && !timedComplete;
              return { complete: timedComplete, expired: timedExpired, remaining: remaining, progress: Math.min(100, Math.round(completedSteps / stepCount * 100)), detail: formatScenarioTime(remaining) + ' remaining; ' + completedSteps + '/' + stepCount + ' protocol steps complete' };
            }
            if (scenario.id === 'preservation') {
              var preservationScore = tissuePreservationScore(procedureState);
              var preservationTarget = Math.max(65, instructorTarget - 10);
              var preservationComplete = completedSteps >= stepCount && (procedureState.errors || 0) <= instructorMaxCautions && control >= instructorTarget && preservationScore >= preservationTarget;
              return { complete: preservationComplete, expired: false, progress: Math.min(100, Math.round((completedSteps / stepCount * 45) + (control / 100 * 25) + (preservationScore / 100 * 20) + ((procedureState.errors || 0) <= instructorMaxCautions ? 10 : 0))), detail: 'Control ' + control + '%, preservation ' + preservationScore + '%, cautions ' + (procedureState.errors || 0) + '/' + instructorMaxCautions };
            }
            return { complete: completedSteps >= stepCount && procedureTechniqueScore(procedureState) >= instructorTarget, expired: false, progress: Math.min(100, Math.round(procedureTechniqueScore(procedureState) / instructorTarget * 100)), detail: 'Technique score ' + procedureTechniqueScore(procedureState) + '/' + instructorTarget };
          }
          function timedProcedureScenarioExpired() {
            if (currentScenarioDefinition().id !== 'timed-practical') return false;
            var startedAt = Number(d.scenarioStartedAt) || 0;
            if (!startedAt) return false;
            return (d.scenarioTimeRemaining != null && Number(d.scenarioTimeRemaining) <= 0) || Date.now() - startedAt >= 240000;
          }
          function procedureScenarioGuidance(status, procedureState) {
            if (status.complete) return 'Objective complete. Review the debrief or restart to improve your evidence.';
            if (status.expired) return 'Time expired. Restart this scenario for a new four-minute attempt.';
            var id = currentScenarioDefinition().id;
            if (id === 'orientation-rescue') return anatomicalView === procedureProtocol.recommendedView ? 'Record orientation with Inspect.' : 'Align the view to ' + procedureProtocol.recommendedView + '.';
            if (id === 'obscured-field') return normalizeTissueState((procedureState || currentProcedure).tissueState).salineDrops > 2 ? 'Use the absorbent wick at the pool edge, then optimize illumination.' : 'Improve clarity and stability before continuing.';
            if (id === 'relationship-pathway') return d.selectedOrgan ? 'Trace the next exposed anatomical connection.' : 'Select an exposed landmark to begin the pathway.';
            if (id === 'structure-trace') return 'Identify another currently exposed structure.';
            if (id === 'restricted-tray') return 'Continue the protocol using only the available instruments.';
            return nextProcedureInfo().action === 'complete' ? 'Review the score and preservation evidence.' : 'Next checkpoint: ' + nextProcedureInfo().label + '.';
          }
          function procedureDebriefData() {
            var metrics = currentProcedure.extensionMetrics || currentProcedure.incisionMetrics;
            var strengths = [];
            if (procedureStepIndex(currentProcedure) >= (procedureProtocol.order || []).length) strengths.push('completed the full instrument sequence');
            if ((currentProcedure.errors || 0) <= instructorMaxCautions) strengths.push('worked within the caution limit');
            if (metrics && metrics.alignment >= 75) strengths.push('maintained corridor alignment');
            if (metrics && (metrics.control || 0) >= 75) strengths.push('used steady tool control');
            if (tissuePreservationScore(currentProcedure) >= 82) strengths.push('preserved tissue clarity and minimized trauma');
            var improve = !metrics ? 'Complete a scalpel or scissors stroke to generate technique feedback.' : (metrics.alignment < 75 ? 'Keep the tool tip closer to the specimen-specific access path.' : (metrics.coverage < 70 ? 'Extend the stroke toward both endpoints.' : ((metrics.control || 0) < 75 ? 'Use a steadier pace and instrument angle.' : 'Try independent mode or a stricter instructor target next.')));
            return { strength: strengths.length ? strengths.join(', ') : 'orientation and sequence awareness are still developing', improve: improve };
          }
          function anatomicalRelationships() {
            var allStructures = allSpecimenStructures();
            var selected = allStructures.find(function (org) { return org.id === d.selectedOrgan; });
            if (!selected) return [];
            var edges = CURATED_ANATOMY_RELATIONSHIPS[spec.bodyShape] || [];
            return edges.filter(function (edge) { return edge.from === selected.id || edge.to === selected.id; }).map(function (edge) {
              var isForward = edge.from === selected.id;
              var otherId = isForward ? edge.to : edge.from;
              var other = allStructures.find(function (org) { return org.id === otherId; });
              return other ? { organ: other, type: edge.type, relation: isForward ? edge.forward : edge.reverse, visible: organs.some(function (org) { return org.id === other.id; }) && structureExposureState(other, currentProcedure) === 'visible' } : null;
            }).filter(Boolean);
          }
          // Relationship profiles pair hue with line rhythm, marker geometry, and readable text.
          function anatomicalRelationshipVisualProfile(type) {
            var key = String(type || '').toLowerCase();
            if (/blood|circul|vascular|coronary/.test(key)) return { category: 'vascular', color: '#fb7185', dash: [], marker: 'circle', lineLabel: 'solid', markerLabel: 'circles' };
            if (/neural|nerve|electrical|sensory/.test(key)) return { category: 'neural', color: '#a78bfa', dash: [8, 3, 2, 3], marker: 'diamond', lineLabel: 'dash-dot', markerLabel: 'diamonds' };
            if (/digest|biliary|secretion/.test(key)) return { category: 'digestive', color: '#fbbf24', dash: [5, 3], marker: 'chevron', lineLabel: 'short-dash', markerLabel: 'chevrons' };
            if (/optic|light|aperture|accommodation|reflection/.test(key)) return { category: 'optical', color: '#38bdf8', dash: [1.5, 3.5], marker: 'triangle', lineLabel: 'dotted', markerLabel: 'triangles' };
            if (/excret|urinary/.test(key)) return { category: 'excretory', color: '#c084fc', dash: [10, 4], marker: 'ring', lineLabel: 'long-dash', markerLabel: 'rings' };
            if (/respirat|ventilat|pulmonary/.test(key)) return { category: 'respiratory', color: '#2dd4bf', dash: [7, 3, 2, 3], marker: 'double-chevron', lineLabel: 'long-short dash', markerLabel: 'double chevrons' };
            if (/joint|muscle|attachment|support|protection|separation|adjacency|locomotion|valve/.test(key)) return { category: 'structural', color: '#cbd5e1', dash: [10, 3], marker: 'square', lineLabel: 'long-dash', markerLabel: 'squares' };
            return { category: 'functional', color: '#67e8f9', dash: [3, 3], marker: 'hexagon', lineLabel: 'even-dash', markerLabel: 'hexagons' };
          }
          function anatomicalRelationshipEncodingText(type) {
            var profile = anatomicalRelationshipVisualProfile(type);
            return profile.category + ', ' + profile.lineLabel + ' line with ' + profile.markerLabel;
          }
          function traceNextAnatomicalRelationship() {
            var relationships = anatomicalRelationships().filter(function (item) { return item.visible; });
            if (!relationships.length) { setProcedureFeedback('No exposed connected structure is available from the current landmark. Improve exposure or select another structure.', 'caution'); return; }
            var index = Math.max(0, Number(d.relationshipTraceIndex) || 0) % relationships.length;
            var nextRelationship = relationships[index];
            chooseOrganFromDirectory(nextRelationship.organ);
            var completesRelationshipScenario = currentScenarioDefinition().id === 'relationship-pathway';
            updMany({ relationshipMode: true, relationshipTraceIndex: (index + 1) % relationships.length, relationshipTraceCount: (Number(d.relationshipTraceCount) || 0) + 1, scenarioCompletedAt: completesRelationshipScenario ? Date.now() : (Number(d.scenarioCompletedAt) || 0) });
            setProcedureFeedback('Relationship traced: ' + nextRelationship.relation + ' ' + nextRelationship.organ.name + ' [' + nextRelationship.type + ']. Visual code: ' + anatomicalRelationshipEncodingText(nextRelationship.type) + '.' + (completesRelationshipScenario ? ' Objective complete.' : ''), 'success');
          }
          function specimenConditionDescription() {
            return { standard: 'Standard teaching specimen', preserved: 'Preserved: firmer tissue response and muted surface color', dehydrated: 'Dehydrated: reduced sheen and greater simulated resistance', cloudy: spec.bodyShape === 'eye' ? 'Cloudy optics: reduced surface clarity' : 'Cloudy preservation film', swollen: 'Swollen: enlarged contours and tighter working space' }[specimenCondition] || 'Standard teaching specimen';
          }
          function specimenHydrationProfile() {
            return {
              frog: { label: 'cutaneous bead film', pattern: 'beads', aspectX: 1.18, aspectY: 0.82, flowX: 0.08, flowY: 0.20 },
              worm: { label: 'segment-following cuticle film', pattern: 'segments', aspectX: 0.62, aspectY: 1.52, flowX: 0.02, flowY: 0.28 },
              pig: { label: 'dermal tissue film', pattern: 'film', aspectX: 1.48, aspectY: 0.72, flowX: 0.18, flowY: 0.18 },
              fish: { label: 'scale-channel film', pattern: 'channels', aspectX: 1.58, aspectY: 0.62, flowX: 0.28, flowY: 0.08 },
              crayfish: { label: 'carapace channel film', pattern: 'channels', aspectX: 1.42, aspectY: 0.68, flowX: 0.24, flowY: 0.10 },
              eye: { label: 'corneal film', pattern: 'radial', aspectX: 1.04, aspectY: 0.96, flowX: 0.04, flowY: 0.12 },
              heart: { label: 'fiber-following myocardial film', pattern: 'fibers', aspectX: 0.88, aspectY: 1.22, flowX: 0.10, flowY: 0.24 }
            }[spec.bodyShape] || { label: 'surface hydration film', pattern: 'film', aspectX: 1, aspectY: 1, flowX: 0.08, flowY: 0.18 };
          }
          // Shared proximity geometry keeps hover guidance aligned with the same specimen-specific teaching path used for scoring.
          function nearestProcedureGuidePoint(point, guide) {
            var nearest = { x: guide[0].x, y: guide[0].y, distance: Infinity };
            for (var guideIndex = 1; guideIndex < guide.length; guideIndex++) {
              var guideA = guide[guideIndex - 1], guideB = guide[guideIndex];
              var guideVX = guideB.x - guideA.x, guideVY = guideB.y - guideA.y;
              var guideLengthSq = guideVX * guideVX + guideVY * guideVY || 1;
              var guideT = Math.max(0, Math.min(1, ((point.x - guideA.x) * guideVX + (point.y - guideA.y) * guideVY) / guideLengthSq));
              var guideX = guideA.x + guideT * guideVX, guideY = guideA.y + guideT * guideVY;
              var guideDX = point.x - guideX, guideDY = point.y - guideY;
              var guideDistance = Math.sqrt(guideDX * guideDX + guideDY * guideDY);
              if (guideDistance < nearest.distance) nearest = { x: guideX, y: guideY, distance: guideDistance };
            }
            return nearest;
          }
          function nearestVisibleStructureTarget(point) {
            var nearestStructure = null, nearestDistance = Infinity;
            organs.forEach(function (organ) {
              if (structureExposureState(organ, currentProcedure) !== 'visible') return;
              var organPoint = variedOrganPoint(organ);
              var organDistance = procedurePointDistance(point, organPoint);
              if (organDistance < nearestDistance) {
                nearestDistance = organDistance;
                nearestStructure = { x: organPoint.x, y: organPoint.y, distance: organDistance, id: organ.id, name: organ.name };
              }
            });
            return nearestStructure;
          }
          function distanceToGuide(point, guide) {
            var best = Infinity;
            for (var i = 1; i < guide.length; i++) {
              var a = guide[i - 1], b = guide[i], vx = b.x - a.x, vy = b.y - a.y;
              var lengthSq = vx * vx + vy * vy || 1;
              var t = Math.max(0, Math.min(1, ((point.x - a.x) * vx + (point.y - a.y) * vy) / lengthSq));
              var px = a.x + t * vx, py = a.y + t * vy, dx = point.x - px, dy = point.y - py;
              best = Math.min(best, Math.sqrt(dx * dx + dy * dy));
            }
            return best;
          }
          function procedurePointDistance(a, b) {
            var dx = a.x - b.x, dy = a.y - b.y;
            return Math.sqrt(dx * dx + dy * dy);
          }
          function procedurePathMetrics(points, samples) {
            var guide = procedureGuidePoints();
            if (!points || points.length < 2) return { alignment: 0, coverage: 0, smoothness: 0, control: 0, precision: 0 };
            var close = points.filter(function (point) { return distanceToGuide(point, guide) <= 0.075; }).length;
            var alignment = close / points.length;
            var first = points[0], last = points[points.length - 1];
            var forward = (procedurePointDistance(first, guide[0]) + procedurePointDistance(last, guide[guide.length - 1])) / 2;
            var reverse = (procedurePointDistance(first, guide[guide.length - 1]) + procedurePointDistance(last, guide[0])) / 2;
            var coverage = Math.max(0, Math.min(1, 1 - Math.min(forward, reverse) / 0.2));
            var turns = [];
            for (var mi = 1; mi < points.length - 1; mi++) {
              var ax = points[mi].x - points[mi - 1].x, ay = points[mi].y - points[mi - 1].y;
              var bx = points[mi + 1].x - points[mi].x, by = points[mi + 1].y - points[mi].y;
              var denom = Math.sqrt(ax * ax + ay * ay) * Math.sqrt(bx * bx + by * by);
              if (denom > 0) turns.push(Math.max(0, Math.min(1, (ax * bx + ay * by) / denom)));
            }
            var smoothness = turns.length ? turns.reduce(function (sum, value) { return sum + value; }, 0) / turns.length : 1;
            var usableSamples = (samples || []).filter(function (sample) { return sample && sample.at != null; });
            var paceControl = 1, pressureControl = 1;
            if (usableSamples.length > 2) {
              var intervals = [], pressures = [];
              for (var si = 1; si < usableSamples.length; si++) intervals.push(Math.max(1, usableSamples[si].at - usableSamples[si - 1].at));
              usableSamples.forEach(function (sample) { if (sample.pressure > 0) pressures.push(sample.pressure); });
              var meanInterval = intervals.reduce(function (sum, value) { return sum + value; }, 0) / intervals.length;
              var intervalSpread = Math.sqrt(intervals.reduce(function (sum, value) { var delta = value - meanInterval; return sum + delta * delta; }, 0) / intervals.length);
              paceControl = Math.max(0, Math.min(1, 1 - intervalSpread / Math.max(18, meanInterval * 1.8)));
              if (pressures.length > 2) {
                var meanPressure = pressures.reduce(function (sum, value) { return sum + value; }, 0) / pressures.length;
                var pressureSpread = Math.sqrt(pressures.reduce(function (sum, value) { var delta = value - meanPressure; return sum + delta * delta; }, 0) / pressures.length);
                pressureControl = Math.max(0, Math.min(1, 1 - pressureSpread / 0.22));
              }
            }
            var guideAngle = Math.atan2(guide[guide.length - 1].y - guide[0].y, guide[guide.length - 1].x - guide[0].x);
            var strokeAngle = Math.atan2(last.y - first.y, last.x - first.x);
            var angleDelta = Math.abs(Math.atan2(Math.sin(strokeAngle - guideAngle), Math.cos(strokeAngle - guideAngle)));
            angleDelta = Math.min(angleDelta, Math.abs(Math.PI - angleDelta));
            var angleControl = Math.max(0, Math.min(1, 1 - angleDelta / (Math.PI / 3)));
            var control = smoothness * 0.35 + paceControl * 0.25 + pressureControl * 0.2 + angleControl * 0.2;
            var precision = alignment * 0.52 + coverage * 0.24 + smoothness * 0.08 + control * 0.1 + angleControl * 0.06;
            return { alignment: Math.round(alignment * 100), coverage: Math.round(coverage * 100), smoothness: Math.round(smoothness * 100), control: Math.round(control * 100), angleControl: Math.round(angleControl * 100), precision: Math.round(precision * 100) };
          }
          function procedurePathQuality(points) { return procedurePathMetrics(points).precision / 100; }
          function procedureTechniqueScore(state) {
            state = state || {};
            var step = procedureStepIndex(state);
            var metrics = [state.incisionMetrics, state.extensionMetrics].filter(Boolean);
            var meanPrecision = metrics.length ? metrics.reduce(function (sum, item) { return sum + (item.precision || 0); }, 0) / metrics.length / 100 : 0;
            var meanControl = metrics.length ? metrics.reduce(function (sum, item) { return sum + (item.control == null ? item.smoothness || 0 : item.control); }, 0) / metrics.length / 100 : 0;
            var completion = step / Math.max(1, (procedureProtocol.order || []).length);
            var safety = Math.max(0, 1 - (Number(state.errors) || 0) * 0.12);
            var preservation = tissuePreservationScore(state) / 100;
            var decisionQuality = procedureDecisionScore(state) / 100;
            var calibrationValues = Object.keys(state.calibrationScores || {}).map(function (key) { return Number(state.calibrationScores[key]) || 0; });
            var calibration = calibrationValues.length ? calibrationValues.reduce(function (sum, value) { return sum + value; }, 0) / calibrationValues.length / 100 : 0;
            return Math.max(0, Math.min(100, Math.round(completion * 42 + meanPrecision * 15 + meanControl * 5 + safety * 8 + preservation * 10 + calibration * 5 + decisionQuality * 15)));
          }
          function procedureMetricSnapshot(state) {
            state = state || {};
            var metrics = state.extensionMetrics || state.incisionMetrics || {};
            var observationField = observationFieldData(state);
            return {
              score: procedureTechniqueScore(state),
              precision: Number(metrics.precision) || 0,
              coverage: Number(metrics.coverage) || 0,
              control: Number(metrics.control == null ? metrics.smoothness : metrics.control) || 0,
              angleControl: Number(metrics.angleControl) || 0,
              calibration: (function () {
                var values = Object.keys(state.calibrationScores || {}).map(function (key) { return Number(state.calibrationScores[key]) || 0; });
                return values.length ? Math.round(values.reduce(function (sum, value) { return sum + value; }, 0) / values.length) : 0;
              })(),
              calibrationScores: Object.assign({}, state.calibrationScores || {}),
              toolCalibration: normalizeToolCalibration(state.toolCalibration || toolCalibration),
              cautions: Number(state.errors) || 0,
              completedSteps: procedureStepIndex(state),
              inputType: state.inputType || 'not recorded',
              preservation: tissuePreservationScore(state),
              decisionQuality: procedureDecisionScore(state),
              anatomicalExposure: procedureExposureData(state).score,
              observationQuality: observationField.quality,
              observationField: observationField,
              moisture: normalizeTissueState(state.tissueState).moisture,
              trauma: normalizeTissueState(state.tissueState).trauma,
              risk: normalizeTissueState(state.tissueState).risk,
              tissueState: normalizeTissueState(state.tissueState)
            };
          }
          function techniqueComparisonData() {
            var entries = (((d.attemptArchive || {})[activeLayer]) || []).slice();
            if (!entries.length) return null;
            var priorIndex = entries.length - 1;
            if (currentProcedure.lastSavedAttemptId === entries[priorIndex].id) priorIndex -= 1;
            if (priorIndex < 0) return null;
            var previous = entries[priorIndex];
            var current = procedureMetricSnapshot(currentProcedure);
            return {
              previous: previous,
              current: current,
              scoreDelta: current.score - (Number(previous.score) || 0),
              precisionDelta: current.precision - (Number(previous.precision) || 0),
              coverageDelta: current.coverage - (Number(previous.coverage) || 0),
              controlDelta: current.control - (Number(previous.control) || 0),
              angleDelta: current.angleControl - (Number(previous.angleControl) || 0),
              cautionDelta: current.cautions - (Number(previous.cautions) || 0),
              preservationDelta: current.preservation - (Number(previous.preservation) || 0),
              observationDelta: current.observationQuality - (Number(previous.observationQuality) || 0)
            };
          }
          function stopTechniqueCompareReplay(canvas) {
            clearDissectionCanvasTimer(canvas || canvasRefNode, 'compareReplay');
          }
          function setTechniqueCompareReplayProgress(value) {
            stopTechniqueCompareReplay();
            var progress = Math.max(0, Math.min(1, Number(value) || 0));
            updMany({ compareReplayProgress: progress, compareReplayPlaying: false });
          }
          function toggleTechniqueCompareReplay() {
            if (d.compareReplayPlaying) {
              stopTechniqueCompareReplay();
              upd('compareReplayPlaying', false);
              setProcedureFeedback('Comparison replay paused. Scrub the slider to inspect the divergence point.');
              return;
            }
            stopTechniqueCompareReplay();
            var comparisonMotionReduced = reducedMotionEnabled;
            try { comparisonMotionReduced = reducedMotionEnabled || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) { comparisonMotionReduced = reducedMotionEnabled; }
            if (comparisonMotionReduced) {
              updMany({ compareReplayProgress: 1, compareReplayPlaying: false });
              setProcedureFeedback('Comparison paths shown at the final frame because reduced motion is active.', 'success');
              return;
            }
            var progress = 0;
            var compareCanvas = canvasRefNode;
            updMany({ compareReplayProgress: 0, compareReplayPlaying: true });
            setProcedureFeedback('Comparison replay started. Progress updates stay quiet until playback is paused or complete.', 'working');
            function advanceComparisonReplay() {
              progress = Math.min(1, progress + 0.025);
              upd('compareReplayProgress', progress);
              if (progress >= 1) {
                stopTechniqueCompareReplay(compareCanvas);
                upd('compareReplayPlaying', false);
                setProcedureFeedback('Comparison replay complete. The full current and saved paths are visible.', 'success');
                return;
              }
              scheduleDissectionCanvasTimer(compareCanvas, 'compareReplay', advanceComparisonReplay, 125);
            }
            scheduleDissectionCanvasTimer(compareCanvas, 'compareReplay', advanceComparisonReplay, 125);
          }
          function saveTechniqueAttempt() {
            stopTechniqueCompareReplay();
            if (procedureStepIndex(currentProcedure) === 0) {
              setProcedureFeedback('Complete at least one technique step before saving an attempt.', 'caution');
              return;
            }
            var snapshot = procedureMetricSnapshot(currentProcedure);
            snapshot.id = Date.now();
            snapshot.savedAt = snapshot.id;
            snapshot.layer = activeLayer;
            snapshot.view = d.anatomicalView || anatomicalView;
            snapshot.condition = specimenCondition;
            snapshot.mode = procedureMode;
            snapshot.incisionPath = (currentProcedure.incisionPath || []).map(function (point) { return { x: point.x, y: point.y }; });
            snapshot.extensionPath = (currentProcedure.extensionPath || []).map(function (point) { return { x: point.x, y: point.y }; });
            snapshot.incisionSamples = compactProcedureSamples(currentProcedure.incisionSamples);
            snapshot.extensionSamples = compactProcedureSamples(currentProcedure.extensionSamples);
            snapshot.actionLog = (currentProcedure.actionLog || []).map(function (entry) {
              return { action: entry.action, label: entry.label, outcome: entry.outcome, outcomeDetail: entry.outcomeDetail, tone: entry.tone, point: entry.point || null, at: entry.at };
            });
            snapshot.cautionLog = (currentProcedure.cautionLog || []).map(function (entry) {
              return { message: entry.message, protocolDeviation: !!entry.protocolDeviation, point: entry.point || null, at: entry.at };
            });
            snapshot.history = (currentProcedure.history || []).slice();
            var archive = Object.assign({}, d.attemptArchive || {});
            var entries = (archive[activeLayer] || []).slice();
            entries.push(snapshot);
            archive[activeLayer] = entries.slice(-6);
            var map = Object.assign({}, d.procedureByLayer || {});
            map[activeLayer] = Object.assign({}, currentProcedure, { lastSavedAttemptId: snapshot.id });
            updMany({
              attemptArchive: archive,
              procedureByLayer: map,
              compareTechniqueAttempts: entries.length > 1,
              compareReplayProgress: 1,
              compareReplayPlaying: false,
              procedureFeedback: { message: 'Attempt saved with a technique score of ' + snapshot.score + '/100. Start a new attempt to practice against this baseline.', tone: 'success', at: Date.now() }
            });
          }
          function startNewTechniqueAttempt() {
            if (!(((d.attemptArchive || {})[activeLayer]) || []).length) {
              setProcedureFeedback('Save the current attempt before starting a new comparison.', 'caution');
              return;
            }
            stopTechniqueCompareReplay();
            var map = Object.assign({}, d.procedureByLayer || {});
            map[activeLayer] = {
              inspected: false, incisionStarted: false, incisionExtended: false,
              retracted: false, pins: [], probed: false, errors: 0, history: [], actionLog: [], cautionLog: [], learningChecks: {},
              tissueState: defaultTissueState()
            };
            updMany({
              procedureByLayer: map,
              activeInstrument: 'probe',
              incisionDepth: 'shallow',
              compareTechniqueAttempts: true,
              compareReplayProgress: 0,
              compareReplayPlaying: false,
              procedureFeedback: { message: 'New attempt started. The pink dashed path shows the saved baseline for this layer.', tone: 'success', at: Date.now() }
            });
          }
          function adaptiveCoachingData() {
            var metrics = currentProcedure.extensionMetrics || currentProcedure.incisionMetrics;
            var liveTissue = normalizeTissueState(currentProcedure.tissueState);
            var cautions = currentProcedure.cautionLog || [];
            var latestCaution = cautions.length ? cautions[cautions.length - 1].message : '';
            var comparison = techniqueComparisonData();
            if (liveTissue.moisture < 38) return { focus: 'Surface moisture', suggestion: 'Apply one controlled saline drop before further traction or cutting.' };
            if (liveTissue.trauma > 42 || liveTissue.risk > 55) return { focus: 'Tissue preservation', suggestion: 'Reduce contact pressure, stabilize the field, and avoid widening the current corridor.' };
            if (liveTissue.tension > 82) return { focus: 'Retraction tension', suggestion: 'Avoid additional traction; use well-spaced anchors and probe within the open field.' };
            if ((currentProcedure.errors || 0) > instructorMaxCautions || latestCaution) {
              return { focus: 'Safety sequence', suggestion: latestCaution || 'Slow down and verify the sequence before the next instrument action.' };
            }
            if (!metrics) return { focus: 'Orientation', suggestion: 'Inspect the layer, choose shallow depth, and preview the teaching corridor before drawing.' };
            var liveObservation = observationFieldData(currentProcedure);
            if (liveObservation.quality < 55) return { focus: liveObservation.limiter, suggestion: liveObservation.guidance };
            if ((d.inspectionLens || d.lensPinned) && sel) {
              var coachingInspection = opticalInspectionData(sel, lensFocusDepth, lensMagnification, lensPinned, currentProcedure);
              if (coachingInspection.quality < 70) return { focus: 'Optical focus', suggestion: coachingInspection.suggestion };
            }
            var nextCalibration = instrumentCalibrationAssessment(nextProcedureInfo().instrument, toolCalibration);
            if (nextCalibration.score < 85) return { focus: 'Instrument calibration', suggestion: nextCalibration.detail };
            if (metrics.alignment < 75) return { focus: 'Path alignment', suggestion: 'Keep the instrument tip centered on the access corridor from start to finish.' };
            if (metrics.coverage < 70) return { focus: 'Path coverage', suggestion: 'Begin closer to the first endpoint and carry the stroke through the second endpoint.' };
            if ((metrics.angleControl || 0) < 72) return { focus: 'Instrument angle', suggestion: 'Use steadier pointer pressure and a more consistent stroke direction.' };
            if ((metrics.control || metrics.smoothness || 0) < 72) return { focus: 'Tool control', suggestion: 'Use one continuous, even-paced motion instead of several abrupt corrections.' };
            if (comparison && comparison.scoreDelta <= 0) return { focus: 'Strategy change', suggestion: 'Replay the prior attempt, then use guided mode for one deliberate correction.' };
            return { focus: 'Independent refinement', suggestion: 'Try independent mode or raise the instructor target by five points.' };
          }
          function applyAdaptiveCoaching() {
            var next = nextProcedureInfo();
            updMany({ procedureMode: 'guided', activeInstrument: next.instrument, adaptiveGuidance: true });
            showProcedureDemonstration();
          }
          function showProcedureDemonstration() {
            var demo = { layer: activeLayer, startedAt: Date.now(), duration: 3200 };
            var demoCanvas = canvasRefNode;
            scheduleDissectionCanvasTimer(demoCanvas, 'procedureDemo', function () {
              clearOwnedTimedDissectionState('_procedureDemo', demo.startedAt, specimen);
            }, demo.duration);
            upd('_procedureDemo', demo);
            setProcedureFeedback('Safe-technique demonstration playing. Watch the moving marker follow the generalized teaching corridor.');
          }
          function setProcedureFeedback(message, tone) {
            upd('procedureFeedback', { message: message, tone: tone || 'success', at: Date.now() });
            if (typeof announceToSR === 'function') announceToSR(message);
          }
          function dissectionConfidenceLabel(level) {
            var normalized = Number(level);
            if (normalized === 1) return __alloT('stem.dissection.confidence_not_sure_yet', 'Not sure yet');
            if (normalized === 2) return __alloT('stem.dissection.confidence_somewhat_sure', 'Somewhat sure');
            return normalized === 3 ? __alloT('stem.dissection.confidence_confident', 'Confident') : '';
          }
          function copyDissectionText(text, successMessage, failureMessage) {
            function reportCopyFailure() {
              if (addToast) addToast(failureMessage, 'error');
              setProcedureFeedback(failureMessage, 'caution');
            }
            try {
              if (typeof navigator === 'undefined' || !navigator.clipboard || typeof navigator.clipboard.writeText !== 'function') {
                reportCopyFailure();
                return false;
              }
              var copyResult = navigator.clipboard.writeText(text);
              if (!copyResult || typeof copyResult.then !== 'function') {
                reportCopyFailure();
                return false;
              }
              Promise.resolve(copyResult).then(function () {
                if (addToast) addToast(successMessage, 'success');
                setProcedureFeedback(successMessage, 'success');
              }, reportCopyFailure);
              return true;
            } catch (e) {
              reportCopyFailure();
              return false;
            }
          }
          function procedureOutcomePoint(action, patch) {
            patch = patch || {};
            var actionPath = action === 'scissors' ? (patch.extensionPath || []) : (action === 'scalpel' ? (patch.incisionPath || []) : []);
            if (actionPath.length) return actionPath[Math.floor((actionPath.length - 1) / 2)];
            if (action === 'forceps' && patch.forcepsPoint) return patch.forcepsPoint;
            if (action === 'pin' && patch.pins && patch.pins.length) return patch.pins[patch.pins.length - 1];
            if (action === 'dropper' && patch.dropperPoint) return patch.dropperPoint;
            if (action === 'wick' && patch.wickPoint) return patch.wickPoint;
            if (action === 'probe' && patch.probedOrganId) {
              var outcomeOrgan = organs.find(function (organ) { return organ.id === patch.probedOrganId; });
              if (outcomeOrgan) return variedOrganPoint(outcomeOrgan);
            }
            if (patch.point && isFinite(patch.point.x) && isFinite(patch.point.y)) return patch.point;
            var fallbackPath = patch.extensionPath || patch.incisionPath || [];
            if (fallbackPath.length) return fallbackPath[Math.floor((fallbackPath.length - 1) / 2)];
            var outcomeGuide = procedureGuidePoints();
            return outcomeGuide[Math.floor((outcomeGuide.length - 1) / 2)] || { x: 0.5, y: 0.48 };
          }
          function procedureOutcomeFeedbackData(action, patch, beforeState, afterState, requestedTone) {
            beforeState = normalizeTissueState(beforeState); afterState = normalizeTissueState(afterState);
            var primaryMetric = {
              inspect: 'clarity', scalpel: 'exposure', scissors: 'exposure', forceps: 'exposure',
              pin: 'stability', probe: 'clarity', dropper: 'moisture', wick: 'clarity', mistake: 'risk'
            }[action] || 'clarity';
            var outcomeLabels = {
              inspect: 'ORIENTATION RECORDED', scalpel: 'ACCESS OPENED', scissors: 'OPENING EXTENDED',
              forceps: 'LAYER LIFTED', pin: 'FIELD STABILIZED', probe: 'STRUCTURE TRACED',
              dropper: 'MOISTURE RESTORED', wick: 'POOL REMOVED', mistake: 'TECHNIQUE CAUTION'
            };
            var metricLabels = { clarity: 'CLARITY', exposure: 'EXPOSURE', stability: 'STABILITY', moisture: 'MOISTURE', risk: 'RISK' };
            var primaryDelta = Number(afterState[primaryMetric]) - Number(beforeState[primaryMetric]);
            var riskDelta = Number(afterState.risk) - Number(beforeState.risk);
            var traumaDelta = Number(afterState.trauma) - Number(beforeState.trauma);
            var calibrationScore = Number(patch && patch.calibrationScore);
            var caution = action === 'mistake' || requestedTone === 'caution' || riskDelta >= 5 || traumaDelta >= 4 || (calibrationScore > 0 && calibrationScore < 85);
            var detail = (metricLabels[primaryMetric] || primaryMetric.toUpperCase()) + ' ' + (primaryDelta >= 0 ? '+' : '') + Math.round(primaryDelta) + '%';
            if (action !== 'mistake' && riskDelta >= 3) detail += ' \u00B7 RISK +' + Math.round(riskDelta) + '%';
            return { label: outcomeLabels[action] || procedureActionLabel(action).toUpperCase(), detail: detail, tone: caution ? 'caution' : 'success' };
          }
          function scheduleReducedCanvasPulseClear(canvasEl, field, duration) {
            var motionReduced = reducedMotionEnabled;
            try { motionReduced = motionReduced || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) {}
            if (!motionReduced || !canvasEl) return;
            var timerKey = field === '_toolContactPulse' ? '_dissReducedContactTimer' : '_dissReducedOutcomeTimer';
            if (canvasEl[timerKey]) clearTimeout(canvasEl[timerKey]);
            var queuedPulse = canvasEl[field];
            if (canvasEl._drawDissectionNow) canvasEl._drawDissectionNow();
            canvasEl[timerKey] = setTimeout(function () {
              if (canvasEl[field] === queuedPulse) canvasEl[field] = null;
              canvasEl[timerKey] = null;
              if (canvasEl._drawDissectionNow) canvasEl._drawDissectionNow();
            }, Math.max(120, Number(duration) || 900) + 40);
          }
          function queueProcedureOutcomeFeedback(action, patch, beforeState, afterState, requestedTone) {
            if (typeof document === 'undefined') return;
            var outcomeCanvas = queryDissectionNode('[data-diss-canvas]');
            if (!outcomeCanvas) return;
            var outcome = procedureOutcomeFeedbackData(action, patch, beforeState, afterState, requestedTone);
            var outcomePoint = procedureOutcomePoint(action, patch);
            outcomeCanvas._toolOutcomePulse = {
              x: outcomePoint.x, y: outcomePoint.y, action: action, label: outcome.label, detail: outcome.detail,
              tone: outcome.tone, layer: activeLayer,
              beforeTissue: normalizeTissueState(beforeState), afterTissue: normalizeTissueState(afterState),
              at: Date.now(), duration: reducedMotionEnabled ? 1100 : 1500
            };
            scheduleReducedCanvasPulseClear(outcomeCanvas, '_toolOutcomePulse', outcomeCanvas._toolOutcomePulse.duration);
          }
          function procedureTransitionSnapshot(procedureState) {
            var snapshot = Object.assign({}, procedureState || {});
            snapshot.tissueState = normalizeTissueState(snapshot.tissueState);
            snapshot.pins = (snapshot.pins || []).map(function (pin) { return { x: pin.x, y: pin.y }; });
            snapshot.incisionPath = Array.isArray(snapshot.incisionPath) ? snapshot.incisionPath.map(function (point) { return { x: point.x, y: point.y }; }) : null;
            snapshot.extensionPath = Array.isArray(snapshot.extensionPath) ? snapshot.extensionPath.map(function (point) { return { x: point.x, y: point.y }; }) : null;
            return snapshot;
          }
          function cloneProcedureUndoValue(value) {
            if (Array.isArray(value)) return value.map(cloneProcedureUndoValue);
            if (value && typeof value === 'object') {
              var copy = {};
              Object.keys(value).forEach(function (key) { copy[key] = cloneProcedureUndoValue(value[key]); });
              return copy;
            }
            return value;
          }
          function procedureActionUndoState(action, procedureState) {
            var snapshot = {};
            var sourceState = procedureState || {};
            dissectionUndoFieldsForAction(action).forEach(function (field) {
              var value = Object.prototype.hasOwnProperty.call(sourceState, field) && sourceState[field] !== undefined
                ? sourceState[field]
                : dissectionUndoDefaultValue(field);
              snapshot[field] = cloneProcedureUndoValue(value);
            });
            return snapshot;
          }
          function queueProcedureUndoTransition(action, beforeProcedure, afterProcedure) {
            if (typeof document === 'undefined') return;
            var undoCanvas = queryDissectionNode('[data-diss-canvas]');
            if (!undoCanvas) return;
            var undoBefore = procedureTransitionSnapshot(beforeProcedure);
            var undoAfter = procedureTransitionSnapshot(afterProcedure);
            var undoPoint = procedureOutcomePoint(action, undoBefore);
            var undoOutcome = procedureOutcomeFeedbackData(action, undoBefore, undoBefore.tissueState, undoAfter.tissueState);
            undoCanvas._toolOutcomePulse = {
              x: undoPoint.x, y: undoPoint.y, action: 'undo-' + action,
              label: 'UNDO · ' + procedureActionLabel(action).toUpperCase(), detail: undoOutcome.detail,
              tone: 'success', layer: activeLayer, beforeTissue: undoBefore.tissueState, afterTissue: undoAfter.tissueState,
              beforeProcedure: undoBefore, at: Date.now(), duration: reducedMotionEnabled ? 1100 : 1500
            };
            scheduleReducedCanvasPulseClear(undoCanvas, '_toolOutcomePulse', undoCanvas._toolOutcomePulse.duration);
          }
          function updateProcedure(patch, message, action, tone) {
            var map = Object.assign({}, d.procedureByLayer || {});
            var tissueBefore = normalizeTissueState(currentProcedure.tissueState);
            var next = Object.assign({}, currentProcedure, patch);
            if (action) next.tissueState = evolveTissueState(action, patch, tissueBefore);
            else next.tissueState = normalizeTissueState(next.tissueState);
            if (action) {
              next.history = (currentProcedure.history || []).concat([action]).slice(-12);
              var actionOutcome = procedureOutcomeFeedbackData(action, patch, tissueBefore, next.tissueState, tone);
              next.actionLog = (currentProcedure.actionLog || []).concat([{
                inputType: patch.inputType || 'unknown',
                assistance: patch.inputType === 'motor-neutral' ? 'motor-neutral' : 'none',
                action: action,
                label: procedureActionLabel(action),
                outcome: actionOutcome.label,
                outcomeDetail: actionOutcome.detail,
                tone: actionOutcome.tone,
                point: patch.point || patch.forcepsPoint || patch.dropperPoint || patch.wickPoint || (patch.pins && patch.pins.length ? patch.pins[patch.pins.length - 1] : null) || (patch.incisionPath && patch.incisionPath.length ? patch.incisionPath[patch.incisionPath.length - 1] : (patch.extensionPath && patch.extensionPath.length ? patch.extensionPath[patch.extensionPath.length - 1] : null)),
                at: Date.now(),
                calibrationScore: Number(patch.calibrationScore) || null,
                toolCalibration: patch.toolCalibration ? Object.assign({}, patch.toolCalibration) : null,
                interactionMetrics: patch.forcepsDragMetrics ? Object.assign({}, patch.forcepsDragMetrics) : (patch.pinDragMetrics ? Object.assign({}, patch.pinDragMetrics) : (patch.probeDragMetrics ? Object.assign({}, patch.probeDragMetrics) : (patch.dropperDragMetrics ? Object.assign({}, patch.dropperDragMetrics) : (patch.wickDragMetrics ? Object.assign({}, patch.wickDragMetrics) : null)))),
                undoState: procedureActionUndoState(action, currentProcedure),
                calibrationScoresBefore: Object.assign({}, currentProcedure.calibrationScores || {}),
                tissueBefore: tissueBefore,
                tissueAfter: next.tissueState
              }]).slice(-18);
              procedureTactile(action === 'scalpel' || action === 'scissors' || action === 'forceps' ? 'firm' : 'tick');
              queueProcedureOutcomeFeedback(action, patch, tissueBefore, next.tissueState, tone);
            }
            map[activeLayer] = next;
            var scenarioCheckpoint = procedureScenarioStatus(next);
            var justCompletedScenario = scenarioCheckpoint.complete && !d.scenarioCompletedAt;
            var enrichedMessage = message + (action ? ' Scenario checkpoint: ' + scenarioCheckpoint.progress + '%. ' + procedureScenarioGuidance(scenarioCheckpoint, next) : '') + (justCompletedScenario ? ' Objective complete.' : '');
            updMany({ procedureByLayer: map, scenarioCompletedAt: justCompletedScenario ? Date.now() : (Number(d.scenarioCompletedAt) || 0), procedureFeedback: { message: enrichedMessage, tone: tone || 'success', at: Date.now() } });
            if (typeof announceToSR === 'function') announceToSR(enrichedMessage);
          }
          function procedureMistake(message, protocolDeviation) {
            procedureTactile('warning');
            var mistakeCanvas = queryDissectionNode('[data-diss-canvas]');
            var mistakePoint = mistakeCanvas && (mistakeCanvas._toolPointer || mistakeCanvas._toolContactPulse);
            var cautionLog = (currentProcedure.cautionLog || []).concat([{ message: message, protocolDeviation: !!protocolDeviation, point: mistakePoint || procedureGuidePoints()[1], at: Date.now() }]).slice(-12);
            var consequenceState = evolveTissueState('mistake', { message: message }, currentProcedure.tissueState);
            queueProcedureOutcomeFeedback('mistake', { point: mistakePoint || procedureGuidePoints()[1] }, currentProcedure.tissueState, consequenceState, 'caution');
            updateProcedure({ errors: (currentProcedure.errors || 0) + 1, protocolDeviations: (currentProcedure.protocolDeviations || 0) + (protocolDeviation ? 1 : 0), cautionLog: cautionLog, tissueState: consequenceState }, message, null, 'caution');
          }
          function performProcedureAction(action, payload) {
            payload = payload || {};
            if (timedProcedureScenarioExpired()) {
              setProcedureFeedback('Timed practical expired. Restart the scenario before recording another action.', 'caution');
              return { ok: false, reason: 'scenario-expired' };
            }
            if (revealedLayers[activeLayer]) { setProcedureFeedback('This layer is complete. Continue to the next layer.'); return; }
            if (currentScenarioDefinition().id === 'restricted-tray' && (action === 'dropper' || action === 'wick')) { setProcedureFeedback('Hydration tools are unavailable in the restricted-tray scenario. Preserve the existing field and continue with the protocol.', 'caution'); return; }
            var expectedStep = nextProcedureInfo();
            if (action !== 'dropper' && action !== 'wick' && expectedStep.action !== 'complete' && expectedStep.action !== action && !procedureActionComplete(currentProcedure, action)) {
              procedureMistake('Protocol sequence check: ' + expectedStep.label + ' must be completed before using ' + ((PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === procedureInstrumentForAction(action); }) || {}).label || action) + '.', true); return;
            }
            if (action !== 'dropper' && action !== 'wick') {
              var actionLearningGate = procedureLearningGateFor(action, currentProcedure);
              if (actionLearningGate.required) {
                var learningMessage = actionLearningGate.phase === 'reflect'
                  ? 'Explain the observed result from ' + procedureActionLabel(actionLearningGate.action).toLowerCase() + ' before beginning another action.'
                  : 'Predict the safe plan before ' + procedureActionLabel(action).toLowerCase() + '. The specimen was not changed.';
                setProcedureFeedback(learningMessage, 'caution');
                focusDissectionTarget('diss-learning-checkpoint');
                return { ok: false, reason: actionLearningGate.phase === 'reflect' ? 'reflection-required' : 'prediction-required' };
              }
            }
            if (action === 'inspect') {
              if (currentProcedure.inspected && !(currentScenarioDefinition().id === 'orientation-rescue' && currentProcedure.orientationRecheckRequired)) { setProcedureFeedback('Orientation is already recorded for this layer.'); return; }
              if ((d.anatomicalView || anatomicalView) !== procedureProtocol.recommendedView) {
                setProcedureFeedback('Align the specimen to the recommended ' + procedureProtocol.recommendedView + ' view before recording orientation.', 'caution');
                return;
              }
              var inspectNextAction = (procedureProtocol.order || [])[1] || 'scalpel';
              var inspectNextTool = (PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === procedureInstrumentForAction(inspectNextAction); }) || {}).label || inspectNextAction;
              updateProcedure({ inspected: true, orientationRecheckRequired: false, orientationObservedAt: Date.now(), inputType: payload.inputType || 'pointer' }, 'Orientation recorded for ' + procedureProtocol.title + '. Use ' + inspectNextTool + ' next: ' + (procedureProtocol.actions[1] || procedureActionLabel(inspectNextAction)) + '.', 'inspect'); return { ok: true };
            }
            if (action === 'scalpel') {
              if (!currentProcedure.inspected) { procedureMistake('Pause and inspect the layer before choosing an incision path.'); return; }
              if (currentProcedure.incisionStarted) { setProcedureFeedback('The initial incision is complete. Use scissors to extend the opening.'); return; }
              if ((d.incisionDepth || 'shallow') !== 'shallow') { procedureMistake('That depth could obscure underlying structures. Switch to shallow depth and try again; no cut was recorded.'); return; }
              var cutPath = payload.points || procedureGuidePoints();
              var cutMetrics = procedurePathMetrics(cutPath, payload.samples);
              if (cutMetrics.precision < 55 || cutMetrics.coverage < 45) { procedureMistake('The stroke needs more corridor alignment or end-to-end coverage. Try again; no cut was recorded.'); return; }
              var scalpelMaterial = procedureMaterialInteractionData('scalpel', currentProcedure);
              if (scalpelMaterial.margin < 0) { procedureMistake(scalpelMaterial.response + ' ' + scalpelMaterial.failureMode); return; }
              sfxDisCut();
              updateProcedure(calibratedActionPatch('scalpel', { incisionStarted: true, incisionPath: cutPath, incisionMetrics: cutMetrics, incisionPressure: procedureMeanPressure(payload.samples), incisionSamples: compactProcedureSamples(payload.samples), inputType: payload.inputType || currentProcedure.inputType || 'button', materialResistance: scalpelMaterial.resistance }), 'Shallow access incision complete through ' + scalpelMaterial.label.toLowerCase() + '. Precision ' + cutMetrics.precision + '%, control ' + cutMetrics.control + '%, calibration ' + toolCalibrationScore('scalpel', toolCalibration) + '%.' + procedureCoachingCue(cutMetrics) + ' Switch to scissors.', 'scalpel'); return { ok: true, path: cutPath };
            }
            if (action === 'scissors') {
              if (!currentProcedure.incisionStarted) { procedureMistake('Scissors extend an opening; begin with a shallow scalpel incision first.'); return; }
              if (currentProcedure.incisionExtended) { setProcedureFeedback('The opening is already extended. Use forceps next.'); return; }
              var extensionPath = payload.points || procedureGuidePoints();
              var extensionMetrics = procedurePathMetrics(extensionPath, payload.samples);
              if (extensionMetrics.precision < 50 || extensionMetrics.coverage < 40) { procedureMistake('Keep the scissors within the opening and extend farther toward both endpoints. Try again; no extension was recorded.'); return; }
              var scissorsMaterial = procedureMaterialInteractionData('scissors', currentProcedure);
              if (scissorsMaterial.margin < 0) { procedureMistake(scissorsMaterial.response + ' ' + scissorsMaterial.failureMode); return; }
              sfxDisCut();
              updateProcedure(calibratedActionPatch('scissors', { incisionExtended: true, extensionPath: extensionPath, extensionMetrics: extensionMetrics, extensionPressure: procedureMeanPressure(payload.samples), extensionSamples: compactProcedureSamples(payload.samples), inputType: payload.inputType || currentProcedure.inputType || 'button', materialResistance: scissorsMaterial.resistance }), 'Opening extended with material resistance ' + scissorsMaterial.resistance + '/100. Precision ' + extensionMetrics.precision + '%, control ' + extensionMetrics.control + '%, calibration ' + toolCalibrationScore('scissors', toolCalibration) + '%.' + procedureCoachingCue(extensionMetrics) + ' Use forceps next.', 'scissors'); return { ok: true, path: extensionPath };
            }
            if (action === 'forceps') {
              if (!currentProcedure.incisionExtended) { procedureMistake('Extend the opening with scissors before using forceps.'); return; }
              if (currentProcedure.retracted) { setProcedureFeedback('The layer is already retracted. Place two pins.'); return; }
              if (payload.point && distanceToGuide(payload.point, procedureGuidePoints()) > 0.11) {
                procedureMistake('Grip near the opened teaching corridor so the layer can be lifted without obscuring the field. Try again.'); return;
              }
              playDissectSound('peel');
              var forcepsDragMetrics = payload.dragMetrics || null;
              var forcepsPatch = { retracted: true, forcepsPoint: payload.point || procedureGuidePoints()[1], forcepsDragMetrics: forcepsDragMetrics, inputType: payload.inputType || (forcepsDragMetrics && forcepsDragMetrics.inputType) || 'button' };
              var forcepsTechnique = forcepsDragMetrics ? ' Direct lift ' + forcepsDragMetrics.liftPercent + '%, control ' + forcepsDragMetrics.control + '%, tension ' + forcepsDragMetrics.projectedTension + '%.' : ' Equivalent action replayed with a controlled lift.';
              updateProcedure(calibratedActionPatch('forceps', forcepsPatch), 'Layer lifted with ' + toolCalibrationScore('forceps', toolCalibration) + '% grip calibration.' + forcepsTechnique + ' Place two well-spaced pins.', 'forceps'); return { ok: true, point: forcepsPatch.forcepsPoint };
            }
            if (action === 'pin') {
              if (!currentProcedure.retracted && !procedureProtocol.preStabilize) { procedureMistake('Retract the opened layer with forceps before placing a pin.'); return; }
              if (procedureProtocol.preStabilize && !currentProcedure.inspected) { procedureMistake('Complete orientation before stabilizing the specimen.'); return; }
              var pins = (currentProcedure.pins || []).slice();
              if (pins.length >= 2) { setProcedureFeedback('Two pins already secure this layer. Use the probe next.'); return; }
              var guide = procedureGuidePoints(), defaultPoint = pins.length ? guide[guide.length - 1] : guide[0];
              var pinPoint = payload.point || defaultPoint;
              if (payload.point) {
                var endpointDistance = Math.min(procedurePointDistance(pinPoint, guide[0]), procedurePointDistance(pinPoint, guide[guide.length - 1]));
                if (endpointDistance > 0.14) { procedureMistake('Place the pin near an end of the opened corridor, away from the observation field. Try again.'); return; }
                if (pins.length && procedurePointDistance(pinPoint, pins[0]) < 0.18) { procedureMistake('The pins are too close together. Place the second pin on the opposite side of the opening.'); return; }
              }
              pins.push(pinPoint); sfxDisPin();
              var pinDragMetrics = payload.pinDragMetrics || null;
              var pinHandoff = pins.length < 2 ? procedureProtocol.pinSecond : ((procedureProtocol.order || [])[procedureStepIndex(Object.assign({}, currentProcedure, { pins: pins }))] || 'probe');
              var pinTechnique = pinDragMetrics ? ' Direct insertion ' + pinDragMetrics.insertionPercent + '%, alignment ' + pinDragMetrics.alignment + '%, control ' + pinDragMetrics.control + '%.' : ' Equivalent action replayed with a controlled anchor.';
              updateProcedure(calibratedActionPatch('pin', { pins: pins, pinDragMetrics: pinDragMetrics, inputType: payload.inputType || (pinDragMetrics && pinDragMetrics.inputType) || 'button' }), (pins.length < 2 ? 'First stabilizer placed. ' + procedureProtocol.pinSecond + '.' : 'Specimen secured with two well-spaced stabilizers. Next protocol action: ' + procedureActionLabel(pinHandoff) + '.') + ' Angle calibration ' + toolCalibrationScore('pin', toolCalibration) + '%.' + pinTechnique, 'pin'); return { ok: true, point: pinPoint };
            }
            if (action === 'dropper') {
              if (payload.point && !specimenContactContext(payload.point).onSpecimen) {
                setProcedureFeedback('Move the dropper over the specimen surface before applying saline.', 'caution'); return;
              }
              var tissueBeforeDrop = normalizeTissueState(currentProcedure.tissueState);
              if (tissueBeforeDrop.moisture >= 96 || tissueBeforeDrop.salineDrops >= 4) {
                setProcedureFeedback('The simulated surface is already fully hydrated. Continue with the next technique step.', 'caution'); return;
              }
              var dropPoint = payload.point || procedureGuidePoints()[1];
              sfxDisProbe();
              var nextDropCount = tissueBeforeDrop.salineDrops + toolCalibration.salineDose;
              var dropperDragMetrics = payload.dropperDragMetrics || null;
              var dropperTechnique = dropperDragMetrics ? ' Direct hydration pass ' + dropperDragMetrics.contactPercent + '% contact, ' + dropperDragMetrics.flowAlignment + '% flow alignment, ' + dropperDragMetrics.control + '% control, pooling risk ' + dropperDragMetrics.poolingRisk + '/100.' : ' Equivalent action replayed with calibrated dose.';
              updateProcedure(
                calibratedActionPatch('dropper', { surfaceCleared: true, dropperPoint: dropPoint, salineDrops: nextDropCount, dropperDragMetrics: dropperDragMetrics, inputType: payload.inputType || (dropperDragMetrics && dropperDragMetrics.inputType) || 'button' }),
                (spec.bodyShape === 'eye' ? 'Controlled saline dose applied to the optical field.' : 'Controlled saline dose applied to the specimen surface.') + ' Moisture and visibility updated.' + dropperTechnique,
                'dropper'
              ); return { ok: true, point: dropPoint };
            }
            if (action === 'wick') {
              var tissueBeforeWick = normalizeTissueState(currentProcedure.tissueState);
              var pooledPoint = currentProcedure.dropperPoint;
              var wickPoint = payload.point || pooledPoint;
              if (!pooledPoint || tissueBeforeWick.salineDrops <= 2) {
                setProcedureFeedback('No excess saline is present. Keep the wick dry and continue the planned technique.', 'caution'); return;
              }
              if (payload.point && !specimenContactContext(payload.point).onSpecimen) {
                setProcedureFeedback('Move the absorbent wick onto the specimen and touch only the pool edge.', 'caution'); return;
              }
              if (payload.point && procedurePointDistance(payload.point, pooledPoint) > 0.16) {
                setProcedureFeedback('Move the wick closer to the visible pool edge so exposed anatomy is not disturbed.', 'caution'); return;
              }
              sfxDisProbe();
              var wickDragMetrics = payload.wickDragMetrics || null;
              var wickTechnique = wickDragMetrics ? ' Direct recovery pass ' + wickDragMetrics.recoveryPercent + '% recovery, ' + wickDragMetrics.edgeAlignment + '% edge alignment, ' + wickDragMetrics.control + '% control.' : ' Equivalent action replayed at the pool edge.';
              updateProcedure(
                { wickPoint: wickPoint, fieldWicked: true, wickDragMetrics: wickDragMetrics, inputType: payload.inputType || (wickDragMetrics && wickDragMetrics.inputType) || 'button' },
                'Excess saline wicked from the field. Pooling risk decreased and the viewing film is balanced.' + wickTechnique,
                'wick'
              ); return { ok: true, point: wickPoint };
            }
            if (action === 'probe') {
              if ((currentProcedure.pins || []).length < 2) { procedureMistake('Secure the retracted layer with two pins before probing.'); return; }
              var target = payload.organ || organs.find(function (org) { return structureExposureState(org, currentProcedure) === 'visible' && !(d.exploredOrgans || {})[specimen + '|' + org.id]; }) || organs.find(function (org) { return structureExposureState(org, currentProcedure) === 'visible'; });
              if (!target) { procedureMistake('No structure is sufficiently exposed to probe. Improve the opening, stability, clarity, or recommended view first.'); return; }
              if (structureExposureState(target, currentProcedure) !== 'visible') { procedureMistake(target.name + ' is not yet exposed enough for safe probing. Continue the specimen protocol before tracing it.'); return; }
              chooseOrganFromDirectory(target); sfxDisProbe();
              var probeDragMetrics = payload.probeDragMetrics || null;
              var probeTechnique = probeDragMetrics ? ' Direct palpation ' + probeDragMetrics.contactPercent + '% contact, ' + probeDragMetrics.alignment + '% alignment, ' + probeDragMetrics.control + '% control, resistance ' + probeDragMetrics.resistance + '/100.' : ' Equivalent action replayed with calibrated contact.';
              updateProcedure(calibratedActionPatch('probe', { probed: true, probedOrganId: target.id, probePressure: toolCalibration.probePressure / 100, probeDragMetrics: probeDragMetrics, inputType: payload.inputType || (probeDragMetrics && probeDragMetrics.inputType) || 'button' }), 'Probe trace complete: ' + target.name + '. Pressure calibration ' + toolCalibrationScore('probe', toolCalibration) + '%.' + probeTechnique + ' Record evidence or complete the layer.', 'probe');
              return { ok: true, organ: target, point: variedOrganPoint(target) };
            }
          }
          function undoProcedureAction() {
            var history = (currentProcedure.history || []).slice(), action = history.pop();
            if (!action) { setProcedureFeedback('There is no technique action to undo.'); return; }
            var previousActionLog = (currentProcedure.actionLog || []).slice();
            var lastActionEntry = previousActionLog[previousActionLog.length - 1];
            var patch = { history: history, actionLog: previousActionLog.slice(0, -1) };
            var restoredLearningChecks = Object.assign({}, currentProcedure.learningChecks || {});
            if (restoredLearningChecks[action]) {
              restoredLearningChecks[action] = Object.assign({}, restoredLearningChecks[action], { reflectionChoice: null, reflectionCorrect: false, reflectionFeedback: 'Repeat the action, then explain the new observed response.' });
              patch.learningChecks = restoredLearningChecks;
            }
            if (lastActionEntry && lastActionEntry.tissueBefore) patch.tissueState = normalizeTissueState(lastActionEntry.tissueBefore);
            if (lastActionEntry && lastActionEntry.calibrationScoresBefore) patch.calibrationScores = Object.assign({}, lastActionEntry.calibrationScoresBefore);
            if (lastActionEntry && lastActionEntry.undoState && typeof lastActionEntry.undoState === 'object' && !Array.isArray(lastActionEntry.undoState)) {
              var restoredUndoState = procedureActionUndoState(action, lastActionEntry.undoState);
              Object.keys(restoredUndoState).forEach(function (field) { patch[field] = cloneProcedureUndoValue(restoredUndoState[field]); });
            } else if (action === 'probe') { patch.probed = false; patch.probedOrganId = null; patch.probeDragMetrics = null; }
            else if (action === 'dropper') { patch.surfaceCleared = false; patch.dropperPoint = null; patch.dropperDragMetrics = null; }
            else if (action === 'wick') { patch.wickPoint = null; patch.fieldWicked = false; patch.wickDragMetrics = null; }
            else if (action === 'pin') { patch.pins = (currentProcedure.pins || []).slice(0, -1); patch.pinDragMetrics = null; }
            else if (action === 'forceps') { patch.retracted = false; patch.forcepsPoint = null; patch.forcepsDragMetrics = null; }
            else if (action === 'scissors') { patch.incisionExtended = false; patch.extensionPath = null; patch.extensionMetrics = null; patch.extensionSamples = null; }
            else if (action === 'scalpel') { patch.incisionStarted = false; patch.incisionPath = null; patch.incisionMetrics = null; patch.incisionSamples = null; }
            else if (action === 'inspect') patch.inspected = false;
            var map = Object.assign({}, d.procedureByLayer || {});
            var restoredProcedure = Object.assign({}, currentProcedure, patch);
            var restoredScenarioStatus = procedureScenarioStatus(restoredProcedure);
            queueProcedureUndoTransition(action, currentProcedure, restoredProcedure);
            map[activeLayer] = restoredProcedure;
            updMany({ procedureByLayer: map, scenarioCompletedAt: restoredScenarioStatus.complete ? (Number(d.scenarioCompletedAt) || Date.now()) : 0, procedureFeedback: { message: 'Undid ' + action + '. The visual field is returning to the previous tissue state; you can try that step again.', tone: 'success', at: Date.now() } });
          }
          function nextProcedureInfo() {
            var idx = procedureStepIndex(currentProcedure);
            var orderedActions = procedureProtocol.order || ['inspect', 'scalpel', 'scissors', 'forceps', 'pin', 'probe'];
            if (idx >= orderedActions.length) return { action: 'complete', instrument: 'probe', label: procedureProtocol.completion };
            var action = orderedActions[idx];
            var label = procedureProtocol.actions[idx] || procedureActionLabel(action);
            if (action === 'pin') label = (currentProcedure.pins || []).length ? procedureProtocol.pinSecond : procedureProtocol.pinFirst;
            return { action: action, instrument: procedureInstrumentForAction(action), label: label };
          }
          function queueProcedureInstrumentReplay(toolId, options) {
            // Equivalent action controls receive the same visible instrument choreography as direct canvas manipulation.
            if (!instrumentVisuals || typeof document === 'undefined') return;
            var replayCanvas = queryDissectionNode('[data-diss-canvas]');
            if (!replayCanvas) return;
            options = options || {};
            var guide = procedureGuidePoints();
            var path = (options.path || ((toolId === 'scalpel' || toolId === 'scissors') ? guide : [])).map(function (point) {
              return { x: point.x, y: point.y };
            });
            var replayPoint = options.point || (path.length ? path[Math.floor((path.length - 1) / 2)] : guide[Math.floor((guide.length - 1) / 2)]);
            if (!replayPoint) replayPoint = { x: 0.5, y: 0.48 };
            var vector = options.vector || (path.length > 1
              ? { x: path[path.length - 1].x - path[0].x, y: path[path.length - 1].y - path[0].y }
              : { x: 0.07, y: -0.05 });
            replayCanvas._toolPointer = null;
            replayCanvas._toolVector = vector;
            replayCanvas._toolPressure = toolId === 'probe'
              ? toolCalibration.probePressure / 100
              : (toolId === 'forceps' ? toolCalibration.forcepsGrip / 100 : 0.46);
            // dissMotionReduced is declared inside the draw loop below, so reading it here threw
            // ReferenceError. Recompute it from what IS in scope, matching that declaration: the
            // user's in-tool preference OR the operating system's. The OS setting has to be part
            // of it — honouring only the in-tool toggle would animate a replay for someone who
            // asked their whole machine for less motion.
            var replayMotionReduced = reducedMotionEnabled;
            try { replayMotionReduced = reducedMotionEnabled || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) { replayMotionReduced = reducedMotionEnabled; }
            replayCanvas._toolContactPulse = {
              x: replayPoint.x, y: replayPoint.y, tool: toolId, at: Date.now(),
              duration: replayMotionReduced ? 900 : 1400, replay: true, replayPath: path, vector: vector
            };
            scheduleReducedCanvasPulseClear(replayCanvas, '_toolContactPulse', replayCanvas._toolContactPulse.duration);
          }
          function performNextProcedureStep() {
            var next = nextProcedureInfo();
            if (next.action === 'complete') {
              var pendingStructure = organs.find(function (organ) {
                return !(d.exploredOrgans || {})[specimen + '|' + organ.id];
              });
              if (pendingStructure) {
                setProcedureFeedback('Technique complete. Inspect ' + pendingStructure.name + ' before completing this layer.', 'caution');
                return false;
              }
              var pendingEvidence = organs.find(function (organ) {
                var key = specimen + '|' + organ.id;
                return !String((d.organNotes || {})[key] || '').trim() || !(d.organConfidence || {})[key];
              });
              if (pendingEvidence) {
                upd('selectedOrgan', pendingEvidence.id);
                setProcedureFeedback('Technique complete. Finish the evidence note and confidence rating for ' + pendingEvidence.name + ' before completing this layer.', 'caution');
                return false;
              }
              peelCurrentLayer(); return true;
            }
            if (activeInstrument !== next.instrument) {
              setProcedureFeedback('Choose ' + ((PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === next.instrument; }) || {}).label || next.instrument) + ' yourself before using the motor-neutral equivalent. No action was recorded.', 'caution');
              focusDissectionTarget('diss-instrument-' + next.instrument);
              return false;
            }
            var nextReadiness = procedureToolReadinessData(next.instrument, currentProcedure);
            if (!nextReadiness.safeToAct) {
              setProcedureFeedback('Action paused. ' + nextReadiness.cue + ' Complete the setup, then try ' + next.label + ' again.', 'caution');
              return false;
            }
            var replayGuide = procedureGuidePoints();
            var replayOptions = {};
            var actionPayload = { inputType: 'motor-neutral' };
            if (next.action === 'scalpel' || next.action === 'scissors') replayOptions.path = replayGuide;
            else if (next.action === 'pin') replayOptions.point = (currentProcedure.pins || []).length ? replayGuide[replayGuide.length - 1] : replayGuide[0];
            else if (next.action === 'probe') {
              var replayOrgan = organs.find(function (organ) {
                return structureExposureState(organ, currentProcedure) === 'visible' && !(d.exploredOrgans || {})[specimen + '|' + organ.id];
              }) || organs.find(function (organ) { return structureExposureState(organ, currentProcedure) === 'visible'; });
              if (!replayOrgan) {
                setProcedureFeedback('Action paused. No structure is sufficiently visible to probe. Improve exposure, clarity, or alignment first.', 'caution');
                return false;
              }
              actionPayload.organ = replayOrgan;
              replayOptions.point = variedOrganPoint(replayOrgan);
            } else replayOptions.point = replayGuide[Math.floor((replayGuide.length - 1) / 2)];
            var actionResult = performProcedureAction(next.action, actionPayload);
            if (!actionResult || !actionResult.ok) return false;
            if (actionResult.path) replayOptions.path = actionResult.path;
            if (actionResult.point) replayOptions.point = actionResult.point;
            queueProcedureInstrumentReplay(next.instrument, replayOptions);
            return true;
          }
          function canBeginDirectInstrument(toolId) {
            var readiness = procedureToolReadinessData(toolId, currentProcedure);
            if (readiness.safeToAct) return true;
            setProcedureFeedback('Cannot start ' + readiness.tool.label + ' yet. ' + readiness.cue, 'caution');
            return false;
          }
          function directGestureProcedureKey(state) {
            var snapshot = procedureTransitionSnapshot(state || currentProcedure);
            // A missing legacy tissue record receives a fresh fallback timestamp on render.
            // Timestamps do not change whether an in-progress gesture is still safe; the
            // remaining procedure and tissue fields continue to invalidate real state changes.
            if (snapshot.tissueState) {
              snapshot.tissueState = Object.assign({}, snapshot.tissueState);
              delete snapshot.tissueState.lastUpdatedAt;
            }
            return JSON.stringify(snapshot);
          }
          function directGestureCalibrationKey() {
            return JSON.stringify(normalizeToolCalibration(toolCalibration));
          }
          function directGestureCameraKey(canvas) {
            return [Number(canvas && canvas._interactionZoom) || Number(d.canvasZoom) || 1, Number(canvas && canvas._interactionPanX) || Number(d.canvasPanX) || 0, Number(canvas && canvas._interactionPanY) || Number(d.canvasPanY) || 0]
              .map(function (value) { return Math.round(value * 1000) / 1000; }).join('|');
          }
          function captureDirectGestureContext(toolId, e) {
            var canvas = e && (e.currentTarget || e.target);
            return {
              tool: toolId, specimen: specimen, layer: activeLayer, view: d.anatomicalView || anatomicalView || 'dorsal', pointerId: e && e.pointerId,
              procedureKey: directGestureProcedureKey(currentProcedure), calibrationKey: directGestureCalibrationKey(),
              incisionDepth: d.incisionDepth || 'shallow', condition: specimenCondition || 'standard',
              lightIntensity: Math.max(20, Math.min(100, Number(d.lightIntensity) || 68)),
              scenarioId: currentScenarioDefinition().id, scenarioStartedAt: Number(d.scenarioStartedAt) || 0, cameraKey: directGestureCameraKey(canvas),
              modeKey: [!!d.quizMode, !!d.practicalMode, !!d.annotateMode, !!d.rulerMode].join('|')
            };
          }
          function directGestureContextIsCurrent(context) {
            var canvas = queryDissectionNode('[data-diss-canvas]');
            return !!context && context.specimen === specimen && context.layer === activeLayer && context.view === (d.anatomicalView || anatomicalView || 'dorsal') && context.tool === activeInstrument
              && context.procedureKey === directGestureProcedureKey(currentProcedure) && context.calibrationKey === directGestureCalibrationKey()
              && context.incisionDepth === (d.incisionDepth || 'shallow') && context.condition === (specimenCondition || 'standard')
              && context.lightIntensity === Math.max(20, Math.min(100, Number(d.lightIntensity) || 68))
              && context.scenarioId === currentScenarioDefinition().id && context.scenarioStartedAt === (Number(d.scenarioStartedAt) || 0) && !timedProcedureScenarioExpired() && context.cameraKey === directGestureCameraKey(canvas)
              && context.modeKey === [!!d.quizMode, !!d.practicalMode, !!d.annotateMode, !!d.rulerMode].join('|');
          }
          function directGesturePointerMatches(context, e) {
            return !context || context.pointerId == null || !e || e.pointerId == null || context.pointerId === e.pointerId;
          }
          function canvasHasActiveDirectGesture(canvas) {
            return !!(canvas && (canvas._toolDrawing || canvas._isPanning || (canvas._forcepsDrag && canvas._forcepsDrag.active) || (canvas._pinDrag && canvas._pinDrag.active) || (canvas._probeDrag && canvas._probeDrag.active) || (canvas._dropperDrag && canvas._dropperDrag.active) || (canvas._wickDrag && canvas._wickDrag.active)));
          }
          function canClaimDirectGesture(canvas, e) {
            if (canvas && canvas._cameraTransition) {
              setProcedureFeedback('Wait for the specimen camera movement to finish before making contact.', 'caution');
              return false;
            }
            if (!canvasHasActiveDirectGesture(canvas)) return true;
            var activePointerId = canvas._toolGestureContext && canvas._toolGestureContext.pointerId;
            if (activePointerId == null) activePointerId = [canvas._forcepsDrag, canvas._pinDrag, canvas._probeDrag, canvas._dropperDrag, canvas._wickDrag].filter(Boolean).map(function (drag) { return drag.pointerId; })[0];
            setProcedureFeedback('Finish or cancel the current specimen gesture before starting another contact' + (activePointerId != null && e && e.pointerId != null && activePointerId !== e.pointerId ? ' with a second pointer.' : '.'), 'caution');
            return false;
          }


          function canvasPointFromEvent(e) {
            var canvas = e.currentTarget || e.target, rect = canvas.getBoundingClientRect();
            var rawX = (e.clientX - rect.left) / rect.width, rawY = (e.clientY - rect.top) / rect.height;
            var zoom = Number(canvas._interactionZoom) || d.canvasZoom || 1, panX = Number(canvas._interactionPanX); if (!isFinite(panX)) panX = d.canvasPanX || 0; var panY = Number(canvas._interactionPanY); if (!isFinite(panY)) panY = d.canvasPanY || 0;
            var logicalWidth = canvas._logicalW || canvas.width || 500, logicalHeight = canvas._logicalH || canvas.height || 600;
            var mappedX = (rawX - 0.5 - panX / logicalWidth) / zoom + 0.5;
            var mappedY = (rawY - 0.5 - panY / logicalHeight) / zoom + 0.5;
            mappedX -= (Number(canvas._parallaxX) || 0) / logicalWidth;
            mappedY -= (Number(canvas._parallaxY) || 0) / logicalHeight;
            return inverseSpecimenVariation({ x: mappedX, y: mappedY });
          }
          function beginProcedureStroke(e) {
            var canvas = e.currentTarget, point = canvasPointFromEvent(e);
            if (!canBeginDirectInstrument(activeInstrument)) return false;
            if (!canClaimDirectGesture(canvas, e)) return false;
            canvas._toolGestureContext = captureDirectGestureContext(activeInstrument, e);
            canvas._toolDrawing = true; canvas._toolStroke = [point];
            canvas._toolPointer = point; canvas._toolVector = null;
            canvas._suppressToolClick = true;
            canvas._toolPressure = Number(e.pressure) || 0.5;
            canvas._toolInputType = e.pointerType || 'mouse';
            canvas._toolSamples = [{ pressure: canvas._toolPressure, at: Date.now(), pointerType: canvas._toolInputType }];
            canvas._toolResistance = { level: 'low', value: 0.12 };
            var liveStatus = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
            if (liveStatus) {
              liveStatus.setAttribute('data-tone', 'working');
              liveStatus.textContent = (activeInstrument === 'scissors' ? 'Scissors' : 'Scalpel') + ' engaged. Keep dragging through the teaching corridor, then release to check the stroke.';
            }
            if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId); e.preventDefault();
          }
          function appendProcedureStroke(e) {
            var canvas = e.currentTarget; if (!canvas._toolDrawing) return;
            if (!directGesturePointerMatches(canvas._toolGestureContext, e)) return false;
            var point = canvasPointFromEvent(e), previous = canvas._toolStroke[canvas._toolStroke.length - 1];
            if (previous) canvas._toolVector = { x: point.x - previous.x, y: point.y - previous.y };
            canvas._toolPointer = point;
            canvas._toolPressure = Number(e.pressure) || canvas._toolPressure || 0.5;
            if (!previous || Math.abs(point.x - previous.x) + Math.abs(point.y - previous.y) > 0.006) {
              canvas._toolStroke.push(point);
              canvas._toolSamples.push({ pressure: Number(e.pressure) || 0.5, at: Date.now(), pointerType: e.pointerType || canvas._toolInputType || 'mouse' });
            }
            var corridorDistance = distanceToGuide(point, procedureGuidePoints());
            var conditionFactor = { preserved: 1.15, dehydrated: 1.32, cloudy: 1.08, swollen: 1.2 }[specimenCondition] || 1;
            var effectiveResistance = corridorDistance * conditionFactor;
            var resistanceLevel = effectiveResistance <= 0.04 ? 'low' : (effectiveResistance <= 0.075 ? 'moderate' : 'high');
            canvas._toolResistance = { level: resistanceLevel, value: Math.max(0.08, Math.min(1, effectiveResistance / 0.12)) };
            if (canvas._lastResistanceLevel !== resistanceLevel) {
              procedureTactile(resistanceLevel === 'high' ? 'warning' : 'tick');
              canvas._lastResistanceLevel = resistanceLevel;
              var resistanceStatus = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
              if (resistanceStatus) {
                resistanceStatus.setAttribute('data-tone', resistanceLevel === 'high' ? 'caution' : 'working');
                resistanceStatus.textContent = ((canvas._toolGestureContext && canvas._toolGestureContext.tool) === 'scissors' ? 'Scissors' : 'Scalpel') + ': '
                  + (resistanceLevel === 'low' ? 'on the teaching path' : (resistanceLevel === 'moderate' ? 'near the corridor edge' : 'drifting outside the safe corridor'))
                  + '. ' + resistanceLevel + ' resistance; pressure ' + Math.round((canvas._toolPressure || 0) * 100) + '%.';
              }
            }
            var strokeSafety = cuttingTrajectorySafety(point, canvas._toolVector, canvas);
            var strokeSafetyKey = strokeSafety ? strokeSafety.organ.id + '|' + (strokeSafety.critical ? 'stop' : 'ahead') : 'clear';
            if (canvas._cuttingSafetyState !== strokeSafetyKey) {
              canvas._cuttingSafetyState = strokeSafetyKey;
              if (strokeSafety) {
                procedureTactile('warning');
                var strokeSafetyStatus = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
                if (strokeSafetyStatus) {
                  strokeSafetyStatus.setAttribute('data-tone', 'caution');
                  strokeSafetyStatus.textContent = (strokeSafety.critical ? 'Stop trajectory: protect ' : 'Landmark ahead: ') + strokeSafety.organ.name + '. Redirect before continuing.';
                }
              }
            }
            e.preventDefault();
          }
          function finishProcedureStroke(e) {
            var canvas = e.currentTarget; if (!canvas._toolDrawing) return false;
            if (!directGesturePointerMatches(canvas._toolGestureContext, e)) return false;
            canvas._toolDrawing = false; var points = (canvas._toolStroke || []).slice(), samples = (canvas._toolSamples || []).slice();
            var gestureContext = canvas._toolGestureContext;
            var inputType = canvas._toolInputType || (e.pointerType || 'mouse');
            canvas._toolStroke = null; canvas._toolSamples = null; canvas._toolInputType = null; canvas._toolGestureContext = null; canvas._toolResistance = null; canvas._lastResistanceLevel = null; canvas._cuttingSafetyState = null; canvas._toolPressure = 0.12; canvas._suppressToolClick = true;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId);
            if (!directGestureContextIsCurrent(gestureContext)) {
              canvas._suppressToolClick = true;
              setProcedureFeedback('Instrument stroke canceled because the tool, specimen, layer, or view changed before release. No cut was recorded.', 'working');
              return true;
            }
            if (points.length < 2) {
              procedureMistake('Drag along the corridor to practice the instrument stroke, or use the equivalent action button.');
              return true;
            }
            performProcedureAction(gestureContext.tool, { points: points, samples: samples, inputType: inputType }); return true;
          }
          function cancelProcedureStroke(e) {
            var canvas = e.currentTarget;
            if (!canvas._toolDrawing) return false;
            if (!directGesturePointerMatches(canvas._toolGestureContext, e)) return false;
            canvas._toolDrawing = false;
            canvas._toolStroke = null;
            canvas._toolSamples = null;
            canvas._toolInputType = null;
            canvas._toolResistance = null;
            canvas._toolGestureContext = null;
            canvas._lastResistanceLevel = null;
            canvas._cuttingSafetyState = null;
            canvas._toolPointer = null;
            canvas._toolIntentState = null;
            canvas._toolVector = null;
            canvas._toolPressure = null;
            canvas._suppressToolClick = false;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId);
            syncCanvasPointerPresentation(canvas, null);
            setProcedureFeedback('Instrument stroke canceled; no cut was recorded.', 'working');
            return true;
          }

          function forcepsDragAssessment(canvas, point, at) {
            var drag = canvas && canvas._forcepsDrag;
            if (!drag) return null;
            point = point || drag.current || drag.start; at = Number(at) || Date.now();
            var dx = point.x - drag.start.x, dy = point.y - drag.start.y;
            var distance = Math.sqrt(dx * dx + dy * dy), elapsed = Math.max(16, at - drag.startedAt);
            var speed = distance / (elapsed / 1000), grip = Math.max(15, Math.min(90, Number(toolCalibration.forcepsGrip) || 40));
            var material = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
            var tissue = normalizeTissueState(currentProcedure.tissueState);
            var safeMax = 0.115 + (Number(material.compliance) || 0.5) * 0.085;
            if (specimenCondition === 'dehydrated') safeMax *= 0.78;
            else if (specimenCondition === 'swollen') safeMax *= 0.88;
            var minimumLift = 0.032;
            var projectedTension = Math.round(Math.min(100, tissue.tension + distance / Math.max(0.01, safeMax) * 31 + Math.max(0, grip - 50) * 0.42));
            var peakSpeed = Math.max(Number(drag.peakSpeed) || 0, speed);
            var key = !drag.startValid ? 'position' : (distance < minimumLift ? 'grasp' : ((grip < 25 || peakSpeed > 1.35) ? 'slip' : ((grip > 50 || projectedTension > 86 || distance > safeMax) ? 'stress' : 'controlled')));
            var label = { position: 'MOVE TO OPENING', grasp: 'GRASP · DRAG', slip: 'SLIP RISK', stress: 'HIGH STRESS', controlled: 'CONTROLLED LIFT' }[key];
            var control = Math.max(0, Math.min(100, Math.round(100 - Math.max(0, peakSpeed - 0.24) * 88 - Math.max(0, distance - safeMax) / Math.max(0.01, safeMax) * 85 - Math.abs(grip - 38) * 1.05)));
            return { key: key, label: label, distance: distance, elapsed: elapsed, speed: speed, peakSpeed: peakSpeed, grip: grip, safeMax: safeMax, minimumLift: minimumLift, projectedTension: projectedTension, liftPercent: Math.max(0, Math.min(100, Math.round(distance / Math.max(0.01, safeMax) * 100))), control: control, validStart: !!drag.startValid };
          }
          function setForcepsDragStatus(canvas, assessment, finalMessage) {
            if (!canvas || !assessment) return;
            var status = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
            var caution = assessment.key === 'position' || assessment.key === 'slip' || assessment.key === 'stress';
            var message = finalMessage || (assessment.key === 'position' ? 'Forceps are outside the opened edge. Release, then grasp the highlighted corridor.' : (assessment.key === 'grasp' ? 'Forceps engaged. Drag away from the opening to lift the tissue, then release.' : (assessment.label + ': lift ' + assessment.liftPercent + ' percent; projected tension ' + assessment.projectedTension + ' percent; control ' + assessment.control + ' percent. ' + (assessment.key === 'controlled' ? 'Release to retract.' : 'Ease back into the solid safe envelope before releasing.'))));
            var stateChanged = canvas._forcepsDragStatusKey !== assessment.key;
            // Keep the canvas metrics visually live, but update the polite live region only at meaningful state changes so assistive technology is not flooded during pointer movement.
            if (status && (stateChanged || finalMessage)) { status.setAttribute('data-tone', caution ? 'caution' : 'working'); status.textContent = message; }
            canvas._toolIntentState = { tool: 'forceps', valid: assessment.key === 'controlled', label: assessment.label, tone: caution ? 'caution' : 'working', target: null, targetLabel: '', at: Date.now() };
            if (stateChanged) { procedureTactile(caution ? 'warning' : 'tick'); canvas._forcepsDragStatusKey = assessment.key; }
          }
          function beginForcepsDrag(e) {
            var canvas = e.currentTarget, point = canvasPointFromEvent(e), guide = procedureGuidePoints();
            if (!canClaimDirectGesture(canvas, e)) return false;
            if (!canBeginDirectInstrument('forceps')) return false;
            var instrumentState = procedureInstrumentStatus('forceps'), readiness = procedureToolReadinessData('forceps', currentProcedure);
            var startValid = instrumentState.readiness === 'ready' && readiness.safeToAct && currentProcedure.incisionExtended && !currentProcedure.retracted && distanceToGuide(point, guide) <= 0.11;
            canvas._forcepsDrag = { active: true, pointerId: e.pointerId, gestureContext: captureDirectGestureContext('forceps', e), start: point, current: point, startedAt: Date.now(), lastAt: Date.now(), lastPoint: point, peakSpeed: 0, samples: 1, startValid: startValid, inputType: e.pointerType || 'mouse' };
            canvas._toolPointer = point; canvas._toolVector = { x: 0, y: 0 }; canvas._toolPressure = Number(e.pressure) || toolCalibration.forcepsGrip / 100;
            canvas._suppressToolClick = true; canvas._forcepsDragStatusKey = null;
            var assessment = forcepsDragAssessment(canvas, point, Date.now()); canvas._forcepsDrag.assessment = assessment;
            setForcepsDragStatus(canvas, assessment, startValid ? null : (!readiness.safeToAct ? readiness.cue : 'Move the forceps to the opened tissue edge before dragging.'));
            if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId);
            canvas.style.cursor = startValid ? 'grabbing' : 'not-allowed';
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            e.preventDefault();
          }
          function appendForcepsDrag(e) {
            var canvas = e.currentTarget, drag = canvas._forcepsDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var point = canvasPointFromEvent(e); point.x = Math.max(0.02, Math.min(0.98, point.x)); point.y = Math.max(0.02, Math.min(0.98, point.y));
            var now = Date.now(), sampleMs = Math.max(8, now - drag.lastAt), sampleDistance = procedurePointDistance(point, drag.lastPoint || drag.start);
            drag.peakSpeed = Math.max(Number(drag.peakSpeed) || 0, sampleDistance / (sampleMs / 1000)); drag.current = point; drag.lastPoint = point; drag.lastAt = now; drag.samples += 1;
            canvas._toolPointer = point; canvas._toolVector = { x: point.x - drag.start.x, y: point.y - drag.start.y }; canvas._toolPressure = Number(e.pressure) || toolCalibration.forcepsGrip / 100;
            var assessment = forcepsDragAssessment(canvas, point, now); drag.assessment = assessment; drag.peakSpeed = assessment.peakSpeed; setForcepsDragStatus(canvas, assessment);
            canvas.style.cursor = assessment.key === 'controlled' ? 'grabbing' : (assessment.key === 'grasp' ? 'grab' : 'not-allowed');
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            e.preventDefault(); return true;
          }
          function finishForcepsDrag(e, cancelled) {
            var canvas = e.currentTarget, drag = canvas._forcepsDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var pointerWasCancelled = !!cancelled;
            var gestureContextChanged = !directGestureContextIsCurrent(drag.gestureContext);
            cancelled = pointerWasCancelled || gestureContextChanged;
            drag.active = false; var assessment = forcepsDragAssessment(canvas, drag.current, Date.now()) || drag.assessment;
            canvas._forcepsDrag = null; canvas._forcepsDragStatusKey = null; canvas._suppressToolClick = true;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId);
            syncCanvasPointerPresentation(canvas, null);
            if (cancelled) { canvas._suppressToolClick = gestureContextChanged && !pointerWasCancelled; canvas._toolPointer = null; canvas._toolVector = null; canvas._toolPressure = null; canvas._toolIntentState = null; setProcedureFeedback('Forceps lift canceled. The tissue returned to its resting position.', 'working'); return true; }
            if (!assessment || assessment.key === 'position') { setProcedureFeedback('No lift recorded. Begin on the opened tissue edge, then drag away from it.', 'caution'); return true; }
            if (assessment.key === 'grasp') { setProcedureFeedback('Forceps grasp released before the tissue was lifted. Drag farther, or use the equivalent action button.', 'working'); return true; }
            if (assessment.key === 'slip') { procedureMistake('The forceps slipped before retraction. Increase grip into the 25–50 percent range and use a slower pull; no lift was recorded.'); return true; }
            if (assessment.key === 'stress') { procedureMistake('The forceps pull exceeded the safe tension envelope. Ease the drag or reduce grip before release; no lift was recorded.'); return true; }
            var dragMetrics = { distance: Math.round(assessment.distance * 1000) / 1000, durationMs: assessment.elapsed, peakSpeed: Math.round(assessment.peakSpeed * 1000) / 1000, liftPercent: assessment.liftPercent, projectedTension: assessment.projectedTension, control: assessment.control, samples: drag.samples, inputType: drag.inputType };
            var forcepsResult = performProcedureAction('forceps', { point: drag.start, dragMetrics: dragMetrics, inputType: drag.inputType });
            if (forcepsResult && forcepsResult.ok) queueProcedureInstrumentReplay('forceps', { point: forcepsResult.point || drag.start, vector: { x: drag.current.x - drag.start.x, y: drag.current.y - drag.start.y } });
            return true;
          }
          function pinDragAssessment(canvas, point, at) {
            var drag = canvas && canvas._pinDrag;
            if (!drag) return null;
            point = point || drag.current || drag.start; at = Number(at) || Date.now();
            var dx = point.x - drag.start.x, dy = point.y - drag.start.y;
            var distance = Math.sqrt(dx * dx + dy * dy), elapsed = Math.max(16, at - drag.startedAt);
            var speed = distance / (elapsed / 1000), grip = Math.max(35, Math.min(90, Number(toolCalibration.pinAngle) || 65));
            var material = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
            var safeTravel = 0.075 + (Number(material.compliance) || 0.5) * 0.045;
            if (specimenCondition === 'dehydrated') safeTravel *= 0.82;
            var projected = dx * drag.inward.x + dy * drag.inward.y;
            var alignment = distance > 0.002 ? Math.max(-1, Math.min(1, projected / distance)) : 1;
            var insertionPercent = Math.max(0, Math.min(100, Math.round(projected / Math.max(0.01, safeTravel) * 100)));
            var spacing = drag.spacing;
            var peakSpeed = Math.max(Number(drag.peakSpeed) || 0, speed);
            var angle = grip;
            var key = !drag.startValid ? 'position'
              : (spacing < 0.18 ? 'crowded'
                : (distance < 0.026 ? 'grasp'
                  : (alignment < 0.35 ? 'direction'
                    : (angle < 55 ? 'shallow' : (angle > 75 ? 'steep' : (projected > safeTravel ? 'stress' : 'stable'))))));
            var label = { position: 'MOVE TO ENDPOINT', crowded: 'TOO CLOSE', grasp: 'GRASP · DRAG', direction: 'ALIGN SHAFT', shallow: 'SHALLOW · SLIP', steep: 'STEEP · STRESS', stress: 'DEPTH LIMIT', stable: 'STABLE ANCHOR' }[key];
            var control = Math.max(0, Math.min(100, Math.round(100 - Math.max(0, peakSpeed - 0.24) * 70 - Math.max(0, 0.72 - alignment) * 42 - Math.abs(angle - 65) * 1.2 - Math.max(0, insertionPercent - 100) * 0.42)));
            return {
              key: key, label: label, distance: distance, elapsed: elapsed, speed: speed, peakSpeed: peakSpeed,
              angle: angle, alignment: Math.round(Math.max(0, alignment) * 100), spacing: spacing, safeTravel: safeTravel,
              insertionPercent: insertionPercent, projectedDepth: Math.round(Math.max(0, projected) * 1000) / 1000,
              control: control, validStart: !!drag.startValid
            };
          }
          function setPinDragStatus(canvas, assessment, finalMessage) {
            if (!canvas || !assessment) return;
            var status = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
            var caution = ['position', 'crowded', 'direction', 'shallow', 'steep', 'stress'].indexOf(assessment.key) >= 0;
            var message = finalMessage || (assessment.key === 'position'
              ? 'Pin is outside an endpoint. Release, then press on the highlighted corridor endpoint.'
              : (assessment.key === 'grasp'
                ? 'Pin engaged. Drag inward along the shaft direction, then release when the anchor reads stable.'
                : (assessment.label + ': insertion ' + assessment.insertionPercent + ' percent; angle ' + assessment.angle + ' degrees; spacing ' + Math.round(assessment.spacing * 100) + ' percent; control ' + assessment.control + ' percent. ' + (assessment.key === 'stable' ? 'Release to place the anchor.' : 'Adjust the drag before releasing.'))));
            var stateChanged = canvas._pinDragStatusKey !== assessment.key;
            if (status && (stateChanged || finalMessage)) { status.setAttribute('data-tone', caution ? 'caution' : 'working'); status.textContent = message; }
            canvas._toolIntentState = { tool: 'pin', valid: assessment.key === 'stable', label: assessment.label, tone: caution ? 'caution' : 'working', target: null, targetLabel: '', at: Date.now() };
            if (stateChanged) { procedureTactile(caution ? 'warning' : 'tick'); canvas._pinDragStatusKey = assessment.key; }
          }
          function beginPinDrag(e) {
            var canvas = e.currentTarget, point = canvasPointFromEvent(e), guide = procedureGuidePoints();
            if (!canClaimDirectGesture(canvas, e)) return false;
            if (!canBeginDirectInstrument('pin')) return false;
            var startDistance = procedurePointDistance(point, guide[0]), endDistance = procedurePointDistance(point, guide[guide.length - 1]);
            var pinTarget = startDistance <= endDistance ? guide[0] : guide[guide.length - 1], pinIndex = startDistance <= endDistance ? 0 : guide.length - 1;
            var middle = guide[Math.floor((guide.length - 1) / 2)] || guide[1] || guide[0];
            var inwardX = middle.x - pinTarget.x, inwardY = middle.y - pinTarget.y, inwardLength = Math.sqrt(inwardX * inwardX + inwardY * inwardY) || 1;
            var pins = currentProcedure.pins || [], spacing = pins.length ? procedurePointDistance(point, pins[0]) : 1;
            var instrumentState = procedureInstrumentStatus('pin'), readiness = procedureToolReadinessData('pin', currentProcedure);
            var prerequisite = procedureProtocol.preStabilize ? currentProcedure.inspected : currentProcedure.retracted;
            var startValid = instrumentState.readiness === 'ready' && readiness.safeToAct && prerequisite && pins.length < 2 && Math.min(startDistance, endDistance) <= 0.14 && spacing >= 0.18;
            canvas._pinDrag = { active: true, pointerId: e.pointerId, gestureContext: captureDirectGestureContext('pin', e), start: point, current: point, target: pinTarget, targetIndex: pinIndex, inward: { x: inwardX / inwardLength, y: inwardY / inwardLength }, spacing: spacing, startedAt: Date.now(), lastAt: Date.now(), lastPoint: point, peakSpeed: 0, samples: 1, startValid: startValid, inputType: e.pointerType || 'mouse' };
            canvas._toolPointer = point; canvas._toolVector = { x: 0, y: 0 }; canvas._toolPressure = toolCalibration.pinAngle / 90; canvas._suppressToolClick = true; canvas._pinDragStatusKey = null;
            var assessment = pinDragAssessment(canvas, point, Date.now()); canvas._pinDrag.assessment = assessment;
            setPinDragStatus(canvas, assessment, startValid ? null : (!readiness.safeToAct ? readiness.cue : 'Move the pin to the indicated endpoint before dragging inward.'));
            if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId);
            canvas.style.cursor = startValid ? 'grabbing' : 'not-allowed';
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            e.preventDefault();
          }
          function appendPinDrag(e) {
            var canvas = e.currentTarget, drag = canvas._pinDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var point = canvasPointFromEvent(e); point.x = Math.max(0.02, Math.min(0.98, point.x)); point.y = Math.max(0.02, Math.min(0.98, point.y));
            var now = Date.now(), sampleMs = Math.max(8, now - drag.lastAt), sampleDistance = procedurePointDistance(point, drag.lastPoint || drag.start);
            drag.peakSpeed = Math.max(Number(drag.peakSpeed) || 0, sampleDistance / (sampleMs / 1000)); drag.current = point; drag.lastPoint = point; drag.lastAt = now; drag.samples += 1;
            canvas._toolPointer = point; canvas._toolVector = { x: point.x - drag.start.x, y: point.y - drag.start.y }; canvas._toolPressure = toolCalibration.pinAngle / 90;
            var assessment = pinDragAssessment(canvas, point, now); drag.assessment = assessment; drag.peakSpeed = assessment.peakSpeed; setPinDragStatus(canvas, assessment);
            canvas.style.cursor = assessment.key === 'stable' ? 'grabbing' : (assessment.key === 'grasp' ? 'grab' : 'not-allowed');
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            e.preventDefault(); return true;
          }
          function finishPinDrag(e, cancelled) {
            var canvas = e.currentTarget, drag = canvas._pinDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var pointerWasCancelled = !!cancelled;
            var gestureContextChanged = !directGestureContextIsCurrent(drag.gestureContext);
            cancelled = pointerWasCancelled || gestureContextChanged;
            drag.active = false; var assessment = pinDragAssessment(canvas, drag.current, Date.now()) || drag.assessment;
            canvas._pinDrag = null; canvas._pinDragStatusKey = null; canvas._suppressToolClick = true;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId);
            syncCanvasPointerPresentation(canvas, null);
            if (cancelled) { canvas._suppressToolClick = gestureContextChanged && !pointerWasCancelled; canvas._toolPointer = null; canvas._toolVector = null; canvas._toolPressure = null; canvas._toolIntentState = null; setProcedureFeedback('Pin placement canceled. The tissue returned to its resting position.', 'working'); return true; }
            if (!assessment || ['position', 'crowded'].indexOf(assessment.key) >= 0) { setProcedureFeedback('No anchor recorded. Begin at the indicated endpoint and keep the second pin outside the spacing ring.', 'caution'); return true; }
            if (assessment.key === 'grasp') { setProcedureFeedback('Pin engaged but was not inserted. Drag inward farther, or use the equivalent action button.', 'working'); return true; }
            if (['direction', 'shallow', 'steep', 'stress'].indexOf(assessment.key) >= 0) { procedureMistake('The pin was released outside the stable insertion envelope. Align the shaft, use a 55–75 degree angle, and release before the depth limit; no anchor was recorded.'); return true; }
            var dragMetrics = { distance: Math.round(assessment.distance * 1000) / 1000, durationMs: assessment.elapsed, peakSpeed: Math.round(assessment.peakSpeed * 1000) / 1000, insertionPercent: assessment.insertionPercent, projectedDepth: assessment.projectedDepth, alignment: assessment.alignment, spacing: Math.round(assessment.spacing * 1000) / 1000, angle: assessment.angle, control: assessment.control, samples: drag.samples, inputType: drag.inputType };
            var pinResult = performProcedureAction('pin', { point: drag.start, pinDragMetrics: dragMetrics, inputType: drag.inputType });
            if (pinResult && pinResult.ok) queueProcedureInstrumentReplay('pin', { point: pinResult.point || drag.start, vector: { x: drag.current.x - drag.start.x, y: drag.current.y - drag.start.y } });
            return true;
          }
          function probeDragAssessment(canvas, point, at) {
            var drag = canvas && canvas._probeDrag;
            if (!drag) return null;
            point = point || drag.current || drag.start; at = Number(at) || Date.now();
            var target = drag.target;
            var targetPoint = drag.targetPoint || (target ? variedOrganPoint(target) : point);
            var targetDistance = target ? procedurePointDistance(point, targetPoint) : 1;
            var elapsed = Math.max(16, at - drag.startedAt);
            var distance = Math.max(0, Number(drag.distance) || 0);
            var speed = distance / (elapsed / 1000);
            var peakSpeed = Math.max(Number(drag.peakSpeed) || 0, speed);
            var pressure = Math.max(10, Math.min(80, Number(toolCalibration.probePressure) || 30));
            var contactPercent = Math.round(Math.max(0, Math.min(100, (Number(drag.contactSamples) || 0) / Math.max(1, Number(drag.sampleCount) || 1) * 100)));
            var alignment = Math.round(Math.max(0, Math.min(100, 100 - targetDistance * 680)));
            var material = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
            var tissue = normalizeTissueState(currentProcedure && currentProcedure.tissueState);
            var depth = target ? structureOpticalDepth(target) : 'structure';
            var depthRank = { surface: 0, structure: 1, deep: 2 }[depth] || 0;
            var resistanceValue = Math.max(8, Math.min(96, Math.round((1 - (Number(material.compliance) || 0.5)) * 54 + depthRank * 16 + tissue.tension * 0.16 + (100 - tissue.moisture) * 0.08)));
            var resistance = resistanceValue < 34 ? 'LOW' : (resistanceValue < 67 ? 'MODERATE' : 'HIGH');
            var deformation = Math.round((2.5 + pressure * 0.13) * (0.58 + Math.max(0.14, Number(material.compliance) || 0.5) * 0.62));
            var state = pressure < 18
              ? { key: 'light', label: 'TOO LIGHT', detail: 'NO TACTILE SIGNAL', pattern: [2, 3] }
              : (pressure > 38
                ? { key: 'stress', label: 'EXCESS PRESSURE', detail: 'TISSUE STRESS', pattern: [2, 2] }
                : { key: 'controlled', label: 'CONTROLLED PALPATION', detail: 'BOUNDARY RESOLVED', pattern: [] });
            var key = !drag.startValid ? 'position'
              : (!target || structureExposureState(target, currentProcedure) !== 'visible' ? 'position'
                : (targetDistance > 0.095 ? 'drift'
                  : (distance < 0.018 ? 'grasp'
                    : (pressure < 18 ? 'light'
                      : (pressure > 38 ? 'stress'
                        : (contactPercent < 58 ? 'drift' : 'controlled'))))));
            var labels = { position: 'MOVE TO STRUCTURE', drift: 'TRACE ON TARGET', grasp: 'GRASP · TRACE', light: 'TOO LIGHT', stress: 'EXCESS PRESSURE', controlled: 'CONTROLLED PALPATION' };
            var pressureControl = Math.max(0, 100 - Math.abs(pressure - 28) * 1.65);
            var control = Math.max(0, Math.min(100, Math.round(pressureControl * 0.42 + alignment * 0.34 + contactPercent * 0.24 - Math.max(0, peakSpeed - 0.24) * 46)));
            return {
              key: key, label: labels[key], detail: key === 'controlled' ? state.detail : (key === 'drift' ? 'KEEP CONTACT' : state.detail),
              state: key === 'controlled' ? state : (key === 'light' ? state : (key === 'stress' ? state : { key: key, label: labels[key], detail: key === 'grasp' ? 'DRAG ACROSS TARGET' : 'FOLLOW STRUCTURE', pattern: key === 'drift' ? [5, 3] : [2, 2] })),
              target: target, targetPoint: targetPoint, distance: distance, targetDistance: targetDistance, elapsed: elapsed, speed: speed,
              peakSpeed: peakSpeed, pressure: pressure, resistanceValue: resistanceValue, resistance: resistance, deformation: deformation,
              contactPercent: contactPercent, alignment: alignment, control: control, validStart: !!drag.startValid
            };
          }
          function setProbeDragStatus(canvas, assessment, finalMessage) {
            if (!assessment) return;
            var status = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
            var targetName = assessment.target ? assessment.target.name : 'the visible structure';
            var message = assessment.key === 'position'
              ? 'Probe target lost. Return to the visible structure before releasing.'
              : (assessment.key === 'grasp'
                ? 'Probe engaged on ' + targetName + '. Trace a short path while keeping the tip on the structure.'
                : (assessment.label + ': pressure ' + assessment.pressure + ' percent; resistance ' + assessment.resistance + '; alignment ' + assessment.alignment + ' percent; contact ' + assessment.contactPercent + ' percent; control ' + assessment.control + ' percent. ' + (assessment.key === 'controlled' ? 'Release to record the trace.' : 'Adjust the path before releasing.')));
            var stateChanged = canvas._probeDragStatusKey !== assessment.key;
            if (status && (stateChanged || finalMessage)) { status.setAttribute('data-tone', assessment.key === 'controlled' ? 'working' : 'caution'); status.textContent = finalMessage || message; }
            canvas._toolIntentState = { tool: 'probe', valid: assessment.key === 'controlled', label: assessment.label, tone: assessment.key === 'controlled' ? 'working' : 'caution', target: assessment.targetPoint || null, targetLabel: targetName, at: Date.now() };
            if (stateChanged) { procedureTactile(assessment.key === 'controlled' ? 'tick' : 'warning'); canvas._probeDragStatusKey = assessment.key; }
          }
          function beginProbeDrag(e) {
            if ((d.canvasZoom || 1) > 1.01) return false;
            if (!canBeginDirectInstrument('probe')) return false;
            if ((currentProcedure.pins || []).length < 2) return false;
            var canvas = e.currentTarget, point = canvasPointFromEvent(e), contact = specimenContactContext(point), target = contact.organ;
            if (!canClaimDirectGesture(canvas, e)) return false;
            if (!target || structureExposureState(target, currentProcedure) !== 'visible') {
              var nearest = nearestVisibleStructureTarget(point);
              target = nearest && nearest.distance <= 0.095 ? organs.find(function (organ) { return organ.id === nearest.id; }) : null;
            }
            if (!target || structureExposureState(target, currentProcedure) !== 'visible') return false;
            var targetPoint = variedOrganPoint(target);
            if (procedurePointDistance(point, targetPoint) > 0.095) return false;
            var now = Date.now();
            canvas._probeDrag = { active: true, pointerId: e.pointerId, gestureContext: captureDirectGestureContext('probe', e), target: target, targetPoint: targetPoint, start: point, current: point, startedAt: now, distance: 0, peakSpeed: 0, contactSamples: 1, sampleCount: 1, samples: [{ x: point.x, y: point.y, pressure: Number(e.pressure) || toolCalibration.probePressure / 100, at: now, pointerType: e.pointerType || 'mouse' }], inputType: e.pointerType || 'mouse', startValid: true };
            canvas._probeDragStatusKey = null; canvas._toolPointer = point; canvas._toolPressure = Number(e.pressure) || toolCalibration.probePressure / 100; canvas._toolInputType = e.pointerType || 'mouse';
            setProbeDragStatus(canvas, probeDragAssessment(canvas, point, now));
            if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId);
            e.preventDefault();
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            return true;
          }
          function appendProbeDrag(e) {
            var canvas = e.currentTarget, drag = canvas._probeDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var point = canvasPointFromEvent(e), previous = drag.current || drag.start, now = Date.now();
            var step = procedurePointDistance(point, previous);
            drag.distance += step; drag.current = point; drag.peakSpeed = Math.max(Number(drag.peakSpeed) || 0, step / (Math.max(16, now - (drag.lastAt || drag.startedAt)) / 1000)); drag.lastAt = now;
            var targetDistance = drag.targetPoint ? procedurePointDistance(point, drag.targetPoint) : 1;
            drag.sampleCount = (Number(drag.sampleCount) || 0) + 1;
            if (targetDistance <= 0.095) drag.contactSamples = (Number(drag.contactSamples) || 0) + 1;
            if (!drag.samples || drag.samples.length < 48) drag.samples.push({ x: point.x, y: point.y, pressure: Number(e.pressure) || toolCalibration.probePressure / 100, at: now, pointerType: e.pointerType || drag.inputType || 'mouse' });
            drag.assessment = probeDragAssessment(canvas, point, now); canvas._toolPointer = point; canvas._toolVector = { x: point.x - previous.x, y: point.y - previous.y }; canvas._toolPressure = Number(e.pressure) || canvas._toolPressure || toolCalibration.probePressure / 100;
            setProbeDragStatus(canvas, drag.assessment);
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            e.preventDefault(); return true;
          }
          function finishProbeDrag(e, cancelled) {
            var canvas = e.currentTarget, drag = canvas._probeDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var pointerWasCancelled = !!cancelled;
            var gestureContextChanged = !directGestureContextIsCurrent(drag.gestureContext);
            cancelled = pointerWasCancelled || gestureContextChanged;
            drag.active = false; var assessment = probeDragAssessment(canvas, drag.current, Date.now()) || drag.assessment;
            canvas._probeDrag = null; canvas._probeDragStatusKey = null; canvas._suppressToolClick = true;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId);
            syncCanvasPointerPresentation(canvas, null);
            if (cancelled) { canvas._suppressToolClick = gestureContextChanged && !pointerWasCancelled; canvas._toolPointer = null; canvas._toolVector = null; canvas._toolPressure = null; canvas._toolIntentState = null; setProcedureFeedback('Probe trace canceled. No structure was recorded.', 'working'); return true; }
            if (!assessment || assessment.key === 'position') { setProcedureFeedback('No probe trace recorded. Press a visible structure before tracing.', 'caution'); return true; }
            if (assessment.key === 'grasp') { setProcedureFeedback('Probe engaged but did not trace far enough. Drag across the structure, or use the equivalent action button.', 'working'); return true; }
            if (assessment.key === 'light') { setProcedureFeedback('Probe pressure was too light to resolve a tactile boundary. Increase calibrated pressure and try again.', 'caution'); return true; }
            if (assessment.key === 'stress') { procedureMistake('Probe release exceeded the calibrated pressure envelope. Reduce pressure before tracing; no structure was recorded.'); return true; }
            if (assessment.key === 'drift') { procedureMistake('Probe trace drifted off the selected structure. Keep the tip on the visible anatomy and trace a short controlled path; no structure was recorded.'); return true; }
            var probeDragMetrics = { distance: Math.round(assessment.distance * 1000) / 1000, durationMs: assessment.elapsed, peakSpeed: Math.round(assessment.peakSpeed * 1000) / 1000, pressure: assessment.pressure, resistance: assessment.resistanceValue, deformation: assessment.deformation, contactPercent: assessment.contactPercent, alignment: assessment.alignment, control: assessment.control, samples: drag.samples, inputType: drag.inputType };
            var probeResult = performProcedureAction('probe', { organ: drag.target, probeDragMetrics: probeDragMetrics, inputType: drag.inputType });
            if (probeResult && probeResult.ok) queueProcedureInstrumentReplay('probe', { point: probeResult.point || drag.targetPoint, vector: { x: drag.current.x - drag.start.x, y: drag.current.y - drag.start.y } });
            return true;
          }
          function dropperDragAssessment(canvas, point, at) {
            var drag = canvas && canvas._dropperDrag;
            if (!drag) return null;
            point = point || drag.current || drag.start; at = Number(at) || Date.now();
            var contact = specimenContactContext(point);
            var profile = specimenHydrationProfile();
            var tissue = normalizeTissueState(currentProcedure.tissueState);
            var dose = Math.max(1, Math.min(3, Math.round(Number(toolCalibration.salineDose) || 1)));
            var dx = point.x - drag.start.x, dy = point.y - drag.start.y;
            var distance = Math.sqrt(dx * dx + dy * dy), elapsed = Math.max(16, at - drag.startedAt);
            var flowX = Number(profile.flowX) || 0, flowY = Number(profile.flowY) || 0, flowLength = Math.sqrt(flowX * flowX + flowY * flowY) || 1;
            var movementLength = distance || 1, flowAlignment = distance < 0.002 ? 100 : Math.round(Math.max(0, Math.min(1, (dx * flowX + dy * flowY) / (movementLength * flowLength))) * 100);
            var sampleCount = Math.max(1, Number(drag.sampleCount) || 1), contactPercent = Math.round(Math.max(0, Math.min(100, (Number(drag.contactSamples) || 0) / sampleCount * 100)));
            var spreadPercent = Math.max(0, Math.min(160, Math.round(distance / 0.16 * 100)));
            var peakSpeed = Math.max(Number(drag.peakSpeed) || 0, distance / (elapsed / 1000));
            var poolingRisk = Math.max(0, Math.min(100, Math.round((dose - 1) * 34 + Math.max(0, 100 - contactPercent) * 0.48 + Math.max(0, tissue.moisture - 78) * 0.34 + (flowAlignment < 38 ? 15 : 0) + Math.max(0, peakSpeed - 0.55) * 28 + (spreadPercent > 110 ? 16 : 0))));
            var control = Math.max(0, Math.min(100, Math.round(100 - Math.max(0, peakSpeed - 0.32) * 64 - Math.max(0, 72 - contactPercent) * 0.52 - Math.max(0, 54 - flowAlignment) * 0.26 - (dose - 1) * 14 - poolingRisk * 0.14)));
            var key = !drag.startValid ? 'position'
              : (tissue.moisture >= 96 ? 'saturated'
                : (distance < 0.026 ? 'grasp'
                  : (!contact.onSpecimen || contactPercent < 56 ? 'drift'
                    : (poolingRisk >= 68 ? 'pooling'
                      : (dose > 1 ? 'broad' : (flowAlignment < 38 ? 'flow' : (control < 68 || spreadPercent > 110 ? 'spread' : 'controlled')))))));
            var labels = { position: 'MOVE TO SPECIMEN', grasp: 'GRASP · DRAG', drift: 'OFF SURFACE', saturated: 'SATURATED · NO DOSE', broad: 'BROAD SPREAD', pooling: 'POOLING RISK', flow: 'FOLLOW FLOW', spread: 'TOO WIDE', controlled: 'CONTROLLED FILM' };
            var details = { position: 'MOVE TIP TO SURFACE', grasp: 'DRAG A SHORT PATH', drift: 'KEEP CONTACT', saturated: 'WAIT FOR MOISTURE', broad: 'REDUCE DOSE TO 1', pooling: 'REDUCE DOSE / DISTANCE', flow: 'ALIGN WITH FLOW', spread: 'SHORTEN THE PASS', controlled: 'RELEASE TO APPLY' };
            var patterns = { position: [5, 4], grasp: [2, 2], drift: [2, 2], saturated: [1, 3], broad: [6, 3], pooling: [2, 2], flow: [5, 3], spread: [5, 3], controlled: [] };
            return { key: key, label: labels[key], detail: details[key], state: { key: key, label: labels[key], detail: details[key], pattern: patterns[key] || [] }, distance: distance, elapsed: elapsed, speed: distance / (elapsed / 1000), peakSpeed: peakSpeed, dose: dose, moisture: tissue.moisture, contactPercent: contactPercent, flowAlignment: flowAlignment, flowDirection: profile.pattern || 'flow', spreadPercent: spreadPercent, poolingRisk: poolingRisk, control: control, validStart: !!drag.startValid };
          }
          function setDropperDragStatus(canvas, assessment, finalMessage) {
            if (!canvas || !assessment) return;
            var status = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
            var caution = ['position', 'drift', 'saturated', 'broad', 'pooling', 'flow', 'spread'].indexOf(assessment.key) >= 0;
            var message = finalMessage || (assessment.key === 'position' ? 'Dropper is outside the specimen. Move to the highlighted surface.' : (assessment.key === 'grasp' ? 'Dropper engaged. Drag a short path with the specimen flow, then release.' : (assessment.label + ': dose ' + assessment.dose + '; contact ' + assessment.contactPercent + '%; flow ' + assessment.flowAlignment + '%; moisture ' + assessment.moisture + '%; pooling ' + assessment.poolingRisk + '/100; control ' + assessment.control + '%. ' + assessment.detail + '.')));
            var stateChanged = canvas._dropperDragStatusKey !== assessment.key;
            if (status && (stateChanged || finalMessage)) { status.setAttribute('data-tone', caution ? 'caution' : 'working'); status.textContent = message; }
            canvas._toolIntentState = { tool: 'dropper', valid: assessment.key === 'controlled', label: assessment.label, tone: caution ? 'caution' : 'working', target: null, targetLabel: 'Specimen surface', at: Date.now() };
            if (stateChanged) { procedureTactile(caution ? 'warning' : 'tick'); canvas._dropperDragStatusKey = assessment.key; }
          }
          function beginDropperDrag(e) {
            if ((d.canvasZoom || 1) > 1.01) return false;
            if (!canBeginDirectInstrument('dropper')) return false;
            var canvas = e.currentTarget, point = canvasPointFromEvent(e), contact = specimenContactContext(point);
            if (!canClaimDirectGesture(canvas, e)) return false;
            if (!contact.onSpecimen) return false;
            var now = Date.now(), tissue = normalizeTissueState(currentProcedure.tissueState);
            canvas._dropperDrag = { active: true, pointerId: e.pointerId, gestureContext: captureDirectGestureContext('dropper', e), start: point, current: point, startedAt: now, lastAt: now, lastPoint: point, peakSpeed: 0, contactSamples: contact.onSpecimen ? 1 : 0, sampleCount: 1, samples: [{ x: point.x, y: point.y, pressure: Number(e.pressure) || 0.5, onSpecimen: !!contact.onSpecimen, at: now, pointerType: e.pointerType || 'mouse' }], inputType: e.pointerType || 'mouse', startValid: true };
            canvas._dropperDragStatusKey = null; canvas._toolPointer = point; canvas._toolVector = { x: 0, y: 0 }; canvas._toolPressure = Number(e.pressure) || 0.5; canvas._toolInputType = e.pointerType || 'mouse'; canvas._suppressToolClick = true;
            setDropperDragStatus(canvas, dropperDragAssessment(canvas, point, now));
            if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId);
            canvas.style.cursor = 'grabbing'; e.preventDefault();
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            return true;
          }
          function appendDropperDrag(e) {
            var canvas = e.currentTarget, drag = canvas._dropperDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var point = canvasPointFromEvent(e), previous = drag.current || drag.start, now = Date.now(), step = procedurePointDistance(point, previous), sampleMs = Math.max(8, now - (drag.lastAt || drag.startedAt));
            var contact = specimenContactContext(point); drag.current = point; drag.lastPoint = point; drag.lastAt = now; drag.peakSpeed = Math.max(Number(drag.peakSpeed) || 0, step / (sampleMs / 1000)); drag.sampleCount = (Number(drag.sampleCount) || 0) + 1; if (contact.onSpecimen) drag.contactSamples = (Number(drag.contactSamples) || 0) + 1;
            if (!drag.samples || drag.samples.length < 64) drag.samples.push({ x: point.x, y: point.y, pressure: Number(e.pressure) || canvas._toolPressure || 0.5, onSpecimen: !!contact.onSpecimen, at: now, pointerType: e.pointerType || drag.inputType || 'mouse' });
            canvas._toolPointer = point; canvas._toolVector = { x: point.x - previous.x, y: point.y - previous.y }; canvas._toolPressure = Number(e.pressure) || canvas._toolPressure || 0.5;
            var assessment = dropperDragAssessment(canvas, point, now); drag.assessment = assessment; drag.peakSpeed = assessment.peakSpeed; setDropperDragStatus(canvas, assessment); canvas.style.cursor = assessment.key === 'controlled' ? 'grabbing' : 'not-allowed';
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow(); e.preventDefault(); return true;
          }
          function finishDropperDrag(e, cancelled) {
            var canvas = e.currentTarget, drag = canvas._dropperDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var pointerWasCancelled = !!cancelled;
            var gestureContextChanged = !directGestureContextIsCurrent(drag.gestureContext);
            cancelled = pointerWasCancelled || gestureContextChanged;
            drag.active = false; var assessment = dropperDragAssessment(canvas, drag.current, Date.now()) || drag.assessment; var inputType = drag.inputType || (e.pointerType || 'mouse'); canvas._dropperDrag = null; canvas._dropperDragStatusKey = null; canvas._suppressToolClick = true;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId); syncCanvasPointerPresentation(canvas, null);
            if (cancelled) { canvas._suppressToolClick = gestureContextChanged && !pointerWasCancelled; canvas._toolPointer = null; canvas._toolVector = null; canvas._toolPressure = null; canvas._toolIntentState = null; setProcedureFeedback('Dropper pass canceled. No saline was applied.', 'working'); return true; }
            if (!assessment || assessment.key === 'position') { setProcedureFeedback('No hydration pass recorded. Press on the specimen surface before dragging.', 'caution'); return true; }
            if (assessment.key === 'grasp') {
              setProcedureFeedback('Dropper contact was too short to demonstrate control. Drag a short flow-aligned path before release; no saline was applied.', 'working');
              return true;
            }
            if (assessment.key === 'saturated') { setProcedureFeedback('The surface is already saturated. Do not add another saline dose.', 'caution'); return true; }
            if (assessment.key === 'broad' || assessment.key === 'flow' || assessment.key === 'spread') { setProcedureFeedback('Hydration pass not recorded: ' + assessment.label + '. Set saline dose to 1 and trace a shorter path aligned with the specimen flow.', 'working'); return true; }
            if (assessment.key === 'pooling') { procedureMistake('The dropper pass created pooling risk. Reduce the dose, shorten the path, and keep the tip on the specimen; no saline was applied.'); return true; }
            if (assessment.key === 'drift') { procedureMistake('The dropper left the specimen surface. Keep contact through a short flow-aligned pass; no saline was applied.'); return true; }
            var dropperDragMetrics = { distance: Math.round(assessment.distance * 1000) / 1000, durationMs: assessment.elapsed, peakSpeed: Math.round(assessment.peakSpeed * 1000) / 1000, dose: assessment.dose, moisture: assessment.moisture, contactPercent: assessment.contactPercent, flowAlignment: assessment.flowAlignment, flowDirection: assessment.flowDirection, spreadPercent: assessment.spreadPercent, poolingRisk: assessment.poolingRisk, control: assessment.control, samples: drag.samples, inputType: inputType };
            var dropperResult = performProcedureAction('dropper', { point: drag.start, dropperDragMetrics: dropperDragMetrics, inputType: inputType });
            if (dropperResult && dropperResult.ok) queueProcedureInstrumentReplay('dropper', { point: dropperResult.point || drag.start, vector: { x: drag.current.x - drag.start.x, y: drag.current.y - drag.start.y } });
            return true;
          }
          function wickDragAssessment(canvas, point, at) {
            var drag = canvas && canvas._wickDrag;
            if (!drag) return null;
            point = point || drag.current || drag.start; at = Number(at) || Date.now();
            var contact = specimenContactContext(point);
            var poolPoint = drag.poolPoint || currentProcedure.dropperPoint || point;
            var tissue = normalizeTissueState(currentProcedure.tissueState);
            var poolDistance = procedurePointDistance(point, poolPoint);
            var distance = procedurePointDistance(point, drag.start), elapsed = Math.max(16, at - drag.startedAt);
            var sampleCount = Math.max(1, Number(drag.sampleCount) || 1), contactPercent = Math.round(Math.max(0, Math.min(100, (Number(drag.contactSamples) || 0) / sampleCount * 100)));
            var edgeTarget = 0.085 + Math.min(0.035, Math.max(0, (tissue.salineDrops || 0) - 3) * 0.01);
            var edgeAlignment = Math.max(0, Math.min(100, Math.round(100 - Math.min(100, Math.abs(poolDistance - edgeTarget) / 0.09 * 100))));
            var recoveryPercent = Math.max(0, Math.min(130, Math.round(distance / 0.12 * 100)));
            var peakSpeed = Math.max(Number(drag.peakSpeed) || 0, distance / (elapsed / 1000));
            var control = Math.max(0, Math.min(100, Math.round(100 - Math.max(0, peakSpeed - 0.30) * 66 - Math.max(0, 72 - contactPercent) * 0.50 - Math.max(0, 72 - edgeAlignment) * 0.35 - Math.max(0, recoveryPercent - 100) * 0.42)));
            var key = !drag.startValid ? 'position'
              : (distance < 0.022 ? 'grasp'
                : (!contact.onSpecimen || contactPercent < 58 || poolDistance > 0.16 ? 'drift'
                  : (poolDistance < 0.035 ? 'center'
                    : (recoveryPercent < 28 ? 'light'
                      : (recoveryPercent > 112 ? 'overreach' : (control < 68 ? 'rough' : 'controlled'))))));
            var labels = { position: 'MOVE TO POOL EDGE', grasp: 'GRASP · DRAG', drift: 'KEEP ON SPECIMEN', center: 'POOL CENTER', light: 'DRAG TO RECOVER', overreach: 'SHORTEN RECOVERY', rough: 'EASE PRESSURE', controlled: 'CONTROLLED RECOVERY' };
            var details = { position: 'FOLLOW THE ARROW', grasp: 'DRAG TOWARD EDGE', drift: 'RETURN TO POOL EDGE', center: 'MOVE OUTWARD', light: 'COVER THE POOL EDGE', overreach: 'PROTECT EXPOSED ANATOMY', rough: 'SLOW THE PASS', controlled: 'RELEASE TO WICK' };
            var patterns = { position: [5, 4], grasp: [2, 2], drift: [2, 2], center: [1, 3], light: [5, 3], overreach: [6, 3], rough: [2, 2], controlled: [] };
            return { key: key, label: labels[key], detail: details[key], state: { key: key, label: labels[key], detail: details[key], pattern: patterns[key] || [] }, poolDistance: poolDistance, edgeAlignment: edgeAlignment, distance: distance, elapsed: elapsed, peakSpeed: peakSpeed, recoveryPercent: recoveryPercent, contactPercent: contactPercent, control: control, poolingBefore: tissue.salineDrops, validStart: !!drag.startValid };
          }
          function setWickDragStatus(canvas, assessment, finalMessage) {
            if (!canvas || !assessment) return;
            var status = queryDissectionNode('#diss-canvas-status') || queryDissectionNode('[data-diss-tool-status]');
            var caution = ['position', 'drift', 'center', 'light', 'overreach', 'rough'].indexOf(assessment.key) >= 0;
            var message = finalMessage || (assessment.key === 'position' ? 'Wick is away from the pool edge. Follow the arrow to the saline footprint.' : (assessment.key === 'grasp' ? 'Wick engaged. Drag outward across the pool edge, then release when recovery reads controlled.' : (assessment.label + ': edge alignment ' + assessment.edgeAlignment + '%; recovery ' + assessment.recoveryPercent + '%; contact ' + assessment.contactPercent + '%; control ' + assessment.control + '%. ' + assessment.detail + '.')));
            var stateChanged = canvas._wickDragStatusKey !== assessment.key;
            if (status && (stateChanged || finalMessage)) { status.setAttribute('data-tone', caution ? 'caution' : 'working'); status.textContent = message; }
            canvas._toolIntentState = { tool: 'wick', valid: assessment.key === 'controlled', label: assessment.label, tone: caution ? 'caution' : 'working', target: canvas._wickDrag && canvas._wickDrag.poolPoint ? canvas._wickDrag.poolPoint : null, targetLabel: 'Pool edge', at: Date.now() };
            if (stateChanged) { procedureTactile(caution ? 'warning' : 'tick'); canvas._wickDragStatusKey = assessment.key; }
          }
          function beginWickDrag(e) {
            if ((d.canvasZoom || 1) > 1.01) return false;
            if (!canBeginDirectInstrument('wick')) return false;
            var canvas = e.currentTarget, point = canvasPointFromEvent(e), contact = specimenContactContext(point), poolPoint = currentProcedure.dropperPoint, tissue = normalizeTissueState(currentProcedure.tissueState);
            if (!canClaimDirectGesture(canvas, e)) return false;
            if (!poolPoint || tissue.salineDrops <= 2 || !contact.onSpecimen) return false;
            var poolDistance = procedurePointDistance(point, poolPoint), now = Date.now();
            canvas._wickDrag = { active: true, pointerId: e.pointerId, gestureContext: captureDirectGestureContext('wick', e), start: point, current: point, poolPoint: poolPoint, startedAt: now, lastAt: now, lastPoint: point, peakSpeed: 0, contactSamples: contact.onSpecimen ? 1 : 0, sampleCount: 1, samples: [{ x: point.x, y: point.y, onSpecimen: !!contact.onSpecimen, at: now, pointerType: e.pointerType || 'mouse' }], inputType: e.pointerType || 'mouse', startValid: poolDistance <= 0.16 };
            canvas._wickDragStatusKey = null; canvas._toolPointer = point; canvas._toolVector = { x: 0, y: 0 }; canvas._toolPressure = 0.45; canvas._toolInputType = e.pointerType || 'mouse'; canvas._suppressToolClick = true;
            setWickDragStatus(canvas, wickDragAssessment(canvas, point, now));
            if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId);
            canvas.style.cursor = 'grabbing'; e.preventDefault();
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow();
            return true;
          }
          function appendWickDrag(e) {
            var canvas = e.currentTarget, drag = canvas._wickDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var point = canvasPointFromEvent(e), previous = drag.current || drag.start, now = Date.now(), step = procedurePointDistance(point, previous), sampleMs = Math.max(8, now - (drag.lastAt || drag.startedAt)), contact = specimenContactContext(point);
            drag.current = point; drag.lastPoint = point; drag.lastAt = now; drag.peakSpeed = Math.max(Number(drag.peakSpeed) || 0, step / (sampleMs / 1000)); drag.sampleCount = (Number(drag.sampleCount) || 0) + 1; if (contact.onSpecimen) drag.contactSamples = (Number(drag.contactSamples) || 0) + 1;
            if (!drag.samples || drag.samples.length < 64) drag.samples.push({ x: point.x, y: point.y, onSpecimen: !!contact.onSpecimen, at: now, pointerType: e.pointerType || drag.inputType || 'mouse' });
            canvas._toolPointer = point; canvas._toolVector = { x: point.x - previous.x, y: point.y - previous.y }; canvas._toolPressure = 0.45;
            var assessment = wickDragAssessment(canvas, point, now); drag.assessment = assessment; drag.peakSpeed = assessment.peakSpeed; setWickDragStatus(canvas, assessment); canvas.style.cursor = assessment.key === 'controlled' ? 'grabbing' : 'not-allowed';
            if (canvas._dissMotionReduced && canvas._drawDissectionNow) canvas._drawDissectionNow(); e.preventDefault(); return true;
          }
          function finishWickDrag(e, cancelled) {
            var canvas = e.currentTarget, drag = canvas._wickDrag;
            if (!drag || !drag.active || (drag.pointerId != null && e.pointerId != null && drag.pointerId !== e.pointerId)) return false;
            var pointerWasCancelled = !!cancelled;
            var gestureContextChanged = !directGestureContextIsCurrent(drag.gestureContext);
            cancelled = pointerWasCancelled || gestureContextChanged;
            drag.active = false; var assessment = wickDragAssessment(canvas, drag.current, Date.now()) || drag.assessment; var inputType = drag.inputType || (e.pointerType || 'mouse'); canvas._wickDrag = null; canvas._wickDragStatusKey = null; canvas._suppressToolClick = true;
            if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId); syncCanvasPointerPresentation(canvas, null);
            if (cancelled) { canvas._suppressToolClick = gestureContextChanged && !pointerWasCancelled; canvas._toolPointer = null; canvas._toolVector = null; canvas._toolPressure = null; canvas._toolIntentState = null; setProcedureFeedback('Wick pass canceled. No saline was removed.', 'working'); return true; }
            if (!assessment || assessment.key === 'position') { setProcedureFeedback('No recovery pass recorded. Press on the pool edge before dragging.', 'caution'); return true; }
            if (assessment.key === 'grasp') {
              setProcedureFeedback('Wick contact was too short to demonstrate recovery control. Drag outward across the pool edge before release; no saline was removed.', 'working');
              return true;
            }
            if (assessment.key === 'center' || assessment.key === 'light') { setProcedureFeedback('Wick engaged but did not recover enough film. Start at the pool edge and drag outward across a short path.', 'working'); return true; }
            if (assessment.key === 'drift' || assessment.key === 'overreach' || assessment.key === 'rough') { procedureMistake('The wick pass left the safe pool-edge envelope. Keep contact on the specimen, slow the drag, and release before exposed anatomy is disturbed; no saline was removed.'); return true; }
            var wickDragMetrics = { distance: Math.round(assessment.distance * 1000) / 1000, durationMs: assessment.elapsed, peakSpeed: Math.round(assessment.peakSpeed * 1000) / 1000, recoveryPercent: assessment.recoveryPercent, edgeAlignment: assessment.edgeAlignment, poolDistance: Math.round(assessment.poolDistance * 1000) / 1000, contactPercent: assessment.contactPercent, control: assessment.control, poolingBefore: assessment.poolingBefore, samples: drag.samples, inputType: inputType };
            var wickResult = performProcedureAction('wick', { point: drag.current, wickDragMetrics: wickDragMetrics, inputType: inputType });
            if (wickResult && wickResult.ok) queueProcedureInstrumentReplay('wick', { point: wickResult.point || drag.current, vector: { x: drag.current.x - drag.start.x, y: drag.current.y - drag.start.y } });
            return true;
          }
          function activeCanvasGesturePointerId(canvas) {
            if (!canvas) return null;
            if (canvas._toolGestureContext && canvas._toolGestureContext.pointerId != null) return canvas._toolGestureContext.pointerId;
            if (canvas._panPointerId != null) return canvas._panPointerId;
            var activeDrag = [canvas._forcepsDrag, canvas._pinDrag, canvas._probeDrag, canvas._dropperDrag, canvas._wickDrag].find(function (drag) { return drag && drag.active; });
            return activeDrag && activeDrag.pointerId != null ? activeDrag.pointerId : null;
          }
          function cancelActiveCanvasGesture(reason, canvas, sourceEvent) {
            if (!canvas) canvas = queryDissectionNode('[data-diss-canvas]');
            if (!canvas || !canvasHasActiveDirectGesture(canvas)) return false;
            var pointerId = sourceEvent && sourceEvent.pointerId != null ? sourceEvent.pointerId : activeCanvasGesturePointerId(canvas);
            var silentCancel = reason == null;
            var cancelEvent = { currentTarget: canvas, target: canvas, pointerId: pointerId, pointerType: sourceEvent && sourceEvent.pointerType };
            var canceled = silentCancel ? canvasHasActiveDirectGesture(canvas) : (finishPinDrag(cancelEvent, true)
              || finishForcepsDrag(cancelEvent, true)
              || finishProbeDrag(cancelEvent, true)
              || finishDropperDrag(cancelEvent, true)
              || finishWickDrag(cancelEvent, true)
              || cancelProcedureStroke(cancelEvent));
            if (!canceled && canvas._isPanning) canceled = true;
            canvas._isPanning = false;
            canvas._wasPanning = false;
            canvas._panPointerId = null;
            canvas._panStartX = null;
            canvas._panStartY = null;
            canvas._panOrigX = null;
            canvas._panOrigY = null;
            canvas._forcepsDrag = null;
            canvas._pinDrag = null;
            canvas._probeDrag = null;
            canvas._dropperDrag = null;
            canvas._wickDrag = null;
            canvas._forcepsDragStatusKey = null;
            canvas._pinDragStatusKey = null;
            canvas._probeDragStatusKey = null;
            canvas._dropperDragStatusKey = null;
            canvas._wickDragStatusKey = null;
            canvas._toolDrawing = false;
            canvas._toolStroke = null;
            canvas._toolSamples = null;
            canvas._toolInputType = null;
            canvas._toolGestureContext = null;
            canvas._toolResistance = null;
            canvas._lastResistanceLevel = null;
            canvas._cuttingSafetyState = null;
            canvas._toolPointer = null;
            canvas._toolIntentState = null;
            canvas._toolVector = null;
            canvas._toolPressure = null;
            canvas._suppressToolClick = false;
            canvas._parallaxTargetX = 0;
            canvas._parallaxTargetY = 0;
            if (canvas.releasePointerCapture && pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(pointerId)) canvas.releasePointerCapture(pointerId);
            syncCanvasPointerPresentation(canvas, null);
            if (canvas._drawDissectionNow) canvas._drawDissectionNow();
            if (canceled && reason) setProcedureFeedback(reason, 'working');
            return canceled;
          }
          function cancelCanvasPointerInteraction(e) {
            if (!e || !canvasHasActiveDirectGesture(e.currentTarget)) return;
            var ownerPointerId = activeCanvasGesturePointerId(e.currentTarget);
            if (ownerPointerId != null && e.pointerId != null && ownerPointerId !== e.pointerId) return;
            cancelActiveCanvasGesture('Specimen contact canceled; no action was recorded.', e.currentTarget, e);
            upd('hoveredOrgan', null);
          }
          // Quiz: deterministic shuffling keeps questions stable during a render while
          // still varying their order for each new study session.
          var quizPool = organs.filter(function (o) { return o.fn; });
          var specimenQuizPool = allSpecimenStructures().filter(function (o) { return o.fn; });
          var encounteredQuizPool = specimenQuizPool.filter(function (organ) {
            return quizPool.some(function (currentOrgan) { return currentOrgan.id === organ.id; }) || !!(d.exploredOrgans || {})[specimen + '|' + organ.id];
          });
          // Hotspot questions must be answerable in the learner's current field.
          // The question itself stays fixed when the learner changes answer method.
          var visibleQuizPool = quizPool.filter(function (o) {
            return structureExposureState(o, currentProcedure) === 'visible';
          });
          function dissHash(value) {
            var hash = 2166136261;
            var input = String(value || '');
            for (var hi = 0; hi < input.length; hi++) { hash ^= input.charCodeAt(hi); hash = Math.imul(hash, 16777619); }
            return hash >>> 0;
          }
          function dissStableOrder(items, salt) {
            return items.slice().sort(function (a, b) {
              return dissHash(salt + '|' + a.id) - dissHash(salt + '|' + b.id);
            });
          }
          var quizSalt = specimen + '|assessment|' + (d.quizSeed || 'default');
          var practicalTargetIds = Array.isArray(d.practicalTargetIds) && d.practicalTargetIds.length
            ? d.practicalTargetIds
            : visibleQuizPool.map(function (organ) { return organ.id; });
          var specimenQuizIds = Object.create(null);
          specimenQuizPool.forEach(function (organ) { specimenQuizIds[organ.id] = true; });
          var rawQuizReviewIds = !d.practicalMode && Array.isArray(d.quizReviewQueue) ? d.quizReviewQueue : [];
          var quizReviewIds = rawQuizReviewIds.filter(function (id, index, ids) {
            return typeof id === 'string' && Object.prototype.hasOwnProperty.call(specimenQuizIds, id) && ids.indexOf(id) === index;
          });
          var activeQuizPool = d.practicalMode
            ? quizPool.filter(function (organ) { return practicalTargetIds.indexOf(organ.id) >= 0; })
            : (d.quizReviewMode && quizReviewIds.length
              ? specimenQuizPool.filter(function (organ) { return quizReviewIds.indexOf(organ.id) >= 0; })
              : encounteredQuizPool);
          var orderedQuizPool = dissStableOrder(activeQuizPool, quizSalt);
          var quizIndex = Number(d.quizIdx) || 0;
          var quizSessionLimit = Math.min(5, orderedQuizPool.length);
          var quizQ = d.quizMode && orderedQuizPool.length > 0
            ? (d.practicalMode ? orderedQuizPool[quizIndex % orderedQuizPool.length] : (!d.quizComplete && quizIndex < quizSessionLimit ? orderedQuizPool[quizIndex] : null))
            : null;
          var hotspotQuizAvailable = !!quizQ && visibleQuizPool.some(function (organ) { return organ.id === quizQ.id; });
          var effectiveQuizAnswerMode = d.quizAnswerMode === 'hotspot' && hotspotQuizAvailable ? 'hotspot' : 'choices';
          var quizOptions = quizQ
            ? dissStableOrder(specimenQuizPool.filter(function (o) { return o.id !== quizQ.id; }), quizSalt + '|' + (d.quizIdx || 0)).slice(0, 3).concat([quizQ])
            : [];
          function displayedSpecimenPoint(org) {
            var point = variedOrganPoint(org);
            var factors = specimenScaleFactors();
            return {
              x: (point.x - 0.5) * factors.x + 0.5,
              y: (point.y - 0.45) * factors.y + 0.45
            };
          }
          quizOptions = dissStableOrder(quizOptions, quizSalt + '|options|' + (d.quizIdx || 0));
          function dissDiagramRegion(org) {
            var displayed = displayedSpecimenPoint(org);
            var vertical = displayed.y < 0.34 ? 'upper' : (displayed.y > 0.66 ? 'lower' : 'middle');
            var horizontal = displayed.x < 0.4 ? 'left' : (displayed.x > 0.6 ? 'right' : 'central');
            return vertical + (horizontal === 'central' ? '-central' : '-' + horizontal);
          }
          var requestedQuizKind = ((d.quizIdx || 0) % 2 === 0) ? 'function' : 'location';
          var locationCandidateMap = {};
          quizOptions.concat(hotspotQuizAvailable ? visibleQuizPool : []).forEach(function (organ) { locationCandidateMap[organ.id] = organ; });
          var targetDiagramRegion = quizQ ? dissDiagramRegion(quizQ) : '';
          var sameRegionCandidates = Object.keys(locationCandidateMap).map(function (id) { return locationCandidateMap[id]; }).filter(function (organ) {
            return quizQ && dissDiagramRegion(organ) === targetDiagramRegion;
          });
          var quizKind = requestedQuizKind === 'location' && hotspotQuizAvailable && sameRegionCandidates.length === 1 ? 'location' : 'function';
          var quizPrompt = quizQ ? (quizKind === 'location'
            ? 'Diagram location clue: which structure is in the ' + targetDiagramRegion + ' region of this view?'
            : 'Function clue: "' + quizQ.fn.split('.')[0] + '."') : '';

          function submitQuizAnswer(answerId) {
            if (!quizQ || d.quizFeedback) return;
            if (d.practicalMode && (!Number(d.practicalEndsAt) || Date.now() >= Number(d.practicalEndsAt))) {
              setProcedureFeedback('The timed practical has ended; this response was not recorded.', 'caution');
              return;
            }
            var correct = answerId === quizQ.id;
            var retryState = d.quizRetry && d.quizRetry.questionId === quizQ.id ? d.quizRetry : null;
            var firstAttempt = !retryState;
            var scoredFirstAttempt = !d.practicalMode && !d.quizReviewMode && firstAttempt;
            var firstAttemptTotal = Number(d.quizFirstAttemptTotal) || 0;
            var firstAttemptScore = Number(d.quizFirstAttemptScore) || 0;
            var supportedCount = Number(d.quizSupportedCount) || 0;
            var reviewQueue = Array.isArray(d.quizReviewQueue) ? d.quizReviewQueue.slice() : [];
            var assessmentEvidence = Object.assign({}, d.assessmentEvidence || {});
            if (scoredFirstAttempt) {
              firstAttemptTotal += 1;
              if (correct) {
                firstAttemptScore += 1;
                assessmentEvidence[specimen + '|' + quizQ.id] = { correct: true, at: Date.now() };
                reviewQueue = reviewQueue.filter(function (structureId) { return structureId !== quizQ.id; });
              } else if (reviewQueue.indexOf(quizQ.id) < 0) reviewQueue.push(quizQ.id);
            }
            if (!correct && !d.practicalMode) {
              var nextAttempt = (Number(retryState && retryState.attempts) || 0) + 1;
              var retryMessage = quizKind === 'location'
                ? 'Not yet. Re-read the prompt and compare candidates in the ' + targetDiagramRegion + ' region. Choose again, or reveal the answer for supported practice.'
                : 'Not yet. Compare each candidate with the function clue. Choose again, or reveal the answer for supported practice.';
              updMany({
                quizRetry: {
                  questionId: quizQ.id,
                  chosen: answerId,
                  attempts: nextAttempt,
                  message: retryMessage
                },
                quizFirstAttemptScore: firstAttemptScore,
                quizFirstAttemptTotal: firstAttemptTotal,
                quizReviewQueue: reviewQueue,
                assessmentEvidence: assessmentEvidence,
                hoveredOrgan: null
              });
              if (addToast) addToast('Try again: use the location and function clues.', 'error');
              return;
            }
            var supported = !d.practicalMode && (!!retryState || !!d.quizReviewMode);
            if (supported && !d.quizReviewMode) supportedCount += 1;
            var nextScore = d.practicalMode || d.quizReviewMode ? (Number(d.quizScore) || 0) + (correct ? 1 : 0) : firstAttemptScore;
            var nextTotal = (d.quizTotal || 0) + 1;
            updMany({
              quizFeedback: { correct: correct, chosen: answerId, supported: supported },
              quizScore: nextScore,
              quizTotal: nextTotal,
              quizFirstAttemptScore: firstAttemptScore,
              quizFirstAttemptTotal: firstAttemptTotal,
              quizSupportedCount: supportedCount,
              quizExplanation: d.practicalMode ? null : quizQ.fn.split('.').slice(0, 2).join('.') + '.',
              quizRetry: null,
              hoveredOrgan: null,
              quizReviewQueue: reviewQueue,
              assessmentEvidence: assessmentEvidence
            });
            if (d.practicalMode) {
              try { window.__alloDissectionPracticalScore = nextScore; } catch (e) {}
            }
            if (correct && scoredFirstAttempt) awardStemXP('dissection', 2, 'Correct first-attempt quiz answer');
            if (addToast) addToast(d.practicalMode ? 'Response recorded for end-of-practical review.' : (supported ? 'Correct with support.' : '\u2705 Correct!'), d.practicalMode || supported ? 'working' : 'success');
          }
          function revealQuizAnswer() {
            if (!quizQ || d.practicalMode || d.quizFeedback || !d.quizRetry) return false;
            var reviewQueue = Array.isArray(d.quizReviewQueue) ? d.quizReviewQueue.slice() : [];
            if (reviewQueue.indexOf(quizQ.id) < 0) reviewQueue.push(quizQ.id);
            updMany({
              quizFeedback: { correct: false, chosen: d.quizRetry.chosen, supported: true },
              quizTotal: (Number(d.quizTotal) || 0) + 1,
              quizSupportedCount: (Number(d.quizSupportedCount) || 0) + (d.quizReviewMode ? 0 : 1),
              quizExplanation: quizQ.fn.split('.').slice(0, 2).join('.') + '.',
              quizRetry: null,
              quizReviewQueue: reviewQueue,
              hoveredOrgan: null
            });
            if (addToast) addToast('Answer shown as supported practice; review this concept again.', 'working');
            return true;
          }
          function peelCurrentLayer() {
            var fromLayerDef = spec.layers[currentLayerIdx] || spec.layers[0];
            var toLayerDef = spec.layers[currentLayerIdx + 1] || null;
            var peelingLayerId = activeLayer;
            var prefersReducedLayerMotion = reducedMotionEnabled;
            try { prefersReducedLayerMotion = reducedMotionEnabled || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) { prefersReducedLayerMotion = reducedMotionEnabled; }
            var incisionDelay = prefersReducedLayerMotion ? 0 : 500;
            var peelCanvas = canvasRefNode;
            cancelDissectionLayerTimers(peelCanvas);
            sfxDisCut();
            if (window._alloHaptic) window._alloHaptic('break');
            upd('_incisionAnim', { active: true, startTick: Date.now(), layerName: peelingLayerId });

            scheduleDissectionCanvasTimer(peelCanvas, 'layerPeel', function () {
              var newRevealed = Object.assign({}, revealedLayers);
              newRevealed[peelingLayerId] = true;
              var transitionDuration = prefersReducedLayerMotion ? 650 : 1150;
              var layerTransition = toLayerDef ? {
                active: true, startedAt: Date.now(), duration: transitionDuration, reducedMotion: prefersReducedLayerMotion,
                specimenShape: spec.bodyShape, fromLayerId: fromLayerDef.id, toLayerId: toLayerDef.id,
                fromName: fromLayerDef.name, toName: toLayerDef.name, fromIcon: fromLayerDef.icon || '', toIcon: toLayerDef.icon || '',
                fromColor: fromLayerDef.color || '#8f5960', toColor: toLayerDef.color || '#fbbf24'
              } : null;
              var transitionPatch = {
                revealedLayers: newRevealed, selectedOrgan: null, _incisionAnim: null, _layerTransition: layerTransition,
                procedureFeedback: { message: toLayerDef ? fromLayerDef.name + ' retracted. ' + toLayerDef.name + ' is now visible.' : fromLayerDef.name + ' completed.', tone: 'success', at: Date.now() }
              };
              if (toLayerDef) transitionPatch.activeLayer = toLayerDef.id;
              updMany(transitionPatch);
              if (toLayerDef && typeof canvasNarrate === 'function') canvasNarrate('dissection', 'layerPeel', 'Peeled ' + fromLayerDef.name + ' layer. Now viewing ' + toLayerDef.name + ' layer with ' + ((spec.organs[toLayerDef.id] || []).length) + ' structures.', { debounce: 1000 });
              if (toLayerDef) scheduleDissectionCanvasTimer(peelCanvas, 'layerTransition', function () {
                clearOwnedTimedDissectionState('_layerTransition', layerTransition.startedAt, specimen);
              }, transitionDuration + 120);
              awardStemXP('dissection', 3, 'Peeled ' + peelingLayerId + ' layer');
              sfxDisReveal();
              if (addToast) addToast('\uD83D\uDD2C +3 XP Layer revealed!', 'success');
            }, incisionDelay);
          }

          // Canvas renderer

          var canvasRefNode = null;
          function currentDissectionRootNode() {
            var canvasRoot = canvasRefNode && canvasRefNode.closest ? canvasRefNode.closest('[data-dissection-root]') : null;
            if (canvasRoot && canvasRoot.isConnected) return canvasRoot;
            try {
              if (typeof document === 'undefined') return null;
              var ownedRoots = document.querySelectorAll('[data-dissection-root][data-dissection-save-owner]');
              for (var ownedRootIndex = 0; ownedRootIndex < ownedRoots.length; ownedRootIndex++) {
                if (ownedRoots[ownedRootIndex].getAttribute('data-dissection-save-owner') === dissectionSaveOwner) return ownedRoots[ownedRootIndex];
              }
            } catch (e) {}
            return null;
          }
          function scopedDissectionQuery(root, selector) {
            if (!root || !root.querySelector) return null;
            var scopedSelector = /^#[A-Za-z0-9_-]+$/.test(String(selector || ''))
              ? '[id="' + String(selector).slice(1) + '"]'
              : selector;
            return root.querySelector(scopedSelector);
          }
          function queryDissectionNode(selector) {
            var root = currentDissectionRootNode();
            return scopedDissectionQuery(root, selector);
          }
          function dissectionNodeById(id) {
            return queryDissectionNode('#' + id);
          }
          var canvasRef = function (canvas) {

            if (!canvas) {
              var detachedCanvas = canvasRefNode;
              canvasRefNode = null;
              if (detachedCanvas) {
                Promise.resolve().then(function () {
                  if (!detachedCanvas.isConnected && detachedCanvas._dissCleanup) detachedCanvas._dissCleanup();
                });
              }
              return;
            }
            canvasRefNode = canvas;

            // Always update zoom/pan on canvas element so animation loop reads latest values

            canvas._zoom = d.canvasZoom || 1;

            canvas._panX = d.canvasPanX || 0;

            canvas._panY = d.canvasPanY || 0;

            // Store all drawing state on canvas so animation loop always has fresh values

            canvas._drawSpec = spec;

            canvas._drawActiveLayer = activeLayer;

            canvas._drawCurrentLayerIdx = currentLayerIdx;

            canvas._drawOrgans = organs;

            canvas._drawRevealedLayers = revealedLayers;

            canvas._drawSel = sel;

            canvas._drawD = d;

            canvas._drawSpecimen = specimen;

            canvas._drawGuidedMode = guidedMode;

            canvas._drawGuidedStep = guidedStep;
            canvas._drawCurrentGuided = currentGuided || null;
            canvas._drawCurrentLayerDef = currentLayerDef;
            canvas._drawProgressPct = progressPct;
            canvas._drawExploredCount = exploredCount;
            canvas._drawTotalOrgans = totalOrgansInSpecimen;
            canvas._dissKeyHandler = dissectionKeyHandler;
            // React reuses this canvas between ordinary state updates. Keep the motion
            // contract live as preferences change, and force a deterministic redraw
            // when reduced motion intentionally disables the RAF loop.
            var nextCanvasMotionReduced = !!d.reducedMotion || !!storedAccessibilityPreferences.reducedMotion;
            try { nextCanvasMotionReduced = nextCanvasMotionReduced || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) {}

            // If animation loop is already running, just update state — don't restart

            if (canvas._dissCleanup) {
              var canvasMotionChanged = canvas._dissMotionReduced !== nextCanvasMotionReduced;
              canvas._dissMotionReduced = nextCanvasMotionReduced;
              syncCanvasPracticalCountdown(canvas);
              if ((nextCanvasMotionReduced || canvasMotionChanged) && canvas._drawDissectionNow) canvas._drawDissectionNow();
              return;
            }
            if (canvas._dissAnim) { cancelAnimationFrame(canvas._dissAnim); canvas._dissAnim = null; }
            var dissAlive = true;
            canvas._dissAlive = true;
            canvas._dissCleanup = cleanupDissectionCanvas;
            try { window.__alloDissectionCanvasCleanup = canvas._dissCleanup; } catch (e) {}
            syncCanvasPracticalCountdown(canvas);

            // PL7 HiDPI: crisp rendering on retina displays.
            if (window.StemLab && window.StemLab.setupHiDPI) {
              window.StemLab.setupHiDPI(canvas, canvas._logicalW || canvas.width, canvas._logicalH || canvas.height);
              // Let the responsive stylesheet control display size while the backing buffer remains HiDPI.
              canvas.style.width = '';
              canvas.style.height = '';
            }
            var ctx = canvas.getContext('2d');
            if (!ctx) return;
            if (canvas._dpr) ctx.setTransform(canvas._dpr, 0, 0, canvas._dpr, 0, 0);

            var W = canvas._logicalW || canvas.width, H = canvas._logicalH || canvas.height;

            var dissTick = 0;
            var dissLastDrawAt = 0;
            var dissFrameCostAverage = 0;
            var dissCanvasInViewport = true;
            var dissIntersectionObserver = null;
            var dissResizeObserver = null;
            var dissTimeTimer = setInterval(function () {
              if (!dissAlive || !canvas.isConnected || isDissectionHidden() || isDissectionOffscreen()) return;
              var latest = canvas._drawD || {};
              upd('timeSpent', (Number(latest.timeSpent) || 0) + 30);
            }, 30000);
            var dissScenarioTimer = setInterval(function () {
              if (!dissAlive || !canvas.isConnected || isDissectionHidden()) return;
              var latest = canvas._drawD || {};
              var latestWorkspaceMode = latest.workspaceMode === 'advanced' || latest.workspaceMode === 'essentials' ? latest.workspaceMode : workspaceMode;
              if (latestWorkspaceMode !== 'advanced' || latest.procedureScenario !== 'timed-practical' || !latest.scenarioStartedAt) return;
              var liveLayer = latest.activeLayer || activeLayer;
              var liveProcedure = ((latest.procedureByLayer || {})[liveLayer]) || {};
              if (procedureStepIndex(liveProcedure) >= Math.max(1, (procedureProtocol.order || []).length)) return;
              var remaining = Math.max(0, 240 - Math.round((Date.now() - Number(latest.scenarioStartedAt)) / 1000));
              if (remaining !== Number(latest.scenarioTimeRemaining)) {
                upd('scenarioTimeRemaining', remaining);
                if (remaining === 0) {
                  setProcedureFeedback('Timed practical expired. Restart the scenario to try again.', 'caution');
                  cancelActiveCanvasGesture('Timed practical expired; active specimen contact was canceled and no action was recorded.', canvas);
                }
              }
            }, 1000);
            var dissMotionReduced = nextCanvasMotionReduced;
            canvas._dissMotionReduced = dissMotionReduced;
            canvas._dissInViewport = true;
            canvas._drawDissectionNow = function () { if (dissAlive && canvas.isConnected && !isDissectionHidden() && !isDissectionOffscreen()) drawDissectionFrame(); };

            function isDissectionHidden() {
              return typeof document !== 'undefined' && !!document.hidden;
            }


            function isDissectionOffscreen() {
              return !dissCanvasInViewport;
            }
            function cancelDissectionFrame() {
              if (canvas._dissAnim && typeof cancelAnimationFrame === 'function') cancelAnimationFrame(canvas._dissAnim);
              canvas._dissAnim = null;
            }

            function scheduleDissectionFrame() {
              if (!dissAlive || dissMotionReduced || canvas._dissAnim || isDissectionHidden() || isDissectionOffscreen()) return;
              if (typeof requestAnimationFrame !== 'function') return;
              canvas._dissAnim = requestAnimationFrame(drawDissectionFrame);
            }
            function updateLivingFunctionTimeline(nowMs) {
              nowMs = Number(nowMs);
              if (!isFinite(nowMs)) nowMs = Date.now();
              var replayToken = Number(d.livingFunctionReplayToken) || 0;
              if (canvas._livingFunctionReplayToken !== replayToken) {
                canvas._livingFunctionReplayToken = replayToken;
                canvas._livingFunctionElapsedMs = 0;
                canvas._livingFunctionLastAt = nowMs;
              }
              if (!isFinite(Number(canvas._livingFunctionElapsedMs))) canvas._livingFunctionElapsedMs = 0;
              var previousAt = Number(canvas._livingFunctionLastAt);
              if (!isFinite(previousAt)) previousAt = nowMs;
              var rawFrameDeltaMs = Math.max(0, nowMs - previousAt);
              var frameDeltaMs = rawFrameDeltaMs > 180 ? 0 : Math.min(100, rawFrameDeltaMs);
              canvas._livingFunctionLastAt = nowMs;
              var speed = (d.livingFunctionSpeed || livingFunctionSpeed) === 'slow' ? 0.62 : ((d.livingFunctionSpeed || livingFunctionSpeed) === 'fast' ? 1.48 : 1);
              var timelineRunning = !!d.livingFunctionEnabled && !d.livingFunctionPaused && !dissMotionReduced;
              if (timelineRunning) canvas._livingFunctionElapsedMs += frameDeltaMs * speed;
              var phase = !d.livingFunctionEnabled || dissMotionReduced ? 0 : (canvas._livingFunctionElapsedMs / 2600) % 1;
              return { phase: phase, wave: (1 - Math.cos(phase * Math.PI * 2)) / 2, running: timelineRunning, elapsedMs: canvas._livingFunctionElapsedMs };
            }

            function cleanupDissectionCanvas() {
              cancelActiveCanvasGesture(null, canvas);
              clearAllDissectionCanvasTimers(canvas);
              dissAlive = false;
              canvas._dissAlive = false;
              cancelDissectionFrame();
              if (dissTimeTimer) { clearInterval(dissTimeTimer); dissTimeTimer = null; }
              if (dissScenarioTimer) { clearInterval(dissScenarioTimer); dissScenarioTimer = null; }
              if (canvas._wheelZoomTimer) { clearTimeout(canvas._wheelZoomTimer); canvas._wheelZoomTimer = null; }
              if (canvas._dissReducedContactTimer) { clearTimeout(canvas._dissReducedContactTimer); canvas._dissReducedContactTimer = null; }
              if (dissIntersectionObserver) { dissIntersectionObserver.disconnect(); dissIntersectionObserver = null; }
              if (dissResizeObserver) { dissResizeObserver.disconnect(); dissResizeObserver = null; }
              if (canvas._dissReducedOutcomeTimer) { clearTimeout(canvas._dissReducedOutcomeTimer); canvas._dissReducedOutcomeTimer = null; }
              canvas._toolContactPulse = null;
              canvas._toolOutcomePulse = null;
              stopCanvasPracticalCountdown(canvas);
              if (typeof document !== 'undefined') { document.removeEventListener('visibilitychange', onDissectionVisibilityChange); document.removeEventListener('fullscreenchange', onDissectionFullscreenChange); }
              if (typeof window !== 'undefined') window.removeEventListener('blur', onDissectionWindowBlur);
              if (window.__alloDissectionCanvasCleanup === canvas._dissCleanup) window.__alloDissectionCanvasCleanup = null;
              if (window._dissectionKeyHandler === canvas._dissKeyHandler) window._dissectionKeyHandler = null;
              canvas._dissCleanup = null;
              canvas._dissKeyHandler = null; canvas._drawDissectionNow = null; canvas._dissMotionReduced = null; canvas._dissInViewport = null;
            }

            function onDissectionVisibilityChange() {
              if (!dissAlive) return;
              if (!canvas.isConnected) { cleanupDissectionCanvas(); return; }
              if (isDissectionHidden()) {
                flushAllDissectionSaves();
                cancelActiveCanvasGesture('Specimen contact canceled when the lab moved to the background; no action was recorded.', canvas);
                canvas._livingFunctionLastAt = null;
                cancelDissectionFrame();
              }
              else { cancelDissectionFrame(); drawDissectionFrame(); }
            }
            function onDissectionWindowBlur() {
              if (!dissAlive) return;
              cancelActiveCanvasGesture('Specimen contact canceled when the window lost focus; no action was recorded.', canvas);
              canvas._livingFunctionLastAt = null;
            }

            function onDissectionIntersection(entries) {
              if (!dissAlive || !entries || !entries.length) return;
              var entry = entries.find(function (candidate) { return candidate && candidate.target === canvas; }) || entries[0];
              var nextCanvasInViewport = !!(entry && entry.isIntersecting && (entry.intersectionRatio == null || entry.intersectionRatio > 0));
              if (nextCanvasInViewport === dissCanvasInViewport) return;
              dissCanvasInViewport = nextCanvasInViewport;
              canvas._dissInViewport = dissCanvasInViewport;
              cancelDissectionFrame();
              if (!dissCanvasInViewport) {
                cancelActiveCanvasGesture('Specimen contact canceled when the canvas moved offscreen; no action was recorded.', canvas);
                canvas._livingFunctionLastAt = null;
              }
              if (dissCanvasInViewport && !isDissectionHidden()) drawDissectionFrame();
            }

            function onDissectionResize() {
              if (!dissAlive || !canvas.isConnected || isDissectionHidden() || isDissectionOffscreen()) return;
              cancelDissectionFrame();
              drawDissectionFrame();
            }

            function onDissectionFullscreenChange() {
              if (!dissAlive || typeof document === 'undefined') return;

              var fullscreenStage = activeDissectionFullscreenElement();
              if (fullscreenStage && fullscreenStage.hasAttribute && fullscreenStage.hasAttribute('data-diss-fullscreen-stage')) {
                if (fullscreenStage.contains && fullscreenStage.contains(canvas)) focusDissectionFullscreenExit(fullscreenStage);
                return;
              }
              var previousStage = null;
              try { previousStage = window.__alloDissectionFullscreenStage; } catch (e) {}
              if (previousStage && previousStage.contains && previousStage.contains(canvas)) restoreDissectionFullscreenFocus(previousStage);
            }

            canvas._dissCleanup = cleanupDissectionCanvas;
            try { window.__alloDissectionCanvasCleanup = canvas._dissCleanup; } catch (e) {}
            if (typeof document !== 'undefined') { document.addEventListener('visibilitychange', onDissectionVisibilityChange); document.addEventListener('fullscreenchange', onDissectionFullscreenChange); }

            if (typeof window !== 'undefined') window.addEventListener('blur', onDissectionWindowBlur);
            try {
              if (typeof window !== 'undefined' && typeof window.IntersectionObserver === 'function') {
                dissIntersectionObserver = new window.IntersectionObserver(onDissectionIntersection, { rootMargin: '180px 0px', threshold: 0.01 });
                dissIntersectionObserver.observe(canvas);
              }
              if (typeof window !== 'undefined' && typeof window.ResizeObserver === 'function') {
                dissResizeObserver = new window.ResizeObserver(onDissectionResize);
                dissResizeObserver.observe(canvas);
              }
            } catch (e) {}

            function drawDissectionFrame() {
              if (!dissAlive) return;
              canvas._dissAnim = null;
              // Stop the loop once React unmounts the canvas — this heavyweight
              // full-anatomy redraw otherwise reschedules itself at 60fps forever,
              // surviving tab switches and navigation away from the tool.
              if (!canvas.isConnected) { cleanupDissectionCanvas(); return; }
              if (isDissectionHidden()) { cancelDissectionFrame(); return; }

              var drawState = canvas._drawD || d || {};
              var frameMotionReduced = !!drawState.reducedMotion || !!storedAccessibilityPreferences.reducedMotion;
              if (isDissectionOffscreen()) { cancelDissectionFrame(); return; }
              try { frameMotionReduced = frameMotionReduced || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) {}
              if (frameMotionReduced !== dissMotionReduced) {
                dissMotionReduced = frameMotionReduced;
                canvas._dissMotionReduced = dissMotionReduced;
                canvas._livingFunctionLastAt = null;
                if (dissMotionReduced) cancelDissectionFrame();
              }
              var frameStartedAt = typeof performance !== 'undefined' && performance.now ? performance.now() : Date.now();
              var liveRenderQuality = drawState.renderQuality || 'auto';
              var saveDataRequested = false;
              try { saveDataRequested = !!(navigator.connection && navigator.connection.saveData); } catch (e) {}
              var constrainedDevice = typeof navigator !== 'undefined' && ((navigator.hardwareConcurrency && navigator.hardwareConcurrency <= 4) || (navigator.deviceMemory && navigator.deviceMemory <= 4));
              var autoBalanced = liveRenderQuality === 'auto' && (constrainedDevice || saveDataRequested);
              var adaptiveFrameFloor = liveRenderQuality === 'auto' ? Math.max(0, Number(canvas._adaptiveFrameFloor) || 0) : 0;
              var minFrameMs = Math.max(adaptiveFrameFloor, liveRenderQuality === 'high' ? 16 : (liveRenderQuality === 'balanced' || autoBalanced ? 50 : (drawState.animSpeed === 'fast' ? 16 : (drawState.animSpeed === 'slow' ? 66 : 33))));
              if (!dissMotionReduced && dissLastDrawAt && arguments[0] && arguments[0] - dissLastDrawAt < minFrameMs) { scheduleDissectionFrame(); return; }
              if (!dissMotionReduced) { dissLastDrawAt = arguments[0] || Date.now(); dissTick++; }

              // Guard: skip frame if canvas dimensions are not finite or zero

              // All anatomy and interaction math stays in logical CSS pixels; the context transform maps it to the HiDPI buffer.
              W = canvas._logicalW || canvas.width;
              H = canvas._logicalH || canvas.height;
              if (canvas._dpr) ctx.setTransform(canvas._dpr, 0, 0, canvas._dpr, 0, 0);

              if (!W || !H || !isFinite(W) || !isFinite(H)) {

                scheduleDissectionFrame();

                return;

              }
              var canvasDisplayRect = canvas.getBoundingClientRect ? canvas.getBoundingClientRect() : null;
              var canvasDisplayWidth = canvasDisplayRect && canvasDisplayRect.width ? canvasDisplayRect.width : W;
              var canvasDisplayHeight = canvasDisplayRect && canvasDisplayRect.height ? canvasDisplayRect.height : H;
              var canvasDisplayRatio = Math.max(W / Math.max(1, canvasDisplayWidth), H / Math.max(1, canvasDisplayHeight));
              var canvasTextPreferenceScale = (!!drawState.largeText || !!storedAccessibilityPreferences.largeText) ? 1.18 : 1;
              var canvasUiScale = Math.max(1, Math.min(2.6, canvasDisplayRatio));
              var canvasCoarsePointer = false;
              try { canvasCoarsePointer = !!(window.matchMedia && window.matchMedia('(pointer: coarse)').matches); } catch (e) {}
              if (canvasCoarsePointer) canvasUiScale = Math.max(1.2, Math.min(2.6, canvasDisplayRatio * 1.25));
              else if (canvasDisplayRatio > 1) canvasUiScale = Math.min(2.3, canvasDisplayRatio * 1.1);
              canvasUiScale = Math.min(2.8, canvasUiScale * canvasTextPreferenceScale);
              var canvasHudScale = Math.max(1, Math.min(2.15, canvasDisplayRatio * canvasTextPreferenceScale));
              canvas._canvasHudScale = canvasHudScale;
              canvas._canvasUiScale = canvasUiScale;

              // Read ALL drawing state from canvas element (updated by canvasRef on each React render)

              // This avoids stale closures that caused the specimen to vanish permanently

              spec = canvas._drawSpec || spec;

              activeLayer = canvas._drawActiveLayer || activeLayer;

              currentLayerIdx = canvas._drawCurrentLayerIdx != null ? canvas._drawCurrentLayerIdx : currentLayerIdx;

              organs = canvas._drawOrgans || organs;

              revealedLayers = canvas._drawRevealedLayers || revealedLayers;

              sel = canvas._drawSel;

              d = canvas._drawD || d;

              // Essentials keeps advanced preferences available for a later return to Advanced,
              // but the live specimen must not retain hidden scenarios, overlays, or challenge states.
              var liveWorkspaceMode = d.workspaceMode === 'advanced' || d.workspaceMode === 'essentials' ? d.workspaceMode : workspaceMode;
              if (liveWorkspaceMode !== 'advanced') {
                d = Object.assign({}, d, {
                  procedureMode: 'guided', procedureScenario: 'precision-access',
                  visualRealism: 'guided', labLight: 'neutral', lightDirection: 'overhead',
                  inspectionLens: false, lensPinned: false, macroInset: false,
                  sceneDetail: true, depthAtlas: false, tissueRelief: true,
                  relationshipMotion: false, focusMode: true, parallaxDepth: false,
                  splitComparison: false, beforeTechniqueView: false,
                  crossSectionMode: false, specimenCondition: 'standard', relationshipMode: false,
                  renderQuality: 'auto', livingFunctionEnabled: false, livingFunctionPaused: false,
                  traceNervous: false, traceCirculation: false, traceDigestion: false,
                  traceRespiration: false, traceExcretory: false, showEndocrine: false,
                  animSpeed: 'normal'
                });
              }

              specimen = canvas._drawSpecimen || specimen;

              guidedMode = canvas._drawGuidedMode;

              guidedStep = canvas._drawGuidedStep || 0;
              currentGuided = canvas._drawCurrentGuided || null;
              currentLayerDef = canvas._drawCurrentLayerDef || spec.layers[currentLayerIdx] || spec.layers[0];
              progressPct = Number(canvas._drawProgressPct) || 0;
              exploredCount = Number(canvas._drawExploredCount) || 0;
              totalOrgansInSpecimen = Number(canvas._drawTotalOrgans) || 0;
              // Rehydrate every canvas presentation input from the latest React state.
              highContrastEnabled = !!d.highContrast || !!storedAccessibilityPreferences.highContrast;
              largeTextEnabled = !!d.largeText || !!storedAccessibilityPreferences.largeText;
              reducedMotionEnabled = !!d.reducedMotion || !!storedAccessibilityPreferences.reducedMotion;
              visualRealism = d.visualRealism || 'guided';
              labLight = d.labLight || 'neutral';
              lightIntensity = Math.max(20, Math.min(100, Number(d.lightIntensity) || 68));
              inspectionLens = !!d.inspectionLens;
              lensPinned = !!d.lensPinned;
              lensMagnification = [2, 4, 6].indexOf(Number(d.lensMagnification)) >= 0 ? Number(d.lensMagnification) : 2;
              lensFocusDepth = ['surface', 'structure', 'deep'].indexOf(d.lensFocusDepth) >= 0 ? d.lensFocusDepth : 'structure';
              opticalPlaneMapActive = inspectionLens || lensPinned;
              instrumentVisuals = d.instrumentVisuals !== false;
              macroInset = d.macroInset !== false;
              sceneDetail = d.sceneDetail !== false;
              depthAtlasEnabled = d.depthAtlas !== false;
              tissueReliefEnabled = d.tissueRelief !== false;
              relationshipMotion = d.relationshipMotion !== false;
              focusMode = d.focusMode !== false;
              parallaxDepth = d.parallaxDepth !== false;
              lightDirection = ['overhead', 'left', 'right', 'raking'].indexOf(d.lightDirection) >= 0 ? d.lightDirection : 'overhead';
              anatomicalView = ['dorsal', 'ventral', 'lateral', 'internal'].indexOf(d.anatomicalView) >= 0 ? d.anatomicalView : 'dorsal';
              crossSectionMode = !!d.crossSectionMode;
              specimenCondition = ['standard', 'preserved', 'dehydrated', 'cloudy', 'swollen'].indexOf(d.specimenCondition) >= 0 ? d.specimenCondition : 'standard';
              relationshipMode = !!d.relationshipMode;
              renderQuality = ['auto', 'high', 'balanced'].indexOf(d.renderQuality) >= 0 ? d.renderQuality : 'auto';
              activeInstrument = d.activeInstrument || 'probe';
              procedureMode = d.procedureMode === 'independent' ? 'independent' : 'guided';
              procedureScenario = d.procedureScenario || 'precision-access';
              toolCalibration = normalizeToolCalibration(d.toolCalibration);
              livingFunctionEnabled = !!d.livingFunctionEnabled;
              livingFunctionPaused = !!d.livingFunctionPaused;
              livingFunctionSpeed = ['slow', 'normal', 'fast'].indexOf(d.livingFunctionSpeed) >= 0 ? d.livingFunctionSpeed : 'normal';
              visualEvidence = Array.isArray(d.visualEvidence) ? d.visualEvidence.filter(function (entry) { return entry && entry.image; }).slice(-evidenceFrameLimit) : [];
              splitComparison = !!d.splitComparison;
              procedureByLayer = d.procedureByLayer || {};
              var liveProcedureState = ((d.procedureByLayer || {})[activeLayer]) || {};
              currentProcedure = Object.assign({
                inspected: false, incisionStarted: false, incisionExtended: false,
                retracted: false, pins: [], probed: false, errors: 0, history: [], actionLog: [], cautionLog: [],
                tissueState: defaultTissueState()
              }, liveProcedureState);
              currentProcedure.tissueState = normalizeTissueState(currentProcedure.tissueState);
              var liveTissueState = normalizeTissueState(liveProcedureState.tissueState);
              var liveTissueTransitionAction = '';
              var liveTissueTransitionProgress = 1;
              var tissueTransition = canvas._toolOutcomePulse;
              if (tissueTransition && tissueTransition.layer === activeLayer && tissueTransition.beforeTissue && tissueTransition.afterTissue) {
                var tissueTransitionDuration = Math.max(1, Number(tissueTransition.duration) || 1500);
                var tissueTransitionLinear = Math.max(0, Math.min(1, (Date.now() - tissueTransition.at) / tissueTransitionDuration));
                liveTissueTransitionAction = tissueTransition.action || '';
                liveTissueTransitionProgress = dissMotionReduced ? 1 : tissueTransitionLinear * tissueTransitionLinear * (3 - 2 * tissueTransitionLinear);
                var liveTransitionIsUndo = liveTissueTransitionAction.indexOf('undo-') === 0;
                if (liveTransitionIsUndo && !dissMotionReduced && tissueTransitionLinear < 1 && tissueTransition.beforeProcedure) {
                  liveProcedureState = procedureTransitionSnapshot(tissueTransition.beforeProcedure);
                }
                liveTissueState = interpolateTissueState(tissueTransition.beforeTissue, tissueTransition.afterTissue, liveTissueTransitionProgress);
              }
              var liveProcedureForObservation = Object.assign({}, liveProcedureState, { tissueState: liveTissueState });
              var liveObservationField = observationFieldData(liveProcedureForObservation);
              var livingTimeline = updateLivingFunctionTimeline(frameStartedAt);
              var livingPhase = livingTimeline.phase;
              var livingWave = livingTimeline.wave;

              try {

              // BULLETPROOF: Reset all canvas state at frame start to prevent leaks

              ctx.setTransform(canvas._dpr || 1, 0, 0, canvas._dpr || 1, 0, 0);

              ctx.globalAlpha = 1;

              ctx.setLineDash([]);

              ctx.lineDashOffset = 0;

              ctx.shadowBlur = 0;

              ctx.shadowColor = 'transparent';

              ctx.clearRect(0, 0, W, H);

              var cx = W * 0.5, cy = H * 0.45;

              // Read zoom + pan from canvas element (always current, avoids stale closure)

              var zoom = canvas._zoom || 1;
              var panX = canvas._panX || 0;
              var panY = canvas._panY || 0;
              var cameraTransition = canvas._cameraTransition;
              if (cameraTransition) {
                var cameraLinear = dissMotionReduced ? 1 : Math.max(0, Math.min(1, (Date.now() - cameraTransition.startedAt) / cameraTransition.duration));
                var cameraEase = cameraLinear < 0.5 ? 4 * cameraLinear * cameraLinear * cameraLinear : 1 - Math.pow(-2 * cameraLinear + 2, 3) / 2;
                zoom = cameraTransition.fromZoom + (cameraTransition.toZoom - cameraTransition.fromZoom) * cameraEase;
                panX = cameraTransition.fromPanX + (cameraTransition.toPanX - cameraTransition.fromPanX) * cameraEase;
                panY = cameraTransition.fromPanY + (cameraTransition.toPanY - cameraTransition.fromPanY) * cameraEase;
                if (cameraLinear >= 1) canvas._cameraTransition = null;
              }
              canvas._interactionZoom = zoom;
              canvas._interactionPanX = panX;
              canvas._interactionPanY = panY;

              var parallaxEnabled = d.parallaxDepth !== false && !dissMotionReduced;
              var parallaxTargetX = parallaxEnabled ? (Number(canvas._parallaxTargetX) || 0) : 0;
              var parallaxTargetY = parallaxEnabled ? (Number(canvas._parallaxTargetY) || 0) : 0;
              canvas._parallaxX = (Number(canvas._parallaxX) || 0) + (parallaxTargetX - (Number(canvas._parallaxX) || 0)) * 0.16;
              canvas._parallaxY = (Number(canvas._parallaxY) || 0) + (parallaxTargetY - (Number(canvas._parallaxY) || 0)) * 0.16;
              var parallaxX = Math.abs(canvas._parallaxX) < 0.01 ? 0 : canvas._parallaxX;
              var parallaxY = Math.abs(canvas._parallaxY) < 0.01 ? 0 : canvas._parallaxY;

              ctx.save();

              ctx.translate(W / 2 + panX, H / 2 + panY);

              ctx.scale(zoom, zoom);

              ctx.translate(-W / 2, -H / 2);

              // Dark dissection tray background

              var isHC = highContrastEnabled;

              // Route the dissection-tray gradient through the global theme palette so it
              // adapts to light/dark/contrast mode instead of forcing a fixed dark surface.
              var _p = (typeof window !== 'undefined' && window.AlloStemTheme && window.AlloStemTheme.palette) ? window.AlloStemTheme.palette() : { canvas: '#0f172a', panel: '#1e293b' };
              var trayGrad = ctx.createLinearGradient(0, 0, 0, H);
              var _topColor = (_p.panel && _p.panel !== _p.canvas) ? _p.panel : _p.canvas;
              trayGrad.addColorStop(0, _topColor); trayGrad.addColorStop(1, _p.canvas);

              ctx.fillStyle = trayGrad; ctx.fillRect(0, 0, W, H);

              // Theme-aware tray border (was hardcoded slate-700) — adapts to current theme palette
              var _borderPalette = (typeof window !== 'undefined' && window.AlloStemTheme && window.AlloStemTheme.palette) ? window.AlloStemTheme.palette() : { border: '#334155' };
              ctx.strokeStyle = _borderPalette.border || '#334155'; ctx.lineWidth = 3; ctx.strokeRect(4, 4, W - 8, H - 8);

              if (sceneDetail) {
                ctx.save();
                // Recessed stainless tray well with a sealed inner gasket and reflected rim light.
                var trayWellGradient = ctx.createRadialGradient(W * 0.48, H * 0.40, Math.min(W, H) * 0.08, W * 0.50, H * 0.48, Math.max(W, H) * 0.72);
                trayWellGradient.addColorStop(0, 'rgba(148,163,184,0.045)');
                trayWellGradient.addColorStop(0.72, 'rgba(15,23,42,0.035)');
                trayWellGradient.addColorStop(1, 'rgba(2,6,23,0.24)');
                ctx.fillStyle = trayWellGradient;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(10, 10, W - 20, H - 20, 16); ctx.fill(); }
                else ctx.fillRect(10, 10, W - 20, H - 20);
                ctx.strokeStyle = 'rgba(226,232,240,0.13)'; ctx.lineWidth = 1.2;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(11.5, 11.5, W - 23, H - 23, 15); ctx.stroke(); }
                else ctx.strokeRect(11.5, 11.5, W - 23, H - 23);
                ctx.strokeStyle = 'rgba(2,6,23,0.46)'; ctx.lineWidth = 2.4;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(17, 17, W - 34, H - 34, 11); ctx.stroke(); }
                else ctx.strokeRect(17, 17, W - 34, H - 34);                var trayTopBevel = ctx.createLinearGradient(0, 4, 0, 30);
                trayTopBevel.addColorStop(0, 'rgba(255,255,255,0.15)'); trayTopBevel.addColorStop(1, 'rgba(255,255,255,0)');
                ctx.fillStyle = trayTopBevel; ctx.fillRect(7, 7, W - 14, 28);
                var trayBottomBevel = ctx.createLinearGradient(0, H - 38, 0, H - 6);
                trayBottomBevel.addColorStop(0, 'rgba(2,6,23,0)'); trayBottomBevel.addColorStop(1, 'rgba(2,6,23,0.42)');
                ctx.fillStyle = trayBottomBevel; ctx.fillRect(7, H - 38, W - 14, 31);
                // Engraved edge ticks suggest a calibrated teaching tray without competing with the scale tool.
                ctx.save(); ctx.strokeStyle = 'rgba(203,213,225,0.20)'; ctx.fillStyle = 'rgba(203,213,225,0.32)'; ctx.lineWidth = 0.75;
                var trayTickCount = 18;
                for (var trayTickIndex = 0; trayTickIndex <= trayTickCount; trayTickIndex++) {
                  var trayTickX = 30 + (W - 60) * trayTickIndex / trayTickCount;
                  var trayTickLong = trayTickIndex % 3 === 0;
                  ctx.beginPath(); ctx.moveTo(trayTickX, 18); ctx.lineTo(trayTickX, 18 + (trayTickLong ? 6 : 3)); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(trayTickX, H - 18); ctx.lineTo(trayTickX, H - 18 - (trayTickLong ? 6 : 3)); ctx.stroke();
                  if (trayTickLong && trayTickIndex > 0 && trayTickIndex < trayTickCount) {
                    ctx.font = '6px Inter, system-ui'; ctx.fillText(String(trayTickIndex / 3), trayTickX - 2, H - 7);
                  }
                }
                for (var traySideTick = 1; traySideTick < 10; traySideTick++) {
                  var trayTickY = 26 + (H - 52) * traySideTick / 10;
                  ctx.beginPath(); ctx.moveTo(18, trayTickY); ctx.lineTo(22 + (traySideTick % 2 ? 0 : 3), trayTickY); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(W - 18, trayTickY); ctx.lineTo(W - 22 - (traySideTick % 2 ? 0 : 3), trayTickY); ctx.stroke();
                }
                ctx.restore();                ctx.strokeStyle = 'rgba(255,255,255,0.055)'; ctx.lineWidth = 18;
                ctx.beginPath(); ctx.moveTo(W * 0.12, H * 0.08); ctx.lineTo(W * 0.78, H * 0.92); ctx.stroke();
                [[16,16],[W-16,16],[16,H-16],[W-16,H-16]].forEach(function (fastener) {
                  var fastenerGrad = ctx.createRadialGradient(fastener[0] - 2, fastener[1] - 2, 1, fastener[0], fastener[1], 5);
                  fastenerGrad.addColorStop(0, 'rgba(248,250,252,0.72)'); fastenerGrad.addColorStop(0.42, 'rgba(100,116,139,0.62)'); fastenerGrad.addColorStop(1, 'rgba(2,6,23,0.82)');
                  ctx.beginPath(); ctx.arc(fastener[0], fastener[1], 4.5, 0, Math.PI * 2); ctx.fillStyle = fastenerGrad; ctx.fill();
                  ctx.strokeStyle = 'rgba(15,23,42,0.72)'; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(fastener[0]-2.5, fastener[1]); ctx.lineTo(fastener[0]+2.5, fastener[1]); ctx.stroke();
                });
                ctx.restore();
              }

              // Faint grid

              ctx.strokeStyle = 'rgba(100,116,139,0.12)'; ctx.lineWidth = 0.5;

              for (var gx = 0; gx < W; gx += 30) { ctx.beginPath(); ctx.moveTo(gx, 0); ctx.lineTo(gx, H); ctx.stroke(); }

              for (var gy = 0; gy < H; gy += 30) { ctx.beginPath(); ctx.moveTo(0, gy); ctx.lineTo(W, gy); ctx.stroke(); }

              var specimenScale = specimenScaleFactors();
              if (sceneDetail) {
                var depthProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                // A specimen-shaped absorbent pad grounds the body and catches the preservation-fluid meniscus.
                var padCenterX = depthProfile.cx * W - parallaxX * 0.16;
                var padCenterY = depthProfile.cy * H + depthProfile.ry * H * 0.18;
                var padRadiusX = depthProfile.rx * W * 1.10;
                var padRadiusY = Math.max(22, depthProfile.ry * H * 0.79);
                ctx.save();
                var padGradient = ctx.createRadialGradient(padCenterX - padRadiusX * 0.18, padCenterY - padRadiusY * 0.24, 4, padCenterX, padCenterY, padRadiusX);
                padGradient.addColorStop(0, 'rgba(226,232,240,0.12)');
                padGradient.addColorStop(0.58, 'rgba(148,163,184,0.075)');
                padGradient.addColorStop(1, 'rgba(30,41,59,0.025)');
                ctx.beginPath(); ctx.ellipse(padCenterX, padCenterY, padRadiusX, padRadiusY, -0.015, 0, Math.PI * 2);
                ctx.fillStyle = padGradient; ctx.fill(); ctx.strokeStyle = 'rgba(203,213,225,0.11)'; ctx.lineWidth = 0.8; ctx.stroke();
                ctx.save(); ctx.beginPath(); ctx.ellipse(padCenterX, padCenterY, padRadiusX * 0.97, padRadiusY * 0.94, -0.015, 0, Math.PI * 2); ctx.clip();
                ctx.globalAlpha = (d.visualRealism || visualRealism) === 'accessible' ? 0.08 : 0.16;
                ctx.strokeStyle = 'rgba(226,232,240,0.28)'; ctx.lineWidth = 0.55;
                for (var padFiberIndex = 0; padFiberIndex < 22; padFiberIndex++) {
                  var padFiberAngle = specimenVariationValue('tray-pad-angle-' + padFiberIndex) * Math.PI * 2;
                  var padFiberRadius = Math.sqrt(specimenVariationValue('tray-pad-radius-' + padFiberIndex));
                  var padFiberX = padCenterX + Math.cos(padFiberAngle) * padRadiusX * padFiberRadius;
                  var padFiberY = padCenterY + Math.sin(padFiberAngle) * padRadiusY * padFiberRadius;
                  var padFiberLength = 4 + specimenVariationValue('tray-pad-length-' + padFiberIndex) * 11;
                  ctx.beginPath(); ctx.moveTo(padFiberX - padFiberLength * 0.5, padFiberY - 1.2); ctx.quadraticCurveTo(padFiberX, padFiberY + 1.4, padFiberX + padFiberLength * 0.5, padFiberY); ctx.stroke();
                }
                ctx.restore(); ctx.restore();
                var depthShadowX = depthProfile.cx * W - parallaxX * 1.5;
                var depthShadowY = depthProfile.cy * H + depthProfile.ry * H * 0.54 - parallaxY * 0.35;
                ctx.save(); ctx.globalAlpha = (d.visualRealism || visualRealism) === 'accessible' ? 0.11 : 0.2;
                var depthShadow = ctx.createRadialGradient(depthShadowX, depthShadowY, 6, depthShadowX, depthShadowY, depthProfile.rx * W * 0.92);
                depthShadow.addColorStop(0, 'rgba(2,6,23,0.72)'); depthShadow.addColorStop(0.58, 'rgba(2,6,23,0.28)'); depthShadow.addColorStop(1, 'rgba(2,6,23,0)');
                ctx.fillStyle = depthShadow; ctx.beginPath(); ctx.ellipse(depthShadowX, depthShadowY, depthProfile.rx * W * 0.88, Math.max(12, depthProfile.ry * H * 0.33), 0, 0, Math.PI * 2); ctx.fill(); ctx.restore();

                // A cool preservation-fluid halo and small condensation beads give the tray depth without obscuring anatomy.
                ctx.save();
                var trayFluidX = depthProfile.cx * W - parallaxX * 0.32;
                var trayFluidY = depthProfile.cy * H + depthProfile.ry * H * 0.28;
                var trayFluid = ctx.createRadialGradient(trayFluidX, trayFluidY, 8, trayFluidX, trayFluidY, depthProfile.rx * W * 1.06);
                trayFluid.addColorStop(0, 'rgba(186,230,253,0.055)');
                trayFluid.addColorStop(0.72, 'rgba(56,189,248,0.022)');
                trayFluid.addColorStop(1, 'rgba(56,189,248,0)');
                ctx.fillStyle = trayFluid;
                ctx.beginPath(); ctx.ellipse(trayFluidX, trayFluidY, depthProfile.rx * W, Math.max(18, depthProfile.ry * H * 0.62), -0.02, 0, Math.PI * 2); ctx.fill();
                ctx.strokeStyle = 'rgba(186,230,253,0.075)'; ctx.lineWidth = 0.7;
                ctx.beginPath(); ctx.ellipse(trayFluidX, trayFluidY, depthProfile.rx * W * 0.94, Math.max(15, depthProfile.ry * H * 0.54), -0.02, 0, Math.PI * 2); ctx.stroke();
                [[W*0.10,H*0.18,3.2],[W*0.88,H*0.72,2.2],[W*0.82,H*0.16,1.7]].forEach(function (bead) {
                  var beadGlow = ctx.createRadialGradient(bead[0]-0.7, bead[1]-0.7, 0.2, bead[0], bead[1], bead[2]);
                  beadGlow.addColorStop(0, 'rgba(248,250,252,0.38)'); beadGlow.addColorStop(0.35, 'rgba(186,230,253,0.13)'); beadGlow.addColorStop(1, 'rgba(14,116,144,0.02)');
                  ctx.beginPath(); ctx.arc(bead[0], bead[1], bead[2], 0, Math.PI * 2); ctx.fillStyle = beadGlow; ctx.fill();
                });
                ctx.restore();
              }
              ctx.save(); ctx.translate(parallaxX, parallaxY); ctx.translate(cx, cy); ctx.transform(1, parallaxY * 0.0007, parallaxX * 0.0007, 1, 0, 0); ctx.scale(specimenScale.x, specimenScale.y); ctx.translate(-cx, -cy);

              ctx.lineJoin = 'round'; ctx.lineCap = 'round';

              // Specular highlight for 3D effect

              if (spec.bodyShape !== 'worm') {

                var specGrad = ctx.createRadialGradient(cx - W * 0.05, cy - H * 0.08, 0, cx - W * 0.05, cy - H * 0.08, W * 0.15);

                specGrad.addColorStop(0, 'rgba(255,255,255,0.06)');

                specGrad.addColorStop(1, 'rgba(255,255,255,0)');

                ctx.fillStyle = specGrad;

                ctx.beginPath(); ctx.ellipse(cx - W * 0.05, cy - H * 0.08, W * 0.12, H * 0.08, -0.3, 0, Math.PI * 2);

                ctx.fill();

              } else {

                // Worm gets elongated highlight

                var wSpecGrad = ctx.createRadialGradient(cx - W * 0.01, H * 0.30, 0, cx - W * 0.01, H * 0.30, W * 0.04);

                wSpecGrad.addColorStop(0, 'rgba(255,255,255,0.06)');

                wSpecGrad.addColorStop(1, 'rgba(255,255,255,0)');

                ctx.fillStyle = wSpecGrad;

                ctx.fillRect(cx - W * 0.03, H * 0.08, W * 0.04, H * 0.84);

              }

              // 3D depth shadow under body

              ctx.globalAlpha = 0.08;

              ctx.beginPath();

              if (spec.bodyShape === 'frog') { ctx.ellipse(cx + 3, cy + 5, W * 0.18, H * 0.30, 0, 0, Math.PI * 2); }

              else if (spec.bodyShape === 'pig') { ctx.ellipse(cx + 3, cy + 5, W * 0.30, H * 0.14, 0, 0, Math.PI * 2); }

              else if (spec.bodyShape === 'fish') { ctx.ellipse(cx + 3, cy + 5, W * 0.32, H * 0.10, 0, 0, Math.PI * 2); }

              else if (spec.bodyShape === 'crayfish') { ctx.ellipse(cx + 3, cy + 5, W * 0.32, H * 0.08, 0, 0, Math.PI * 2); }

              else if (spec.bodyShape === 'worm') { ctx.ellipse(cx + 2, H * 0.50 + 5, W * 0.05, H * 0.42, 0, 0, Math.PI * 2); }

              // Theme-aware shadow: derives the darkest tone from the current palette so the
              // shadow reads as "depth" in both light and dark themes instead of pure black.
              var _shadowPalette = (typeof window !== 'undefined' && window.AlloStemTheme && window.AlloStemTheme.palette) ? window.AlloStemTheme.palette() : { canvas: '#0f172a' };
              var _shc = _shadowPalette.canvas || '#0f172a';
              ctx.fillStyle = 'rgba(' + parseInt(_shc.slice(1, 3), 16) + ',' + parseInt(_shc.slice(3, 5), 16) + ',' + parseInt(_shc.slice(5, 7), 16) + ',0.3)';
              ctx.fill();

              ctx.globalAlpha = 1;

              // Tray-level stipple was removed: specimen microtexture is clipped and lit in the material pass after organism drawing.
              // Compact instrument bay: grounded metal tools replace the earlier faint line-art corner icons.
              if (sceneDetail) {
                ctx.save();
                var instrumentBayX = W - 142, instrumentBayY = H - 94, instrumentBayW = 122, instrumentBayH = 57;
                ctx.shadowColor = 'rgba(2,6,23,0.54)'; ctx.shadowBlur = 10; ctx.shadowOffsetY = 4;
                var instrumentBayGradient = ctx.createLinearGradient(instrumentBayX, instrumentBayY, instrumentBayX, instrumentBayY + instrumentBayH);
                instrumentBayGradient.addColorStop(0, 'rgba(148,163,184,0.18)');
                instrumentBayGradient.addColorStop(0.45, 'rgba(30,41,59,0.34)');
                instrumentBayGradient.addColorStop(1, 'rgba(2,6,23,0.40)');
                ctx.fillStyle = instrumentBayGradient;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(instrumentBayX, instrumentBayY, instrumentBayW, instrumentBayH, 8); ctx.fill(); }
                else ctx.fillRect(instrumentBayX, instrumentBayY, instrumentBayW, instrumentBayH);
                ctx.shadowBlur = 0; ctx.shadowOffsetY = 0;
                ctx.strokeStyle = 'rgba(203,213,225,0.24)'; ctx.lineWidth = 0.8;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(instrumentBayX + 0.5, instrumentBayY + 0.5, instrumentBayW - 1, instrumentBayH - 1, 8); ctx.stroke(); }
                else ctx.strokeRect(instrumentBayX + 0.5, instrumentBayY + 0.5, instrumentBayW - 1, instrumentBayH - 1);
                ctx.font = 'bold 6px Inter, system-ui'; ctx.fillStyle = 'rgba(226,232,240,0.58)'; ctx.fillText('INSTRUMENT BAY', instrumentBayX + 9, instrumentBayY + 11);

                // Scalpel with a brushed handle and a distinct beveled blade.
                ctx.save(); ctx.translate(instrumentBayX + 34, instrumentBayY + 33); ctx.rotate(-0.28);
                var bayScalpelHandle = ctx.createLinearGradient(-24, -3, 13, 3);
                bayScalpelHandle.addColorStop(0, '#475569'); bayScalpelHandle.addColorStop(0.48, '#cbd5e1'); bayScalpelHandle.addColorStop(1, '#64748b');
                ctx.fillStyle = bayScalpelHandle; ctx.fillRect(-24, -2.4, 37, 4.8);
                ctx.strokeStyle = 'rgba(248,250,252,0.45)'; ctx.lineWidth = 0.65; ctx.strokeRect(-24, -2.4, 37, 4.8);
                ctx.beginPath(); ctx.moveTo(13, -2.6); ctx.lineTo(28, -1.4); ctx.lineTo(31, 0); ctx.lineTo(13, 2.6); ctx.closePath();
                var bayBlade = ctx.createLinearGradient(13, -3, 30, 3); bayBlade.addColorStop(0, '#94a3b8'); bayBlade.addColorStop(0.5, '#f8fafc'); bayBlade.addColorStop(1, '#64748b');
                ctx.fillStyle = bayBlade; ctx.fill(); ctx.strokeStyle = '#334155'; ctx.lineWidth = 0.55; ctx.stroke();
                ctx.restore();

                // Forceps keep a visible spring gap and fine gripping tips.
                ctx.save(); ctx.translate(instrumentBayX + 84, instrumentBayY + 34); ctx.rotate(0.24);
                var bayForceps = ctx.createLinearGradient(-27, 0, 25, 0); bayForceps.addColorStop(0, '#64748b'); bayForceps.addColorStop(0.55, '#e2e8f0'); bayForceps.addColorStop(1, '#94a3b8');
                ctx.strokeStyle = bayForceps; ctx.lineWidth = 2.1; ctx.lineCap = 'round';
                ctx.beginPath(); ctx.moveTo(-27, -5); ctx.quadraticCurveTo(-5, -2.4, 25, -1); ctx.moveTo(-27, 5); ctx.quadraticCurveTo(-5, 2.4, 25, 1); ctx.stroke();
                ctx.strokeStyle = 'rgba(248,250,252,0.42)'; ctx.lineWidth = 0.55;
                ctx.beginPath(); ctx.moveTo(-19, -3.8); ctx.lineTo(18, -1.2); ctx.moveTo(-19, 3.8); ctx.lineTo(18, 1.2); ctx.stroke();
                ctx.restore();

                // Three color-headed pins sit in a magnetic rail below the instruments.
                ctx.strokeStyle = 'rgba(148,163,184,0.34)'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(instrumentBayX + 10, instrumentBayY + 49); ctx.lineTo(instrumentBayX + instrumentBayW - 10, instrumentBayY + 49); ctx.stroke();
                ['#f9a8d4','#7dd3fc','#fde68a'].forEach(function (bayPinColor, bayPinIndex) {
                  var bayPinX = instrumentBayX + 42 + bayPinIndex * 13;
                  ctx.strokeStyle = '#cbd5e1'; ctx.lineWidth = 0.8; ctx.beginPath(); ctx.moveTo(bayPinX, instrumentBayY + 43); ctx.lineTo(bayPinX + 5, instrumentBayY + 51); ctx.stroke();
                  var bayPinGradient = ctx.createRadialGradient(bayPinX - 1, instrumentBayY + 42, 0.5, bayPinX, instrumentBayY + 43, 3);
                  bayPinGradient.addColorStop(0, '#ffffff'); bayPinGradient.addColorStop(1, bayPinColor);
                  ctx.beginPath(); ctx.arc(bayPinX, instrumentBayY + 43, 2.7, 0, Math.PI * 2); ctx.fillStyle = bayPinGradient; ctx.fill();
                });
                ctx.restore();
              }
              // Get current layer styling

              var curLayer = spec.layers[currentLayerIdx] || spec.layers[0];

              var layerColor = curLayer.color || '#94a3b8';

              var layerStroke = curLayer.accent || '#94a3b8';

              // cx, cy declared at top of drawDissectionFrame



              // Create body gradient for 3D depth effect

              var bodyGrad = ctx.createRadialGradient(cx - W * 0.05, cy - H * 0.05, 10, cx, cy, W * 0.30);

              bodyGrad.addColorStop(0, layerColor);

              bodyGrad.addColorStop(0.7, layerColor);

              bodyGrad.addColorStop(1, layerStroke);

              // Breathing animation for organic feel

              var breathScale = 1 + livingWave * (activeLayer === 'organs' ? 0.012 : 0.005);



              ctx.save();

              ctx.shadowColor = 'rgba(0,0,0,0.3)'; ctx.shadowBlur = 12;



// €â€ Draw organ pins â€â€”€â”€ Draw specimen body based on bodyShape ──

              if (spec.bodyShape === 'frog') {
                // Frog: integrated head-and-torso silhouette with articulated limbs and nested anatomy.
                var frogAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var frogRealistic = (d.visualRealism || visualRealism) === 'realistic';
                var frogVentral = anatomicalView === 'ventral' || anatomicalView === 'internal';
                var frogBreath = (!livingFunctionEnabled || dissMotionReduced) ? 1 : breathScale;
                var frogDeepLayer = activeLayer !== 'skin';
                var frogOutline = frogAccessible ? '#14532d' : '#263f27';
                var frogDorsalLight = frogAccessible ? '#4ade80' : '#657a3d';
                var frogDorsalMid = frogAccessible ? '#22c55e' : '#465d31';
                var frogDorsalDark = frogAccessible ? '#15803d' : '#263f27';
                var frogVentralLight = frogAccessible ? '#d9f99d' : '#b7b58b';
                var frogVentralDark = frogAccessible ? '#65a30d' : '#74795c';

                function frogSkinGradient(x1, y1, x2, y2) {
                  var gradient = ctx.createLinearGradient(x1, y1, x2, y2);
                  gradient.addColorStop(0, frogVentral ? frogVentralLight : frogDorsalLight);
                  gradient.addColorStop(0.48, frogVentral ? '#a8aa7d' : frogDorsalMid);
                  gradient.addColorStop(1, frogVentral ? frogVentralDark : frogDorsalDark);
                  return gradient;
                }

                function traceFrogTorso() {
                  ctx.beginPath();
                  ctx.moveTo(cx, cy - H * 0.34);
                  ctx.bezierCurveTo(cx + W * 0.075, cy - H * 0.345, cx + W * 0.145, cy - H * 0.31, cx + W * 0.16, cy - H * 0.255);
                  ctx.bezierCurveTo(cx + W * 0.185, cy - H * 0.205, cx + W * 0.19, cy - H * 0.145, cx + W * 0.175, cy - H * 0.095);
                  ctx.bezierCurveTo(cx + W * 0.18 * frogBreath, cy - H * 0.015, cx + W * 0.15 * frogBreath, cy + H * 0.085, cx + W * 0.135, cy + H * 0.15);
                  ctx.bezierCurveTo(cx + W * 0.16, cy + H * 0.20, cx + W * 0.13, cy + H * 0.245, cx + W * 0.075, cy + H * 0.265);
                  ctx.bezierCurveTo(cx + W * 0.035, cy + H * 0.282, cx - W * 0.035, cy + H * 0.282, cx - W * 0.075, cy + H * 0.265);
                  ctx.bezierCurveTo(cx - W * 0.13, cy + H * 0.245, cx - W * 0.16, cy + H * 0.20, cx - W * 0.135, cy + H * 0.15);
                  ctx.bezierCurveTo(cx - W * 0.15 * frogBreath, cy + H * 0.085, cx - W * 0.18 * frogBreath, cy - H * 0.015, cx - W * 0.175, cy - H * 0.095);
                  ctx.bezierCurveTo(cx - W * 0.19, cy - H * 0.145, cx - W * 0.185, cy - H * 0.205, cx - W * 0.16, cy - H * 0.255);
                  ctx.bezierCurveTo(cx - W * 0.145, cy - H * 0.31, cx - W * 0.075, cy - H * 0.345, cx, cy - H * 0.34);
                  ctx.closePath();
                }

                function drawFrogLimbSegment(x1, y1, x2, y2, r1, r2, alpha) {
                  var dx = x2 - x1; var dy = y2 - y1;
                  var length = Math.max(1, Math.sqrt(dx * dx + dy * dy));
                  var nx = -dy / length; var ny = dx / length;
                  ctx.save(); ctx.globalAlpha = alpha;
                  ctx.beginPath();
                  ctx.moveTo(x1 + nx * r1, y1 + ny * r1);
                  ctx.bezierCurveTo(x1 + dx * 0.34 + nx * r1, y1 + dy * 0.34 + ny * r1, x1 + dx * 0.72 + nx * r2, y1 + dy * 0.72 + ny * r2, x2 + nx * r2, y2 + ny * r2);
                  ctx.quadraticCurveTo(x2 + dx / length * r2, y2 + dy / length * r2, x2 - nx * r2, y2 - ny * r2);
                  ctx.bezierCurveTo(x1 + dx * 0.72 - nx * r2, y1 + dy * 0.72 - ny * r2, x1 + dx * 0.34 - nx * r1, y1 + dy * 0.34 - ny * r1, x1 - nx * r1, y1 - ny * r1);
                  ctx.quadraticCurveTo(x1 - dx / length * r1, y1 - dy / length * r1, x1 + nx * r1, y1 + ny * r1);
                  ctx.closePath(); ctx.fillStyle = frogSkinGradient(x1 + nx * r1, y1 + ny * r1, x1 - nx * r1, y1 - ny * r1); ctx.fill();
                  ctx.strokeStyle = frogOutline; ctx.lineWidth = frogAccessible ? 1.45 : 0.85; ctx.stroke();
                  ctx.restore();
                }

                function drawFrogHindFoot(side, footX, footY, alpha) {
                  var toeSpread = [-0.062,-0.032,0,0.035,0.069];
                  var toeLengths = [0.065,0.088,0.102,0.088,0.062];
                  var toeTips = [];
                  for (var toeIndex = 0; toeIndex < 5; toeIndex++) {
                    toeTips.push({
                      x: footX + side * W * toeSpread[toeIndex],
                      y: footY + H * toeLengths[toeIndex]
                    });
                  }
                  ctx.save(); ctx.globalAlpha = alpha * (frogAccessible ? 0.28 : 0.38);
                  ctx.beginPath(); ctx.moveTo(footX, footY - H * 0.008);
                  toeTips.forEach(function (tip) { ctx.lineTo(tip.x, tip.y - H * 0.006); });
                  ctx.closePath(); ctx.fillStyle = frogVentral ? frogVentralLight : frogDorsalMid; ctx.fill(); ctx.restore();
                  ctx.save(); ctx.globalAlpha = alpha; ctx.strokeStyle = frogOutline; ctx.lineCap = 'round';
                  toeTips.forEach(function (tip, toeIndex) {
                    ctx.beginPath(); ctx.moveTo(footX, footY); ctx.quadraticCurveTo((footX + tip.x) / 2 + side * (toeIndex - 2) * 0.6, (footY + tip.y) / 2, tip.x, tip.y);
                    ctx.lineWidth = toeIndex === 2 ? 1.25 : 0.95; ctx.stroke();
                    ctx.beginPath(); ctx.ellipse(tip.x, tip.y, 1.7, 1.15, 0, 0, Math.PI * 2); ctx.fillStyle = frogVentral ? frogVentralLight : frogDorsalMid; ctx.fill();
                  });
                  ctx.restore();
                }

                function drawFrogHindLimb(side, alpha) {
                  var hipX = cx + side * W * 0.115; var hipY = cy + H * 0.13;
                  var kneeX = cx + side * W * 0.275; var kneeY = cy + H * 0.105;
                  var ankleX = cx + side * W * 0.315; var ankleY = cy + H * 0.245;
                  var footX = cx + side * W * 0.265; var footY = cy + H * 0.355;
                  drawFrogLimbSegment(hipX, hipY, kneeX, kneeY, W * 0.036, W * 0.028, alpha);
                  drawFrogLimbSegment(kneeX, kneeY, ankleX, ankleY, W * 0.026, W * 0.017, alpha);
                  drawFrogLimbSegment(ankleX, ankleY, footX, footY, W * 0.015, W * 0.008, alpha);
                  drawFrogHindFoot(side, footX, footY, alpha);
                  ctx.save(); ctx.globalAlpha = alpha * 0.22; ctx.strokeStyle = 'rgba(248,250,252,0.65)'; ctx.lineWidth = 0.55;
                  ctx.beginPath(); ctx.moveTo(hipX + side * 2, hipY - 2); ctx.quadraticCurveTo(kneeX, kneeY - 3, ankleX, ankleY - 2); ctx.stroke(); ctx.restore();
                }

                function drawFrogForelimb(side, alpha) {
                  var shoulderX = cx + side * W * 0.145; var shoulderY = cy - H * 0.12;
                  var elbowX = cx + side * W * 0.225; var elbowY = cy - H * 0.045;
                  var wristX = cx + side * W * 0.205; var wristY = cy + H * 0.065;
                  drawFrogLimbSegment(shoulderX, shoulderY, elbowX, elbowY, W * 0.021, W * 0.015, alpha);
                  drawFrogLimbSegment(elbowX, elbowY, wristX, wristY, W * 0.015, W * 0.008, alpha);
                  ctx.save(); ctx.globalAlpha = alpha; ctx.strokeStyle = frogOutline; ctx.lineCap = 'round';
                  var fingerOffsets = [-0.035,-0.014,0.013,0.032];
                  var fingerLengths = [0.041,0.052,0.049,0.036];
                  fingerOffsets.forEach(function (offset, fingerIndex) {
                    var tipX = wristX + side * W * offset;
                    var tipY = wristY + H * fingerLengths[fingerIndex];
                    ctx.beginPath(); ctx.moveTo(wristX, wristY); ctx.quadraticCurveTo((wristX + tipX) / 2, tipY - H * 0.012, tipX, tipY);
                    ctx.lineWidth = 0.85; ctx.stroke();
                    ctx.beginPath(); ctx.ellipse(tipX, tipY, 1.25, 0.9, 0, 0, Math.PI * 2); ctx.fillStyle = frogVentral ? frogVentralLight : frogDorsalMid; ctx.fill();
                  });
                  ctx.restore();
                }

                // Folded hindlimbs and smaller forelimbs establish the characteristic resting posture.
                drawFrogHindLimb(-1, frogDeepLayer ? 0.34 : 0.92);
                drawFrogHindLimb(1, frogDeepLayer ? 0.34 : 0.92);
                drawFrogForelimb(-1, frogDeepLayer ? 0.32 : 0.88);
                drawFrogForelimb(1, frogDeepLayer ? 0.32 : 0.88);

                var frogBodyGradient = frogSkinGradient(cx - W * 0.13, cy - H * 0.25, cx + W * 0.15, cy + H * 0.22);
                ctx.save(); ctx.globalAlpha = frogDeepLayer ? (frogAccessible ? 0.28 : 0.36) : 1;
                traceFrogTorso(); ctx.fillStyle = frogBodyGradient; ctx.fill(); ctx.strokeStyle = frogOutline; ctx.lineWidth = frogAccessible ? 2 : 1.15; ctx.stroke();
                ctx.restore(); ctx.shadowBlur = 0;

                // Dorsolateral folds, chromatophores, and moisture all conform to the unified torso.
                if (!frogDeepLayer || activeLayer === 'skin') {
                  ctx.save(); traceFrogTorso(); ctx.clip();
                  if (!frogVentral) {
                    ctx.globalAlpha = frogAccessible ? 0.22 : 0.34;
                    for (var frogSpot = 0; frogSpot < 44; frogSpot++) {
                      var spotAngle = frogSpot * 2.399963;
                      var spotRadius = Math.sqrt((frogSpot + 0.7) / 44);
                      var spotX = cx + Math.cos(spotAngle) * W * 0.135 * spotRadius;
                      var spotY = cy - H * 0.025 + Math.sin(spotAngle) * H * 0.235 * spotRadius;
                      var spotSize = 1.2 + (frogSpot % 5) * 0.72;
                      ctx.beginPath(); ctx.ellipse(spotX, spotY, spotSize * 1.5, spotSize, spotAngle * 0.15, 0, Math.PI * 2);
                      ctx.fillStyle = frogSpot % 4 === 0 ? 'rgba(24,48,25,0.72)' : 'rgba(47,73,31,0.54)'; ctx.fill();
                    }
                    [-1,1].forEach(function (side) {
                      ctx.beginPath(); ctx.moveTo(cx + side * W * 0.095, cy - H * 0.245);
                      ctx.bezierCurveTo(cx + side * W * 0.115, cy - H * 0.09, cx + side * W * 0.10, cy + H * 0.08, cx + side * W * 0.075, cy + H * 0.20);
                      ctx.strokeStyle = 'rgba(31,65,35,0.72)'; ctx.lineWidth = 2.3; ctx.stroke();
                      ctx.beginPath(); ctx.moveTo(cx + side * W * 0.088, cy - H * 0.245);
                      ctx.bezierCurveTo(cx + side * W * 0.103, cy - H * 0.09, cx + side * W * 0.09, cy + H * 0.08, cx + side * W * 0.067, cy + H * 0.20);
                      ctx.strokeStyle = 'rgba(226,232,240,0.12)'; ctx.lineWidth = 0.65; ctx.stroke();
                    });
                  } else {
                    var bellyGrad = ctx.createRadialGradient(cx - W * 0.035, cy - H * 0.06, 2, cx, cy + H * 0.02, W * 0.16);
                    bellyGrad.addColorStop(0, 'rgba(236,232,190,0.48)'); bellyGrad.addColorStop(1, 'rgba(203,205,158,0.08)');
                    ctx.fillStyle = bellyGrad; ctx.fillRect(cx - W * 0.18, cy - H * 0.27, W * 0.36, H * 0.54);
                  }
                  if (!frogAccessible) {
                    var frogSheen = ctx.createLinearGradient(cx - W * 0.13, cy - H * 0.24, cx + W * 0.12, cy + H * 0.14);
                    frogSheen.addColorStop(0, 'rgba(255,255,255,0)'); frogSheen.addColorStop(0.38, 'rgba(255,255,255,0.15)'); frogSheen.addColorStop(0.56, 'rgba(255,255,255,0.025)'); frogSheen.addColorStop(1, 'rgba(255,255,255,0)');
                    ctx.fillStyle = frogSheen; ctx.fillRect(cx - W * 0.2, cy - H * 0.35, W * 0.4, H * 0.64);
                  }
                  ctx.restore();
                }

                // The head is continuous with the trunk; eyes and tympana are embedded landmarks.
                var frogHeadY = cy - H * 0.265;
                [-1,1].forEach(function (side) {
                  var frogEyeX = cx + side * W * 0.102;
                  var frogEyeY = frogHeadY - H * 0.012;
                  var frogEyeR = W * 0.026;
                  ctx.save(); ctx.globalAlpha = frogDeepLayer ? 0.36 : 1;
                  ctx.beginPath(); ctx.ellipse(frogEyeX, frogEyeY + 1.2, frogEyeR * 1.10, frogEyeR * 0.88, 0, 0, Math.PI * 2); ctx.fillStyle = 'rgba(19,31,18,0.40)'; ctx.fill();
                  var frogIris = ctx.createRadialGradient(frogEyeX - 1, frogEyeY - 1, 0.6, frogEyeX, frogEyeY, frogEyeR);
                  frogIris.addColorStop(0, 'rgba(194,168,84,0.94)'); frogIris.addColorStop(0.5, 'rgba(116,91,42,0.98)'); frogIris.addColorStop(1, 'rgba(35,29,18,1)');
                  ctx.beginPath(); ctx.ellipse(frogEyeX, frogEyeY, frogEyeR, frogEyeR * 0.82, 0, 0, Math.PI * 2); ctx.fillStyle = frogIris; ctx.fill(); ctx.strokeStyle = 'rgba(18,22,15,0.9)'; ctx.lineWidth = 0.9; ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(frogEyeX, frogEyeY, frogEyeR * 0.58, frogEyeR * 0.19, 0, 0, Math.PI * 2); ctx.fillStyle = '#080b08'; ctx.fill();
                  ctx.beginPath(); ctx.moveTo(frogEyeX - frogEyeR * 0.88, frogEyeY + frogEyeR * 0.18); ctx.quadraticCurveTo(frogEyeX, frogEyeY + frogEyeR * 0.42, frogEyeX + frogEyeR * 0.88, frogEyeY + frogEyeR * 0.18);
                  ctx.strokeStyle = 'rgba(214,222,183,0.46)'; ctx.lineWidth = frogAccessible ? 1.2 : 0.65; ctx.stroke();
                  if (!frogAccessible) { ctx.beginPath(); ctx.ellipse(frogEyeX - 2, frogEyeY - 2, 1.5, 0.65, -0.4, 0, Math.PI * 2); ctx.fillStyle = 'rgba(255,255,255,0.30)'; ctx.fill(); }
                  ctx.restore();

                  var tympanumX = cx + side * W * 0.145; var tympanumY = frogHeadY + H * 0.043;
                  ctx.save(); ctx.globalAlpha = frogDeepLayer ? 0.30 : 0.82;
                  var tympanumGrad = ctx.createRadialGradient(tympanumX - 1, tympanumY - 1, 1, tympanumX, tympanumY, W * 0.022);
                  tympanumGrad.addColorStop(0, frogVentral ? '#9c9674' : '#6f7746'); tympanumGrad.addColorStop(0.72, frogVentral ? '#6f6c55' : '#424d31'); tympanumGrad.addColorStop(1, '#253026');
                  ctx.beginPath(); ctx.arc(tympanumX, tympanumY, W * 0.0215, 0, Math.PI * 2); ctx.fillStyle = tympanumGrad; ctx.fill(); ctx.strokeStyle = 'rgba(25,34,24,0.78)'; ctx.lineWidth = 0.8; ctx.stroke();
                  ctx.beginPath(); ctx.arc(tympanumX, tympanumY, W * 0.013, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(203,213,174,0.22)'; ctx.lineWidth = 0.5; ctx.stroke(); ctx.restore();
                });
                ctx.save(); ctx.globalAlpha = frogDeepLayer ? 0.32 : 0.78;
                [-1,1].forEach(function (side) { ctx.beginPath(); ctx.ellipse(cx + side * W * 0.034, cy - H * 0.326, 2.1, 1.25, side * 0.12, 0, Math.PI * 2); ctx.fillStyle = frogOutline; ctx.fill(); });
                ctx.beginPath(); ctx.moveTo(cx - W * 0.115, cy - H * 0.286); ctx.quadraticCurveTo(cx, cy - H * 0.255, cx + W * 0.115, cy - H * 0.286); ctx.strokeStyle = 'rgba(24,34,22,0.54)'; ctx.lineWidth = 0.8; ctx.stroke(); ctx.restore();

                if (activeLayer === 'muscle') {
                  // Layered muscle masses replace generic animated stripes.
                  ctx.save(); ctx.globalAlpha = 0.72;
                  var pectoralGrad = ctx.createLinearGradient(cx, cy - H * 0.16, cx, cy - H * 0.01); pectoralGrad.addColorStop(0, '#fca5a5'); pectoralGrad.addColorStop(1, '#b91c1c');
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.moveTo(cx + side * W * 0.015, cy - H * 0.145); ctx.bezierCurveTo(cx + side * W * 0.105, cy - H * 0.16, cx + side * W * 0.13, cy - H * 0.065, cx + side * W * 0.035, cy - H * 0.015); ctx.lineTo(cx, cy - H * 0.055); ctx.closePath(); ctx.fillStyle = pectoralGrad; ctx.fill(); ctx.strokeStyle = '#7f1d1d'; ctx.lineWidth = 0.65; ctx.stroke();
                  });
                  ctx.beginPath(); ctx.roundRect ? ctx.roundRect(cx - W * 0.052, cy - H * 0.035, W * 0.104, H * 0.235, 8) : ctx.rect(cx - W * 0.052, cy - H * 0.035, W * 0.104, H * 0.235);
                  ctx.fillStyle = 'rgba(248,113,113,0.84)'; ctx.fill(); ctx.strokeStyle = '#991b1b'; ctx.lineWidth = 0.7; ctx.stroke();
                  ctx.strokeStyle = 'rgba(127,29,29,0.66)'; ctx.lineWidth = 0.5;
                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.03); ctx.lineTo(cx, cy + H * 0.19); ctx.stroke();
                  for (var rectusBand = 1; rectusBand < 6; rectusBand++) { var bandY = cy - H * 0.025 + rectusBand * H * 0.036; ctx.beginPath(); ctx.moveTo(cx - W * 0.047, bandY); ctx.lineTo(cx + W * 0.047, bandY); ctx.stroke(); }
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.ellipse(cx + side * W * 0.185, cy + H * 0.13, W * 0.065, H * 0.052, side * 0.25, 0, Math.PI * 2); ctx.fillStyle = '#dc5b5b'; ctx.fill(); ctx.strokeStyle = '#991b1b'; ctx.stroke();
                    ctx.beginPath(); ctx.ellipse(cx + side * W * 0.285, cy + H * 0.19, W * 0.036, H * 0.082, side * 0.13, 0, Math.PI * 2); ctx.fillStyle = '#ef7770'; ctx.fill(); ctx.stroke();
                    ctx.strokeStyle = 'rgba(254,202,202,0.42)'; ctx.lineWidth = 0.5;
                    for (var muscleFiber = -2; muscleFiber <= 2; muscleFiber++) { ctx.beginPath(); ctx.moveTo(cx + side * W * 0.16, cy + H * (0.10 + muscleFiber * 0.012)); ctx.lineTo(cx + side * W * 0.31, cy + H * (0.22 + muscleFiber * 0.015)); ctx.stroke(); }
                  });
                  ctx.restore();
                }

                if (activeLayer === 'organs') {
                  // Organs are drawn posterior-to-anterior so overlap conveys their position in the coelom.
                  ctx.save(); ctx.globalAlpha = frogAccessible ? 0.9 : 0.78;
                  ctx.beginPath(); ctx.ellipse(cx, cy + H * 0.015, W * 0.132, H * 0.215, 0, 0, Math.PI * 2); ctx.fillStyle = 'rgba(252,231,207,0.16)'; ctx.fill(); ctx.strokeStyle = 'rgba(254,240,138,0.32)'; ctx.lineWidth = 0.8; ctx.setLineDash([3,3]); ctx.stroke(); ctx.setLineDash([]);
                  // Dorsal kidneys remain partially visible behind the gut.
                  [-1,1].forEach(function (side) { ctx.beginPath(); ctx.ellipse(cx + side * W * 0.052, cy + H * 0.115, W * 0.025, H * 0.088, side * 0.08, 0, Math.PI * 2); ctx.fillStyle = '#5b3429'; ctx.fill(); ctx.strokeStyle = '#3f241d'; ctx.lineWidth = 0.55; ctx.stroke(); });
                  // Paired sac-like lungs.
                  [-1,1].forEach(function (side) { ctx.beginPath(); ctx.ellipse(cx + side * W * 0.071, cy - H * 0.105, W * 0.035, H * 0.057, side * 0.08, 0, Math.PI * 2); ctx.fillStyle = '#eaa1a1'; ctx.fill(); ctx.strokeStyle = '#a83a42'; ctx.lineWidth = 0.55; ctx.stroke(); ctx.save(); ctx.globalAlpha = 0.28; ctx.strokeStyle = '#7f1d1d'; ctx.lineWidth = 0.35; for(var alveolar=0; alveolar<5; alveolar++){ctx.beginPath(); ctx.arc(cx + side * W * (0.061 + (alveolar%2)*0.018), cy - H * (0.13 - Math.floor(alveolar/2)*0.025), 2.4, 0, Math.PI*2); ctx.stroke();} ctx.restore(); });
                  // Yellow branched fat bodies flank the anterior kidneys.
                  [-1,1].forEach(function (side) { for(var fatFinger=0; fatFinger<4; fatFinger++){ var fatX=cx+side*W*(0.035+fatFinger*0.013); ctx.beginPath(); ctx.moveTo(cx+side*W*0.04,cy-H*0.045); ctx.quadraticCurveTo(fatX,cy+H*0.005,fatX+side*W*0.01,cy+H*(0.035+fatFinger*0.012)); ctx.strokeStyle='#fbbf24'; ctx.lineWidth=2.1-fatFinger*0.2; ctx.stroke(); } });
                  // Three-chambered heart: paired atria over one muscular ventricle and conus.
                  var frogHeartX = cx; var frogHeartY = cy - H * 0.105;
                  [-1,1].forEach(function (side) { ctx.beginPath(); ctx.ellipse(frogHeartX + side * W * 0.019, frogHeartY - H * 0.008, W * 0.022, H * 0.022, side * 0.15, 0, Math.PI * 2); ctx.fillStyle = side < 0 ? '#b4535a' : '#a64752'; ctx.fill(); ctx.strokeStyle='#7f1d1d';ctx.lineWidth=0.5;ctx.stroke(); });
                  ctx.beginPath(); ctx.ellipse(frogHeartX, frogHeartY + H * 0.027, W * 0.031, H * 0.037, 0, 0, Math.PI * 2); ctx.fillStyle='#b91c1c';ctx.fill();ctx.strokeStyle='#7f1d1d';ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(frogHeartX + W * 0.012, frogHeartY - H * 0.022); ctx.quadraticCurveTo(frogHeartX + W * 0.032, frogHeartY - H * 0.075, frogHeartX + W * 0.018, frogHeartY - H * 0.102); ctx.strokeStyle='#dc2626';ctx.lineWidth=3;ctx.stroke();
                  // Three liver lobes overlap the posterior heart margin.
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.057, cy - H * 0.015, W * 0.056, H * 0.055, -0.12, 0, Math.PI*2); ctx.fillStyle='#6f3529';ctx.fill();ctx.strokeStyle='#4a251f';ctx.lineWidth=0.55;ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.057, cy - H * 0.015, W * 0.056, H * 0.055, 0.12, 0, Math.PI*2); ctx.fillStyle='#74372b';ctx.fill();ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx, cy + H * 0.018, W * 0.052, H * 0.046, 0, 0, Math.PI*2); ctx.fillStyle='#653027';ctx.fill();ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.027, cy + H * 0.018, W * 0.013, H * 0.022, 0.2, 0, Math.PI*2);ctx.fillStyle='#4d9a50';ctx.fill();ctx.strokeStyle='#1f6d35';ctx.lineWidth=0.45;ctx.stroke();
                  // J-shaped stomach with a duodenal turn.
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.055, cy + H * 0.03); ctx.bezierCurveTo(cx - W * 0.105, cy + H * 0.035, cx - W * 0.105, cy + H * 0.12, cx - W * 0.055, cy + H * 0.13); ctx.bezierCurveTo(cx - W * 0.018, cy + H * 0.138, cx - W * 0.012, cy + H * 0.10, cx - W * 0.036, cy + H * 0.083); ctx.bezierCurveTo(cx - W * 0.06, cy + H * 0.065, cx - W * 0.035, cy + H * 0.045, cx - W * 0.055, cy + H * 0.03); ctx.closePath(); var stomachGrad=ctx.createLinearGradient(cx-W*0.10,cy+H*0.04,cx-W*0.02,cy+H*0.13);stomachGrad.addColorStop(0,'#f4d39a');stomachGrad.addColorStop(1,'#c88852');ctx.fillStyle=stomachGrad;ctx.fill();ctx.strokeStyle='#9a5b32';ctx.lineWidth=0.6;ctx.stroke();
                  ctx.beginPath();ctx.moveTo(cx-W*0.01,cy+H*0.105);ctx.quadraticCurveTo(cx+W*0.035,cy+H*0.115,cx+W*0.068,cy+H*0.09);ctx.strokeStyle='#eabf72';ctx.lineWidth=3.2;ctx.stroke();
                  ctx.beginPath();ctx.moveTo(cx-W*0.012,cy+H*0.10);ctx.quadraticCurveTo(cx+W*0.028,cy+H*0.092,cx+W*0.067,cy+H*0.078);ctx.strokeStyle='#e7b54d';ctx.lineWidth=1.4;ctx.stroke();
                  ctx.beginPath();ctx.ellipse(cx-W*0.092,cy+H*0.115,W*0.012,H*0.009,-0.2,0,Math.PI*2);ctx.fillStyle='#7f1d1d';ctx.fill();
                  // Coiled ileum sits centrally and feeds a short, broad large intestine.
                  ctx.strokeStyle='#d78349';ctx.lineWidth=4.1;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(cx+W*0.055,cy+H*0.10);ctx.bezierCurveTo(cx+W*0.09,cy+H*0.13,cx-W*0.07,cy+H*0.14,cx-W*0.025,cy+H*0.17);ctx.bezierCurveTo(cx+W*0.04,cy+H*0.20,cx+W*0.085,cy+H*0.145,cx+W*0.035,cy+H*0.135);ctx.bezierCurveTo(cx-W*0.025,cy+H*0.12,cx-W*0.07,cy+H*0.19,cx+W*0.015,cy+H*0.21);ctx.stroke();
                  ctx.beginPath();ctx.moveTo(cx+W*0.015,cy+H*0.205);ctx.bezierCurveTo(cx+W*0.02,cy+H*0.235,cx+W*0.008,cy+H*0.25,cx,cy+H*0.262);ctx.strokeStyle='#b86b42';ctx.lineWidth=6.2;ctx.stroke();
                  // Bilobed urinary bladder and shared cloacal outlet.
                  ctx.save();ctx.globalAlpha=0.48;[-1,1].forEach(function(side){ctx.beginPath();ctx.ellipse(cx+side*W*0.025,cy+H*0.245,W*0.027,H*0.024,side*0.18,0,Math.PI*2);ctx.fillStyle='#bae6fd';ctx.fill();ctx.strokeStyle='#60a5fa';ctx.lineWidth=0.5;ctx.stroke();});ctx.restore();
                  ctx.beginPath();ctx.ellipse(cx,cy+H*0.268,W*0.013,H*0.008,0,0,Math.PI*2);ctx.fillStyle='#704838';ctx.fill();
                  ctx.restore();
                }

                if (activeLayer === 'skeleton') {
                  ctx.save(); ctx.globalAlpha = frogAccessible ? 0.9 : 0.72; var frogBone='#d9d6c7'; var frogBoneEdge='#8f9188';
                  ctx.beginPath(); ctx.moveTo(cx-W*0.105,cy-H*0.31);ctx.quadraticCurveTo(cx,cy-H*0.35,cx+W*0.105,cy-H*0.31);ctx.lineTo(cx+W*0.12,cy-H*0.245);ctx.quadraticCurveTo(cx,cy-H*0.22,cx-W*0.12,cy-H*0.245);ctx.closePath();ctx.fillStyle='rgba(217,214,199,0.26)';ctx.fill();ctx.strokeStyle=frogBone;ctx.lineWidth=1.25;ctx.stroke();
                  for(var frogVert=0;frogVert<9;frogVert++){var frogVertY=cy-H*0.205+frogVert*H*0.041;ctx.beginPath();ctx.ellipse(cx,frogVertY,W*0.012,H*0.009,0,0,Math.PI*2);ctx.fillStyle=frogBone;ctx.fill();ctx.strokeStyle=frogBoneEdge;ctx.lineWidth=0.4;ctx.stroke();}
                  ctx.beginPath();ctx.moveTo(cx,cy+H*0.12);ctx.lineTo(cx,cy+H*0.255);ctx.strokeStyle=frogBone;ctx.lineWidth=3.5;ctx.stroke();
                  ctx.beginPath();ctx.moveTo(cx-W*0.105,cy-H*0.14);ctx.lineTo(cx,cy-H*0.09);ctx.lineTo(cx+W*0.105,cy-H*0.14);ctx.moveTo(cx-W*0.09,cy-H*0.08);ctx.lineTo(cx,cy-H*0.12);ctx.lineTo(cx+W*0.09,cy-H*0.08);ctx.strokeStyle=frogBone;ctx.lineWidth=1.6;ctx.stroke();
                  ctx.beginPath();ctx.moveTo(cx-W*0.09,cy+H*0.15);ctx.lineTo(cx,cy+H*0.20);ctx.lineTo(cx+W*0.09,cy+H*0.15);ctx.moveTo(cx-W*0.09,cy+H*0.15);ctx.lineTo(cx-W*0.13,cy+H*0.22);ctx.moveTo(cx+W*0.09,cy+H*0.15);ctx.lineTo(cx+W*0.13,cy+H*0.22);ctx.strokeStyle=frogBone;ctx.lineWidth=2;ctx.stroke();
                  [-1,1].forEach(function(side){
                    ctx.beginPath();ctx.moveTo(cx+side*W*0.12,cy+H*0.16);ctx.lineTo(cx+side*W*0.275,cy+H*0.105);ctx.lineTo(cx+side*W*0.315,cy+H*0.245);ctx.lineTo(cx+side*W*0.265,cy+H*0.355);ctx.strokeStyle=frogBone;ctx.lineWidth=2.35;ctx.stroke();
                    ctx.beginPath();ctx.moveTo(cx+side*W*0.14,cy-H*0.12);ctx.lineTo(cx+side*W*0.225,cy-H*0.045);ctx.lineTo(cx+side*W*0.205,cy+H*0.065);ctx.strokeStyle=frogBone;ctx.lineWidth=1.65;ctx.stroke();
                  });
                  ctx.restore();
                }

                if (activeLayer === 'nervous') {
                  ctx.save(); ctx.globalAlpha = frogAccessible ? 0.92 : 0.76;
                  ctx.beginPath();ctx.ellipse(cx,cy-H*0.285,W*0.045,H*0.026,0,0,Math.PI*2);ctx.fillStyle='rgba(196,181,253,0.76)';ctx.fill();ctx.strokeStyle='#7c3aed';ctx.lineWidth=0.9;ctx.stroke();
                  [-1,1].forEach(function(side){ctx.beginPath();ctx.ellipse(cx+side*W*0.025,cy-H*0.292,W*0.019,H*0.014,0,0,Math.PI*2);ctx.fillStyle='#a78bfa';ctx.fill();ctx.beginPath();ctx.moveTo(cx+side*W*0.027,cy-H*0.29);ctx.lineTo(cx+side*W*0.088,cy-H*0.275);ctx.strokeStyle='#c084fc';ctx.lineWidth=1;ctx.stroke();});
                  ctx.beginPath();ctx.moveTo(cx,cy-H*0.255);ctx.lineTo(cx,cy+H*0.19);ctx.strokeStyle='#a78bfa';ctx.lineWidth=2.2;ctx.stroke();
                  for(var frogNerve=0;frogNerve<9;frogNerve++){var nerveY=cy-H*0.19+frogNerve*H*0.046;ctx.beginPath();ctx.moveTo(cx,nerveY);ctx.lineTo(cx-W*0.085,nerveY+H*0.012);ctx.moveTo(cx,nerveY);ctx.lineTo(cx+W*0.085,nerveY+H*0.012);ctx.strokeStyle='rgba(192,132,252,0.58)';ctx.lineWidth=0.65;ctx.stroke();}
                  [-1,1].forEach(function(side){ctx.beginPath();ctx.moveTo(cx+side*W*0.035,cy+H*0.14);ctx.bezierCurveTo(cx+side*W*0.13,cy+H*0.18,cx+side*W*0.23,cy+H*0.24,cx+side*W*0.29,cy+H*0.34);ctx.strokeStyle='#c084fc';ctx.lineWidth=1.8;ctx.stroke();ctx.beginPath();ctx.moveTo(cx+side*W*0.02,cy-H*0.12);ctx.quadraticCurveTo(cx+side*W*0.10,cy-H*0.09,cx+side*W*0.19,cy-H*0.025);ctx.strokeStyle='#a78bfa';ctx.lineWidth=1.1;ctx.stroke();});
                  ctx.restore();
                }


              } else if (spec.bodyShape === 'worm') {

                // Earthworm geometry is built from one curved centerline so surface
                // segments, setae, and internal systems remain anatomically registered.
                var wormTop = cy - H * 0.38;
                var wormLength = H * 0.76;
                var wormBaseWidth = W * 0.047;
                var wormSegmentCount = 40;
                var wormAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var wormRealistic = (d.visualRealism || visualRealism) === 'realistic';

                function earthwormPoint(t) {
                  var clamped = Math.max(0, Math.min(1, t));
                  return {
                    x: cx + Math.sin(clamped * Math.PI * 2.15 + 0.32) * W * 0.026 + Math.sin(clamped * Math.PI * 5.1) * W * 0.005,
                    y: wormTop + wormLength * clamped
                  };
                }
                function earthwormWidth(t) {
                  var anteriorTaper = Math.min(1, Math.max(0, t / 0.055));
                  var posteriorTaper = Math.min(1, Math.max(0, (1 - t) / 0.105));
                  var taper = 0.34 + Math.min(anteriorTaper, posteriorTaper) * 0.66;
                  var clitellumExpansion = t >= 0.29 && t <= 0.39 ? 1.16 : 1;
                  return wormBaseWidth * taper * clitellumExpansion;
                }
                function earthwormFrame(t) {
                  var before = earthwormPoint(Math.max(0, t - 0.004));
                  var after = earthwormPoint(Math.min(1, t + 0.004));
                  var tx = after.x - before.x, ty = after.y - before.y;
                  var tangentLength = Math.sqrt(tx * tx + ty * ty) || 1;
                  tx /= tangentLength; ty /= tangentLength;
                  return { point: earthwormPoint(t), tx: tx, ty: ty, nx: -ty, ny: tx, width: earthwormWidth(t) };
                }
                function traceEarthwormBody() {
                  var bodySamples = 56;
                  ctx.beginPath();
                  for (var leftSample = 0; leftSample <= bodySamples; leftSample++) {
                    var leftT = leftSample / bodySamples, leftFrame = earthwormFrame(leftT);
                    var leftX = leftFrame.point.x + leftFrame.nx * leftFrame.width;
                    var leftY = leftFrame.point.y + leftFrame.ny * leftFrame.width;
                    if (leftSample === 0) ctx.moveTo(leftX, leftY); else ctx.lineTo(leftX, leftY);
                  }
                  for (var rightSample = bodySamples; rightSample >= 0; rightSample--) {
                    var rightT = rightSample / bodySamples, rightFrame = earthwormFrame(rightT);
                    ctx.lineTo(rightFrame.point.x - rightFrame.nx * rightFrame.width, rightFrame.point.y - rightFrame.ny * rightFrame.width);
                  }
                  ctx.closePath();
                }
                function traceEarthwormLine(offsetRatio, startT, endT) {
                  var lineSteps = 44;
                  ctx.beginPath();
                  for (var lineStep = 0; lineStep <= lineSteps; lineStep++) {
                    var lineT = startT + (endT - startT) * lineStep / lineSteps;
                    var lineFrame = earthwormFrame(lineT);
                    var lineX = lineFrame.point.x + lineFrame.nx * lineFrame.width * offsetRatio;
                    var lineY = lineFrame.point.y + lineFrame.ny * lineFrame.width * offsetRatio;
                    if (lineStep === 0) ctx.moveTo(lineX, lineY); else ctx.lineTo(lineX, lineY);
                  }
                }
                function traceEarthwormBand(startT, endT, widthScale) {
                  var bandSteps = 12;
                  ctx.beginPath();
                  for (var bandLeft = 0; bandLeft <= bandSteps; bandLeft++) {
                    var bandLeftT = startT + (endT - startT) * bandLeft / bandSteps;
                    var bandLeftFrame = earthwormFrame(bandLeftT);
                    var bandLeftX = bandLeftFrame.point.x + bandLeftFrame.nx * bandLeftFrame.width * widthScale;
                    var bandLeftY = bandLeftFrame.point.y + bandLeftFrame.ny * bandLeftFrame.width * widthScale;
                    if (bandLeft === 0) ctx.moveTo(bandLeftX, bandLeftY); else ctx.lineTo(bandLeftX, bandLeftY);
                  }
                  for (var bandRight = bandSteps; bandRight >= 0; bandRight--) {
                    var bandRightT = startT + (endT - startT) * bandRight / bandSteps;
                    var bandRightFrame = earthwormFrame(bandRightT);
                    ctx.lineTo(bandRightFrame.point.x - bandRightFrame.nx * bandRightFrame.width * widthScale, bandRightFrame.point.y - bandRightFrame.ny * bandRightFrame.width * widthScale);
                  }
                  ctx.closePath();
                }

                var wormSurfaceGradient = ctx.createLinearGradient(cx - wormBaseWidth * 1.4, 0, cx + wormBaseWidth * 1.4, 0);
                wormSurfaceGradient.addColorStop(0, layerStroke);
                wormSurfaceGradient.addColorStop(0.24, layerColor);
                wormSurfaceGradient.addColorStop(0.52, wormAccessible ? '#ddd6fe' : layerColor);
                wormSurfaceGradient.addColorStop(0.78, layerColor);
                wormSurfaceGradient.addColorStop(1, layerStroke);
                traceEarthwormBody(); ctx.fillStyle = wormSurfaceGradient; ctx.fill();
                ctx.strokeStyle = layerStroke; ctx.lineWidth = wormAccessible ? 2 : 1.15; ctx.stroke();
                ctx.shadowBlur = 0;

                // Soft dorsal shading and moist sheen follow the same curved axis.
                ctx.save(); traceEarthwormBody(); ctx.clip();
                var wormVolumeGradient = ctx.createLinearGradient(cx - wormBaseWidth, 0, cx + wormBaseWidth, 0);
                wormVolumeGradient.addColorStop(0, 'rgba(35,20,36,' + (wormRealistic ? '0.24' : '0.13') + ')');
                wormVolumeGradient.addColorStop(0.42, 'rgba(255,255,255,0.16)');
                wormVolumeGradient.addColorStop(0.62, 'rgba(255,255,255,0.035)');
                wormVolumeGradient.addColorStop(1, 'rgba(35,20,36,0.18)');
                ctx.fillStyle = wormVolumeGradient; ctx.fillRect(cx - W * 0.11, wormTop - H * 0.02, W * 0.22, wormLength + H * 0.04);
                ctx.globalAlpha = wormAccessible ? 0.12 : 0.2;
                traceEarthwormLine(-0.28, 0.035, 0.96);
                var wormMoistPalette = (typeof window !== 'undefined' && window.AlloStemTheme && window.AlloStemTheme.palette) ? window.AlloStemTheme.palette() : { text: '#ffffff' };
                ctx.strokeStyle = wormMoistPalette.text || '#ffffff'; ctx.lineWidth = wormBaseWidth * 0.32; ctx.lineCap = 'round'; ctx.stroke();
                ctx.restore();

                // Fine annuli are perpendicular to the local body tangent and tighten at both ends.
                ctx.globalAlpha = wormAccessible ? 0.34 : (wormRealistic ? 0.28 : 0.2);
                for (var wormSegment = 1; wormSegment < wormSegmentCount; wormSegment++) {
                  var segmentT = wormSegment / wormSegmentCount;
                  var segmentFrame = earthwormFrame(segmentT);
                  var segmentInset = segmentT >= 0.29 && segmentT <= 0.39 ? 0.97 : 0.9;
                  ctx.beginPath();
                  ctx.moveTo(segmentFrame.point.x + segmentFrame.nx * segmentFrame.width * segmentInset, segmentFrame.point.y + segmentFrame.ny * segmentFrame.width * segmentInset);
                  ctx.lineTo(segmentFrame.point.x - segmentFrame.nx * segmentFrame.width * segmentInset, segmentFrame.point.y - segmentFrame.ny * segmentFrame.width * segmentInset);
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = wormAccessible ? 0.9 : 0.55; ctx.stroke();
                }
                ctx.globalAlpha = 1;

                // The clitellum is a gently expanded glandular saddle integrated into the annuli.
                traceEarthwormBand(0.29, 0.39, 1.015);
                ctx.fillStyle = wormAccessible ? 'rgba(253,230,138,0.48)' : 'rgba(221,176,156,' + (wormRealistic ? '0.46' : '0.34') + ')'; ctx.fill();
                ctx.strokeStyle = wormAccessible ? '#92400e' : layerStroke; ctx.lineWidth = wormAccessible ? 1.5 : 0.8; ctx.stroke();
                [0.29, 0.39].forEach(function (clitellumEdgeT) {
                  var edgeFrame = earthwormFrame(clitellumEdgeT);
                  ctx.beginPath();
                  ctx.moveTo(edgeFrame.point.x + edgeFrame.nx * edgeFrame.width, edgeFrame.point.y + edgeFrame.ny * edgeFrame.width);
                  ctx.lineTo(edgeFrame.point.x - edgeFrame.nx * edgeFrame.width, edgeFrame.point.y - edgeFrame.ny * edgeFrame.width);
                  ctx.strokeStyle = 'rgba(83,54,56,0.48)'; ctx.lineWidth = 1; ctx.stroke();
                });

                // Anterior prostomium forms a small sensory lip over the mouth.
                var prostomiumFrame = earthwormFrame(0);
                var prostomiumAngle = Math.atan2(prostomiumFrame.ty, prostomiumFrame.tx);
                ctx.save(); ctx.translate(prostomiumFrame.point.x, prostomiumFrame.point.y - H * 0.004); ctx.rotate(prostomiumAngle);
                ctx.beginPath(); ctx.ellipse(-H * 0.004, 0, H * 0.012, wormBaseWidth * 0.46, 0, 0, Math.PI * 2);
                ctx.fillStyle = layerColor; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.8; ctx.stroke();
                ctx.beginPath(); ctx.arc(-H * 0.009, 0, 1.15, 0, Math.PI * 2); ctx.fillStyle = 'rgba(52,31,39,0.58)'; ctx.fill();
                ctx.restore();

                // Four paired setal positions are suggested on alternating segments without exaggeration.
                ctx.globalAlpha = wormAccessible ? 0.68 : 0.42;
                for (var setaSegment = 3; setaSegment < wormSegmentCount - 2; setaSegment += 2) {
                  var setaT = (setaSegment + 0.5) / wormSegmentCount;
                  if (setaT >= 0.29 && setaT <= 0.39) continue;
                  var setaFrame = earthwormFrame(setaT);
                  [-1, 1].forEach(function (setaSide) {
                    [0.76, 0.94].forEach(function (setaBand, setaIndex) {
                      var setaBaseX = setaFrame.point.x + setaFrame.nx * setaFrame.width * setaBand * setaSide;
                      var setaBaseY = setaFrame.point.y + setaFrame.ny * setaFrame.width * setaBand * setaSide;
                      ctx.beginPath(); ctx.moveTo(setaBaseX, setaBaseY);
                      ctx.lineTo(setaBaseX + setaFrame.nx * setaSide * (3.2 + setaIndex), setaBaseY + setaFrame.ny * setaSide * (3.2 + setaIndex) - setaFrame.ty * 1.4);
                      ctx.strokeStyle = wormAccessible ? '#27272a' : 'rgba(70,47,43,0.8)'; ctx.lineWidth = 0.55; ctx.stroke();
                    });
                  });
                }
                ctx.globalAlpha = 1;

                if (activeLayer === 'skin') {
                  // Deterministic mucus-gland points reinforce cutaneous respiration without sparkle.
                  ctx.globalAlpha = wormAccessible ? 0.34 : 0.2;
                  for (var mucusGland = 0; mucusGland < 28; mucusGland++) {
                    var mucusT = 0.04 + ((mucusGland * 29) % 91) / 100;
                    var mucusFrame = earthwormFrame(mucusT);
                    var mucusOffset = ((mucusGland % 5) - 2) / 2.7;
                    ctx.beginPath(); ctx.arc(mucusFrame.point.x + mucusFrame.nx * mucusFrame.width * mucusOffset, mucusFrame.point.y + mucusFrame.ny * mucusFrame.width * mucusOffset, 0.7, 0, Math.PI * 2);
                    ctx.fillStyle = wormMoistPalette.text || '#ffffff'; ctx.fill();
                  }
                  ctx.globalAlpha = 1;
                }

                if (activeLayer === 'muscle') {
                  ctx.globalAlpha = wormAccessible ? 0.68 : 0.46;
                  [-0.5, 0, 0.5].forEach(function (muscleOffset) {
                    traceEarthwormLine(muscleOffset, 0.025, 0.975);
                    ctx.strokeStyle = '#b91c1c'; ctx.lineWidth = muscleOffset === 0 ? 1.15 : 0.8; ctx.stroke();
                  });
                  for (var circularBand = 2; circularBand < wormSegmentCount - 1; circularBand += 2) {
                    var circularFrame = earthwormFrame(circularBand / wormSegmentCount);
                    ctx.beginPath();
                    ctx.moveTo(circularFrame.point.x + circularFrame.nx * circularFrame.width * 0.88, circularFrame.point.y + circularFrame.ny * circularFrame.width * 0.88);
                    ctx.lineTo(circularFrame.point.x - circularFrame.nx * circularFrame.width * 0.88, circularFrame.point.y - circularFrame.ny * circularFrame.width * 0.88);
                    ctx.strokeStyle = '#f87171'; ctx.lineWidth = 0.8; ctx.stroke();
                  }
                  ctx.globalAlpha = 1;
                }

                if (activeLayer === 'organs') {
                  ctx.globalAlpha = wormAccessible ? 0.82 : 0.64;
                  // Digestive tube, pharynx, crop, and muscular gizzard follow the body axis.
                  traceEarthwormLine(0, 0.055, 0.95); ctx.strokeStyle = '#f59e0b'; ctx.lineWidth = wormBaseWidth * 0.48; ctx.stroke();
                  var pharynxFrame = earthwormFrame(0.105);
                  ctx.beginPath(); ctx.ellipse(pharynxFrame.point.x, pharynxFrame.point.y, wormBaseWidth * 0.52, H * 0.018, 0.05, 0, Math.PI * 2); ctx.fillStyle = '#fcd34d'; ctx.fill();
                  var cropFrame = earthwormFrame(0.315);
                  ctx.beginPath(); ctx.ellipse(cropFrame.point.x, cropFrame.point.y, wormBaseWidth * 0.62, H * 0.018, -0.04, 0, Math.PI * 2); ctx.fillStyle = '#fde68a'; ctx.fill(); ctx.strokeStyle = '#d97706'; ctx.lineWidth = 0.6; ctx.stroke();
                  var gizzardFrame = earthwormFrame(0.37);
                  ctx.beginPath(); ctx.ellipse(gizzardFrame.point.x, gizzardFrame.point.y, wormBaseWidth * 0.72, H * 0.02, 0.04, 0, Math.PI * 2); ctx.fillStyle = '#c084fc'; ctx.fill(); ctx.strokeStyle = '#7c3aed'; ctx.stroke();
                  // Five paired aortic arches encircle the anterior digestive tract.
                  for (var archIndex = 0; archIndex < 5; archIndex++) {
                    var archFrame = earthwormFrame(0.16 + archIndex * 0.026);
                    ctx.beginPath(); ctx.ellipse(archFrame.point.x, archFrame.point.y, archFrame.width * 0.72, H * 0.009, 0, 0, Math.PI * 2);
                    ctx.strokeStyle = '#dc2626'; ctx.lineWidth = 1.3; ctx.stroke();
                  }
                  // Paired seminal vesicles sit beside the arches.
                  [0.22, 0.25].forEach(function (vesicleT) {
                    var vesicleFrame = earthwormFrame(vesicleT);
                    [-1, 1].forEach(function (vesicleSide) {
                      ctx.beginPath(); ctx.ellipse(vesicleFrame.point.x + vesicleFrame.nx * vesicleFrame.width * 0.46 * vesicleSide, vesicleFrame.point.y + vesicleFrame.ny * vesicleFrame.width * 0.46 * vesicleSide, W * 0.007, H * 0.008, 0, 0, Math.PI * 2);
                      ctx.fillStyle = '#f8fafc'; ctx.fill(); ctx.strokeStyle = '#cbd5e1'; ctx.lineWidth = 0.5; ctx.stroke();
                    });
                  });
                  ctx.globalAlpha = 1;
                }

                if (activeLayer === 'nervous') {
                  ctx.globalAlpha = wormAccessible ? 0.88 : 0.72;
                  // Ventral cord sits slightly off the digestive midline in this teaching view.
                  traceEarthwormLine(0.28, 0.06, 0.97); ctx.strokeStyle = '#fbbf24'; ctx.lineWidth = wormAccessible ? 2 : 1.35; ctx.stroke();
                  var cerebralFrame = earthwormFrame(0.065);
                  [-1, 1].forEach(function (ganglionSide) {
                    ctx.beginPath(); ctx.ellipse(cerebralFrame.point.x + cerebralFrame.nx * ganglionSide * W * 0.006, cerebralFrame.point.y + cerebralFrame.ny * ganglionSide * W * 0.006, W * 0.0065, H * 0.006, 0, 0, Math.PI * 2);
                    ctx.fillStyle = '#fde047'; ctx.fill();
                  });
                  for (var ganglionIndex = 4; ganglionIndex < wormSegmentCount - 1; ganglionIndex += 3) {
                    var ganglionFrame = earthwormFrame(ganglionIndex / wormSegmentCount);
                    ctx.beginPath(); ctx.arc(ganglionFrame.point.x + ganglionFrame.nx * ganglionFrame.width * 0.28, ganglionFrame.point.y + ganglionFrame.ny * ganglionFrame.width * 0.28, wormAccessible ? 2.5 : 1.8, 0, Math.PI * 2);
                    ctx.fillStyle = '#f59e0b'; ctx.fill();
                  }
                  ctx.globalAlpha = 1;
                }


              } else if (spec.bodyShape === 'pig') {

                // Fetal pig in a natural side-lying teaching-specimen posture. The
                // restrained landmarks and closed eyelid avoid a mascot-like face.
                var pigAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var pigRealistic = (d.visualRealism || visualRealism) === 'realistic';
                var pigOutlineWidth = pigAccessible ? 2.2 : 1.25;
                var pigTissueGradient = ctx.createLinearGradient(cx - W * 0.16, cy - H * 0.15, cx + W * 0.20, cy + H * 0.16);
                pigTissueGradient.addColorStop(0, layerStroke);
                pigTissueGradient.addColorStop(0.18, layerColor);
                pigTissueGradient.addColorStop(0.58, layerColor);
                pigTissueGradient.addColorStop(1, layerStroke);

                function tracePigBody() {
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.17, cy - H * 0.105);
                  ctx.bezierCurveTo(cx - W * 0.09, cy - H * 0.155, cx + W * 0.055, cy - H * 0.17, cx + W * 0.155, cy - H * 0.125);
                  ctx.bezierCurveTo(cx + W * 0.225, cy - H * 0.095, cx + W * 0.245, cy - H * 0.018, cx + W * 0.215, cy + H * 0.075);
                  ctx.bezierCurveTo(cx + W * 0.185, cy + H * 0.145, cx + W * 0.075, cy + H * 0.17, cx - W * 0.055, cy + H * 0.145);
                  ctx.bezierCurveTo(cx - W * 0.145, cy + H * 0.13, cx - W * 0.19, cy + H * 0.075, cx - W * 0.185, cy - H * 0.015);
                  ctx.bezierCurveTo(cx - W * 0.185, cy - H * 0.055, cx - W * 0.18, cy - H * 0.085, cx - W * 0.17, cy - H * 0.105);
                  ctx.closePath();
                }

                function drawPigLimb(shoulderX, shoulderY, kneeX, kneeY, hoofX, hoofY, farSide) {
                  ctx.save();
                  ctx.globalAlpha = farSide ? 0.55 : 0.96;
                  ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                  ctx.beginPath(); ctx.moveTo(shoulderX, shoulderY);
                  ctx.quadraticCurveTo((shoulderX + kneeX) / 2 - W * 0.012, (shoulderY + kneeY) / 2, kneeX, kneeY);
                  ctx.quadraticCurveTo((kneeX + hoofX) / 2 + W * 0.008, (kneeY + hoofY) / 2, hoofX, hoofY);
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = farSide ? W * 0.027 : W * 0.033; ctx.stroke();
                  ctx.strokeStyle = layerColor; ctx.lineWidth = farSide ? W * 0.021 : W * 0.027; ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(kneeX, kneeY, W * 0.018, H * 0.012, -0.25, 0, Math.PI * 2);
                  ctx.fillStyle = layerColor; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.7; ctx.stroke();
                  var hoofAngle = Math.atan2(hoofY - kneeY, hoofX - kneeX);
                  ctx.save(); ctx.translate(hoofX, hoofY); ctx.rotate(hoofAngle - Math.PI / 2);
                  [-1, 1].forEach(function (hoofSide) {
                    ctx.beginPath();
                    ctx.ellipse(hoofSide * W * 0.008, H * 0.005, W * 0.0075, H * 0.016, hoofSide * 0.08, 0, Math.PI * 2);
                    ctx.fillStyle = pigAccessible ? '#3f3f46' : 'rgba(63,47,46,0.78)'; ctx.fill();
                  });
                  ctx.restore(); ctx.restore();
                }

                // Far-side limbs sit behind the torso and are deliberately quieter.
                drawPigLimb(cx - W * 0.085, cy + H * 0.075, cx - W * 0.055, cy + H * 0.17, cx + W * 0.005, cy + H * 0.235, true);
                drawPigLimb(cx + W * 0.135, cy + H * 0.075, cx + W * 0.205, cy + H * 0.155, cx + W * 0.235, cy + H * 0.225, true);

                tracePigBody();
                ctx.fillStyle = pigTissueGradient; ctx.fill();
                ctx.strokeStyle = layerStroke; ctx.lineWidth = pigOutlineWidth; ctx.stroke();
                ctx.shadowBlur = 0;

                // Directional volume and mild preservation mottling stay inside the torso.
                ctx.save(); tracePigBody(); ctx.clip();
                var pigVolume = ctx.createRadialGradient(cx - W * 0.055, cy - H * 0.08, W * 0.01, cx, cy, W * 0.29);
                pigVolume.addColorStop(0, 'rgba(255,255,255,' + (pigAccessible ? '0.16' : '0.22') + ')');
                pigVolume.addColorStop(0.58, 'rgba(255,255,255,0.015)');
                pigVolume.addColorStop(1, 'rgba(49,24,31,' + (pigRealistic ? '0.18' : '0.10') + ')');
                ctx.fillStyle = pigVolume; ctx.fillRect(cx - W * 0.24, cy - H * 0.2, W * 0.5, H * 0.4);
                if (!pigAccessible) {
                  ctx.globalAlpha = pigRealistic ? 0.13 : 0.075;
                  for (var pigMottle = 0; pigMottle < 34; pigMottle++) {
                    var mottleX = cx - W * 0.15 + ((pigMottle * 47) % 101) / 100 * W * 0.35;
                    var mottleY = cy - H * 0.11 + ((pigMottle * 73) % 101) / 100 * H * 0.23;
                    ctx.beginPath(); ctx.ellipse(mottleX, mottleY, W * (0.004 + (pigMottle % 4) * 0.0015), H * 0.003, 0.3, 0, Math.PI * 2);
                    ctx.fillStyle = pigMottle % 3 ? layerStroke : '#f8d7d2'; ctx.fill();
                  }
                }
                ctx.restore();

                // Neck and head use a tapered fetal profile instead of a circular face.
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.165, cy - H * 0.09);
                ctx.bezierCurveTo(cx - W * 0.205, cy - H * 0.115, cx - W * 0.235, cy - H * 0.115, cx - W * 0.262, cy - H * 0.075);
                ctx.bezierCurveTo(cx - W * 0.282, cy - H * 0.047, cx - W * 0.305, cy - H * 0.035, cx - W * 0.325, cy - H * 0.014);
                ctx.bezierCurveTo(cx - W * 0.333, cy + H * 0.003, cx - W * 0.322, cy + H * 0.026, cx - W * 0.297, cy + H * 0.042);
                ctx.bezierCurveTo(cx - W * 0.265, cy + H * 0.072, cx - W * 0.22, cy + H * 0.085, cx - W * 0.18, cy + H * 0.052);
                ctx.bezierCurveTo(cx - W * 0.158, cy + H * 0.025, cx - W * 0.15, cy - H * 0.055, cx - W * 0.165, cy - H * 0.09);
                ctx.closePath();
                var pigHeadGradient = ctx.createLinearGradient(cx - W * 0.31, cy - H * 0.09, cx - W * 0.17, cy + H * 0.08);
                pigHeadGradient.addColorStop(0, layerColor); pigHeadGradient.addColorStop(0.72, layerColor); pigHeadGradient.addColorStop(1, layerStroke);
                ctx.fillStyle = pigHeadGradient; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = pigOutlineWidth; ctx.stroke();

                // Blunt tapered muzzle, small nares, and a closed eyelid read as fetal anatomy.
                ctx.beginPath(); ctx.ellipse(cx - W * 0.322, cy + H * 0.006, W * 0.021, H * 0.024, -0.18, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(255,255,255,0.07)'; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.8; ctx.stroke();
                [-1, 1].forEach(function (nostrilSide) {
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.326, cy + H * 0.006 + nostrilSide * H * 0.009, W * 0.0032, H * 0.0028, nostrilSide * 0.35, 0, Math.PI * 2);
                  ctx.fillStyle = 'rgba(58,35,39,0.72)'; ctx.fill();
                });
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.265, cy - H * 0.049);
                ctx.quadraticCurveTo(cx - W * 0.246, cy - H * 0.057, cx - W * 0.231, cy - H * 0.046);
                ctx.strokeStyle = pigAccessible ? '#27272a' : 'rgba(61,38,43,0.72)'; ctx.lineWidth = pigAccessible ? 1.8 : 1.15; ctx.stroke();
                ctx.beginPath(); ctx.arc(cx - W * 0.245, cy - H * 0.048, 1.15, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(61,38,43,0.48)'; ctx.fill();

                // Thin folded pinna with a visible attachment and inner ridge.
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.215, cy - H * 0.09);
                ctx.bezierCurveTo(cx - W * 0.228, cy - H * 0.155, cx - W * 0.187, cy - H * 0.178, cx - W * 0.158, cy - H * 0.125);
                ctx.bezierCurveTo(cx - W * 0.17, cy - H * 0.098, cx - W * 0.19, cy - H * 0.085, cx - W * 0.215, cy - H * 0.09);
                ctx.closePath(); ctx.fillStyle = layerColor; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = 1; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx - W * 0.208, cy - H * 0.103);
                ctx.quadraticCurveTo(cx - W * 0.198, cy - H * 0.143, cx - W * 0.169, cy - H * 0.127);
                ctx.strokeStyle = 'rgba(70,39,45,0.28)'; ctx.lineWidth = 0.9; ctx.stroke();

                // Near-side limbs show different flexion at elbow/knee and hock.
                drawPigLimb(cx - W * 0.125, cy + H * 0.065, cx - W * 0.175, cy + H * 0.16, cx - W * 0.118, cy + H * 0.245, false);
                drawPigLimb(cx + W * 0.125, cy + H * 0.07, cx + W * 0.19, cy + H * 0.165, cx + W * 0.105, cy + H * 0.255, false);

                // Shoulder, flank, jaw, and hock contours add anatomical structure.
                ctx.strokeStyle = 'rgba(75,45,49,' + (pigAccessible ? '0.48' : '0.24') + ')'; ctx.lineWidth = pigAccessible ? 1.4 : 0.8;
                ctx.beginPath(); ctx.arc(cx - W * 0.12, cy - H * 0.012, W * 0.042, -1.1, 1.15); ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx + W * 0.105, cy - H * 0.105); ctx.quadraticCurveTo(cx + W * 0.145, cy - H * 0.01, cx + W * 0.12, cy + H * 0.095); ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx - W * 0.29, cy + H * 0.04); ctx.quadraticCurveTo(cx - W * 0.25, cy + H * 0.062, cx - W * 0.205, cy + H * 0.052); ctx.stroke();

                // Fetal-specific external landmark: a short, softly modeled umbilical stump.
                if (activeLayer === 'skin') {
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.005, cy + H * 0.132);
                  ctx.bezierCurveTo(cx + W * 0.018, cy + H * 0.165, cx + W * 0.012, cy + H * 0.19, cx + W * 0.028, cy + H * 0.207);
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = W * 0.013; ctx.stroke();
                  ctx.strokeStyle = layerColor; ctx.lineWidth = W * 0.009; ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.029, cy + H * 0.21, W * 0.009, H * 0.006, 0.35, 0, Math.PI * 2);
                  ctx.fillStyle = layerStroke; ctx.fill();
                }

                // The fetal tail is a narrow taper with one relaxed curve, not an adult corkscrew.
                ctx.beginPath(); ctx.moveTo(cx + W * 0.218, cy - H * 0.055);
                ctx.bezierCurveTo(cx + W * 0.268, cy - H * 0.08, cx + W * 0.286, cy - H * 0.125, cx + W * 0.26, cy - H * 0.15);
                ctx.strokeStyle = layerStroke; ctx.lineWidth = pigAccessible ? 2.5 : 1.8; ctx.lineCap = 'round'; ctx.stroke();

                if (activeLayer === 'skin') {
                  // Sparse deterministic follicles and soft skin creases avoid plastic flatness.
                  ctx.save(); tracePigBody(); ctx.clip();
                  ctx.globalAlpha = pigAccessible ? 0.1 : (pigRealistic ? 0.22 : 0.14);
                  for (var pigFollicle = 0; pigFollicle < 42; pigFollicle++) {
                    var follicleX = cx - W * 0.15 + ((pigFollicle * 37) % 97) / 96 * W * 0.34;
                    var follicleY = cy - H * 0.105 + ((pigFollicle * 61) % 89) / 88 * H * 0.21;
                    ctx.beginPath(); ctx.moveTo(follicleX, follicleY);
                    ctx.lineTo(follicleX + Math.sin(pigFollicle * 1.7) * 1.2, follicleY - 1.8);
                    ctx.strokeStyle = '#5f4548'; ctx.lineWidth = 0.45; ctx.stroke();
                  }
                  ctx.restore();
                  ctx.globalAlpha = pigAccessible ? 0.3 : 0.16;
                  [ -0.055, 0.002, 0.057 ].forEach(function (foldOffset) {
                    ctx.beginPath(); ctx.moveTo(cx - W * 0.07, cy + H * foldOffset);
                    ctx.quadraticCurveTo(cx + W * 0.005, cy + H * (foldOffset - 0.012), cx + W * 0.075, cy + H * foldOffset);
                    ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.65; ctx.stroke();
                  });
                  ctx.globalAlpha = 1;
                }
                if (activeLayer === 'organs') {
                  // Side-lying visceral cavity with thoracic and abdominal organs registered to the torso.
                  ctx.save(); tracePigBody(); ctx.clip();
                  var pigOrganAlpha = pigAccessible ? 0.95 : 0.84;
                  ctx.globalAlpha = pigOrganAlpha;

                  // Peritoneal cavity opening provides a shared depth field rather than isolated symbols.
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.145, cy - H * 0.092);
                  ctx.bezierCurveTo(cx - W * 0.055, cy - H * 0.135, cx + W * 0.13, cy - H * 0.12, cx + W * 0.188, cy - H * 0.065);
                  ctx.bezierCurveTo(cx + W * 0.215, cy - H * 0.012, cx + W * 0.178, cy + H * 0.105, cx + W * 0.105, cy + H * 0.132);
                  ctx.bezierCurveTo(cx + W * 0.015, cy + H * 0.165, cx - W * 0.115, cy + H * 0.12, cx - W * 0.145, cy + H * 0.055);
                  ctx.bezierCurveTo(cx - W * 0.164, cy + H * 0.005, cx - W * 0.165, cy - H * 0.05, cx - W * 0.145, cy - H * 0.092);
                  ctx.closePath();
                  var pigCavityGrad = ctx.createRadialGradient(cx - W * 0.035, cy - H * 0.045, 4, cx + W * 0.04, cy + H * 0.02, W * 0.25);
                  pigCavityGrad.addColorStop(0, 'rgba(102,55,57,0.72)'); pigCavityGrad.addColorStop(0.62, 'rgba(70,38,43,0.86)'); pigCavityGrad.addColorStop(1, 'rgba(39,25,31,0.92)');
                  ctx.fillStyle = pigCavityGrad; ctx.fill(); ctx.strokeStyle = 'rgba(254,205,211,0.36)'; ctx.lineWidth = 0.85; ctx.stroke();

                  // Dorsal aorta and vena cava remain visible behind the viscera.
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.125, cy - H * 0.082); ctx.bezierCurveTo(cx - W * 0.015, cy - H * 0.105, cx + W * 0.105, cy - H * 0.09, cx + W * 0.18, cy - H * 0.065);
                  ctx.strokeStyle = 'rgba(220,38,38,0.72)'; ctx.lineWidth = 1.6; ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.12, cy - H * 0.068); ctx.bezierCurveTo(cx - W * 0.01, cy - H * 0.078, cx + W * 0.105, cy - H * 0.065, cx + W * 0.18, cy - H * 0.045);
                  ctx.strokeStyle = 'rgba(59,130,246,0.62)'; ctx.lineWidth = 1.2; ctx.stroke();

                  // Large fetal thymus extends from the neck into the anterior mediastinum.
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.155, cy - H * 0.065);
                  ctx.bezierCurveTo(cx - W * 0.135, cy - H * 0.10, cx - W * 0.10, cy - H * 0.095, cx - W * 0.088, cy - H * 0.055);
                  ctx.bezierCurveTo(cx - W * 0.078, cy - H * 0.018, cx - W * 0.11, cy + H * 0.005, cx - W * 0.145, cy - H * 0.012);
                  ctx.closePath(); ctx.fillStyle = '#d9a0a7'; ctx.fill(); ctx.strokeStyle = '#9f5e68'; ctx.lineWidth = 0.55; ctx.stroke();
                  for (var thymusLobe = 0; thymusLobe < 4; thymusLobe++) { ctx.beginPath(); ctx.arc(cx - W * (0.142 - thymusLobe * 0.015), cy - H * (0.055 - (thymusLobe % 2) * 0.018), W * 0.012, 0, Math.PI * 2); ctx.strokeStyle = 'rgba(126,59,71,0.38)'; ctx.lineWidth = 0.4; ctx.stroke(); }

                  // Lobed fetal lungs surround the four-chambered heart.
                  function drawPigLungLobe(x, y, rx, ry, rotation, alpha) {
                    ctx.save(); ctx.globalAlpha = alpha;
                    var lungGrad = ctx.createRadialGradient(x - rx * 0.25, y - ry * 0.25, 1, x, y, Math.max(rx, ry));
                    lungGrad.addColorStop(0, '#f4b7b2'); lungGrad.addColorStop(0.62, '#d98282'); lungGrad.addColorStop(1, '#a85360');
                    ctx.beginPath(); ctx.ellipse(x, y, rx, ry, rotation, 0, Math.PI * 2); ctx.fillStyle = lungGrad; ctx.fill(); ctx.strokeStyle = '#8d4652'; ctx.lineWidth = 0.5; ctx.stroke();
                    ctx.strokeStyle = 'rgba(127,29,29,0.25)'; ctx.lineWidth = 0.35;
                    ctx.beginPath(); ctx.moveTo(x - rx * 0.65, y); ctx.quadraticCurveTo(x, y - ry * 0.22, x + rx * 0.65, y + ry * 0.18); ctx.stroke();
                    ctx.restore();
                  }
                  drawPigLungLobe(cx - W * 0.092, cy - H * 0.025, W * 0.042, H * 0.055, -0.16, 0.78);
                  drawPigLungLobe(cx - W * 0.035, cy - H * 0.035, W * 0.039, H * 0.06, 0.08, 0.88);
                  drawPigLungLobe(cx - W * 0.064, cy + H * 0.018, W * 0.035, H * 0.036, -0.12, 0.84);

                  var pigHeartX = cx - W * 0.058; var pigHeartY = cy - H * 0.012;
                  ctx.beginPath(); ctx.moveTo(pigHeartX, pigHeartY - H * 0.042);
                  ctx.bezierCurveTo(pigHeartX - W * 0.037, pigHeartY - H * 0.05, pigHeartX - W * 0.045, pigHeartY - H * 0.005, pigHeartX - W * 0.018, pigHeartY + H * 0.042);
                  ctx.bezierCurveTo(pigHeartX - W * 0.002, pigHeartY + H * 0.066, pigHeartX + W * 0.037, pigHeartY + H * 0.025, pigHeartX + W * 0.034, pigHeartY - H * 0.012);
                  ctx.bezierCurveTo(pigHeartX + W * 0.03, pigHeartY - H * 0.045, pigHeartX + W * 0.012, pigHeartY - H * 0.048, pigHeartX, pigHeartY - H * 0.042); ctx.closePath();
                  var pigHeartGrad = ctx.createLinearGradient(pigHeartX - W * 0.03, pigHeartY - H * 0.04, pigHeartX + W * 0.03, pigHeartY + H * 0.05); pigHeartGrad.addColorStop(0,'#ca5358');pigHeartGrad.addColorStop(1,'#7f2734');ctx.fillStyle=pigHeartGrad;ctx.fill();ctx.strokeStyle='#631f2b';ctx.lineWidth=0.65;ctx.stroke();
                  ctx.beginPath();ctx.moveTo(pigHeartX,pigHeartY-H*0.033);ctx.lineTo(pigHeartX-W*0.002,pigHeartY+H*0.038);ctx.strokeStyle='rgba(254,202,202,0.42)';ctx.lineWidth=0.55;ctx.stroke();

                  // The diaphragm forms a curved muscular boundary between thorax and abdomen.
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.022, cy - H * 0.095); ctx.bezierCurveTo(cx - W * 0.002, cy - H * 0.02, cx + W * 0.003, cy + H * 0.065, cx - W * 0.005, cy + H * 0.112);
                  ctx.strokeStyle = 'rgba(248,113,113,0.75)'; ctx.lineWidth = 2.4; ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.014, cy - H * 0.09); ctx.bezierCurveTo(cx + W * 0.005, cy - H * 0.015, cx + W * 0.01, cy + H * 0.06, cx + W * 0.002, cy + H * 0.105); ctx.strokeStyle='rgba(254,202,202,0.35)';ctx.lineWidth=0.6;ctx.stroke();

                  // Five liver lobes overlap near the cranial abdomen.
                  var pigLiverLobes = [
                    [0.018,-0.052,0.055,0.044,-0.15],[0.062,-0.038,0.052,0.05,0.10],[0.026,0.005,0.052,0.042,0.12],[0.082,0.012,0.048,0.04,-0.08],[0.108,-0.012,0.034,0.035,0.20]
                  ];
                  pigLiverLobes.forEach(function (lobe, lobeIndex) { ctx.beginPath();ctx.ellipse(cx+W*lobe[0],cy+H*lobe[1],W*lobe[2],H*lobe[3],lobe[4],0,Math.PI*2);ctx.fillStyle=lobeIndex%2?'#71362d':'#7c3d32';ctx.fill();ctx.strokeStyle='#4f2925';ctx.lineWidth=0.45;ctx.stroke(); });

                  // Bean-shaped kidneys stay dorsal and posterior, one partly occluded by bowel.
                  [-1,1].forEach(function (kidneySide) {
                    var kidneyX = cx + W * (0.115 + kidneySide * 0.028); var kidneyY = cy - H * (0.06 - kidneySide * 0.008);
                    ctx.save(); ctx.globalAlpha = kidneySide < 0 ? 0.52 : 0.84;
                    ctx.beginPath(); ctx.ellipse(kidneyX, kidneyY, W * 0.028, H * 0.045, 0.12, 0, Math.PI * 2);ctx.fillStyle='#78463a';ctx.fill();ctx.strokeStyle='#4c2c26';ctx.lineWidth=0.5;ctx.stroke();
                    ctx.beginPath();ctx.arc(kidneyX-W*0.008,kidneyY,W*0.008,-1.2,1.2);ctx.strokeStyle='rgba(248,190,150,0.42)';ctx.lineWidth=0.55;ctx.stroke();ctx.restore();
                  });

                  // Monogastric stomach, spleen, and pancreas share a visible spatial relationship.
                  var pigStomachX = cx + W * 0.062; var pigStomachY = cy + H * 0.038;
                  ctx.beginPath();ctx.moveTo(pigStomachX-W*0.025,pigStomachY-H*0.035);ctx.bezierCurveTo(pigStomachX+W*0.045,pigStomachY-H*0.04,pigStomachX+W*0.055,pigStomachY+H*0.02,pigStomachX+W*0.015,pigStomachY+H*0.052);ctx.bezierCurveTo(pigStomachX-W*0.02,pigStomachY+H*0.075,pigStomachX-W*0.055,pigStomachY+H*0.025,pigStomachX-W*0.025,pigStomachY-H*0.035);ctx.closePath();var pigStomachGrad=ctx.createLinearGradient(pigStomachX,pigStomachY-H*0.04,pigStomachX,pigStomachY+H*0.06);pigStomachGrad.addColorStop(0,'#e7c18f');pigStomachGrad.addColorStop(1,'#b77a52');ctx.fillStyle=pigStomachGrad;ctx.fill();ctx.strokeStyle='#835236';ctx.lineWidth=0.55;ctx.stroke();
                  ctx.beginPath();ctx.ellipse(pigStomachX-W*0.045,pigStomachY+H*0.018,W*0.011,H*0.05,-0.35,0,Math.PI*2);ctx.fillStyle='#762c38';ctx.fill();ctx.strokeStyle='#4c1d2a';ctx.lineWidth=0.45;ctx.stroke();
                  ctx.beginPath();ctx.moveTo(pigStomachX-W*0.008,pigStomachY+H*0.048);ctx.bezierCurveTo(pigStomachX+W*0.035,pigStomachY+H*0.06,pigStomachX+W*0.065,pigStomachY+H*0.035,pigStomachX+W*0.09,pigStomachY+H*0.05);ctx.strokeStyle='#eabf6a';ctx.lineWidth=2.1;ctx.stroke();

                  // Jejunal loops sit ventrally while the porcine spiral colon forms a watch-spring coil.
                  ctx.strokeStyle='#d88a57';ctx.lineWidth=3.8;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(cx+W*0.105,cy+H*0.072);ctx.bezierCurveTo(cx+W*0.14,cy+H*0.09,cx+W*0.065,cy+H*0.11,cx+W*0.105,cy+H*0.125);ctx.bezierCurveTo(cx+W*0.155,cy+H*0.145,cx+W*0.085,cy+H*0.155,cx+W*0.055,cy+H*0.125);ctx.bezierCurveTo(cx+W*0.025,cy+H*0.095,cx+W*0.075,cy+H*0.075,cx+W*0.12,cy+H*0.09);ctx.stroke();
                  var colonCx = cx + W * 0.155; var colonCy = cy + H * 0.075;
                  ctx.strokeStyle='#a95f43';ctx.lineWidth=3.4;ctx.beginPath();
                  for(var colonStep=0;colonStep<=54;colonStep++){var colonT=colonStep/54*Math.PI*4.7;var colonRadius=W*(0.041-colonStep/54*0.034);var colonX=colonCx+Math.cos(colonT)*colonRadius;var colonY=colonCy+Math.sin(colonT)*colonRadius*0.58;if(colonStep===0)ctx.moveTo(colonX,colonY);else ctx.lineTo(colonX,colonY);}ctx.stroke();

                  // Urinary bladder and urachus connect directly toward the umbilical stump.
                  var pigBladderX = cx + W * 0.18; var pigBladderY = cy + H * 0.085;
                  ctx.save();ctx.globalAlpha=0.62;ctx.beginPath();ctx.ellipse(pigBladderX,pigBladderY,W*0.028,H*0.038,0.18,0,Math.PI*2);ctx.fillStyle='#bae6fd';ctx.fill();ctx.strokeStyle='#60a5fa';ctx.lineWidth=0.55;ctx.stroke();ctx.restore();
                  ctx.beginPath();ctx.moveTo(pigBladderX-W*0.008,pigBladderY+H*0.028);ctx.bezierCurveTo(cx+W*0.13,cy+H*0.13,cx+W*0.07,cy+H*0.155,cx+W*0.025,cy+H*0.19);ctx.strokeStyle='#e2e8f0';ctx.lineWidth=1.1;ctx.stroke();

                  // Umbilical vein courses to the liver; paired arteries return alongside the bladder.
                  ctx.beginPath();ctx.moveTo(cx+W*0.025,cy+H*0.19);ctx.bezierCurveTo(cx+W*0.04,cy+H*0.12,cx+W*0.04,cy+H*0.055,cx+W*0.055,cy+H*0.012);ctx.strokeStyle='#a855f7';ctx.lineWidth=2.2;ctx.stroke();
                  [-1,1].forEach(function (arterySide) {ctx.beginPath();ctx.moveTo(cx+W*0.025,cy+H*0.19);ctx.quadraticCurveTo(cx+W*(0.10+arterySide*0.012),cy+H*0.13,pigBladderX+arterySide*W*0.018,pigBladderY);ctx.strokeStyle='#dc5265';ctx.lineWidth=0.9;ctx.stroke();});

                  if (!d.quizMode) {
                    ctx.font='6px Inter, system-ui';ctx.fillStyle='rgba(254,226,226,0.58)';ctx.fillText('thorax',cx-W*0.135,cy-H*0.105);ctx.fillText('abdomen',cx+W*0.035,cy-H*0.105);
                  }
                  ctx.restore();
                }
                if (activeLayer === 'skeleton') {

                  // Use higher opacity and darker colors for visibility

                  ctx.globalAlpha = 0.85;

                  var boneColor = '#94a3b8';

                  var boneFill = 'rgba(148,163,184,0.35)';

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 1.5;

    // €â€ Draw organ pins â€â€”€â”€ Skull ──

                  ctx.beginPath(); ctx.ellipse(cx - W * 0.24, cy - H * 0.02, W * 0.07, H * 0.06, -0.2, 0, Math.PI * 2);

                  ctx.fillStyle = boneFill; ctx.fill(); ctx.stroke();

                  // Eye socket

                  ctx.beginPath(); ctx.arc(cx - W * 0.24, cy - H * 0.05, W * 0.015, 0, Math.PI * 2);

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 0.8; ctx.stroke();

                  // Jaw

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.27, cy + H * 0.02);

                  ctx.quadraticCurveTo(cx - W * 0.30, cy + H * 0.01, cx - W * 0.30, cy - H * 0.01);

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 1.2; ctx.stroke();

    // €â€ Draw organ pins â€â€”€â”€ Spine (vertebral column) ──

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.17, cy - H * 0.02);

                  ctx.lineTo(cx + W * 0.20, cy - H * 0.02);

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 4; ctx.stroke();

                  // Vertebrae marks + neural arches

                  for (var pv = 0; pv < 18; pv++) {

                    var pvx = cx - W * 0.15 + pv * (W * 0.34 / 18);

                    ctx.beginPath(); ctx.moveTo(pvx, cy - H * 0.05); ctx.lineTo(pvx, cy + H * 0.01);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 0.8; ctx.stroke();

                    // Spinous process (dorsal bump)

                    ctx.beginPath(); ctx.arc(pvx, cy - H * 0.055, 1.8, 0, Math.PI * 2);

                    ctx.fillStyle = boneFill; ctx.fill();

                  }

    // €â€ Draw organ pins â€â€”€â”€ Ribs (extending from vertebrae down to sternum) ──

                  for (var pr = 0; pr < 8; pr++) {

                    var prx = cx - W * 0.12 + pr * (W * 0.18 / 8);

                    ctx.beginPath();

                    ctx.moveTo(prx, cy - H * 0.04);

                    ctx.bezierCurveTo(prx - W * 0.015, cy + H * 0.02, prx - W * 0.01, cy + H * 0.06, prx + W * 0.005, cy + H * 0.09);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 1; ctx.stroke();

                  }

    // €â€ Draw organ pins â€â€”€â”€ Sternum (connecting rib tips ventrally) ──

                  ctx.beginPath();

                  ctx.moveTo(cx - W * 0.10, cy + H * 0.09);

                  ctx.lineTo(cx + W * 0.06, cy + H * 0.09);

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 2.5; ctx.stroke();

                  // Sternebrae

                  for (var stb = 0; stb < 6; stb++) {

                    var stbx = cx - W * 0.09 + stb * (W * 0.15 / 6);

                    ctx.beginPath(); ctx.arc(stbx, cy + H * 0.09, 2.5, 0, Math.PI * 2);

                    ctx.fillStyle = boneFill; ctx.fill();

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 0.6; ctx.stroke();

                  }

    // €â€ Draw organ pins â€â€”€â”€ Scapula (shoulder blade — inside body outline) ──

                  ctx.beginPath();

                  ctx.moveTo(cx - W * 0.16, cy - H * 0.04);

                  ctx.lineTo(cx - W * 0.19, cy - H * 0.11);

                  ctx.lineTo(cx - W * 0.12, cy - H * 0.09);

                  ctx.closePath();

                  ctx.fillStyle = boneFill; ctx.fill();

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 1.5; ctx.stroke();

                  // Scapular spine

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.17, cy - H * 0.10);

                  ctx.lineTo(cx - W * 0.14, cy - H * 0.06);

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 1; ctx.stroke();

    // €â€ Draw organ pins â€â€”€â”€ Pelvis (ilium-ischium-pubis, inside body) ──

                  ctx.beginPath();

                  ctx.moveTo(cx + W * 0.12, cy - H * 0.04);

                  ctx.bezierCurveTo(cx + W * 0.16, cy - H * 0.08, cx + W * 0.20, cy - H * 0.04, cx + W * 0.18, cy);

                  ctx.bezierCurveTo(cx + W * 0.20, cy + H * 0.04, cx + W * 0.16, cy + H * 0.08, cx + W * 0.12, cy + H * 0.04);

                  ctx.closePath();

                  ctx.fillStyle = boneFill; ctx.fill();

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 1.5; ctx.stroke();

                  // Acetabulum (hip socket)

                  ctx.beginPath(); ctx.arc(cx + W * 0.14, cy + H * 0.01, W * 0.012, 0, Math.PI * 2);

                  ctx.strokeStyle = boneColor; ctx.lineWidth = 0.8; ctx.stroke();

                  // Obturator foramen

                  ctx.beginPath(); ctx.ellipse(cx + W * 0.15, cy + H * 0.03, W * 0.008, H * 0.015, 0, 0, Math.PI * 2);

                  ctx.strokeStyle = 'rgba(200,214,229,0.4)'; ctx.lineWidth = 0.6; ctx.stroke();

                  ctx.strokeStyle = boneColor;

    // €â€ Draw organ pins â€â€”€â”€ Forelimb bones ──

                  // Humerus + radius/ulna for each front leg

                  [[-0.135, -0.005], [-0.105, 0.005]].forEach(function (lp) {

                    var legX = cx + W * lp[0];

                    // Humerus (upper)

                    ctx.beginPath(); ctx.moveTo(legX, cy + H * 0.12);

                    ctx.lineTo(legX - W * 0.005, cy + H * 0.20);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 2.5; ctx.stroke();

                    // Elbow joint

                    ctx.beginPath(); ctx.arc(legX - W * 0.005, cy + H * 0.20, 3, 0, Math.PI * 2);

                    ctx.fillStyle = boneFill; ctx.fill(); ctx.strokeStyle = boneColor; ctx.lineWidth = 0.8; ctx.stroke();

                    // Radius/ulna (lower)

                    ctx.beginPath(); ctx.moveTo(legX - W * 0.005, cy + H * 0.20);

                    ctx.lineTo(legX, cy + H * 0.29);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 2; ctx.stroke();

                  });

    // €â€ Draw organ pins â€â€”€â”€ Hindlimb bones ──

                  // Femur + tibia/fibula for each rear leg

                  [[0.115, -0.005], [0.145, 0.005]].forEach(function (lp) {

                    var legX = cx + W * lp[0];

                    // Femur (upper)

                    ctx.beginPath(); ctx.moveTo(legX, cy + H * 0.12);

                    ctx.lineTo(legX + W * 0.005, cy + H * 0.20);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 2.5; ctx.stroke();

                    // Knee joint

                    ctx.beginPath(); ctx.arc(legX + W * 0.005, cy + H * 0.20, 3, 0, Math.PI * 2);

                    ctx.fillStyle = boneFill; ctx.fill(); ctx.strokeStyle = boneColor; ctx.lineWidth = 0.8; ctx.stroke();

                    // Tibia/fibula (lower)

                    ctx.beginPath(); ctx.moveTo(legX + W * 0.005, cy + H * 0.20);

                    ctx.lineTo(legX, cy + H * 0.29);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 2; ctx.stroke();

                  });

    // €â€ Draw organ pins â€â€”€â”€ Hoof bones (distal phalanges) ──

                  [cx - W * 0.135, cx - W * 0.105, cx + W * 0.115, cx + W * 0.145].forEach(function (hx) {

                    ctx.beginPath();

                    ctx.moveTo(hx - W * 0.008, cy + H * 0.30);

                    ctx.lineTo(hx - W * 0.01, cy + H * 0.32);

                    ctx.lineTo(hx, cy + H * 0.31);

                    ctx.lineTo(hx + W * 0.01, cy + H * 0.32);

                    ctx.lineTo(hx + W * 0.008, cy + H * 0.30);

                    ctx.strokeStyle = boneColor; ctx.lineWidth = 1; ctx.stroke();

                  });

                  ctx.globalAlpha = 1;

                }





              } else if (spec.bodyShape === 'fish') {

                var fishAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var fishRealistic = (d.visualRealism || visualRealism) === 'realistic';
                var fishBodyGradient = ctx.createLinearGradient(cx, cy - H * 0.15, cx, cy + H * 0.14);
                fishBodyGradient.addColorStop(0, fishAccessible ? layerColor : 'rgba(240,220,132,0.96)');
                fishBodyGradient.addColorStop(0.34, fishAccessible ? layerColor : 'rgba(176,166,78,0.98)');
                fishBodyGradient.addColorStop(0.62, fishAccessible ? layerColor : 'rgba(116,121,66,0.98)');
                fishBodyGradient.addColorStop(1, fishAccessible ? layerColor : 'rgba(78,88,54,0.98)');

                // ══ PERCH (Perca) — fusiform bony fish ══

                // ======== FINS (drawn first so body covers fin bases) ========

                // ── Spiny dorsal fin (behind body) ──
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.10, cy - H * 0.14);
                // Fin rises from dorsal contour — base merges INTO body
                ctx.bezierCurveTo(cx - W * 0.08, cy - H * 0.22, cx - W * 0.01, cy - H * 0.26, cx + W * 0.03, cy - H * 0.24);
                ctx.bezierCurveTo(cx + W * 0.06, cy - H * 0.22, cx + W * 0.08, cy - H * 0.19, cx + W * 0.10, cy - H * 0.13);
                // Return path dips INTO body so body covers it
                ctx.lineTo(cx + W * 0.10, cy - H * 0.10);
                ctx.lineTo(cx - W * 0.10, cy - H * 0.11);
                ctx.closePath();
                ctx.fillStyle = layerColor; ctx.globalAlpha = 0.45; ctx.fill();
                // Only stroke the OUTER edge (not the base that body will cover)
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.10, cy - H * 0.14);
                ctx.bezierCurveTo(cx - W * 0.08, cy - H * 0.22, cx - W * 0.01, cy - H * 0.26, cx + W * 0.03, cy - H * 0.24);
                ctx.bezierCurveTo(cx + W * 0.06, cy - H * 0.22, cx + W * 0.08, cy - H * 0.19, cx + W * 0.10, cy - H * 0.13);
                ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.8; ctx.stroke(); ctx.globalAlpha = 1;
                // Spine rays
                ctx.globalAlpha = 0.35;
                for (var ds = 0; ds < 9; ds++) {
                  var dsP = ds / 8;
                  var dsX = cx - W * 0.10 + dsP * W * 0.20;
                  var dsBaseY = cy - H * 0.14;
                  var dsH = Math.sin(dsP * Math.PI) * H * 0.12 + H * 0.03;
                  ctx.beginPath(); ctx.moveTo(dsX, dsBaseY); ctx.lineTo(dsX, dsBaseY - dsH);
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.5; ctx.stroke();
                }
                ctx.globalAlpha = 1;

                // ── Soft dorsal fin (behind body) ──
                ctx.beginPath();
                ctx.moveTo(cx + W * 0.14, cy - H * 0.11);
                ctx.bezierCurveTo(cx + W * 0.15, cy - H * 0.17, cx + W * 0.18, cy - H * 0.18, cx + W * 0.21, cy - H * 0.15);
                ctx.bezierCurveTo(cx + W * 0.22, cy - H * 0.13, cx + W * 0.22, cy - H * 0.10, cx + W * 0.22, cy - H * 0.09);
                ctx.lineTo(cx + W * 0.22, cy - H * 0.06);
                ctx.lineTo(cx + W * 0.14, cy - H * 0.08);
                ctx.closePath();
                ctx.fillStyle = layerColor; ctx.globalAlpha = 0.40; ctx.fill();
                ctx.beginPath();
                ctx.moveTo(cx + W * 0.14, cy - H * 0.11);
                ctx.bezierCurveTo(cx + W * 0.15, cy - H * 0.17, cx + W * 0.18, cy - H * 0.18, cx + W * 0.21, cy - H * 0.15);
                ctx.bezierCurveTo(cx + W * 0.22, cy - H * 0.13, cx + W * 0.22, cy - H * 0.10, cx + W * 0.22, cy - H * 0.09);
                ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.7; ctx.stroke(); ctx.globalAlpha = 1;
                // Soft rays
                ctx.globalAlpha = 0.25;
                for (var sd = 0; sd < 5; sd++) {
                  var sdP = sd / 4;
                  var sdX = cx + W * (0.14 + sdP * 0.08);
                  ctx.beginPath(); ctx.moveTo(sdX, cy - H * 0.10);
                  ctx.lineTo(sdX, cy - H * (0.10 + Math.sin(sdP * Math.PI) * 0.07 + 0.02));
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.35; ctx.stroke();
                }
                ctx.globalAlpha = 1;

                // ── Anal fin (behind body) ──
                ctx.beginPath();
                ctx.moveTo(cx + W * 0.12, cy + H * 0.11);
                ctx.bezierCurveTo(cx + W * 0.13, cy + H * 0.17, cx + W * 0.16, cy + H * 0.18, cx + W * 0.18, cy + H * 0.14);
                ctx.lineTo(cx + W * 0.18, cy + H * 0.08);
                ctx.lineTo(cx + W * 0.12, cy + H * 0.08);
                ctx.closePath();
                ctx.fillStyle = layerColor; ctx.globalAlpha = 0.40; ctx.fill();
                ctx.beginPath();
                ctx.moveTo(cx + W * 0.12, cy + H * 0.11);
                ctx.bezierCurveTo(cx + W * 0.13, cy + H * 0.17, cx + W * 0.16, cy + H * 0.18, cx + W * 0.18, cy + H * 0.14);
                ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.7; ctx.stroke(); ctx.globalAlpha = 1;

                // ── Pelvic fin (behind body) ──
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.06, cy + H * 0.12);
                ctx.bezierCurveTo(cx - W * 0.08, cy + H * 0.16, cx - W * 0.10, cy + H * 0.19, cx - W * 0.08, cy + H * 0.20);
                ctx.bezierCurveTo(cx - W * 0.05, cy + H * 0.19, cx - W * 0.03, cy + H * 0.16, cx - W * 0.02, cy + H * 0.13);
                ctx.lineTo(cx - W * 0.02, cy + H * 0.10);
                ctx.lineTo(cx - W * 0.06, cy + H * 0.10);
                ctx.closePath();
                ctx.fillStyle = layerColor; ctx.globalAlpha = 0.35; ctx.fill();
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.06, cy + H * 0.12);
                ctx.bezierCurveTo(cx - W * 0.08, cy + H * 0.16, cx - W * 0.10, cy + H * 0.19, cx - W * 0.08, cy + H * 0.20);
                ctx.bezierCurveTo(cx - W * 0.05, cy + H * 0.19, cx - W * 0.03, cy + H * 0.16, cx - W * 0.02, cy + H * 0.13);
                ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.6; ctx.stroke(); ctx.globalAlpha = 1;

                // ── Pectoral fin (behind body) ──
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.16, cy + H * 0.02);
                ctx.bezierCurveTo(cx - W * 0.19, cy + H * 0.06, cx - W * 0.20, cy + H * 0.12, cx - W * 0.17, cy + H * 0.14);
                ctx.bezierCurveTo(cx - W * 0.14, cy + H * 0.12, cx - W * 0.13, cy + H * 0.08, cx - W * 0.14, cy + H * 0.04);
                ctx.lineTo(cx - W * 0.14, cy + H * 0.00);
                ctx.lineTo(cx - W * 0.16, cy + H * 0.00);
                ctx.closePath();
                ctx.fillStyle = layerColor; ctx.globalAlpha = 0.35; ctx.fill();
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.16, cy + H * 0.02);
                ctx.bezierCurveTo(cx - W * 0.19, cy + H * 0.06, cx - W * 0.20, cy + H * 0.12, cx - W * 0.17, cy + H * 0.14);
                ctx.bezierCurveTo(cx - W * 0.14, cy + H * 0.12, cx - W * 0.13, cy + H * 0.08, cx - W * 0.14, cy + H * 0.04);
                ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.6; ctx.stroke(); ctx.globalAlpha = 1;

                // ======== BODY (drawn on top, covers fin bases) ========

                // ── Body (streamlined fusiform shape) ──

                ctx.beginPath();

                ctx.moveTo(cx - W * 0.30, cy); // snout tip

                ctx.bezierCurveTo(cx - W * 0.25, cy - H * 0.08, cx - W * 0.18, cy - H * 0.13, cx - W * 0.10, cy - H * 0.14);

                ctx.bezierCurveTo(cx - W * 0.02, cy - H * 0.15, cx + W * 0.08, cy - H * 0.13, cx + W * 0.16, cy - H * 0.10);

                ctx.bezierCurveTo(cx + W * 0.22, cy - H * 0.08, cx + W * 0.25, cy - H * 0.06, cx + W * 0.26, cy - H * 0.04);

                // Caudal peduncle

                ctx.lineTo(cx + W * 0.28, cy - H * 0.03);

                // Caudal fin (forked)

                ctx.lineTo(cx + W * 0.34, cy - H * 0.12);

                ctx.bezierCurveTo(cx + W * 0.33, cy - H * 0.06, cx + W * 0.33, cy + H * 0.06, cx + W * 0.34, cy + H * 0.12);

                ctx.lineTo(cx + W * 0.28, cy + H * 0.03);

                // Ventral contour

                ctx.lineTo(cx + W * 0.26, cy + H * 0.04);

                ctx.bezierCurveTo(cx + W * 0.22, cy + H * 0.08, cx + W * 0.16, cy + H * 0.10, cx + W * 0.08, cy + H * 0.12);

                ctx.bezierCurveTo(cx - W * 0.02, cy + H * 0.14, cx - W * 0.12, cy + H * 0.14, cx - W * 0.20, cy + H * 0.10);

                ctx.bezierCurveTo(cx - W * 0.25, cy + H * 0.07, cx - W * 0.28, cy + H * 0.04, cx - W * 0.30, cy);

                ctx.closePath();

                ctx.fillStyle = fishBodyGradient; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = fishRealistic ? 1.15 : 1.5; ctx.stroke();

                if (!fishAccessible) {
                  ctx.save();
                  ctx.globalAlpha = fishRealistic ? 0.18 : 0.11;
                  var fishIridescence = ctx.createLinearGradient(cx - W * 0.18, cy - H * 0.07, cx + W * 0.20, cy + H * 0.07);
                  fishIridescence.addColorStop(0, 'rgba(226,232,240,0)');
                  fishIridescence.addColorStop(0.42, 'rgba(224,242,254,0.82)');
                  fishIridescence.addColorStop(0.62, 'rgba(254,249,195,0.54)');
                  fishIridescence.addColorStop(1, 'rgba(226,232,240,0)');
                  ctx.fillStyle = fishIridescence;
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.01, cy - H * 0.025, W * 0.23, H * 0.075, -0.04, 0, Math.PI * 2); ctx.fill();
                  ctx.restore();
                }

                ctx.shadowBlur = 0;

                // Dorsal highlight

                ctx.beginPath();

                ctx.moveTo(cx - W * 0.20, cy - H * 0.08);

                ctx.quadraticCurveTo(cx, cy - H * 0.12, cx + W * 0.18, cy - H * 0.06);

                ctx.strokeStyle = 'rgba(255,255,255,0.08)'; ctx.lineWidth = 3; ctx.stroke();

                // ── Operculum (gill cover) ──

                ctx.beginPath();

                ctx.moveTo(cx - W * 0.18, cy - H * 0.10);

                ctx.bezierCurveTo(cx - W * 0.16, cy - H * 0.04, cx - W * 0.16, cy + H * 0.04, cx - W * 0.18, cy + H * 0.08);

                ctx.strokeStyle = layerStroke; ctx.lineWidth = 1.2; ctx.stroke();

                // ── Eye ──

                var fishEyeX = cx - W * 0.23;
                var fishEyeY = cy - H * 0.02;
                var fishEyeGradient = ctx.createRadialGradient(fishEyeX - 1.5, fishEyeY - 1.4, 0.8, fishEyeX, fishEyeY, 6);
                fishEyeGradient.addColorStop(0, 'rgba(226,232,240,0.72)');
                fishEyeGradient.addColorStop(0.24, 'rgba(148,136,66,0.96)');
                fishEyeGradient.addColorStop(0.62, 'rgba(55,48,24,0.98)');
                fishEyeGradient.addColorStop(1, 'rgba(15,23,42,0.96)');
                ctx.beginPath(); ctx.ellipse(fishEyeX, fishEyeY, 6.2, 5.6, -0.1, 0, Math.PI * 2);
                ctx.fillStyle = fishEyeGradient; ctx.fill(); ctx.strokeStyle = 'rgba(15,23,42,0.82)'; ctx.lineWidth = 0.9; ctx.stroke();
                ctx.beginPath(); ctx.ellipse(fishEyeX + 0.3, fishEyeY, 2.35, 2.75, -0.1, 0, Math.PI * 2);
                ctx.fillStyle = '#05070a'; ctx.fill();
                if (!fishAccessible) {
                  ctx.beginPath(); ctx.ellipse(fishEyeX - 1.6, fishEyeY - 1.5, 1.25, 0.58, -0.35, 0, Math.PI * 2);
                  ctx.fillStyle = 'rgba(226,232,240,0.35)'; ctx.fill();
                }

                // ── Mouth ──

                ctx.beginPath(); ctx.moveTo(cx - W * 0.30, cy); ctx.lineTo(cx - W * 0.28, cy + H * 0.02);

                ctx.strokeStyle = layerStroke; ctx.lineWidth = 1; ctx.stroke();


  // €â€ Draw organ pins â€â€”€â”€ Scale pattern ──

                ctx.globalAlpha = 0.12;

                for (var sc = 0; sc < 12; sc++) {

                  for (var sr = 0; sr < 4; sr++) {

                    var scx = cx - W * 0.15 + sc * W * 0.035 + (sr % 2) * W * 0.017;

                    var scy = cy - H * 0.08 + sr * H * 0.04;

                    ctx.beginPath(); ctx.arc(scx, scy, W * 0.012, 0, Math.PI, true);

                    ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.4; ctx.stroke();

                  }

                }

                ctx.globalAlpha = 1;

  // €â€ Draw organ pins â€â€”€â”€ Caudal fin rays ──

                ctx.globalAlpha = 0.25;

                for (var cfr = 0; cfr < 7; cfr++) {

                  ctx.beginPath(); ctx.moveTo(cx + W * 0.28, cy);

                  ctx.lineTo(cx + W * 0.34, cy - H * 0.10 + cfr * H * 0.03);

                  ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.4; ctx.stroke();

                }

                ctx.globalAlpha = 1;

  // €â€ Draw organ pins â€â€”€â”€ Lateral line ──

                ctx.globalAlpha = 0.3;

                for (var ll = 0; ll < 18; ll++) {

                  ctx.beginPath(); ctx.arc(cx - W * 0.22 + ll * W * 0.025, cy - H * 0.01, 1, 0, Math.PI * 2);

                  ctx.fillStyle = layerStroke; ctx.fill();

                }

                ctx.globalAlpha = 1;

  // €â€ Draw organ pins â€â€”€â”€ Perch vertical bars ──

                ctx.globalAlpha = 0.08;

                for (var pb = 0; pb < 6; pb++) {

                  var pbx = cx - W * 0.10 + pb * W * 0.06;

                  ctx.beginPath(); ctx.moveTo(pbx, cy - H * 0.10); ctx.lineTo(pbx, cy + H * 0.08);

                  ctx.strokeStyle = '#1a1a1a'; ctx.lineWidth = W * 0.015; ctx.stroke();

                }

                ctx.globalAlpha = 1;

  // €â€ Draw organ pins â€â€”€â”€ Layer overlays ──

                if (activeLayer === 'organs') {
                  // Perch internal cutaway: dorsal buoyancy/excretory organs and ventral viscera share one coelomic cavity.
                  var perchOrganAlpha = fishAccessible ? 0.98 : (fishRealistic ? 0.93 : 0.88);
                  var swimBladderPulse = dissMotionReduced ? 0 : Math.sin(dissTick * 0.022) * H * 0.0018;

                  function tracePerchCoelom() {
                    ctx.beginPath();
                    ctx.moveTo(cx - W * 0.205, cy - H * 0.075);
                    ctx.bezierCurveTo(cx - W * 0.13, cy - H * 0.115, cx + W * 0.055, cy - H * 0.112, cx + W * 0.205, cy - H * 0.067);
                    ctx.bezierCurveTo(cx + W * 0.225, cy - H * 0.018, cx + W * 0.19, cy + H * 0.087, cx + W * 0.105, cy + H * 0.102);
                    ctx.bezierCurveTo(cx - W * 0.015, cy + H * 0.125, cx - W * 0.155, cy + H * 0.092, cx - W * 0.205, cy + H * 0.045);
                    ctx.bezierCurveTo(cx - W * 0.225, cy + H * 0.006, cx - W * 0.222, cy - H * 0.042, cx - W * 0.205, cy - H * 0.075);
                    ctx.closePath();
                  }

                  function drawPerchGillArch(archIndex) {
                    var archX = cx - W * (0.205 - archIndex * 0.011);
                    var archTopY = cy - H * (0.060 - archIndex * 0.003);
                    var archBottomY = cy + H * (0.045 - archIndex * 0.002);
                    ctx.beginPath();
                    ctx.moveTo(archX, archTopY);
                    ctx.bezierCurveTo(archX - W * 0.018, cy - H * 0.018, archX - W * 0.018, cy + H * 0.018, archX, archBottomY);
                    ctx.strokeStyle = fishAccessible ? '#dc2626' : 'rgba(153,27,27,0.96)';
                    ctx.lineWidth = 2.4 - archIndex * 0.18; ctx.stroke();
                    for (var filamentIndex = 0; filamentIndex < 7; filamentIndex++) {
                      var filamentT = (filamentIndex + 1) / 8;
                      var filamentY = archTopY + (archBottomY - archTopY) * filamentT;
                      var filamentReach = W * (0.018 + Math.sin(filamentT * Math.PI) * 0.010);
                      ctx.beginPath(); ctx.moveTo(archX - W * 0.007, filamentY);
                      ctx.lineTo(archX - filamentReach, filamentY - H * 0.004);
                      ctx.strokeStyle = fishAccessible ? '#fb7185' : 'rgba(248,113,113,0.86)';
                      ctx.lineWidth = 0.9; ctx.stroke();
                    }
                  }

                  ctx.save();
                  tracePerchCoelom();
                  var perchCavityGradient = ctx.createLinearGradient(cx, cy - H * 0.11, cx, cy + H * 0.11);
                  perchCavityGradient.addColorStop(0, fishAccessible ? 'rgba(30,41,59,0.96)' : 'rgba(74,45,42,0.90)');
                  perchCavityGradient.addColorStop(0.52, fishAccessible ? 'rgba(51,65,85,0.96)' : 'rgba(91,55,45,0.92)');
                  perchCavityGradient.addColorStop(1, fishAccessible ? 'rgba(71,85,105,0.96)' : 'rgba(61,38,34,0.94)');
                  ctx.fillStyle = perchCavityGradient; ctx.globalAlpha = perchOrganAlpha; ctx.fill();
                  ctx.strokeStyle = fishAccessible ? '#fbbf24' : 'rgba(253,230,138,0.62)';
                  ctx.lineWidth = fishAccessible ? 1.8 : 1.15; ctx.stroke();
                  tracePerchCoelom(); ctx.clip(); ctx.globalAlpha = 1;

                  // A faint dorsal peritoneal reflection helps the cutaway read as depth rather than a flat diagram.
                  var perchCavitySheen = ctx.createLinearGradient(cx, cy - H * 0.11, cx, cy + H * 0.02);
                  perchCavitySheen.addColorStop(0, 'rgba(226,232,240,0.14)');
                  perchCavitySheen.addColorStop(1, 'rgba(226,232,240,0)');
                  ctx.fillStyle = perchCavitySheen; ctx.fillRect(cx - W * 0.22, cy - H * 0.12, W * 0.46, H * 0.15);

                  // The kidney is a dark, elongated organ fixed against the dorsal body wall.
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.145, cy - H * 0.088);
                  ctx.bezierCurveTo(cx - W * 0.055, cy - H * 0.106, cx + W * 0.105, cy - H * 0.100, cx + W * 0.190, cy - H * 0.072);
                  ctx.bezierCurveTo(cx + W * 0.115, cy - H * 0.069, cx - W * 0.045, cy - H * 0.071, cx - W * 0.145, cy - H * 0.061);
                  ctx.closePath();
                  var perchKidneyGradient = ctx.createLinearGradient(cx, cy - H * 0.102, cx, cy - H * 0.060);
                  perchKidneyGradient.addColorStop(0, fishAccessible ? '#7f1d1d' : 'rgba(69,26,31,0.98)');
                  perchKidneyGradient.addColorStop(1, fishAccessible ? '#991b1b' : 'rgba(103,35,39,0.94)');
                  ctx.fillStyle = perchKidneyGradient; ctx.fill();
                  ctx.strokeStyle = 'rgba(254,202,202,0.30)'; ctx.lineWidth = 0.7; ctx.stroke();

                  // Dorsal aorta follows the roof of the cavity; the ventral aorta feeds the gill arches.
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.175, cy - H * 0.058); ctx.bezierCurveTo(cx - W * 0.02, cy - H * 0.082, cx + W * 0.11, cy - H * 0.074, cx + W * 0.205, cy - H * 0.052);
                  ctx.strokeStyle = fishAccessible ? '#ef4444' : 'rgba(239,68,68,0.72)'; ctx.lineWidth = 1.15; ctx.stroke();

                  // Swim bladder: a thin-walled dorsal sac with a vascular rete mirabile patch.
                  ctx.beginPath();
                  ctx.ellipse(cx + W * 0.015, cy - H * 0.043, W * 0.120, H * 0.032 + swimBladderPulse, -0.025, 0, Math.PI * 2);
                  var perchBladderGradient = ctx.createRadialGradient(cx - W * 0.025, cy - H * 0.058, 2, cx + W * 0.015, cy - H * 0.043, W * 0.125);
                  perchBladderGradient.addColorStop(0, fishAccessible ? 'rgba(255,255,255,0.96)' : 'rgba(239,246,255,0.90)');
                  perchBladderGradient.addColorStop(0.56, fishAccessible ? 'rgba(186,230,253,0.90)' : 'rgba(191,219,254,0.63)');
                  perchBladderGradient.addColorStop(1, fishAccessible ? 'rgba(56,189,248,0.72)' : 'rgba(96,165,250,0.32)');
                  ctx.fillStyle = perchBladderGradient; ctx.fill();
                  ctx.strokeStyle = fishAccessible ? '#38bdf8' : 'rgba(147,197,253,0.82)'; ctx.lineWidth = 1.1; ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.022, cy - H * 0.057, W * 0.052, H * 0.009, -0.04, Math.PI * 1.08, Math.PI * 1.88);
                  ctx.strokeStyle = 'rgba(255,255,255,0.52)'; ctx.lineWidth = 1; ctx.stroke();

                  // Rete mirabile is represented as a compact vascular network along the anterior bladder wall.
                  for (var reteIndex = 0; reteIndex < 7; reteIndex++) {
                    var reteAngle = reteIndex * Math.PI * 2 / 7;
                    var reteX = cx - W * 0.080 + Math.cos(reteAngle) * W * 0.012;
                    var reteY = cy - H * 0.035 + Math.sin(reteAngle) * H * 0.010;
                    ctx.beginPath(); ctx.moveTo(cx - W * 0.080, cy - H * 0.035); ctx.lineTo(reteX, reteY);
                    ctx.strokeStyle = reteIndex % 2 ? '#60a5fa' : '#f87171'; ctx.lineWidth = 0.75; ctx.stroke();
                  }

                  // Paired gonads sit between the swim bladder and gut and taper posteriorly.
                  [-1, 1].forEach(function (gonadSide) {
                    ctx.beginPath();
                    ctx.ellipse(cx + W * 0.095, cy + H * (0.007 + gonadSide * 0.014), W * 0.090, H * 0.014, gonadSide * 0.035, 0, Math.PI * 2);
                    ctx.fillStyle = fishAccessible ? 'rgba(253,224,71,0.90)' : 'rgba(250,204,21,0.64)'; ctx.fill();
                    ctx.strokeStyle = 'rgba(254,240,138,0.52)'; ctx.lineWidth = 0.65; ctx.stroke();
                  });

                  // Three overlapping liver lobes occupy the anterior ventral cavity.
                  [[-0.115, 0.026, 0.060, 0.033, -0.12], [-0.080, 0.046, 0.050, 0.028, 0.10], [-0.135, 0.052, 0.038, 0.024, -0.28]].forEach(function (liverLobe) {
                    ctx.beginPath(); ctx.ellipse(cx + W * liverLobe[0], cy + H * liverLobe[1], W * liverLobe[2], H * liverLobe[3], liverLobe[4], 0, Math.PI * 2);
                    var perchLiverGradient = ctx.createRadialGradient(cx + W * (liverLobe[0] - 0.012), cy + H * (liverLobe[1] - 0.010), 1, cx + W * liverLobe[0], cy + H * liverLobe[1], W * liverLobe[2]);
                    perchLiverGradient.addColorStop(0, fishAccessible ? '#b45309' : 'rgba(180,83,9,0.96)');
                    perchLiverGradient.addColorStop(1, fishAccessible ? '#78350f' : 'rgba(120,53,15,0.96)');
                    ctx.fillStyle = perchLiverGradient; ctx.fill(); ctx.strokeStyle = 'rgba(253,186,116,0.34)'; ctx.lineWidth = 0.6; ctx.stroke();
                  });

                  // J-shaped stomach passes posteriorly before turning into the pyloric region.
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.070, cy + H * 0.027);
                  ctx.bezierCurveTo(cx - W * 0.030, cy + H * 0.018, cx + W * 0.012, cy + H * 0.035, cx + W * 0.010, cy + H * 0.058);
                  ctx.bezierCurveTo(cx + W * 0.008, cy + H * 0.080, cx - W * 0.022, cy + H * 0.086, cx - W * 0.038, cy + H * 0.070);
                  ctx.bezierCurveTo(cx - W * 0.050, cy + H * 0.058, cx - W * 0.050, cy + H * 0.040, cx - W * 0.070, cy + H * 0.027);
                  ctx.closePath();
                  var perchStomachGradient = ctx.createLinearGradient(cx - W * 0.05, cy + H * 0.02, cx, cy + H * 0.085);
                  perchStomachGradient.addColorStop(0, fishAccessible ? '#fed7aa' : 'rgba(253,186,116,0.90)');
                  perchStomachGradient.addColorStop(1, fishAccessible ? '#fb923c' : 'rgba(194,65,12,0.86)');
                  ctx.fillStyle = perchStomachGradient; ctx.fill(); ctx.strokeStyle = 'rgba(255,237,213,0.58)'; ctx.lineWidth = 0.9; ctx.stroke();

                  // Pyloric ceca are distinct blind fingers, not a single generic line.
                  ctx.lineCap = 'round';
                  for (var cecumIndex = 0; cecumIndex < 5; cecumIndex++) {
                    var cecumBaseX = cx + W * (0.002 + cecumIndex * 0.008);
                    var cecumBaseY = cy + H * (0.063 + cecumIndex * 0.003);
                    ctx.beginPath(); ctx.moveTo(cecumBaseX, cecumBaseY);
                    ctx.quadraticCurveTo(cecumBaseX + W * 0.014, cecumBaseY + H * 0.012, cecumBaseX + W * (0.010 + cecumIndex * 0.004), cecumBaseY + H * (0.034 + cecumIndex * 0.002));
                    ctx.strokeStyle = fishAccessible ? '#fef3c7' : 'rgba(254,215,170,0.88)'; ctx.lineWidth = fishAccessible ? 2.4 : 1.8; ctx.stroke();
                  }

                  // Intestine loops compactly through the posterior ventral cavity and exits near the anus.
                  ctx.beginPath();
                  ctx.moveTo(cx + W * 0.018, cy + H * 0.071);
                  ctx.bezierCurveTo(cx + W * 0.070, cy + H * 0.102, cx + W * 0.105, cy + H * 0.052, cx + W * 0.135, cy + H * 0.076);
                  ctx.bezierCurveTo(cx + W * 0.165, cy + H * 0.100, cx + W * 0.185, cy + H * 0.056, cx + W * 0.122, cy + H * 0.045);
                  ctx.bezierCurveTo(cx + W * 0.082, cy + H * 0.038, cx + W * 0.080, cy + H * 0.082, cx + W * 0.183, cy + H * 0.084);
                  ctx.lineTo(cx + W * 0.215, cy + H * 0.080);
                  ctx.strokeStyle = fishAccessible ? '#fdba74' : 'rgba(234,88,12,0.86)'; ctx.lineWidth = fishAccessible ? 4.6 : 3.4; ctx.stroke();
                  ctx.strokeStyle = 'rgba(255,237,213,0.34)'; ctx.lineWidth = 0.8; ctx.stroke();

                  // Four branchial arches carry dense filaments; the operculum remains visible outside the cutaway.
                  for (var gillArchIndex = 0; gillArchIndex < 4; gillArchIndex++) drawPerchGillArch(gillArchIndex);

                  // Two functional chambers are shown with the sinus venosus and bulbus arteriosus in series.
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.205, cy + H * 0.050, W * 0.013, H * 0.013, -0.20, 0, Math.PI * 2);
                  ctx.fillStyle = fishAccessible ? '#7dd3fc' : 'rgba(56,189,248,0.72)'; ctx.fill();
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.188, cy + H * 0.056, W * 0.018, H * 0.018, 0.20, 0, Math.PI * 2);
                  ctx.fillStyle = fishAccessible ? '#be123c' : 'rgba(190,24,93,0.90)'; ctx.fill(); ctx.strokeStyle = 'rgba(254,205,211,0.56)'; ctx.lineWidth = 0.7; ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.165, cy + H * 0.068, W * 0.022, H * 0.024, -0.12, 0, Math.PI * 2);
                  var perchVentricleGradient = ctx.createRadialGradient(cx - W * 0.172, cy + H * 0.058, 1, cx - W * 0.165, cy + H * 0.068, W * 0.024);
                  perchVentricleGradient.addColorStop(0, fishAccessible ? '#fb7185' : 'rgba(244,63,94,0.96)');
                  perchVentricleGradient.addColorStop(1, fishAccessible ? '#9f1239' : 'rgba(136,19,55,0.98)');
                  ctx.fillStyle = perchVentricleGradient; ctx.fill(); ctx.strokeStyle = 'rgba(254,205,211,0.62)'; ctx.lineWidth = 1; ctx.stroke();
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.142, cy + H * 0.051, W * 0.017, H * 0.012, -0.38, 0, Math.PI * 2);
                  ctx.fillStyle = fishAccessible ? '#fda4af' : 'rgba(251,113,133,0.84)'; ctx.fill();
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.142, cy + H * 0.049); ctx.bezierCurveTo(cx - W * 0.165, cy + H * 0.028, cx - W * 0.178, cy + H * 0.010, cx - W * 0.190, cy - H * 0.005);
                  ctx.strokeStyle = fishAccessible ? '#fb7185' : 'rgba(251,113,133,0.86)'; ctx.lineWidth = 1.8; ctx.stroke();

                  // Small spleen sits beside the stomach as a dark red reference landmark.
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.016, cy + H * 0.042, W * 0.020, H * 0.009, 0.30, 0, Math.PI * 2);
                  ctx.fillStyle = fishAccessible ? '#7f1d1d' : 'rgba(127,29,29,0.88)'; ctx.fill();

                  ctx.restore();
                }
                if (activeLayer === 'skeleton') {

                  ctx.globalAlpha = 0.7; var boneC = '#94a3b8';

                  ctx.strokeStyle = boneC; ctx.lineWidth = 1.5;

                  ctx.beginPath(); ctx.ellipse(cx - W * 0.25, cy - H * 0.01, W * 0.06, H * 0.05, 0, 0, Math.PI * 2);

                  ctx.fillStyle = 'rgba(148,163,184,0.2)'; ctx.fill(); ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.28, cy);

                  ctx.lineTo(cx - W * 0.30, cy + H * 0.03); ctx.lineTo(cx - W * 0.24, cy + H * 0.02); ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.19, cy - H * 0.02);

                  ctx.lineTo(cx + W * 0.26, cy - H * 0.02); ctx.lineWidth = 3; ctx.stroke();

                  for (var fv = 0; fv < 22; fv++) {

                    var fvx = cx - W * 0.18 + fv * (W * 0.43 / 22);

                    ctx.beginPath(); ctx.moveTo(fvx, cy - H * 0.05); ctx.lineTo(fvx, cy + H * 0.01);

                    ctx.strokeStyle = boneC; ctx.lineWidth = 0.6; ctx.stroke();

                  }

                  for (var rb = 0; rb < 10; rb++) {

                    var rbx = cx - W * 0.15 + rb * W * 0.03;

                    ctx.beginPath(); ctx.moveTo(rbx, cy);

                    ctx.quadraticCurveTo(rbx - W * 0.005, cy + H * 0.06, rbx, cy + H * 0.08);

                    ctx.strokeStyle = boneC; ctx.lineWidth = 0.5; ctx.stroke();

                  }

                  ctx.beginPath(); ctx.ellipse(cx - W * 0.19, cy, W * 0.02, H * 0.06, 0, 0, Math.PI * 2);

                  ctx.strokeStyle = boneC; ctx.lineWidth = 0.8; ctx.stroke();

                  ctx.globalAlpha = 1;

                }





              } else if (spec.bodyShape === 'crayfish') {
                // Crayfish: articulated calcified plates, jointed appendages, and a layered tail fan.
                var crayAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var crayRealistic = (d.visualRealism || visualRealism) === 'realistic';
                var crayOutlineWidth = crayAccessible ? 1.7 : 1.05;
                var crayShellLight = crayAccessible ? layerColor : 'rgba(184,70,54,0.98)';
                var crayShellMid = crayAccessible ? layerColor : 'rgba(132,48,39,0.99)';
                var crayShellDark = crayAccessible ? layerColor : 'rgba(74,31,30,0.99)';

                function drawCraySegment(x1, y1, x2, y2, r1, r2, alpha) {
                  var dx = x2 - x1; var dy = y2 - y1;
                  var len = Math.max(1, Math.sqrt(dx * dx + dy * dy));
                  var nx = -dy / len; var ny = dx / len;
                  ctx.save(); ctx.globalAlpha = alpha == null ? 1 : alpha;
                  var segmentGrad = ctx.createLinearGradient(x1 + nx * r1, y1 + ny * r1, x1 - nx * r1, y1 - ny * r1);
                  segmentGrad.addColorStop(0, crayShellLight); segmentGrad.addColorStop(0.48, crayShellMid); segmentGrad.addColorStop(1, crayShellDark);
                  ctx.beginPath();
                  ctx.moveTo(x1 + nx * r1, y1 + ny * r1);
                  ctx.lineTo(x2 + nx * r2, y2 + ny * r2);
                  ctx.quadraticCurveTo(x2 + dx / len * r2, y2 + dy / len * r2, x2 - nx * r2, y2 - ny * r2);
                  ctx.lineTo(x1 - nx * r1, y1 - ny * r1);
                  ctx.quadraticCurveTo(x1 - dx / len * r1, y1 - dy / len * r1, x1 + nx * r1, y1 + ny * r1);
                  ctx.closePath(); ctx.fillStyle = segmentGrad; ctx.fill();
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = crayOutlineWidth * 0.75; ctx.stroke();
                  ctx.restore();
                }

                function drawCrayWalkingLeg(side, index, alpha) {
                  var hipX = cx - W * 0.07 + index * W * 0.047;
                  var hipY = cy + side * H * (0.095 + index * 0.002);
                  var kneeX = hipX - W * (0.018 - index * 0.002);
                  var kneeY = cy + side * H * (0.17 + index * 0.012);
                  var ankleX = hipX - W * (0.055 - index * 0.004);
                  var ankleY = cy + side * H * (0.235 + index * 0.012);
                  var tipX = ankleX - W * (0.035 - index * 0.003);
                  var tipY = cy + side * H * (0.255 + index * 0.012);
                  drawCraySegment(hipX, hipY, kneeX, kneeY, 2.8, 2.1, alpha);
                  drawCraySegment(kneeX, kneeY, ankleX, ankleY, 2.2, 1.25, alpha);
                  ctx.save(); ctx.globalAlpha = alpha;
                  ctx.beginPath(); ctx.moveTo(ankleX, ankleY); ctx.quadraticCurveTo(tipX - W * 0.01, tipY, tipX, tipY);
                  ctx.strokeStyle = layerStroke; ctx.lineWidth = 1.05; ctx.stroke();
                  ctx.beginPath(); ctx.arc(kneeX, kneeY, 1.8, 0, Math.PI * 2); ctx.fillStyle = crayShellDark; ctx.fill();
                  ctx.restore();
                }

                function drawCrayCheliped(side) {
                  var shoulderX = cx - W * 0.13; var shoulderY = cy + side * H * 0.075;
                  var elbowX = cx - W * 0.22; var elbowY = cy + side * H * 0.145;
                  var wristX = cx - W * 0.285; var wristY = cy + side * H * 0.205;
                  var palmX = cx - W * 0.325; var palmY = cy + side * H * 0.225;
                  drawCraySegment(shoulderX, shoulderY, elbowX, elbowY, 5.5, 4.5, side < 0 ? 0.78 : 0.96);
                  drawCraySegment(elbowX, elbowY, wristX, wristY, 4.2, 3.4, side < 0 ? 0.82 : 1);
                  ctx.save(); ctx.globalAlpha = side < 0 ? 0.84 : 1;
                  ctx.beginPath(); ctx.ellipse(palmX, palmY, W * 0.043, H * 0.027, side * 0.13, 0, Math.PI * 2);
                  var clawPalm = ctx.createLinearGradient(palmX, palmY - H * 0.03, palmX, palmY + H * 0.03);
                  clawPalm.addColorStop(0, crayShellLight); clawPalm.addColorStop(0.56, crayShellMid); clawPalm.addColorStop(1, crayShellDark);
                  ctx.fillStyle = clawPalm; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = crayOutlineWidth; ctx.stroke();
                  var clawTipX = cx - W * 0.405;
                  ctx.beginPath(); ctx.moveTo(palmX - W * 0.025, palmY - side * H * 0.006);
                  ctx.bezierCurveTo(cx - W * 0.355, palmY - side * H * 0.035, cx - W * 0.39, palmY - side * H * 0.028, clawTipX, palmY - side * H * 0.008);
                  ctx.bezierCurveTo(cx - W * 0.382, palmY - side * H * 0.014, cx - W * 0.35, palmY - side * H * 0.005, palmX - W * 0.018, palmY + side * H * 0.005);
                  ctx.closePath(); ctx.fillStyle = clawPalm; ctx.fill(); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(palmX - W * 0.022, palmY + side * H * 0.010);
                  ctx.bezierCurveTo(cx - W * 0.355, palmY + side * H * 0.040, cx - W * 0.382, palmY + side * H * 0.032, cx - W * 0.397, palmY + side * H * 0.016);
                  ctx.bezierCurveTo(cx - W * 0.373, palmY + side * H * 0.020, cx - W * 0.35, palmY + side * H * 0.012, palmX - W * 0.016, palmY + side * H * 0.003);
                  ctx.closePath(); ctx.fillStyle = clawPalm; ctx.fill(); ctx.stroke();
                  ctx.strokeStyle = 'rgba(248,250,252,0.16)'; ctx.lineWidth = 0.6;
                  ctx.beginPath(); ctx.moveTo(palmX - W * 0.018, palmY - side * H * 0.010); ctx.lineTo(clawTipX + W * 0.006, palmY - side * H * 0.009); ctx.stroke();
                  ctx.restore();
                }

                // Fine antennae sit behind the shell, while the shorter antennules angle toward the tray.
                ctx.save(); ctx.strokeStyle = layerStroke; ctx.lineCap = 'round';
                [-1, 1].forEach(function (side) {
                  ctx.globalAlpha = side < 0 ? 0.62 : 0.9;
                  ctx.lineWidth = 0.85;
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.20, cy + side * H * 0.045);
                  ctx.bezierCurveTo(cx - W * 0.28, cy + side * H * 0.12, cx - W * 0.36, cy + side * H * 0.08, cx - W * 0.45, cy + side * H * 0.14); ctx.stroke();
                  ctx.lineWidth = 0.55;
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.215, cy + side * H * 0.025);
                  ctx.bezierCurveTo(cx - W * 0.275, cy + side * H * 0.055, cx - W * 0.32, cy + side * H * 0.025, cx - W * 0.355, cy + side * H * 0.06); ctx.stroke();
                });
                ctx.restore();

                // Far-side appendages are subdued to create depth.
                for (var farLeg = 0; farLeg < 4; farLeg++) drawCrayWalkingLeg(-1, farLeg, 0.48);
                drawCrayCheliped(-1);

                // Swimmerets emerge beneath each abdominal plate.
                ctx.save(); ctx.globalAlpha = 0.42; ctx.strokeStyle = layerStroke;
                for (var swim = 0; swim < 5; swim++) {
                  var swimX = cx + W * (0.145 + swim * 0.041);
                  [-1, 1].forEach(function (side) {
                    ctx.beginPath(); ctx.moveTo(swimX, cy + side * H * 0.045);
                    ctx.quadraticCurveTo(swimX + W * 0.012, cy + side * H * 0.082, swimX + W * 0.002, cy + side * H * 0.098);
                    ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.55; ctx.stroke();
                  });
                }
                ctx.restore();

                // Overlapping abdomen plates taper naturally into the telson.
                for (var craySeg = 5; craySeg >= 0; craySeg--) {
                  var plateX = cx + W * (0.115 + craySeg * 0.041);
                  var plateHalfW = W * 0.030;
                  var plateHalfH = H * (0.103 - craySeg * 0.009);
                  var plateGrad = ctx.createLinearGradient(plateX, cy - plateHalfH, plateX, cy + plateHalfH);
                  plateGrad.addColorStop(0, crayShellLight); plateGrad.addColorStop(0.52, crayShellMid); plateGrad.addColorStop(1, crayShellDark);
                  ctx.beginPath();
                  ctx.moveTo(plateX - plateHalfW, cy - plateHalfH * 0.78);
                  ctx.bezierCurveTo(plateX - plateHalfW * 0.2, cy - plateHalfH, plateX + plateHalfW * 0.75, cy - plateHalfH * 0.84, plateX + plateHalfW, cy - plateHalfH * 0.58);
                  ctx.lineTo(plateX + plateHalfW, cy + plateHalfH * 0.58);
                  ctx.bezierCurveTo(plateX + plateHalfW * 0.75, cy + plateHalfH * 0.84, plateX - plateHalfW * 0.2, cy + plateHalfH, plateX - plateHalfW, cy + plateHalfH * 0.78);
                  ctx.closePath(); ctx.fillStyle = plateGrad; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = crayOutlineWidth * 0.72; ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(plateX - plateHalfW * 0.55, cy - plateHalfH * 0.62);
                  ctx.quadraticCurveTo(plateX, cy - plateHalfH * 0.77, plateX + plateHalfW * 0.56, cy - plateHalfH * 0.50);
                  ctx.strokeStyle = 'rgba(248,250,252,0.14)'; ctx.lineWidth = 0.6; ctx.stroke();
                }

                // Tail fan: central telson plus paired overlapping uropods and reinforcing rays.
                var crayTailBase = cx + W * 0.35;
                var tailFanGrad = ctx.createLinearGradient(crayTailBase, cy - H * 0.09, crayTailBase + W * 0.085, cy + H * 0.09);
                tailFanGrad.addColorStop(0, crayShellLight); tailFanGrad.addColorStop(0.55, crayShellMid); tailFanGrad.addColorStop(1, crayShellDark);
                [-1, 1].forEach(function (side) {
                  ctx.save(); ctx.globalAlpha = side < 0 ? 0.76 : 0.92;
                  ctx.beginPath(); ctx.moveTo(crayTailBase - W * 0.006, cy + side * H * 0.035);
                  ctx.bezierCurveTo(crayTailBase + W * 0.025, cy + side * H * 0.105, crayTailBase + W * 0.092, cy + side * H * 0.10, crayTailBase + W * 0.085, cy + side * H * 0.025);
                  ctx.bezierCurveTo(crayTailBase + W * 0.045, cy + side * H * 0.018, crayTailBase + W * 0.018, cy + side * H * 0.018, crayTailBase - W * 0.006, cy + side * H * 0.035);
                  ctx.closePath(); ctx.fillStyle = tailFanGrad; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = crayOutlineWidth * 0.75; ctx.stroke();
                  for (var fanRay = 1; fanRay <= 3; fanRay++) {
                    ctx.beginPath(); ctx.moveTo(crayTailBase + W * 0.012, cy + side * H * 0.032);
                    ctx.lineTo(crayTailBase + W * (0.035 + fanRay * 0.013), cy + side * H * (0.03 + fanRay * 0.014));
                    ctx.strokeStyle = 'rgba(248,250,252,0.13)'; ctx.lineWidth = 0.45; ctx.stroke();
                  }
                  ctx.restore();
                });
                ctx.beginPath(); ctx.moveTo(crayTailBase - W * 0.008, cy - H * 0.045);
                ctx.bezierCurveTo(crayTailBase + W * 0.032, cy - H * 0.052, crayTailBase + W * 0.072, cy - H * 0.025, crayTailBase + W * 0.087, cy);
                ctx.bezierCurveTo(crayTailBase + W * 0.072, cy + H * 0.025, crayTailBase + W * 0.032, cy + H * 0.052, crayTailBase - W * 0.008, cy + H * 0.045);
                ctx.closePath(); ctx.fillStyle = tailFanGrad; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = crayOutlineWidth; ctx.stroke();

                // Unified cephalothorax and rostrum, avoiding the former separate triangle-and-oval look.
                var crayCarapaceGradient = ctx.createLinearGradient(cx - W * 0.08, cy - H * 0.15, cx + W * 0.03, cy + H * 0.15);
                crayCarapaceGradient.addColorStop(0, crayShellLight); crayCarapaceGradient.addColorStop(0.46, crayShellMid); crayCarapaceGradient.addColorStop(1, crayShellDark);
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.29, cy);
                ctx.lineTo(cx - W * 0.215, cy - H * 0.048);
                ctx.bezierCurveTo(cx - W * 0.19, cy - H * 0.115, cx - W * 0.105, cy - H * 0.145, cx - W * 0.015, cy - H * 0.14);
                ctx.bezierCurveTo(cx + W * 0.07, cy - H * 0.135, cx + W * 0.115, cy - H * 0.103, cx + W * 0.125, cy - H * 0.055);
                ctx.bezierCurveTo(cx + W * 0.135, cy - H * 0.018, cx + W * 0.135, cy + H * 0.018, cx + W * 0.125, cy + H * 0.055);
                ctx.bezierCurveTo(cx + W * 0.115, cy + H * 0.103, cx + W * 0.07, cy + H * 0.135, cx - W * 0.015, cy + H * 0.14);
                ctx.bezierCurveTo(cx - W * 0.105, cy + H * 0.145, cx - W * 0.19, cy + H * 0.115, cx - W * 0.215, cy + H * 0.048);
                ctx.closePath(); ctx.fillStyle = crayCarapaceGradient; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = crayOutlineWidth; ctx.stroke();
                ctx.shadowBlur = 0;

                // Cervical groove, branchial contours, and calcified microtexture.
                ctx.beginPath(); ctx.moveTo(cx - W * 0.035, cy - H * 0.132);
                ctx.bezierCurveTo(cx - W * 0.055, cy - H * 0.062, cx - W * 0.055, cy + H * 0.062, cx - W * 0.035, cy + H * 0.132);
                ctx.strokeStyle = 'rgba(40,18,20,0.64)'; ctx.lineWidth = 1.1; ctx.stroke();
                [-1, 1].forEach(function (side) {
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.015, cy + side * H * 0.075);
                  ctx.quadraticCurveTo(cx + W * 0.055, cy + side * H * 0.115, cx + W * 0.105, cy + side * H * 0.074);
                  ctx.strokeStyle = 'rgba(248,250,252,0.12)'; ctx.lineWidth = 0.7; ctx.stroke();
                });
                if (!crayAccessible) {
                  ctx.save(); ctx.globalAlpha = crayRealistic ? 0.19 : 0.12;
                  for (var shellDot = 0; shellDot < 36; shellDot++) {
                    var shellCol = shellDot % 9; var shellRow = Math.floor(shellDot / 9);
                    var shellX = cx - W * 0.17 + shellCol * W * 0.031;
                    var shellY = cy - H * 0.085 + shellRow * H * 0.052 + (shellCol % 2) * H * 0.008;
                    ctx.beginPath(); ctx.ellipse(shellX, shellY, 1.1, 0.65, -0.25, 0, Math.PI * 2);
                    ctx.fillStyle = shellRow % 2 ? 'rgba(248,250,252,0.48)' : 'rgba(15,23,42,0.58)'; ctx.fill();
                  }
                  ctx.restore();
                }
                ctx.beginPath(); ctx.moveTo(cx - W * 0.175, cy - H * 0.09);
                ctx.quadraticCurveTo(cx - W * 0.03, cy - H * 0.135, cx + W * 0.085, cy - H * 0.083);
                ctx.strokeStyle = 'rgba(255,255,255,0.16)'; ctx.lineWidth = 1.5; ctx.stroke();

                // Movable eye stalks with faceted, low-glare compound eyes.
                [-1, 1].forEach(function (side) {
                  var eyeStalkX = cx - W * 0.215; var eyeStalkY = cy + side * H * 0.073;
                  var crayEyeX = cx - W * 0.245; var crayEyeY = cy + side * H * 0.101;
                  drawCraySegment(eyeStalkX, eyeStalkY, crayEyeX, crayEyeY, 2.4, 2.05, side < 0 ? 0.8 : 1);
                  ctx.save(); ctx.globalAlpha = side < 0 ? 0.84 : 1;
                  var compoundEyeGrad = ctx.createRadialGradient(crayEyeX - 1, crayEyeY - 1, 0.5, crayEyeX, crayEyeY, 5);
                  compoundEyeGrad.addColorStop(0, 'rgba(82,82,72,0.9)'); compoundEyeGrad.addColorStop(0.42, 'rgba(17,24,39,0.98)'); compoundEyeGrad.addColorStop(1, 'rgba(2,6,23,1)');
                  ctx.beginPath(); ctx.ellipse(crayEyeX, crayEyeY, 4.9, 4.0, side * 0.18, 0, Math.PI * 2); ctx.fillStyle = compoundEyeGrad; ctx.fill();
                  ctx.strokeStyle = 'rgba(148,163,184,0.35)'; ctx.lineWidth = 0.35;
                  ctx.beginPath(); ctx.moveTo(crayEyeX - 3.4, crayEyeY); ctx.lineTo(crayEyeX + 3.4, crayEyeY); ctx.moveTo(crayEyeX, crayEyeY - 2.8); ctx.lineTo(crayEyeX, crayEyeY + 2.8); ctx.stroke();
                  ctx.restore();
                });

                // Near-side walking legs and cheliped overlap the body edge for readable articulation.
                for (var nearLeg = 0; nearLeg < 4; nearLeg++) drawCrayWalkingLeg(1, nearLeg, 0.94);
                drawCrayCheliped(1);

                if (activeLayer === 'muscle') {
                  ctx.save(); ctx.globalAlpha = 0.54;
                  for (var muscleSeg = 0; muscleSeg < 6; muscleSeg++) {
                    var muscleX = cx + W * (0.125 + muscleSeg * 0.041);
                    var muscleH = H * (0.065 - muscleSeg * 0.004);
                    ctx.beginPath(); ctx.ellipse(muscleX, cy + H * 0.012, W * 0.020, muscleH, 0, 0, Math.PI * 2);
                    ctx.fillStyle = muscleSeg % 2 ? '#fca5a5' : '#ef4444'; ctx.fill();
                    ctx.strokeStyle = 'rgba(127,29,29,0.65)'; ctx.lineWidth = 0.45; ctx.stroke();
                  }
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.ellipse(cx - W * 0.245, cy + side * H * 0.18, W * 0.037, H * 0.017, side * 0.55, 0, Math.PI * 2);
                    ctx.fillStyle = '#f87171'; ctx.fill();
                  });
                  ctx.restore();
                }

                if (activeLayer === 'organs') {
                  ctx.save(); ctx.globalAlpha = 0.62;
                  // Gastric mill and paired green glands at the anterior end.
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.12, cy, W * 0.038, H * 0.048, 0, 0, Math.PI * 2); ctx.fillStyle = '#fcd34d'; ctx.fill();
                  ctx.strokeStyle = '#92400e'; ctx.lineWidth = 0.65;
                  for (var tooth = -1; tooth <= 1; tooth++) { ctx.beginPath(); ctx.moveTo(cx - W * 0.14, cy + tooth * H * 0.014); ctx.lineTo(cx - W * 0.10, cy - tooth * H * 0.006); ctx.stroke(); }
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.arc(cx - W * 0.205, cy + side * H * 0.038, W * 0.012, 0, Math.PI * 2); ctx.fillStyle = '#86efac'; ctx.fill();
                  });
                  // Hepatopancreas lobes, dorsal heart, gonad, and feathery gills.
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.ellipse(cx - W * 0.015, cy + side * H * 0.034, W * 0.057, H * 0.036, side * 0.12, 0, Math.PI * 2); ctx.fillStyle = '#d97706'; ctx.fill();
                  });
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.035, cy - H * 0.035, W * 0.027, H * 0.022, 0, 0, Math.PI * 2); ctx.fillStyle = '#dc2626'; ctx.fill();
                  ctx.beginPath(); ctx.ellipse(cx + W * 0.075, cy, W * 0.042, H * 0.016, 0, 0, Math.PI * 2); ctx.fillStyle = '#f9a8d4'; ctx.fill();
                  [-1,1].forEach(function (side) {
                    for (var gill = 0; gill < 4; gill++) {
                      var gillX = cx - W * 0.015 + gill * W * 0.033;
                      ctx.beginPath(); ctx.moveTo(gillX, cy + side * H * 0.072); ctx.lineTo(gillX + W * 0.012, cy + side * H * 0.112);
                      ctx.strokeStyle = '#fda4af'; ctx.lineWidth = 1.4; ctx.stroke();
                      ctx.lineWidth = 0.45;
                      for (var filament = 1; filament <= 3; filament++) {
                        var fy = cy + side * H * (0.075 + filament * 0.009);
                        ctx.beginPath(); ctx.moveTo(gillX + W * 0.002, fy); ctx.lineTo(gillX + W * 0.014, fy + side * H * 0.004); ctx.stroke();
                      }
                    }
                  });
                  ctx.restore();
                }

                if (activeLayer === 'nervous') {
                  ctx.save(); ctx.globalAlpha = 0.76;
                  ctx.beginPath(); ctx.ellipse(cx - W * 0.17, cy, W * 0.021, H * 0.017, 0, 0, Math.PI * 2); ctx.fillStyle = '#fde047'; ctx.fill();
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.148, cy + H * 0.018);
                  ctx.bezierCurveTo(cx - W * 0.04, cy + H * 0.04, cx + W * 0.18, cy + H * 0.035, cx + W * 0.34, cy + H * 0.018);
                  ctx.strokeStyle = '#fbbf24'; ctx.lineWidth = 1.9; ctx.stroke();
                  for (var ganglion = 0; ganglion < 8; ganglion++) {
                    var ganglionX = cx - W * 0.11 + ganglion * W * 0.061;
                    ctx.beginPath(); ctx.ellipse(ganglionX, cy + H * 0.032, 3.1, 2.2, 0, 0, Math.PI * 2); ctx.fillStyle = '#f59e0b'; ctx.fill();
                  }
                  ctx.strokeStyle = 'rgba(253,224,71,0.72)'; ctx.lineWidth = 0.65;
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.moveTo(cx - W * 0.17, cy); ctx.quadraticCurveTo(cx - W * 0.25, cy + side * H * 0.04, cx - W * 0.34, cy + side * H * 0.10); ctx.stroke();
                  });
                  ctx.restore();
                }


              } else if (spec.bodyShape === 'eye') {
                // Sheep eye: layered lateral cross-section with a transparent optical path.
                var eyeAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var eyeRealistic = (d.visualRealism || visualRealism) === 'realistic';
                var eyeCloudy = (d.specimenCondition || specimenCondition) === 'cloudy';
                var eyeR = Math.min(W * 0.285, H * 0.36);
                var eyeCx = cx + W * 0.015;
                var eyeCy = cy;
                var eyeRx = eyeR;
                var eyeRy = eyeR * 0.83;

                function traceSheepEyeGlobe(scaleX, scaleY) {
                  var rx = eyeRx * (scaleX == null ? 1 : scaleX);
                  var ry = eyeRy * (scaleY == null ? 1 : scaleY);
                  ctx.beginPath();
                  ctx.ellipse(eyeCx, eyeCy, rx, ry, -0.015, 0, Math.PI * 2);
                }

                // Orbital fat and extraocular muscle remnants sit behind the isolated globe.
                if (activeLayer === 'skin') {
                  ctx.save(); ctx.globalAlpha = eyeAccessible ? 0.34 : 0.5;
                  [[0.48,-0.78,0.24,0.13,-0.35],[0.72,-0.48,0.20,0.12,0.2],[0.70,0.57,0.23,0.13,-0.18],[0.42,0.80,0.20,0.11,0.3]].forEach(function (fat) {
                    ctx.beginPath(); ctx.ellipse(eyeCx + eyeRx * fat[0], eyeCy + eyeRy * fat[1], eyeR * fat[2], eyeR * fat[3], fat[4], 0, Math.PI * 2);
                    var fatGrad = ctx.createRadialGradient(eyeCx + eyeRx * fat[0] - 3, eyeCy + eyeRy * fat[1] - 3, 1, eyeCx + eyeRx * fat[0], eyeCy + eyeRy * fat[1], eyeR * fat[2]);
                    fatGrad.addColorStop(0, 'rgba(254,240,138,0.72)'); fatGrad.addColorStop(1, 'rgba(161,98,7,0.28)');
                    ctx.fillStyle = fatGrad; ctx.fill(); ctx.strokeStyle = 'rgba(120,53,15,0.3)'; ctx.lineWidth = 0.55; ctx.stroke();
                  });
                  [-1,1].forEach(function (side) {
                    ctx.beginPath(); ctx.moveTo(eyeCx - eyeRx * 0.10, eyeCy + side * eyeRy * 0.92);
                    ctx.bezierCurveTo(eyeCx + eyeRx * 0.22, eyeCy + side * eyeRy * 1.12, eyeCx + eyeRx * 0.62, eyeCy + side * eyeRy * 1.04, eyeCx + eyeRx * 0.84, eyeCy + side * eyeRy * 0.72);
                    ctx.strokeStyle = side < 0 ? 'rgba(190,24,93,0.58)' : 'rgba(159,18,57,0.48)'; ctx.lineWidth = eyeR * 0.075; ctx.stroke();
                    ctx.strokeStyle = 'rgba(254,205,211,0.25)'; ctx.lineWidth = 1; ctx.stroke();
                  });
                  ctx.restore();
                }

                // Dense collagen of the sclera receives a restrained pearly gradient.
                var scleraGrad = ctx.createRadialGradient(eyeCx - eyeRx * 0.30, eyeCy - eyeRy * 0.34, eyeR * 0.08, eyeCx, eyeCy, eyeR * 1.12);
                scleraGrad.addColorStop(0, eyeAccessible ? '#f8fafc' : '#f5f1e8');
                scleraGrad.addColorStop(0.64, eyeAccessible ? '#e2e8f0' : '#d8d1c4');
                scleraGrad.addColorStop(1, eyeAccessible ? '#94a3b8' : '#8f877d');
                traceSheepEyeGlobe(); ctx.fillStyle = scleraGrad; ctx.fill(); ctx.strokeStyle = eyeAccessible ? '#475569' : '#6b625b'; ctx.lineWidth = eyeAccessible ? 2.2 : 1.45; ctx.stroke();
                ctx.shadowBlur = 0;

                // Choroid: thin, vascular, heavily pigmented band inside the sclera.
                var choroidGrad = ctx.createRadialGradient(eyeCx - eyeRx * 0.2, eyeCy - eyeRy * 0.25, eyeR * 0.08, eyeCx, eyeCy, eyeR);
                choroidGrad.addColorStop(0, 'rgba(101,61,48,0.98)'); choroidGrad.addColorStop(0.72, 'rgba(54,33,34,0.99)'); choroidGrad.addColorStop(1, 'rgba(24,18,22,1)');
                traceSheepEyeGlobe(0.945,0.925); ctx.fillStyle = choroidGrad; ctx.fill();

                // Tapetum lucidum occupies the dorsal posterior fundus in sheep, not a full neon ring.
                ctx.save();
                ctx.beginPath();
                ctx.ellipse(eyeCx + eyeRx * 0.19, eyeCy - eyeRy * 0.08, eyeRx * 0.71, eyeRy * 0.79, -0.02, -1.03, 0.16);
                var tapetumGrad = ctx.createLinearGradient(eyeCx, eyeCy - eyeRy * 0.70, eyeCx + eyeRx * 0.78, eyeCy + eyeRy * 0.15);
                tapetumGrad.addColorStop(0, 'rgba(125,211,252,0.56)'); tapetumGrad.addColorStop(0.44, 'rgba(94,234,212,0.46)'); tapetumGrad.addColorStop(0.76, 'rgba(190,242,100,0.28)'); tapetumGrad.addColorStop(1, 'rgba(148,163,184,0.05)');
                ctx.strokeStyle = tapetumGrad; ctx.lineWidth = eyeR * 0.055; ctx.stroke();
                ctx.restore();

                // Retina is a delicate translucent lining over the choroid and tapetum.
                traceSheepEyeGlobe(0.89,0.865);
                ctx.fillStyle = eyeAccessible ? 'rgba(254,249,195,0.92)' : 'rgba(244,230,183,0.72)'; ctx.fill();
                ctx.strokeStyle = 'rgba(253,230,138,0.65)'; ctx.lineWidth = eyeAccessible ? 1.5 : 0.85; ctx.stroke();

                // Vitreous gel fills the posterior globe with soft depth and condition-aware clarity.
                var vitreousGrad = ctx.createRadialGradient(eyeCx - eyeRx * 0.28, eyeCy - eyeRy * 0.30, 2, eyeCx + eyeRx * 0.05, eyeCy, eyeRx * 0.86);
                vitreousGrad.addColorStop(0, eyeCloudy ? 'rgba(226,232,240,0.58)' : 'rgba(240,249,255,0.48)');
                vitreousGrad.addColorStop(0.62, eyeCloudy ? 'rgba(203,213,225,0.45)' : 'rgba(186,230,253,0.22)');
                vitreousGrad.addColorStop(1, eyeCloudy ? 'rgba(148,163,184,0.35)' : 'rgba(125,211,252,0.08)');
                traceSheepEyeGlobe(0.845,0.81); ctx.fillStyle = vitreousGrad; ctx.fill();

                // Posterior optic nerve with layered dural sheath and central axon bundle.
                var nerveStartX = eyeCx + eyeRx * 0.88; var nerveStartY = eyeCy + eyeRy * 0.11;
                var nerveEndX = eyeCx + eyeRx * 1.31; var nerveEndY = eyeCy + eyeRy * 0.24;
                ctx.beginPath(); ctx.moveTo(nerveStartX, nerveStartY); ctx.lineTo(nerveEndX, nerveEndY);
                ctx.strokeStyle = 'rgba(203,213,225,0.92)'; ctx.lineWidth = eyeR * 0.12; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(nerveStartX, nerveStartY); ctx.lineTo(nerveEndX, nerveEndY);
                ctx.strokeStyle = 'rgba(226,190,110,0.9)'; ctx.lineWidth = eyeR * 0.065; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(nerveStartX, nerveStartY); ctx.lineTo(nerveEndX, nerveEndY);
                ctx.strokeStyle = 'rgba(113,63,18,0.52)'; ctx.lineWidth = 0.7; ctx.stroke();

                // Corneal dome and anterior chamber form a true bulge rather than a painted arc.
                var corneaBaseX = eyeCx - eyeRx * 0.82;
                var corneaTipX = eyeCx - eyeRx * 1.105;
                ctx.beginPath();
                ctx.moveTo(corneaBaseX, eyeCy - eyeRy * 0.48);
                ctx.bezierCurveTo(eyeCx - eyeRx * 1.03, eyeCy - eyeRy * 0.42, corneaTipX, eyeCy - eyeRy * 0.20, corneaTipX, eyeCy);
                ctx.bezierCurveTo(corneaTipX, eyeCy + eyeRy * 0.20, eyeCx - eyeRx * 1.03, eyeCy + eyeRy * 0.42, corneaBaseX, eyeCy + eyeRy * 0.48);
                ctx.bezierCurveTo(eyeCx - eyeRx * 0.88, eyeCy + eyeRy * 0.24, eyeCx - eyeRx * 0.88, eyeCy - eyeRy * 0.24, corneaBaseX, eyeCy - eyeRy * 0.48);
                ctx.closePath();
                var corneaGrad = ctx.createLinearGradient(corneaTipX, eyeCy, corneaBaseX, eyeCy);
                corneaGrad.addColorStop(0, eyeCloudy ? 'rgba(226,232,240,0.62)' : 'rgba(224,242,254,0.34)');
                corneaGrad.addColorStop(0.58, eyeCloudy ? 'rgba(203,213,225,0.48)' : 'rgba(186,230,253,0.16)');
                corneaGrad.addColorStop(1, 'rgba(125,211,252,0.05)');
                ctx.fillStyle = corneaGrad; ctx.fill(); ctx.strokeStyle = eyeAccessible ? '#38bdf8' : 'rgba(125,211,252,0.82)'; ctx.lineWidth = eyeAccessible ? 2.2 : 1.35; ctx.stroke();

                // Ciliary body, iris leaflets, and zonular fibers surround the biconvex lens.
                var lensX = eyeCx - eyeRx * 0.43;
                var lensRx = eyeRx * 0.19; var lensRy = eyeRy * 0.37;
                [-1,1].forEach(function (side) {
                  var ciliaryY = eyeCy + side * eyeRy * 0.43;
                  ctx.beginPath(); ctx.ellipse(eyeCx - eyeRx * 0.57, ciliaryY, eyeRx * 0.12, eyeRy * 0.095, side * 0.28, 0, Math.PI * 2);
                  ctx.fillStyle = eyeAccessible ? '#92400e' : '#68453c'; ctx.fill(); ctx.strokeStyle = 'rgba(45,28,28,0.72)'; ctx.lineWidth = 0.75; ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(eyeCx - eyeRx * 0.67, ciliaryY - side * eyeRy * 0.025);
                  ctx.bezierCurveTo(eyeCx - eyeRx * 0.66, eyeCy + side * eyeRy * 0.28, eyeCx - eyeRx * 0.61, eyeCy + side * eyeRy * 0.16, eyeCx - eyeRx * 0.58, eyeCy + side * eyeRy * 0.115);
                  ctx.strokeStyle = eyeAccessible ? '#78350f' : '#54352f'; ctx.lineWidth = eyeR * 0.055; ctx.stroke();
                  for (var zonule = 0; zonule < 5; zonule++) {
                    var zf = zonule / 4;
                    var zonuleStartX = eyeCx - eyeRx * (0.53 - zf * 0.025);
                    var zonuleStartY = eyeCy + side * eyeRy * (0.34 + zf * 0.055);
                    var zonuleEndX = lensX + eyeRx * (0.03 + zf * 0.08);
                    var zonuleEndY = eyeCy + side * lensRy * (0.68 + zf * 0.07);
                    ctx.beginPath(); ctx.moveTo(zonuleStartX, zonuleStartY); ctx.lineTo(zonuleEndX, zonuleEndY);
                    ctx.strokeStyle = 'rgba(226,232,240,0.48)'; ctx.lineWidth = 0.48; ctx.stroke();
                  }
                });

                var lensGrad = ctx.createRadialGradient(lensX - lensRx * 0.34, eyeCy - lensRy * 0.35, 1, lensX, eyeCy, lensRy);
                lensGrad.addColorStop(0, eyeCloudy ? 'rgba(248,250,252,0.95)' : 'rgba(255,255,255,0.82)');
                lensGrad.addColorStop(0.55, eyeCloudy ? 'rgba(203,213,225,0.88)' : 'rgba(224,242,254,0.48)');
                lensGrad.addColorStop(1, eyeCloudy ? 'rgba(148,163,184,0.75)' : 'rgba(125,211,252,0.22)');
                ctx.beginPath(); ctx.ellipse(lensX, eyeCy, lensRx, lensRy, 0, 0, Math.PI * 2);
                ctx.fillStyle = lensGrad; ctx.fill(); ctx.strokeStyle = eyeAccessible ? '#0ea5e9' : 'rgba(186,230,253,0.82)'; ctx.lineWidth = eyeAccessible ? 1.5 : 0.9; ctx.stroke();
                if (!eyeAccessible) {
                  ctx.beginPath(); ctx.ellipse(lensX - lensRx * 0.2, eyeCy - lensRy * 0.22, lensRx * 0.34, lensRy * 0.55, -0.08, Math.PI * 0.84, Math.PI * 1.52);
                  ctx.strokeStyle = 'rgba(255,255,255,0.34)'; ctx.lineWidth = 1.1; ctx.stroke();
                }

                // The iris is shown as two pigmented leaflets with an open optical aperture.
                [-1,1].forEach(function (side) {
                  ctx.beginPath();
                  ctx.moveTo(eyeCx - eyeRx * 0.68, eyeCy + side * eyeRy * 0.34);
                  ctx.bezierCurveTo(eyeCx - eyeRx * 0.72, eyeCy + side * eyeRy * 0.24, eyeCx - eyeRx * 0.70, eyeCy + side * eyeRy * 0.12, eyeCx - eyeRx * 0.63, eyeCy + side * eyeRy * 0.075);
                  ctx.bezierCurveTo(eyeCx - eyeRx * 0.58, eyeCy + side * eyeRy * 0.12, eyeCx - eyeRx * 0.59, eyeCy + side * eyeRy * 0.27, eyeCx - eyeRx * 0.63, eyeCy + side * eyeRy * 0.35);
                  ctx.closePath(); ctx.fillStyle = eyeAccessible ? '#a16207' : '#6b4a32'; ctx.fill(); ctx.strokeStyle = 'rgba(41,27,20,0.76)'; ctx.lineWidth = 0.7; ctx.stroke();
                });

                // Aqueous humor in the anterior chamber remains visibly distinct from vitreous gel.
                ctx.save(); ctx.globalAlpha = eyeCloudy ? 0.34 : 0.18;
                ctx.beginPath(); ctx.ellipse(eyeCx - eyeRx * 0.74, eyeCy, eyeRx * 0.19, eyeRy * 0.34, 0, 0, Math.PI * 2);
                ctx.fillStyle = eyeCloudy ? '#cbd5e1' : '#7dd3fc'; ctx.fill(); ctx.restore();

                // Optic disc and retinal vessels emerge where the nerve exits the globe.
                var opticDiscX = eyeCx + eyeRx * 0.76; var opticDiscY = eyeCy + eyeRy * 0.10;
                ctx.beginPath(); ctx.ellipse(opticDiscX, opticDiscY, eyeR * 0.035, eyeR * 0.026, 0.2, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(253,186,116,0.86)'; ctx.fill(); ctx.strokeStyle = 'rgba(154,52,18,0.55)'; ctx.lineWidth = 0.55; ctx.stroke();
                ctx.save(); ctx.globalAlpha = 0.46; ctx.strokeStyle = '#dc2626'; ctx.lineWidth = 0.7;
                [-0.55,-0.18,0.20,0.52].forEach(function (vesselOffset) {
                  ctx.beginPath(); ctx.moveTo(opticDiscX, opticDiscY);
                  ctx.bezierCurveTo(eyeCx + eyeRx * 0.48, eyeCy + eyeRy * vesselOffset * 0.44, eyeCx + eyeRx * 0.12, eyeCy + eyeRy * vesselOffset * 0.72, eyeCx - eyeRx * 0.05, eyeCy + eyeRy * vesselOffset * 0.80); ctx.stroke();
                });
                ctx.restore();

                // Sheep retinal specialization is represented as an area centralis, not a human fovea/macula.
                ctx.beginPath(); ctx.ellipse(eyeCx + eyeRx * 0.52, eyeCy - eyeRy * 0.02, eyeRx * 0.075, eyeRy * 0.038, -0.08, 0, Math.PI * 2);
                ctx.strokeStyle = 'rgba(250,204,21,0.42)'; ctx.lineWidth = 0.75; ctx.stroke();
                if (!d.quizMode) {
                  ctx.font = '6px Inter, system-ui'; ctx.fillStyle = 'rgba(254,240,138,0.58)';
                  ctx.fillText('Area centralis', eyeCx + eyeRx * 0.43, eyeCy - eyeRy * 0.08);
                  ctx.fillStyle = 'rgba(253,186,116,0.58)'; ctx.fillText('Optic disc', opticDiscX + 5, opticDiscY + 2);
                }

                // Animated rays retain the optics lesson while respecting reduced-motion preferences.
                var rayPhase = dissMotionReduced ? 0 : (dissTick * 0.02) % (Math.PI * 2);
                var rayAlpha = eyeAccessible ? 0.72 : 0.43 + Math.sin(rayPhase) * 0.08;
                var corneaHitX = eyeCx - eyeRx * 1.02;
                var pupilX = eyeCx - eyeRx * 0.63;
                var focusX = eyeCx + eyeRx * 0.79;
                ctx.save(); ctx.globalAlpha = rayAlpha; ctx.strokeStyle = '#fbbf24'; ctx.lineWidth = eyeAccessible ? 1.8 : 1.15;
                ctx.beginPath(); ctx.moveTo(eyeCx - eyeRx * 1.47, eyeCy - eyeRy * 0.24); ctx.lineTo(corneaHitX, eyeCy - eyeRy * 0.07); ctx.stroke();
                ctx.beginPath(); ctx.moveTo(corneaHitX, eyeCy - eyeRy * 0.07); ctx.quadraticCurveTo(pupilX, eyeCy - eyeRy * 0.02, lensX, eyeCy); ctx.stroke();
                ctx.beginPath(); ctx.moveTo(lensX, eyeCy); ctx.lineTo(focusX, eyeCy + eyeRy * 0.035); ctx.stroke();
                var focusGlow = ctx.createRadialGradient(focusX, eyeCy + eyeRy * 0.035, 0, focusX, eyeCy + eyeRy * 0.035, 5);
                focusGlow.addColorStop(0, 'rgba(251,191,36,0.82)'); focusGlow.addColorStop(1, 'rgba(251,191,36,0)');
                ctx.beginPath(); ctx.arc(focusX, eyeCy + eyeRy * 0.035, 5, 0, Math.PI * 2); ctx.fillStyle = focusGlow; ctx.fill();
                if (!d.quizMode) {
                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#fbbf24'; ctx.fillText('Refracted light', eyeCx - eyeRx * 1.46, eyeCy - eyeRy * 0.29);
                }
                ctx.restore();

                if (eyeRealistic && !eyeCloudy) {
                  ctx.beginPath(); ctx.ellipse(eyeCx - eyeRx * 0.86, eyeCy - eyeRy * 0.18, eyeRx * 0.10, eyeRy * 0.17, -0.24, Math.PI * 0.84, Math.PI * 1.52);
                  ctx.strokeStyle = 'rgba(255,255,255,0.32)'; ctx.lineWidth = 1.15; ctx.stroke();
                }


              } else if (spec.bodyShape === 'heart') {
                // Sheep heart: asymmetric ventricular mass, atrial appendages, and anatomically routed great vessels.
                var heartMotionActive = livingFunctionEnabled && !dissMotionReduced;
                var heartScale = heartMotionActive ? 1 + livingWave * 0.018 : 1;
                var heartAccessible = (d.visualRealism || visualRealism) === 'accessible';
                var heartRealistic = (d.visualRealism || visualRealism) === 'realistic';

                function traceSheepHeartBody() {
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.055, cy + H * 0.29);
                  ctx.bezierCurveTo(cx - W * 0.145, cy + H * 0.235, cx - W * 0.235, cy + H * 0.095, cx - W * 0.215, cy - H * 0.035);
                  ctx.bezierCurveTo(cx - W * 0.205, cy - H * 0.125, cx - W * 0.145, cy - H * 0.205, cx - W * 0.065, cy - H * 0.20);
                  ctx.bezierCurveTo(cx - W * 0.015, cy - H * 0.245, cx + W * 0.095, cy - H * 0.225, cx + W * 0.155, cy - H * 0.155);
                  ctx.bezierCurveTo(cx + W * 0.235, cy - H * 0.075, cx + W * 0.215, cy + H * 0.08, cx + W * 0.115, cy + H * 0.185);
                  ctx.bezierCurveTo(cx + W * 0.055, cy + H * 0.25, cx - W * 0.015, cy + H * 0.295, cx - W * 0.055, cy + H * 0.29);
                  ctx.closePath();
                }

                ctx.save();
                ctx.translate(cx, cy); ctx.scale(heartScale, heartScale); ctx.translate(-cx, -cy);

                // Great vessels are layered behind the myocardial mass and retain lumen depth.
                ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                var aortaGrad = ctx.createLinearGradient(cx - W * 0.08, cy - H * 0.36, cx + W * 0.16, cy - H * 0.20);
                aortaGrad.addColorStop(0, '#7f1d1d'); aortaGrad.addColorStop(0.46, '#dc2626'); aortaGrad.addColorStop(1, '#991b1b');
                ctx.beginPath(); ctx.moveTo(cx - W * 0.035, cy - H * 0.12);
                ctx.bezierCurveTo(cx - W * 0.045, cy - H * 0.25, cx - W * 0.02, cy - H * 0.34, cx + W * 0.055, cy - H * 0.34);
                ctx.bezierCurveTo(cx + W * 0.125, cy - H * 0.34, cx + W * 0.155, cy - H * 0.285, cx + W * 0.155, cy - H * 0.225);
                ctx.strokeStyle = 'rgba(69,10,10,0.9)'; ctx.lineWidth = W * 0.043; ctx.stroke();
                ctx.strokeStyle = aortaGrad; ctx.lineWidth = W * 0.032; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx + W * 0.025, cy - H * 0.335); ctx.lineTo(cx + W * 0.015, cy - H * 0.405);
                ctx.moveTo(cx + W * 0.070, cy - H * 0.335); ctx.lineTo(cx + W * 0.076, cy - H * 0.405);
                ctx.moveTo(cx + W * 0.112, cy - H * 0.31); ctx.lineTo(cx + W * 0.14, cy - H * 0.375);
                ctx.strokeStyle = '#b91c1c'; ctx.lineWidth = W * 0.013; ctx.stroke();

                var pulmGrad = ctx.createLinearGradient(cx + W * 0.015, cy - H * 0.12, cx + W * 0.17, cy - H * 0.30);
                pulmGrad.addColorStop(0, '#1e3a8a'); pulmGrad.addColorStop(0.52, '#2563eb'); pulmGrad.addColorStop(1, '#1e40af');
                ctx.beginPath(); ctx.moveTo(cx + W * 0.035, cy - H * 0.08); ctx.bezierCurveTo(cx + W * 0.055, cy - H * 0.19, cx + W * 0.09, cy - H * 0.26, cx + W * 0.145, cy - H * 0.29);
                ctx.strokeStyle = 'rgba(23,37,84,0.88)'; ctx.lineWidth = W * 0.038; ctx.stroke(); ctx.strokeStyle = pulmGrad; ctx.lineWidth = W * 0.027; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx + W * 0.135, cy - H * 0.282); ctx.quadraticCurveTo(cx + W * 0.20, cy - H * 0.29, cx + W * 0.235, cy - H * 0.255);
                ctx.moveTo(cx + W * 0.13, cy - H * 0.286); ctx.quadraticCurveTo(cx + W * 0.09, cy - H * 0.34, cx + W * 0.045, cy - H * 0.35);
                ctx.strokeStyle = pulmGrad; ctx.lineWidth = W * 0.017; ctx.stroke();

                // Cranial and caudal venae cavae enter the right atrium from opposite directions.
                ctx.beginPath(); ctx.moveTo(cx + W * 0.17, cy - H * 0.09); ctx.lineTo(cx + W * 0.19, cy - H * 0.36);
                ctx.moveTo(cx + W * 0.16, cy + H * 0.055); ctx.lineTo(cx + W * 0.205, cy + H * 0.235);
                ctx.strokeStyle = 'rgba(30,58,138,0.92)'; ctx.lineWidth = W * 0.031; ctx.stroke();
                ctx.strokeStyle = 'rgba(59,130,246,0.78)'; ctx.lineWidth = W * 0.021; ctx.stroke();

                var myocardiumGrad = ctx.createRadialGradient(cx - W * 0.11, cy - H * 0.14, W * 0.018, cx, cy + H * 0.04, W * 0.30);
                myocardiumGrad.addColorStop(0, heartAccessible ? layerColor : '#c95f59');
                myocardiumGrad.addColorStop(0.43, heartAccessible ? layerColor : '#a83f3f');
                myocardiumGrad.addColorStop(0.78, heartAccessible ? layerColor : '#762f36');
                myocardiumGrad.addColorStop(1, heartAccessible ? layerColor : '#4c2230');
                traceSheepHeartBody(); ctx.fillStyle = myocardiumGrad; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = heartAccessible ? 2.1 : 1.35; ctx.stroke();
                ctx.shadowBlur = 0;

                // Atrial auricles break the silhouette at the base, as in a preserved mammalian heart.
                ctx.beginPath();
                ctx.moveTo(cx - W * 0.075, cy - H * 0.18);
                ctx.bezierCurveTo(cx - W * 0.13, cy - H * 0.245, cx - W * 0.205, cy - H * 0.20, cx - W * 0.19, cy - H * 0.125);
                ctx.bezierCurveTo(cx - W * 0.15, cy - H * 0.145, cx - W * 0.11, cy - H * 0.14, cx - W * 0.075, cy - H * 0.18);
                ctx.closePath(); ctx.fillStyle = heartAccessible ? layerColor : '#8f3940'; ctx.fill(); ctx.strokeStyle = layerStroke; ctx.lineWidth = 0.9; ctx.stroke();
                ctx.beginPath();
                ctx.moveTo(cx + W * 0.105, cy - H * 0.17);
                ctx.bezierCurveTo(cx + W * 0.155, cy - H * 0.225, cx + W * 0.215, cy - H * 0.17, cx + W * 0.19, cy - H * 0.105);
                ctx.bezierCurveTo(cx + W * 0.155, cy - H * 0.125, cx + W * 0.13, cy - H * 0.13, cx + W * 0.105, cy - H * 0.17);
                ctx.closePath(); ctx.fillStyle = heartAccessible ? layerColor : '#74343c'; ctx.fill(); ctx.stroke();

                // Directional myocardial fibers follow the ventricular spiral rather than a flat fill.
                if (!heartAccessible && sceneDetail) {
                  ctx.save(); traceSheepHeartBody(); ctx.clip(); ctx.globalAlpha = heartRealistic ? 0.16 : 0.1;
                  for (var fiber = 0; fiber < 18; fiber++) {
                    var fiberT = fiber / 17;
                    var fiberY = cy - H * 0.15 + fiberT * H * 0.38;
                    ctx.beginPath(); ctx.moveTo(cx - W * (0.18 - fiberT * 0.10), fiberY);
                    ctx.bezierCurveTo(cx - W * 0.04, fiberY + H * 0.045, cx + W * 0.08, fiberY + H * 0.025, cx + W * (0.17 - fiberT * 0.08), fiberY - H * 0.025);
                    ctx.strokeStyle = fiber % 3 === 0 ? 'rgba(254,202,202,0.72)' : 'rgba(69,10,10,0.62)'; ctx.lineWidth = 0.55; ctx.stroke();
                  }
                  ctx.restore();
                }

                // Interventricular and atrioventricular grooves anchor the external surface anatomy.
                ctx.beginPath(); ctx.moveTo(cx + W * 0.02, cy - H * 0.13);
                ctx.bezierCurveTo(cx - W * 0.005, cy - H * 0.01, cx - W * 0.02, cy + H * 0.13, cx - W * 0.055, cy + H * 0.275);
                ctx.strokeStyle = 'rgba(60,16,28,0.78)'; ctx.lineWidth = 2.2; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx - W * 0.17, cy - H * 0.075); ctx.bezierCurveTo(cx - W * 0.04, cy - H * 0.02, cx + W * 0.095, cy - H * 0.02, cx + W * 0.18, cy - H * 0.085);
                ctx.strokeStyle = 'rgba(69,10,10,0.62)'; ctx.lineWidth = 1.4; ctx.stroke();

                // Coronary vessels branch along the sulci with a subtle paired venous channel.
                ctx.beginPath(); ctx.moveTo(cx + W * 0.015, cy - H * 0.135);
                ctx.bezierCurveTo(cx - W * 0.015, cy - H * 0.005, cx - W * 0.02, cy + H * 0.14, cx - W * 0.055, cy + H * 0.26);
                ctx.strokeStyle = '#dc2626'; ctx.lineWidth = heartAccessible ? 2.5 : 1.65; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx + W * 0.008, cy - H * 0.09);
                ctx.bezierCurveTo(cx - W * 0.04, cy + H * 0.02, cx - W * 0.07, cy + H * 0.11, cx - W * 0.13, cy + H * 0.18);
                ctx.moveTo(cx - W * 0.005, cy + H * 0.03); ctx.quadraticCurveTo(cx + W * 0.055, cy + H * 0.095, cx + W * 0.09, cy + H * 0.17);
                ctx.strokeStyle = '#ef4444'; ctx.lineWidth = 0.9; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx + W * 0.028, cy - H * 0.115); ctx.bezierCurveTo(cx + W * 0.01, cy + H * 0.02, cx - W * 0.005, cy + H * 0.15, cx - W * 0.04, cy + H * 0.255);
                ctx.strokeStyle = 'rgba(37,99,235,0.72)'; ctx.lineWidth = 0.75; ctx.stroke();

                // Pericardial reflection follows the organ rather than a generic ellipse.
                ctx.save(); ctx.setLineDash([4,4]); traceSheepHeartBody(); ctx.strokeStyle = 'rgba(226,232,240,0.22)'; ctx.lineWidth = 0.85; ctx.stroke(); ctx.setLineDash([]); ctx.restore();
                ctx.beginPath(); ctx.moveTo(cx - W * 0.15, cy - H * 0.13); ctx.quadraticCurveTo(cx - W * 0.07, cy - H * 0.20, cx + W * 0.02, cy - H * 0.18);
                ctx.strokeStyle = 'rgba(255,255,255,0.15)'; ctx.lineWidth = 1.7; ctx.stroke();

                // Left ventricular wall remains visibly more substantial than the right.
                ctx.beginPath(); ctx.moveTo(cx - W * 0.035, cy - H * 0.08); ctx.bezierCurveTo(cx - W * 0.17, cy + H * 0.03, cx - W * 0.14, cy + H * 0.18, cx - W * 0.06, cy + H * 0.25);
                ctx.strokeStyle = 'rgba(127,29,29,0.20)'; ctx.lineWidth = 12; ctx.stroke();
                ctx.beginPath(); ctx.moveTo(cx + W * 0.04, cy - H * 0.09); ctx.bezierCurveTo(cx + W * 0.15, cy + H * 0.01, cx + W * 0.12, cy + H * 0.13, cx + W * 0.04, cy + H * 0.20);
                ctx.strokeStyle = 'rgba(30,58,138,0.13)'; ctx.lineWidth = 7; ctx.stroke();
                ctx.restore();

                // ECG display stays fixed while the specimen subtly contracts above it.
                var ecgY = H - 35; var ecgW = W * 0.6; var ecgX = (W - ecgW) / 2;
                var ecgPanel = ctx.createLinearGradient(ecgX, ecgY - 20, ecgX, ecgY + 15);
                ecgPanel.addColorStop(0, 'rgba(2,6,23,0.86)'); ecgPanel.addColorStop(1, 'rgba(15,23,42,0.66)');
                ctx.fillStyle = ecgPanel; ctx.fillRect(ecgX - 5, ecgY - 20, ecgW + 10, 35);
                ctx.strokeStyle = 'rgba(34,197,94,0.14)'; ctx.lineWidth = 0.3;
                for (var eg = 0; eg < 6; eg++) { ctx.beginPath(); ctx.moveTo(ecgX, ecgY - 15 + eg * 5); ctx.lineTo(ecgX + ecgW, ecgY - 15 + eg * 5); ctx.stroke(); }
                ctx.strokeStyle = '#22c55e'; ctx.lineWidth = 1.35; ctx.beginPath();
                for (var ep = 0; ep < ecgW; ep++) {
                  var et = ((ep + (dissMotionReduced ? 0 : dissTick * 2)) % ecgW) / ecgW;
                  var ey = ecgY;
                  if (et > 0.05 && et < 0.15) ey -= Math.sin((et - 0.05) * 10 * Math.PI) * 4;
                  else if (et > 0.20 && et < 0.22) ey += (et - 0.20) * 200;
                  else if (et > 0.22 && et < 0.26) ey -= 15 - (et - 0.22) * 375;
                  else if (et > 0.26 && et < 0.28) ey += (et - 0.26) * 150;
                  else if (et > 0.35 && et < 0.50) ey -= Math.sin((et - 0.35) * 6.67 * Math.PI) * 5;
                  ep === 0 ? ctx.moveTo(ecgX + ep, ey) : ctx.lineTo(ecgX + ep, ey);
                }
                ctx.stroke();
                var bpm = dissMotionReduced ? 72 : 72 + Math.floor(Math.sin(dissTick * 0.02) * 5);
                if (!d.quizMode) {
                  ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = '#22c55e'; ctx.fillText(bpm + ' BPM', ecgX + ecgW + 8, ecgY);
                  ctx.font = '6px Inter, system-ui'; ctx.fillStyle = 'rgba(34,197,94,0.58)';
                  ctx.fillText('P', ecgX + ecgW * 0.10, ecgY - 18); ctx.fillText('QRS', ecgX + ecgW * 0.23, ecgY - 18); ctx.fillText('T', ecgX + ecgW * 0.42, ecgY - 18);
                }
                // Conduction system animation

                if (!d.quizMode && (activeLayer === 'nervous' || activeLayer === 'conduction' || (activeLayer === 'organs' && d.selectedOrgan === 'conduction'))) {

                  ctx.globalAlpha = 0.7;

                  var condPhase = (dissTick * 0.03) % 1;

                  // SA Node (pacemaker)

                  var saGlow = Math.max(0, Math.sin(condPhase * Math.PI * 2));

                  ctx.beginPath(); ctx.arc(cx + W * 0.12, cy - H * 0.14, 5 + saGlow * 3, 0, Math.PI * 2);

                  var saGrad = ctx.createRadialGradient(cx + W * 0.12, cy - H * 0.14, 0, cx + W * 0.12, cy - H * 0.14, 5 + saGlow * 3);

                  saGrad.addColorStop(0, 'rgba(251,191,36,' + (0.5 + saGlow * 0.5) + ')');

                  saGrad.addColorStop(1, 'rgba(251,191,36,0)');

                  ctx.fillStyle = saGrad; ctx.fill();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#fbbf24'; ctx.fillText('SA Node', cx + W * 0.13, cy - H * 0.18);

                  // AV Node

                  var avDelay = Math.max(0, Math.sin((condPhase - 0.15) * Math.PI * 2));

                  ctx.beginPath(); ctx.arc(cx, cy - H * 0.04, 4 + avDelay * 2, 0, Math.PI * 2);

                  var avGrad = ctx.createRadialGradient(cx, cy - H * 0.04, 0, cx, cy - H * 0.04, 4 + avDelay * 2);

                  avGrad.addColorStop(0, 'rgba(34,197,94,' + (0.4 + avDelay * 0.5) + ')');

                  avGrad.addColorStop(1, 'rgba(34,197,94,0)');

                  ctx.fillStyle = avGrad; ctx.fill();

                  ctx.fillStyle = '#22c55e'; ctx.fillText('AV Node', cx + W * 0.02, cy - H * 0.06);

                  // Bundle of His

                  var hisPhase = Math.max(0, Math.sin((condPhase - 0.3) * Math.PI * 2));

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.02); ctx.lineTo(cx, cy + H * 0.06);

                  ctx.strokeStyle = 'rgba(59,130,246,' + (0.3 + hisPhase * 0.5) + ')'; ctx.lineWidth = 2; ctx.stroke();

                  ctx.fillStyle = '#3b82f6'; ctx.fillText('Bundle of His', cx + W * 0.02, cy + H * 0.02);

                  // Left and right bundle branches

                  ctx.beginPath(); ctx.moveTo(cx, cy + H * 0.06);

                  ctx.lineTo(cx - W * 0.08, cy + H * 0.18);

                  ctx.strokeStyle = 'rgba(59,130,246,' + (0.2 + hisPhase * 0.4) + ')'; ctx.lineWidth = 1.5; ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx, cy + H * 0.06);

                  ctx.lineTo(cx + W * 0.06, cy + H * 0.16);

                  ctx.stroke();

                  // Purkinje fibers (fan out in ventricles)

                  var purkPhase = Math.max(0, Math.sin((condPhase - 0.5) * Math.PI * 2));

                  ctx.strokeStyle = 'rgba(168,85,247,' + (0.2 + purkPhase * 0.4) + ')'; ctx.lineWidth = 0.8;

                  for (var pk = 0; pk < 5; pk++) {

                    ctx.beginPath(); ctx.moveTo(cx - W * 0.08, cy + H * 0.18);

                    ctx.lineTo(cx - W * 0.12 + pk * W * 0.02, cy + H * 0.22 + pk * H * 0.01);

                    ctx.stroke();

                    ctx.beginPath(); ctx.moveTo(cx + W * 0.06, cy + H * 0.16);

                    ctx.lineTo(cx + W * 0.02 + pk * W * 0.02, cy + H * 0.20 + pk * H * 0.01);

                    ctx.stroke();

                  }

                  ctx.fillStyle = '#a855f7'; ctx.fillText('Purkinje Fibers', cx - W * 0.14, cy + H * 0.24);

                  // Signal propagation indicator

                  var sigY = cy - H * 0.14 + condPhase * H * 0.38;

                  ctx.beginPath(); ctx.arc(cx, sigY, 3, 0, Math.PI * 2);

                  ctx.fillStyle = 'rgba(251,191,36,0.8)'; ctx.fill();

                  ctx.globalAlpha = 1;

                }

                // Internal cutaway: chamber geometry, wall thickness, valves, chordae, and trabeculae.
                if (activeLayer === 'organs' || activeLayer === 'chambers' || activeLayer === 'interior') {
                  ctx.save();
                  var heartCutawayAccessible = (d.visualRealism || visualRealism) === 'accessible';
                  var heartCutawayAlpha = heartCutawayAccessible ? 0.96 : 0.88;
                  var valvePhase = dissMotionReduced ? 0 : Math.sin(dissTick * 0.05);
                  var valveLift = Math.max(0, valvePhase) * 2.4;

                  // The opened myocardial field retains the external silhouette around every chamber.
                  traceSheepHeartBody();
                  var cutawayBed = ctx.createRadialGradient(cx - W * 0.06, cy - H * 0.04, 4, cx, cy + H * 0.05, W * 0.27);
                  cutawayBed.addColorStop(0, 'rgba(139,55,62,' + heartCutawayAlpha + ')');
                  cutawayBed.addColorStop(0.62, 'rgba(91,34,45,' + heartCutawayAlpha + ')');
                  cutawayBed.addColorStop(1, 'rgba(52,24,35,' + heartCutawayAlpha + ')');
                  ctx.fillStyle = cutawayBed; ctx.fill(); ctx.strokeStyle = 'rgba(254,202,202,0.34)'; ctx.lineWidth = 0.8; ctx.stroke();

                  // Atria sit above the atrioventricular plane with thinner, ridged walls.
                  function traceHeartAtrium(side) {
                    var atriumCx = cx + side * W * 0.085;
                    ctx.beginPath();
                    ctx.moveTo(atriumCx - W * 0.057, cy - H * 0.13);
                    ctx.bezierCurveTo(atriumCx - W * 0.052, cy - H * 0.205, atriumCx + W * 0.035, cy - H * 0.215, atriumCx + W * 0.061, cy - H * 0.145);
                    ctx.bezierCurveTo(atriumCx + W * 0.075, cy - H * 0.09, atriumCx + W * 0.045, cy - H * 0.035, atriumCx, cy - H * 0.038);
                    ctx.bezierCurveTo(atriumCx - W * 0.04, cy - H * 0.04, atriumCx - W * 0.066, cy - H * 0.08, atriumCx - W * 0.057, cy - H * 0.13);
                    ctx.closePath();
                  }
                  traceHeartAtrium(-1); ctx.fillStyle = 'rgba(104,24,32,0.94)'; ctx.fill(); ctx.strokeStyle = 'rgba(248,113,113,0.62)'; ctx.lineWidth = 1.05; ctx.stroke();
                  traceHeartAtrium(1); ctx.fillStyle = 'rgba(24,54,105,0.94)'; ctx.fill(); ctx.strokeStyle = 'rgba(96,165,250,0.64)'; ctx.stroke();
                  [-1,1].forEach(function (side) {
                    var atriumCx = cx + side * W * 0.085;
                    ctx.save(); ctx.globalAlpha = 0.34; ctx.strokeStyle = side < 0 ? '#fca5a5' : '#93c5fd'; ctx.lineWidth = 0.55;
                    for (var atrialRidge = 0; atrialRidge < 4; atrialRidge++) {
                      var ridgeY = cy - H * (0.17 - atrialRidge * 0.026);
                      ctx.beginPath(); ctx.moveTo(atriumCx - W * 0.038, ridgeY); ctx.quadraticCurveTo(atriumCx, ridgeY + H * 0.014, atriumCx + W * 0.038, ridgeY - H * 0.004); ctx.stroke();
                    }
                    ctx.restore();
                  });

                  // Left ventricular myocardium is substantially thicker than the right ventricular wall.
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.145, cy - H * 0.025);
                  ctx.bezierCurveTo(cx - W * 0.19, cy + H * 0.055, cx - W * 0.155, cy + H * 0.205, cx - W * 0.06, cy + H * 0.272);
                  ctx.bezierCurveTo(cx - W * 0.015, cy + H * 0.23, cx + W * 0.005, cy + H * 0.075, cx - W * 0.015, cy - H * 0.02);
                  ctx.closePath(); ctx.fillStyle = '#8f333d'; ctx.fill(); ctx.strokeStyle = '#541f2a'; ctx.lineWidth = 1.2; ctx.stroke();
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.112, cy + H * 0.005);
                  ctx.bezierCurveTo(cx - W * 0.135, cy + H * 0.072, cx - W * 0.112, cy + H * 0.176, cx - W * 0.061, cy + H * 0.226);
                  ctx.bezierCurveTo(cx - W * 0.03, cy + H * 0.18, cx - W * 0.023, cy + H * 0.075, cx - W * 0.035, cy + H * 0.002);
                  ctx.closePath();
                  var lvCavity = ctx.createLinearGradient(cx - W * 0.10, cy, cx - W * 0.04, cy + H * 0.22);
                  lvCavity.addColorStop(0, 'rgba(127,29,29,0.98)'); lvCavity.addColorStop(1, 'rgba(69,10,10,0.98)');
                  ctx.fillStyle = lvCavity; ctx.fill(); ctx.strokeStyle = 'rgba(254,202,202,0.44)'; ctx.lineWidth = 0.75; ctx.stroke();

                  ctx.beginPath();
                  ctx.moveTo(cx + W * 0.012, cy - H * 0.025);
                  ctx.bezierCurveTo(cx + W * 0.075, cy - H * 0.02, cx + W * 0.17, cy + H * 0.035, cx + W * 0.148, cy + H * 0.14);
                  ctx.bezierCurveTo(cx + W * 0.128, cy + H * 0.215, cx + W * 0.055, cy + H * 0.22, cx + W * 0.008, cy + H * 0.155);
                  ctx.closePath(); ctx.fillStyle = '#753543'; ctx.fill(); ctx.strokeStyle = '#49202d'; ctx.lineWidth = 0.9; ctx.stroke();
                  ctx.beginPath();
                  ctx.moveTo(cx + W * 0.038, cy + H * 0.005);
                  ctx.bezierCurveTo(cx + W * 0.092, cy + H * 0.005, cx + W * 0.138, cy + H * 0.058, cx + W * 0.12, cy + H * 0.127);
                  ctx.bezierCurveTo(cx + W * 0.105, cy + H * 0.17, cx + W * 0.058, cy + H * 0.175, cx + W * 0.026, cy + H * 0.132);
                  ctx.closePath();
                  var rvCavity = ctx.createLinearGradient(cx + W * 0.04, cy, cx + W * 0.12, cy + H * 0.15);
                  rvCavity.addColorStop(0, 'rgba(30,64,175,0.94)'); rvCavity.addColorStop(1, 'rgba(23,37,84,0.98)');
                  ctx.fillStyle = rvCavity; ctx.fill(); ctx.strokeStyle = 'rgba(147,197,253,0.42)'; ctx.lineWidth = 0.7; ctx.stroke();

                  // The interventricular septum is a muscular partition, not a single divider line.
                  ctx.beginPath();
                  ctx.moveTo(cx - W * 0.018, cy - H * 0.012);
                  ctx.bezierCurveTo(cx + W * 0.012, cy + H * 0.075, cx + W * 0.012, cy + H * 0.18, cx - W * 0.055, cy + H * 0.248);
                  ctx.bezierCurveTo(cx - W * 0.025, cy + H * 0.17, cx - W * 0.022, cy + H * 0.07, cx - W * 0.038, cy - H * 0.008);
                  ctx.closePath(); ctx.fillStyle = '#9e4147'; ctx.fill(); ctx.strokeStyle = 'rgba(69,10,10,0.7)'; ctx.lineWidth = 0.65; ctx.stroke();

                  // AV valve leaflets open toward their ventricles; mitral has two, tricuspid three.
                  var mitralX = cx - W * 0.073; var mitralY = cy - H * 0.018;
                  var tricuspidX = cx + W * 0.074; var tricuspidY = cy - H * 0.012;
                  function drawHeartValveLeaflet(x, y, width, side, color) {
                    ctx.beginPath(); ctx.moveTo(x - width, y);
                    ctx.quadraticCurveTo(x, y + side * (H * 0.024 + valveLift), x + width, y);
                    ctx.quadraticCurveTo(x, y + side * H * 0.012, x - width, y);
                    ctx.closePath(); ctx.fillStyle = color; ctx.fill(); ctx.strokeStyle = 'rgba(120,53,15,0.7)'; ctx.lineWidth = 0.45; ctx.stroke();
                  }
                  drawHeartValveLeaflet(mitralX - W * 0.012, mitralY, W * 0.027, 1, '#fde68a');
                  drawHeartValveLeaflet(mitralX + W * 0.022, mitralY, W * 0.022, 1, '#fef3c7');
                  drawHeartValveLeaflet(tricuspidX - W * 0.025, tricuspidY, W * 0.020, 1, '#dbeafe');
                  drawHeartValveLeaflet(tricuspidX, tricuspidY, W * 0.022, 1, '#bfdbfe');
                  drawHeartValveLeaflet(tricuspidX + W * 0.025, tricuspidY, W * 0.019, 1, '#dbeafe');

                  // Papillary muscles rise from the wall and anchor multiple branching chordae.
                  var heartPapillary = [
                    { x: cx - W * 0.103, y: cy + H * 0.145, valveX: mitralX - W * 0.014, valveY: mitralY + H * 0.008, color: '#b94a50' },
                    { x: cx - W * 0.048, y: cy + H * 0.165, valveX: mitralX + W * 0.020, valveY: mitralY + H * 0.008, color: '#a63e48' },
                    { x: cx + W * 0.072, y: cy + H * 0.115, valveX: tricuspidX - W * 0.016, valveY: tricuspidY + H * 0.008, color: '#75404d' },
                    { x: cx + W * 0.112, y: cy + H * 0.105, valveX: tricuspidX + W * 0.022, valveY: tricuspidY + H * 0.008, color: '#6d3948' }
                  ];
                  heartPapillary.forEach(function (papillary) {
                    ctx.beginPath(); ctx.moveTo(papillary.x - W * 0.009, papillary.y + H * 0.018);
                    ctx.quadraticCurveTo(papillary.x, papillary.y - H * 0.018, papillary.x + W * 0.009, papillary.y + H * 0.018);
                    ctx.closePath(); ctx.fillStyle = papillary.color; ctx.fill(); ctx.strokeStyle = 'rgba(69,10,10,0.55)'; ctx.lineWidth = 0.45; ctx.stroke();
                    for (var chord = -1; chord <= 1; chord++) {
                      ctx.beginPath(); ctx.moveTo(papillary.x, papillary.y - H * 0.011);
                      ctx.quadraticCurveTo((papillary.x + papillary.valveX) / 2 + chord * 2, (papillary.y + papillary.valveY) / 2, papillary.valveX + chord * W * 0.009, papillary.valveY);
                      ctx.strokeStyle = 'rgba(254,240,138,0.74)'; ctx.lineWidth = heartCutawayAccessible ? 0.75 : 0.48; ctx.stroke();
                    }
                  });

                  // Trabeculae carneae follow each ventricular wall and add cavity texture.
                  ctx.save(); ctx.globalAlpha = 0.36; ctx.lineWidth = 0.7;
                  for (var trabecula = 0; trabecula < 7; trabecula++) {
                    var trabeculaT = trabecula / 6;
                    ctx.beginPath(); ctx.moveTo(cx - W * (0.108 - trabeculaT * 0.052), cy + H * (0.045 + trabeculaT * 0.15));
                    ctx.quadraticCurveTo(cx - W * 0.075, cy + H * (0.07 + trabeculaT * 0.12), cx - W * (0.04 + trabeculaT * 0.012), cy + H * (0.055 + trabeculaT * 0.14));
                    ctx.strokeStyle = '#fca5a5'; ctx.stroke();
                    if (trabecula < 5) {
                      ctx.beginPath(); ctx.moveTo(cx + W * (0.046 + trabeculaT * 0.04), cy + H * (0.035 + trabeculaT * 0.10));
                      ctx.quadraticCurveTo(cx + W * 0.09, cy + H * (0.05 + trabeculaT * 0.10), cx + W * (0.118 - trabeculaT * 0.015), cy + H * (0.045 + trabeculaT * 0.09));
                      ctx.strokeStyle = '#93c5fd'; ctx.stroke();
                    }
                  }
                  ctx.restore();

                  // Semilunar cusps occupy the ventricular outflow tracts.
                  function drawSemilunarValve(x, y, color) {
                    [-1,0,1].forEach(function (cusp) {
                      ctx.beginPath(); ctx.arc(x + cusp * W * 0.009, y, W * 0.011, 0, Math.PI);
                      ctx.strokeStyle = color; ctx.lineWidth = heartCutawayAccessible ? 1.15 : 0.75; ctx.stroke();
                    });
                  }
                  drawSemilunarValve(cx - W * 0.022, cy - H * 0.095, '#fde68a');
                  drawSemilunarValve(cx + W * 0.052, cy - H * 0.085, '#bfdbfe');

                  // Compact labels remain inside the specimen instead of covering the tissue margins.
                  if (!d.quizMode) {
                    ctx.font = 'bold 7px Inter, system-ui';
                    ctx.fillStyle = 'rgba(254,226,226,0.82)'; ctx.fillText('LA', cx - W * 0.10, cy - H * 0.115); ctx.fillText('LV', cx - W * 0.085, cy + H * 0.105);
                    ctx.fillStyle = 'rgba(219,234,254,0.88)'; ctx.fillText('RA', cx + W * 0.075, cy - H * 0.115); ctx.fillText('RV', cx + W * 0.075, cy + H * 0.085);
                    ctx.font = '5px Inter, system-ui'; ctx.fillStyle = 'rgba(254,240,138,0.72)'; ctx.fillText('mitral', mitralX - W * 0.03, mitralY - 4);
                    ctx.fillStyle = 'rgba(219,234,254,0.72)'; ctx.fillText('tricuspid', tricuspidX - W * 0.025, tricuspidY - 4);
                  }

                  // Flow paths bend through the valves and pause when reduced motion is requested.
                  ctx.save(); ctx.setLineDash([3,5]); ctx.lineDashOffset = dissMotionReduced ? 0 : -dissTick * 0.38; ctx.globalAlpha = 0.65;
                  ctx.strokeStyle = '#60a5fa'; ctx.lineWidth = 1.25;
                  ctx.beginPath(); ctx.moveTo(cx + W * 0.105, cy - H * 0.205); ctx.bezierCurveTo(cx + W * 0.10, cy - H * 0.10, tricuspidX, tricuspidY, cx + W * 0.075, cy + H * 0.12); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(cx + W * 0.075, cy + H * 0.12); ctx.bezierCurveTo(cx + W * 0.10, cy + H * 0.04, cx + W * 0.08, cy - H * 0.045, cx + W * 0.06, cy - H * 0.15); ctx.stroke();
                  ctx.strokeStyle = '#f87171';
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.11, cy - H * 0.20); ctx.bezierCurveTo(cx - W * 0.105, cy - H * 0.10, mitralX, mitralY, cx - W * 0.075, cy + H * 0.17); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(cx - W * 0.075, cy + H * 0.17); ctx.bezierCurveTo(cx - W * 0.04, cy + H * 0.08, cx - W * 0.045, cy - H * 0.04, cx - W * 0.03, cy - H * 0.19); ctx.stroke();
                  ctx.restore();
                  ctx.restore();
                }

              }



              ctx.restore();



              // Technique completion follows the recorded specimen-specific access path without decorative particles.
              var _incisionAnim = d._incisionAnim;
              if (!d.quizMode && _incisionAnim && _incisionAnim.active) {
                var incElapsed = (Date.now() - _incisionAnim.startTick) / 500;
                if (incElapsed <= 1) {
                  var completionProcedure = ((d.procedureByLayer || {})[activeLayer]) || {};
                  var completionPath = (completionProcedure.extensionPath && completionProcedure.extensionPath.length > 1)
                    ? completionProcedure.extensionPath
                    : ((completionProcedure.incisionPath && completionProcedure.incisionPath.length > 1) ? completionProcedure.incisionPath : procedureGuidePoints());
                  var completionSamples = (completionProcedure.extensionSamples && completionProcedure.extensionSamples.length)
                    ? completionProcedure.extensionSamples : (completionProcedure.incisionSamples || []);
                  var completionSegments = [], completionLength = 0;
                  for (var completionSegmentIdx = 1; completionSegmentIdx < completionPath.length; completionSegmentIdx++) {
                    var completionFrom = completionPath[completionSegmentIdx - 1], completionTo = completionPath[completionSegmentIdx];
                    var completionDX = (completionTo.x - completionFrom.x) * W, completionDY = (completionTo.y - completionFrom.y) * H;
                    var completionSegmentLength = Math.sqrt(completionDX * completionDX + completionDY * completionDY);
                    completionSegments.push({ from: completionFrom, to: completionTo, start: completionLength, length: completionSegmentLength });
                    completionLength += completionSegmentLength;
                  }
                  var completionTarget = Math.max(0, Math.min(1, incElapsed)) * completionLength;
                  var completionSweep = completionPath.length ? [completionPath[0]] : [];
                  for (var completionBuildIdx = 0; completionBuildIdx < completionSegments.length; completionBuildIdx++) {
                    var completionSegment = completionSegments[completionBuildIdx];
                    if (completionTarget >= completionSegment.start + completionSegment.length) {
                      completionSweep.push(completionSegment.to);
                    } else {
                      var completionLocal = completionSegment.length ? Math.max(0, (completionTarget - completionSegment.start) / completionSegment.length) : 0;
                      completionSweep.push({
                        x: completionSegment.from.x + (completionSegment.to.x - completionSegment.from.x) * completionLocal,
                        y: completionSegment.from.y + (completionSegment.to.y - completionSegment.from.y) * completionLocal
                      });
                      break;
                    }
                  }
                  if (completionSweep.length > 1) {
                    var completionPressureTotal = completionSamples.reduce(function (sum, sample) { return sum + Math.max(0.08, Math.min(1, Number(sample.pressure) || 0.34)); }, 0);
                    var completionPressure = completionSamples.length ? completionPressureTotal / completionSamples.length : 0.34;
                    var completionEdgeSpread = 2.2 + completionPressure * 2.4;
                    var completionAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                    function traceTechniqueCompletionEdge(offset) {
                      ctx.beginPath();
                      completionSweep.forEach(function (completionPoint, completionPointIdx) {
                        var completionPrevious = completionSweep[Math.max(0, completionPointIdx - 1)];
                        var completionNext = completionSweep[Math.min(completionSweep.length - 1, completionPointIdx + 1)];
                        var completionTangentX = (completionNext.x - completionPrevious.x) * W;
                        var completionTangentY = (completionNext.y - completionPrevious.y) * H;
                        var completionTangentLength = Math.sqrt(completionTangentX * completionTangentX + completionTangentY * completionTangentY) || 1;
                        var completionX = completionPoint.x * W - completionTangentY / completionTangentLength * offset;
                        var completionY = completionPoint.y * H + completionTangentX / completionTangentLength * offset;
                        if (!completionPointIdx) ctx.moveTo(completionX, completionY); else ctx.lineTo(completionX, completionY);
                      });
                    }
                    ctx.save(); ctx.lineCap = 'round'; ctx.lineJoin = 'round'; ctx.globalAlpha = 0.94;
                    traceTechniqueCompletionEdge(0); ctx.strokeStyle = completionAccessible ? '#020617' : 'rgba(24,10,14,0.90)'; ctx.lineWidth = completionEdgeSpread * 2.1; ctx.stroke();
                    [-1, 1].forEach(function (completionSide) {
                      traceTechniqueCompletionEdge(completionSide * completionEdgeSpread);
                      ctx.strokeStyle = completionAccessible ? (completionSide < 0 ? '#facc15' : '#f8fafc') : (completionSide < 0 ? 'rgba(190,110,118,0.94)' : 'rgba(254,202,202,0.88)');
                      ctx.lineWidth = completionAccessible ? 2.2 : 1.25; ctx.stroke();
                    });
                    var completionTip = completionSweep[completionSweep.length - 1];
                    var completionTipPrevious = completionSweep[completionSweep.length - 2];
                    var completionTipAngle = Math.atan2((completionTip.y - completionTipPrevious.y) * H, (completionTip.x - completionTipPrevious.x) * W);
                    ctx.translate(completionTip.x * W, completionTip.y * H); ctx.rotate(completionTipAngle);
                    ctx.shadowColor = completionAccessible ? 'rgba(0,0,0,0.9)' : 'rgba(2,6,23,0.68)'; ctx.shadowBlur = completionAccessible ? 2 : 5;
                    ctx.strokeStyle = completionAccessible ? '#f8fafc' : '#64748b'; ctx.lineWidth = completionAccessible ? 4 : 3;
                    ctx.beginPath(); ctx.moveTo(-18, 0); ctx.lineTo(-6, 0); ctx.stroke(); ctx.shadowBlur = 0;
                    var completionBladeGradient = ctx.createLinearGradient(-7, -5, 5, 4);
                    completionBladeGradient.addColorStop(0, '#f8fafc'); completionBladeGradient.addColorStop(0.55, '#94a3b8'); completionBladeGradient.addColorStop(1, '#475569');
                    ctx.fillStyle = completionBladeGradient; ctx.strokeStyle = completionAccessible ? '#020617' : 'rgba(226,232,240,0.82)'; ctx.lineWidth = 1;
                    ctx.beginPath(); ctx.moveTo(-7, -4.5); ctx.lineTo(5, 0); ctx.lineTo(-7, 4.5); ctx.closePath(); ctx.fill(); ctx.stroke();
                    ctx.restore();
                  }
                }
              }

              // ── Draw organ pins ──
              // Progressive exposure veil lets covered anatomy remain spatially legible without appearing dissected or selectable.
              function drawAnatomicalExposureVeil() {
                if (!sceneDetail || currentLayerIdx === 0) return;
                var exposureData = procedureExposureData(canvasProcedure);
                if (exposureData.score >= 94) return;
                var veilAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var veilProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var veilAspect = spec.bodyShape === 'worm' ? 1.48 : ((spec.bodyShape === 'pig' || spec.bodyShape === 'fish' || spec.bodyShape === 'crayfish') ? 1.22 : 1);
                var veilColor = currentLayerDef.id === 'muscle' ? '112,45,53'
                  : (currentLayerDef.id === 'skeleton' ? '148,163,184'
                    : (currentLayerDef.id === 'organs' || currentLayerDef.id === 'chambers' || currentLayerDef.id === 'interior' ? '92,49,56' : '142,91,82'));
                var veilStrength = Math.max(0.18, Math.min(0.92, 1 - exposureData.score / 112));
                ctx.save();
                ctx.beginPath(); ctx.ellipse(veilProfile.cx * W, veilProfile.cy * H, veilProfile.rx * W, veilProfile.ry * H, 0, 0, Math.PI * 2); ctx.clip();
                organs.forEach(function (organ, organIndex) {
                  var exposureState = structureExposureState(organ, canvasProcedure);
                  if (exposureState === 'visible') return;
                  var exposurePoint = variedOrganPoint(organ);
                  var exposureX = exposurePoint.x * W, exposureY = exposurePoint.y * H;
                  var exposureDepth = structureOpticalDepth(organ);
                  var exposureRadius = Math.max(12, Math.min(24, Math.min(W, H) * (exposureDepth === 'deep' ? 0.044 : (exposureDepth === 'surface' ? 0.027 : 0.036))));
                  var stateStrength = exposureState === 'emerging' ? 0.52 : (exposureState === 'obscured' ? 0.82 : 0.94);
                  var localAlpha = Math.min(0.92, veilStrength * stateStrength + (exposureState === 'occluded' ? 0.18 : 0));
                  ctx.save(); ctx.translate(exposureX, exposureY); ctx.scale(veilAspect, 1);
                  if (veilAccessible) {
                    // A solid mask keeps covered structures genuinely covered; rings and hatching carry the same meaning without color.
                    ctx.fillStyle = exposureState === 'emerging' ? 'rgba(71,85,105,0.76)' : '#334155';
                    ctx.beginPath(); ctx.arc(0, 0, exposureRadius * (exposureState === 'emerging' ? 1.05 : 1.18), 0, Math.PI * 2); ctx.fill();
                  } else {
                    var exposureVeil = ctx.createRadialGradient(0, 0, exposureRadius * 0.08, 0, 0, exposureRadius * 1.32);
                    exposureVeil.addColorStop(0, 'rgba(' + veilColor + ',' + localAlpha + ')');
                    exposureVeil.addColorStop(0.62, 'rgba(' + veilColor + ',' + (localAlpha * 0.72) + ')');
                    exposureVeil.addColorStop(1, 'rgba(' + veilColor + ',0)');
                    ctx.fillStyle = exposureVeil; ctx.beginPath(); ctx.arc(0, 0, exposureRadius * 1.32, 0, Math.PI * 2); ctx.fill();
                  }
                  ctx.strokeStyle = veilAccessible ? '#f8fafc' : 'rgba(254,226,226,' + (exposureState === 'emerging' ? '0.72' : '0.34') + ')';
                  ctx.lineWidth = veilAccessible ? 2.2 / veilAspect : 1 / veilAspect;
                  if (exposureState === 'emerging') {
                    ctx.setLineDash([4 / veilAspect, 3 / veilAspect]);
                    ctx.beginPath(); ctx.arc(0, 0, exposureRadius, -1.18, 1.18); ctx.stroke(); ctx.setLineDash([]);
                    ctx.strokeStyle = veilAccessible ? '#0f172a' : 'rgba(15,23,42,0.48)';
                    ctx.beginPath(); ctx.arc(0, 0, exposureRadius * 0.72, 1.70, 4.56); ctx.stroke();
                  } else {
                    ctx.beginPath(); ctx.arc(0, 0, exposureRadius * 0.92, 0, Math.PI * 2); ctx.stroke();
                    ctx.globalAlpha = veilAccessible ? 0.9 : 0.38;
                    ctx.beginPath(); ctx.arc(0, 0, exposureRadius * 0.86, 0, Math.PI * 2); ctx.clip();
                    ctx.strokeStyle = veilAccessible ? '#0f172a' : 'rgba(71,85,105,0.58)'; ctx.lineWidth = 0.75 / veilAspect;
                    for (var veilHatch = -2; veilHatch <= 2; veilHatch++) {
                      var hatchOffset = veilHatch * exposureRadius * 0.34;
                      ctx.beginPath(); ctx.moveTo(-exposureRadius, hatchOffset - exposureRadius * 0.48); ctx.lineTo(exposureRadius, hatchOffset + exposureRadius * 0.48); ctx.stroke();
                    }
                  }
                  ctx.restore();
                });
                ctx.restore();
              }
              drawAnatomicalExposureVeil();

              // Responsive tissue relief gives visible structures elevation, recess, and contact shadow before labels are drawn.
              function drawSpecimenTissueRelief() {
                if (!tissueReliefEnabled || !sceneDetail) return;
                var reliefMode = d.visualRealism || visualRealism;
                var reliefAccessible = highContrastEnabled || reliefMode === 'accessible';
                var reliefProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var reliefCenterX = reliefProfile.cx * W, reliefCenterY = reliefProfile.cy * H;
                var reliefRadiusX = reliefProfile.rx * W, reliefRadiusY = reliefProfile.ry * H;
                var reliefPointer = canvas._toolPointer;
                var reliefDirection = {
                  overhead: { x: 0.50, y: 0.12 }, left: { x: 0.12, y: 0.38 },
                  right: { x: 0.88, y: 0.38 }, raking: { x: 0.18, y: 0.82 }
                }[lightDirection] || { x: 0.50, y: 0.12 };
                var reliefLightX = (reliefPointer ? reliefPointer.x : reliefDirection.x) * W;
                var reliefLightY = (reliefPointer ? reliefPointer.y : reliefDirection.y) * H;
                var reliefBaseRadius = Math.max(8, Math.min(18, Math.min(W, H) * (organs.length >= 10 ? 0.024 : 0.032)));
                var reliefAspect = spec.bodyShape === 'worm' ? 1.42 : ((spec.bodyShape === 'pig' || spec.bodyShape === 'fish' || spec.bodyShape === 'crayfish') ? 1.24 : 1);
                ctx.save(); ctx.beginPath(); ctx.ellipse(reliefCenterX, reliefCenterY, reliefRadiusX, reliefRadiusY, 0, 0, Math.PI * 2); ctx.clip();
                organs.filter(function (organ) { return structureExposureState(organ, currentProcedure) === 'visible'; }).forEach(function (organ) {
                  var reliefPoint = variedOrganPoint(organ);
                  var reliefX = reliefPoint.x * W, reliefY = reliefPoint.y * H;
                  var reliefDepth = structureOpticalDepth(organ);
                  var reliefSelected = d.selectedOrgan === organ.id, reliefHovered = !reliefSelected && d.hoveredOrgan === organ.id;
                  if (reliefAccessible && !reliefSelected && !reliefHovered) return;
                  var lightAngle = Math.atan2(reliefLightY - reliefY, reliefLightX - reliefX);
                  var lightOffset = reliefDepth === 'surface' ? 0.20 : (reliefDepth === 'deep' ? 0.10 : 0.16);
                  var reliefRadius = reliefBaseRadius * (reliefDepth === 'deep' ? 1.20 : (reliefDepth === 'surface' ? 0.78 : 1));
                  if (reliefSelected || reliefHovered) reliefRadius *= 1.18;
                  var highlightX = Math.cos(lightAngle) * reliefRadius * lightOffset;
                  var highlightY = Math.sin(lightAngle) * reliefRadius * lightOffset;
                  var shadowX = -Math.cos(lightAngle) * reliefRadius * (reliefDepth === 'deep' ? 0.08 : 0.18);
                  var shadowY = -Math.sin(lightAngle) * reliefRadius * (reliefDepth === 'deep' ? 0.08 : 0.18);
                  ctx.save(); ctx.translate(reliefX, reliefY); ctx.scale(reliefAspect, 1);
                  if (reliefAccessible) {
                    ctx.strokeStyle = '#ffffff'; ctx.lineWidth = 2.2 / reliefAspect;
                    ctx.beginPath(); ctx.arc(0, 0, reliefRadius, lightAngle - 1.05, lightAngle + 1.05); ctx.stroke();
                    ctx.strokeStyle = '#0f172a'; ctx.setLineDash(reliefDepth === 'deep' ? [2, 2] : []);
                    ctx.beginPath(); ctx.arc(0, 0, reliefRadius + 3, lightAngle + 2.10, lightAngle + 4.18); ctx.stroke(); ctx.setLineDash([]);
                  } else {
                    ctx.globalAlpha = reliefMode === 'realistic' ? 0.92 : 0.66;
                    ctx.globalCompositeOperation = 'multiply';
                    var reliefShadow = ctx.createRadialGradient(shadowX, shadowY, reliefRadius * 0.08, shadowX, shadowY, reliefRadius * 1.18);
                    if (reliefDepth === 'deep') {
                      reliefShadow.addColorStop(0, 'rgba(2,6,23,0.02)'); reliefShadow.addColorStop(0.52, 'rgba(2,6,23,0.17)'); reliefShadow.addColorStop(1, 'rgba(2,6,23,0)');
                    } else {
                      reliefShadow.addColorStop(0, 'rgba(2,6,23,0.22)'); reliefShadow.addColorStop(0.44, 'rgba(2,6,23,0.10)'); reliefShadow.addColorStop(1, 'rgba(2,6,23,0)');
                    }
                    ctx.fillStyle = reliefShadow; ctx.beginPath(); ctx.arc(0, 0, reliefRadius * 1.18, 0, Math.PI * 2); ctx.fill();
                    ctx.globalCompositeOperation = 'screen';
                    var reliefHighlight = ctx.createRadialGradient(highlightX, highlightY, 0, highlightX, highlightY, reliefRadius * (reliefDepth === 'surface' ? 0.84 : 0.66));
                    reliefHighlight.addColorStop(0, 'rgba(255,255,255,' + (reliefSelected ? '0.38' : (reliefDepth === 'surface' ? '0.25' : '0.17')) + ')');
                    reliefHighlight.addColorStop(0.42, 'rgba(224,242,254,0.08)'); reliefHighlight.addColorStop(1, 'rgba(255,255,255,0)');
                    ctx.fillStyle = reliefHighlight; ctx.beginPath(); ctx.arc(0, 0, reliefRadius, 0, Math.PI * 2); ctx.fill();
                    ctx.globalCompositeOperation = 'source-over';
                    ctx.strokeStyle = 'rgba(255,255,255,' + (reliefSelected || reliefHovered ? '0.42' : '0.18') + ')'; ctx.lineWidth = (reliefSelected ? 1.35 : 0.72) / reliefAspect;
                    ctx.beginPath(); ctx.arc(0, 0, reliefRadius * 0.78, lightAngle - 0.92, lightAngle + 0.92); ctx.stroke();
                    if (reliefDepth === 'deep') {
                      ctx.strokeStyle = 'rgba(15,23,42,0.34)'; ctx.setLineDash([1.5, 2.5]);
                      ctx.beginPath(); ctx.arc(0, 0, reliefRadius * 0.92, lightAngle + 1.70, lightAngle + 4.58); ctx.stroke(); ctx.setLineDash([]);
                    }
                  }
                  ctx.restore();
                });
                ctx.restore();
              }
              drawSpecimenTissueRelief();

              // Specimen-shaped layer transition opens along the dominant body axis to reveal the new anatomy beneath.
              // Spatial navigation overlays show orientation turns and layer depth changes without moving landmarks away from their hit targets.
              var _viewTransition = d._viewTransition;
              var viewTransitionProgress = 1;
              if (_viewTransition && _viewTransition.active) {
                var viewTransitionElapsed = Math.max(0, Date.now() - (_viewTransition.startedAt || Date.now()));
                var viewTransitionDuration = Math.max(1, Number(_viewTransition.duration) || 820);
                var viewTransitionLinear = Math.max(0, Math.min(1, viewTransitionElapsed / viewTransitionDuration));
                viewTransitionProgress = _viewTransition.reducedMotion || dissMotionReduced ? 1 : viewTransitionLinear * viewTransitionLinear * (3 - 2 * viewTransitionLinear);
                if (!_viewTransition.reducedMotion && !dissMotionReduced && viewTransitionLinear < 1) {
                  var viewTransitionProfile = SPECIMEN_MATERIAL_PROFILES[_viewTransition.specimenShape] || SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                  var viewTransitionCX = viewTransitionProfile.cx * W, viewTransitionCY = viewTransitionProfile.cy * H;
                  var viewTransitionRX = viewTransitionProfile.rx * W * 1.03, viewTransitionRY = viewTransitionProfile.ry * H * 1.03;
                  var viewTransitionHorizontal = viewTransitionRX > viewTransitionRY * 1.18;
                  var viewTurnPhase = Math.sin(viewTransitionProgress * Math.PI);
                  ctx.save(); ctx.beginPath(); ctx.ellipse(viewTransitionCX, viewTransitionCY, viewTransitionRX, viewTransitionRY, 0, 0, Math.PI * 2); ctx.clip();
                  var viewSweepPosition = viewTransitionProgress * 2 - 1;
                  var viewTurnGradient = viewTransitionHorizontal
                    ? ctx.createLinearGradient(0, viewTransitionCY - viewTransitionRY, 0, viewTransitionCY + viewTransitionRY)
                    : ctx.createLinearGradient(viewTransitionCX - viewTransitionRX, 0, viewTransitionCX + viewTransitionRX, 0);
                  viewTurnGradient.addColorStop(0, 'rgba(56,189,248,' + (0.04 + viewTurnPhase * 0.12).toFixed(3) + ')');
                  viewTurnGradient.addColorStop(Math.max(0.02, Math.min(0.98, viewTransitionProgress)), 'rgba(248,250,252,' + (0.08 + viewTurnPhase * 0.18).toFixed(3) + ')');
                  viewTurnGradient.addColorStop(1, 'rgba(15,23,42,' + (viewTurnPhase * 0.16).toFixed(3) + ')');
                  ctx.fillStyle = viewTurnGradient; ctx.fillRect(viewTransitionCX - viewTransitionRX, viewTransitionCY - viewTransitionRY, viewTransitionRX * 2, viewTransitionRY * 2);
                  var viewSweepX = viewTransitionHorizontal ? viewTransitionCX : viewTransitionCX + viewSweepPosition * viewTransitionRX;
                  var viewSweepY = viewTransitionHorizontal ? viewTransitionCY + viewSweepPosition * viewTransitionRY : viewTransitionCY;
                  ctx.strokeStyle = highContrastEnabled ? '#ffffff' : '#7dd3fc'; ctx.lineWidth = highContrastEnabled ? 2.8 : 1.6;
                  ctx.beginPath();
                  if (viewTransitionHorizontal) { ctx.moveTo(viewTransitionCX - viewTransitionRX, viewSweepY); ctx.quadraticCurveTo(viewTransitionCX, viewSweepY - 8 * viewTurnPhase, viewTransitionCX + viewTransitionRX, viewSweepY); }
                  else { ctx.moveTo(viewSweepX, viewTransitionCY - viewTransitionRY); ctx.quadraticCurveTo(viewSweepX + 8 * viewTurnPhase, viewTransitionCY, viewSweepX, viewTransitionCY + viewTransitionRY); }
                  ctx.stroke();
                  ctx.setLineDash([4, 4]); ctx.globalAlpha = 0.58; ctx.beginPath(); ctx.ellipse(viewTransitionCX, viewTransitionCY, viewTransitionRX * 0.93, viewTransitionRY * 0.93, 0, Math.PI, Math.PI * 2); ctx.stroke();
                  ctx.setLineDash([]); ctx.globalAlpha = 0.92; ctx.beginPath(); ctx.ellipse(viewTransitionCX, viewTransitionCY, viewTransitionRX * 0.93, viewTransitionRY * 0.93, 0, 0, Math.PI); ctx.stroke();
                  var viewStartX = viewTransitionHorizontal ? viewTransitionCX - viewTransitionRX * 0.78 : viewTransitionCX;
                  var viewStartY = viewTransitionHorizontal ? viewTransitionCY : viewTransitionCY - viewTransitionRY * 0.78;
                  var viewTargetX = viewTransitionHorizontal ? viewTransitionCX + viewTransitionRX * 0.78 : viewTransitionCX;
                  var viewTargetY = viewTransitionHorizontal ? viewTransitionCY : viewTransitionCY + viewTransitionRY * 0.78;
                  ctx.fillStyle = highContrastEnabled ? '#ffffff' : '#bae6fd'; ctx.beginPath(); ctx.arc(viewStartX, viewStartY, 3.2, 0, Math.PI * 2); ctx.fill();
                  ctx.save(); ctx.translate(viewTargetX, viewTargetY); ctx.rotate(Math.PI / 4); ctx.fillRect(-3, -3, 6, 6); ctx.restore(); ctx.restore();
                }
              }
              var _layerBrowseTransition = d._layerBrowseTransition;
              var layerBrowseProgress = 1;
              if (_layerBrowseTransition && _layerBrowseTransition.active) {
                var layerBrowseElapsed = Math.max(0, Date.now() - (_layerBrowseTransition.startedAt || Date.now()));
                var layerBrowseDuration = Math.max(1, Number(_layerBrowseTransition.duration) || 720);
                var layerBrowseLinear = Math.max(0, Math.min(1, layerBrowseElapsed / layerBrowseDuration));
                layerBrowseProgress = _layerBrowseTransition.reducedMotion || dissMotionReduced ? 1 : 1 - Math.pow(1 - layerBrowseLinear, 3);
                if (!_layerBrowseTransition.reducedMotion && !dissMotionReduced && layerBrowseLinear < 1) {
                  var layerBrowseProfile = SPECIMEN_MATERIAL_PROFILES[_layerBrowseTransition.specimenShape] || SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                  var layerBrowseCX = layerBrowseProfile.cx * W, layerBrowseCY = layerBrowseProfile.cy * H;
                  var layerBrowseRX = layerBrowseProfile.rx * W, layerBrowseRY = layerBrowseProfile.ry * H;
                  var layerBrowseDeeper = _layerBrowseTransition.direction === 'deeper';
                  ctx.save(); ctx.beginPath(); ctx.ellipse(layerBrowseCX, layerBrowseCY, layerBrowseRX * 1.03, layerBrowseRY * 1.03, 0, 0, Math.PI * 2); ctx.clip();
                  for (var layerDepthRing = 0; layerDepthRing < 4; layerDepthRing++) {
                    var layerDepthPhase = Math.max(0, Math.min(1, layerBrowseProgress + layerDepthRing * 0.12));
                    var layerDepthScale = layerBrowseDeeper ? 1 - layerDepthPhase * 0.54 : 0.46 + layerDepthPhase * 0.54;
                    ctx.globalAlpha = Math.max(0.12, 0.76 - layerDepthRing * 0.13) * (1 - layerBrowseLinear * 0.42);
                    ctx.strokeStyle = highContrastEnabled ? '#ffffff' : (layerBrowseDeeper ? '#c4b5fd' : '#67e8f9');
                    ctx.lineWidth = highContrastEnabled ? 2.5 : 1.5; ctx.setLineDash(layerDepthRing % 2 ? [3, 3] : []);
                    ctx.beginPath(); ctx.ellipse(layerBrowseCX, layerBrowseCY, layerBrowseRX * layerDepthScale, layerBrowseRY * layerDepthScale, 0, 0, Math.PI * 2); ctx.stroke();
                  }
                  ctx.setLineDash([]); ctx.globalAlpha = 1; ctx.fillStyle = highContrastEnabled ? '#ffffff' : (layerBrowseDeeper ? '#ddd6fe' : '#a5f3fc');
                  if (layerBrowseDeeper) { ctx.save(); ctx.translate(layerBrowseCX, layerBrowseCY); ctx.rotate(Math.PI / 4); ctx.fillRect(-4, -4, 8, 8); ctx.restore(); }
                  else { ctx.beginPath(); ctx.arc(layerBrowseCX, layerBrowseCY, 4, 0, Math.PI * 2); ctx.fill(); }
                  ctx.restore();
                }
              }
              var _layerTransition = d._layerTransition;
              var layerTransitionProgress = 1;
              if (_layerTransition && _layerTransition.active) {
                var transitionElapsed = Math.max(0, Date.now() - (_layerTransition.startedAt || Date.now()));
                var transitionDuration = Math.max(1, Number(_layerTransition.duration) || 1150);
                var transitionLinear = Math.max(0, Math.min(1, transitionElapsed / transitionDuration));
                layerTransitionProgress = _layerTransition.reducedMotion || dissMotionReduced ? 1 : 1 - Math.pow(1 - transitionLinear, 3);
                var transitionProfile = SPECIMEN_MATERIAL_PROFILES[_layerTransition.specimenShape] || SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var transitionCX = transitionProfile.cx * W, transitionCY = transitionProfile.cy * H;
                var transitionRX = transitionProfile.rx * W * 1.02, transitionRY = transitionProfile.ry * H * 1.02;
                var transitionHorizontalBody = transitionRX > transitionRY * 1.18;
                var transitionFromColor = String(_layerTransition.fromColor || '#8f5960');
                var transitionToColor = String(_layerTransition.toColor || '#fbbf24');
                if (transitionFromColor.indexOf('var(') === 0) transitionFromColor = '#94a3b8';
                if (transitionToColor.indexOf('var(') === 0) transitionToColor = '#cbd5e1';
                ctx.save();
                ctx.beginPath(); ctx.ellipse(transitionCX, transitionCY, transitionRX, transitionRY, 0, 0, Math.PI * 2); ctx.clip();
                var transitionSheet = ctx.createRadialGradient(transitionCX - transitionRX * 0.18, transitionCY - transitionRY * 0.22, 4, transitionCX, transitionCY, Math.max(transitionRX, transitionRY));
                transitionSheet.addColorStop(0, transitionFromColor); transitionSheet.addColorStop(0.72, transitionFromColor); transitionSheet.addColorStop(1, 'rgba(30,20,24,0.96)');
                ctx.fillStyle = transitionSheet; ctx.globalAlpha = 0.90;
                if (transitionHorizontalBody) {
                  var verticalOpening = transitionRY * layerTransitionProgress;
                  ctx.fillRect(transitionCX - transitionRX, transitionCY - transitionRY, transitionRX * 2, Math.max(0, transitionRY - verticalOpening));
                  ctx.fillRect(transitionCX - transitionRX, transitionCY + verticalOpening, transitionRX * 2, Math.max(0, transitionRY - verticalOpening));
                  var edgeCurl = ctx.createLinearGradient(0, transitionCY - verticalOpening - 8, 0, transitionCY - verticalOpening + 8);
                  edgeCurl.addColorStop(0, 'rgba(15,23,42,0)'); edgeCurl.addColorStop(0.5, transitionToColor); edgeCurl.addColorStop(1, 'rgba(255,255,255,0.34)');
                  ctx.globalAlpha = Math.max(0, 0.72 * (1 - transitionLinear)); ctx.fillStyle = edgeCurl;
                  ctx.fillRect(transitionCX - transitionRX, transitionCY - verticalOpening - 7, transitionRX * 2, 14);
                  ctx.fillRect(transitionCX - transitionRX, transitionCY + verticalOpening - 7, transitionRX * 2, 14);
                } else {
                  var horizontalOpening = transitionRX * layerTransitionProgress;
                  ctx.fillRect(transitionCX - transitionRX, transitionCY - transitionRY, Math.max(0, transitionRX - horizontalOpening), transitionRY * 2);
                  ctx.fillRect(transitionCX + horizontalOpening, transitionCY - transitionRY, Math.max(0, transitionRX - horizontalOpening), transitionRY * 2);
                  var sideCurl = ctx.createLinearGradient(transitionCX - horizontalOpening - 8, 0, transitionCX - horizontalOpening + 8, 0);
                  sideCurl.addColorStop(0, 'rgba(15,23,42,0)'); sideCurl.addColorStop(0.5, transitionToColor); sideCurl.addColorStop(1, 'rgba(255,255,255,0.34)');
                  ctx.globalAlpha = Math.max(0, 0.72 * (1 - transitionLinear)); ctx.fillStyle = sideCurl;
                  ctx.fillRect(transitionCX - horizontalOpening - 7, transitionCY - transitionRY, 14, transitionRY * 2);
                  ctx.fillRect(transitionCX + horizontalOpening - 7, transitionCY - transitionRY, 14, transitionRY * 2);
                }
                // Temporary connective strands bridge the opening and release as the flaps retract.
                ctx.globalAlpha = Math.max(0, (1 - transitionLinear) * 0.48); ctx.strokeStyle = 'rgba(254,205,211,0.82)'; ctx.lineWidth = 0.8;
                for (var transitionFiberIndex = 0; transitionFiberIndex < 8; transitionFiberIndex++) {
                  var transitionFiberT = (transitionFiberIndex + 1) / 9;
                  if (transitionHorizontalBody) {
                    var transitionFiberX = transitionCX - transitionRX * 0.72 + transitionRX * 1.44 * transitionFiberT;
                    ctx.beginPath(); ctx.moveTo(transitionFiberX - 3, transitionCY - transitionRY * layerTransitionProgress); ctx.lineTo(transitionFiberX + 3, transitionCY + transitionRY * layerTransitionProgress); ctx.stroke();
                  } else {
                    var transitionFiberY = transitionCY - transitionRY * 0.72 + transitionRY * 1.44 * transitionFiberT;
                    ctx.beginPath(); ctx.moveTo(transitionCX - transitionRX * layerTransitionProgress, transitionFiberY - 3); ctx.lineTo(transitionCX + transitionRX * layerTransitionProgress, transitionFiberY + 3); ctx.stroke();
                  }
                }
                ctx.restore();
              }
              // The focus-plane map projects lens depth across visible landmarks using distinct contour patterns.
              function drawOpticalFocusPlaneMap() {
                if (!opticalPlaneMapActive) return;
                var planeAccent = lensFocusDepth === 'surface' ? '#fbbf24' : (lensFocusDepth === 'deep' ? '#c4b5fd' : '#67e8f9');
                var planeAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var planeProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                ctx.save(); ctx.beginPath(); ctx.ellipse(planeProfile.cx * W, planeProfile.cy * H, planeProfile.rx * W, planeProfile.ry * H, 0, 0, Math.PI * 2); ctx.clip();
                organs.filter(function (organ) { return structureExposureState(organ, currentProcedure) === 'visible'; }).forEach(function (organ) {
                  var planePoint = variedOrganPoint(organ), planeX = planePoint.x * W, planeY = planePoint.y * H;
                  var planeStatus = opticalPlaneStatus(organ, lensFocusDepth);
                  var planeSelected = d.selectedOrgan === organ.id, planeHovered = !planeSelected && d.hoveredOrgan === organ.id;
                  var planeRadius = (planeAccessible ? 15 : 12) + (planeSelected || planeHovered ? 3 : 0);
                  ctx.save(); ctx.strokeStyle = planeAccessible ? '#ffffff' : planeAccent;
                  ctx.lineWidth = planeAccessible ? 2.4 : (planeSelected ? 1.8 : 1.05);
                  ctx.globalAlpha = planeSelected || planeHovered ? 0.94 : (planeStatus === 'resolved' ? 0.64 : (planeStatus === 'soft' ? 0.42 : 0.30));
                  if (planeStatus === 'resolved') {
                    for (var planeArc = 0; planeArc < 4; planeArc++) {
                      var planeStart = planeArc * Math.PI / 2 + 0.14;
                      ctx.beginPath(); ctx.arc(planeX, planeY, planeRadius, planeStart, planeStart + 0.58); ctx.stroke();
                    }
                    ctx.beginPath(); ctx.arc(planeX, planeY, 2.2, 0, Math.PI * 2); ctx.fillStyle = planeAccessible ? '#ffffff' : planeAccent; ctx.fill();
                  } else if (planeStatus === 'soft') {
                    ctx.setLineDash([4, 3]); ctx.beginPath(); ctx.arc(planeX, planeY, planeRadius + 1, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  } else {
                    ctx.setLineDash([1.2, 3]); ctx.beginPath(); ctx.arc(planeX, planeY, planeRadius, 0, Math.PI * 2); ctx.stroke();
                    ctx.beginPath(); ctx.arc(planeX, planeY, planeRadius + 4, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  }
                  ctx.restore();
                });
                ctx.restore();
              }
              if (!d.quizMode) drawOpticalFocusPlaneMap();
              var focusRelatedIds = d.selectedOrgan ? anatomicalRelationships().map(function (relationship) { return relationship.organ.id; }) : [];
              var focusEntryProgress = 1;
              if (d.selectedOrgan) {
                if (canvas._focusOrganId !== d.selectedOrgan) {
                  canvas._focusOrganId = d.selectedOrgan;
                  canvas._focusStartedAt = Date.now();
                }
                if (!dissMotionReduced) {
                  var focusElapsed = Math.max(0, Date.now() - (canvas._focusStartedAt || Date.now()));
                  var focusLinear = Math.min(1, focusElapsed / 520);
                  focusEntryProgress = 1 - Math.pow(1 - focusLinear, 3);
                }
              } else {
                canvas._focusOrganId = null;
                canvas._focusStartedAt = null;
              }

              // The inspection footprint is a depth capsule, not an exact anatomical outline: front/back edges make local depth and coverage spatially legible.
              function structureInspectionFootprint(org) {
                var structureKey = String((org && org.id) || '') + ' ' + String((org && org.name) || '');
                structureKey = structureKey.toLowerCase();
                var depth = structureOpticalDepth(org);
                var baseRadius = Math.max(10, Math.min(19, Math.min(W, H) * (organs.length >= 10 ? 0.025 : 0.034)));
                var conduit = /vessel|arter|vena|vein|aorta|cord|nerve|intestin|ureter|duct|esophagus|trachea|spinal|chordae/.test(structureKey);
                var chambered = /heart|stomach|bladder|crop|gizzard|atrium|ventricle|cloaca|lens|humor/.test(structureKey);
                var layered = /skin|integument|cuticle|scale|carapace|membrane|muscle|myomere|retina|cornea|sclera|choroid|conjunctiva|diaphragm|septum|pericardium|valve/.test(structureKey);
                var lobed = /liver|lung|kidney|gonad|gill|pancreas|thymus|fat bod|gland/.test(structureKey);
                var morphology = conduit ? 'conduit' : (chambered ? 'chambered' : (layered ? 'layered' : (lobed ? 'lobed' : 'compact')));
                var horizontalSpecimen = /pig|fish|crayfish|eye|heart/.test(String(spec.bodyShape || specimen || '').toLowerCase());
                var rotation = morphology === 'conduit' ? (/worm/.test(String(spec.bodyShape || specimen || '').toLowerCase()) ? Math.PI / 2 : (horizontalSpecimen ? 0 : -0.35)) : (morphology === 'lobed' ? 0.18 : (morphology === 'layered' ? -0.10 : 0));
                return {
                  rx: baseRadius * (morphology === 'conduit' ? 1.65 : (morphology === 'lobed' ? 1.35 : (morphology === 'layered' ? 1.40 : (morphology === 'chambered' ? 1.15 : 1.08)))),
                  ry: baseRadius * (morphology === 'conduit' ? 0.48 : (morphology === 'lobed' ? 0.80 : (morphology === 'layered' ? 0.62 : (morphology === 'chambered' ? 1 : 0.95)))),
                  rotation: rotation,
                  morphology: morphology,
                  morphologyLabel: morphology === 'conduit' ? 'TUBE' : morphology.toUpperCase(),
                  depth: depth,
                  depthOffset: depth === 'surface' ? 3 : (depth === 'deep' ? 12 : 7)
                };
              }
              // Morphology contours and internal marks distinguish structure classes without claiming an exact organ boundary.
              function traceStructureMorphologyContour(profile, yOffset, inflate) {
                var rx = profile.rx + (inflate || 0), ry = profile.ry + (inflate || 0), y = yOffset || 0;
                ctx.beginPath();
                if (profile.morphology === 'conduit') {
                  var straight = Math.max(0, rx - ry);
                  ctx.moveTo(-straight, y - ry); ctx.lineTo(straight, y - ry);
                  ctx.arc(straight, y, ry, -Math.PI / 2, Math.PI / 2);
                  ctx.lineTo(-straight, y + ry); ctx.arc(-straight, y, ry, Math.PI / 2, Math.PI * 1.5); ctx.closePath();
                } else if (profile.morphology === 'lobed') {
                  ctx.moveTo(0, y - ry);
                  ctx.bezierCurveTo(rx * 0.18, y - ry * 0.76, rx * 0.38, y - ry * 0.98, rx * 0.58, y - ry * 0.72);
                  ctx.bezierCurveTo(rx * 0.92, y - ry * 0.52, rx * 1.02, y - ry * 0.15, rx * 0.90, y + ry * 0.12);
                  ctx.bezierCurveTo(rx * 0.78, y + ry * 0.44, rx * 0.48, y + ry * 0.82, rx * 0.14, y + ry * 0.74);
                  ctx.bezierCurveTo(-rx * 0.10, y + ry * 1.02, -rx * 0.38, y + ry * 0.90, -rx * 0.50, y + ry * 0.62);
                  ctx.bezierCurveTo(-rx * 0.92, y + ry * 0.56, -rx * 1.05, y + ry * 0.15, -rx * 0.88, y - ry * 0.18);
                  ctx.bezierCurveTo(-rx * 0.72, y - ry * 0.52, -rx * 0.35, y - ry * 0.82, 0, y - ry); ctx.closePath();
                } else {
                  ctx.ellipse(0, y, rx, ry, 0, 0, Math.PI * 2);
                }
              }
              function drawStructureMorphologyTexture(profile) {
                ctx.save(); ctx.globalAlpha *= 0.72; ctx.lineWidth = Math.max(0.9, ctx.lineWidth * 0.72);
                if (profile.morphology === 'conduit') {
                  ctx.setLineDash([3.5, 2.5]); ctx.beginPath(); ctx.moveTo(-profile.rx * 0.68, 0); ctx.lineTo(profile.rx * 0.68, 0); ctx.stroke(); ctx.setLineDash([]);
                  [-0.34, 0, 0.34].forEach(function (fraction) { ctx.beginPath(); ctx.moveTo(profile.rx * fraction, -profile.ry * 0.54); ctx.lineTo(profile.rx * fraction, profile.ry * 0.54); ctx.stroke(); });
                } else if (profile.morphology === 'lobed') {
                  ctx.beginPath(); ctx.moveTo(-profile.rx * 0.42, -profile.ry * 0.56); ctx.quadraticCurveTo(-profile.rx * 0.08, 0, -profile.rx * 0.34, profile.ry * 0.54);
                  ctx.moveTo(profile.rx * 0.34, -profile.ry * 0.54); ctx.quadraticCurveTo(profile.rx * 0.05, 0, profile.rx * 0.44, profile.ry * 0.44); ctx.stroke();
                } else if (profile.morphology === 'chambered') {
                  ctx.beginPath(); ctx.ellipse(0, 0, profile.rx * 0.52, profile.ry * 0.56, 0, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(0, -profile.ry * 0.54); ctx.lineTo(0, profile.ry * 0.54); ctx.stroke();
                } else if (profile.morphology === 'layered') {
                  [-0.42, 0, 0.42].forEach(function (fraction) { ctx.beginPath(); ctx.moveTo(-profile.rx * 0.66, profile.ry * fraction); ctx.quadraticCurveTo(0, profile.ry * (fraction + 0.18), profile.rx * 0.66, profile.ry * fraction); ctx.stroke(); });
                } else {
                  ctx.beginPath(); ctx.arc(0, 0, Math.min(profile.rx, profile.ry) * 0.28, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(-profile.rx * 0.42, 0); ctx.lineTo(profile.rx * 0.42, 0); ctx.moveTo(0, -profile.ry * 0.42); ctx.lineTo(0, profile.ry * 0.42); ctx.stroke();
                }
                ctx.restore();
              }              function structureBoundaryAttachmentPoint(org, towardPoint) {
                var centerPoint = variedOrganPoint(org);
                var centerX = centerPoint.x * W, centerY = centerPoint.y * H;
                var towardX = towardPoint.x * W, towardY = towardPoint.y * H;
                var vectorX = towardX - centerX, vectorY = towardY - centerY;
                if (Math.abs(vectorX) + Math.abs(vectorY) < 0.01) return { x: centerX, y: centerY };
                var profile = structureInspectionFootprint(org);
                var cosine = Math.cos(-profile.rotation), sine = Math.sin(-profile.rotation);
                var localX = vectorX * cosine - vectorY * sine;
                var localY = vectorX * sine + vectorY * cosine;
                var denominator = Math.sqrt((localX * localX) / (profile.rx * profile.rx) + (localY * localY) / (profile.ry * profile.ry)) || 1;
                localX /= denominator; localY /= denominator;
                cosine = Math.cos(profile.rotation); sine = Math.sin(profile.rotation);
                return { x: centerX + localX * cosine - localY * sine, y: centerY + localX * sine + localY * cosine };
              }
              function drawStructureInspectionFootprint(org, x, y, selected, hovered, visibility) {
                if (!selected && !hovered) return;
                var profile = structureInspectionFootprint(org);
                var footprintAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var footprintAccent = footprintAccessible ? '#ffffff' : (selected ? '#fbbf24' : '#60a5fa');
                var footprintScale = selected ? 0.72 + focusEntryProgress * 0.28 : 1;
                var backDash = visibility === 'visible' ? [] : (visibility === 'emerging' ? [5, 3] : (visibility === 'obscured' ? [2, 3] : [1, 4]));
                ctx.save(); ctx.translate(x, y); ctx.scale(footprintScale, footprintScale); ctx.rotate(profile.rotation);
                ctx.strokeStyle = footprintAccent; ctx.fillStyle = selected ? 'rgba(251,191,36,0.07)' : 'rgba(96,165,250,0.06)';
                ctx.lineWidth = footprintAccessible ? 2.5 : (selected ? 1.8 : 1.35);
                ctx.globalAlpha = visibility === 'visible' ? 0.92 : 0.74;
                ctx.setLineDash(backDash);
                traceStructureMorphologyContour(profile, profile.depthOffset, 0); ctx.fill(); ctx.stroke();
                ctx.setLineDash([]);
                ctx.globalAlpha = 0.58;
                ctx.beginPath(); ctx.moveTo(-profile.rx, 0); ctx.lineTo(-profile.rx, profile.depthOffset);
                ctx.moveTo(profile.rx, 0); ctx.lineTo(profile.rx, profile.depthOffset); ctx.stroke();
                ctx.globalAlpha = 1;
                traceStructureMorphologyContour(profile, 0, 0); ctx.stroke();
                drawStructureMorphologyTexture(profile);
                if (profile.depth === 'deep') {
                  ctx.setLineDash([2, 2.8]); traceStructureMorphologyContour(profile, profile.depthOffset, 3); ctx.stroke(); ctx.setLineDash([]);
                }
                ctx.beginPath(); ctx.moveTo(0, -profile.ry); ctx.lineTo(0, profile.depthOffset - profile.ry); ctx.stroke();
                ctx.fillStyle = footprintAccent;
                if (profile.depth === 'surface') {
                  ctx.beginPath(); ctx.arc(0, profile.depthOffset - profile.ry, 2.5, 0, Math.PI * 2); ctx.fill();
                } else if (profile.depth === 'structure') {
                  ctx.save(); ctx.translate(0, profile.depthOffset - profile.ry); ctx.rotate(Math.PI / 4); ctx.fillRect(-2.5, -2.5, 5, 5); ctx.restore();
                } else {
                  ctx.beginPath(); ctx.arc(0, profile.depthOffset - profile.ry, 3.3, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.arc(0, profile.depthOffset - profile.ry, 1.25, 0, Math.PI * 2); ctx.fill();
                }
                ctx.rotate(-profile.rotation);
                ctx.font = 'bold 8px Inter, system-ui'; ctx.textAlign = 'center'; ctx.textBaseline = 'top';
                ctx.fillStyle = footprintAccessible ? '#ffffff' : (selected ? '#fde68a' : '#bfdbfe');
                var footprintDepthLabel = profile.depth === 'surface' ? 'SURFACE' : (profile.depth === 'deep' ? 'DEEP' : 'MID');
                ctx.fillText(footprintDepthLabel + ' \u00B7 ' + profile.morphologyLabel, 0, profile.ry + profile.depthOffset + 4);
                ctx.restore();
              }
              // Animated pathway markers retain distinct silhouettes so relationship meaning never depends on motion or color.
              function drawAnatomicalRelationshipMarker(x, y, angle, marker, size, color, accessible) {
                ctx.save(); ctx.translate(x, y); ctx.rotate(angle || 0); ctx.strokeStyle = color; ctx.fillStyle = color;
                ctx.lineWidth = accessible ? 2.2 : 1.5; ctx.lineJoin = 'round'; ctx.lineCap = 'round'; ctx.beginPath();
                if (marker === 'diamond') {
                  ctx.moveTo(size, 0); ctx.lineTo(0, size); ctx.lineTo(-size, 0); ctx.lineTo(0, -size); ctx.closePath();
                } else if (marker === 'chevron' || marker === 'double-chevron') {
                  ctx.moveTo(-size * 0.72, -size); ctx.lineTo(size * 0.58, 0); ctx.lineTo(-size * 0.72, size);
                  if (marker === 'double-chevron') { ctx.moveTo(-size * 0.08, -size); ctx.lineTo(size * 1.18, 0); ctx.lineTo(-size * 0.08, size); }
                } else if (marker === 'triangle') {
                  ctx.moveTo(size, 0); ctx.lineTo(-size * 0.72, size * 0.88); ctx.lineTo(-size * 0.72, -size * 0.88); ctx.closePath();
                } else if (marker === 'ring') {
                  ctx.arc(0, 0, size * 0.78, 0, Math.PI * 2);
                } else if (marker === 'square') {
                  ctx.rect(-size * 0.72, -size * 0.72, size * 1.44, size * 1.44);
                } else if (marker === 'hexagon') {
                  for (var hexPoint = 0; hexPoint < 6; hexPoint++) { var hexAngle = hexPoint * Math.PI / 3; var hexX = Math.cos(hexAngle) * size, hexY = Math.sin(hexAngle) * size; if (hexPoint) ctx.lineTo(hexX, hexY); else ctx.moveTo(hexX, hexY); } ctx.closePath();
                } else {
                  ctx.arc(0, 0, size * 0.72, 0, Math.PI * 2);
                }
                if (marker === 'chevron' || marker === 'double-chevron' || marker === 'ring') ctx.stroke();
                else { ctx.fill(); if (accessible) { ctx.strokeStyle = '#0f172a'; ctx.lineWidth = 0.9; ctx.stroke(); } }
                ctx.restore();
              }
              if (d.relationshipMode && d.selectedOrgan) {
                var relationshipSource = organs.find(function (org) { return org.id === d.selectedOrgan; });
                if (relationshipSource) {
                  var relationshipSourcePoint = variedOrganPoint(relationshipSource);
                  anatomicalRelationships().filter(function (relationship) { return relationship.visible; }).forEach(function (relationship, relationshipIdx) {
                    var relationshipPoint = variedOrganPoint(relationship.organ);
                    var sourceAttachment = structureBoundaryAttachmentPoint(relationshipSource, relationshipPoint);
                    var targetAttachment = structureBoundaryAttachmentPoint(relationship.organ, relationshipSourcePoint);
                    var sourceX = sourceAttachment.x, sourceY = sourceAttachment.y;
                    var targetX = targetAttachment.x, targetY = targetAttachment.y;
                    var relationshipDX = targetX - sourceX, relationshipDY = targetY - sourceY;
                    var relationshipLength = Math.sqrt(relationshipDX * relationshipDX + relationshipDY * relationshipDY) || 1;
                    var relationshipBend = Math.min(42, relationshipLength * 0.2) * (relationshipIdx % 2 ? -1 : 1);
                    var controlX = (sourceX + targetX) / 2 - relationshipDY / relationshipLength * relationshipBend;
                    var controlY = (sourceY + targetY) / 2 + relationshipDX / relationshipLength * relationshipBend;
                    var relationshipVisual = anatomicalRelationshipVisualProfile(relationship.type);
                    var relationshipAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                    var relationshipColor = relationshipAccessible ? '#f8fafc' : relationshipVisual.color;
                    ctx.save();
                    ctx.setLineDash(relationshipVisual.dash);
                    ctx.strokeStyle = relationshipColor; ctx.globalAlpha = 0.72; ctx.lineWidth = relationshipAccessible ? 3 : 1.7;
                    ctx.beginPath(); ctx.moveTo(sourceX, sourceY); ctx.quadraticCurveTo(controlX, controlY, targetX, targetY); ctx.stroke();
                    ctx.setLineDash([]); ctx.globalAlpha = 1;
                    var arrowAngle = Math.atan2(targetY - controlY, targetX - controlX);
                    ctx.fillStyle = relationshipColor; ctx.beginPath(); ctx.moveTo(targetX, targetY);
                    ctx.lineTo(targetX - Math.cos(arrowAngle - 0.5) * 8, targetY - Math.sin(arrowAngle - 0.5) * 8);
                    ctx.lineTo(targetX - Math.cos(arrowAngle + 0.5) * 8, targetY - Math.sin(arrowAngle + 0.5) * 8); ctx.closePath(); ctx.fill();
                    ctx.fillRect(sourceX - 2.5, sourceY - 2.5, 5, 5);
                    for (var flowMarkerIdx = 0; flowMarkerIdx < 3; flowMarkerIdx++) {
                      var flowT = relationshipMotion && !dissMotionReduced ? (dissTick * 0.012 + flowMarkerIdx / 3 + relationshipIdx * 0.13) % 1 : (flowMarkerIdx + 1) / 4;
                      var flowInverse = 1 - flowT;
                      var flowX = flowInverse * flowInverse * sourceX + 2 * flowInverse * flowT * controlX + flowT * flowT * targetX;
                      var flowY = flowInverse * flowInverse * sourceY + 2 * flowInverse * flowT * controlY + flowT * flowT * targetY;
                      var flowTangentX = 2 * flowInverse * (controlX - sourceX) + 2 * flowT * (targetX - controlX);
                      var flowTangentY = 2 * flowInverse * (controlY - sourceY) + 2 * flowT * (targetY - controlY);
                      ctx.shadowColor = relationshipColor; ctx.shadowBlur = relationshipAccessible ? 3 : 7;
                      drawAnatomicalRelationshipMarker(flowX, flowY, Math.atan2(flowTangentY, flowTangentX), relationshipVisual.marker, flowMarkerIdx === 1 ? 3.8 : 3, relationshipColor, relationshipAccessible);
                    }
                    ctx.shadowBlur = 0; ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = '#f8fafc';
                    ctx.fillText(relationship.type, controlX + 5, controlY - 5);
                    ctx.restore();
                  });
                }
              }
              // Shape-coded depth landmarks pair color, geometry, and line style so depth never depends on hue alone.
              function drawDepthAtlasMarker(x, y, depth, selected, hovered, muted) {
                if (!depthAtlasEnabled) return;
                var atlasAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var atlasColor = depth === 'surface' ? '#67e8f9' : (depth === 'deep' ? '#c4b5fd' : '#fcd34d');
                var atlasRadius = selected ? 10.5 : (hovered ? 9.5 : 8.5);
                ctx.save(); ctx.translate(x, y); ctx.globalAlpha = muted ? 0.38 : (selected || hovered ? 1 : 0.82);
                ctx.strokeStyle = atlasAccessible ? '#ffffff' : atlasColor; ctx.lineWidth = atlasAccessible ? 2.4 : 1.55;
                ctx.fillStyle = 'rgba(15,23,42,0.32)';
                if (depth === 'surface') {
                  ctx.beginPath(); ctx.arc(0, 0, atlasRadius, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                } else if (depth === 'structure') {
                  ctx.rotate(Math.PI / 4); ctx.beginPath(); ctx.rect(-atlasRadius * 0.7, -atlasRadius * 0.7, atlasRadius * 1.4, atlasRadius * 1.4); ctx.fill(); ctx.stroke();
                } else {
                  ctx.beginPath(); ctx.arc(0, 0, atlasRadius - 2.5, 0, Math.PI * 2); ctx.stroke();
                  ctx.setLineDash([2, 2.6]); ctx.beginPath(); ctx.arc(0, 0, atlasRadius + 2.5, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                }
                ctx.restore();
              }
              // Exposure markers use distinct geometry so emerging and covered structures never read like exposed targets.
              function drawExposureStateMarker(x, y, visibility, selected, hovered, muted) {
                if (visibility === 'visible') return false;
                var markerAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var markerRadius = selected ? 7.5 : (hovered ? 7 : 6.2);
                ctx.save(); ctx.translate(x, y); ctx.globalAlpha = muted ? 0.44 : 0.92;
                ctx.strokeStyle = markerAccessible ? '#f8fafc' : (visibility === 'emerging' ? '#fda4af' : '#cbd5e1');
                ctx.fillStyle = markerAccessible ? '#334155' : 'rgba(51,65,85,0.82)';
                ctx.lineWidth = markerAccessible ? 2.1 : 1.45;
                ctx.beginPath(); ctx.arc(0, 0, markerRadius, 0, Math.PI * 2); ctx.fill();
                if (visibility === 'emerging') {
                  ctx.beginPath(); ctx.arc(0, 0, markerRadius, -1.22, 1.22); ctx.stroke();
                  ctx.beginPath(); ctx.arc(0, 0, markerRadius * 0.44, 1.72, 4.54); ctx.stroke();
                } else {
                  ctx.setLineDash([2.2, 2.4]); ctx.beginPath(); ctx.arc(0, 0, markerRadius, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  ctx.lineWidth = markerAccessible ? 1.65 : 1;
                  ctx.beginPath(); ctx.moveTo(-markerRadius * 0.48, -markerRadius * 0.48); ctx.lineTo(markerRadius * 0.48, markerRadius * 0.48);
                  ctx.moveTo(-markerRadius * 0.48, markerRadius * 0.48); ctx.lineTo(markerRadius * 0.48, -markerRadius * 0.48); ctx.stroke();
                }
                ctx.restore();
                return true;
              }
              // Adaptive hotspot labels share collision-aware columns and remain clickable.
              var specimenTextScaleX = Math.max(0.01, Math.abs(specimenScale.x || 1));
              var specimenTextScaleY = Math.max(0.01, Math.abs(specimenScale.y || 1));
              function fillReadableSpecimenText(text, x, y) {
                ctx.save(); ctx.translate(x, y);
                ctx.scale(1 / (specimenScale.x || 1), 1 / (specimenScale.y || 1));
                ctx.fillText(text, 0, 0); ctx.restore();
              }
              ctx.font = (10 * canvasUiScale) + 'px Inter, system-ui, sans-serif';
              var canvasLabelsVisible = d.labelMode !== 'hidden' && !d.quizMode;
              var denseHotspotView = canvasLabelsVisible && organs.length >= 8 && zoom < 1.22;
              var adaptiveHotspotLayout = organs.filter(function (org) { return !d.quizMode || structureExposureState(org, currentProcedure) === 'visible'; }).map(function (org, oi) {
                var point = variedOrganPoint(org), visibility = structureExposureState(org, currentProcedure);
                var selected = d.selectedOrgan === org.id, hovered = !selected && d.hoveredOrgan === org.id;
                var related = focusRelatedIds.indexOf(org.id) >= 0;
                var guidedCallout = currentGuided && currentGuided.organId === org.id;
                var fullLabel = !d.quizMode && (selected || hovered || guidedCallout || (canvasLabelsVisible && !canvasCoarsePointer && (!denseHotspotView || related || (oi % 2 === 0 && visibility === 'visible'))));
                var opticalDepth = structureOpticalDepth(org);
                var baseLabel = visibility === 'occluded' ? org.name + ' \u00B7 occluded' : (visibility === 'obscured' ? org.name + ' \u00B7 covered' : (visibility === 'emerging' ? org.name + ' \u00B7 emerging' : org.name));
                var depthDescriptor = depthAtlasEnabled && (selected || hovered) ? ' \u00B7 ' + anatomicalDepthLabel(opticalDepth) : '';
                var displayLabel = selected ? baseLabel + depthDescriptor + ' \u00B7 selected' : baseLabel + depthDescriptor;
                if (canvasCoarsePointer) {
                  var coarseLabelName = org.name.length > 20 ? org.name.slice(0, 19) + '\u2026' : org.name;
                  displayLabel = selected ? coarseLabelName + ' \u00B7 selected' : coarseLabelName;
                }
                var width = (fullLabel ? ctx.measureText(displayLabel).width + 14 * canvasUiScale : 18 * canvasUiScale) / specimenTextScaleX, height = 18 * canvasUiScale / specimenTextScaleY;
                var pointX = point.x * W, pointY = point.y * H;
                var side = pointX < W * 0.47 ? 'left' : (pointX > W * 0.53 ? 'right' : (oi % 2 ? 'left' : 'right'));
                var rightReservedTop = d.crossSectionMode ? 72 + spec.layers.length * 18 : 150;
                if (side === 'right' && pointY < rightReservedTop && pointX < W * 0.74) side = 'left';
                var calloutOffsetX = 18 * canvasUiScale / specimenTextScaleX;
                var preferredX = side === 'right' ? pointX + calloutOffsetX : pointX - width - calloutOffsetX;
                return { id: org.id, index: oi, pointX: pointX, pointY: pointY,
                  x: Math.max(26 * canvasUiScale / specimenTextScaleX, Math.min(W - width - 26 * canvasUiScale / specimenTextScaleX, preferredX)), preferredY: Math.max(82, Math.min(H - 64, pointY - height / 2)),
                  y: pointY - height / 2, width: width, height: height, side: side, fullLabel: fullLabel,
                  displayLabel: displayLabel, visibility: visibility, depth: opticalDepth, selected: selected, hovered: hovered, related: related };
              });
              function resolveAdaptiveLabelColumn(items, minY, maxY) {
                var gap = 4 * canvasUiScale / specimenTextScaleY, cursorY = minY;
                items.sort(function (a, b) { return a.preferredY - b.preferredY || a.index - b.index; });
                items.forEach(function (item) { item.y = Math.max(item.preferredY, cursorY); cursorY = item.y + item.height + gap; });
                if (!items.length) return;
                var overflow = items[items.length - 1].y + items[items.length - 1].height - maxY;
                if (overflow > 0) items.forEach(function (item) { item.y -= overflow; });
                for (var ri = items.length - 2; ri >= 0; ri--) items[ri].y = Math.min(items[ri].y, items[ri + 1].y - items[ri].height - gap);
                if (items[0].y < minY) { var underflow = minY - items[0].y; items.forEach(function (item) { item.y += underflow; }); }
              }
              resolveAdaptiveLabelColumn(adaptiveHotspotLayout.filter(function (item) { return item.side === 'left'; }), 82, H - 45);
              resolveAdaptiveLabelColumn(adaptiveHotspotLayout.filter(function (item) { return item.side === 'right'; }), d.crossSectionMode ? 80 + spec.layers.length * 18 : 150, H - 45);
              var adaptiveHotspotById = {};
              adaptiveHotspotLayout.forEach(function (item) { adaptiveHotspotById[item.id] = item; });
              canvas._hotspotLabelBoxes = adaptiveHotspotLayout.map(function (item) {
                return { id: item.id, x: item.x / W, y: item.y / H, width: item.width / W, height: item.height / H };
              });

              function drawAdaptiveHotspotGuidance() {
              organs.forEach(function (org, oi) {
                var layout = adaptiveHotspotById[org.id]; if (!layout) return;
                var isSel = layout.selected, isHov = layout.hovered;
                var isFocusRelated = layout.related;
                var focusMuted = (focusMode || (denseHotspotView && d.selectedOrgan)) && d.selectedOrgan && !isSel && !isFocusRelated;
                var exposureMuted = layout.visibility !== 'visible';
                ctx.save(); ctx.globalAlpha = focusMuted ? ((d.visualRealism || visualRealism) === 'accessible' ? 0.46 : 0.24) : (exposureMuted ? ((d.visualRealism || visualRealism) === 'accessible' ? 0.86 : 0.68) : 1);
                var px = layout.pointX, py = layout.pointY;
                drawStructureInspectionFootprint(org, px, py, isSel, isHov, layout.visibility);
                if (!isSel && (!exposureMuted || isHov)) drawDepthAtlasMarker(px, py, layout.depth, false, isHov, focusMuted);
                var pulse = 1;
                if (isSel) {
                  var focusRadius = 12 + (1 - focusEntryProgress) * 24 + (dissMotionReduced ? 0 : (Math.sin(dissTick * 0.045) + 1) * 2);
                  ctx.save(); ctx.strokeStyle = (d.visualRealism || visualRealism) === 'accessible' ? '#facc15' : 'rgba(254,240,138,0.86)';
                  ctx.lineWidth = (d.visualRealism || visualRealism) === 'accessible' ? 2.8 : 1.5;
                  for (var fq = 0; fq < 4; fq++) { var fs = fq * Math.PI / 2 + 0.14; ctx.beginPath(); ctx.arc(px, py, focusRadius + 8, fs, fs + 0.58); ctx.stroke(); }
                  // Reticle marks make selected state readable without color alone.
                  ctx.beginPath(); ctx.moveTo(px-focusRadius-13,py); ctx.lineTo(px-focusRadius-6,py); ctx.moveTo(px+focusRadius+6,py); ctx.lineTo(px+focusRadius+13,py);
                  ctx.moveTo(px,py-focusRadius-13); ctx.lineTo(px,py-focusRadius-6); ctx.moveTo(px,py+focusRadius+6); ctx.lineTo(px,py+focusRadius+13); ctx.stroke(); ctx.restore();
                }
                var lx = layout.x, ly = layout.y, connectorX = layout.side === 'right' ? lx : lx + layout.width, connectorY = ly + layout.height / 2;
                if (!d.quizMode) {
                  var leaderStartX = px + (layout.side === 'right' ? 6 : -6), leaderControlX = leaderStartX + (connectorX - leaderStartX) * 0.54;
                  ctx.beginPath(); ctx.moveTo(leaderStartX, py); ctx.quadraticCurveTo(leaderControlX, py, connectorX, connectorY);
                  ctx.strokeStyle = isSel ? 'rgba(251,191,36,0.92)' : (isHov ? 'rgba(96,165,250,0.86)' : (isFocusRelated ? 'rgba(45,212,191,0.72)' : 'rgba(203,213,225,0.34)'));
                  ctx.lineWidth = isSel ? 1.8 : (isHov ? 1.45 : 0.85);
                  var depthLeaderDash = exposureMuted ? [1.2, 3.8] : (layout.depth === 'deep' ? [1.2, 3.2] : (layout.depth === 'structure' ? [4, 3] : []));
                  ctx.setLineDash(isSel || isHov ? [] : (depthAtlasEnabled ? depthLeaderDash : [2.5,3])); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.arc(connectorX, connectorY, isSel ? 2.2 : 1.5, 0, Math.PI * 2); ctx.fillStyle = ctx.strokeStyle; ctx.fill();
                }
                if (layout.fullLabel) {
                  ctx.fillStyle = isSel ? 'rgba(251,191,36,0.94)' : (isHov ? 'rgba(30,64,175,0.94)' : (isFocusRelated ? 'rgba(15,118,110,0.90)' : 'rgba(15,23,42,0.88)'));
                  ctx.strokeStyle = isSel ? '#f59e0b' : (isHov ? '#60a5fa' : (isFocusRelated ? '#2dd4bf' : 'rgba(148,163,184,0.48)')); ctx.lineWidth = isSel || isHov ? 1.1 : 0.65;
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(lx,ly,layout.width,layout.height,5); ctx.fill(); ctx.stroke(); } else { ctx.fillRect(lx,ly,layout.width,layout.height); ctx.strokeRect(lx,ly,layout.width,layout.height); }
                  if (isSel || isHov) { ctx.fillStyle = isSel ? '#92400e' : '#93c5fd'; ctx.fillRect(lx+1,ly+3,3,layout.height-6); }
                  ctx.font = (isSel ? 'bold ' : '') + (10 * canvasUiScale) + 'px Inter, system-ui, sans-serif'; ctx.fillStyle = isSel ? '#1e293b' : '#f8fafc';
                  var labelInsetX = 7 * canvasUiScale / specimenTextScaleX;
                  var labelTextX = specimenScale.x < 0 ? lx + layout.width - labelInsetX : lx + labelInsetX;
                  fillReadableSpecimenText(layout.displayLabel, labelTextX, ly + 12.5 * canvasUiScale / specimenTextScaleY);
                } else if (!d.quizMode) {
                  var markerNum = String(layout.index + 1), compactCX = lx + layout.width/2, compactCY = ly + layout.height/2;
                  var compactMarkerRadius = (canvasCoarsePointer ? 11 : 8) * canvasUiScale;
                  ctx.beginPath(); ctx.ellipse(compactCX, compactCY, compactMarkerRadius / specimenTextScaleX, compactMarkerRadius / specimenTextScaleY, 0, 0, Math.PI*2); ctx.fillStyle = isFocusRelated ? 'rgba(15,118,110,0.94)' : 'rgba(30,41,59,0.90)'; ctx.fill();
                  ctx.strokeStyle = isFocusRelated ? '#5eead4' : 'rgba(203,213,225,0.58)'; ctx.lineWidth=.8; ctx.stroke(); ctx.fillStyle='#fff'; ctx.font='bold ' + ((canvasCoarsePointer ? 9.5 : 8) * canvasUiScale) + 'px Inter, system-ui';
                  ctx.save(); ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
                  fillReadableSpecimenText(markerNum, compactCX, compactCY); ctx.restore();
                }
                var exposureMarkerDrawn = drawExposureStateMarker(px, py, layout.visibility, isSel, isHov, focusMuted);
                if (!exposureMarkerDrawn && !isSel) {
                  var sysCol = layerStroke || '#94a3b8'; ctx.beginPath(); ctx.arc(px,py,5*pulse,0,Math.PI*2);
                  var pinGrad = ctx.createRadialGradient(px-1,py-1,1,px,py,5*pulse); pinGrad.addColorStop(0,isSel?'#fef08a':isHov?'#bfdbfe':isFocusRelated?'#cffafe':'#fff');
                  pinGrad.addColorStop(1,isSel?'#f59e0b':isHov?'#3b82f6':isFocusRelated?'#0891b2':sysCol); ctx.fillStyle=pinGrad; ctx.fill(); ctx.strokeStyle=isSel?'#f59e0b':'rgba(255,255,255,.7)'; ctx.lineWidth=1.5; ctx.stroke();
                }
                if (isHov) { ctx.beginPath(); ctx.arc(px,py,10,0,Math.PI*2); ctx.strokeStyle='rgba(96,165,250,.68)'; ctx.lineWidth=1.7; ctx.stroke(); }
                ctx.restore();
              });
              }

              // Annotation drawing overlay

              if (!d.quizMode && d.annotations && d.annotations.length > 0) {

                ctx.strokeStyle = '#ec4899'; ctx.lineWidth = 2; ctx.lineCap = 'round';

                d.annotations.forEach(function (ann) {

                  if (ann.prevX !== undefined) {

                    ctx.beginPath(); ctx.moveTo(ann.prevX * W, ann.prevY * H);

                    ctx.lineTo(ann.x * W, ann.y * H); ctx.stroke();

                  }

                  ctx.beginPath(); ctx.arc(ann.x * W, ann.y * H, 2, 0, Math.PI * 2);

                  ctx.fillStyle = '#ec4899'; ctx.fill();

                });

              }

              // Clear annotations button hint

              if (!d.quizMode && d.annotateMode && d.annotations && d.annotations.length > 0) {

                ctx.font = '8px Inter, system-ui'; ctx.fillStyle = 'rgba(236,72,153,0.6)';

                ctx.fillText('Use Clear annotations in Tools', 10, H - 5);

              }

              // Procedural instrument overlays
              var canvasProcedure = d.beforeTechniqueView ? {} : Object.assign({}, liveProcedureState, { tissueState: liveTissueState });
              if (d.beforeTechniqueView) {
                ctx.save(); ctx.font = 'bold 11px Inter, system-ui'; ctx.fillStyle = 'rgba(15,23,42,0.82)';
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(W - 132, 14, 116, 25, 7); ctx.fill(); } else { ctx.fillRect(W - 132, 14, 116, 25); }
                ctx.fillStyle = '#f8fafc'; ctx.fillText('Before technique', W - 121, 31); ctx.restore();
              }
              function drawProcedurePath(points, color, width, dashed) {
                if (!points || points.length < 2) return;
                ctx.save();
                ctx.strokeStyle = color; ctx.lineWidth = width; ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                ctx.setLineDash(dashed ? [6, 5] : []);
                ctx.beginPath(); ctx.moveTo(points[0].x * W, points[0].y * H);
                for (var pi = 1; pi < points.length; pi++) ctx.lineTo(points[pi].x * W, points[pi].y * H);
                ctx.stroke(); ctx.restore();
              }
              function drawPressureReplayTrail(points, samples, progress, color, dashed) {
                // Pressure is encoded redundantly by trail width, perpendicular ticks, a radial gauge, and numeric text.
                if (!points || points.length < 2) return null;
                var accessibleReplay = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var replayRatio = Math.max(0, Math.min(1, Number(progress) || 0));
                var segmentPosition = replayRatio * (points.length - 1);
                var finalSegment = Math.min(points.length - 2, Math.floor(segmentPosition));
                var finalMix = segmentPosition - finalSegment;
                var latestPoint = points[0], latestPressure = 0.34, latestAngle = 0;
                ctx.save(); ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                for (var replayTrailIndex = 0; replayTrailIndex <= finalSegment; replayTrailIndex++) {
                  var replayFrom = points[replayTrailIndex], replayTo = points[replayTrailIndex + 1];
                  var segmentMix = replayTrailIndex === finalSegment ? finalMix : 1;
                  if (replayRatio >= 1 && replayTrailIndex === finalSegment) segmentMix = 1;
                  var replayEnd = {
                    x: replayFrom.x + (replayTo.x - replayFrom.x) * segmentMix,
                    y: replayFrom.y + (replayTo.y - replayFrom.y) * segmentMix
                  };
                  var sampleIndex = samples && samples.length
                    ? Math.min(samples.length - 1, Math.round((replayTrailIndex + segmentMix) / Math.max(1, points.length - 1) * (samples.length - 1)))
                    : -1;
                  var samplePressure = sampleIndex >= 0 ? Number(samples[sampleIndex].pressure) : 0.34;
                  samplePressure = Math.max(0.08, Math.min(1, samplePressure || 0.34));
                  var fromX = replayFrom.x * W, fromY = replayFrom.y * H, endX = replayEnd.x * W, endY = replayEnd.y * H;
                  var segmentAngle = Math.atan2(endY - fromY, endX - fromX);
                  var trailWidth = (accessibleReplay ? 2.6 : 1.7) + samplePressure * 4.2;
                  ctx.strokeStyle = accessibleReplay ? '#ffffff' : color; ctx.lineWidth = trailWidth;
                  ctx.setLineDash(dashed ? [6, 5] : []);
                  ctx.beginPath(); ctx.moveTo(fromX, fromY); ctx.lineTo(endX, endY); ctx.stroke();
                  if (!dashed && (replayTrailIndex % 4 === 0 || replayTrailIndex === finalSegment)) {
                    var tickX = endX, tickY = endY, tickSize = 2.2 + samplePressure * 4;
                    ctx.setLineDash([]); ctx.lineWidth = accessibleReplay ? 1.7 : 1;
                    ctx.beginPath();
                    ctx.moveTo(tickX - Math.sin(segmentAngle) * tickSize, tickY + Math.cos(segmentAngle) * tickSize);
                    ctx.lineTo(tickX + Math.sin(segmentAngle) * tickSize, tickY - Math.cos(segmentAngle) * tickSize);
                    ctx.stroke();
                  }
                  latestPoint = replayEnd; latestPressure = samplePressure; latestAngle = segmentAngle;
                  if (replayTrailIndex === finalSegment) break;
                }
                ctx.restore();
                return { point: latestPoint, pressure: latestPressure, angle: latestAngle };
              }
              function drawAnatomicalAccessCorridor(points) {
                if (!points || points.length < 2) return;
                // The access corridor shows full instrument clearance, travel direction, endpoints, and depth risk instead of a single target line.
                var corridorMode = d.visualRealism || visualRealism;
                var corridorMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var corridorDeep = (d.incisionDepth || 'shallow') === 'deep';
                var corridorAccessible = corridorMode === 'accessible' || highContrastEnabled;
                var corridorHalfWidth = (spec.bodyShape === 'worm' ? 6.2 : (spec.bodyShape === 'eye' ? 8.6 : 7.4)) + corridorMaterial.compliance * 3.2;
                var corridorLeft = [], corridorRight = [], corridorSegments = [], corridorLength = 0;
                for (var corridorIndex = 0; corridorIndex < points.length; corridorIndex++) {
                  var corridorPrevious = points[Math.max(0, corridorIndex - 1)], corridorNext = points[Math.min(points.length - 1, corridorIndex + 1)];
                  var corridorDX = (corridorNext.x - corridorPrevious.x) * W, corridorDY = (corridorNext.y - corridorPrevious.y) * H;
                  var corridorTangentLength = Math.sqrt(corridorDX * corridorDX + corridorDY * corridorDY) || 1;
                  var corridorNX = -corridorDY / corridorTangentLength, corridorNY = corridorDX / corridorTangentLength;
                  var corridorTaper = corridorIndex === 0 || corridorIndex === points.length - 1 ? 0.72 : 1;
                  corridorLeft.push({ x: points[corridorIndex].x * W + corridorNX * corridorHalfWidth * corridorTaper, y: points[corridorIndex].y * H + corridorNY * corridorHalfWidth * corridorTaper });
                  corridorRight.push({ x: points[corridorIndex].x * W - corridorNX * corridorHalfWidth * corridorTaper, y: points[corridorIndex].y * H - corridorNY * corridorHalfWidth * corridorTaper });
                  if (corridorIndex > 0) {
                    var segmentDX = (points[corridorIndex].x - points[corridorIndex - 1].x) * W, segmentDY = (points[corridorIndex].y - points[corridorIndex - 1].y) * H;
                    var segmentLength = Math.sqrt(segmentDX * segmentDX + segmentDY * segmentDY);
                    corridorSegments.push({ from: points[corridorIndex - 1], to: points[corridorIndex], start: corridorLength, length: segmentLength });
                    corridorLength += segmentLength;
                  }
                }
                function corridorPointAt(ratio) {
                  var targetDistance = Math.max(0, Math.min(1, ratio)) * corridorLength;
                  var segment = corridorSegments[corridorSegments.length - 1];
                  for (var segmentIndex = 0; segmentIndex < corridorSegments.length; segmentIndex++) {
                    if (targetDistance <= corridorSegments[segmentIndex].start + corridorSegments[segmentIndex].length) { segment = corridorSegments[segmentIndex]; break; }
                  }
                  var localRatio = segment.length ? (targetDistance - segment.start) / segment.length : 0;
                  var x = (segment.from.x + (segment.to.x - segment.from.x) * localRatio) * W;
                  var y = (segment.from.y + (segment.to.y - segment.from.y) * localRatio) * H;
                  return { x: x, y: y, angle: Math.atan2((segment.to.y - segment.from.y) * H, (segment.to.x - segment.from.x) * W) };
                }
                ctx.save(); ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                ctx.beginPath(); ctx.moveTo(corridorLeft[0].x, corridorLeft[0].y);
                for (var corridorLeftIndex = 1; corridorLeftIndex < corridorLeft.length; corridorLeftIndex++) ctx.lineTo(corridorLeft[corridorLeftIndex].x, corridorLeft[corridorLeftIndex].y);
                for (var corridorRightIndex = corridorRight.length - 1; corridorRightIndex >= 0; corridorRightIndex--) ctx.lineTo(corridorRight[corridorRightIndex].x, corridorRight[corridorRightIndex].y);
                ctx.closePath();
                if (corridorAccessible) {
                  ctx.fillStyle = corridorDeep ? 'rgba(190,24,93,0.24)' : 'rgba(13,148,136,0.18)';
                } else {
                  var corridorGradient = ctx.createLinearGradient(points[0].x * W, points[0].y * H, points[points.length - 1].x * W, points[points.length - 1].y * H);
                  corridorGradient.addColorStop(0, corridorDeep ? 'rgba(251,113,133,0.08)' : 'rgba(45,212,191,0.08)');
                  corridorGradient.addColorStop(0.5, corridorDeep ? 'rgba(251,113,133,0.22)' : 'rgba(45,212,191,0.20)');
                  corridorGradient.addColorStop(1, corridorDeep ? 'rgba(251,113,133,0.08)' : 'rgba(45,212,191,0.08)');
                  ctx.fillStyle = corridorGradient;
                }
                ctx.fill();
                ctx.strokeStyle = corridorAccessible ? (corridorDeep ? '#e11d48' : '#0f766e') : (corridorDeep ? '#fb7185' : '#5eead4');
                ctx.lineWidth = corridorAccessible ? 2.4 : 1.35;
                [corridorLeft, corridorRight].forEach(function (corridorEdge) {
                  ctx.beginPath(); ctx.moveTo(corridorEdge[0].x, corridorEdge[0].y);
                  for (var corridorEdgeIndex = 1; corridorEdgeIndex < corridorEdge.length; corridorEdgeIndex++) ctx.lineTo(corridorEdge[corridorEdgeIndex].x, corridorEdge[corridorEdgeIndex].y);
                  ctx.stroke();
                });
                ctx.setLineDash(corridorDeep ? [3, 3] : [7, 5]);
                ctx.lineWidth = corridorAccessible ? 2 : 1.2;
                ctx.beginPath(); ctx.moveTo(points[0].x * W, points[0].y * H);
                for (var corridorCenterIndex = 1; corridorCenterIndex < points.length; corridorCenterIndex++) ctx.lineTo(points[corridorCenterIndex].x * W, points[corridorCenterIndex].y * H);
                ctx.stroke(); ctx.setLineDash([]);
                for (var corridorMarkIndex = 1; corridorMarkIndex <= 5; corridorMarkIndex++) {
                  var corridorMark = corridorPointAt(corridorMarkIndex / 6);
                  ctx.save(); ctx.translate(corridorMark.x, corridorMark.y); ctx.rotate(corridorMark.angle);
                  ctx.globalAlpha = corridorAccessible ? 0.92 : 0.64;
                  if (corridorDeep) {
                    ctx.beginPath(); ctx.moveTo(-4, -4); ctx.lineTo(4, 4); ctx.moveTo(4, -4); ctx.lineTo(-4, 4); ctx.stroke();
                  } else {
                    ctx.beginPath(); ctx.moveTo(-4, -4); ctx.lineTo(1, 0); ctx.lineTo(-4, 4); ctx.stroke();
                  }
                  ctx.restore();
                }
                var corridorStart = corridorPointAt(0), corridorFinish = corridorPointAt(1), corridorMid = corridorPointAt(0.5);
                ctx.fillStyle = corridorAccessible ? '#ffffff' : '#ccfbf1'; ctx.strokeStyle = corridorAccessible ? '#0f172a' : '#0f766e'; ctx.lineWidth = 1.8;
                ctx.beginPath(); ctx.arc(corridorStart.x, corridorStart.y, 5.5, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                ctx.beginPath(); ctx.moveTo(corridorFinish.x, corridorFinish.y - 6.5); ctx.lineTo(corridorFinish.x + 6.5, corridorFinish.y); ctx.lineTo(corridorFinish.x, corridorFinish.y + 6.5); ctx.lineTo(corridorFinish.x - 6.5, corridorFinish.y); ctx.closePath(); ctx.fill(); ctx.stroke();
                var defaultCorridorDepthLabel = 'SAFE SHALLOW CORRIDOR';
                var corridorDepthLabel = corridorDeep ? 'DEEP PRACTICE \u00B7 PROTECTED' : (procedureProtocol.corridorLabel ? 'SAFE \u00B7 ' + procedureProtocol.corridorLabel : defaultCorridorDepthLabel);
                ctx.font = 'bold 8px Inter, system-ui';
                var corridorLabelWidth = Math.ceil(ctx.measureText(corridorDepthLabel).width) + 12;
                var corridorLabelX = Math.max(8, Math.min(W - corridorLabelWidth - 8, corridorMid.x - corridorLabelWidth / 2));
                var corridorLabelY = Math.max(8, corridorMid.y - corridorHalfWidth - 22);
                ctx.fillStyle = corridorAccessible ? '#ffffff' : 'rgba(15,23,42,0.88)';
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(corridorLabelX, corridorLabelY, corridorLabelWidth, 17, 5);
                else ctx.rect(corridorLabelX, corridorLabelY, corridorLabelWidth, 17);
                ctx.fill();
                ctx.strokeStyle = corridorDeep ? '#fb7185' : '#5eead4'; ctx.lineWidth = corridorAccessible ? 1.6 : 0.8; ctx.stroke();
                ctx.fillStyle = corridorDeep ? (corridorAccessible ? '#9f1239' : '#fecdd3') : (corridorAccessible ? '#134e4a' : '#99f6e4');
                ctx.fillText(corridorDepthLabel, corridorLabelX + 6, corridorLabelY + 12);
                ctx.restore();
              }
              function drawProcedureOpening(points, extended) {
                if (!points || points.length < 2) return;
                var openingMode = d.visualRealism || visualRealism;
                var openingMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var openingMetrics = extended ? canvasProcedure.extensionMetrics : canvasProcedure.incisionMetrics;
                var openingControl = openingMetrics ? Number(openingMetrics.control == null ? openingMetrics.smoothness : openingMetrics.control) / 100 : 0.8;
                var fieldWidthFactor = 0.76 + liveObservationField.aperture / 100 * 0.34 + liveObservationField.stability / 100 * 0.12;
                var halfWidth = (extended ? 7.5 : 3.8) * (0.82 + openingMaterial.compliance * 0.34) * fieldWidthFactor;
                var openingTransitionActive = (liveTissueTransitionAction === 'scalpel' && !extended) || (liveTissueTransitionAction === 'scissors' && extended);
                var openingUndoActive = (liveTissueTransitionAction === 'undo-scalpel' && !extended) || (liveTissueTransitionAction === 'undo-scissors' && extended);
                var openingTransition = openingTransitionActive ? liveTissueTransitionProgress : (openingUndoActive ? 1 - liveTissueTransitionProgress : 1);
                halfWidth *= 0.18 + openingTransition * 0.82;
                var leftEdge = [], rightEdge = [];
                for (var oi = 0; oi < points.length; oi++) {
                  var previousPoint = points[Math.max(0, oi - 1)], nextPoint = points[Math.min(points.length - 1, oi + 1)];
                  var tangentX = (nextPoint.x - previousPoint.x) * W, tangentY = (nextPoint.y - previousPoint.y) * H;
                  var tangentLength = Math.sqrt(tangentX * tangentX + tangentY * tangentY) || 1;
                  var normalX = -tangentY / tangentLength, normalY = tangentX / tangentLength;
                  var controlVariation = (1 - Math.max(0, Math.min(1, openingControl))) * Math.sin(oi * 1.73) * 0.22;
                  var endpointTaper = oi === 0 || oi === points.length - 1 ? 0.58 : 1;
                  var localWidth = halfWidth * endpointTaper * (0.96 + controlVariation);
                  leftEdge.push({ x: points[oi].x * W + normalX * localWidth, y: points[oi].y * H + normalY * localWidth });
                  rightEdge.push({ x: points[oi].x * W - normalX * localWidth, y: points[oi].y * H - normalY * localWidth });
                }
                ctx.save(); ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                ctx.beginPath(); ctx.moveTo(leftEdge[0].x, leftEdge[0].y);
                for (var leftIdx = 1; leftIdx < leftEdge.length; leftIdx++) ctx.lineTo(leftEdge[leftIdx].x, leftEdge[leftIdx].y);
                for (var rightIdx = rightEdge.length - 1; rightIdx >= 0; rightIdx--) ctx.lineTo(rightEdge[rightIdx].x, rightEdge[rightIdx].y);
                ctx.closePath();
                var openingGradient = ctx.createLinearGradient(leftEdge[0].x, leftEdge[0].y, rightEdge[Math.floor(rightEdge.length / 2)].x, rightEdge[Math.floor(rightEdge.length / 2)].y);
                openingGradient.addColorStop(0, openingMode === 'accessible' ? '#f8fafc' : '#7f4f55');
                openingGradient.addColorStop(0.22, openingMode === 'accessible' ? '#020617' : '#32181d');
                openingGradient.addColorStop(0.78, openingMode === 'accessible' ? '#020617' : '#160d11');
                openingGradient.addColorStop(1, openingMode === 'accessible' ? '#f8fafc' : '#a56970');
                ctx.shadowColor = 'rgba(2,6,23,0.78)'; ctx.shadowBlur = extended ? 12 : 6;
                ctx.fillStyle = openingGradient; ctx.fill(); ctx.shadowBlur = 0;
                // Subsurface detail resolves progressively as the observation field improves.
                ctx.save(); ctx.clip();
                var fieldDetailCount = 3 + Math.round(liveObservationField.quality / 14);
                ctx.globalAlpha = openingMode === 'accessible' ? 0.7 : 0.18 + liveObservationField.quality / 240;
                ctx.strokeStyle = openingMode === 'accessible' ? '#fde68a' : (liveObservationField.clarity < 55 ? '#cbd5e1' : '#fecdd3');
                ctx.lineWidth = openingMode === 'accessible' ? 1.25 : 0.72;
                for (var fieldDetailIdx = 0; fieldDetailIdx < fieldDetailCount; fieldDetailIdx++) {
                  var fieldT = (fieldDetailIdx + 0.65) / (fieldDetailCount + 0.3);
                  var fieldFloat = fieldT * (points.length - 1);
                  var fieldIndex = Math.min(points.length - 2, Math.floor(fieldFloat));
                  var fieldLocal = fieldFloat - fieldIndex;
                  var fieldA = points[fieldIndex], fieldB = points[fieldIndex + 1];
                  var fieldX = (fieldA.x + (fieldB.x - fieldA.x) * fieldLocal) * W;
                  var fieldY = (fieldA.y + (fieldB.y - fieldA.y) * fieldLocal) * H;
                  var fieldDX = (fieldB.x - fieldA.x) * W, fieldDY = (fieldB.y - fieldA.y) * H;
                  var fieldLength = Math.sqrt(fieldDX * fieldDX + fieldDY * fieldDY) || 1;
                  var fieldNX = -fieldDY / fieldLength, fieldNY = fieldDX / fieldLength;
                  var fieldReach = halfWidth * (0.45 + (fieldDetailIdx % 3) * 0.11);
                  ctx.beginPath();
                  ctx.moveTo(fieldX - fieldNX * fieldReach, fieldY - fieldNY * fieldReach);
                  ctx.quadraticCurveTo(fieldX + fieldDX / fieldLength * 3, fieldY + fieldDY / fieldLength * 3, fieldX + fieldNX * fieldReach, fieldY + fieldNY * fieldReach);
                  ctx.stroke();
                }
                if (liveObservationField.clarity < 62) {
                  ctx.globalAlpha = Math.min(0.34, (62 - liveObservationField.clarity) / 100);
                  ctx.fillStyle = '#e2e8f0'; ctx.fillRect(0, 0, W, H);
                }
                ctx.restore();
                [leftEdge, rightEdge].forEach(function (edge, edgeIdx) {
                  ctx.beginPath(); ctx.moveTo(edge[0].x, edge[0].y);
                  for (var edgePointIdx = 1; edgePointIdx < edge.length; edgePointIdx++) ctx.lineTo(edge[edgePointIdx].x, edge[edgePointIdx].y);
                  ctx.strokeStyle = openingMode === 'accessible' ? (edgeIdx ? '#f8fafc' : '#facc15') : (edgeIdx ? 'rgba(254,202,202,0.72)' : 'rgba(190,110,118,0.82)');
                  ctx.lineWidth = openingMode === 'accessible' ? 2.3 : 1.25; ctx.stroke();
                });
                ctx.beginPath(); ctx.moveTo(points[0].x * W, points[0].y * H);
                for (var centerIdx = 1; centerIdx < points.length; centerIdx++) ctx.lineTo(points[centerIdx].x * W, points[centerIdx].y * H);
                ctx.strokeStyle = openingMode === 'accessible' ? '#020617' : 'rgba(15,8,12,0.76)';
                ctx.lineWidth = extended ? 2.2 : 1.1; ctx.stroke();
                if (liveTissueState.trauma > 18) {
                  var stressMarkCount = Math.min(7, Math.max(2, Math.round(liveTissueState.trauma / 12)));
                  ctx.strokeStyle = openingMode === 'accessible' ? '#e11d48' : 'rgba(248,113,113,0.72)'; ctx.lineWidth = 0.85;
                  for (var stressIdx = 0; stressIdx < stressMarkCount; stressIdx++) {
                    var stressPoint = points[Math.min(points.length - 1, Math.round((stressIdx + 1) / (stressMarkCount + 1) * (points.length - 1)))];
                    var stressDirection = stressIdx % 2 ? 1 : -1;
                    ctx.beginPath(); ctx.moveTo(stressPoint.x * W - 3, stressPoint.y * H + stressDirection * 2);
                    ctx.lineTo(stressPoint.x * W + 3, stressPoint.y * H + stressDirection * 5); ctx.stroke();
                  }
                }
                // Edge microstructure keeps the opening visually tied to the organism instead of reading as a generic slit.
                ctx.globalAlpha = openingMode === 'accessible' ? 0.76 : 0.38;
                ctx.strokeStyle = openingMode === 'accessible' ? '#facc15' : openingMaterial.accent;
                ctx.fillStyle = openingMaterial.accent; ctx.lineWidth = 0.8;
                for (var openingDetailIdx = 1; openingDetailIdx < points.length - 1; openingDetailIdx += 2) {
                  var openingDetailPoint = points[openingDetailIdx];
                  var openingPrevious = points[Math.max(0, openingDetailIdx - 1)], openingNext = points[Math.min(points.length - 1, openingDetailIdx + 1)];
                  var openingDX = (openingNext.x - openingPrevious.x) * W, openingDY = (openingNext.y - openingPrevious.y) * H;
                  var openingLength = Math.sqrt(openingDX * openingDX + openingDY * openingDY) || 1;
                  var openingNX = -openingDY / openingLength, openingNY = openingDX / openingLength;
                  if (openingMaterial.pattern === 'facets') {
                    ctx.beginPath(); ctx.arc(openingDetailPoint.x * W, openingDetailPoint.y * H, 1.8, 0, Math.PI * 2); ctx.stroke();
                  } else {
                    var detailReach = openingMaterial.pattern === 'segments' ? halfWidth * 1.35 : halfWidth * 0.92;
                    ctx.beginPath();
                    ctx.moveTo(openingDetailPoint.x * W - openingNX * detailReach, openingDetailPoint.y * H - openingNY * detailReach);
                    ctx.lineTo(openingDetailPoint.x * W + openingNX * detailReach, openingDetailPoint.y * H + openingNY * detailReach);
                    ctx.stroke();
                  }
                }
                ctx.restore();
              }
              function drawTissueFlaps(guide, pins, forcepsPoint) {
                if (!guide || guide.length < 2) return;
                var flapMode = d.visualRealism || visualRealism;
                var flapMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var flapTissue = normalizeTissueState(liveTissueState);
                var flapPhysics = tissuePhysicsData(flapTissue);
                var start = guide[0], middle = guide[Math.floor((guide.length - 1) / 2)], end = guide[guide.length - 1];
                var directionX = end.x - start.x, directionY = end.y - start.y;
                var directionLength = Math.sqrt(directionX * directionX + directionY * directionY) || 1;
                var normalX = -directionY / directionLength, normalY = directionX / directionLength;
                var midpoint = { x: (start.x + end.x + middle.x) / 3, y: (start.y + end.y + middle.y) / 3 };
                var pinPull = (pins || []).reduce(function (sum, pin) {
                  var dx = pin.x - midpoint.x, dy = pin.y - midpoint.y;
                  return sum + Math.min(0.055, Math.sqrt(dx * dx + dy * dy) * 0.18);
                }, 0) / Math.max(1, (pins || []).length);
                var forcepsPull = forcepsPoint ? Math.min(0.022, procedurePointDistance(forcepsPoint, midpoint) * 0.1 + 0.008) : 0;
                var settleAmplitude = (100 - flapPhysics.damping) / 100 * 0.0032;
                var settle = dissMotionReduced ? 0 : Math.sin(dissTick * 0.022) * settleAmplitude;
                var dynamicCompliance = 0.62 + flapPhysics.compliance / 100 * 0.58;
                var tensionSpread = 0.86 + flapTissue.tension / 100 * 0.22;
                var spread = (flapMode === 'realistic' ? 0.074 : 0.058) * (0.76 + flapMaterial.compliance * 0.24) * dynamicCompliance * tensionSpread + pinPull + forcepsPull + settle;
                var flapTransition = liveTissueTransitionAction === 'forceps' ? liveTissueTransitionProgress : (liveTissueTransitionAction === 'undo-forceps' ? 1 - liveTissueTransitionProgress : 1);
                spread *= 0.12 + flapTransition * 0.88;
                var inset = 0.009;
                var layerColor = ((spec.layers[currentLayerIdx] || {}).color) || '#d89b8f';
                function offsetFlapPoint(point, amount) { return { x: point.x + normalX * amount, y: point.y + normalY * amount }; }
                var flaps = [
                  [offsetFlapPoint(start, inset), offsetFlapPoint(middle, inset), offsetFlapPoint(end, inset), offsetFlapPoint(end, spread), offsetFlapPoint(middle, spread * 1.08), offsetFlapPoint(start, spread)],
                  [offsetFlapPoint(start, -inset), offsetFlapPoint(start, -spread), offsetFlapPoint(middle, -spread * 1.08), offsetFlapPoint(end, -spread), offsetFlapPoint(end, -inset), offsetFlapPoint(middle, -inset)]
                ];
                var flapSag = flapPhysics.sag / 100 * 0.032 * Math.max(0.2, 1 - (pins || []).length * 0.34);
                flaps.forEach(function (flap, flapIndex) {
                  flap.forEach(function (flapPoint, flapPointIndex) {
                    var isOuterPoint = flapIndex === 0 ? flapPointIndex >= 3 : flapPointIndex >= 1 && flapPointIndex <= 3;
                    if (!isOuterPoint) return;
                    var outerWeight = flapPointIndex === 4 || flapPointIndex === 2 ? 1 : 0.72;
                    var flutter = dissMotionReduced ? 0 : Math.sin(dissTick * 0.027 + flapIndex * 1.7 + flapPointIndex * 0.6) * settleAmplitude * 0.7;
                    flapPoint.y += flapSag * outerWeight + flutter;
                  });
                });

                // Next-layer tissue bed becomes visible beneath the retracted flaps.
                var nextTissueLayer = spec.layers[Math.min(spec.layers.length - 1, currentLayerIdx + 1)] || {};
                var nextTissueId = nextTissueLayer.id || 'organs';
                var nextTissueColor = nextTissueId === 'muscle' ? '#a94452'
                  : (nextTissueId === 'skeleton' ? '#cbd5e1'
                    : (nextTissueId === 'organs' || nextTissueId === 'chambers' || nextTissueId === 'interior' ? '#7c4a3f' : '#816066'));
                var bedSpread = Math.max(0.026, spread * (0.52 + liveObservationField.aperture / 500));
                var bedPoints = [
                  offsetFlapPoint(start, bedSpread), offsetFlapPoint(middle, bedSpread * 0.92), offsetFlapPoint(end, bedSpread),
                  offsetFlapPoint(end, -bedSpread), offsetFlapPoint(middle, -bedSpread * 0.92), offsetFlapPoint(start, -bedSpread)
                ];
                ctx.save(); ctx.beginPath(); ctx.moveTo(bedPoints[0].x * W, bedPoints[0].y * H);
                for (var bedPointIdx = 1; bedPointIdx < bedPoints.length; bedPointIdx++) ctx.lineTo(bedPoints[bedPointIdx].x * W, bedPoints[bedPointIdx].y * H);
                ctx.closePath();
                var tissueBedGradient = ctx.createLinearGradient(bedPoints[0].x * W, bedPoints[0].y * H, bedPoints[4].x * W, bedPoints[4].y * H);
                tissueBedGradient.addColorStop(0, flapMode === 'accessible' ? '#fde68a' : nextTissueColor);
                tissueBedGradient.addColorStop(0.46, flapMode === 'accessible' ? '#fef3c7' : '#5c3138');
                tissueBedGradient.addColorStop(1, flapMode === 'accessible' ? '#fbbf24' : nextTissueColor);
                ctx.fillStyle = tissueBedGradient; ctx.shadowColor = 'rgba(2,6,23,0.72)'; ctx.shadowBlur = 12; ctx.fill(); ctx.shadowBlur = 0;
                ctx.strokeStyle = flapMode === 'accessible' ? '#422006' : 'rgba(254,202,202,0.42)'; ctx.lineWidth = flapMode === 'accessible' ? 1.8 : 0.8; ctx.stroke();
                ctx.globalAlpha = (flapMode === 'realistic' ? 0.24 : 0.16) + liveObservationField.quality / 360;
                ctx.strokeStyle = nextTissueId === 'skeleton' ? '#f8fafc' : (liveObservationField.clarity < 55 ? '#cbd5e1' : '#fecdd3'); ctx.lineWidth = 0.75;
                var bedFiberCount = 3 + Math.round(liveObservationField.quality / 16);
                for (var bedFiberIdx = 0; bedFiberIdx < bedFiberCount; bedFiberIdx++) {
                  var bedFiberT = (bedFiberIdx + 0.7) / (bedFiberCount + 0.4);
                  var bedFiberBaseX = start.x + (end.x - start.x) * bedFiberT;
                  var bedFiberBaseY = start.y + (end.y - start.y) * bedFiberT;
                  var bedFiberHalf = bedSpread * (0.46 + (bedFiberIdx % 2) * 0.16);
                  ctx.beginPath();
                  ctx.moveTo((bedFiberBaseX - normalX * bedFiberHalf) * W, (bedFiberBaseY - normalY * bedFiberHalf) * H);
                  ctx.quadraticCurveTo(bedFiberBaseX * W, bedFiberBaseY * H, (bedFiberBaseX + normalX * bedFiberHalf) * W, (bedFiberBaseY + normalY * bedFiberHalf) * H);
                  ctx.stroke();
                }
                // Light-aware cavity depth separates the retracted field into rim, wall, and deeper bed.
                function traceRetractedFieldContour(scale) {
                  ctx.beginPath();
                  bedPoints.forEach(function (bedPoint, bedPointIndex) {
                    var contourX = (midpoint.x + (bedPoint.x - midpoint.x) * scale) * W;
                    var contourY = (midpoint.y + (bedPoint.y - midpoint.y) * scale) * H;
                    if (!bedPointIndex) ctx.moveTo(contourX, contourY); else ctx.lineTo(contourX, contourY);
                  });
                  ctx.closePath();
                }
                var cavityLightVector = {
                  overhead: { x: 0, y: -1 }, left: { x: -1, y: -0.18 },
                  right: { x: 1, y: -0.18 }, raking: { x: -0.74, y: 0.68 }
                }[lightDirection] || { x: 0, y: -1 };
                var cavityLightStrength = Math.max(0.2, Math.min(1, (Number(d.lightIntensity || lightIntensity) || 68) / 100));
                var cavityCenterX = midpoint.x * W, cavityCenterY = midpoint.y * H;
                var cavitySpan = Math.max(18, bedSpread * Math.max(W, H) * 1.55);
                ctx.save(); traceRetractedFieldContour(0.98); ctx.clip();
                if (flapMode === 'accessible' || highContrastEnabled) {
                  ctx.fillStyle = 'rgba(15,23,42,0.30)'; ctx.fillRect(cavityCenterX - cavitySpan, cavityCenterY - cavitySpan, cavitySpan * 2, cavitySpan * 2);
                } else {
                  var cavityShadow = ctx.createLinearGradient(
                    cavityCenterX + cavityLightVector.x * cavitySpan, cavityCenterY + cavityLightVector.y * cavitySpan,
                    cavityCenterX - cavityLightVector.x * cavitySpan, cavityCenterY - cavityLightVector.y * cavitySpan
                  );
                  cavityShadow.addColorStop(0, 'rgba(15,8,12,0)');
                  cavityShadow.addColorStop(0.52, 'rgba(15,8,12,0.06)');
                  cavityShadow.addColorStop(1, 'rgba(15,8,12,' + (0.26 + cavityLightStrength * 0.22) + ')');
                  ctx.globalCompositeOperation = 'multiply'; ctx.fillStyle = cavityShadow;
                  ctx.fillRect(cavityCenterX - cavitySpan, cavityCenterY - cavitySpan, cavitySpan * 2, cavitySpan * 2);
                  var cavityHighlight = ctx.createLinearGradient(
                    cavityCenterX + cavityLightVector.x * cavitySpan, cavityCenterY + cavityLightVector.y * cavitySpan,
                    cavityCenterX, cavityCenterY
                  );
                  cavityHighlight.addColorStop(0, 'rgba(255,241,230,' + (0.08 + cavityLightStrength * 0.12) + ')');
                  cavityHighlight.addColorStop(1, 'rgba(255,241,230,0)');
                  ctx.globalCompositeOperation = 'screen'; ctx.fillStyle = cavityHighlight;
                  ctx.fillRect(cavityCenterX - cavitySpan, cavityCenterY - cavitySpan, cavitySpan * 2, cavitySpan * 2);
                }
                ctx.globalCompositeOperation = 'source-over';
                for (var cavityContourIdx = 0; cavityContourIdx < 3; cavityContourIdx++) {
                  var cavityContourScale = 0.82 - cavityContourIdx * 0.18;
                  traceRetractedFieldContour(cavityContourScale);
                  ctx.strokeStyle = (flapMode === 'accessible' || highContrastEnabled)
                    ? (cavityContourIdx === 2 ? '#facc15' : '#f8fafc')
                    : 'rgba(254,202,202,' + (0.22 - cavityContourIdx * 0.045) + ')';
                  ctx.lineWidth = (flapMode === 'accessible' || highContrastEnabled) ? (1.8 - cavityContourIdx * 0.25) : 0.65;
                  ctx.setLineDash((flapMode === 'accessible' || highContrastEnabled) && cavityContourIdx === 2 ? [4, 3] : []);
                  ctx.stroke();
                }
                ctx.setLineDash([]);
                if (flapMode !== 'accessible' && sceneDetail && flapTissue.moisture > 48) {
                  var cavitySheenOffsetX = cavityLightVector.x * 2.5, cavitySheenOffsetY = cavityLightVector.y * 2.5;
                  ctx.strokeStyle = 'rgba(255,255,255,' + Math.min(0.26, 0.05 + flapTissue.moisture / 520) + ')';
                  ctx.lineWidth = 1.1;
                  ctx.beginPath();
                  ctx.moveTo(start.x * W + cavitySheenOffsetX, start.y * H + cavitySheenOffsetY);
                  ctx.quadraticCurveTo(middle.x * W + cavitySheenOffsetX, middle.y * H + cavitySheenOffsetY, end.x * W + cavitySheenOffsetX, end.y * H + cavitySheenOffsetY);
                  ctx.stroke();
                }
                ctx.restore();
                ctx.restore();

                flaps.forEach(function (flap, flapIdx) {
                  ctx.save(); ctx.beginPath(); ctx.moveTo(flap[0].x * W, flap[0].y * H);
                  for (var fi = 1; fi < flap.length; fi++) ctx.lineTo(flap[fi].x * W, flap[fi].y * H);
                  ctx.closePath(); ctx.shadowColor = 'rgba(2,6,23,0.68)'; ctx.shadowBlur = 14;
                  var flapLiftPixels = 4 + spread * Math.min(W, H) * 0.18;
                  ctx.shadowOffsetX = -cavityLightVector.x * flapLiftPixels + normalX * (flapIdx ? -2.5 : 2.5);
                  ctx.shadowOffsetY = -cavityLightVector.y * flapLiftPixels + normalY * (flapIdx ? -2.5 : 2.5);
                  var flapGrad = ctx.createLinearGradient(flap[0].x * W, flap[0].y * H, flap[4].x * W, flap[4].y * H);
                  flapGrad.addColorStop(0, layerColor); flapGrad.addColorStop(0.7, flapMode === 'realistic' ? '#8f5960' : '#a87579'); flapGrad.addColorStop(1, '#5f3b43');
                  ctx.fillStyle = flapGrad; ctx.fill(); ctx.shadowBlur = 0;
                  // A light-facing cut rim reinforces flap elevation without adding another label.
                  var cutRim = flapIdx === 0 ? [flap[0], flap[1], flap[2]] : [flap[4], flap[5], flap[0]];
                  ctx.beginPath(); ctx.moveTo(cutRim[0].x * W, cutRim[0].y * H);
                  for (var cutRimIdx = 1; cutRimIdx < cutRim.length; cutRimIdx++) ctx.lineTo(cutRim[cutRimIdx].x * W, cutRim[cutRimIdx].y * H);
                  ctx.strokeStyle = flapMode === 'accessible' ? '#facc15' : 'rgba(255,241,230,' + (0.22 + cavityLightStrength * 0.24) + ')';
                  ctx.lineWidth = flapMode === 'accessible' ? 2.2 : 1.35; ctx.stroke();
                  if (flapTissue.moisture > 55) {
                    ctx.globalAlpha = 0.08 + flapTissue.moisture / 100 * 0.18; ctx.strokeStyle = '#ffffff'; ctx.lineWidth = 1.1;
                    ctx.beginPath(); ctx.moveTo(flap[0].x * W, flap[0].y * H);
                    ctx.quadraticCurveTo(flap[2].x * W, flap[2].y * H, flap[4].x * W, flap[4].y * H); ctx.stroke(); ctx.globalAlpha = 1;
                  }
                  ctx.strokeStyle = flapMode === 'accessible' ? '#f8fafc' : 'rgba(254,202,202,0.68)'; ctx.lineWidth = flapMode === 'accessible' ? 2.5 : 1.2; ctx.stroke();
                  if (flapMode === 'realistic') {
                    ctx.globalAlpha = 0.24; ctx.strokeStyle = '#fee2e2'; ctx.lineWidth = 0.8;
                    for (var fiberIdx = 0; fiberIdx < 3; fiberIdx++) {
                      var fiberT = (fiberIdx + 1) / 4;
                      var fiberStart = { x: start.x + (end.x - start.x) * fiberT, y: start.y + (end.y - start.y) * fiberT };
                      var fiberOuter = offsetFlapPoint(fiberStart, (flapIdx ? -1 : 1) * spread * (0.72 + fiberIdx * 0.05));
                      ctx.beginPath(); ctx.moveTo(fiberStart.x * W, fiberStart.y * H); ctx.lineTo(fiberOuter.x * W, fiberOuter.y * H); ctx.stroke();
                    }
                  }
                  ctx.restore();
                });
                (pins || []).forEach(function (pin) {
                  var pinSide = ((pin.x - midpoint.x) * normalX + (pin.y - midpoint.y) * normalY) >= 0 ? 1 : -1;
                  var flapAnchor = offsetFlapPoint(middle, spread * pinSide);
                  ctx.save(); ctx.setLineDash([3, 3]); ctx.strokeStyle = flapMode === 'accessible' ? '#facc15' : 'rgba(226,232,240,0.58)'; ctx.lineWidth = 1.2;
                  ctx.beginPath(); ctx.moveTo(flapAnchor.x * W, flapAnchor.y * H); ctx.lineTo(pin.x * W, pin.y * H); ctx.stroke(); ctx.restore();
                });
              }
              function drawForcepsTractionPreview(guide, pointer) {
                if (!guide || guide.length < 2 || !pointer) return;
                // Live traction converts grip calibration, material compliance, and tissue tension into a visible pre-commit tissue tent.
                var directDrag = canvas._forcepsDrag && canvas._forcepsDrag.active ? canvas._forcepsDrag : null;
                var contactPointer = directDrag ? directDrag.start : pointer;
                var directAssessment = directDrag ? (directDrag.assessment || forcepsDragAssessment(canvas, directDrag.current, Date.now())) : null;
                var tractionMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var tractionTissue = normalizeTissueState(liveTissueState);
                var tractionAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var nearest = null;
                for (var tractionSegmentIndex = 1; tractionSegmentIndex < guide.length; tractionSegmentIndex++) {
                  var tractionA = guide[tractionSegmentIndex - 1], tractionB = guide[tractionSegmentIndex];
                  var tractionVX = tractionB.x - tractionA.x, tractionVY = tractionB.y - tractionA.y;
                  var tractionLengthSq = tractionVX * tractionVX + tractionVY * tractionVY || 1;
                  var tractionT = Math.max(0, Math.min(1, ((contactPointer.x - tractionA.x) * tractionVX + (contactPointer.y - tractionA.y) * tractionVY) / tractionLengthSq));
                  var tractionX = tractionA.x + tractionVX * tractionT, tractionY = tractionA.y + tractionVY * tractionT;
                  var tractionDX = contactPointer.x - tractionX, tractionDY = contactPointer.y - tractionY;
                  var tractionDistance = Math.sqrt(tractionDX * tractionDX + tractionDY * tractionDY);
                  if (!nearest || tractionDistance < nearest.distance) nearest = { x: tractionX, y: tractionY, dx: tractionVX, dy: tractionVY, distance: tractionDistance };
                }
                if (!nearest) return;
                var baseX = nearest.x * W, baseY = nearest.y * H;
                var renderPointer = directDrag ? directDrag.current : pointer;
                var pointerX = renderPointer.x * W, pointerY = renderPointer.y * H;
                var tangentX = nearest.dx * W, tangentY = nearest.dy * H;
                var tangentLength = Math.sqrt(tangentX * tangentX + tangentY * tangentY) || 1;
                tangentX /= tangentLength; tangentY /= tangentLength;
                var pullX = pointerX - baseX, pullY = pointerY - baseY;
                var pullLength = Math.sqrt(pullX * pullX + pullY * pullY);
                if (pullLength < 3) { pullX = -tangentY; pullY = tangentX; pullLength = 1; }
                pullX /= pullLength; pullY /= pullLength;
                var tractionValid = directDrag ? !!directDrag.startValid : nearest.distance <= 0.11;
                var grip = Math.max(15, Math.min(90, Number(toolCalibration.forcepsGrip) || 40));
                var tractionState = grip < 25
                  ? { key: 'slip', label: 'SLIP RISK', pattern: [4, 3] }
                  : ((grip > 50 || tractionTissue.tension > 82)
                    ? { key: 'stress', label: 'HIGH STRESS', pattern: [2, 2] }
                    : { key: 'controlled', label: 'CONTROLLED LIFT', pattern: [] });
                if (directAssessment) tractionState = { key: directAssessment.key, label: directAssessment.label, pattern: directAssessment.key === 'controlled' || directAssessment.key === 'grasp' ? [] : (directAssessment.key === 'stress' ? [2, 2] : [4, 3]) };
                if (!tractionValid) tractionState = { key: 'position', label: 'MOVE TO OPENING', pattern: [5, 4] };
                var complianceFactor = 0.7 + (Number(tractionMaterial.compliance) || 0.5) * 0.55;
                var gripFactor = tractionState.key === 'slip' ? 0.58 : (tractionState.key === 'stress' ? 1.08 : 0.86);
                var tractionPulse = dissMotionReduced ? 0 : Math.sin(dissTick * 0.045) * (tractionState.key === 'controlled' ? 0.8 : 0.35);
                var liftDistance = tractionValid ? (directAssessment ? Math.max(4, Math.min(directAssessment.safeMax * Math.min(W, H) * 0.82, pullLength * complianceFactor * 0.72)) : (9 + grip * 0.16) * complianceFactor * gripFactor + tractionPulse) : 4;
                var liftX = baseX + pullX * liftDistance, liftY = baseY + pullY * liftDistance;
                var halfSpan = 13 + complianceFactor * 7;
                var baseAX = baseX - tangentX * halfSpan, baseAY = baseY - tangentY * halfSpan;
                var baseBX = baseX + tangentX * halfSpan, baseBY = baseY + tangentY * halfSpan;
                var tractionColor = tractionState.key === 'stress' ? '#fb7185' : (tractionState.key === 'slip' ? '#facc15' : '#5eead4');
                var layerColor = ((spec.layers[currentLayerIdx] || {}).color) || '#a87579';
                ctx.save();
                if (directAssessment && tractionValid) {
                  // The solid outer ring is the safe lift envelope; the dashed inner ring marks the minimum effective lift.
                  ctx.save(); ctx.globalAlpha = tractionAccessible ? 0.92 : 0.52;
                  ctx.strokeStyle = tractionAccessible ? '#ffffff' : '#5eead4'; ctx.lineWidth = tractionAccessible ? 2.2 : 1.15; ctx.setLineDash([]);
                  ctx.beginPath(); ctx.ellipse(baseX, baseY, directAssessment.safeMax * W, directAssessment.safeMax * H, 0, 0, Math.PI * 2); ctx.stroke();
                  ctx.setLineDash([3, 3]); ctx.globalAlpha *= 0.72;
                  ctx.beginPath(); ctx.ellipse(baseX, baseY, directAssessment.minimumLift * W, directAssessment.minimumLift * H, 0, 0, Math.PI * 2); ctx.stroke();
                  ctx.setLineDash([]); ctx.globalAlpha = 1;
                  ctx.strokeStyle = tractionAccessible ? '#ffffff' : tractionColor; ctx.lineWidth = tractionAccessible ? 2.4 : 1.45; ctx.setLineDash(tractionState.pattern);
                  ctx.beginPath(); ctx.moveTo(liftX, liftY); ctx.lineTo(pointerX, pointerY); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.arc(pointerX, pointerY, tractionState.key === 'controlled' ? 5 : 7, 0, Math.PI * 2); ctx.stroke();
                  ctx.restore();
                }
                if (!tractionValid) {
                  ctx.strokeStyle = tractionAccessible ? '#ffffff' : '#facc15'; ctx.lineWidth = tractionAccessible ? 2.4 : 1.4; ctx.setLineDash([5, 4]);
                  ctx.beginPath(); ctx.moveTo(pointerX, pointerY); ctx.lineTo(baseX, baseY); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.arc(baseX, baseY, 13, 0, Math.PI * 2); ctx.stroke();
                } else {
                  ctx.beginPath(); ctx.moveTo(baseAX, baseAY);
                  ctx.quadraticCurveTo(baseX + pullX * liftDistance * 0.48, baseY + pullY * liftDistance * 0.48, liftX, liftY);
                  ctx.quadraticCurveTo(baseX + pullX * liftDistance * 0.48, baseY + pullY * liftDistance * 0.48, baseBX, baseBY);
                  ctx.quadraticCurveTo(baseX + pullX * 2, baseY + pullY * 2, baseAX, baseAY); ctx.closePath();
                  var tractionGradient = ctx.createLinearGradient(baseX, baseY, liftX, liftY);
                  tractionGradient.addColorStop(0, tractionAccessible ? '#000000' : layerColor);
                  tractionGradient.addColorStop(0.68, tractionAccessible ? '#000000' : '#a87579');
                  tractionGradient.addColorStop(1, tractionAccessible ? '#000000' : '#5f3b43');
                  ctx.fillStyle = tractionGradient; ctx.shadowColor = 'rgba(2,6,23,0.68)'; ctx.shadowBlur = 10; ctx.fill(); ctx.shadowBlur = 0;
                  ctx.strokeStyle = tractionAccessible ? '#ffffff' : tractionColor; ctx.lineWidth = tractionAccessible ? 2.6 : 1.5; ctx.setLineDash(tractionState.pattern); ctx.stroke(); ctx.setLineDash([]);
                  for (var tractionFiberIndex = -1; tractionFiberIndex <= 1; tractionFiberIndex++) {
                    var tractionFiberOffset = tractionFiberIndex * halfSpan * 0.42;
                    var tractionFiberX = baseX + tangentX * tractionFiberOffset, tractionFiberY = baseY + tangentY * tractionFiberOffset;
                    ctx.strokeStyle = tractionAccessible ? '#ffffff' : tractionColor;
                    ctx.lineWidth = tractionState.key === 'stress' ? 1.8 : 1.05; ctx.setLineDash(tractionState.pattern);
                    ctx.beginPath(); ctx.moveTo(tractionFiberX, tractionFiberY); ctx.lineTo(liftX, liftY); ctx.stroke(); ctx.setLineDash([]);
                  }
                  if (tractionTissue.moisture > 55) {
                    ctx.globalAlpha = 0.18 + tractionTissue.moisture / 500; ctx.strokeStyle = '#ffffff'; ctx.lineWidth = 1;
                    ctx.beginPath(); ctx.moveTo(baseAX, baseAY); ctx.quadraticCurveTo(liftX, liftY - 2, baseBX, baseBY); ctx.stroke(); ctx.globalAlpha = 1;
                  }
                  ctx.strokeStyle = tractionAccessible ? '#ffffff' : tractionColor; ctx.lineWidth = tractionAccessible ? 2.4 : 1.7;
                  ctx.beginPath(); ctx.moveTo(liftX - tangentX * 9 - pullX * 5, liftY - tangentY * 9 - pullY * 5); ctx.lineTo(liftX, liftY);
                  ctx.lineTo(liftX + tangentX * 9 - pullX * 5, liftY + tangentY * 9 - pullY * 5); ctx.stroke();
                  if (tractionState.key === 'controlled') {
                    ctx.beginPath(); ctx.moveTo(liftX - 5, liftY - 13); ctx.lineTo(liftX - 1, liftY - 9); ctx.lineTo(liftX + 7, liftY - 17); ctx.stroke();
                  } else if (tractionState.key === 'slip') {
                    ctx.setLineDash([3, 2]); ctx.beginPath(); ctx.moveTo(liftX - tangentX * 5, liftY - tangentY * 5); ctx.lineTo(baseX - tangentX * 5, baseY - tangentY * 5);
                    ctx.moveTo(liftX + tangentX * 5, liftY + tangentY * 5); ctx.lineTo(baseX + tangentX * 5, baseY + tangentY * 5); ctx.stroke(); ctx.setLineDash([]);
                  } else if (tractionState.key === 'stress') {
                    ctx.beginPath(); ctx.moveTo(liftX, liftY - 19); ctx.lineTo(liftX + 10, liftY - 2); ctx.lineTo(liftX - 10, liftY - 2); ctx.closePath(); ctx.stroke();
                    ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = tractionAccessible ? '#ffffff' : '#fecdd3'; ctx.fillText('!', liftX - 2, liftY - 7);
                  } else {
                    ctx.beginPath(); ctx.arc(liftX, liftY, 6, 0, Math.PI * 2); ctx.stroke();
                  }
                }
                var tractionLabel = tractionState.label + (directAssessment ? ' \u00B7 LIFT ' + directAssessment.liftPercent + '% \u00B7 TENSION ' + directAssessment.projectedTension + '%' : ' \u00B7 GRIP ' + grip + '%');
                ctx.font = 'bold 8px Inter, system-ui';
                var tractionLabelWidth = Math.min(directAssessment ? 214 : 142, Math.ceil(ctx.measureText(tractionLabel).width) + 12);
                var tractionLabelX = Math.max(8, Math.min(W - tractionLabelWidth - 8, liftX + 14));
                var tractionLabelY = Math.max(8, Math.min(H - 22, liftY - 28));
                ctx.fillStyle = tractionAccessible ? '#000000' : 'rgba(15,23,42,0.92)';
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(tractionLabelX, tractionLabelY, tractionLabelWidth, 18, 5); else ctx.rect(tractionLabelX, tractionLabelY, tractionLabelWidth, 18);
                ctx.fill(); ctx.strokeStyle = tractionAccessible ? '#ffffff' : tractionColor; ctx.lineWidth = 1.1; ctx.stroke();
                ctx.fillStyle = tractionAccessible ? '#ffffff' : (tractionState.key === 'stress' ? '#fecdd3' : (tractionState.key === 'slip' ? '#fef08a' : '#ccfbf1'));
                ctx.fillText(tractionLabel.slice(0, directAssessment ? 43 : 27), tractionLabelX + 6, tractionLabelY + 12);
                ctx.restore();
              }
              function drawLocalizedTechniqueEvidence(procedureState, tissueState) {
                if (!procedureState || !tissueState) return;
                // Localized technique evidence anchors persistent consequences to the learner's actual paths and contact points.
                var evidenceAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var evidenceScores = procedureState.calibrationScores || {};
                var evidenceCues = [];
                var evidenceEdgeColor = evidenceAccessible ? '#ffffff' : '#fb7185';
                var evidenceStressColor = evidenceAccessible ? '#ffffff' : '#facc15';
                ctx.save(); ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX, materialRY, 0, 0, Math.PI * 2); ctx.clip();
                var evidenceMetrics = procedureState.extensionMetrics || procedureState.incisionMetrics || {};
                var evidencePrecision = Number(evidenceMetrics.precision) || 100;
                var evidenceEdgeSeverity = Math.max(
                  Number(tissueState.trauma) || 0,
                  100 - evidencePrecision,
                  procedureState.incisionStarted ? 100 - (Number(evidenceScores.scalpel) || 100) : 0,
                  procedureState.incisionExtended ? 100 - (Number(evidenceScores.scissors) || 100) : 0
                );
                var evidenceEdgeMarks = 0;
                if (evidenceEdgeSeverity > 22) {
                  var evidencePaths = [procedureState.incisionPath || [], procedureState.extensionPath || []];
                  ctx.strokeStyle = evidenceEdgeColor; ctx.lineWidth = evidenceAccessible ? 2.4 : 1.35;
                  ctx.setLineDash(evidenceEdgeSeverity > 54 ? [2, 2] : []);
                  evidencePaths.forEach(function (path) {
                    if (path.length < 2) return;
                    var evidenceStep = Math.max(1, Math.floor(path.length / 7));
                    for (var evidencePathIndex = evidenceStep; evidencePathIndex < path.length; evidencePathIndex += evidenceStep) {
                      var evidencePoint = path[evidencePathIndex];
                      var evidencePrevious = path[Math.max(0, evidencePathIndex - evidenceStep)];
                      var evidenceDX = (evidencePoint.x - evidencePrevious.x) * W, evidenceDY = (evidencePoint.y - evidencePrevious.y) * H;
                      var evidenceLength = Math.sqrt(evidenceDX * evidenceDX + evidenceDY * evidenceDY) || 1;
                      var evidenceNX = -evidenceDY / evidenceLength, evidenceNY = evidenceDX / evidenceLength;
                      var evidencePX = evidencePoint.x * W + parallaxX, evidencePY = evidencePoint.y * H + parallaxY;
                      var evidenceSlash = 4 + Math.min(5, evidenceEdgeSeverity / 16);
                      ctx.beginPath();
                      ctx.moveTo(evidencePX - evidenceNX * 2 - evidenceDX / evidenceLength * evidenceSlash, evidencePY - evidenceNY * 2 - evidenceDY / evidenceLength * evidenceSlash);
                      ctx.lineTo(evidencePX - evidenceNX * 2 + evidenceDX / evidenceLength * evidenceSlash, evidencePY - evidenceNY * 2 + evidenceDY / evidenceLength * evidenceSlash);
                      ctx.moveTo(evidencePX + evidenceNX * 3 - evidenceDX / evidenceLength * evidenceSlash, evidencePY + evidenceNY * 3 - evidenceDY / evidenceLength * evidenceSlash);
                      ctx.lineTo(evidencePX + evidenceNX * 3 + evidenceDX / evidenceLength * evidenceSlash, evidencePY + evidenceNY * 3 + evidenceDY / evidenceLength * evidenceSlash);
                      ctx.stroke(); evidenceEdgeMarks++;
                    }
                  });
                  ctx.setLineDash([]);
                  if (evidenceEdgeMarks) evidenceCues.push('EDGE ' + evidenceEdgeMarks);
                }
                var evidenceForcepsScore = Number(evidenceScores.forceps);
                var evidenceForcepsSeverity = Math.max(evidenceForcepsScore ? 100 - evidenceForcepsScore : 0, (Number(tissueState.tension) || 0) - 62);
                if (procedureState.forcepsPoint && evidenceForcepsSeverity > 18) {
                  var evidenceForcepsX = procedureState.forcepsPoint.x * W + parallaxX, evidenceForcepsY = procedureState.forcepsPoint.y * H + parallaxY;
                  ctx.strokeStyle = evidenceStressColor; ctx.lineWidth = evidenceAccessible ? 2.5 : 1.5; ctx.setLineDash([4, 2]);
                  ctx.beginPath(); ctx.ellipse(evidenceForcepsX, evidenceForcepsY, 17, 9, 0, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.moveTo(evidenceForcepsX - 23, evidenceForcepsY - 8); ctx.lineTo(evidenceForcepsX - 12, evidenceForcepsY); ctx.lineTo(evidenceForcepsX - 23, evidenceForcepsY + 8);
                  ctx.moveTo(evidenceForcepsX + 23, evidenceForcepsY - 8); ctx.lineTo(evidenceForcepsX + 12, evidenceForcepsY); ctx.lineTo(evidenceForcepsX + 23, evidenceForcepsY + 8); ctx.stroke();
                  evidenceCues.push('GRIP 1');
                }
                var evidencePinScore = Number(evidenceScores.pin);
                var evidencePinSeverity = Math.max(evidencePinScore ? 100 - evidencePinScore : 0, (Number(tissueState.tension) || 0) - 68);
                if ((procedureState.pins || []).length && evidencePinSeverity > 18) {
                  ctx.strokeStyle = evidenceStressColor; ctx.lineWidth = evidenceAccessible ? 2.3 : 1.25;
                  (procedureState.pins || []).forEach(function (pin) {
                    var evidencePinX = pin.x * W + parallaxX, evidencePinY = pin.y * H + parallaxY;
                    ctx.beginPath(); ctx.moveTo(evidencePinX, evidencePinY - 13); ctx.lineTo(evidencePinX + 13, evidencePinY); ctx.lineTo(evidencePinX, evidencePinY + 13); ctx.lineTo(evidencePinX - 13, evidencePinY); ctx.closePath(); ctx.stroke();
                    for (var evidenceSpoke = 0; evidenceSpoke < 6; evidenceSpoke++) {
                      var evidenceAngle = evidenceSpoke / 6 * Math.PI * 2;
                      ctx.beginPath(); ctx.moveTo(evidencePinX + Math.cos(evidenceAngle) * 7, evidencePinY + Math.sin(evidenceAngle) * 7);
                      ctx.lineTo(evidencePinX + Math.cos(evidenceAngle) * (14 + evidencePinSeverity * 0.08), evidencePinY + Math.sin(evidenceAngle) * (14 + evidencePinSeverity * 0.08)); ctx.stroke();
                    }
                  });
                  evidenceCues.push('ANCHOR ' + (procedureState.pins || []).length);
                }
                var evidenceProbeScore = Number(evidenceScores.probe);
                var evidenceProbeSeverity = Math.max(evidenceProbeScore ? 100 - evidenceProbeScore : 0, (Number(tissueState.trauma) || 0) - 34);
                if (procedureState.probed && procedureState.probedOrganId && evidenceProbeSeverity > 16) {
                  var evidenceProbeOrgan = organs.find(function (organ) { return organ.id === procedureState.probedOrganId; });
                  if (evidenceProbeOrgan) {
                    var evidenceProbePoint = variedOrganPoint(evidenceProbeOrgan);
                    var evidenceProbeX = evidenceProbePoint.x * W + parallaxX, evidenceProbeY = evidenceProbePoint.y * H + parallaxY;
                    ctx.strokeStyle = evidenceEdgeColor; ctx.lineWidth = evidenceAccessible ? 2.4 : 1.4; ctx.setLineDash([2, 2]);
                    ctx.beginPath(); ctx.arc(evidenceProbeX, evidenceProbeY, 12, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                    ctx.beginPath(); ctx.moveTo(evidenceProbeX - 7, evidenceProbeY - 7); ctx.lineTo(evidenceProbeX + 7, evidenceProbeY + 7);
                    ctx.moveTo(evidenceProbeX + 7, evidenceProbeY - 7); ctx.lineTo(evidenceProbeX - 7, evidenceProbeY + 7); ctx.stroke();
                    evidenceCues.push('CONTACT 1');
                  }
                }
                var evidenceDropperScore = Number(evidenceScores.dropper);
                var evidenceDropperDrag = procedureState.dropperDragMetrics || {};
                var evidencePooling = (Number(tissueState.salineDrops) || 0) > 2 || (evidenceDropperScore && evidenceDropperScore < 85) || Number(evidenceDropperDrag.poolingRisk) >= 64;
                if (procedureState.dropperPoint && evidencePooling) {
                  var evidencePoolX = procedureState.dropperPoint.x * W + parallaxX, evidencePoolY = procedureState.dropperPoint.y * H + parallaxY;
                  ctx.strokeStyle = evidenceStressColor; ctx.lineWidth = evidenceAccessible ? 2.3 : 1.25; ctx.setLineDash([6, 3]);
                  ctx.beginPath(); ctx.ellipse(evidencePoolX, evidencePoolY, 22, 12, 0, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  for (var evidencePoolHatch = -12; evidencePoolHatch <= 12; evidencePoolHatch += 6) {
                    ctx.beginPath(); ctx.moveTo(evidencePoolX + evidencePoolHatch - 5, evidencePoolY - 5); ctx.lineTo(evidencePoolX + evidencePoolHatch + 5, evidencePoolY + 5); ctx.stroke();
                  }
                  evidenceCues.push('POOL 1');
                  if (evidenceDropperDrag.contactPercent != null) evidenceCues.push('FILM ' + evidenceDropperDrag.contactPercent + '%');
                }
                ctx.restore();
                if (!evidenceCues.length) return;
                ctx.save(); ctx.font = 'bold 8px Inter, system-ui';
                var evidenceTitle = 'LOCAL TECHNIQUE EVIDENCE \u00B7 ' + evidenceCues.length;
                var evidenceDetail = evidenceCues.join(' / ');
                var evidenceWidth = Math.min(196, Math.max(154, Math.ceil(Math.max(ctx.measureText(evidenceTitle).width, ctx.measureText(evidenceDetail).width)) + 14));
                var evidenceX = 10, evidenceY = 62;
                ctx.fillStyle = evidenceAccessible ? '#000000' : 'rgba(15,23,42,0.92)'; ctx.strokeStyle = evidenceAccessible ? '#ffffff' : '#facc15'; ctx.lineWidth = 1.1; ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(evidenceX, evidenceY, evidenceWidth, 32, 6); else ctx.rect(evidenceX, evidenceY, evidenceWidth, 32);
                ctx.fill(); ctx.stroke(); ctx.fillStyle = evidenceAccessible ? '#ffffff' : '#fef08a'; ctx.fillText(evidenceTitle.slice(0, 34), evidenceX + 7, evidenceY + 12);
                ctx.font = '7px Inter, system-ui'; ctx.fillStyle = evidenceAccessible ? '#ffffff' : '#e2e8f0'; ctx.fillText(evidenceDetail.slice(0, 40), evidenceX + 7, evidenceY + 24);
                ctx.restore();
              }
              function drawProcedureHandoffCue(nextInfo, openingPath) {
                if (!nextInfo || procedureMode !== 'guided') return;
                // The handoff cue links the completed technique state to the next instrument and its anatomical target without relying on color.
                var handoffStep = procedureStepIndex(canvasProcedure);
                var handoffGuide = procedureGuidePoints();
                var handoffMiddle = handoffGuide[Math.floor((handoffGuide.length - 1) / 2)] || { x: 0.5, y: 0.48 };
                var handoffTarget = handoffMiddle;
                if (nextInfo.action === 'scalpel' || nextInfo.action === 'scissors') {
                  handoffTarget = handoffGuide[0] || handoffMiddle;
                } else if (nextInfo.action === 'forceps') {
                  var handoffOpening = openingPath && openingPath.length ? openingPath : handoffGuide;
                  handoffTarget = handoffOpening[Math.floor((handoffOpening.length - 1) / 2)] || handoffMiddle;
                } else if (nextInfo.action === 'pin') {
                  handoffTarget = (canvasProcedure.pins || []).length ? (handoffGuide[handoffGuide.length - 1] || handoffMiddle) : (handoffGuide[0] || handoffMiddle);
                } else if (nextInfo.action === 'probe') {
                  var handoffOrgan = organs.find(function (organ) {
                    return structureExposureState(organ, currentProcedure) === 'visible' && !(d.exploredOrgans || {})[specimen + '|' + organ.id];
                  }) || organs.find(function (organ) { return structureExposureState(organ, currentProcedure) === 'visible'; });
                  if (handoffOrgan) handoffTarget = variedOrganPoint(handoffOrgan);
                }
                var handoffX = handoffTarget.x * W, handoffY = handoffTarget.y * H;
                var handoffSelected = nextInfo.action === 'complete' || activeInstrument === nextInfo.instrument;
                var handoffAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var handoffAccent = handoffAccessible ? '#ffffff' : (handoffSelected ? '#5eead4' : '#facc15');
                var handoffPulse = dissMotionReduced ? 0 : Math.sin(dissTick * 0.045) * 2;
                var handoffLabelWidth = 184, handoffLabelHeight = 44;
                var handoffLabelX = Math.max(8, W - handoffLabelWidth - 10), handoffLabelY = 10;
                var handoffTitle = nextInfo.action === 'complete'
                  ? 'SEQUENCE 6/6 \u00B7 COMPLETE'
                  : 'NEXT ' + (handoffStep + 1) + '/6 \u00B7 ' + (handoffSelected ? 'READY ' : 'SELECT ') + String(nextInfo.instrument || '').toUpperCase();
                ctx.save();
                ctx.strokeStyle = handoffAccent; ctx.fillStyle = handoffAccessible ? '#000000' : 'rgba(15,23,42,0.92)';
                ctx.lineWidth = handoffAccessible ? 2.4 : 1.3; ctx.setLineDash(handoffSelected ? [] : [5, 3]);
                ctx.beginPath(); ctx.moveTo(handoffLabelX + handoffLabelWidth * 0.5, handoffLabelY + handoffLabelHeight); ctx.lineTo(handoffX, handoffY); ctx.stroke(); ctx.setLineDash([]);
                if (nextInfo.action === 'scalpel' || nextInfo.action === 'scissors') {
                  ctx.setLineDash(nextInfo.action === 'scissors' ? [6, 3] : [9, 4]); ctx.beginPath();
                  handoffGuide.forEach(function (point, pointIndex) {
                    if (!pointIndex) ctx.moveTo(point.x * W, point.y * H); else ctx.lineTo(point.x * W, point.y * H);
                  });
                  ctx.stroke(); ctx.setLineDash([]);
                  var handoffEnd = handoffGuide[handoffGuide.length - 1] || handoffMiddle;
                  ctx.beginPath(); ctx.moveTo(handoffEnd.x * W - 6, handoffEnd.y * H - 4); ctx.lineTo(handoffEnd.x * W, handoffEnd.y * H); ctx.lineTo(handoffEnd.x * W - 6, handoffEnd.y * H + 4); ctx.stroke();
                }
                ctx.translate(handoffX, handoffY);
                if (nextInfo.action === 'inspect') {
                  ctx.beginPath(); ctx.moveTo(-16, -8); ctx.lineTo(-16, -16); ctx.lineTo(-8, -16);
                  ctx.moveTo(8, -16); ctx.lineTo(16, -16); ctx.lineTo(16, -8);
                  ctx.moveTo(16, 8); ctx.lineTo(16, 16); ctx.lineTo(8, 16);
                  ctx.moveTo(-8, 16); ctx.lineTo(-16, 16); ctx.lineTo(-16, 8); ctx.stroke();
                } else if (nextInfo.action === 'forceps') {
                  ctx.beginPath(); ctx.moveTo(-18, -12); ctx.lineTo(-4, 0); ctx.lineTo(-18, 12);
                  ctx.moveTo(18, -12); ctx.lineTo(4, 0); ctx.lineTo(18, 12); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(0, -5); ctx.lineTo(0, -18 - handoffPulse); ctx.moveTo(-4, -14 - handoffPulse); ctx.lineTo(0, -18 - handoffPulse); ctx.lineTo(4, -14 - handoffPulse); ctx.stroke();
                } else if (nextInfo.action === 'pin') {
                  ctx.beginPath(); ctx.moveTo(0, -17 - handoffPulse); ctx.lineTo(17 + handoffPulse, 0); ctx.lineTo(0, 17 + handoffPulse); ctx.lineTo(-17 - handoffPulse, 0); ctx.closePath(); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(-7, 0); ctx.lineTo(7, 0); ctx.moveTo(0, -7); ctx.lineTo(0, 7); ctx.stroke();
                } else if (nextInfo.action === 'probe') {
                  ctx.beginPath(); ctx.arc(0, 0, 13 + handoffPulse, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.arc(0, 0, 6, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(-19, 0); ctx.lineTo(-8, 0); ctx.moveTo(8, 0); ctx.lineTo(19, 0); ctx.moveTo(0, -19); ctx.lineTo(0, -8); ctx.moveTo(0, 8); ctx.lineTo(0, 19); ctx.stroke();
                } else if (nextInfo.action === 'complete') {
                  ctx.beginPath(); ctx.arc(0, 0, 15 + handoffPulse, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(-5, 0); ctx.lineTo(-15, -10); ctx.moveTo(-5, 0); ctx.lineTo(-15, 10);
                  ctx.moveTo(5, 0); ctx.lineTo(15, -10); ctx.moveTo(5, 0); ctx.lineTo(15, 10); ctx.stroke();
                } else {
                  ctx.beginPath(); ctx.arc(0, 0, 13 + handoffPulse, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(-7, 0); ctx.lineTo(7, 0); ctx.moveTo(0, -7); ctx.lineTo(0, 7); ctx.stroke();
                }
                ctx.restore();
                ctx.save(); ctx.fillStyle = handoffAccessible ? '#000000' : 'rgba(15,23,42,0.94)'; ctx.strokeStyle = handoffAccent; ctx.lineWidth = 1.2; ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(handoffLabelX, handoffLabelY, handoffLabelWidth, handoffLabelHeight, 6); else ctx.rect(handoffLabelX, handoffLabelY, handoffLabelWidth, handoffLabelHeight);
                ctx.fill(); ctx.stroke();
                ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = handoffAccessible ? '#ffffff' : handoffAccent; ctx.fillText(handoffTitle.slice(0, 34), handoffLabelX + 7, handoffLabelY + 11);
                ctx.font = '7px Inter, system-ui'; ctx.fillStyle = handoffAccessible ? '#ffffff' : '#e2e8f0'; ctx.fillText(String(nextInfo.label || '').slice(0, 38), handoffLabelX + 7, handoffLabelY + 23);
                var handoffSegmentWidth = 24, handoffSegmentGap = 4, handoffRailX = handoffLabelX + 7, handoffRailY = handoffLabelY + 31;
                for (var handoffSegment = 0; handoffSegment < 6; handoffSegment++) {
                  var handoffSegmentX = handoffRailX + handoffSegment * (handoffSegmentWidth + handoffSegmentGap);
                  ctx.beginPath(); ctx.rect(handoffSegmentX, handoffRailY, handoffSegmentWidth, 6);
                  if (handoffSegment < handoffStep || handoffStep >= 6) {
                    ctx.fillStyle = handoffAccessible ? '#ffffff' : '#5eead4'; ctx.fill();
                  } else {
                    ctx.strokeStyle = handoffAccessible ? '#ffffff' : (handoffSegment === handoffStep ? '#facc15' : '#64748b'); ctx.lineWidth = handoffSegment === handoffStep ? 1.8 : 0.8; ctx.stroke();
                    if (handoffSegment === handoffStep) {
                      ctx.beginPath(); ctx.moveTo(handoffSegmentX + 3, handoffRailY + 5); ctx.lineTo(handoffSegmentX + 8, handoffRailY + 1);
                      ctx.moveTo(handoffSegmentX + 10, handoffRailY + 5); ctx.lineTo(handoffSegmentX + 15, handoffRailY + 1);
                      ctx.moveTo(handoffSegmentX + 17, handoffRailY + 5); ctx.lineTo(handoffSegmentX + 22, handoffRailY + 1); ctx.stroke();
                    }
                  }
                }
                ctx.restore();
              }
              // Legacy signature: function drawDropperSpreadPreview(pointer)
              function drawDropperSpreadPreview(pointer, canvasEl) {
                if (!pointer) return;
                var directDropperDrag = !!(canvasEl && canvasEl._dropperDrag && canvasEl._dropperDrag.active);
                var directDropperAssessment = directDropperDrag ? dropperDragAssessment(canvasEl, pointer, Date.now()) : null;
                // The live dose forecast uses the same organism flow profile as applied saline, exposing spread and pooling risk before commitment.
                var forecastContact = specimenContactContext(pointer);
                var forecastMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var forecastProfile = specimenHydrationProfile();
                var forecastTissue = normalizeTissueState(liveTissueState);
                var forecastAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var forecastDose = directDropperAssessment ? directDropperAssessment.dose : Math.max(1, Math.min(3, Math.round(Number(toolCalibration.salineDose) || 1)));
                var forecastState = !forecastContact.onSpecimen
                  ? { key: 'position', label: 'MOVE TO SPECIMEN', pattern: [5, 4] }
                  : (forecastTissue.moisture >= 96
                    ? { key: 'saturated', label: 'SATURATED \u00B7 NO DOSE', pattern: [1, 3] }
                    : (forecastDose === 1
                      ? { key: 'controlled', label: 'CONTROLLED FILM', pattern: [] }
                      : (forecastDose === 2
                        ? { key: 'broad', label: 'BROAD SPREAD', pattern: [6, 3] }
                        : { key: 'pooling', label: 'POOLING RISK', pattern: [2, 2] })));
                if (directDropperAssessment) forecastState = directDropperAssessment.state;
                var forecastColor = ['pooling', 'drift'].indexOf(forecastState.key) >= 0 ? '#fb7185'
                  : (forecastState.key === 'broad' || forecastState.key === 'saturated' || forecastState.key === 'position' ? '#facc15' : '#7dd3fc');
                var forecastX = pointer.x * W, forecastY = pointer.y * H;
                var forecastRadius = 14 + forecastDose * 7;
                var forecastFlowX = (Number(forecastProfile.flowX) || 0) * forecastRadius;
                var forecastFlowY = (Number(forecastProfile.flowY) || 0) * forecastRadius;
                if (Math.abs(forecastFlowX) + Math.abs(forecastFlowY) < 2) forecastFlowY = forecastRadius * 0.28;
                var forecastFilmX = forecastX + forecastFlowX, forecastFilmY = forecastY + forecastFlowY;
                ctx.save();
                if (!forecastContact.onSpecimen) {
                  var forecastTargetX = forecastMaterial.cx * W, forecastTargetY = forecastMaterial.cy * H;
                  ctx.strokeStyle = forecastAccessible ? '#ffffff' : forecastColor; ctx.lineWidth = forecastAccessible ? 2.4 : 1.4; ctx.setLineDash(forecastState.pattern);
                  ctx.beginPath(); ctx.moveTo(forecastX, forecastY); ctx.lineTo(forecastTargetX, forecastTargetY); ctx.stroke();
                  ctx.beginPath(); ctx.arc(forecastTargetX, forecastTargetY, 15, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.moveTo(forecastX - 7, forecastY - 7); ctx.lineTo(forecastX + 7, forecastY + 7);
                  ctx.moveTo(forecastX + 7, forecastY - 7); ctx.lineTo(forecastX - 7, forecastY + 7); ctx.stroke();
                } else {
                  ctx.beginPath(); ctx.ellipse(forecastMaterial.cx * W, forecastMaterial.cy * H, forecastMaterial.rx * W, forecastMaterial.ry * H, 0, 0, Math.PI * 2); ctx.clip();
                  ctx.translate(forecastFilmX, forecastFilmY); ctx.scale(Number(forecastProfile.aspectX) || 1, Number(forecastProfile.aspectY) || 1);
                  ctx.strokeStyle = forecastAccessible ? '#ffffff' : forecastColor;
                  ctx.fillStyle = forecastAccessible ? 'rgba(0,0,0,0.16)' : (forecastState.key === 'pooling' ? 'rgba(251,113,133,0.16)' : 'rgba(125,211,252,0.12)');
                  ctx.lineWidth = forecastAccessible ? 2.5 / Math.max(0.5, Number(forecastProfile.aspectX) || 1) : 1.3;
                  ctx.setLineDash(forecastState.pattern);
                  ctx.beginPath(); ctx.arc(0, 0, forecastRadius, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); ctx.setLineDash([]);
                  if (forecastProfile.pattern === 'radial') {
                    for (var forecastRing = 1; forecastRing <= 2; forecastRing++) {
                      ctx.beginPath(); ctx.arc(0, 0, forecastRadius * forecastRing / 3, 0, Math.PI * 2); ctx.stroke();
                    }
                  } else if (forecastProfile.pattern === 'segments') {
                    for (var forecastSegment = -2; forecastSegment <= 2; forecastSegment++) {
                      var forecastSegmentY = forecastSegment * forecastRadius * 0.25;
                      ctx.beginPath(); ctx.moveTo(-forecastRadius * 0.68, forecastSegmentY); ctx.quadraticCurveTo(0, forecastSegmentY + (forecastSegment % 2 ? 2 : -2), forecastRadius * 0.68, forecastSegmentY); ctx.stroke();
                    }
                  } else if (forecastProfile.pattern === 'fibers') {
                    for (var forecastFiber = -2; forecastFiber <= 2; forecastFiber++) {
                      ctx.beginPath(); ctx.moveTo(-forecastRadius * 0.72, forecastFiber * 4 - 6); ctx.lineTo(forecastRadius * 0.72, forecastFiber * 4 + 6); ctx.stroke();
                    }
                  } else if (forecastProfile.pattern === 'channels') {
                    ctx.beginPath(); ctx.moveTo(-forecastRadius * 0.72, 0); ctx.quadraticCurveTo(0, -7, forecastRadius * 0.72, 0);
                    ctx.moveTo(-forecastRadius * 0.18, -forecastRadius * 0.56); ctx.quadraticCurveTo(0, 0, forecastRadius * 0.36, forecastRadius * 0.5); ctx.stroke();
                  } else if (forecastProfile.pattern === 'beads') {
                    for (var forecastBead = 0; forecastBead < 6; forecastBead++) {
                      var forecastBeadAngle = forecastBead / 6 * Math.PI * 2;
                      ctx.beginPath(); ctx.arc(Math.cos(forecastBeadAngle) * forecastRadius * 0.55, Math.sin(forecastBeadAngle) * forecastRadius * 0.55, 2.2, 0, Math.PI * 2); ctx.stroke();
                    }
                  }
                  if (forecastState.key === 'pooling') {
                    ctx.globalAlpha = 0.72; ctx.lineWidth = 1;
                    for (var forecastHatch = -forecastRadius; forecastHatch <= forecastRadius; forecastHatch += 6) {
                      ctx.beginPath(); ctx.moveTo(forecastHatch - 6, -forecastRadius * 0.55); ctx.lineTo(forecastHatch + 6, forecastRadius * 0.55); ctx.stroke();
                    }
                    ctx.globalAlpha = 1;
                  }
                  ctx.restore(); ctx.save();
                  var forecastFlowEndX = forecastX + forecastFlowX * 1.7, forecastFlowEndY = forecastY + forecastFlowY * 1.7;
                  var forecastFlowLength = Math.sqrt(forecastFlowX * forecastFlowX + forecastFlowY * forecastFlowY) || 1;
                  var forecastFlowUnitX = forecastFlowX / forecastFlowLength, forecastFlowUnitY = forecastFlowY / forecastFlowLength;
                  ctx.strokeStyle = forecastAccessible ? '#ffffff' : forecastColor; ctx.lineWidth = forecastAccessible ? 2.5 : 1.7;
                  ctx.beginPath(); ctx.moveTo(forecastX, forecastY); ctx.lineTo(forecastFlowEndX, forecastFlowEndY);
                  ctx.lineTo(forecastFlowEndX - forecastFlowUnitX * 7 - forecastFlowUnitY * 4, forecastFlowEndY - forecastFlowUnitY * 7 + forecastFlowUnitX * 4);
                  ctx.moveTo(forecastFlowEndX, forecastFlowEndY);
                  ctx.lineTo(forecastFlowEndX - forecastFlowUnitX * 7 + forecastFlowUnitY * 4, forecastFlowEndY - forecastFlowUnitY * 7 - forecastFlowUnitX * 4); ctx.stroke();
                  for (var forecastDrop = 0; forecastDrop < forecastDose; forecastDrop++) {
                    var forecastDropX = forecastX - (forecastDose - 1) * 4 + forecastDrop * 8;
                    var forecastDropY = forecastY - 22 - (forecastDrop % 2) * 3;
                    ctx.beginPath(); ctx.moveTo(forecastDropX, forecastDropY - 5); ctx.quadraticCurveTo(forecastDropX + 5, forecastDropY + 2, forecastDropX, forecastDropY + 6);
                    ctx.quadraticCurveTo(forecastDropX - 5, forecastDropY + 2, forecastDropX, forecastDropY - 5); ctx.fillStyle = forecastAccessible ? '#000000' : 'rgba(125,211,252,0.42)'; ctx.fill(); ctx.stroke();
                  }
                  if (forecastState.key === 'controlled') {
                    ctx.beginPath(); ctx.moveTo(forecastX + 10, forecastY - 16); ctx.lineTo(forecastX + 14, forecastY - 12); ctx.lineTo(forecastX + 23, forecastY - 22); ctx.stroke();
                  } else if (forecastState.key === 'broad') {
                    ctx.setLineDash([5, 3]); ctx.beginPath(); ctx.arc(forecastFilmX, forecastFilmY, forecastRadius + 5, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                  } else if (forecastState.key === 'pooling') {
                    ctx.beginPath(); ctx.moveTo(forecastX, forecastY - 35); ctx.lineTo(forecastX + 11, forecastY - 17); ctx.lineTo(forecastX - 11, forecastY - 17); ctx.closePath(); ctx.stroke();
                    ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = forecastAccessible ? '#ffffff' : '#fecdd3'; ctx.fillText('!', forecastX - 2, forecastY - 22);
                  } else {
                    ctx.beginPath(); ctx.moveTo(forecastX - 9, forecastY - 18); ctx.lineTo(forecastX + 9, forecastY - 18); ctx.stroke();
                  }
                }
                ctx.restore();
                ctx.save(); ctx.font = 'bold 8px Inter, system-ui';
                if (directDropperDrag && canvasEl._dropperDrag.samples && canvasEl._dropperDrag.samples.length > 1) {
                  ctx.save(); ctx.beginPath(); ctx.ellipse(forecastMaterial.cx * W, forecastMaterial.cy * H, forecastMaterial.rx * W, forecastMaterial.ry * H, 0, 0, Math.PI * 2); ctx.clip();
                  ctx.strokeStyle = forecastAccessible ? '#ffffff' : (forecastState.key === 'controlled' ? '#5eead4' : '#facc15'); ctx.lineWidth = forecastAccessible ? 2.5 : 1.5; ctx.setLineDash(forecastState.pattern || []); ctx.beginPath();
                  canvasEl._dropperDrag.samples.forEach(function (sample, sampleIndex) { var sx = sample.x * W, sy = sample.y * H; if (sampleIndex === 0) ctx.moveTo(sx, sy); else ctx.lineTo(sx, sy); }); ctx.stroke(); ctx.setLineDash([]); ctx.restore();
                }
                var forecastTitle = forecastState.label + ' \\u00B7 DOSE ' + forecastDose;
                var forecastDetail = directDropperAssessment ? ('CONTACT ' + directDropperAssessment.contactPercent + '% \\u00B7 FLOW ' + directDropperAssessment.flowAlignment + '% \\u00B7 CTRL ' + directDropperAssessment.control + '%') : ('MOISTURE ' + forecastTissue.moisture + '% \\u00B7 ' + String(forecastProfile.pattern || 'film').toUpperCase() + ' FLOW');
                var forecastLabelWidth = Math.min(190, Math.max(148, Math.ceil(Math.max(ctx.measureText(forecastTitle).width, ctx.measureText(forecastDetail).width)) + 14));
                var forecastLabelX = Math.max(8, Math.min(W - forecastLabelWidth - 8, forecastX + 24));
                var forecastLabelY = Math.max(8, Math.min(H - 38, forecastY - 42));
                ctx.fillStyle = forecastAccessible ? '#000000' : 'rgba(15,23,42,0.93)'; ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(forecastLabelX, forecastLabelY, forecastLabelWidth, 34, 6); else ctx.rect(forecastLabelX, forecastLabelY, forecastLabelWidth, 34);
                ctx.fill(); ctx.strokeStyle = forecastAccessible ? '#ffffff' : forecastColor; ctx.lineWidth = 1.2; ctx.stroke();
                ctx.fillStyle = forecastAccessible ? '#ffffff' : (forecastState.key === 'pooling' ? '#fecdd3' : (forecastState.key === 'controlled' ? '#dbeafe' : '#fef08a'));
                ctx.fillText(forecastTitle.slice(0, 32), forecastLabelX + 7, forecastLabelY + 12);
                ctx.font = '7px Inter, system-ui'; ctx.fillStyle = forecastAccessible ? '#facc15' : '#e2e8f0';
                ctx.fillText(forecastDetail.slice(0, 34), forecastLabelX + 7, forecastLabelY + 25);
                ctx.restore();
              }
              function drawWickRecoveryPreview(pointer, canvasEl) {
                if (!pointer) return;
                var wickDragActive = !!(canvasEl && canvasEl._wickDrag && canvasEl._wickDrag.active);
                var wickAssessment = wickDragActive ? wickDragAssessment(canvasEl, pointer, Date.now()) : null;
                var wickPoolPoint = wickDragActive && canvasEl._wickDrag.poolPoint ? canvasEl._wickDrag.poolPoint : currentProcedure.dropperPoint;
                if (!wickPoolPoint) return;
                var wickContact = specimenContactContext(pointer);
                var wickTissue = normalizeTissueState(liveTissueState);
                var wickDistance = procedurePointDistance(pointer, wickPoolPoint);
                var wickState = wickAssessment ? wickAssessment.state : (wickTissue.salineDrops > 2 && wickContact.onSpecimen && wickDistance <= 0.16 ? { key: 'controlled', label: 'POOL EDGE READY', detail: 'PRESS · DRAG · RELEASE', pattern: [] } : { key: 'position', label: 'MOVE TO POOL EDGE', detail: 'FOLLOW THE ARROW', pattern: [5, 4] });
                var wickColor = wickState.key === 'controlled' ? '#5eead4' : (wickState.key === 'position' ? '#facc15' : '#fb7185');
                var wickAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var wickX = pointer.x * W, wickY = pointer.y * H, wickPoolX = wickPoolPoint.x * W, wickPoolY = wickPoolPoint.y * H;
                ctx.save(); ctx.beginPath(); ctx.ellipse((SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog).cx * W, (SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog).cy * H, (SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog).rx * W, (SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog).ry * H, 0, 0, Math.PI * 2); ctx.clip();
                ctx.strokeStyle = wickAccessible ? '#ffffff' : wickColor; ctx.lineWidth = wickAccessible ? 2.4 : 1.4; ctx.setLineDash(wickState.pattern || [5, 3]); ctx.beginPath(); ctx.moveTo(wickX, wickY); ctx.lineTo(wickPoolX, wickPoolY); ctx.stroke(); ctx.setLineDash([]);
                if (wickDragActive && canvasEl._wickDrag.samples && canvasEl._wickDrag.samples.length > 1) { ctx.lineWidth = wickAccessible ? 2.8 : 1.7; ctx.beginPath(); canvasEl._wickDrag.samples.forEach(function (sample, sampleIndex) { var sx = sample.x * W, sy = sample.y * H; if (sampleIndex === 0) ctx.moveTo(sx, sy); else ctx.lineTo(sx, sy); }); ctx.stroke(); }
                ctx.lineWidth = wickAccessible ? 2.5 : 1.4; ctx.setLineDash([6, 3]); ctx.beginPath(); ctx.ellipse(wickPoolX, wickPoolY, 20, 12, 0, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                ctx.save(); ctx.translate(wickX, wickY); ctx.rotate(Math.atan2(wickPoolY - wickY, wickPoolX - wickX)); ctx.strokeStyle = wickAccessible ? '#ffffff' : '#99f6e4'; ctx.lineWidth = wickAccessible ? 2.4 : 1.3; ctx.beginPath(); ctx.ellipse(0, 0, 14, 7, 0, 0, Math.PI * 2); ctx.stroke(); for (var wickPreviewFiber = -8; wickPreviewFiber <= 8; wickPreviewFiber += 4) { ctx.beginPath(); ctx.moveTo(wickPreviewFiber, -5); ctx.lineTo(wickPreviewFiber + 4, 5); ctx.stroke(); } ctx.restore();
                ctx.restore(); ctx.save(); ctx.font = 'bold 8px Inter, system-ui';
                var wickTitle = wickState.label + ' · POOL ' + wickTissue.salineDrops + ' DROPS';
                var wickDetail = wickAssessment ? ('RECOVERY ' + wickAssessment.recoveryPercent + '% · EDGE ' + wickAssessment.edgeAlignment + '% · CTRL ' + wickAssessment.control + '%') : ('POOL DISTANCE ' + Math.round(wickDistance * 100) + '% · TOUCH EDGE TO RECOVER');
                var wickLabelWidth = Math.min(196, Math.max(150, Math.ceil(Math.max(ctx.measureText(wickTitle).width, ctx.measureText(wickDetail).width)) + 14));
                var wickLabelX = Math.max(8, Math.min(W - wickLabelWidth - 8, wickX + 22)), wickLabelY = Math.max(8, Math.min(H - 38, wickY - 40));
                ctx.fillStyle = wickAccessible ? '#000000' : 'rgba(15,23,42,0.93)'; ctx.beginPath(); if (ctx.roundRect) ctx.roundRect(wickLabelX, wickLabelY, wickLabelWidth, 34, 6); else ctx.rect(wickLabelX, wickLabelY, wickLabelWidth, 34); ctx.fill(); ctx.strokeStyle = wickAccessible ? '#ffffff' : wickColor; ctx.lineWidth = 1.2; ctx.stroke();
                ctx.fillStyle = wickAccessible ? '#ffffff' : (wickState.key === 'controlled' ? '#ccfbf1' : '#fef08a'); ctx.fillText(wickTitle.slice(0, 34), wickLabelX + 7, wickLabelY + 12); ctx.font = '7px Inter, system-ui'; ctx.fillStyle = wickAccessible ? '#facc15' : '#e2e8f0'; ctx.fillText(wickDetail.slice(0, 38), wickLabelX + 7, wickLabelY + 25); ctx.restore();
              }
              // Legacy signature: function drawProbePalpationPreview(pointer, organ)
              function drawProbePalpationPreview(pointer, organ, canvasEl) {
                var directProbeDrag = !!(canvasEl && canvasEl._probeDrag && canvasEl._probeDrag.active);
                var directProbeAssessment = directProbeDrag ? probeDragAssessment(canvasEl, pointer, Date.now()) : null;
                if (directProbeAssessment && directProbeAssessment.target) organ = directProbeAssessment.target;
                if (!pointer || !organ) return;
                // Live palpation maps calibrated pressure, organism material, tissue condition, and anatomical depth into pre-commit deformation feedback.
                var palpationMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var palpationTissue = normalizeTissueState(liveTissueState);
                var palpationAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var palpationPressure = directProbeAssessment ? directProbeAssessment.pressure : Math.max(10, Math.min(80, Number(toolCalibration.probePressure) || 30));
                var palpationDepth = structureOpticalDepth(organ);
                var palpationDepthRank = { surface: 0, structure: 1, deep: 2 }[palpationDepth] || 0;
                var palpationResistanceValue = Math.max(8, Math.min(96, Math.round(
                  (1 - (Number(palpationMaterial.compliance) || 0.5)) * 54
                  + palpationDepthRank * 16
                  + palpationTissue.tension * 0.16
                  + (100 - palpationTissue.moisture) * 0.08
                )));
                var palpationResistance = palpationResistanceValue < 34 ? 'LOW' : (palpationResistanceValue < 67 ? 'MODERATE' : 'HIGH');
                if (directProbeAssessment) { palpationResistanceValue = directProbeAssessment.resistanceValue; palpationResistance = directProbeAssessment.resistance; }
                var palpationState = palpationPressure < 18
                  ? { key: 'light', label: 'TOO LIGHT', detail: 'NO TACTILE SIGNAL', pattern: [2, 3] }
                  : (palpationPressure > 38
                    ? { key: 'stress', label: 'EXCESS PRESSURE', detail: 'TISSUE STRESS', pattern: [2, 2] }
                    : { key: 'controlled', label: 'CONTROLLED PALPATION', detail: 'BOUNDARY RESOLVED', pattern: [] });
                if (directProbeAssessment) palpationState = directProbeAssessment.state;
                var palpationOrganPoint = variedOrganPoint(organ);
                var palpationX = pointer.x * W, palpationY = pointer.y * H;
                var palpationOrganX = palpationOrganPoint.x * W, palpationOrganY = palpationOrganPoint.y * H;
                var palpationGuideDX = palpationOrganX - palpationX, palpationGuideDY = palpationOrganY - palpationY;
                var palpationGuideDistance = Math.sqrt(palpationGuideDX * palpationGuideDX + palpationGuideDY * palpationGuideDY);
                var palpationCompliance = Math.max(0.14, Number(palpationMaterial.compliance) || 0.5);
                var palpationDeformation = directProbeAssessment ? directProbeAssessment.deformation : (2.5 + palpationPressure * 0.13) * (0.58 + palpationCompliance * 0.62);
                var palpationColor = palpationState.key === 'stress' ? '#fb7185' : (palpationState.key === 'light' || (directProbeAssessment && directProbeAssessment.key === 'drift') ? '#facc15' : '#5eead4');
                var depthRadii = [19, 13, 7];
                ctx.save();
                if (palpationGuideDistance > 3) {
                  ctx.strokeStyle = palpationAccessible ? '#ffffff' : palpationColor; ctx.lineWidth = palpationAccessible ? 2.2 : 1.2; ctx.setLineDash([4, 3]);
                  ctx.beginPath(); ctx.moveTo(palpationX, palpationY); ctx.lineTo(palpationOrganX, palpationOrganY); ctx.stroke(); ctx.setLineDash([]);
                }
                ctx.translate(palpationX, palpationY);
                ctx.strokeStyle = palpationAccessible ? '#ffffff' : palpationColor;
                depthRadii.forEach(function (radius, depthIndex) {
                  ctx.lineWidth = depthIndex === palpationDepthRank ? (palpationAccessible ? 3.2 : 2.4) : (palpationAccessible ? 1.8 : 1);
                  ctx.setLineDash(depthIndex === 0 ? [] : (depthIndex === 1 ? [5, 3] : [1.5, 2.5]));
                  ctx.beginPath();
                  if (depthIndex === 1) {
                    ctx.moveTo(0, -radius); ctx.lineTo(radius, 0); ctx.lineTo(0, radius); ctx.lineTo(-radius, 0); ctx.closePath();
                  } else {
                    ctx.arc(0, 0, radius, 0, Math.PI * 2);
                    if (depthIndex === 2) { ctx.moveTo(radius + 3, 0); ctx.arc(0, 0, radius + 3, 0, Math.PI * 2); }
                  }
                  ctx.stroke();
                });
                ctx.setLineDash(palpationState.pattern);
                ctx.globalAlpha = palpationState.key === 'stress' ? 0.30 : 0.18;
                ctx.fillStyle = palpationAccessible ? '#000000' : palpationColor;
                ctx.beginPath(); ctx.ellipse(0, 3, 13 + palpationDeformation, Math.max(3, 8 - palpationDeformation * 0.32), 0, 0, Math.PI * 2); ctx.fill();
                ctx.globalAlpha = 1; ctx.strokeStyle = palpationAccessible ? '#ffffff' : palpationColor;
                ctx.lineWidth = palpationAccessible ? 2.4 : 1.5; ctx.stroke(); ctx.setLineDash([]);
                for (var displacementIndex = 0; displacementIndex < 4; displacementIndex++) {
                  var displacementAngle = displacementIndex * Math.PI / 2;
                  var displacementStart = 21;
                  var displacementEnd = displacementStart + Math.min(10, palpationDeformation * 0.78);
                  var displacementCos = Math.cos(displacementAngle), displacementSin = Math.sin(displacementAngle);
                  ctx.beginPath();
                  ctx.moveTo(displacementCos * displacementStart, displacementSin * displacementStart);
                  ctx.lineTo(displacementCos * displacementEnd, displacementSin * displacementEnd);
                  ctx.lineTo(displacementCos * (displacementEnd - 4) - displacementSin * 2.5, displacementSin * (displacementEnd - 4) + displacementCos * 2.5);
                  ctx.moveTo(displacementCos * displacementEnd, displacementSin * displacementEnd);
                  ctx.lineTo(displacementCos * (displacementEnd - 4) + displacementSin * 2.5, displacementSin * (displacementEnd - 4) - displacementCos * 2.5);
                  ctx.stroke();
                }
                if (palpationState.key === 'controlled') {
                  ctx.lineWidth = 2.6; ctx.beginPath(); ctx.moveTo(8, -20); ctx.lineTo(12, -16); ctx.lineTo(20, -25); ctx.stroke();
                } else if (palpationState.key === 'light') {
                  ctx.setLineDash([2, 2]); ctx.beginPath(); ctx.moveTo(-7, -24); ctx.lineTo(7, -24); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.arc(0, -24, 4, 0, Math.PI * 2); ctx.stroke();
                } else {
                  ctx.beginPath(); ctx.moveTo(0, -31); ctx.lineTo(11, -13); ctx.lineTo(-11, -13); ctx.closePath(); ctx.stroke();
                  ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = palpationAccessible ? '#ffffff' : '#fecdd3'; ctx.fillText('!', -2, -18);
                  ctx.globalAlpha = 0.62; ctx.lineWidth = 1;
                  for (var stressHatch = -8; stressHatch <= 8; stressHatch += 4) {
                    ctx.beginPath(); ctx.moveTo(stressHatch - 5, -2); ctx.lineTo(stressHatch + 5, 8); ctx.stroke();
                  }
                  ctx.globalAlpha = 1;
                }
                ctx.restore();
                var palpationLabelHeight = directProbeAssessment ? 54 : 42;
                var palpationLabelWidth = directProbeAssessment ? Math.min(214, Math.max(164, Math.ceil(ctx.measureText(organ.name).width) + 52)) : Math.min(188, Math.max(142, Math.ceil(ctx.measureText(organ.name).width) + 28));
                var palpationLabelX = Math.max(8, Math.min(W - palpationLabelWidth - 8, palpationX + 28));
                var palpationLabelY = Math.max(8, Math.min(H - palpationLabelHeight - 6, palpationY - 42));
                ctx.save(); ctx.fillStyle = palpationAccessible ? '#000000' : 'rgba(15,23,42,0.93)';
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(palpationLabelX, palpationLabelY, palpationLabelWidth, palpationLabelHeight, 6); else ctx.rect(palpationLabelX, palpationLabelY, palpationLabelWidth, palpationLabelHeight);
                ctx.fill(); ctx.strokeStyle = palpationAccessible ? '#ffffff' : palpationColor; ctx.lineWidth = 1.2; ctx.stroke();
                ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = palpationAccessible ? '#ffffff' : (palpationState.key === 'stress' ? '#fecdd3' : (palpationState.key === 'light' ? '#fef08a' : '#ccfbf1'));
                ctx.fillText((palpationState.label + ' \u00B7 ' + palpationPressure + '%').slice(0, 31), palpationLabelX + 7, palpationLabelY + 11);
                ctx.font = '7px Inter, system-ui'; ctx.fillStyle = palpationAccessible ? '#ffffff' : '#e2e8f0';
                ctx.fillText(organ.name.slice(0, 31), palpationLabelX + 7, palpationLabelY + 23);
                ctx.fillStyle = palpationAccessible ? '#facc15' : palpationColor;
                ctx.fillText(anatomicalDepthLabel(palpationDepth).toUpperCase() + ' \u00B7 ' + palpationResistance + ' RESISTANCE', palpationLabelX + 7, palpationLabelY + 35);
                if (directProbeAssessment) { ctx.fillStyle = palpationAccessible ? '#facc15' : '#99f6e4'; ctx.fillText(('ALIGN ' + directProbeAssessment.alignment + '% · CONTACT ' + directProbeAssessment.contactPercent + '% · CONTROL ' + directProbeAssessment.control + '%').slice(0, 37), palpationLabelX + 7, palpationLabelY + 47); }
                ctx.restore();
              }
              function drawPinStabilityPreview(guide, pointer, pins) {
                if (!guide || guide.length < 2 || !pointer) return;
                // Pin preview combines endpoint placement, separation, shaft angle, insertion depth, and flap tension before commitment.
                pins = pins || [];
                var pinAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var pinMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                var pinTissue = normalizeTissueState(liveTissueState);
                var directPinDrag = canvas._pinDrag && canvas._pinDrag.active ? canvas._pinDrag : null;
                var directPinAssessment = directPinDrag ? (directPinDrag.assessment || pinDragAssessment(canvas, directPinDrag.current, Date.now())) : null;
                var pinPointer = directPinDrag ? directPinDrag.start : pointer;
                var pinStart = guide[0], pinEnd = guide[guide.length - 1];
                var startDistance = procedurePointDistance(pinPointer, pinStart), endDistance = procedurePointDistance(pinPointer, pinEnd);
                var pinTarget = startDistance <= endDistance ? pinStart : pinEnd;
                var endpointDistance = Math.min(startDistance, endDistance);
                var pinSpacing = pins.length ? procedurePointDistance(pointer, pins[0]) : 1;
                var pinAngle = Math.max(35, Math.min(90, Number(toolCalibration.pinAngle) || 65));
                var pinState = endpointDistance > 0.14
                  ? { key: 'position', label: 'MOVE TO ENDPOINT', pattern: [5, 4] }
                  : (pinSpacing < 0.18
                    ? { key: 'crowded', label: 'TOO CLOSE', pattern: [2, 2] }
                    : (pinAngle < 55
                      ? { key: 'shallow', label: 'SHALLOW \u00B7 SLIP', pattern: [5, 3] }
                      : (pinAngle > 75
                        ? { key: 'steep', label: 'STEEP \u00B7 STRESS', pattern: [2, 2] }
                        : { key: 'stable', label: 'STABLE ANCHOR', pattern: [] })));
                if (directPinAssessment) pinState = { key: directPinAssessment.key, label: directPinAssessment.label, pattern: directPinAssessment.key === 'stable' || directPinAssessment.key === 'grasp' ? [] : (directPinAssessment.key === 'shallow' || directPinAssessment.key === 'direction' ? [5, 3] : [2, 2]) };
                var pinColor = pinState.key === 'stable' ? '#7dd3fc'
                  : (pinState.key === 'position' ? '#facc15' : '#fb7185');
                var tipX = pinPointer.x * W, tipY = pinPointer.y * H;
                var guideDX = (pinEnd.x - pinStart.x) * W, guideDY = (pinEnd.y - pinStart.y) * H;
                var guideLength = Math.sqrt(guideDX * guideDX + guideDY * guideDY) || 1;
                var tangentX = guideDX / guideLength, tangentY = guideDY / guideLength;
                var normalX = -tangentY, normalY = tangentX;
                var openingMid = guide[Math.floor((guide.length - 1) / 2)] || { x: (pinStart.x + pinEnd.x) / 2, y: (pinStart.y + pinEnd.y) / 2 };
                var targetSide = pinTarget === pinStart ? -1 : 1;
                var pinAngleOffset = (pinAngle - 65) * Math.PI / 180 * 0.72 * targetSide;
                var shaftDirectionX = normalX * Math.cos(pinAngleOffset) + tangentX * Math.sin(pinAngleOffset);
                var shaftDirectionY = normalY * Math.cos(pinAngleOffset) + tangentY * Math.sin(pinAngleOffset);
                var shaftLength = 25 + Math.sin(pinAngle * Math.PI / 180) * 9;
                var headX = directPinDrag ? directPinDrag.current.x * W : tipX - shaftDirectionX * shaftLength, headY = directPinDrag ? directPinDrag.current.y * H : tipY - shaftDirectionY * shaftLength;
                var insertionDepth = (6 + (Number(pinMaterial.compliance) || 0.5) * 5) * Math.sin(pinAngle * Math.PI / 180);
                var insertionX = tipX + shaftDirectionX * insertionDepth, insertionY = tipY + shaftDirectionY * insertionDepth;
                var anchorRadius = 7 + (Number(pinMaterial.compliance) || 0.5) * 4 + pinTissue.tension / 100 * 2;
                var pinPulse = dissMotionReduced ? 0 : (Math.sin(dissTick * 0.055) + 1) * 0.9;
                ctx.save();
                if (directPinAssessment && directPinAssessment.validStart) {
                  ctx.save(); ctx.globalAlpha = pinAccessible ? 0.92 : 0.58; ctx.strokeStyle = pinAccessible ? '#ffffff' : '#7dd3fc'; ctx.lineWidth = pinAccessible ? 2.2 : 1.15; ctx.setLineDash([]);
                  ctx.beginPath(); ctx.ellipse(tipX, tipY, directPinAssessment.safeTravel * W, directPinAssessment.safeTravel * H, 0, 0, Math.PI * 2); ctx.stroke();
                  ctx.setLineDash([3, 3]); ctx.globalAlpha *= 0.72; ctx.beginPath(); ctx.ellipse(tipX, tipY, 0.026 * W, 0.026 * H, 0, 0, Math.PI * 2); ctx.stroke();
                  ctx.setLineDash([]); ctx.globalAlpha = 1; ctx.restore();
                }
                ctx.strokeStyle = pinAccessible ? '#ffffff' : pinColor; ctx.fillStyle = pinAccessible ? '#000000' : 'rgba(15,23,42,0.78)';
                ctx.lineWidth = pinAccessible ? 2.5 : 1.6;
                if (pinState.key === 'position') {
                  ctx.setLineDash([5, 4]); ctx.beginPath(); ctx.moveTo(tipX, tipY); ctx.lineTo(pinTarget.x * W, pinTarget.y * H); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.arc(pinTarget.x * W, pinTarget.y * H, 13 + pinPulse, 0, Math.PI * 2); ctx.stroke();
                } else {
                  ctx.save(); ctx.translate(tipX, tipY); ctx.rotate(Math.atan2(guideDY, guideDX));
                  ctx.scale(1, Math.max(0.28, Math.cos(pinAngle * Math.PI / 180)));
                  ctx.setLineDash(pinState.pattern); ctx.beginPath(); ctx.ellipse(0, 0, anchorRadius, anchorRadius * 0.68, 0, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); ctx.setLineDash([]); ctx.restore();
                  ctx.setLineDash(pinState.pattern); ctx.beginPath(); ctx.moveTo(headX, headY); ctx.lineTo(tipX, tipY); ctx.stroke();
                  ctx.setLineDash([2, 2]); ctx.globalAlpha = 0.66; ctx.beginPath(); ctx.moveTo(tipX, tipY); ctx.lineTo(insertionX, insertionY); ctx.stroke(); ctx.setLineDash([]); ctx.globalAlpha = 1;
                  var headGradient = ctx.createRadialGradient(headX - 2, headY - 2, 1, headX, headY, 7);
                  headGradient.addColorStop(0, '#ffffff'); headGradient.addColorStop(0.35, pinAccessible ? '#ffffff' : pinColor); headGradient.addColorStop(1, pinAccessible ? '#000000' : '#1e3a8a');
                  ctx.fillStyle = headGradient; ctx.beginPath(); ctx.arc(headX, headY, pinAccessible ? 7.5 : 6, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                  var tissueAnchorX = openingMid.x * W, tissueAnchorY = openingMid.y * H;
                  var tensionFan = 2 + Math.round((Number(pinMaterial.compliance) || 0.5) * 2 + pinTissue.tension / 70);
                  ctx.strokeStyle = pinAccessible ? '#ffffff' : pinColor; ctx.setLineDash(pinState.pattern);
                  for (var pinTensionIndex = 0; pinTensionIndex < tensionFan; pinTensionIndex++) {
                    var pinFanOffset = (pinTensionIndex - (tensionFan - 1) / 2) * (4 + pinTissue.tension / 60);
                    ctx.lineWidth = pinState.key === 'steep' ? 1.8 : 1;
                    ctx.beginPath(); ctx.moveTo(tipX + tangentX * pinFanOffset, tipY + tangentY * pinFanOffset);
                    ctx.lineTo(tissueAnchorX + tangentX * pinFanOffset * 0.45, tissueAnchorY + tangentY * pinFanOffset * 0.45); ctx.stroke();
                  }
                  ctx.setLineDash([]);
                  if (pins.length) {
                    var existingX = pins[0].x * W, existingY = pins[0].y * H;
                    var spacingDX = tipX - existingX, spacingDY = tipY - existingY;
                    var spacingLength = Math.sqrt(spacingDX * spacingDX + spacingDY * spacingDY) || 1;
                    var spacingNX = -spacingDY / spacingLength, spacingNY = spacingDX / spacingLength;
                    ctx.setLineDash(pinState.key === 'crowded' ? [2, 2] : [6, 4]); ctx.lineWidth = pinAccessible ? 2 : 1;
                    ctx.beginPath(); ctx.moveTo(existingX, existingY); ctx.lineTo(tipX, tipY);
                    ctx.moveTo(existingX - spacingNX * 4, existingY - spacingNY * 4); ctx.lineTo(existingX + spacingNX * 4, existingY + spacingNY * 4);
                    ctx.moveTo(tipX - spacingNX * 4, tipY - spacingNY * 4); ctx.lineTo(tipX + spacingNX * 4, tipY + spacingNY * 4); ctx.stroke(); ctx.setLineDash([]);
                  }
                  if (pinState.key === 'stable') {
                    ctx.lineWidth = 2.4; ctx.beginPath(); ctx.moveTo(tipX + 9, tipY - 10); ctx.lineTo(tipX + 13, tipY - 6); ctx.lineTo(tipX + 21, tipY - 15); ctx.stroke();
                  } else if (pinState.key === 'shallow') {
                    ctx.setLineDash([3, 2]); ctx.beginPath(); ctx.moveTo(tipX - tangentX * 15, tipY - tangentY * 15); ctx.lineTo(tipX + tangentX * 15, tipY + tangentY * 15);
                    ctx.moveTo(tipX + tangentX * 10 - normalX * 4, tipY + tangentY * 10 - normalY * 4); ctx.lineTo(tipX + tangentX * 15, tipY + tangentY * 15); ctx.lineTo(tipX + tangentX * 10 + normalX * 4, tipY + tangentY * 10 + normalY * 4); ctx.stroke(); ctx.setLineDash([]);
                  } else if (pinState.key === 'crowded') {
                    ctx.beginPath(); ctx.arc(tipX, tipY, 12, 0, Math.PI * 2); ctx.arc(pins[0].x * W, pins[0].y * H, 12, 0, Math.PI * 2); ctx.stroke();
                    ctx.lineWidth = 2.4; ctx.beginPath(); ctx.moveTo(tipX - 5, tipY - 5); ctx.lineTo(tipX + 5, tipY + 5); ctx.moveTo(tipX + 5, tipY - 5); ctx.lineTo(tipX - 5, tipY + 5); ctx.stroke();
                  } else if (pinState.key === 'direction') {
                    ctx.setLineDash([3, 3]); ctx.beginPath(); ctx.moveTo(headX, headY); ctx.lineTo(tipX, tipY); ctx.stroke(); ctx.setLineDash([]);
                    ctx.beginPath(); ctx.moveTo(tipX - normalX * 8, tipY - normalY * 8); ctx.lineTo(tipX, tipY); ctx.lineTo(tipX + normalX * 8, tipY + normalY * 8); ctx.stroke();
                  } else if (pinState.key === 'steep') {
                    ctx.beginPath(); ctx.moveTo(tipX, tipY - 18); ctx.lineTo(tipX + 10, tipY - 1); ctx.lineTo(tipX - 10, tipY - 1); ctx.closePath(); ctx.stroke();
                    for (var pinStressHatch = -5; pinStressHatch <= 5; pinStressHatch += 5) {
                      ctx.beginPath(); ctx.moveTo(tipX + pinStressHatch - 3, tipY + 5); ctx.lineTo(tipX + pinStressHatch + 3, tipY + 11); ctx.stroke();
                    }
                  }
                }
                var pinLabel = pinState.label + ' \u00B7 ' + pinAngle + '\u00B0' + (directPinAssessment ? ' \u00B7 INSERT ' + directPinAssessment.insertionPercent + '% \u00B7 ALIGN ' + directPinAssessment.alignment + '%' : '');
                ctx.font = 'bold 8px Inter, system-ui';
                var pinLabelWidth = Math.min(directPinAssessment ? 204 : 136, Math.ceil(ctx.measureText(pinLabel).width) + 12);
                var pinLabelX = Math.max(8, Math.min(W - pinLabelWidth - 8, headX + 11));
                var pinLabelY = Math.max(8, Math.min(H - 22, headY - 22));
                ctx.fillStyle = pinAccessible ? '#000000' : 'rgba(15,23,42,0.92)';
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(pinLabelX, pinLabelY, pinLabelWidth, 18, 5); else ctx.rect(pinLabelX, pinLabelY, pinLabelWidth, 18);
                ctx.fill(); ctx.strokeStyle = pinAccessible ? '#ffffff' : pinColor; ctx.lineWidth = 1.1; ctx.stroke();
                ctx.fillStyle = pinAccessible ? '#ffffff' : (pinState.key === 'stable' ? '#dbeafe' : '#fef08a');
                ctx.fillText(pinLabel.slice(0, directPinAssessment ? 42 : 27), pinLabelX + 6, pinLabelY + 12);
                ctx.restore();
              }
              if (!d.quizMode && (d.procedureMode || 'guided') === 'guided' && (d.visualRealism || 'guided') !== 'realistic' && !d.beforeTechniqueView && !canvasProcedure.incisionStarted && !revealedLayers[activeLayer]) {
                drawAnatomicalAccessCorridor(procedureGuidePoints());
              }
              var overlayGuide = procedureGuidePoints();
              var liveDemo = d._procedureDemo;
              if (!d.quizMode && liveDemo && liveDemo.layer === activeLayer && Date.now() - liveDemo.startedAt < 3000) {
                var demoProgress = Math.max(0, Math.min(1, (Date.now() - liveDemo.startedAt) / 2600));
                var demoPath = [overlayGuide[0]], demoPoint;
                if (demoProgress <= 0.5) {
                  var firstProgress = demoProgress * 2;
                  demoPoint = { x: overlayGuide[0].x + (overlayGuide[1].x - overlayGuide[0].x) * firstProgress, y: overlayGuide[0].y + (overlayGuide[1].y - overlayGuide[0].y) * firstProgress };
                  demoPath.push(demoPoint);
                } else {
                  var secondProgress = (demoProgress - 0.5) * 2;
                  demoPath.push(overlayGuide[1]);
                  demoPoint = { x: overlayGuide[1].x + (overlayGuide[2].x - overlayGuide[1].x) * secondProgress, y: overlayGuide[1].y + (overlayGuide[2].y - overlayGuide[1].y) * secondProgress };
                  demoPath.push(demoPoint);
                }
                drawProcedurePath(demoPath, '#67e8f9', 5, false);
                ctx.beginPath(); ctx.arc(demoPoint.x * W, demoPoint.y * H, 7, 0, Math.PI * 2);
                ctx.fillStyle = '#fef08a'; ctx.fill(); ctx.strokeStyle = '#0e7490'; ctx.lineWidth = 2; ctx.stroke();
              }
              var liveReplay = d._procedureReplay;
              if (!d.quizMode && liveReplay && liveReplay.layer === activeLayer && Date.now() - liveReplay.startedAt < liveReplay.duration) {
                var replayProgress = Math.max(0, Math.min(0.999, (Date.now() - liveReplay.startedAt) / liveReplay.duration));
                var replayActions = liveReplay.actions || [];
                var replayIndex = Math.min(replayActions.length - 1, Math.floor(replayProgress * replayActions.length));
                var replayEntry = replayActions[Math.max(0, replayIndex)] || { action: 'inspect', label: 'Inspect' };
                var replayLocal = replayProgress * Math.max(1, replayActions.length) - Math.max(0, replayIndex);
                var replayDisplayProgress = dissMotionReduced ? 1 : replayLocal;
                var replayCuttingAction = replayEntry.action === 'scalpel' || replayEntry.action === 'scissors';
                var replayPath = replayEntry.action === 'scissors'
                  ? (canvasProcedure.extensionPath || canvasProcedure.incisionPath || overlayGuide)
                  : (replayEntry.action === 'scalpel' ? (canvasProcedure.incisionPath || overlayGuide) : []);
                var replaySamples = replayEntry.action === 'scissors'
                  ? (canvasProcedure.extensionSamples || [])
                  : (replayEntry.action === 'scalpel' ? (canvasProcedure.incisionSamples || []) : []);
                var replayTrace = replayCuttingAction
                  ? drawPressureReplayTrail(replayPath, replaySamples, replayDisplayProgress, 'rgba(34,211,238,0.88)', false)
                  : null;
                var replayPoint = replayTrace ? replayTrace.point : (overlayGuide[Math.floor((overlayGuide.length - 1) / 2)] || overlayGuide[0]);
                var replayCalibration = normalizeToolCalibration(replayEntry.toolCalibration || toolCalibration);
                var replayMeasureText = 'ORIENTATION';
                var replayStatusText = 'POSITION CHECK';
                if (replayCuttingAction) {
                  var replayDistance = distanceToGuide(replayPoint, overlayGuide);
                  replayStatusText = replayDistance <= 0.04 ? 'ON PATH' : (replayDistance <= 0.075 ? 'EDGE' : 'CORRECTION');
                  replayMeasureText = 'PRESSURE ' + Math.round((replayTrace ? replayTrace.pressure : 0.34) * 100) + '%';
                } else if (replayEntry.action === 'forceps') {
                  if (canvasProcedure.forcepsPoint) replayPoint = canvasProcedure.forcepsPoint;
                  replayMeasureText = 'GRIP ' + replayCalibration.forcepsGrip + '%'; replayStatusText = 'LIFT';
                } else if (replayEntry.action === 'pin') {
                  var replayPinOrdinal = replayActions.slice(0, replayIndex + 1).filter(function (entry) { return entry.action === 'pin'; }).length - 1;
                  if ((canvasProcedure.pins || []).length) replayPoint = canvasProcedure.pins[Math.max(0, Math.min((canvasProcedure.pins || []).length - 1, replayPinOrdinal))];
                  replayMeasureText = 'ANGLE ' + replayCalibration.pinAngle + '\u00B0'; replayStatusText = 'ANCHOR';
                } else if (replayEntry.action === 'probe') {
                  var replayProbeOrgan = organs.find(function (organ) { return organ.id === canvasProcedure.probedOrganId; });
                  if (replayProbeOrgan) replayPoint = variedOrganPoint(replayProbeOrgan);
                  replayMeasureText = 'PRESSURE ' + replayCalibration.probePressure + '%'; replayStatusText = 'TRACE';
                } else if (replayEntry.action === 'dropper') {
                  if (canvasProcedure.dropperPoint) replayPoint = canvasProcedure.dropperPoint;
                  replayMeasureText = 'DOSE ' + replayCalibration.salineDose; replayStatusText = 'HYDRATE';
                } else if (replayEntry.action === 'wick') {
                  if (canvasProcedure.wickPoint) replayPoint = canvasProcedure.wickPoint;
                  replayMeasureText = 'ABSORB'; replayStatusText = 'WICK';
                }
                var replayAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var replayMarkerX = replayPoint.x * W, replayMarkerY = replayPoint.y * H;
                ctx.save(); ctx.translate(replayMarkerX, replayMarkerY);
                ctx.shadowColor = replayAccessible ? 'rgba(0,0,0,0.9)' : 'rgba(8,145,178,0.7)'; ctx.shadowBlur = replayAccessible ? 3 : 10;
                ctx.strokeStyle = replayAccessible ? '#ffffff' : '#67e8f9'; ctx.fillStyle = replayAccessible ? '#000000' : 'rgba(15,23,42,0.82)';
                ctx.lineWidth = replayAccessible ? 2.5 : 1.8; ctx.beginPath();
                if (replayCuttingAction && replayStatusText === 'ON PATH') {
                  ctx.arc(0, 0, 8, 0, Math.PI * 2);
                } else if (replayCuttingAction && replayStatusText === 'EDGE') {
                  ctx.moveTo(0, -9); ctx.lineTo(9, 0); ctx.lineTo(0, 9); ctx.lineTo(-9, 0); ctx.closePath();
                } else if (replayCuttingAction) {
                  ctx.moveTo(0, -10); ctx.lineTo(10, 8); ctx.lineTo(-10, 8); ctx.closePath();
                } else if (replayEntry.action === 'forceps') {
                  ctx.moveTo(-11, -8); ctx.lineTo(-2, 0); ctx.lineTo(-11, 8); ctx.moveTo(11, -8); ctx.lineTo(2, 0); ctx.lineTo(11, 8);
                } else if (replayEntry.action === 'pin') {
                  ctx.moveTo(-10, 0); ctx.lineTo(10, 0); ctx.moveTo(0, -10); ctx.lineTo(0, 10); ctx.moveTo(5, 0); ctx.arc(0, 0, 5, 0, Math.PI * 2);
                } else if (replayEntry.action === 'dropper') {
                  ctx.ellipse(0, 0, 11, 6, 0, 0, Math.PI * 2); ctx.moveTo(0, -13); ctx.lineTo(0, -5);
                } else if (replayEntry.action === 'probe') {
                  ctx.arc(0, 0, 9, 0, Math.PI * 2); ctx.moveTo(-13, 0); ctx.lineTo(13, 0); ctx.moveTo(0, -13); ctx.lineTo(0, 13);
                } else {
                  ctx.arc(0, 0, 8, 0, Math.PI * 2); ctx.moveTo(-12, 0); ctx.lineTo(-8, 0); ctx.moveTo(8, 0); ctx.lineTo(12, 0);
                }
                if (replayCuttingAction || replayEntry.action === 'dropper' || replayEntry.action === 'inspect') ctx.fill();
                ctx.stroke();
                if (replayCuttingAction && replayTrace) {
                  ctx.shadowBlur = 0; ctx.strokeStyle = '#facc15'; ctx.lineWidth = 2;
                  ctx.beginPath(); ctx.arc(0, 0, 12, -Math.PI / 2, -Math.PI / 2 + Math.PI * 2 * replayTrace.pressure); ctx.stroke();
                }
                ctx.restore();
                ctx.save(); ctx.shadowBlur = 0; ctx.font = 'bold 8px Inter, system-ui';
                var replayTitle = 'STEP ' + (replayIndex + 1) + '/' + replayActions.length + ' \u00B7 ' + (replayEntry.label || procedureActionLabel(replayEntry.action)).toUpperCase();
                var replayDetail = replayMeasureText + ' \u00B7 ' + replayStatusText;
                var replayLabelWidth = Math.min(174, Math.max(118, Math.ceil(Math.max(ctx.measureText(replayTitle).width, ctx.measureText(replayDetail).width)) + 14));
                var replayLabelX = Math.max(8, Math.min(W - replayLabelWidth - 8, replayMarkerX + 14));
                var replayLabelY = Math.max(8, Math.min(H - 42, replayMarkerY - 33));
                ctx.fillStyle = replayAccessible ? '#000000' : 'rgba(15,23,42,0.92)';
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(replayLabelX, replayLabelY, replayLabelWidth, 34, 6); else ctx.rect(replayLabelX, replayLabelY, replayLabelWidth, 34);
                ctx.fill(); ctx.strokeStyle = replayAccessible ? '#ffffff' : '#67e8f9'; ctx.lineWidth = 1; ctx.stroke();
                ctx.fillStyle = '#ffffff'; ctx.fillText(replayTitle.slice(0, 31), replayLabelX + 7, replayLabelY + 11);
                ctx.fillStyle = replayAccessible ? '#facc15' : '#a5f3fc'; ctx.fillText(replayDetail.slice(0, 31), replayLabelX + 7, replayLabelY + 23);
                ctx.fillStyle = replayAccessible ? '#ffffff' : '#22d3ee';
                ctx.fillRect(replayLabelX + 1, replayLabelY + 30, Math.max(2, (replayLabelWidth - 2) * replayProgress), 3);
                ctx.restore();
              }
              var liveInstrument = d.activeInstrument || 'probe';
              if ((d.visualRealism || 'guided') !== 'realistic' && liveInstrument === 'forceps' && canvasProcedure.incisionExtended && !canvasProcedure.retracted) {
                ctx.save(); ctx.setLineDash([4, 3]); ctx.strokeStyle = '#5eead4'; ctx.lineWidth = 2;
                ctx.beginPath(); ctx.arc(overlayGuide[1].x * W, overlayGuide[1].y * H, 18, 0, Math.PI * 2); ctx.stroke(); ctx.restore();
              }
              if ((d.visualRealism || 'guided') !== 'realistic' && liveInstrument === 'pin' && canvasProcedure.retracted && (canvasProcedure.pins || []).length < 2) {
                [overlayGuide[0], overlayGuide[overlayGuide.length - 1]].forEach(function (target) {
                  ctx.save(); ctx.setLineDash([3, 3]); ctx.strokeStyle = '#f9a8d4'; ctx.lineWidth = 2;
                  ctx.beginPath(); ctx.arc(target.x * W, target.y * H, 13, 0, Math.PI * 2); ctx.stroke(); ctx.restore();
                });
              }
              var savedLayerAttempts = (((d.attemptArchive || {})[activeLayer]) || []);
              var previousTechniqueIndex = savedLayerAttempts.length - 1;
              if (previousTechniqueIndex >= 0 && currentProcedure.lastSavedAttemptId === savedLayerAttempts[previousTechniqueIndex].id) previousTechniqueIndex -= 1;
              var previousTechniqueAttempt = previousTechniqueIndex >= 0 ? savedLayerAttempts[previousTechniqueIndex] : null;
              var comparisonCurrentPath = currentProcedure.extensionPath || currentProcedure.incisionPath || [];
              var compareCanvasProgress = Math.max(0, Math.min(1, d.compareReplayProgress == null ? 1 : Number(d.compareReplayProgress) || 0));
              if (!d.quizMode && d.compareTechniqueAttempts && previousTechniqueAttempt) {
                var comparisonPreviousPath = previousTechniqueAttempt.extensionPath || previousTechniqueAttempt.incisionPath || [];
                if ((previousTechniqueAttempt.incisionPath || []).length > 1) drawPressureReplayTrail(previousTechniqueAttempt.incisionPath, previousTechniqueAttempt.incisionSamples || [], compareCanvasProgress, 'rgba(244,114,182,0.78)', true);
                if ((previousTechniqueAttempt.extensionPath || []).length > 1) drawPressureReplayTrail(previousTechniqueAttempt.extensionPath, previousTechniqueAttempt.extensionSamples || [], compareCanvasProgress, 'rgba(244,114,182,0.66)', true);
                if (!d.beforeTechniqueView) {
                  if ((currentProcedure.incisionPath || []).length > 1) drawPressureReplayTrail(currentProcedure.incisionPath, currentProcedure.incisionSamples || [], compareCanvasProgress, 'rgba(34,211,238,0.88)', false);
                  if ((currentProcedure.extensionPath || []).length > 1) drawPressureReplayTrail(currentProcedure.extensionPath, currentProcedure.extensionSamples || [], compareCanvasProgress, 'rgba(34,211,238,0.72)', false);
                }
                function drawComparisonMarker(point, label, color, diamond) {
                  if (!point || !isFinite(Number(point.x)) || !isFinite(Number(point.y))) return;
                  var markerX = Math.max(8, Math.min(W - 8, Number(point.x) * W));
                  var markerY = Math.max(8, Math.min(H - 8, Number(point.y) * H));
                  var markerAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                  ctx.save(); ctx.translate(markerX, markerY); ctx.fillStyle = markerAccessible ? '#000000' : color; ctx.strokeStyle = markerAccessible ? '#ffffff' : '#0f172a'; ctx.lineWidth = markerAccessible ? 1.8 : 1.2;
                  ctx.beginPath();
                  if (diamond) { ctx.moveTo(0, -6); ctx.lineTo(6, 0); ctx.lineTo(0, 6); ctx.lineTo(-6, 0); ctx.closePath(); } else { ctx.arc(0, 0, 5.5, 0, Math.PI * 2); }
                  ctx.fill(); ctx.stroke(); ctx.fillStyle = markerAccessible ? '#ffffff' : '#0f172a'; ctx.font = 'bold 7px Inter, system-ui'; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText(label || '!', 0, .5); ctx.restore();
                }
                var comparisonPreviousCautions = (previousTechniqueAttempt.actionLog || []).filter(function (entry) { return entry && entry.tone === 'caution'; }).concat(previousTechniqueAttempt.cautionLog || []);
                var comparisonCurrentCautions = (currentProcedure.actionLog || []).filter(function (entry) { return entry && entry.tone === 'caution'; }).concat(currentProcedure.cautionLog || []);
                function comparisonMarkerProgress(entry, markerIndex, total, path) {
                  if (entry && entry.point && path && path.length > 1) {
                    var nearestIndex = 0, nearestDistance = Infinity;
                    path.forEach(function (pathPoint, pathIndex) {
                      var distance = procedurePointDistance(entry.point, pathPoint);
                      if (distance < nearestDistance) { nearestDistance = distance; nearestIndex = pathIndex; }
                    });
                    return nearestIndex / Math.max(1, path.length - 1);
                  }
                  return (markerIndex + 1) / Math.max(1, total + 1);
                }
                comparisonPreviousCautions.slice(-4).forEach(function (entry, markerIndex) {
                  if (comparisonMarkerProgress(entry, markerIndex, comparisonPreviousCautions.length, comparisonPreviousPath) <= compareCanvasProgress + 0.015) {
                    drawComparisonMarker(entry.point || (comparisonPreviousPath.length ? comparisonPreviousPath[Math.floor((comparisonPreviousPath.length - 1) * (markerIndex + 1) / (comparisonPreviousCautions.length + 1))] : null), '!', '#f472b6', true);
                  }
                });
                comparisonCurrentCautions.slice(-4).forEach(function (entry, markerIndex) {
                  if (comparisonMarkerProgress(entry, markerIndex, comparisonCurrentCautions.length, comparisonCurrentPath) <= compareCanvasProgress + 0.015) {
                    drawComparisonMarker(entry.point || (comparisonCurrentPath.length ? comparisonCurrentPath[Math.floor((comparisonCurrentPath.length - 1) * (markerIndex + 1) / (comparisonCurrentCautions.length + 1))] : null), '!', '#f59e0b', false);
                  }
                });
                ctx.save();
                ctx.font = 'bold 9px Inter, system-ui'; ctx.fillStyle = highContrastEnabled ? '#ffffff' : '#f8fafc';
                // Previous attempt \u00B7 dashed pressure trail remains the saved-path semantic.
                // ctx.fillText('Previous attempt \u00B7 dashed pressure trail' ...legacy canvas wording...);
                var comparisonLegend = 'COMPARE ' + Math.round(compareCanvasProgress * 100) + '%  ·  current solid cyan  ·  saved dashed magenta';
                var comparisonLegendWidth = Math.min(W - 24, Math.max(192, Math.ceil(ctx.measureText(comparisonLegend).width) + 16));
                var comparisonLegendX = 12, comparisonLegendY = Math.max(10, H - 35);
                ctx.fillStyle = highContrastEnabled ? '#000000' : 'rgba(15,23,42,0.88)';
                ctx.beginPath(); if (ctx.roundRect) ctx.roundRect(comparisonLegendX, comparisonLegendY, comparisonLegendWidth, 22, 5); else ctx.rect(comparisonLegendX, comparisonLegendY, comparisonLegendWidth, 22); ctx.fill();
                ctx.strokeStyle = highContrastEnabled ? '#ffffff' : '#67e8f9'; ctx.lineWidth = 1; ctx.stroke(); ctx.fillStyle = highContrastEnabled ? '#ffffff' : '#f8fafc'; ctx.fillText(comparisonLegend.slice(0, 60), comparisonLegendX + 8, comparisonLegendY + 14); ctx.restore();
              }
              if (canvasProcedure.incisionStarted) drawProcedureOpening(canvasProcedure.incisionPath, false);
              if (canvasProcedure.incisionExtended) drawProcedureOpening(canvasProcedure.extensionPath, true);
              var activeOpeningPath = canvasProcedure.extensionPath || canvasProcedure.incisionPath || overlayGuide;
              if (instrumentVisuals && !d.quizMode && !d.annotateMode && !d.rulerMode) {
                drawProcedureHandoffCue(nextProcedureInfo(), activeOpeningPath);
              }
              if (liveInstrument === 'forceps' && canvasProcedure.incisionExtended && !canvasProcedure.retracted && canvas._toolPointer) {
                drawForcepsTractionPreview(activeOpeningPath, canvas._toolPointer);
              }
              if (liveInstrument === 'pin' && canvasProcedure.retracted && (canvasProcedure.pins || []).length < 2 && canvas._toolPointer) {
                drawPinStabilityPreview(activeOpeningPath, canvas._toolPointer, canvasProcedure.pins || []);
              }
              if (canvasProcedure.retracted) {
                drawTissueFlaps(activeOpeningPath, canvasProcedure.pins || [], canvasProcedure.forcepsPoint);
                if (canvasProcedure.forcepsPoint) {
                  var liftedX = canvasProcedure.forcepsPoint.x * W, liftedY = canvasProcedure.forcepsPoint.y * H;
                  ctx.save(); ctx.strokeStyle = '#5eead4'; ctx.fillStyle = 'rgba(15,23,42,0.88)'; ctx.lineWidth = 1.4;
                  ctx.setLineDash([4, 3]); ctx.beginPath(); ctx.arc(liftedX, liftedY, 12, Math.PI * 0.12, Math.PI * 0.88); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.moveTo(liftedX - 7, liftedY + 1); ctx.quadraticCurveTo(liftedX, liftedY - 10, liftedX + 7, liftedY + 1); ctx.stroke();
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(Math.min(W - 62, liftedX + 14), Math.max(7, liftedY - 17), 54, 17, 5); ctx.fill(); }
                  else ctx.fillRect(Math.min(W - 62, liftedX + 14), Math.max(7, liftedY - 17), 54, 17);
                  ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = '#99f6e4'; ctx.fillText('LIFTED', Math.min(W - 56, liftedX + 20), Math.max(19, liftedY - 5));
                  ctx.restore();
                }
              }
              if (liveInstrument === 'probe' && (canvasProcedure.pins || []).length >= 2 && canvas._toolPointer) {
                var liveProbeContact = specimenContactContext(canvas._toolPointer);
                // Legacy call: drawProbePalpationPreview(canvas._toolPointer, liveProbeContact.organ)
                drawProbePalpationPreview(canvas._toolPointer, canvas._probeDrag && canvas._probeDrag.active ? canvas._probeDrag.target : liveProbeContact.organ, canvas);
              }
              if (liveInstrument === 'dropper' && canvas._toolPointer) {
                // Legacy call: drawDropperSpreadPreview(canvas._toolPointer)
                drawDropperSpreadPreview(canvas._toolPointer, canvas);
              }
              if (liveInstrument === 'wick' && canvas._toolPointer) { drawWickRecoveryPreview(canvas._toolPointer, canvas); }
              if (canvasProcedure.surfaceCleared && canvasProcedure.dropperPoint) {
                // Organism-specific hydration visualization maps saline spread to surface anatomy and gravity.
                var dropX = canvasProcedure.dropperPoint.x * W, dropY = canvasProcedure.dropperPoint.y * H;
                var hydrationProfile = specimenHydrationProfile();
                var visibleSalineDrops = Math.max(1, Math.min(4, Number(liveTissueState.salineDrops) || 1));
                var hydrationUndoScale = liveTissueTransitionAction === 'undo-dropper' ? 1 - liveTissueTransitionProgress : 1;
                var hydrationAge = liveTissueState.lastAction === 'dropper' ? Math.max(0, Date.now() - liveTissueState.lastUpdatedAt) : 2000;
                var hydrationSpread = dissMotionReduced ? 1 : Math.max(0.18, Math.min(1, hydrationAge / 880));
                var hydrationRipple = !dissMotionReduced && hydrationAge < 1450 ? hydrationAge / 1450 : 1;
                var hydrationPooling = visibleSalineDrops > 2;
                var hydrationAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var hydrationRadius = (17 + visibleSalineDrops * 5) * hydrationSpread * (0.16 + hydrationUndoScale * 0.84);
                var hydrationDriftX = hydrationProfile.flowX * hydrationRadius * hydrationSpread;
                var hydrationDriftY = hydrationProfile.flowY * hydrationRadius * hydrationSpread;
                var hydrationFilmX = dropX + hydrationDriftX, hydrationFilmY = dropY + hydrationDriftY;
                var hydrationMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                ctx.save(); ctx.beginPath(); ctx.ellipse(hydrationMaterial.cx * W, hydrationMaterial.cy * H, hydrationMaterial.rx * W, hydrationMaterial.ry * H, 0, 0, Math.PI * 2); ctx.clip();
                ctx.save(); ctx.translate(hydrationFilmX, hydrationFilmY); ctx.scale(hydrationProfile.aspectX, hydrationProfile.aspectY);
                if (!hydrationAccessible) {
                  var hydrationGradient = ctx.createRadialGradient(-hydrationRadius * 0.22, -hydrationRadius * 0.28, 1, 0, 0, hydrationRadius);
                  hydrationGradient.addColorStop(0, 'rgba(240,249,255,0.66)');
                  hydrationGradient.addColorStop(0.42, 'rgba(125,211,252,' + (hydrationPooling ? '0.22' : '0.15') + ')');
                  hydrationGradient.addColorStop(1, 'rgba(14,165,233,0.025)');
                  ctx.fillStyle = hydrationGradient; ctx.beginPath(); ctx.arc(0, 0, hydrationRadius, 0, Math.PI * 2); ctx.fill();
                }
                ctx.strokeStyle = hydrationPooling ? (hydrationAccessible ? '#facc15' : 'rgba(251,191,36,0.88)') : (hydrationAccessible ? '#ffffff' : 'rgba(186,230,253,0.74)');
                ctx.lineWidth = hydrationAccessible ? 2.4 / hydrationProfile.aspectX : 1.15 / hydrationProfile.aspectX;
                ctx.beginPath(); ctx.arc(0, 0, hydrationRadius, 0, Math.PI * 2); ctx.stroke();
                if (hydrationProfile.pattern === 'radial') {
                  for (var hydrationRing = 1; hydrationRing <= 3; hydrationRing++) {
                    ctx.setLineDash(hydrationRing === 2 ? [3, 2] : []); ctx.beginPath(); ctx.arc(0, 0, hydrationRadius * hydrationRing / 4, 0, Math.PI * 2); ctx.stroke();
                  }
                  ctx.setLineDash([]);
                } else if (hydrationProfile.pattern === 'segments') {
                  for (var hydrationSegment = -2; hydrationSegment <= 2; hydrationSegment++) {
                    var hydrationSegmentY = hydrationSegment * hydrationRadius * 0.27;
                    ctx.beginPath(); ctx.moveTo(-hydrationRadius * 0.66, hydrationSegmentY); ctx.quadraticCurveTo(0, hydrationSegmentY + (hydrationSegment % 2 ? 2 : -2), hydrationRadius * 0.66, hydrationSegmentY); ctx.stroke();
                  }
                } else if (hydrationProfile.pattern === 'fibers') {
                  for (var hydrationFiber = -2; hydrationFiber <= 2; hydrationFiber++) {
                    ctx.beginPath(); ctx.moveTo(-hydrationRadius * 0.62, hydrationRadius * (0.42 + hydrationFiber * 0.08));
                    ctx.bezierCurveTo(-hydrationRadius * 0.28, -hydrationRadius * 0.30, hydrationRadius * 0.20, hydrationRadius * 0.24, hydrationRadius * 0.62, -hydrationRadius * (0.30 + hydrationFiber * 0.05)); ctx.stroke();
                  }
                } else if (hydrationProfile.pattern === 'channels') {
                  for (var hydrationChannel = -2; hydrationChannel <= 2; hydrationChannel++) {
                    var hydrationChannelY = hydrationChannel * hydrationRadius * 0.19;
                    ctx.beginPath(); ctx.moveTo(-hydrationRadius * 0.70, hydrationChannelY - 2); ctx.quadraticCurveTo(0, hydrationChannelY + 3, hydrationRadius * 0.72, hydrationChannelY); ctx.stroke();
                  }
                } else if (hydrationProfile.pattern === 'beads') {
                  for (var hydrationBead = 0; hydrationBead < visibleSalineDrops + 3; hydrationBead++) {
                    var hydrationBeadAngle = hydrationBead * 2.18;
                    var hydrationBeadRadius = hydrationRadius * (0.30 + (hydrationBead % 3) * 0.18);
                    ctx.beginPath(); ctx.ellipse(Math.cos(hydrationBeadAngle) * hydrationBeadRadius, Math.sin(hydrationBeadAngle) * hydrationBeadRadius * 0.72, 2.2 + hydrationBead % 2, 1.5 + hydrationBead % 2, hydrationBeadAngle, 0, Math.PI * 2); ctx.stroke();
                  }
                } else {
                  for (var hydrationFilmLine = -2; hydrationFilmLine <= 2; hydrationFilmLine++) {
                    ctx.beginPath(); ctx.arc(0, hydrationFilmLine * 2, hydrationRadius * (0.38 + Math.abs(hydrationFilmLine) * 0.11), -1.08, -0.14); ctx.stroke();
                  }
                }
                if (hydrationRipple < 1) {
                  ctx.globalAlpha = Math.max(0, 0.72 * (1 - hydrationRipple)); ctx.setLineDash([4, 3]);
                  for (var hydrationWave = 0; hydrationWave < 2; hydrationWave++) {
                    ctx.beginPath(); ctx.arc(-hydrationDriftX, -hydrationDriftY, 8 + (hydrationRipple + hydrationWave * 0.22) * hydrationRadius * 1.18, 0, Math.PI * 2); ctx.stroke();
                  }
                  ctx.setLineDash([]);
                }
                ctx.restore(); ctx.restore();
                var salineFieldLabel = hydrationPooling ? 'POOLING · REDUCE DOSE' : ({ eye: 'OPTICAL FILM RESTORED', worm: 'CUTICLE HYDRATED', fish: 'SCALE FILM RESTORED', crayfish: 'CARAPACE FILM RESTORED', heart: 'MYOCARDIAL FILM RESTORED', pig: 'TISSUE FILM RESTORED', frog: 'SKIN FILM RESTORED' }[spec.bodyShape] || 'SURFACE HYDRATED');
                ctx.save(); ctx.font = 'bold 7px Inter, system-ui';
                var salineLabelWidth = Math.min(W - 18, Math.max(94, ctx.measureText(salineFieldLabel).width + 12));
                var clearLabelX = Math.max(8, Math.min(W - salineLabelWidth - 8, hydrationFilmX + 18)), clearLabelY = Math.max(7, hydrationFilmY - 27);
                ctx.fillStyle = hydrationAccessible ? 'rgba(0,0,0,0.94)' : 'rgba(8,47,73,0.90)';
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(clearLabelX, clearLabelY, salineLabelWidth, 18, 5); ctx.fill(); }
                else ctx.fillRect(clearLabelX, clearLabelY, salineLabelWidth, 18);
                ctx.strokeStyle = hydrationPooling ? '#facc15' : '#7dd3fc'; ctx.lineWidth = hydrationAccessible ? 1.8 : 0.8; ctx.stroke();
                ctx.fillStyle = hydrationPooling ? '#fef08a' : '#e0f2fe'; ctx.fillText(salineFieldLabel, clearLabelX + 6, clearLabelY + 12);
                ctx.restore();
              }
              if (canvasProcedure.fieldWicked && canvasProcedure.wickPoint) {
                // A cross-hatched footprint records where excess fluid was removed without implying tissue damage.
                var wickedX = canvasProcedure.wickPoint.x * W, wickedY = canvasProcedure.wickPoint.y * H;
                var wickedAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var wickedUndoScale = liveTissueTransitionAction === 'undo-wick' ? 1 - liveTissueTransitionProgress : 1;
                ctx.save(); ctx.translate(wickedX, wickedY); ctx.scale(0.18 + wickedUndoScale * 0.82, 0.18 + wickedUndoScale * 0.82);
                ctx.strokeStyle = wickedAccessible ? '#ffffff' : '#99f6e4';
                ctx.lineWidth = wickedAccessible ? 2.2 : 1.1;
                ctx.setLineDash([5, 3]); ctx.beginPath(); ctx.ellipse(0, 0, 24, 13, 0, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                for (var wickedFiber = -12; wickedFiber <= 12; wickedFiber += 6) {
                  ctx.beginPath(); ctx.moveTo(wickedFiber - 4, -6); ctx.lineTo(wickedFiber + 4, 6); ctx.stroke();
                }
                ctx.font = 'bold 7px Inter, system-ui'; ctx.fillStyle = wickedAccessible ? '#ffffff' : '#ccfbf1';
                ctx.fillText('WICKED · BALANCED FILM', -42, -18);
                ctx.restore();
              }
              // Probe confirmation remains anchored to the traced structure as persistent visual evidence.
              if (canvasProcedure.probed && canvasProcedure.probedOrganId) {
                var probedStructure = organs.find(function (organ) { return organ.id === canvasProcedure.probedOrganId; });
                if (probedStructure) {
                  var probedPoint = variedOrganPoint(probedStructure);
                  var probedX = probedPoint.x * W, probedY = probedPoint.y * H;
                  var probeConfirmPulse = dissMotionReduced ? 0 : (Math.sin(dissTick * 0.05) + 1) * 1.5;
                  var probeUndoScale = liveTissueTransitionAction === 'undo-probe' ? 1 - liveTissueTransitionProgress : 1;
                  ctx.save(); ctx.translate(probedX, probedY); ctx.scale(0.22 + probeUndoScale * 0.78, 0.22 + probeUndoScale * 0.78); ctx.translate(-probedX, -probedY);
                  ctx.shadowColor = 'rgba(45,212,191,0.72)'; ctx.shadowBlur = 9;
                  ctx.strokeStyle = (d.visualRealism || visualRealism) === 'accessible' ? '#facc15' : '#2dd4bf';
                  ctx.lineWidth = (d.visualRealism || visualRealism) === 'accessible' ? 3 : 2;
                  for (var probeArcIdx = 0; probeArcIdx < 4; probeArcIdx++) {
                    var probeArcStart = probeArcIdx * Math.PI / 2 + 0.12;
                    ctx.beginPath(); ctx.arc(probedX, probedY, 13 + probeConfirmPulse, probeArcStart, probeArcStart + 0.86); ctx.stroke();
                  }
                  ctx.shadowBlur = 0;
                  ctx.beginPath(); ctx.moveTo(probedX - 5, probedY); ctx.lineTo(probedX - 1, probedY + 4); ctx.lineTo(probedX + 7, probedY - 6);
                  ctx.strokeStyle = (d.visualRealism || visualRealism) === 'accessible' ? '#fef08a' : '#99f6e4'; ctx.lineWidth = 2.4; ctx.stroke();
                  var probeLabel = 'Trace: ' + probedStructure.name;
                  ctx.font = 'bold 9px Inter, system-ui';
                  var probeLabelWidth = ctx.measureText(probeLabel).width + 12;
                  var probeLabelX = Math.min(W - probeLabelWidth - 8, probedX + 17);
                  var probeLabelY = Math.max(8, probedY - 25);
                  ctx.fillStyle = 'rgba(15,23,42,0.90)';
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(probeLabelX, probeLabelY, probeLabelWidth, 19, 5); ctx.fill(); }
                  else ctx.fillRect(probeLabelX, probeLabelY, probeLabelWidth, 19);
                  ctx.strokeStyle = '#2dd4bf'; ctx.lineWidth = 0.8; ctx.stroke();
                  ctx.fillStyle = '#ccfbf1'; ctx.fillText(probeLabel, probeLabelX + 6, probeLabelY + 13);
                  ctx.restore();
                }
              }
              // Committed pins read as angled metal anchors with insertion collars, cast shadows, and shape-coded heads.
              function drawCommittedRetractionPin(pin, pinIdx) {
                var pinX = pin.x * W, pinY = pin.y * H;
                var openingCenter = activeOpeningPath[Math.floor((activeOpeningPath.length - 1) / 2)] || overlayGuide[1];
                var pinDX = pin.x - openingCenter.x, pinDY = pin.y - openingCenter.y;
                var pinDistance = Math.sqrt(pinDX * pinDX + pinDY * pinDY) || 1;
                var pinOutX = pinDX / pinDistance, pinOutY = pinDY / pinDistance;
                var committedCalibration = normalizeToolCalibration(canvasProcedure.toolCalibration || toolCalibration);
                var committedPinAngle = Math.max(35, Math.min(90, Number(committedCalibration.pinAngle) || 65));
                var pinProjection = 7 + (90 - committedPinAngle) / 55 * 14;
                var transitioningPin = pinIdx === (canvasProcedure.pins || []).length - 1;
                var pinInsertionProgress = liveTissueTransitionAction === 'pin' && transitioningPin
                  ? liveTissueTransitionProgress
                  : (liveTissueTransitionAction === 'undo-pin' && transitioningPin ? 1 - liveTissueTransitionProgress : 1);
                pinProjection *= 0.18 + pinInsertionProgress * 0.82;
                var pinHeadX = pinX + pinOutX * pinProjection, pinHeadY = pinY + pinOutY * pinProjection;
                var pinAccessible = highContrastEnabled || (d.visualRealism || visualRealism) === 'accessible';
                var pinLightVector = {
                  overhead: { x: 0, y: -1 }, left: { x: -1, y: -0.18 },
                  right: { x: 1, y: -0.18 }, raking: { x: -0.74, y: 0.68 }
                }[lightDirection] || { x: 0, y: -1 };
                var pinLightStrength = Math.max(0.2, Math.min(1, (Number(d.lightIntensity || lightIntensity) || 68) / 100));
                var pinShadowOffset = 2.5 + pinLightStrength * 3.5;
                var pinHeadRadius = (pinAccessible ? 7.5 : 6.3) * (0.72 + pinInsertionProgress * 0.28);
                ctx.save(); ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                // Tension tether terminates at the insertion point rather than passing through the pin.
                ctx.setLineDash([3, 3]); ctx.strokeStyle = pinAccessible ? '#facc15' : 'rgba(226,232,240,0.48)'; ctx.lineWidth = pinAccessible ? 1.8 : 1;
                ctx.beginPath(); ctx.moveTo(openingCenter.x * W, openingCenter.y * H); ctx.lineTo(pinX, pinY); ctx.stroke(); ctx.setLineDash([]);
                // The elevated shaft casts away from the active lab light.
                ctx.strokeStyle = pinAccessible ? '#020617' : 'rgba(2,6,23,0.42)'; ctx.lineWidth = pinAccessible ? 4.8 : 4;
                ctx.beginPath(); ctx.moveTo(pinX - pinLightVector.x * pinShadowOffset, pinY - pinLightVector.y * pinShadowOffset);
                ctx.lineTo(pinHeadX - pinLightVector.x * pinShadowOffset, pinHeadY - pinLightVector.y * pinShadowOffset); ctx.stroke();
                ctx.strokeStyle = pinAccessible ? '#f8fafc' : '#94a3b8'; ctx.lineWidth = pinAccessible ? 3.2 : 2.6;
                ctx.beginPath(); ctx.moveTo(pinX, pinY); ctx.lineTo(pinHeadX, pinHeadY); ctx.stroke();
                ctx.strokeStyle = pinAccessible ? '#facc15' : 'rgba(248,250,252,0.82)'; ctx.lineWidth = 0.9;
                ctx.beginPath(); ctx.moveTo(pinX + pinOutY, pinY - pinOutX); ctx.lineTo(pinHeadX + pinOutY, pinHeadY - pinOutX); ctx.stroke();
                // Concentric collar and depth notch make the insertion site legible without relying on pin color.
                ctx.fillStyle = pinAccessible ? '#020617' : 'rgba(51,65,85,0.88)';
                ctx.strokeStyle = pinAccessible ? '#f8fafc' : 'rgba(226,232,240,0.84)'; ctx.lineWidth = pinAccessible ? 2 : 1.2;
                ctx.beginPath(); ctx.arc(pinX, pinY, 4.2, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                ctx.beginPath(); ctx.arc(pinX, pinY, 7.2, -0.95, 0.95); ctx.stroke();
                var pinHeadGradient = ctx.createRadialGradient(
                  pinHeadX + pinLightVector.x * pinHeadRadius * 0.42, pinHeadY + pinLightVector.y * pinHeadRadius * 0.42, 0.8,
                  pinHeadX, pinHeadY, pinHeadRadius
                );
                pinHeadGradient.addColorStop(0, '#ffffff');
                pinHeadGradient.addColorStop(0.34, pinIdx % 2 ? '#7dd3fc' : '#f9a8d4');
                pinHeadGradient.addColorStop(1, pinIdx % 2 ? '#0369a1' : '#be185d');
                ctx.fillStyle = pinHeadGradient; ctx.strokeStyle = pinAccessible ? '#f8fafc' : 'rgba(255,255,255,0.82)'; ctx.lineWidth = pinAccessible ? 2.1 : 1.15;
                ctx.beginPath();
                if (pinIdx % 2) {
                  ctx.moveTo(pinHeadX, pinHeadY - pinHeadRadius); ctx.lineTo(pinHeadX + pinHeadRadius, pinHeadY);
                  ctx.lineTo(pinHeadX, pinHeadY + pinHeadRadius); ctx.lineTo(pinHeadX - pinHeadRadius, pinHeadY); ctx.closePath();
                } else {
                  ctx.arc(pinHeadX, pinHeadY, pinHeadRadius, 0, Math.PI * 2);
                }
                ctx.fill(); ctx.stroke();
                ctx.restore();
              }
              (canvasProcedure.pins || []).forEach(drawCommittedRetractionPin);
              var pinSettleVisible = liveTissueTransitionAction === 'pin' ? liveTissueTransitionProgress >= 0.72 : (liveTissueTransitionAction === 'undo-pin' ? liveTissueTransitionProgress < 0.36 : true);
              if ((canvasProcedure.pins || []).length >= 2 && pinSettleVisible) {
                var stablePinA = canvasProcedure.pins[0], stablePinB = canvasProcedure.pins[1];
                var stablePinAX = stablePinA.x * W, stablePinAY = stablePinA.y * H;
                var stablePinBX = stablePinB.x * W, stablePinBY = stablePinB.y * H;
                var stablePinMidX = (stablePinAX + stablePinBX) / 2, stablePinMidY = (stablePinAY + stablePinBY) / 2;
                ctx.save(); ctx.setLineDash([4, 3]); ctx.strokeStyle = 'rgba(125,211,252,0.72)'; ctx.lineWidth = 1.2;
                ctx.beginPath(); ctx.moveTo(stablePinAX, stablePinAY); ctx.lineTo(stablePinBX, stablePinBY); ctx.stroke(); ctx.setLineDash([]);
                ctx.fillStyle = 'rgba(15,23,42,0.88)';
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(stablePinMidX - 34, stablePinMidY - 10, 68, 18, 5); ctx.fill(); }
                else ctx.fillRect(stablePinMidX - 34, stablePinMidY - 10, 68, 18);
                ctx.font = 'bold 7px Inter, system-ui'; ctx.fillStyle = '#bae6fd'; ctx.fillText('STABLE ANCHOR', stablePinMidX - 28, stablePinMidY + 2);
                ctx.restore();
              }
              if (canvas._toolStroke && canvas._toolStroke.length > 1) drawProcedurePath(canvas._toolStroke, '#fef08a', 3, true);
              if (canvas._toolResistance && canvas._toolPointer) {
                var resistance = canvas._toolResistance, resistancePoint = canvas._toolPointer;
                var resistanceColor = resistance.level === 'high' ? '#fb7185' : (resistance.level === 'moderate' ? '#fbbf24' : '#34d399');
                ctx.save();
                ctx.beginPath(); ctx.arc(resistancePoint.x * W, resistancePoint.y * H, 13 + resistance.value * 8, 0, Math.PI * 2);
                ctx.strokeStyle = resistanceColor; ctx.lineWidth = 2.5; ctx.stroke();
                ctx.fillStyle = 'rgba(15,23,42,0.86)';
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(resistancePoint.x * W + 18, resistancePoint.y * H - 14, 84, 21, 6); ctx.fill(); }
                else ctx.fillRect(resistancePoint.x * W + 18, resistancePoint.y * H - 14, 84, 21);
                ctx.font = 'bold 9px Inter, system-ui'; ctx.fillStyle = resistanceColor;
                ctx.fillText('Resistance: ' + resistance.level, resistancePoint.x * W + 23, resistancePoint.y * H);
                ctx.restore();
              }

              // Ruler tool overlay

              if (!d.quizMode && d.rulerMode && d.rulerStart && d.rulerEnd) {

                ctx.strokeStyle = '#fbbf24'; ctx.lineWidth = 1.5; ctx.setLineDash([]);

                ctx.beginPath(); ctx.moveTo(d.rulerStart.x * W, d.rulerStart.y * H);

                ctx.lineTo(d.rulerEnd.x * W, d.rulerEnd.y * H); ctx.stroke();

                // Endpoints

                ctx.beginPath(); ctx.arc(d.rulerStart.x * W, d.rulerStart.y * H, 3, 0, Math.PI * 2); ctx.fillStyle = '#fbbf24'; ctx.fill();

                ctx.beginPath(); ctx.arc(d.rulerEnd.x * W, d.rulerEnd.y * H, 3, 0, Math.PI * 2); ctx.fill();

                // Distance

                var rdx = d.rulerEnd.x - d.rulerStart.x;

                var rdy = d.rulerEnd.y - d.rulerStart.y;

                var rDist = Math.sqrt(rdx * rdx + rdy * rdy);

                var rCm = (rDist * (spec.bodyShape === 'worm' ? 15 : spec.bodyShape === 'pig' ? 25 : spec.bodyShape === 'fish' ? 20 : spec.bodyShape === 'crayfish' ? 12 : spec.bodyShape === 'frog' ? 8 : 3)).toFixed(1);

                ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = '#fbbf24';

                ctx.fillText(rCm + ' cm', (d.rulerStart.x + d.rulerEnd.x) / 2 * W + 5, (d.rulerStart.y + d.rulerEnd.y) / 2 * H - 5);

              }

              // Scale bar

              ctx.fillStyle = 'rgba(255,255,255,0.25)';

              ctx.fillRect(W - 80, H - 18, 60, 2);

              ctx.font = '8px Inter, system-ui'; ctx.fillText('Scale Bar', W - 72, H - 7);

              // Endocrine system overlay

              if (!d.quizMode && d.showEndocrine) {

                ctx.globalAlpha = 0.6; ctx.font = '6px Inter, system-ui';

                var glands = [];

                if (spec.bodyShape === 'frog') {

                  glands = [

                    { name: 'pituitary', x: cx, y: cy - H * 0.24, hormone: 'GH, TSH, FSH', color: '#ec4899' },

                    { name: 'thyroid', x: cx - W * 0.03, y: cy - H * 0.18, hormone: 'T3, T4 (metabolism)', color: '#f472b6' },

                    { name: 'parathyroid', x: cx + W * 0.03, y: cy - H * 0.17, hormone: 'PTH (calcium)', color: '#fb7185' },

                    { name: 'adrenals', x: cx + W * 0.06, y: cy + H * 0.07, hormone: 'cortisol, adrenaline', color: '#fbbf24' },

                    { name: 'pancreas (islets)', x: cx + W * 0.04, y: cy - H * 0.02, hormone: 'insulin, glucagon', color: '#34d399' },

                    { name: 'gonads', x: cx, y: cy + H * 0.12, hormone: 'estrogen/testosterone', color: '#a78bfa' }

                  ];

                } else if (spec.bodyShape === 'pig') {

                  glands = [

                    { name: 'pituitary', x: cx - W * 0.26, y: cy - H * 0.06, hormone: 'master gland', color: '#ec4899' },

                    { name: 'thyroid', x: cx - W * 0.16, y: cy - H * 0.06, hormone: 'T3, T4', color: '#f472b6' },

                    { name: 'thymus', x: cx - W * 0.08, y: cy - H * 0.08, hormone: 'thymosin (immunity)', color: '#fbbf24' },

                    { name: 'adrenals', x: cx + W * 0.09, y: cy + H * 0.065, hormone: 'cortisol', color: '#fbbf24' },

                    { name: 'pancreas', x: cx + W * 0.02, y: cy + H * 0.04, hormone: 'insulin', color: '#34d399' }

                  ];

                }

                glands.forEach(function (gl) {

                  // Gland marker (pulsing circle)

                  var glPulse = 1 + Math.sin(dissTick * 0.04) * 0.2;

                  ctx.beginPath(); ctx.arc(gl.x, gl.y, 5 * glPulse, 0, Math.PI * 2);

                  var glGrad = ctx.createRadialGradient(gl.x, gl.y, 0, gl.x, gl.y, 5 * glPulse);

                  glGrad.addColorStop(0, gl.color); glGrad.addColorStop(1, gl.color.slice(0, -1) + ',0)');

                  ctx.fillStyle = glGrad; ctx.fill();

                  // Hormone arrows radiating out

                  ctx.strokeStyle = gl.color; ctx.lineWidth = 0.5;

                  for (var ha = 0; ha < 4; ha++) {

                    var hAngle = ha * Math.PI / 2 + dissTick * 0.02;

                    var hLen = 8 + Math.sin(dissTick * 0.05 + ha) * 3;

                    ctx.beginPath(); ctx.moveTo(gl.x + Math.cos(hAngle) * 6, gl.y + Math.sin(hAngle) * 6);

                    ctx.lineTo(gl.x + Math.cos(hAngle) * hLen, gl.y + Math.sin(hAngle) * hLen);

                    ctx.stroke();

                  }

                  // Labels

                  ctx.fillStyle = gl.color;

                  ctx.fillText(gl.name, gl.x + 8, gl.y - 3);

                  ctx.fillStyle = 'rgba(255,255,255,0.35)';

                  ctx.fillText(gl.hormone, gl.x + 8, gl.y + 5);

                });

                ctx.globalAlpha = 1;

              }

              // Nervous system tracing overlay

              if (!d.quizMode && d.traceNervous) {

                ctx.setLineDash([3, 5]);

                ctx.lineDashOffset = -dissTick * 0.5;

                ctx.lineWidth = 1.5; ctx.globalAlpha = 0.7;

                if (spec.bodyShape === 'frog') {

                  ctx.strokeStyle = '#a855f7';

                  // Brain → spinal cord

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.25); // brain

                  ctx.lineTo(cx, cy + H * 0.10); // spinal cord

                  ctx.stroke();

                  // Cranial nerves radiating from brain

                  for (var cn = 0; cn < 5; cn++) {

                    var cnAngle = -1.2 + cn * 0.5;

                    ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.24);

                    ctx.lineTo(cx + Math.cos(cnAngle) * W * 0.08, cy - H * 0.24 + Math.sin(cnAngle) * H * 0.06);

                    ctx.stroke();

                  }

                  // Sciatic nerves to legs

                  [-1, 1].forEach(function (s) {

                    ctx.beginPath(); ctx.moveTo(cx, cy + H * 0.08);

                    ctx.quadraticCurveTo(cx + s * W * 0.06, cy + H * 0.18, cx + s * W * 0.10, cy + H * 0.35);

                    ctx.stroke();

                  });

                  // Brachial plexus to arms

                  [-1, 1].forEach(function (s) {

                    ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.12);

                    ctx.quadraticCurveTo(cx + s * W * 0.08, cy - H * 0.08, cx + s * W * 0.14, cy + H * 0.05);

                    ctx.stroke();

                  });

                  // Signal pulse animation

                  var sigT = (dissTick * 0.01) % 1;

                  var sigY = cy - H * 0.25 + sigT * H * 0.35;

                  ctx.beginPath(); ctx.arc(cx, sigY, 3, 0, Math.PI * 2);

                  ctx.fillStyle = '#e879f9'; ctx.fill();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#a855f7';

                  ctx.fillText('Brain', cx + W * 0.03, cy - H * 0.26);

                  ctx.fillText('Spinal Cord', cx + W * 0.02, cy);

                  ctx.fillText('Sciatic', cx + W * 0.08, cy + H * 0.25);

                  ctx.fillText('Brachial', cx + W * 0.06, cy - H * 0.10);

                } else if (spec.bodyShape === 'worm') {

                  ctx.strokeStyle = '#a855f7';

                  // Ventral nerve cord

                  ctx.beginPath(); ctx.moveTo(cx, H * 0.07); ctx.lineTo(cx, H * 0.93); ctx.stroke();

                  // Segmental ganglia

                  for (var sg = 0; sg < 20; sg++) {

                    var sgY = H * 0.10 + sg * H * 0.04;

                    ctx.beginPath(); ctx.arc(cx, sgY, 2, 0, Math.PI * 2);

                    ctx.fillStyle = '#c084fc'; ctx.fill();

                    // Lateral nerves

                    ctx.beginPath(); ctx.moveTo(cx, sgY);

                    ctx.lineTo(cx - W * 0.04, sgY); ctx.stroke();

                    ctx.beginPath(); ctx.moveTo(cx, sgY);

                    ctx.lineTo(cx + W * 0.04, sgY); ctx.stroke();

                  }

                  var wSigT = (dissTick * 0.008) % 1;

                  ctx.beginPath(); ctx.arc(cx, H * 0.07 + wSigT * H * 0.86, 3, 0, Math.PI * 2);

                  ctx.fillStyle = '#e879f9'; ctx.fill();

                  ctx.font = '6px Inter, system-ui'; ctx.fillStyle = '#a855f7';

                  ctx.fillText('Ventral Nerve Cord', cx + W * 0.05, H * 0.50);

                  ctx.fillText('Segmental Ganglia', cx + W * 0.05, H * 0.52);

                } else if (spec.bodyShape === 'crayfish') {

                  ctx.strokeStyle = '#a855f7';

                  // Brain → ventral cord

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.20, cy); // brain

                  ctx.lineTo(cx + W * 0.25, cy); // ventral cord

                  ctx.stroke();

                  // Ganglia

                  for (var cg = 0; cg < 6; cg++) {

                    ctx.beginPath(); ctx.arc(cx - W * 0.15 + cg * W * 0.08, cy, 2, 0, Math.PI * 2);

                    ctx.fillStyle = '#c084fc'; ctx.fill();

                  }

                  ctx.font = '6px Inter, system-ui'; ctx.fillStyle = '#a855f7';

                  ctx.fillText('Brain', cx - W * 0.22, cy - H * 0.03);

                  ctx.fillText('Ventral Cord', cx + W * 0.05, cy - H * 0.03);

                }

                ctx.setLineDash([]); ctx.lineDashOffset = 0;

                ctx.globalAlpha = 1;

              }

              // One shared playback clock keeps all functional pathways synchronized and pausable.
              var activeCanvasTraceKey = d.traceCirculation ? 'circulation' : (d.traceDigestion ? 'digestion' : (d.traceRespiration ? 'respiration' : (d.traceExcretory ? 'excretory' : null)));
              var systemPlaybackPaused = !!d.systemPlaybackPaused;
              var systemReplayToken = Number(d.systemTraceReplayToken) || 0;
              if (canvas._systemTraceReplayToken !== systemReplayToken) {
                canvas._systemTraceReplayToken = systemReplayToken;
                canvas._systemTraceStartTick = dissTick;
                canvas._systemTraceFrozenTick = null;
              }
              if (systemPlaybackPaused && canvas._systemTraceFrozenTick == null) canvas._systemTraceFrozenTick = dissTick;
              if (!systemPlaybackPaused) canvas._systemTraceFrozenTick = null;
              var systemTraceBaseTick = systemPlaybackPaused ? (canvas._systemTraceFrozenTick == null ? dissTick : canvas._systemTraceFrozenTick) : dissTick;
              var systemTraceTick = Math.max(0, systemTraceBaseTick - (Number(canvas._systemTraceStartTick) || 0));
              // Excretory flow tracing overlay

              if (!d.quizMode && d.traceExcretory && activeLayer === 'organs') {

                ctx.setLineDash([4, 4]);

                ctx.lineDashOffset = -systemTraceTick * 0.4;

                ctx.lineWidth = 2; ctx.globalAlpha = 0.65;

                if (spec.bodyShape === 'frog') {

                  ctx.strokeStyle = '#84cc16';

                  // Kidneys → ureters → bladder → cloaca

                  [-1, 1].forEach(function (s) {

                    ctx.beginPath();

                    ctx.moveTo(cx + s * W * 0.06, cy + H * 0.08); // kidney

                    ctx.quadraticCurveTo(cx + s * W * 0.03, cy + H * 0.12, cx, cy + H * 0.15); // ureter → bladder

                    ctx.stroke();

                  });

                  ctx.beginPath(); ctx.moveTo(cx, cy + H * 0.15); // bladder

                  ctx.lineTo(cx, cy + H * 0.22); // cloaca

                  ctx.stroke();

                  // Filtrate animation

                  var filtT = (systemTraceTick * 0.006) % 1;

                  ctx.beginPath(); ctx.arc(cx + Math.cos(filtT * 6) * W * 0.03, cy + H * 0.08 + filtT * H * 0.14, 2, 0, Math.PI * 2);

                  ctx.fillStyle = '#a3e635'; ctx.fill();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#84cc16';

                  ctx.fillText('Kidneys', cx + W * 0.08, cy + H * 0.09);

                  ctx.fillText('Ureters', cx + W * 0.04, cy + H * 0.13);

                  ctx.fillText('Bladder', cx + W * 0.03, cy + H * 0.16);

                  ctx.fillText('Cloaca', cx + W * 0.02, cy + H * 0.23);

                } else if (spec.bodyShape === 'worm') {

                  ctx.strokeStyle = '#84cc16';

                  // Nephridia along body

                  for (var neph = 0; neph < 8; neph++) {

                    var nephY = H * 0.22 + neph * H * 0.08;

                    ctx.beginPath();

                    ctx.moveTo(cx - W * 0.04, nephY);

                    ctx.quadraticCurveTo(cx - W * 0.05, nephY + H * 0.02, cx - W * 0.045, nephY + H * 0.04);

                    ctx.stroke();

                    // Nephridiopore

                    ctx.beginPath(); ctx.arc(cx - W * 0.045, nephY + H * 0.04, 1.5, 0, Math.PI * 2);

                    ctx.fillStyle = '#84cc16'; ctx.fill();

                  }

                  ctx.font = '6px Inter, system-ui'; ctx.fillStyle = '#84cc16';

                  ctx.fillText('Nephridia', cx - W * 0.09, H * 0.35);

                  ctx.fillText('Nephridiopores', cx - W * 0.10, H * 0.37);

                } else if (spec.bodyShape === 'pig') {

                  ctx.strokeStyle = '#84cc16';

                  ctx.beginPath();

                  ctx.moveTo(cx + W * 0.08, cy + H * 0.08); // kidney

                  ctx.quadraticCurveTo(cx + W * 0.06, cy + H * 0.12, cx + W * 0.04, cy + H * 0.14); // ureter

                  ctx.lineTo(cx, cy + H * 0.16); // bladder

                  ctx.stroke();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#84cc16';

                  ctx.fillText('Kidney', cx + W * 0.09, cy + H * 0.07);

                  ctx.fillText('Ureter', cx + W * 0.07, cy + H * 0.13);

                  ctx.fillText('Bladder', cx + W * 0.01, cy + H * 0.18);

                }

                ctx.setLineDash([]); ctx.lineDashOffset = 0;

                ctx.globalAlpha = 1;

              }

              // Circulatory flow tracing overlay

              if (!d.quizMode && d.traceCirculation && activeLayer === 'organs') {

                ctx.setLineDash([5, 4]);

                ctx.lineWidth = 2; ctx.globalAlpha = 0.6;

                if (spec.bodyShape === 'frog') {

                  // Heart → Arteries (red, oxygenated)

                  ctx.lineDashOffset = -systemTraceTick * 0.6;

                  ctx.strokeStyle = '#ef4444';

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.12); // heart

                  ctx.quadraticCurveTo(cx - W * 0.08, cy - H * 0.18, cx - W * 0.04, cy - H * 0.24); // carotid → head

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.12);

                  ctx.quadraticCurveTo(cx + W * 0.05, cy - H * 0.05, cx + W * 0.03, cy + H * 0.06); // systemic → body

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx + W * 0.03, cy + H * 0.06);

                  ctx.lineTo(cx + W * 0.12, cy + H * 0.30); // to legs

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx + W * 0.03, cy + H * 0.06);

                  ctx.lineTo(cx - W * 0.12, cy + H * 0.30);

                  ctx.stroke();

                  // Veins (blue, deoxygenated) → back to heart

                  ctx.lineDashOffset = systemTraceTick * 0.6;

                  ctx.strokeStyle = '#3b82f6';

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.12, cy + H * 0.32);

                  ctx.quadraticCurveTo(cx - W * 0.06, cy + H * 0.15, cx - W * 0.02, cy - H * 0.10);

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx + W * 0.12, cy + H * 0.32);

                  ctx.quadraticCurveTo(cx + W * 0.06, cy + H * 0.15, cx + W * 0.02, cy - H * 0.10);

                  ctx.stroke();

                  // Pulmonary loop

                  ctx.strokeStyle = '#a855f7';

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.12);

                  ctx.quadraticCurveTo(cx - W * 0.10, cy - H * 0.10, cx - W * 0.08, cy - H * 0.06);

                  ctx.stroke();

                  // Labels

                  ctx.font = '7px Inter, system-ui';

                  ctx.fillStyle = '#ef4444'; ctx.fillText('Arteries (O₂)', cx - W * 0.12, cy - H * 0.20);

                  ctx.fillStyle = '#3b82f6'; ctx.fillText('Veins (CO₂)', cx + W * 0.06, cy + H * 0.20);

                  ctx.fillStyle = '#a855f7'; ctx.fillText('Pulmonary', cx - W * 0.14, cy - H * 0.08);

                  // Blood cell animation

                  var bcT = (systemTraceTick * 0.008) % 1;

                  ctx.beginPath(); ctx.arc(cx + bcT * W * 0.15, cy - H * 0.12 + (bcT * H * 0.42), 3, 0, Math.PI * 2);

                  ctx.fillStyle = '#ef4444'; ctx.fill();

                } else if (spec.bodyShape === 'heart') {

                  // Through chambers

                  ctx.lineDashOffset = -systemTraceTick * 0.5;

                  ctx.strokeStyle = '#3b82f6'; // deoxygenated

                  ctx.beginPath(); ctx.moveTo(cx + W * 0.15, cy - H * 0.25); // SVC

                  ctx.lineTo(cx + W * 0.10, cy - H * 0.10); // RA

                  ctx.lineTo(cx + W * 0.08, cy + H * 0.05); // RV

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx + W * 0.08, cy + H * 0.05);

                  ctx.quadraticCurveTo(cx + W * 0.15, cy - H * 0.15, cx + W * 0.20, cy - H * 0.25); // pulmonary artery

                  ctx.stroke();

                  ctx.strokeStyle = '#ef4444'; // oxygenated

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.15, cy - H * 0.20); // pulmonary vein

                  ctx.lineTo(cx - W * 0.10, cy - H * 0.10); // LA

                  ctx.lineTo(cx - W * 0.08, cy + H * 0.05); // LV

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.08, cy + H * 0.05);

                  ctx.quadraticCurveTo(cx - W * 0.15, cy, cx, cy - H * 0.28); // aorta

                  ctx.stroke();

                  ctx.font = '7px Inter, system-ui';

                  ctx.fillStyle = '#3b82f6'; ctx.fillText('SVC', cx + W * 0.16, cy - H * 0.26);

                  ctx.fillText('Pulm. Artery', cx + W * 0.16, cy - H * 0.16);

                  ctx.fillStyle = '#ef4444'; ctx.fillText('Pulm. Vein', cx - W * 0.20, cy - H * 0.22);

                  ctx.fillText('Aorta', cx - W * 0.04, cy - H * 0.30);

                }

                ctx.setLineDash([]); ctx.lineDashOffset = 0;

                ctx.globalAlpha = 1;

              }

              // Respiratory flow tracing overlay

              if (!d.quizMode && d.traceRespiration && activeLayer === 'organs') {

                ctx.setLineDash([4, 5]);

                ctx.lineDashOffset = -systemTraceTick * 0.7;

                ctx.lineWidth = 2; ctx.globalAlpha = 0.7;

                if (spec.bodyShape === 'frog') {

                  ctx.strokeStyle = '#38bdf8';

                  // Air path: nares → glottis → lungs

                  ctx.beginPath();

                  ctx.moveTo(cx, cy - H * 0.28); // nostrils

                  ctx.lineTo(cx, cy - H * 0.20); // pharynx

                  ctx.lineTo(cx, cy - H * 0.15); // glottis

                  ctx.stroke();

                  // Left lung

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.15);

                  ctx.quadraticCurveTo(cx - W * 0.06, cy - H * 0.12, cx - W * 0.08, cy - H * 0.06);

                  ctx.stroke();

                  // Right lung

                  ctx.beginPath(); ctx.moveTo(cx, cy - H * 0.15);

                  ctx.quadraticCurveTo(cx + W * 0.06, cy - H * 0.12, cx + W * 0.08, cy - H * 0.06);

                  ctx.stroke();

                  // Air particles animation

                  for (var ap = 0; ap < 5; ap++) {

                    var apT = ((systemTraceTick * 0.01 + ap * 0.2) % 1);

                    var apY = cy - H * 0.28 + apT * H * 0.22;

                    ctx.beginPath(); ctx.arc(cx + Math.sin(apT * 8) * W * 0.01, apY, 2, 0, Math.PI * 2);

                    ctx.fillStyle = '#38bdf8'; ctx.fill();

                  }

                  // O2/CO2 labels

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#38bdf8';

                  ctx.fillText('O₂ In', cx + W * 0.02, cy - H * 0.26);

                  ctx.fillStyle = '#ef4444';

                  ctx.fillText('CO₂ Out', cx + W * 0.02, cy - H * 0.24);

                  // Cutaneous respiration note

                  ctx.fillStyle = 'rgba(56,189,248,0.4)'; ctx.font = '6px Inter, system-ui';

                  ctx.fillText('Cutaneous', cx + W * 0.10, cy + H * 0.05);

                } else if (spec.bodyShape === 'pig') {

                  ctx.strokeStyle = '#38bdf8';

                  ctx.beginPath();

                  ctx.moveTo(cx - W * 0.32, cy - H * 0.01); // nostrils

                  ctx.lineTo(cx - W * 0.20, cy - H * 0.02); // trachea

                  ctx.stroke();

                  // Bronchi split

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.20, cy - H * 0.02);

                  ctx.quadraticCurveTo(cx - W * 0.12, cy - H * 0.06, cx - W * 0.06, cy - H * 0.08);

                  ctx.stroke();

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.20, cy - H * 0.02);

                  ctx.quadraticCurveTo(cx - W * 0.12, cy + H * 0.02, cx - W * 0.06, cy + H * 0.02);

                  ctx.stroke();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#38bdf8';

                  ctx.fillText('Trachea', cx - W * 0.22, cy - H * 0.05);

                  ctx.fillText('L. Bronchus', cx - W * 0.10, cy - H * 0.10);

                  ctx.fillText('R. Bronchus', cx - W * 0.10, cy + H * 0.05);

                } else if (spec.bodyShape === 'fish') {

                  ctx.strokeStyle = '#38bdf8';

                  // Water flow through gills

                  ctx.beginPath(); ctx.moveTo(cx - W * 0.35, cy); // mouth intake

                  ctx.lineTo(cx - W * 0.22, cy); // through pharynx

                  ctx.stroke();

                  // Through gills and out operculum

                  for (var gf = 0; gf < 3; gf++) {

                    ctx.beginPath(); ctx.moveTo(cx - W * 0.22, cy - H * 0.02 + gf * H * 0.02);

                    ctx.lineTo(cx - W * 0.28, cy - H * 0.03 + gf * H * 0.02);

                    ctx.stroke();

                  }

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#38bdf8';

                  ctx.fillText('Water In', cx - W * 0.38, cy - H * 0.02);

                  ctx.fillText('O₂ Exchange', cx - W * 0.26, cy - H * 0.06);

                  ctx.fillText('Water Out', cx - W * 0.30, cy + H * 0.06);

                }

                ctx.setLineDash([]); ctx.lineDashOffset = 0;

                ctx.globalAlpha = 1;

              }

              // Digestive tract tracing overlay

              if (!d.quizMode && d.traceDigestion && activeLayer === 'organs') {

                ctx.setLineDash([6, 4]);

                ctx.lineDashOffset = -systemTraceTick * 0.6;

                ctx.lineWidth = 2.5; ctx.globalAlpha = 0.7;

                if (spec.bodyShape === 'frog') {

                  ctx.strokeStyle = '#f59e0b';

                  ctx.beginPath();

                  ctx.moveTo(cx, cy - H * 0.26); // mouth

                  ctx.lineTo(cx, cy - H * 0.18); // esophagus

                  ctx.quadraticCurveTo(cx + W * 0.02, cy - H * 0.12, cx + W * 0.04, cy - H * 0.05); // stomach

                  ctx.quadraticCurveTo(cx + W * 0.06, cy + H * 0.02, cx + W * 0.03, cy + H * 0.05); // duodenum

                  ctx.quadraticCurveTo(cx - W * 0.02, cy + H * 0.10, cx, cy + H * 0.14); // intestines

                  ctx.quadraticCurveTo(cx + W * 0.03, cy + H * 0.18, cx, cy + H * 0.22); // large intestine

                  ctx.stroke();

                  // Food bolus moving along path

                  var foodT = (systemTraceTick * 0.005) % 1;

                  var foodY = cy - H * 0.26 + foodT * H * 0.48;

                  ctx.beginPath(); ctx.arc(cx + Math.sin(foodT * 10) * W * 0.02, foodY, 4, 0, Math.PI * 2);

                  ctx.fillStyle = '#92400e'; ctx.fill();

                  ctx.strokeStyle = '#f59e0b'; ctx.lineWidth = 1; ctx.stroke();

                  // Labels

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#f59e0b';

                  ctx.fillText('Mouth', cx + W * 0.02, cy - H * 0.27);

                  ctx.fillText('Esophagus', cx + W * 0.04, cy - H * 0.17);

                  ctx.fillText('Stomach', cx + W * 0.07, cy - H * 0.06);

                  ctx.fillText('Sm. Intestine', cx + W * 0.06, cy + H * 0.08);

                  ctx.fillText('Lg. Intestine', cx + W * 0.04, cy + H * 0.18);

                  ctx.fillText('Cloaca', cx + W * 0.02, cy + H * 0.23);

                } else if (spec.bodyShape === 'worm') {

                  ctx.strokeStyle = '#f59e0b';

                  ctx.beginPath();

                  ctx.moveTo(cx, H * 0.06); // mouth

                  ctx.lineTo(cx, H * 0.10); // pharynx

                  ctx.lineTo(cx, H * 0.28); // crop

                  ctx.lineTo(cx, H * 0.33); // gizzard

                  ctx.lineTo(cx, H * 0.88); // intestine

                  ctx.lineTo(cx, H * 0.94); // anus

                  ctx.stroke();

                  var wFoodT = (systemTraceTick * 0.003) % 1;

                  var wFoodY = H * 0.06 + wFoodT * H * 0.88;

                  ctx.beginPath(); ctx.arc(cx, wFoodY, 3, 0, Math.PI * 2);

                  ctx.fillStyle = '#92400e'; ctx.fill();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#f59e0b';

                  ctx.fillText('Mouth', cx + W * 0.05, H * 0.065);

                  ctx.fillText('Pharynx', cx + W * 0.05, H * 0.11);

                  ctx.fillText('Crop', cx + W * 0.05, H * 0.29);

                  ctx.fillText('Gizzard', cx + W * 0.05, H * 0.34);

                  ctx.fillText('Intestine', cx + W * 0.05, H * 0.60);

                  ctx.fillText('Anus', cx + W * 0.05, H * 0.945);

                } else if (spec.bodyShape === 'pig') {

                  ctx.strokeStyle = '#f59e0b';

                  ctx.beginPath();

                  ctx.moveTo(cx - W * 0.30, cy); // mouth

                  ctx.lineTo(cx - W * 0.20, cy); // esophagus

                  ctx.quadraticCurveTo(cx - W * 0.05, cy - H * 0.02, cx, cy + H * 0.02); // stomach

                  ctx.quadraticCurveTo(cx + W * 0.08, cy + H * 0.06, cx + W * 0.10, cy + H * 0.10); // intestines

                  ctx.quadraticCurveTo(cx + W * 0.12, cy + H * 0.14, cx + W * 0.16, cy + H * 0.12); // rectum

                  ctx.stroke();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#f59e0b';

                  ctx.fillText('Mouth', cx - W * 0.33, cy - H * 0.02);

                  ctx.fillText('Esophagus', cx - W * 0.18, cy - H * 0.03);

                  ctx.fillText('Stomach', cx + W * 0.02, cy - H * 0.01);

                  ctx.fillText('Intestines', cx + W * 0.09, cy + H * 0.13);

                } else if (spec.bodyShape === 'fish') {

                  ctx.strokeStyle = '#f59e0b';

                  ctx.beginPath();

                  ctx.moveTo(cx - W * 0.32, cy); // mouth

                  ctx.lineTo(cx - W * 0.18, cy); // pharynx

                  ctx.quadraticCurveTo(cx - W * 0.08, cy + H * 0.02, cx, cy + H * 0.03); // stomach

                  ctx.lineTo(cx + W * 0.10, cy + H * 0.02); // pyloric caeca

                  ctx.lineTo(cx + W * 0.20, cy + H * 0.04); // intestine

                  ctx.stroke();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#f59e0b';

                  ctx.fillText('Mouth', cx - W * 0.35, cy - H * 0.02);

                  ctx.fillText('Stomach', cx - W * 0.04, cy + H * 0.06);

                  ctx.fillText('Intestine', cx + W * 0.12, cy + H * 0.07);

                }

                ctx.setLineDash([]); ctx.lineDashOffset = 0;

                ctx.globalAlpha = 1;

              }

              // Cross-section indicators

              if (activeLayer === 'organs' && (spec.bodyShape === 'frog' || spec.bodyShape === 'pig' || spec.bodyShape === 'fish')) {

                ctx.globalAlpha = 0.15; ctx.strokeStyle = '#94a3b8'; ctx.lineWidth = 0.5; ctx.setLineDash([4, 6]);

                // Transverse section lines

                var sectionYs = spec.bodyShape === 'frog' ? [cy - H * 0.12, cy, cy + H * 0.10] :

                  spec.bodyShape === 'pig' ? [cy - H * 0.06, cy + H * 0.04] :

                    [cy - H * 0.04, cy + H * 0.04];

                sectionYs.forEach(function (sy, si) {

                  ctx.beginPath(); ctx.moveTo(10, sy); ctx.lineTo(W - 10, sy); ctx.stroke();

                  ctx.font = '7px Inter, system-ui'; ctx.fillStyle = 'rgba(148,163,184,0.3)';

                  ctx.fillText('Section' + ' ' + String.fromCharCode(65 + si), 12, sy - 3);

                });

                ctx.setLineDash([]); ctx.globalAlpha = 1;

              }

              // System color legend (top-right)

              var legendSys = ['circulatory', 'digestive', 'respiratory', 'nervous', 'skeletal', 'muscular', 'excretory', 'reproductive'];

              var sysColors = { circulatory: '#ef4444', digestive: '#f59e0b', respiratory: '#3b82f6', nervous: '#8b5cf6', skeletal: '#e2e8f0', muscular: '#f87171', excretory: '#a78bfa', reproductive: '#ec4899' };

              var legendLabels = ['Circulatory', 'Digestive', 'Respiratory', 'Nervous', 'Skeletal', 'Muscular', 'Excretory', 'Reproductive'];

              ctx.font = '7px Inter, system-ui';

              for (var li = 0; li < legendSys.length; li++) {

                var lx = W - 58, ly = 50 + li * 12;

                ctx.beginPath(); ctx.arc(lx, ly, 3, 0, Math.PI * 2);

                ctx.fillStyle = sysColors[legendSys[li]] || '#94a3b8'; ctx.fill();

                ctx.fillStyle = 'rgba(255,255,255,0.4)';

                ctx.fillText(legendLabels[li], lx + 6, ly + 3);

              }

              // Progress bar at top

              if (totalOrgansInSpecimen > 0) {

                var barW = W - 28;

                ctx.fillStyle = 'rgba(30,41,59,0.6)';

                ctx.fillRect(14, 28, barW, 4);

                ctx.fillStyle = progressPct >= 100 ? '#22c55e' : '#3b82f6';

                ctx.fillRect(14, 28, barW * (progressPct / 100), 4);

                ctx.font = '9px Inter, system-ui, sans-serif';

                ctx.fillStyle = 'rgba(255,255,255,0.35)';

                fillReadableSpecimenText(exploredCount + '/' + totalOrgansInSpecimen + ' viewed \u00B7 observation ' + progressPct + '%', specimenScale.x < 0 ? W - 14 / specimenTextScaleX : 14 / specimenTextScaleX, 42 / specimenTextScaleY);

              }

              // Hover tooltip

              var hovOrg = d.hoveredOrgan ? organs.find(function (o) { return o.id === d.hoveredOrgan; }) : null;

              if (hovOrg && !d.quizMode && d.selectedOrgan !== hovOrg.id) {

                var hoverPoint = variedOrganPoint(hovOrg);
                var hpx = hoverPoint.x * W, hpy = hoverPoint.y * H;

                var hoverDepthLabel = anatomicalDepthLabel(structureOpticalDepth(hovOrg));
                var hoverExposure = structureExposureState(hovOrg, currentProcedure);
                var hoverMorphology = structureInspectionFootprint(hovOrg).morphologyLabel.toLowerCase();
                var hText = hovOrg.name + ' \u00B7 ' + hoverDepthLabel + ' \u00B7 ' + hoverExposure + ' \u00B7 ' + hoverMorphology + ': ' + hovOrg.fn.split('.')[0] + '.';

                ctx.font = '10px Inter, system-ui, sans-serif';

                var hLines = [];

                var words = hText.split(' ');

                var line = '';

                for (var wi = 0; wi < words.length; wi++) {

                  var testLine = line + words[wi] + ' ';

                  if (ctx.measureText(testLine).width > 180 && line) { hLines.push(line.trim()); line = words[wi] + ' '; }

                  else { line = testLine; }

                }

                if (line.trim()) hLines.push(line.trim());

                if (hLines.length > 3) hLines = hLines.slice(0, 3);

                var hBoxH = hLines.length * 14 + 10;

                var hBoxW = 196;

                var hbx = Math.min(hpx + 20, W - hBoxW - 10);

                var hby = Math.max(hpy - hBoxH - 10, 50);

                ctx.fillStyle = 'rgba(15,23,42,0.92)';

                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(hbx, hby, hBoxW, hBoxH, 6); ctx.fill(); } else { ctx.fillRect(hbx, hby, hBoxW, hBoxH); }

                ctx.strokeStyle = 'rgba(59,130,246,0.4)'; ctx.lineWidth = 1; ctx.stroke();

                ctx.fillStyle = '#e2e8f0';

                for (var hi = 0; hi < hLines.length; hi++) {

                  ctx.fillText(hLines[hi], hbx + 8, hby + 16 + hi * 14);

                }

              }

              // Guided walkthrough prompt

              function drawGuidedSpecimenPrompt() {
              if (guidedMode && currentGuided) {


                // Highlight guided organ with arrow

                var gOrg = organs.find(function (o) { return o.id === currentGuided.organId; });

                if (gOrg) {

                  var guidedPoint = variedOrganPoint(gOrg);
                  var gx = guidedPoint.x * W, gy = guidedPoint.y * H;

                  ctx.beginPath(); ctx.arc(gx, gy, 16 + Math.sin(dissTick * 0.08) * 3, 0, Math.PI * 2);

                  ctx.strokeStyle = 'rgba(147,51,234,0.7)'; ctx.lineWidth = 2.5; ctx.setLineDash([4, 3]); ctx.stroke(); ctx.setLineDash([]);

                }

              }
              }



              ctx.restore(); // End deterministic specimen-proportion transform

              // Advanced view, condition, and cross-section overlays stay screen-aligned.
              ctx.save();
              var liveAnatomicalView = d.anatomicalView || 'dorsal';
              var liveCondition = d.specimenCondition || 'standard';
              var conditionTint = { preserved: '148,163,184', dehydrated: '180,126,84', cloudy: '226,232,240', swollen: '251,146,160' }[liveCondition];
              if (conditionTint) {
                ctx.globalCompositeOperation = liveCondition === 'cloudy' ? 'screen' : 'soft-light';
                ctx.fillStyle = 'rgba(' + conditionTint + ',' + (liveCondition === 'cloudy' ? '0.16' : '0.18') + ')';
                ctx.beginPath(); ctx.ellipse(cx, cy, W * (liveCondition === 'swollen' ? 0.39 : 0.35), H * (liveCondition === 'swollen' ? 0.36 : 0.32), 0, 0, Math.PI * 2); ctx.fill();
                ctx.globalCompositeOperation = 'source-over';
                if (liveCondition === 'dehydrated') {
                  ctx.strokeStyle = 'rgba(120,53,15,0.34)'; ctx.lineWidth = 1;
                  for (var crackIdx = 0; crackIdx < 7; crackIdx++) {
                    var crackX = W * (0.31 + crackIdx * 0.065);
                    ctx.beginPath(); ctx.moveTo(crackX, H * 0.35); ctx.lineTo(crackX - 5, H * 0.46); ctx.lineTo(crackX + 3, H * 0.58); ctx.stroke();
                  }
                }
              }
              ctx.fillStyle = 'rgba(15,23,42,0.82)';
              if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(14, 50, 118, 24, 7); ctx.fill(); } else ctx.fillRect(14, 50, 118, 24);
              ctx.font = 'bold 9px Inter, system-ui'; ctx.fillStyle = '#e0f2fe'; ctx.fillText('View: ' + liveAnatomicalView, 23, 66);
              if (d.crossSectionMode) {
                var sectionX = W - 122, sectionY = 52, sectionW = 106, sectionH = 18;
                ctx.fillStyle = 'rgba(15,23,42,0.9)';
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(sectionX - 6, sectionY - 19, sectionW + 12, spec.layers.length * sectionH + 28, 8); ctx.fill(); }
                else ctx.fillRect(sectionX - 6, sectionY - 19, sectionW + 12, spec.layers.length * sectionH + 28);
                ctx.font = 'bold 9px Inter, system-ui'; ctx.fillStyle = '#f8fafc'; ctx.fillText('Layer cross-section', sectionX, sectionY - 5);
                spec.layers.forEach(function (layer, layerIdx) {
                  var isCurrentSection = layer.id === activeLayer;
                  ctx.fillStyle = layer.color || (isCurrentSection ? '#f59e0b' : '#64748b');
                  ctx.globalAlpha = isCurrentSection ? 1 : 0.62;
                  ctx.fillRect(sectionX, sectionY + layerIdx * sectionH, sectionW, sectionH - 2);
                  ctx.globalAlpha = 1; ctx.font = (isCurrentSection ? 'bold ' : '') + '8px Inter, system-ui'; ctx.fillStyle = isCurrentSection ? '#0f172a' : '#f8fafc';
                  ctx.fillText((layerIdx + 1) + '. ' + layer.name, sectionX + 5, sectionY + 12 + layerIdx * sectionH);
                });
              }
              ctx.restore();

              // Specimen-specific material, moisture, and adjustable lab-light pass.
              var liveVisualMode = d.visualRealism || 'guided';
              var liveLight = d.labLight || 'neutral';
              var liveLightIntensity = Math.max(20, Math.min(100, Number(d.lightIntensity) || 68));
              var liveLightStrength = liveLightIntensity / 100;
              var liveIllumination = illuminationAssessmentData(liveProcedureState, liveLightIntensity);
              var liveMaterialProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
              var materialCX = liveMaterialProfile.cx * W + parallaxX, materialCY = liveMaterialProfile.cy * H + parallaxY;
              var materialRX = liveMaterialProfile.rx * W, materialRY = liveMaterialProfile.ry * H;
              var liveLightDirection = d.lightDirection || 'overhead';
              var surfaceLightVector = {
                overhead: { x: -0.12, y: -1 },
                left: { x: -1, y: -0.14 },
                right: { x: 1, y: -0.14 },
                raking: { x: -0.72, y: 0.70 }
              }[liveLightDirection] || { x: -0.12, y: -1 };
              var surfaceLightMagnitude = Math.sqrt(surfaceLightVector.x * surfaceLightVector.x + surfaceLightVector.y * surfaceLightVector.y) || 1;
              surfaceLightVector.x /= surfaceLightMagnitude; surfaceLightVector.y /= surfaceLightMagnitude;
              var materialCondition = d.specimenCondition || specimenCondition;
              var materialMoistureRatio = Math.max(0, Math.min(1, liveTissueState.moisture / 100));
              var materialRoughness = materialCondition === 'dehydrated' ? 0.88 : (materialCondition === 'preserved' ? 0.62 : (materialCondition === 'cloudy' ? 0.70 : (materialCondition === 'swollen' ? 0.34 : 0.42)));
              materialRoughness = Math.min(0.96, materialRoughness + liveTissueState.trauma / 420);
              var surfaceMaterialLayer = currentLayerIdx === 0;
              if (liveVisualMode !== 'accessible' && sceneDetail && surfaceMaterialLayer) {
                ctx.save();
                ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX, materialRY, 0, 0, Math.PI * 2); ctx.clip();
                ctx.globalCompositeOperation = 'soft-light';
                var materialDensity = liveVisualMode === 'realistic' ? liveMaterialProfile.density : Math.round(liveMaterialProfile.density * 0.58);
                if (liveMaterialProfile.pattern === 'chromatophores') {
                  for (var chromatophoreIdx = 0; chromatophoreIdx < materialDensity; chromatophoreIdx++) {
                    var chromAngle = specimenVariationValue('chrom-angle-' + chromatophoreIdx) * Math.PI * 2;
                    var chromRadius = Math.sqrt(specimenVariationValue('chrom-radius-' + chromatophoreIdx));
                    var chromX = materialCX + Math.cos(chromAngle) * materialRX * chromRadius;
                    var chromY = materialCY + Math.sin(chromAngle) * materialRY * chromRadius;
                    var chromSize = 1.2 + specimenVariationValue('chrom-size-' + chromatophoreIdx) * 4;
                    ctx.beginPath(); ctx.arc(chromX, chromY, chromSize, 0, Math.PI * 2);
                    ctx.fillStyle = chromatophoreIdx % 3 === 0 ? 'rgba(37,99,70,0.34)' : (chromatophoreIdx % 3 === 1 ? 'rgba(120,53,15,0.26)' : 'rgba(253,230,138,0.18)'); ctx.fill();
                  }
                } else if (liveMaterialProfile.pattern === 'segments') {
                  ctx.strokeStyle = 'rgba(226,232,240,0.28)'; ctx.lineWidth = 1;
                  for (var segmentIdx = 0; segmentIdx < materialDensity; segmentIdx++) {
                    var segmentT = (segmentIdx + 1) / (materialDensity + 1);
                    var segmentY = materialCY - materialRY + segmentT * materialRY * 2;
                    var segmentHalf = materialRX * Math.sqrt(Math.max(0, 1 - Math.pow((segmentY - materialCY) / materialRY, 2)));
                    ctx.beginPath(); ctx.moveTo(materialCX - segmentHalf, segmentY); ctx.quadraticCurveTo(materialCX, segmentY + Math.sin(segmentIdx) * 2, materialCX + segmentHalf, segmentY); ctx.stroke();
                  }
                  var cuticleGlow = ctx.createLinearGradient(materialCX - materialRX, materialCY, materialCX + materialRX, materialCY);
                  cuticleGlow.addColorStop(0, 'rgba(59,130,246,0.05)'); cuticleGlow.addColorStop(0.5, 'rgba(255,255,255,0.22)'); cuticleGlow.addColorStop(1, 'rgba(236,72,153,0.08)');
                  ctx.fillStyle = cuticleGlow; ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                } else if (liveMaterialProfile.pattern === 'follicles') {
                  for (var follicleIdx = 0; follicleIdx < materialDensity; follicleIdx++) {
                    var follicleAngle = specimenVariationValue('follicle-angle-' + follicleIdx) * Math.PI * 2;
                    var follicleRadius = Math.sqrt(specimenVariationValue('follicle-radius-' + follicleIdx));
                    var follicleX = materialCX + Math.cos(follicleAngle) * materialRX * follicleRadius;
                    var follicleY = materialCY + Math.sin(follicleAngle) * materialRY * follicleRadius;
                    ctx.fillStyle = 'rgba(71,85,105,0.24)'; ctx.beginPath(); ctx.arc(follicleX, follicleY, 1.1, 0, Math.PI * 2); ctx.fill();
                    if (liveVisualMode === 'realistic' && follicleIdx % 3 === 0) {
                      ctx.strokeStyle = 'rgba(241,245,249,0.18)'; ctx.lineWidth = 0.7; ctx.beginPath(); ctx.moveTo(follicleX, follicleY); ctx.lineTo(follicleX + 3, follicleY - 5); ctx.stroke();
                    }
                  }
                } else if (liveMaterialProfile.pattern === 'scales') {
                  ctx.strokeStyle = 'rgba(224,242,254,0.3)'; ctx.lineWidth = liveVisualMode === 'realistic' ? 1.1 : 0.8;
                  var scaleRows = liveVisualMode === 'realistic' ? 9 : 6, scaleCols = liveVisualMode === 'realistic' ? 15 : 10;
                  for (var scaleRow = 0; scaleRow < scaleRows; scaleRow++) {
                    for (var scaleCol = 0; scaleCol < scaleCols; scaleCol++) {
                      var scaleX = materialCX - materialRX + (scaleCol + 0.5 + (scaleRow % 2) * 0.5) * (materialRX * 2 / scaleCols);
                      var scaleY = materialCY - materialRY + (scaleRow + 0.7) * (materialRY * 2 / scaleRows);
                      ctx.beginPath(); ctx.ellipse(scaleX, scaleY, materialRX / scaleCols * 0.82, materialRY / scaleRows * 0.62, 0, 0.12, Math.PI - 0.12); ctx.stroke();
                    }
                  }
                  var scaleSheen = ctx.createLinearGradient(materialCX - materialRX, materialCY - materialRY, materialCX + materialRX, materialCY + materialRY);
                  scaleSheen.addColorStop(0.15, 'rgba(14,165,233,0.04)'); scaleSheen.addColorStop(0.5, 'rgba(255,255,255,0.24)'); scaleSheen.addColorStop(0.76, 'rgba(168,85,247,0.08)');
                  ctx.fillStyle = scaleSheen; ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                } else if (liveMaterialProfile.pattern === 'facets') {
                  ctx.strokeStyle = 'rgba(226,232,240,0.22)'; ctx.lineWidth = 0.9;
                  for (var facetIdx = 0; facetIdx < materialDensity; facetIdx++) {
                    var facetAngle = specimenVariationValue('facet-angle-' + facetIdx) * Math.PI * 2;
                    var facetRadius = Math.sqrt(specimenVariationValue('facet-radius-' + facetIdx));
                    var facetX = materialCX + Math.cos(facetAngle) * materialRX * facetRadius;
                    var facetY = materialCY + Math.sin(facetAngle) * materialRY * facetRadius;
                    var facetSize = 3 + specimenVariationValue('facet-size-' + facetIdx) * 5;
                    ctx.beginPath();
                    for (var facetSide = 0; facetSide < 6; facetSide++) {
                      var facetSideAngle = facetSide / 6 * Math.PI * 2;
                      var facetPX = facetX + Math.cos(facetSideAngle) * facetSize, facetPY = facetY + Math.sin(facetSideAngle) * facetSize * 0.7;
                      if (!facetSide) ctx.moveTo(facetPX, facetPY); else ctx.lineTo(facetPX, facetPY);
                    }
                    ctx.closePath(); ctx.stroke();
                  }
                } else if (liveMaterialProfile.pattern === 'radial') {
                  ctx.strokeStyle = 'rgba(224,242,254,0.24)'; ctx.lineWidth = 1;
                  for (var radialRing = 1; radialRing <= 6; radialRing++) {
                    ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX * radialRing / 6, materialRY * radialRing / 6, 0, 0, Math.PI * 2); ctx.stroke();
                  }
                  for (var radialRay = 0; radialRay < 18; radialRay++) {
                    var radialAngle = radialRay / 18 * Math.PI * 2;
                    ctx.beginPath(); ctx.moveTo(materialCX + Math.cos(radialAngle) * materialRX * 0.2, materialCY + Math.sin(radialAngle) * materialRY * 0.2);
                    ctx.lineTo(materialCX + Math.cos(radialAngle) * materialRX, materialCY + Math.sin(radialAngle) * materialRY); ctx.stroke();
                  }
                  var cornealGlow = ctx.createRadialGradient(materialCX - materialRX * 0.28, materialCY - materialRY * 0.32, 2, materialCX, materialCY, materialRX);
                  cornealGlow.addColorStop(0, 'rgba(255,255,255,0.34)'); cornealGlow.addColorStop(0.35, 'rgba(186,230,253,0.10)'); cornealGlow.addColorStop(1, 'rgba(15,23,42,0.08)');
                  ctx.fillStyle = cornealGlow; ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                } else if (liveMaterialProfile.pattern === 'fibers') {
                  ctx.strokeStyle = 'rgba(254,202,202,0.26)'; ctx.lineWidth = 1.1;
                  for (var fiberLineIdx = 0; fiberLineIdx < materialDensity; fiberLineIdx++) {
                    var fiberT = (fiberLineIdx + 1) / (materialDensity + 1);
                    var fiberStartX = materialCX - materialRX + fiberT * materialRX * 1.35;
                    var fiberStartY = materialCY + materialRY;
                    ctx.beginPath(); ctx.moveTo(fiberStartX, fiberStartY);
                    ctx.bezierCurveTo(fiberStartX - 20, materialCY + materialRY * 0.32, fiberStartX + 32, materialCY - materialRY * 0.28, fiberStartX + materialRX * 0.6, materialCY - materialRY); ctx.stroke();
                  }
                }
                // Directional illumination turns the texture into surface relief; moisture sharpens sheen while dehydration breaks it up.
                ctx.globalCompositeOperation = 'screen';
                var materialSpecularStrength = liveLightStrength * (0.28 + materialMoistureRatio * 0.72) * (1 - materialRoughness * 0.58);
                var directionalSurfaceGradient = ctx.createLinearGradient(
                  materialCX + surfaceLightVector.x * materialRX, materialCY + surfaceLightVector.y * materialRY,
                  materialCX - surfaceLightVector.x * materialRX, materialCY - surfaceLightVector.y * materialRY
                );
                directionalSurfaceGradient.addColorStop(0, 'rgba(255,255,255,' + (0.025 + materialSpecularStrength * 0.14).toFixed(3) + ')');
                directionalSurfaceGradient.addColorStop(0.26, 'rgba(255,255,255,' + (materialSpecularStrength * 0.07).toFixed(3) + ')');
                directionalSurfaceGradient.addColorStop(0.64, 'rgba(186,230,253,0.008)');
                directionalSurfaceGradient.addColorStop(1, 'rgba(15,23,42,0)');
                ctx.fillStyle = directionalSurfaceGradient; ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                ctx.globalCompositeOperation = 'source-over';
                var materialRimAngle = Math.atan2(surfaceLightVector.y, surfaceLightVector.x);
                ctx.strokeStyle = 'rgba(255,255,255,' + (0.035 + materialSpecularStrength * 0.16).toFixed(3) + ')';
                ctx.lineWidth = materialCondition === 'swollen' ? 2.8 : 1.7;
                ctx.setLineDash(materialCondition === 'dehydrated' ? [3, 3] : []);
                ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX * 0.985, materialRY * 0.985, 0, materialRimAngle - 1.02, materialRimAngle + 1.02); ctx.stroke(); ctx.setLineDash([]);
                var materialSheenAlpha = (liveVisualMode === 'realistic' ? 0.17 : 0.10) * materialSpecularStrength;
                ctx.strokeStyle = 'rgba(255,255,255,' + materialSheenAlpha.toFixed(3) + ')'; ctx.lineWidth = materialCondition === 'swollen' ? 2.6 : 2;
                var sheenTangentX = -surfaceLightVector.y, sheenTangentY = surfaceLightVector.x;
                ctx.setLineDash(materialCondition === 'dehydrated' ? [3, 3] : []);
                for (var sheenIdx = 0; sheenIdx < 4; sheenIdx++) {
                  var sheenOffset = 0.08 + (sheenIdx - 1.5) * 0.14;
                  var sheenCenterX = materialCX + surfaceLightVector.x * materialRX * sheenOffset;
                  var sheenCenterY = materialCY + surfaceLightVector.y * materialRY * sheenOffset;
                  var sheenStartX = sheenCenterX - sheenTangentX * materialRX * 0.52;
                  var sheenStartY = sheenCenterY - sheenTangentY * materialRY * 0.52;
                  var sheenEndX = sheenCenterX + sheenTangentX * materialRX * 0.48;
                  var sheenEndY = sheenCenterY + sheenTangentY * materialRY * 0.48;
                  ctx.beginPath(); ctx.moveTo(sheenStartX, sheenStartY);
                  ctx.quadraticCurveTo(sheenCenterX + surfaceLightVector.x * 8, sheenCenterY + surfaceLightVector.y * 8, sheenEndX, sheenEndY); ctx.stroke();
                }
                ctx.setLineDash([]);                if ((liveMaterialProfile.pattern === 'chromatophores' || liveMaterialProfile.pattern === 'scales' || liveMaterialProfile.pattern === 'radial') && (d.specimenCondition || specimenCondition) !== 'dehydrated') {
                  for (var dropletIdx = 0; dropletIdx < Math.max(1, Math.round((liveVisualMode === 'realistic' ? 9 : 5) * liveTissueState.moisture / 100)); dropletIdx++) {
                    var dropletAngle = specimenVariationValue('droplet-angle-' + dropletIdx) * Math.PI * 2;
                    var dropletRadius = Math.sqrt(specimenVariationValue('droplet-radius-' + dropletIdx));
                    var dropletX = materialCX + Math.cos(dropletAngle) * materialRX * dropletRadius * 0.84;
                    var dropletY = materialCY + Math.sin(dropletAngle) * materialRY * dropletRadius * 0.84;
                    var dropletSize = 1.8 + specimenVariationValue('droplet-size-' + dropletIdx) * 3.2;
                    var dropletGrad = ctx.createRadialGradient(dropletX - 1, dropletY - 1, 0.5, dropletX, dropletY, dropletSize);
                    dropletGrad.addColorStop(0, 'rgba(255,255,255,0.6)'); dropletGrad.addColorStop(0.45, 'rgba(186,230,253,0.13)'); dropletGrad.addColorStop(1, 'rgba(15,23,42,0.08)');
                    ctx.fillStyle = dropletGrad; ctx.beginPath(); ctx.ellipse(dropletX, dropletY, dropletSize, dropletSize * 0.72, -0.4, 0, Math.PI * 2); ctx.fill();
                  }
                }
                ctx.restore();
              }

              // Tissue-state visualization makes accumulated moisture, clarity, tension, and trauma visible on the specimen.
              if (sceneDetail && liveTissueState) {
                if (surfaceMaterialLayer) {
                ctx.save();
                ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX, materialRY, 0, 0, Math.PI * 2); ctx.clip();
                if (liveTissueState.trauma > 3) {
                  ctx.globalCompositeOperation = 'multiply';
                  ctx.fillStyle = 'rgba(127,29,29,' + Math.min(0.24, liveTissueState.trauma / 420) + ')';
                  ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                }
                if (liveTissueState.clarity < 76) {
                  ctx.globalCompositeOperation = 'screen';
                  ctx.fillStyle = 'rgba(226,232,240,' + Math.min(0.22, (76 - liveTissueState.clarity) / 230) + ')';
                  ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                }
                if (liveTissueState.moisture < 58) {
                  ctx.globalCompositeOperation = 'source-over';
                  ctx.strokeStyle = 'rgba(120,53,15,' + Math.min(0.42, (58 - liveTissueState.moisture) / 110) + ')';
                  ctx.lineWidth = 0.7;
                  var drynessLines = Math.max(3, Math.round((58 - liveTissueState.moisture) / 5));
                  for (var drynessIdx = 0; drynessIdx < drynessLines; drynessIdx++) {
                    var drynessT = (drynessIdx + 1) / (drynessLines + 1);
                    var drynessY = materialCY - materialRY * 0.72 + drynessT * materialRY * 1.44;
                    ctx.beginPath();
                    ctx.moveTo(materialCX - materialRX * 0.62, drynessY);
                    ctx.quadraticCurveTo(materialCX, drynessY + Math.sin(drynessIdx * 1.7) * 5, materialCX + materialRX * 0.62, drynessY - 2);
                    ctx.stroke();
                  }
                }
                ctx.restore();
                }
                if (liveTissueState.tension > 62 && liveProcedureState.retracted) {
                  var tensionGuide = procedureGuidePoints();
                  ctx.save(); ctx.strokeStyle = 'rgba(251,191,36,' + Math.min(0.72, liveTissueState.tension / 135) + ')'; ctx.lineWidth = 1; ctx.setLineDash([3,4]);
                  [tensionGuide[0], tensionGuide[tensionGuide.length - 1]].forEach(function (tensionPoint) {
                    var tensionX = tensionPoint.x * W + parallaxX, tensionY = tensionPoint.y * H + parallaxY;
                    ctx.beginPath(); ctx.arc(tensionX, tensionY, 10 + liveTissueState.tension * 0.05, 0, Math.PI * 2); ctx.stroke();
                  });
                  ctx.setLineDash([]); ctx.restore();
                }
              }
              if (sceneDetail && instrumentVisuals && liveTissueState) {
                drawLocalizedTechniqueEvidence(liveProcedureState, liveTissueState);
              }

              if (focusMode && d.selectedOrgan) {
                var focusedStructure = organs.find(function (org) { return org.id === d.selectedOrgan; });
                if (focusedStructure) {
                  var focusedPoint = variedOrganPoint(focusedStructure);
                  var focusedScale = specimenScaleFactors();
                  var focusedX = ((focusedPoint.x - 0.5) * focusedScale.x + 0.5) * W + parallaxX;
                  var focusedY = ((focusedPoint.y - 0.45) * focusedScale.y + 0.45) * H + parallaxY;
                  ctx.save(); ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX * 1.08, materialRY * 1.08, 0, 0, Math.PI * 2); ctx.clip();
                  var focusVignette = ctx.createRadialGradient(focusedX, focusedY, 16, focusedX, focusedY, Math.max(materialRX, materialRY) * 1.1);
                  focusVignette.addColorStop(0, 'rgba(2,6,23,0)');
                  focusVignette.addColorStop(0.22, 'rgba(2,6,23,0.025)');
                  focusVignette.addColorStop(0.62, 'rgba(2,6,23,' + (liveVisualMode === 'accessible' ? '0.13' : '0.23') + ')');
                  focusVignette.addColorStop(1, 'rgba(2,6,23,' + (liveVisualMode === 'accessible' ? '0.22' : '0.42') + ')');
                  ctx.fillStyle = focusVignette; ctx.fillRect(materialCX - materialRX * 1.1, materialCY - materialRY * 1.1, materialRX * 2.2, materialRY * 2.2);
                  ctx.restore();
                  ctx.save(); ctx.strokeStyle = liveVisualMode === 'accessible' ? '#facc15' : 'rgba(254,240,138,0.48)'; ctx.lineWidth = liveVisualMode === 'accessible' ? 2.4 : 1;
                  ctx.beginPath(); ctx.arc(focusedX, focusedY, 28 + (1 - focusEntryProgress) * 24, 0, Math.PI * 2); ctx.stroke(); ctx.restore();
                }
              }

              ctx.save();
              ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX * 1.08, materialRY * 1.08, 0, 0, Math.PI * 2); ctx.clip();
              // Keep adjustable specimen lighting on anatomy so instructional overlays retain their intended contrast.
              var lightPointer = canvas._toolPointer;
              var directionAnchor = {
                overhead: { x: 0.50, y: 0.20 },
                left: { x: 0.18, y: 0.43 },
                right: { x: 0.82, y: 0.43 },
                raking: { x: 0.24, y: 0.76 }
              }[lightDirection] || { x: 0.50, y: 0.20 };
              var pointerLightWeight = lightPointer ? 0.22 : 0;
              var lightX = (directionAnchor.x * (1 - pointerLightWeight) + (lightPointer ? lightPointer.x : directionAnchor.x) * pointerLightWeight) * W;
              var lightY = (directionAnchor.y * (1 - pointerLightWeight) + (lightPointer ? lightPointer.y : directionAnchor.y) * pointerLightWeight) * H;
              var lightTint = liveLight === 'warm' ? '255,214,170' : (liveLight === 'cool' ? '186,230,253' : '241,245,249');
              var directionalShade = lightDirection === 'left' ? ctx.createLinearGradient(0, 0, W, 0)
                : (lightDirection === 'right' ? ctx.createLinearGradient(W, 0, 0, 0)
                  : (lightDirection === 'raking' ? ctx.createLinearGradient(0, H, W, 0) : ctx.createLinearGradient(0, 0, 0, H)));
              directionalShade.addColorStop(0, 'rgba(' + lightTint + ',' + (0.045 + liveLightStrength * 0.14 + (lightDirection === 'raking' ? 0.035 : 0)).toFixed(3) + ')');
              directionalShade.addColorStop(0.46, 'rgba(' + lightTint + ',' + (0.008 + liveLightStrength * 0.018).toFixed(3) + ')');
              directionalShade.addColorStop(1, 'rgba(2,6,23,' + (0.08 + (1 - liveLightStrength) * 0.24 + (lightDirection === 'raking' ? 0.045 : 0)).toFixed(3) + ')');
              ctx.fillStyle = directionalShade; ctx.fillRect(0, 0, W, H);
              var labGlow = ctx.createRadialGradient(lightX, lightY, 12, lightX, lightY, liveVisualMode === 'realistic' ? W * 0.48 : W * 0.62);
              labGlow.addColorStop(0, 'rgba(' + lightTint + ',' + (0.06 + liveLightStrength * 0.17 + (lightDirection === 'raking' ? 0.025 : 0)).toFixed(3) + ')');
              labGlow.addColorStop(0.55, 'rgba(' + lightTint + ',' + (0.012 + liveLightStrength * 0.05).toFixed(3) + ')');
              labGlow.addColorStop(1, 'rgba(2,6,23,' + (0.05 + (1 - liveLightStrength) * 0.20).toFixed(3) + ')');
              ctx.fillStyle = labGlow; ctx.fillRect(0, 0, W, H);
              if (liveLightStrength < 0.48) {
                ctx.fillStyle = 'rgba(2,6,23,' + ((0.48 - liveLightStrength) * 0.58).toFixed(3) + ')';
                ctx.fillRect(0, 0, W, H);
              }
              if (liveIllumination.glareRisk >= 25 && liveVisualMode !== 'accessible') {
                ctx.save(); ctx.beginPath(); ctx.ellipse(materialCX, materialCY, materialRX, materialRY, 0, 0, Math.PI * 2); ctx.clip();
                ctx.globalCompositeOperation = 'screen';
                var glareX = materialCX + (lightX - materialCX) * 0.24;
                var glareY = materialCY + (lightY - materialCY) * 0.18;
                var glareRadius = Math.max(18, Math.min(materialRX, materialRY) * (0.34 + liveIllumination.glareRisk / 250));
                var glareGradient = ctx.createRadialGradient(glareX, glareY, 1, glareX, glareY, glareRadius);
                glareGradient.addColorStop(0, 'rgba(' + lightTint + ',' + Math.min(0.56, liveIllumination.glareRisk / 150).toFixed(3) + ')');
                glareGradient.addColorStop(0.42, 'rgba(' + lightTint + ',' + Math.min(0.24, liveIllumination.glareRisk / 360).toFixed(3) + ')');
                glareGradient.addColorStop(1, 'rgba(' + lightTint + ',0)');
                ctx.fillStyle = glareGradient; ctx.fillRect(materialCX - materialRX, materialCY - materialRY, materialRX * 2, materialRY * 2);
                ctx.restore();
              }
              ctx.restore();
              // Paint semantic guidance last so specimen light, glare, and tissue effects never reduce interaction clarity.
              var screenPhysiologyHudReserve = !d.quizMode && (d.livingFunctionEnabled || (activeCanvasTraceKey && activeLayer === 'organs')) ? 76 : 0;
              function drawFinalSpecimenGuidanceOverlay() {
                ctx.save();
                ctx.translate(parallaxX, parallaxY); ctx.translate(cx, cy); ctx.transform(1, parallaxY * 0.0007, parallaxX * 0.0007, 1, 0, 0); ctx.scale(specimenScale.x, specimenScale.y); ctx.translate(-cx, -cy);
                drawAdaptiveHotspotGuidance(); // anatomy-attached hotspot callouts, depth landmarks, and selection reticles
                drawGuidedSpecimenPrompt(); // anatomy-attached guided target ring
                ctx.restore();
              }
              drawFinalSpecimenGuidanceOverlay();

              ctx.restore(); // End zoom transform
              // Screen UI stays fixed to the canvas edge while anatomy-attached cues follow specimen zoom, pan, and orientation.
              function drawFinalScreenGuidanceOverlay() {
                var screenGuideScale = canvasHudScale;
                var guidedScreenHeight = guidedMode && currentGuided ? Math.min(64, 34 * screenGuideScale) : 0;
                var guidedScreenY = guidedScreenHeight ? H - guidedScreenHeight - 14 - screenPhysiologyHudReserve : H - 14 - screenPhysiologyHudReserve;
                if (guidedScreenHeight) {
                  ctx.save(); ctx.fillStyle = 'rgba(88,28,135,0.96)'; ctx.strokeStyle = 'rgba(216,180,254,0.76)'; ctx.lineWidth = 1;
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(14, guidedScreenY, W - 28, guidedScreenHeight, 8); ctx.fill(); ctx.stroke(); }
                  else { ctx.fillRect(14, guidedScreenY, W - 28, guidedScreenHeight); ctx.strokeRect(14, guidedScreenY, W - 28, guidedScreenHeight); }
                  ctx.font = 'bold ' + (12 * screenGuideScale) + 'px Inter, system-ui, sans-serif'; ctx.fillStyle = '#ffffff';
                  var screenPromptText = currentGuided.prompt, screenPromptLimit = Math.max(120, W - 52);
                  while (screenPromptText.length > 12 && ctx.measureText(screenPromptText).width > screenPromptLimit) screenPromptText = screenPromptText.slice(0, -2).trim() + '\u2026';
                  ctx.fillText(screenPromptText, 24, guidedScreenY + guidedScreenHeight / 2 + 4 * screenGuideScale); ctx.restore();
                }
                var screenLayerDef = spec.layers[currentLayerIdx];
                var layerPillHeight = 26 * screenGuideScale;
                var layerPillY = Math.max(48, (guidedScreenHeight ? guidedScreenY - 9 : H - 14) - layerPillHeight);
                if (screenLayerDef) {
                  var screenLayerLabel = screenLayerDef.icon + ' ' + screenLayerDef.name + ' Layer';
                  ctx.save(); ctx.font = 'bold ' + (12 * screenGuideScale) + 'px Inter, system-ui, sans-serif';
                  var layerPillWidth = Math.min(W - 28, ctx.measureText(screenLayerLabel).width + 22 * screenGuideScale);
                  ctx.fillStyle = 'rgba(15,23,42,0.9)'; ctx.strokeStyle = 'rgba(148,163,184,0.52)';
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(14, layerPillY, layerPillWidth, layerPillHeight, 7); ctx.fill(); ctx.stroke(); }
                  else { ctx.fillRect(14, layerPillY, layerPillWidth, layerPillHeight); ctx.strokeRect(14, layerPillY, layerPillWidth, layerPillHeight); }
                  ctx.fillStyle = '#f8fafc'; ctx.fillText(screenLayerLabel, 25, layerPillY + 18 * screenGuideScale); ctx.restore();
                }
                var screenCompactCount = adaptiveHotspotLayout.filter(function (item) { return !item.fullLabel; }).length;
                if (!d.quizMode && screenCompactCount > 0) {
                  var screenDeclutter = 'Adaptive labels \u00B7 ' + screenCompactCount + ' compact \u00B7 ' + (canvasCoarsePointer ? 'select to expand' : 'hover to expand');
                  ctx.save(); ctx.font = 'bold ' + (9.5 * screenGuideScale) + 'px Inter, system-ui';
                  var screenDeclutterWidth = Math.min(W - 28, ctx.measureText(screenDeclutter).width + 18 * screenGuideScale);
                  var screenDeclutterHeight = 22 * screenGuideScale, screenDeclutterX = (W - screenDeclutterWidth) / 2;
                  var screenDeclutterY = Math.max(44, layerPillY - screenDeclutterHeight - 8);
                  ctx.fillStyle = 'rgba(15,23,42,0.9)'; ctx.strokeStyle = 'rgba(45,212,191,0.64)';
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(screenDeclutterX, screenDeclutterY, screenDeclutterWidth, screenDeclutterHeight, 7); ctx.fill(); ctx.stroke(); }
                  else { ctx.fillRect(screenDeclutterX, screenDeclutterY, screenDeclutterWidth, screenDeclutterHeight); ctx.strokeRect(screenDeclutterX, screenDeclutterY, screenDeclutterWidth, screenDeclutterHeight); }
                  ctx.fillStyle = '#99f6e4'; ctx.fillText(screenDeclutter, screenDeclutterX + 9 * screenGuideScale, screenDeclutterY + 15 * screenGuideScale); ctx.restore();
                }
              }
              drawFinalScreenGuidanceOverlay();

              // A calibrated screen-fixed orientation and scale HUD keeps anatomical direction accurate across specimen shapes and zoom.
              var hudView = String(d.anatomicalView || anatomicalView || 'dorsal').toUpperCase();
              var specimenSpanCm = spec.bodyShape === 'worm' ? 15 : spec.bodyShape === 'pig' ? 25 : spec.bodyShape === 'fish' ? 20 : spec.bodyShape === 'crayfish' ? 12 : spec.bodyShape === 'frog' ? 8 : 3;
              var scaleCm = (spec.bodyShape === 'pig' || spec.bodyShape === 'fish') ? 2 : ((spec.bodyShape === 'eye' || spec.bodyShape === 'heart') ? 0.5 : 1);
              var scalePixels = Math.max(28, Math.min(92, (scaleCm / specimenSpanCm) * W * zoom));
              var horizontalAxis = spec.bodyShape === 'pig' || spec.bodyShape === 'fish' || spec.bodyShape === 'crayfish' || spec.bodyShape === 'eye';
              var cardiacAxis = spec.bodyShape === 'heart';
              var axisStart = cardiacAxis ? 'BASE' : 'A';
              var axisEnd = cardiacAxis ? 'APEX' : 'P';
              var orientationText = horizontalAxis ? axisStart + ' \u2190  \u2192 ' + axisEnd : axisStart + ' \u2191  \u2193 ' + axisEnd;
              ctx.save();
              var specimenHudLabel = spec.icon + ' ' + spec.name + '  /  ' + hudView;
              ctx.font = 'bold ' + (12 * canvasHudScale) + 'px Inter, system-ui, sans-serif';
              var specimenHudWidth = Math.min(W - 28, Math.max(154 * canvasHudScale, ctx.measureText(specimenHudLabel).width + 22 * canvasHudScale));
              var macroLikelyOnLeft = inspectionLens && macroInset && Number(canvas._lensDisplayX) > W * 0.58;
              var specimenHudX = macroLikelyOnLeft ? W - specimenHudWidth - 14 : 14;
              ctx.fillStyle = 'rgba(15,23,42,0.86)'; ctx.strokeStyle = 'rgba(148,163,184,0.34)'; ctx.lineWidth = 1;
              if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(specimenHudX, 13, specimenHudWidth, 30 * canvasHudScale, 8 * canvasHudScale); ctx.fill(); ctx.stroke(); }
              else { ctx.fillRect(specimenHudX, 13, specimenHudWidth, 30 * canvasHudScale); ctx.strokeRect(specimenHudX, 13, specimenHudWidth, 30 * canvasHudScale); }
              ctx.fillStyle = '#e2e8f0'; ctx.fillText(specimenHudLabel, specimenHudX + 11 * canvasHudScale, 13 + 20 * canvasHudScale);

              var compassWidth = Math.min(W - 28, Math.max(144 * canvasHudScale, scalePixels + 18 * canvasHudScale)), compassHeight = 62 * canvasHudScale, compassX = W - compassWidth - 14;
              var compassBottomInset = guidedMode && currentGuided ? Math.max(72, 64 * canvasHudScale) : 14;
              var compassY = H - compassHeight - compassBottomInset;
              var liveLensX = Number(canvas._lensDisplayX), liveLensY = Number(canvas._lensDisplayY);
              var lensNearCompass = inspectionLens && isFinite(liveLensX) && isFinite(liveLensY) && liveLensX > compassX - 42 * canvasHudScale && liveLensY > compassY - 42 * canvasHudScale;
              if (lensNearCompass) compassY = Math.max(82, compassY - 76 * canvasHudScale);
              ctx.fillStyle = 'rgba(15,23,42,0.88)'; ctx.strokeStyle = 'rgba(148,163,184,0.38)';
              if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(compassX, compassY, compassWidth, compassHeight, 8 * canvasHudScale); ctx.fill(); ctx.stroke(); }
              else { ctx.fillRect(compassX, compassY, compassWidth, compassHeight); ctx.strokeRect(compassX, compassY, compassWidth, compassHeight); }
              ctx.font = 'bold ' + (12 * canvasHudScale) + 'px Inter, system-ui'; ctx.fillStyle = '#bae6fd';
              ctx.fillText(orientationText, compassX + 10 * canvasHudScale, compassY + 16 * canvasHudScale);
              ctx.font = (10 * canvasHudScale) + 'px Inter, system-ui'; ctx.fillStyle = '#94a3b8';
              ctx.fillText(cardiacAxis ? 'base-apex' : 'anterior-posterior', compassX + 10 * canvasHudScale, compassY + 31 * canvasHudScale);
              var scaleX = compassX + 10 * canvasHudScale, scaleY = compassY + 44 * canvasHudScale;
              ctx.strokeStyle = '#f8fafc'; ctx.lineWidth = 1.5; ctx.beginPath();
              ctx.moveTo(scaleX, scaleY); ctx.lineTo(scaleX + scalePixels, scaleY);
              ctx.moveTo(scaleX, scaleY - 4 * canvasHudScale); ctx.lineTo(scaleX, scaleY + 4 * canvasHudScale);
              ctx.moveTo(scaleX + scalePixels, scaleY - 4 * canvasHudScale); ctx.lineTo(scaleX + scalePixels, scaleY + 4 * canvasHudScale); ctx.stroke();
              var scaleLabel = scaleCm + ' cm  /  ' + zoom.toFixed(1) + 'x';
              ctx.font = 'bold ' + (10 * canvasHudScale) + 'px Inter, system-ui'; ctx.fillStyle = '#f8fafc';
              ctx.fillText(scaleLabel, scaleX, compassY + 58 * canvasHudScale);
              ctx.restore();

              // Screen-fixed transition labels state the same change conveyed by sweep direction, ring motion, and endpoint shapes.
              function drawSpatialTransitionBadge(y, title, fromLabel, toLabel, directionLabel, progress, alpha, accent) {
                if (alpha <= 0) return;
                ctx.save(); ctx.globalAlpha = alpha;
                var spatialBadgeWidth = Math.min(286, W - 36), spatialBadgeHeight = 46, spatialBadgeX = (W - spatialBadgeWidth) / 2;
                ctx.shadowColor = 'rgba(2,6,23,0.62)'; ctx.shadowBlur = 12;
                var spatialBadgeGradient = ctx.createLinearGradient(spatialBadgeX, y, spatialBadgeX + spatialBadgeWidth, y + spatialBadgeHeight);
                spatialBadgeGradient.addColorStop(0, 'rgba(15,23,42,0.96)'); spatialBadgeGradient.addColorStop(1, 'rgba(30,41,59,0.95)');
                ctx.fillStyle = spatialBadgeGradient; ctx.strokeStyle = accent; ctx.lineWidth = 1.1;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(spatialBadgeX, y, spatialBadgeWidth, spatialBadgeHeight, 10); ctx.fill(); ctx.stroke(); }
                else { ctx.fillRect(spatialBadgeX, y, spatialBadgeWidth, spatialBadgeHeight); ctx.strokeRect(spatialBadgeX, y, spatialBadgeWidth, spatialBadgeHeight); }
                ctx.shadowBlur = 0; ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = accent; ctx.fillText(title + ' \u00B7 ' + directionLabel, spatialBadgeX + 12, y + 14);
                ctx.fillStyle = '#f8fafc'; ctx.beginPath(); ctx.arc(spatialBadgeX + 16, y + 28, 3, 0, Math.PI * 2); ctx.fill();
                ctx.save(); ctx.translate(spatialBadgeX + spatialBadgeWidth - 16, y + 28); ctx.rotate(Math.PI / 4); ctx.fillRect(-3, -3, 6, 6); ctx.restore();
                ctx.font = 'bold 10px Inter, system-ui'; ctx.textAlign = 'center'; ctx.fillText(String(fromLabel).toUpperCase() + '  \u2192  ' + String(toLabel).toUpperCase(), spatialBadgeX + spatialBadgeWidth / 2, y + 31); ctx.textAlign = 'start';
                ctx.fillStyle = 'rgba(148,163,184,0.26)'; ctx.fillRect(spatialBadgeX + 12, y + 37, spatialBadgeWidth - 24, 3);
                ctx.fillStyle = accent; ctx.fillRect(spatialBadgeX + 12, y + 37, (spatialBadgeWidth - 24) * Math.max(0, Math.min(1, progress)), 3); ctx.restore();
              }
              var spatialBadgeY = (_layerTransition && _layerTransition.active) ? 74 : 22;
              if (_viewTransition && _viewTransition.active) {
                var viewBadgeElapsed = Math.max(0, Date.now() - (_viewTransition.startedAt || Date.now()));
                var viewBadgeDuration = Math.max(1, Number(_viewTransition.duration) || 820);
                var viewBadgeLinear = Math.max(0, Math.min(1, viewBadgeElapsed / viewBadgeDuration));
                var viewBadgeAlpha = _viewTransition.reducedMotion || dissMotionReduced ? 1 : Math.min(1, viewBadgeLinear / 0.12, (1 - viewBadgeLinear) / 0.16);
                drawSpatialTransitionBadge(spatialBadgeY, 'VIEW CHANGE', _viewTransition.fromView, _viewTransition.toView, 'CIRCLE START / DIAMOND TARGET', viewTransitionProgress, viewBadgeAlpha, '#7dd3fc');
                spatialBadgeY += 52;
              }
              if (_layerBrowseTransition && _layerBrowseTransition.active) {
                var layerBrowseBadgeElapsed = Math.max(0, Date.now() - (_layerBrowseTransition.startedAt || Date.now()));
                var layerBrowseBadgeDuration = Math.max(1, Number(_layerBrowseTransition.duration) || 720);
                var layerBrowseBadgeLinear = Math.max(0, Math.min(1, layerBrowseBadgeElapsed / layerBrowseBadgeDuration));
                var layerBrowseBadgeAlpha = _layerBrowseTransition.reducedMotion || dissMotionReduced ? 1 : Math.min(1, layerBrowseBadgeLinear / 0.12, (1 - layerBrowseBadgeLinear) / 0.16);
                drawSpatialTransitionBadge(spatialBadgeY, 'LAYER VIEW', _layerBrowseTransition.fromName, _layerBrowseTransition.toName, _layerBrowseTransition.direction === 'deeper' ? 'RINGS IN / DEEPER' : 'RINGS OUT / SURFACE', layerBrowseProgress, layerBrowseBadgeAlpha, _layerBrowseTransition.direction === 'deeper' ? '#c4b5fd' : '#67e8f9');
              }
              // Screen-fixed confirmation stays readable even when the specimen is mirrored, zoomed, or panned.
              if (_layerTransition && _layerTransition.active) {
                var badgeElapsed = Math.max(0, Date.now() - (_layerTransition.startedAt || Date.now()));
                var badgeDuration = Math.max(1, Number(_layerTransition.duration) || 1150);
                var badgeLinear = Math.max(0, Math.min(1, badgeElapsed / badgeDuration));
                var badgeAlpha = _layerTransition.reducedMotion || dissMotionReduced ? 1 : Math.min(1, badgeLinear / 0.14, (1 - badgeLinear) / 0.16);
                if (badgeAlpha > 0) {
                  ctx.save(); ctx.globalAlpha = badgeAlpha;
                  var layerBadgeWidth = Math.min(278, W - 36), layerBadgeHeight = 46, layerBadgeX = (W - layerBadgeWidth) / 2, layerBadgeY = 22;
                  ctx.shadowColor = 'rgba(2,6,23,0.62)'; ctx.shadowBlur = 12;
                  var layerBadgeGradient = ctx.createLinearGradient(layerBadgeX, layerBadgeY, layerBadgeX + layerBadgeWidth, layerBadgeY + layerBadgeHeight);
                  layerBadgeGradient.addColorStop(0, 'rgba(15,23,42,0.96)'); layerBadgeGradient.addColorStop(1, 'rgba(15,118,110,0.94)');
                  ctx.fillStyle = layerBadgeGradient; ctx.strokeStyle = 'rgba(153,246,228,0.72)'; ctx.lineWidth = 1;
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(layerBadgeX, layerBadgeY, layerBadgeWidth, layerBadgeHeight, 10); ctx.fill(); ctx.stroke(); }
                  else { ctx.fillRect(layerBadgeX, layerBadgeY, layerBadgeWidth, layerBadgeHeight); ctx.strokeRect(layerBadgeX, layerBadgeY, layerBadgeWidth, layerBadgeHeight); }
                  ctx.shadowBlur = 0; ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = '#99f6e4'; ctx.fillText('LAYER REVEALED', layerBadgeX + 12, layerBadgeY + 14);
                  ctx.font = 'bold 11px Inter, system-ui'; ctx.fillStyle = '#f8fafc';
                  ctx.fillText((_layerTransition.fromIcon || '') + ' ' + _layerTransition.fromName + '  \u2192  ' + (_layerTransition.toIcon || '') + ' ' + _layerTransition.toName, layerBadgeX + 12, layerBadgeY + 31);
                  ctx.fillStyle = 'rgba(153,246,228,0.22)'; ctx.fillRect(layerBadgeX + 12, layerBadgeY + 37, layerBadgeWidth - 24, 3);
                  ctx.fillStyle = '#5eead4'; ctx.fillRect(layerBadgeX + 12, layerBadgeY + 37, (layerBadgeWidth - 24) * (_layerTransition.reducedMotion ? 1 : layerTransitionProgress), 3);
                  ctx.restore();
                }
              }

              // The living-function model is opt-in and explicitly distinct from preserved specimen behavior.
              var livingProfile = specimenLivingFunctionProfile();
              var livingPhaseLabel = livingProfile.phases[livingPhase < 0.5 ? 0 : 1];
              if (!d.quizMode && d.livingFunctionEnabled) {
                ctx.save();
                ctx.globalAlpha = liveVisualMode === 'accessible' ? 0.88 : 0.68;
                ctx.strokeStyle = livingProfile.color; ctx.fillStyle = livingProfile.color; ctx.lineWidth = liveVisualMode === 'accessible' ? 2.8 : 1.7;
                ctx.shadowColor = livingProfile.color; ctx.shadowBlur = liveVisualMode === 'accessible' ? 4 : 8;
                if (livingProfile.motion === 'frog-buccal') {
                  var frogThroatY = cy - H * 0.18;
                  ctx.beginPath(); ctx.ellipse(cx, frogThroatY, W * (0.035 + livingWave * 0.014), H * (0.018 + livingWave * 0.012), 0, 0, Math.PI * 2); ctx.stroke();
                  [-1, 1].forEach(function (side) {
                    ctx.beginPath(); ctx.ellipse(cx + side * W * 0.075, cy - H * 0.065, W * (0.035 + livingWave * 0.012), H * (0.055 + livingWave * 0.018), side * 0.08, 0, Math.PI * 2); ctx.stroke();
                  });
                  ctx.beginPath(); ctx.moveTo(cx, frogThroatY + H * 0.02); ctx.lineTo(cx, cy - H * 0.12); ctx.stroke();
                } else if (livingProfile.motion === 'worm-peristalsis') {
                  for (var livingSegmentIdx = 0; livingSegmentIdx < 12; livingSegmentIdx++) {
                    var livingSegmentPhase = (livingPhase + livingSegmentIdx / 12) % 1;
                    var livingSegmentCompression = (1 - Math.cos(livingSegmentPhase * Math.PI * 2)) / 2;
                    var livingSegmentY = H * 0.2 + livingSegmentIdx * H * 0.05;
                    ctx.globalAlpha = 0.28 + livingSegmentCompression * 0.55;
                    ctx.beginPath(); ctx.ellipse(cx, livingSegmentY, W * (0.055 - livingSegmentCompression * 0.014), H * 0.012, 0, 0, Math.PI * 2); ctx.stroke();
                  }
                } else if (livingProfile.motion === 'fish-operculum') {
                  var livingGillX = cx - W * 0.18, livingGillY = cy - H * 0.01;
                  ctx.beginPath(); ctx.arc(livingGillX, livingGillY, 17 + livingWave * 8, -1.05, 1.05); ctx.stroke();
                  for (var livingWaterIdx = 0; livingWaterIdx < 4; livingWaterIdx++) {
                    var livingWaterOffset = ((livingPhase + livingWaterIdx / 4) % 1) * W * 0.16;
                    ctx.beginPath(); ctx.moveTo(livingGillX - W * 0.12 + livingWaterOffset, livingGillY - 12 + livingWaterIdx * 8);
                    ctx.quadraticCurveTo(livingGillX - W * 0.08 + livingWaterOffset, livingGillY - 17 + livingWaterIdx * 8, livingGillX - W * 0.04 + livingWaterOffset, livingGillY - 12 + livingWaterIdx * 8); ctx.stroke();
                  }
                } else if (livingProfile.motion === 'crayfish-gills') {
                  [-1, 1].forEach(function (side) {
                    for (var livingGillIdx = 0; livingGillIdx < 5; livingGillIdx++) {
                      var livingGillBaseX = cx - W * 0.06 + livingGillIdx * W * 0.03;
                      var livingGillBaseY = cy + side * H * 0.075;
                      ctx.beginPath(); ctx.moveTo(livingGillBaseX, livingGillBaseY);
                      ctx.quadraticCurveTo(livingGillBaseX + W * 0.018, livingGillBaseY + side * H * (0.028 + livingWave * 0.012), livingGillBaseX + W * 0.032, livingGillBaseY + side * H * 0.045); ctx.stroke();
                    }
                  });
                } else if (livingProfile.motion === 'pig-diaphragm') {
                  [-1, 1].forEach(function (side) {
                    ctx.beginPath(); ctx.ellipse(cx + side * W * 0.075, cy - H * 0.055, W * (0.052 + livingWave * 0.012), H * (0.07 + livingWave * 0.018), side * 0.08, 0, Math.PI * 2); ctx.stroke();
                  });
                  ctx.beginPath(); ctx.arc(cx, cy + H * (0.045 + livingWave * 0.018), W * 0.14, Math.PI * 1.08, Math.PI * 1.92); ctx.stroke();
                } else if (livingProfile.motion === 'eye-pupil') {
                  var livingEyeX = cx - W * 0.17, livingEyeY = cy - H * 0.02;
                  var livingPupilRadius = 6 + (1 - livingWave) * 9;
                  ctx.beginPath(); ctx.arc(livingEyeX, livingEyeY, livingPupilRadius, 0, Math.PI * 2); ctx.stroke();
                  for (var livingRayIdx = 0; livingRayIdx < 4; livingRayIdx++) {
                    var livingRayY = livingEyeY - 18 + livingRayIdx * 12;
                    ctx.beginPath(); ctx.moveTo(livingEyeX - W * 0.18, livingRayY); ctx.lineTo(livingEyeX - livingPupilRadius - 4, livingEyeY + (livingRayY - livingEyeY) * 0.35); ctx.stroke();
                  }
                } else if (livingProfile.motion === 'heart-cycle') {
                  var heartPulseScale = 1 + livingWave * 0.08;
                  ctx.save(); ctx.translate(cx, cy); ctx.scale(heartPulseScale, heartPulseScale);
                  ctx.beginPath(); ctx.moveTo(0, H * 0.13);
                  ctx.bezierCurveTo(-W * 0.15, H * 0.02, -W * 0.12, -H * 0.13, 0, -H * 0.05);
                  ctx.bezierCurveTo(W * 0.12, -H * 0.13, W * 0.15, H * 0.02, 0, H * 0.13); ctx.stroke();
                  ctx.restore();
                  ctx.beginPath(); ctx.arc(cx, cy - H * 0.045, 8 + (1 - livingWave) * 5, 0, Math.PI * 2); ctx.stroke();
                }
                ctx.shadowBlur = 0;
                var livingHudW = Math.min(228, W - 28), livingHudX = W - livingHudW - 14, livingHudY = H - 79;
                ctx.fillStyle = 'rgba(15,23,42,0.92)'; ctx.strokeStyle = livingProfile.color; ctx.lineWidth = 1;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(livingHudX, livingHudY, livingHudW, 47, 9); ctx.fill(); ctx.stroke(); }
                else { ctx.fillRect(livingHudX, livingHudY, livingHudW, 47); ctx.strokeRect(livingHudX, livingHudY, livingHudW, 47); }
                ctx.font = 'bold 9px Inter, system-ui'; ctx.fillStyle = '#f8fafc'; ctx.fillText('LIVING FUNCTION MODEL', livingHudX + 9, livingHudY + 14);
                ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = livingProfile.color;
                ctx.fillText((dissMotionReduced ? 'STATIC' : (d.livingFunctionPaused ? 'PAUSED' : livingPhaseLabel.toUpperCase())).slice(0, 30), livingHudX + 9, livingHudY + 27);
                ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#cbd5e1'; ctx.fillText('In-life physiology · not preserved motion', livingHudX + 9, livingHudY + 39);
                ctx.restore();
              }

              // Screen-fixed functional pathway HUD pairs color with labels, motion state, and direction arrows.
              if (activeCanvasTraceKey && activeLayer === 'organs') {
                var traceHUDConfig = {
                  circulation: { label: 'Circulatory pathway', color: '#fb7185', secondary: '#60a5fa', icon: '\u2665' },
                  digestion: { label: 'Digestive pathway', color: '#fbbf24', secondary: '#fdba74', icon: '\u25C9' },
                  respiration: { label: 'Respiratory pathway', color: '#38bdf8', secondary: '#f87171', icon: '\u2248' },
                  excretory: { label: 'Excretory pathway', color: '#a3e635', secondary: '#c084fc', icon: '\u25C6' }
                }[activeCanvasTraceKey];
                var traceHUDStatus = dissMotionReduced ? 'STATIC \u00B7 reduced motion' : (systemPlaybackPaused ? 'PAUSED \u00B7 directions held' : 'PLAYING \u00B7 directional flow');
                ctx.save();
                var traceHUDX = 14, traceHUDY = H - 82, traceHUDW = 184, traceHUDH = 50;
                ctx.fillStyle = 'rgba(15,23,42,0.92)'; ctx.strokeStyle = traceHUDConfig.color; ctx.lineWidth = 1;
                if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(traceHUDX, traceHUDY, traceHUDW, traceHUDH, 9); ctx.fill(); ctx.stroke(); }
                else { ctx.fillRect(traceHUDX, traceHUDY, traceHUDW, traceHUDH); ctx.strokeRect(traceHUDX, traceHUDY, traceHUDW, traceHUDH); }
                ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = '#f8fafc'; ctx.fillText(traceHUDConfig.icon + ' ' + traceHUDConfig.label, traceHUDX + 10, traceHUDY + 15);
                ctx.font = 'bold 7px Inter, system-ui'; ctx.fillStyle = traceHUDConfig.color; ctx.fillText(traceHUDStatus, traceHUDX + 10, traceHUDY + 27);
                var traceLineY = traceHUDY + 39;
                var traceMiniGradient = ctx.createLinearGradient(traceHUDX + 10, traceLineY, traceHUDX + traceHUDW - 10, traceLineY);
                traceMiniGradient.addColorStop(0, traceHUDConfig.secondary); traceMiniGradient.addColorStop(0.48, traceHUDConfig.color); traceMiniGradient.addColorStop(1, traceHUDConfig.secondary);
                ctx.strokeStyle = traceMiniGradient; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(traceHUDX + 10, traceLineY); ctx.lineTo(traceHUDX + traceHUDW - 12, traceLineY); ctx.stroke();
                for (var traceHUDMarkerIndex = 0; traceHUDMarkerIndex < 3; traceHUDMarkerIndex++) {
                  var traceHUDPhase = dissMotionReduced || systemPlaybackPaused ? (traceHUDMarkerIndex + 1) / 4 : (systemTraceTick * 0.012 + traceHUDMarkerIndex / 3) % 1;
                  var traceHUDMarkerX = traceHUDX + 12 + traceHUDPhase * (traceHUDW - 28);
                  ctx.fillStyle = traceHUDConfig.color; ctx.beginPath(); ctx.moveTo(traceHUDMarkerX + 5, traceLineY); ctx.lineTo(traceHUDMarkerX - 3, traceLineY - 4); ctx.lineTo(traceHUDMarkerX - 3, traceLineY + 4); ctx.closePath(); ctx.fill();
                }
                if (activeCanvasTraceKey === 'circulation') {
                  ctx.beginPath(); ctx.arc(traceHUDX + traceHUDW - 21, traceHUDY + 13, 3, 0, Math.PI * 2); ctx.fillStyle = traceHUDConfig.secondary; ctx.fill();
                }
                ctx.restore();
              }
              function drawAnatomicalOrientationCompass() {
                if (!sceneDetail || d.quizMode) return;
                var compass = anatomicalOrientationData();
                var compassAccessible = highContrastEnabled || liveVisualMode === 'accessible';
                var compassAccent = compassAccessible ? '#ffffff' : (compass.aligned ? '#2dd4bf' : '#fbbf24');
                var compassX = 14, compassY = 14;
                var compassW = compass.axis === 'horizontal' ? 188 : 126;
                var compassH = compass.axis === 'horizontal' ? 55 : 91;
                var compassStatus = compass.view.toUpperCase() + (compass.aligned ? ' · ALIGNED' : ' · TARGET ' + compass.targetView.toUpperCase());
                ctx.save();
                ctx.fillStyle = compassAccessible ? '#000000' : 'rgba(15,23,42,0.88)';
                ctx.strokeStyle = compassAccent; ctx.lineWidth = compassAccessible ? 2 : 1;
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(compassX, compassY, compassW, compassH, 8); else ctx.rect(compassX, compassY, compassW, compassH);
                ctx.fill(); ctx.stroke();
                ctx.font = 'bold ' + (compassAccessible ? '9' : '8') + 'px Inter, system-ui';
                ctx.fillStyle = compassAccessible ? '#ffffff' : '#e2e8f0';
                ctx.fillText('ANATOMICAL AXIS', compassX + 9, compassY + 13);
                ctx.font = 'bold 7px Inter, system-ui'; ctx.fillStyle = compassAccent;
                ctx.fillText(compassStatus.slice(0, 27), compassX + 9, compassY + 24);
                ctx.strokeStyle = compassAccent; ctx.fillStyle = compassAccent;
                ctx.lineWidth = compassAccessible ? 2.2 : 1.5;
                if (compass.axis === 'horizontal') {
                  var axisLeftX = compassX + 15, axisRightX = compassX + compassW - 15, axisY = compassY + 35;
                  var axisStartX = compass.mirrored ? axisRightX : axisLeftX;
                  var axisEndX = compass.mirrored ? axisLeftX : axisRightX;
                  ctx.beginPath(); ctx.moveTo(axisLeftX, axisY); ctx.lineTo(axisRightX, axisY); ctx.stroke();
                  ctx.beginPath(); ctx.arc(axisStartX, axisY, compassAccessible ? 4.5 : 3.7, 0, Math.PI * 2); ctx.fill();
                  ctx.beginPath(); ctx.moveTo(axisEndX, axisY - 5); ctx.lineTo(axisEndX + 5, axisY); ctx.lineTo(axisEndX, axisY + 5); ctx.lineTo(axisEndX - 5, axisY); ctx.closePath(); ctx.fill();
                  ctx.font = 'bold 7px Inter, system-ui'; ctx.textAlign = 'center';
                  ctx.fillText(compass.start.slice(0, 12), axisStartX, compassY + 50);
                  ctx.fillText(compass.end.slice(0, 12), axisEndX, compassY + 50);
                } else {
                  var axisX = compassX + 18, axisTopY = compassY + 31, axisBottomY = compassY + compassH - 13;
                  ctx.beginPath(); ctx.moveTo(axisX, axisTopY); ctx.lineTo(axisX, axisBottomY); ctx.stroke();
                  ctx.beginPath(); ctx.arc(axisX, axisTopY, compassAccessible ? 4.5 : 3.7, 0, Math.PI * 2); ctx.fill();
                  ctx.beginPath(); ctx.moveTo(axisX, axisBottomY - 5); ctx.lineTo(axisX + 5, axisBottomY); ctx.lineTo(axisX, axisBottomY + 5); ctx.lineTo(axisX - 5, axisBottomY); ctx.closePath(); ctx.fill();
                  ctx.font = 'bold 7px Inter, system-ui'; ctx.textAlign = 'left';
                  ctx.fillText(compass.start.slice(0, 12), axisX + 10, axisTopY + 3);
                  ctx.fillText(compass.end.slice(0, 12), axisX + 10, axisBottomY + 3);
                }
                ctx.restore();
              }
              drawAnatomicalOrientationCompass();
              var liveScreenPointer = canvas._toolPointer;
              var queuedContactPulse = canvas._toolContactPulse;
              var contactPulseDuration = queuedContactPulse ? Math.max(360, Number(queuedContactPulse.duration) || 720) : 720;
              var activeContactPulse = queuedContactPulse && Date.now() - queuedContactPulse.at < contactPulseDuration ? queuedContactPulse : null;
              var contactPulseProgress = activeContactPulse ? Math.max(0, Math.min(1, (Date.now() - activeContactPulse.at) / contactPulseDuration)) : 1;
              var replayScreenPointer = null;
              if (!liveScreenPointer && activeContactPulse && activeContactPulse.replay && activeContactPulse.replayPath && activeContactPulse.replayPath.length) {
                // Replay advances along the real teaching path, with eased travel and no looping.
                var replayTravel = dissMotionReduced ? 0.5 : Math.max(0, Math.min(1, (contactPulseProgress - 0.12) / 0.66));
                replayTravel = replayTravel * replayTravel * (3 - 2 * replayTravel);
                var replayPath = activeContactPulse.replayPath;
                var replayPathPosition = replayTravel * Math.max(0, replayPath.length - 1);
                var replayPathIndex = Math.min(replayPath.length - 1, Math.floor(replayPathPosition));
                var replayPathNext = Math.min(replayPath.length - 1, replayPathIndex + 1);
                var replayPathMix = replayPathPosition - replayPathIndex;
                replayScreenPointer = {
                  x: replayPath[replayPathIndex].x + (replayPath[replayPathNext].x - replayPath[replayPathIndex].x) * replayPathMix,
                  y: replayPath[replayPathIndex].y + (replayPath[replayPathNext].y - replayPath[replayPathIndex].y) * replayPathMix
                };
              }
              var screenPointer = liveScreenPointer || replayScreenPointer || (activeContactPulse && activeContactPulse.replay ? activeContactPulse : null) || (d.lensPinned && d.lensPinnedPoint ? d.lensPinnedPoint : null) || activeContactPulse;
              var pointerIsSynthetic = !liveScreenPointer && !!screenPointer;
              // One-shot outcome feedback localizes the tissue-state change at the actual action point with shape and text redundancy.
              var queuedOutcomePulse = canvas._toolOutcomePulse;
              var outcomePulseDuration = queuedOutcomePulse ? Math.max(700, Number(queuedOutcomePulse.duration) || 1500) : 1500;
              var activeOutcomePulse = queuedOutcomePulse && Date.now() - queuedOutcomePulse.at < outcomePulseDuration ? queuedOutcomePulse : null;
              if (activeOutcomePulse && !d.quizMode && !d.annotateMode && !d.rulerMode) {
                var outcomeProgress = Math.max(0, Math.min(1, (Date.now() - activeOutcomePulse.at) / outcomePulseDuration));
                var outcomeScale = specimenScaleFactors();
                var outcomeDisplayedX = (activeOutcomePulse.x - 0.5) * outcomeScale.x + 0.5;
                var outcomeDisplayedY = (activeOutcomePulse.y - 0.45) * outcomeScale.y + 0.45;
                var outcomeScreenX = (outcomeDisplayedX * W + parallaxX - W / 2) * zoom + W / 2 + panX;
                var outcomeScreenY = (outcomeDisplayedY * H + parallaxY - H / 2) * zoom + H / 2 + panY;
                var outcomeAccessible = highContrastEnabled || liveVisualMode === 'accessible';
                var outcomeCaution = activeOutcomePulse.tone === 'caution';
                var outcomeColor = outcomeAccessible ? '#ffffff' : (outcomeCaution ? '#fb7185' : '#2dd4bf');
                var outcomeMotionProgress = dissMotionReduced ? 0.5 : outcomeProgress;
                ctx.save();
                ctx.strokeStyle = outcomeColor; ctx.fillStyle = outcomeAccessible ? '#000000' : 'rgba(15,23,42,0.92)';
                ctx.lineWidth = outcomeAccessible ? 2.7 : 1.8;
                if (!dissMotionReduced) {
                  ctx.globalAlpha = Math.max(0, 0.72 * (1 - outcomeProgress));
                  ctx.beginPath(); ctx.arc(outcomeScreenX, outcomeScreenY, 15 + outcomeMotionProgress * 30, 0, Math.PI * 2); ctx.stroke();
                  ctx.globalAlpha = 1;
                }
                if (outcomeCaution) {
                  ctx.beginPath(); ctx.moveTo(outcomeScreenX, outcomeScreenY - 15); ctx.lineTo(outcomeScreenX + 15, outcomeScreenY + 12); ctx.lineTo(outcomeScreenX - 15, outcomeScreenY + 12); ctx.closePath(); ctx.fill(); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(outcomeScreenX, outcomeScreenY - 7); ctx.lineTo(outcomeScreenX, outcomeScreenY + 3); ctx.stroke();
                  ctx.beginPath(); ctx.arc(outcomeScreenX, outcomeScreenY + 7, 1.4, 0, Math.PI * 2); ctx.fillStyle = outcomeColor; ctx.fill();
                } else {
                  ctx.beginPath(); ctx.arc(outcomeScreenX, outcomeScreenY, 14, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(outcomeScreenX - 6, outcomeScreenY); ctx.lineTo(outcomeScreenX - 1, outcomeScreenY + 5); ctx.lineTo(outcomeScreenX + 7, outcomeScreenY - 6); ctx.stroke();
                }
                var outcomeLabel = String(activeOutcomePulse.label || 'STATE UPDATED');
                var outcomeDetail = String(activeOutcomePulse.detail || 'TISSUE RESPONSE RECORDED');
                ctx.font = 'bold ' + (outcomeAccessible ? '9' : '8') + 'px Inter, system-ui';
                var outcomeTextWidth = Math.min(190, Math.max(ctx.measureText(outcomeLabel).width, ctx.measureText(outcomeDetail).width) + 14);
                var outcomeLabelX = Math.max(5, Math.min(W - outcomeTextWidth - 5, outcomeScreenX - outcomeTextWidth / 2));
                var outcomeLabelY = outcomeScreenY > 66 ? outcomeScreenY - 57 : outcomeScreenY + 22;
                ctx.fillStyle = outcomeAccessible ? '#000000' : 'rgba(15,23,42,0.94)';
                ctx.beginPath();
                if (ctx.roundRect) ctx.roundRect(outcomeLabelX, outcomeLabelY, outcomeTextWidth, 34, 6); else ctx.rect(outcomeLabelX, outcomeLabelY, outcomeTextWidth, 34);
                ctx.fill(); ctx.strokeStyle = outcomeColor; ctx.stroke();
                ctx.fillStyle = outcomeAccessible ? '#ffffff' : outcomeColor; ctx.fillText(outcomeLabel.slice(0, 30), outcomeLabelX + 7, outcomeLabelY + 13);
                ctx.font = (outcomeAccessible ? '9' : '8') + 'px Inter, system-ui'; ctx.fillStyle = outcomeAccessible ? '#ffffff' : '#e2e8f0';
                ctx.fillText(outcomeDetail.slice(0, 34), outcomeLabelX + 7, outcomeLabelY + 26);
                ctx.restore();
              }
              if (screenPointer) {
                var pointerScale = specimenScaleFactors();
                var displayedPointerX = (screenPointer.x - 0.5) * pointerScale.x + 0.5;
                var displayedPointerY = (screenPointer.y - 0.45) * pointerScale.y + 0.45;
                var pointerScreenX = (displayedPointerX * W + parallaxX - W / 2) * zoom + W / 2 + panX;
                var pointerScreenY = (displayedPointerY * H + parallaxY - H / 2) * zoom + H / 2 + panY;
                var activeGestureContext = canvas._toolGestureContext || [canvas._forcepsDrag, canvas._pinDrag, canvas._probeDrag, canvas._dropperDrag, canvas._wickDrag].map(function (drag) { return drag && drag.active ? drag.gestureContext : null; }).find(Boolean);
                var activePointerTool = (activeGestureContext && activeGestureContext.tool) || d.activeInstrument || 'probe';
                var pointerInFrame = pointerScreenX > 0 && pointerScreenX < W && pointerScreenY > 0 && pointerScreenY < H;
                var contactPressure = Math.max(0.08, Math.min(1, Number(canvas._toolPressure) || 0.12));
                var contactResistance = canvas._toolResistance ? canvas._toolResistance.value : 0.1;
                var liveContactContext = specimenContactContext(screenPointer);

                // Shape-coded intent reticle previews whether contact will act, wait, or protect anatomy before commitment.
                var toolIntentState = canvas._toolIntentState;
                if (instrumentVisuals && toolIntentState && toolIntentState.tool === activePointerTool && !pointerIsSynthetic && !canvas._toolDrawing && pointerInFrame && !d.quizMode && !d.annotateMode && !d.rulerMode) {
                  var intentAccessible = highContrastEnabled || liveVisualMode === 'accessible';
                  var intentReady = !!toolIntentState.valid;
                  var intentLabel = toolIntentState.label || (intentReady ? 'READY' : 'WAIT');
                  var intentColor = intentAccessible ? '#ffffff' : (intentReady ? '#2dd4bf' : (intentLabel === 'PROTECT' ? '#fb7185' : '#fbbf24'));
                  var intentRadius = intentAccessible ? 15 : 13;
                  var intentTarget = toolIntentState.target;
                  if (!intentReady && intentTarget && isFinite(intentTarget.x) && isFinite(intentTarget.y)) {
                    var intentTargetDisplayedX = (intentTarget.x - 0.5) * pointerScale.x + 0.5;
                    var intentTargetDisplayedY = (intentTarget.y - 0.45) * pointerScale.y + 0.45;
                    var intentTargetScreenX = (intentTargetDisplayedX * W + parallaxX - W / 2) * zoom + W / 2 + panX;
                    var intentTargetScreenY = (intentTargetDisplayedY * H + parallaxY - H / 2) * zoom + H / 2 + panY;
                    var intentGuideDX = intentTargetScreenX - pointerScreenX, intentGuideDY = intentTargetScreenY - pointerScreenY;
                    var intentGuideDistance = Math.sqrt(intentGuideDX * intentGuideDX + intentGuideDY * intentGuideDY);
                    if (intentGuideDistance > intentRadius * 2.4) {
                      var intentGuideUX = intentGuideDX / intentGuideDistance, intentGuideUY = intentGuideDY / intentGuideDistance;
                      var intentGuideColor = intentAccessible ? '#ffffff' : '#67e8f9';
                      var intentTargetLabel = (toolIntentState.targetLabel || 'TARGET').toUpperCase();
                      ctx.save();
                      ctx.strokeStyle = intentGuideColor; ctx.fillStyle = intentGuideColor;
                      ctx.globalAlpha = intentAccessible ? 1 : 0.92; ctx.lineWidth = intentAccessible ? 2.6 : 1.8;
                      ctx.setLineDash([6, 5]);
                      ctx.beginPath();
                      ctx.moveTo(pointerScreenX + intentGuideUX * (intentRadius + 4), pointerScreenY + intentGuideUY * (intentRadius + 4));
                      ctx.lineTo(intentTargetScreenX - intentGuideUX * 11, intentTargetScreenY - intentGuideUY * 11);
                      ctx.stroke(); ctx.setLineDash([]);
                      ctx.beginPath();
                      ctx.moveTo(intentTargetScreenX, intentTargetScreenY);
                      ctx.lineTo(intentTargetScreenX - intentGuideUX * 12 - intentGuideUY * 5, intentTargetScreenY - intentGuideUY * 12 + intentGuideUX * 5);
                      ctx.lineTo(intentTargetScreenX - intentGuideUX * 12 + intentGuideUY * 5, intentTargetScreenY - intentGuideUY * 12 - intentGuideUX * 5);
                      ctx.closePath(); ctx.fill();
                      ctx.beginPath(); ctx.arc(intentTargetScreenX, intentTargetScreenY, intentAccessible ? 9 : 8, 0, Math.PI * 2); ctx.stroke();
                      ctx.beginPath(); ctx.moveTo(intentTargetScreenX - 4, intentTargetScreenY); ctx.lineTo(intentTargetScreenX + 4, intentTargetScreenY); ctx.moveTo(intentTargetScreenX, intentTargetScreenY - 4); ctx.lineTo(intentTargetScreenX, intentTargetScreenY + 4); ctx.stroke();
                      ctx.font = 'bold ' + (intentAccessible ? '9' : '8') + 'px Inter, system-ui'; ctx.textAlign = 'center';
                      var intentTargetTextWidth = ctx.measureText(intentTargetLabel).width + 10;
                      var intentTargetTextX = Math.max(intentTargetTextWidth / 2 + 4, Math.min(W - intentTargetTextWidth / 2 - 4, intentTargetScreenX));
                      var intentTargetTextY = intentTargetScreenY > 34 ? intentTargetScreenY - 29 : intentTargetScreenY + 18;
                      ctx.fillStyle = intentAccessible ? '#020617' : 'rgba(15,23,42,0.9)';
                      if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(intentTargetTextX - intentTargetTextWidth / 2, intentTargetTextY, intentTargetTextWidth, 16, 4); ctx.fill(); }
                      else ctx.fillRect(intentTargetTextX - intentTargetTextWidth / 2, intentTargetTextY, intentTargetTextWidth, 16);
                      ctx.fillStyle = intentAccessible ? '#f8fafc' : intentGuideColor; ctx.fillText(intentTargetLabel, intentTargetTextX, intentTargetTextY + 11);
                      ctx.restore();
                    }
                  }
                  ctx.save(); ctx.translate(pointerScreenX, pointerScreenY);
                  ctx.strokeStyle = intentColor; ctx.fillStyle = intentAccessible ? 'rgba(2,6,23,0.86)' : 'rgba(15,23,42,0.58)';
                  ctx.lineWidth = intentAccessible ? 2.5 : 1.7;
                  if (intentReady) {
                    ctx.beginPath(); ctx.arc(0, 0, intentRadius, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(-5, 0); ctx.lineTo(-1, 4); ctx.lineTo(6, -5); ctx.stroke();
                  } else if (intentLabel === 'PROTECT') {
                    ctx.beginPath(); ctx.moveTo(0, -intentRadius); ctx.lineTo(intentRadius, intentRadius * 0.78); ctx.lineTo(-intentRadius, intentRadius * 0.78); ctx.closePath(); ctx.fill(); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(0, -6); ctx.lineTo(0, 4); ctx.stroke(); ctx.beginPath(); ctx.arc(0, 8, 1.4, 0, Math.PI * 2); ctx.fillStyle = intentColor; ctx.fill();
                  } else {
                    ctx.setLineDash(intentLabel === 'TRAY' ? [3, 3] : []);
                    ctx.beginPath(); ctx.moveTo(0, -intentRadius); ctx.lineTo(intentRadius, 0); ctx.lineTo(0, intentRadius); ctx.lineTo(-intentRadius, 0); ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.setLineDash([]);
                    ctx.beginPath(); ctx.moveTo(-5, -5); ctx.lineTo(5, 5); ctx.moveTo(5, -5); ctx.lineTo(-5, 5); ctx.stroke();
                  }
                  ctx.font = 'bold ' + (intentAccessible ? '9' : '8') + 'px Inter, system-ui'; ctx.textAlign = 'center';
                  var intentTextWidth = ctx.measureText(intentLabel).width + 10;
                  ctx.fillStyle = intentAccessible ? '#020617' : 'rgba(15,23,42,0.88)';
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(-intentTextWidth / 2, intentRadius + 5, intentTextWidth, 16, 4); ctx.fill(); }
                  else ctx.fillRect(-intentTextWidth / 2, intentRadius + 5, intentTextWidth, 16);
                  ctx.fillStyle = intentAccessible ? '#f8fafc' : intentColor; ctx.fillText(intentLabel, 0, intentRadius + 17);
                  ctx.restore();
                }

                // A pressure-weighted live trail shows both stroke placement and corridor alignment before a cut is committed.
                if (instrumentVisuals && canvas._toolDrawing && canvas._toolStroke && canvas._toolStroke.length > 1 && (activePointerTool === 'scalpel' || activePointerTool === 'scissors')) {
                  var liveStrokePoints = canvas._toolStroke.slice(-42);
                  var liveStrokeSamples = (canvas._toolSamples || []).slice(-42);
                  var liveStrokeGuide = procedureGuidePoints();
                  ctx.save(); ctx.lineCap = 'round'; ctx.lineJoin = 'round';
                  for (var liveStrokeIdx = 1; liveStrokeIdx < liveStrokePoints.length; liveStrokeIdx++) {
                    var liveStrokeFrom = liveStrokePoints[liveStrokeIdx - 1], liveStrokeTo = liveStrokePoints[liveStrokeIdx];
                    var liveFromX = ((liveStrokeFrom.x - 0.5) * pointerScale.x + 0.5) * W;
                    var liveFromY = ((liveStrokeFrom.y - 0.45) * pointerScale.y + 0.45) * H;
                    var liveToX = ((liveStrokeTo.x - 0.5) * pointerScale.x + 0.5) * W;
                    var liveToY = ((liveStrokeTo.y - 0.45) * pointerScale.y + 0.45) * H;
                    liveFromX = (liveFromX + parallaxX - W / 2) * zoom + W / 2 + panX;
                    liveFromY = (liveFromY + parallaxY - H / 2) * zoom + H / 2 + panY;
                    liveToX = (liveToX + parallaxX - W / 2) * zoom + W / 2 + panX;
                    liveToY = (liveToY + parallaxY - H / 2) * zoom + H / 2 + panY;
                    var liveStrokeDistance = distanceToGuide(liveStrokeTo, liveStrokeGuide);
                    var liveStrokePressure = Math.max(0.08, Math.min(1, Number((liveStrokeSamples[liveStrokeIdx] || {}).pressure) || contactPressure));
                    ctx.strokeStyle = liveStrokeDistance <= 0.04 ? '#2dd4bf' : (liveStrokeDistance <= 0.075 ? '#facc15' : '#fb7185');
                    ctx.globalAlpha = 0.42 + liveStrokePressure * 0.46;
                    ctx.lineWidth = (activePointerTool === 'scissors' ? 2.4 : 1.6) + liveStrokePressure * 2.2;
                    ctx.beginPath(); ctx.moveTo(liveFromX, liveFromY); ctx.lineTo(liveToX, liveToY); ctx.stroke();
                  }
                  var liveLeadDistance = distanceToGuide(liveStrokePoints[liveStrokePoints.length - 1], liveStrokeGuide);
                  var liveLeadLabel = liveLeadDistance <= 0.04 ? 'ON PATH' : (liveLeadDistance <= 0.075 ? 'EDGE' : 'DRIFT');
                  ctx.globalAlpha = 0.9; ctx.font = 'bold 7px Inter, system-ui';
                  ctx.fillStyle = liveLeadDistance <= 0.04 ? '#99f6e4' : (liveLeadDistance <= 0.075 ? '#fde68a' : '#fecdd3');
                  ctx.fillText(liveLeadLabel, pointerScreenX + 11, pointerScreenY - 13);
                  ctx.restore();
                }

                // Organism-aware contact deformation makes every instrument read as physically connected to the specimen.
                if (instrumentVisuals && pointerInFrame && (!pointerIsSynthetic || activeContactPulse) && !d.quizMode && !d.annotateMode && !d.rulerMode) {
                  var toolMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                  var contactOnSpecimen = liveContactContext.onSpecimen;
                  var underTipOrgan = liveContactContext.organ;
                  var responseTool = activeContactPulse && pointerIsSynthetic ? activeContactPulse.tool : activePointerTool;
                  var cuttingAtProtectedStructure = contactOnSpecimen && !!underTipOrgan && (responseTool === 'scalpel' || responseTool === 'scissors');
                  var contactAccent = cuttingAtProtectedStructure ? '#fb7185' : (contactOnSpecimen ? toolMaterial.accent : '#94a3b8');
                  var pulseProgress = contactPulseProgress;
                  var pulseStrength = activeContactPulse ? (dissMotionReduced ? 0.52 : Math.sin(pulseProgress * Math.PI)) : 0;
                  var calibratedContactPressure = responseTool === 'probe'
                    ? toolCalibration.probePressure / 100
                    : (responseTool === 'forceps' ? toolCalibration.forcepsGrip / 100 * 0.82 : contactPressure);
                  var surfacePressure = Math.max(contactPressure, calibratedContactPressure, pulseStrength * 0.78);
                  var surfaceCompliance = contactOnSpecimen ? Math.max(0.12, Number(toolMaterial.compliance) || 0.5) : 0.08;
                  var contactRadius = 10 + surfacePressure * (6 + surfaceCompliance * 8) + contactResistance * 4;
                  var contactOrientation = toolMaterial.rx < toolMaterial.ry ? Math.PI / 2 : 0;
                  var previewGuide = procedureGuidePoints();
                  var previewEndpointDistance = Math.min(procedurePointDistance(screenPointer, previewGuide[0]), procedurePointDistance(screenPointer, previewGuide[previewGuide.length - 1]));
                  var previewGuideDistance = distanceToGuide(screenPointer, previewGuide);
                  var previewPins = currentProcedure.pins || [];
                  var previewPinSpacing = previewPins.length ? procedurePointDistance(screenPointer, previewPins[0]) : 1;
                  var activeContactIntent = !pointerIsSynthetic && canvas._toolIntentState && canvas._toolIntentState.tool === responseTool ? canvas._toolIntentState : null;
                  var forcepsPreviewValid = currentProcedure.incisionExtended && !currentProcedure.retracted && previewGuideDistance <= 0.11;
                  if (activeContactIntent && responseTool === 'forceps') forcepsPreviewValid = !!activeContactIntent.valid;
                  var pinPreviewPrerequisite = procedureProtocol.preStabilize ? currentProcedure.inspected : currentProcedure.retracted;
                  var pinPreviewValid = pinPreviewPrerequisite && previewPins.length < 2 && previewEndpointDistance <= 0.14 && previewPinSpacing >= 0.18;
                  if (activeContactIntent && responseTool === 'pin') pinPreviewValid = !!activeContactIntent.valid;
                  var pinAnglePreview = toolCalibration.pinAngle < 55 ? 'SHALLOW SLIP' : (toolCalibration.pinAngle > 75 ? 'STEEP STRESS' : 'STABLE');
                  var pinTechniqueValid = pinPreviewValid && toolCalibration.pinAngle >= 55 && toolCalibration.pinAngle <= 75;
                  var probePreviewMode = nextProcedureInfo().action === 'inspect' ? 'inspect' : 'trace';
                  var probePreviewPrerequisite = probePreviewMode === 'inspect' || previewPins.length >= 2;
                  var probePreviewValid = probePreviewPrerequisite && !!underTipOrgan;
                  if (activeContactIntent && responseTool === 'probe') probePreviewValid = !!activeContactIntent.valid;
                  var probePressurePreview = toolCalibration.probePressure < 18 ? 'TOO LIGHT' : (toolCalibration.probePressure > 38 ? 'EXCESS PRESSURE' : 'CONTROLLED');
                  var probeTechniqueValid = probePreviewValid && toolCalibration.probePressure >= 18 && toolCalibration.probePressure <= 38;
                  var probeDepthPreview = underTipOrgan ? anatomicalDepthLabel(structureOpticalDepth(underTipOrgan)).toUpperCase() : '';
                  var previewTissue = normalizeTissueState(currentProcedure.tissueState);
                  var forcepsGripPreview = toolCalibration.forcepsGrip < 25 ? 'SLIP RISK' : ((toolCalibration.forcepsGrip > 50 || previewTissue.tension > 82) ? 'HIGH STRESS' : 'CONTROLLED');
                  var dropperPreviewValid = contactOnSpecimen && previewTissue.moisture < 96;
                  var dropperDosePreview = previewTissue.moisture >= 96 ? 'SATURATED' : (toolCalibration.salineDose === 1 ? 'CONTROLLED FILM' : (toolCalibration.salineDose === 2 ? 'BROAD SPREAD' : 'POOLING RISK'));
                  var dropperTechniqueValid = dropperPreviewValid && toolCalibration.salineDose === 1;
                  var wickPoolPoint = currentProcedure.dropperPoint;
                  var wickPreviewDistance = wickPoolPoint ? procedurePointDistance(screenPointer, wickPoolPoint) : 1;
                  var wickPreviewValid = contactOnSpecimen && !!wickPoolPoint && previewTissue.salineDrops > 2 && wickPreviewDistance <= 0.16;
                  var trajectorySafety = (responseTool === 'scalpel' || responseTool === 'scissors') && contactOnSpecimen
                    ? cuttingTrajectorySafety(screenPointer, canvas._toolVector, canvas)
                    : null;
                  var interactionPreviewLabel = responseTool === 'forceps'
                    ? (forcepsPreviewValid ? 'LIFT PREVIEW: ' + forcepsGripPreview : 'MOVE FORCEPS TO OPENING')
                    : (responseTool === 'pin'
                      ? (pinPreviewValid ? 'ANCHOR PREVIEW: ' + pinAnglePreview : (previewPinSpacing < 0.18 ? 'ANCHOR TOO CLOSE' : 'MOVE PIN TO ENDPOINT'))
                      : (responseTool === 'probe'
                        ? (probePreviewValid ? (probePreviewMode === 'inspect' ? 'INSPECT: ' + underTipOrgan.name : 'PALPATE: ' + underTipOrgan.name + ' \u00B7 ' + probePressurePreview + ' \u00B7 ' + probeDepthPreview) : 'SELECT VISIBLE STRUCTURE')
                        : (responseTool === 'dropper'
                          ? (dropperPreviewValid ? 'DROP PREVIEW: ' + dropperDosePreview : (previewTissue.moisture >= 96 ? 'SURFACE MOISTURE OPTIMAL' : 'MOVE DROPPER TO SPECIMEN'))
                          : (responseTool === 'wick' ? (wickPreviewValid ? 'WICK PREVIEW: POOL EDGE' : (previewTissue.salineDrops > 2 ? 'MOVE WICK TO POOL EDGE' : 'NO EXCESS FLUID')) : ''))));
                  if ((responseTool === 'scalpel' || responseTool === 'scissors') && trajectorySafety) {
                    interactionPreviewLabel = (trajectorySafety.critical ? 'STOP: PROTECT ' : 'LANDMARK AHEAD: ') + trajectorySafety.organ.name;
                  }

                  // The projected safety envelope shows direction, clearance, and the first anatomical intercept without relying on color alone.
                  if ((responseTool === 'scalpel' || responseTool === 'scissors') && contactOnSpecimen) {
                    var safetyVector = canvas._toolVector;
                    if (!safetyVector || Math.abs(safetyVector.x) + Math.abs(safetyVector.y) <= 0.0015) {
                      safetyVector = { x: previewGuide[previewGuide.length - 1].x - previewGuide[0].x, y: previewGuide[previewGuide.length - 1].y - previewGuide[0].y };
                    }
                    var safetyScreenDX = safetyVector.x * pointerScale.x * W, safetyScreenDY = safetyVector.y * pointerScale.y * H;
                    var safetyScreenLength = Math.sqrt(safetyScreenDX * safetyScreenDX + safetyScreenDY * safetyScreenDY) || 1;
                    var safetyUnitX = safetyScreenDX / safetyScreenLength, safetyUnitY = safetyScreenDY / safetyScreenLength;
                    var safetyForecastLength = 54 + surfacePressure * 18;
                    var safetyEndX = pointerScreenX + safetyUnitX * safetyForecastLength, safetyEndY = pointerScreenY + safetyUnitY * safetyForecastLength;
                    var safetyNormalX = -safetyUnitY, safetyNormalY = safetyUnitX;
                    var safetyAccessible = highContrastEnabled || liveVisualMode === 'accessible';
                    var safetyCritical = !!(trajectorySafety && trajectorySafety.critical);
                    var safetyColor = trajectorySafety ? (safetyCritical ? '#fb7185' : '#facc15') : '#5eead4';
                    ctx.save(); ctx.strokeStyle = safetyAccessible ? '#ffffff' : safetyColor;
                    ctx.fillStyle = safetyAccessible ? 'rgba(0,0,0,0.18)' : (trajectorySafety ? 'rgba(251,191,36,0.07)' : 'rgba(45,212,191,0.05)');
                    ctx.lineWidth = safetyAccessible ? 2.1 : 1.15; ctx.setLineDash(trajectorySafety ? [4, 3] : [7, 5]);
                    ctx.beginPath(); ctx.moveTo(pointerScreenX, pointerScreenY);
                    ctx.lineTo(safetyEndX + safetyNormalX * 9, safetyEndY + safetyNormalY * 9);
                    ctx.lineTo(safetyEndX - safetyNormalX * 9, safetyEndY - safetyNormalY * 9); ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.setLineDash([]);
                    ctx.beginPath(); ctx.moveTo(pointerScreenX + safetyUnitX * 8, pointerScreenY + safetyUnitY * 8); ctx.lineTo(safetyEndX, safetyEndY); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(safetyEndX - safetyUnitX * 7 + safetyNormalX * 4, safetyEndY - safetyUnitY * 7 + safetyNormalY * 4);
                    ctx.lineTo(safetyEndX, safetyEndY);
                    ctx.lineTo(safetyEndX - safetyUnitX * 7 - safetyNormalX * 4, safetyEndY - safetyUnitY * 7 - safetyNormalY * 4); ctx.stroke();
                    if (trajectorySafety) {
                      var safetyTargetX = (((trajectorySafety.point.x - 0.5) * pointerScale.x + 0.5) * W + parallaxX - W / 2) * zoom + W / 2 + panX;
                      var safetyTargetY = (((trajectorySafety.point.y - 0.45) * pointerScale.y + 0.45) * H + parallaxY - H / 2) * zoom + H / 2 + panY;
                      ctx.strokeStyle = safetyAccessible ? '#facc15' : safetyColor; ctx.lineWidth = safetyAccessible ? 2.6 : 1.8;
                      ctx.setLineDash(safetyCritical ? [3, 2] : [6, 4]);
                      ctx.beginPath(); ctx.arc(safetyTargetX, safetyTargetY, safetyCritical ? 19 : 15, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                      ctx.beginPath(); ctx.moveTo(safetyTargetX - 11, safetyTargetY + 10); ctx.lineTo(safetyTargetX, safetyTargetY - 10); ctx.lineTo(safetyTargetX + 11, safetyTargetY + 10); ctx.closePath(); ctx.stroke();
                      ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = safetyAccessible ? '#facc15' : safetyColor; ctx.fillText('!', safetyTargetX - 2, safetyTargetY + 6);
                      var safetyLabel = (safetyCritical ? (trajectorySafety.protocolProtected ? 'PROTECT · ' : 'STOP · ') : 'AHEAD · ') + trajectorySafety.organ.name;
                      var safetyLabelWidth = Math.min(150, Math.ceil(ctx.measureText(safetyLabel).width) + 12);
                      var safetyLabelX = Math.max(8, Math.min(W - safetyLabelWidth - 8, safetyTargetX + 19));
                      var safetyLabelY = Math.max(8, Math.min(H - 24, safetyTargetY - 22));
                      ctx.fillStyle = safetyAccessible ? '#000000' : 'rgba(15,23,42,0.92)';
                      ctx.beginPath();
                      if (ctx.roundRect) ctx.roundRect(safetyLabelX, safetyLabelY, safetyLabelWidth, 18, 5); else ctx.rect(safetyLabelX, safetyLabelY, safetyLabelWidth, 18);
                      ctx.fill(); ctx.strokeStyle = safetyColor; ctx.lineWidth = 1; ctx.stroke();
                      ctx.fillStyle = safetyAccessible ? '#ffffff' : (safetyCritical ? '#fecdd3' : '#fef3c7');
                      ctx.font = 'bold 8px Inter, system-ui'; ctx.fillText(safetyLabel.slice(0, 25), safetyLabelX + 6, safetyLabelY + 12);
                    } else {
                      ctx.strokeStyle = safetyAccessible ? '#ffffff' : safetyColor; ctx.lineWidth = 1.8;
                      ctx.beginPath(); ctx.moveTo(safetyEndX - 5, safetyEndY); ctx.lineTo(safetyEndX - 1, safetyEndY + 4); ctx.lineTo(safetyEndX + 6, safetyEndY - 5); ctx.stroke();
                    }
                    ctx.restore();
                  }
                  ctx.save(); ctx.translate(pointerScreenX, pointerScreenY); ctx.rotate(contactOrientation);
                  var indentationScaleY = Math.max(0.24, 0.52 - surfacePressure * surfaceCompliance * 0.18);
                  ctx.scale(1 + surfaceCompliance * 0.18, indentationScaleY);
                  var contactShadow = ctx.createRadialGradient(-2, -2, 1, 0, 0, contactRadius);
                  contactShadow.addColorStop(0, 'rgba(2,6,23,' + (0.20 + surfacePressure * 0.30).toFixed(2) + ')');
                  contactShadow.addColorStop(0.5, 'rgba(15,23,42,0.15)');
                  contactShadow.addColorStop(0.78, 'rgba(255,255,255,' + (0.04 + toolMaterial.moisture * 0.09).toFixed(2) + ')');
                  contactShadow.addColorStop(1, 'rgba(15,23,42,0)');
                  ctx.fillStyle = contactShadow; ctx.beginPath(); ctx.arc(0, 0, contactRadius, 0, Math.PI * 2); ctx.fill();
                  ctx.strokeStyle = canvas._toolDrawing ? (cuttingAtProtectedStructure ? '#fb7185' : '#facc15') : contactAccent;
                  ctx.globalAlpha = 0.44 + surfacePressure * 0.34; ctx.lineWidth = liveVisualMode === 'accessible' ? 2.2 : 1.15;
                  ctx.beginPath(); ctx.arc(0, 0, 5 + surfacePressure * 4, 0, Math.PI * 2); ctx.stroke();
                  ctx.restore();

                  // Surface microstructure bends or shifts around the contact point according to the specimen material.
                  ctx.save(); ctx.translate(pointerScreenX, pointerScreenY); ctx.strokeStyle = contactAccent; ctx.fillStyle = contactAccent;
                  ctx.globalAlpha = liveVisualMode === 'accessible' ? 0.78 : 0.46; ctx.lineWidth = liveVisualMode === 'accessible' ? 1.8 : 0.9;
                  if (!contactOnSpecimen) {
                    ctx.setLineDash([3, 3]);
                    ctx.beginPath(); ctx.moveTo(-12, -7); ctx.lineTo(-12, -12); ctx.lineTo(-7, -12);
                    ctx.moveTo(12, -7); ctx.lineTo(12, -12); ctx.lineTo(7, -12);
                    ctx.moveTo(-12, 7); ctx.lineTo(-12, 12); ctx.lineTo(-7, 12);
                    ctx.moveTo(12, 7); ctx.lineTo(12, 12); ctx.lineTo(7, 12); ctx.stroke(); ctx.setLineDash([]);
                  } else if (toolMaterial.pattern === 'segments') {
                    for (var segmentContactIdx = -1; segmentContactIdx <= 1; segmentContactIdx++) {
                      var segmentContactY = segmentContactIdx * 6;
                      ctx.beginPath(); ctx.moveTo(-13, segmentContactY); ctx.quadraticCurveTo(0, segmentContactY + surfacePressure * 4, 13, segmentContactY); ctx.stroke();
                    }
                  } else if (toolMaterial.pattern === 'scales') {
                    for (var scaleContactIdx = -1; scaleContactIdx <= 1; scaleContactIdx++) {
                      ctx.beginPath(); ctx.arc(scaleContactIdx * 8, 1 + Math.abs(scaleContactIdx) * 2, 7, Math.PI * 1.12, Math.PI * 1.88); ctx.stroke();
                    }
                  } else if (toolMaterial.pattern === 'facets') {
                    ctx.beginPath();
                    for (var facetContactIdx = 0; facetContactIdx < 6; facetContactIdx++) {
                      var facetAngle = facetContactIdx * Math.PI / 3;
                      var facetX = Math.cos(facetAngle) * (9 + contactResistance * 3), facetY = Math.sin(facetAngle) * (9 + contactResistance * 3);
                      if (!facetContactIdx) ctx.moveTo(facetX, facetY); else ctx.lineTo(facetX, facetY);
                    }
                    ctx.closePath(); ctx.stroke();
                  } else if (toolMaterial.pattern === 'radial') {
                    for (var radialContactIdx = 0; radialContactIdx < 3; radialContactIdx++) {
                      ctx.globalAlpha = 0.48 - radialContactIdx * 0.11; ctx.beginPath(); ctx.arc(0, 0, 7 + radialContactIdx * 6 + pulseStrength * 4, 0, Math.PI * 2); ctx.stroke();
                    }
                  } else if (toolMaterial.pattern === 'fibers') {
                    ctx.rotate(-0.42);
                    for (var fiberContactIdx = -2; fiberContactIdx <= 2; fiberContactIdx++) {
                      ctx.beginPath(); ctx.moveTo(-13, fiberContactIdx * 4); ctx.quadraticCurveTo(0, fiberContactIdx * 4 + surfacePressure * 3, 13, fiberContactIdx * 4); ctx.stroke();
                    }
                  } else if (toolMaterial.pattern === 'follicles') {
                    for (var follicleContactIdx = 0; follicleContactIdx < 7; follicleContactIdx++) {
                      var follicleAngle = follicleContactIdx * 2.4;
                      ctx.beginPath(); ctx.arc(Math.cos(follicleAngle) * (7 + follicleContactIdx), Math.sin(follicleAngle) * (5 + follicleContactIdx * 0.6), 1, 0, Math.PI * 2); ctx.fill();
                    }
                    ctx.beginPath(); ctx.moveTo(-15, 0); ctx.quadraticCurveTo(0, surfacePressure * 5, 15, 0); ctx.stroke();
                  } else {
                    for (var pigmentContactIdx = 0; pigmentContactIdx < 6; pigmentContactIdx++) {
                      var pigmentAngle = pigmentContactIdx * Math.PI / 3;
                      ctx.beginPath(); ctx.arc(Math.cos(pigmentAngle) * 11, Math.sin(pigmentAngle) * 7, 1.4, 0, Math.PI * 2); ctx.fill();
                    }
                  }
                  ctx.restore();

                  // Each tool adds a recognizable contact cue without relying on color alone.
                  ctx.save(); ctx.translate(pointerScreenX, pointerScreenY); ctx.strokeStyle = '#f8fafc'; ctx.fillStyle = '#f8fafc'; ctx.globalAlpha = 0.68; ctx.lineWidth = 1.2;
                  if (responseTool === 'forceps') {
                    ctx.strokeStyle = forcepsPreviewValid ? '#99f6e4' : '#fbbf24';
                    ctx.beginPath(); ctx.moveTo(-11, -8); ctx.lineTo(-3, 0); ctx.lineTo(-11, 8); ctx.moveTo(11, -8); ctx.lineTo(3, 0); ctx.lineTo(11, 8); ctx.stroke();
                    ctx.globalAlpha = 0.30; ctx.fillStyle = forcepsPreviewValid ? '#5eead4' : '#fbbf24';
                    ctx.beginPath(); ctx.moveTo(-13, 4); ctx.quadraticCurveTo(0, -13 - surfacePressure * 5, 13, 4); ctx.quadraticCurveTo(0, -3, -13, 4); ctx.fill();
                    ctx.globalAlpha = 0.76; ctx.beginPath(); ctx.moveTo(0, -6); ctx.lineTo(0, -18); ctx.moveTo(-3, -14); ctx.lineTo(0, -18); ctx.lineTo(3, -14); ctx.stroke();
                  } else if (responseTool === 'pin') {
                    ctx.strokeStyle = pinTechniqueValid ? '#7dd3fc' : '#fbbf24';
                    ctx.beginPath(); ctx.arc(0, 0, 8 + pulseStrength * 5, 0, Math.PI * 2); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(-13, 0); ctx.lineTo(13, 0); ctx.moveTo(0, -13); ctx.lineTo(0, 13); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(-7, 12); ctx.moveTo(0, 0); ctx.lineTo(7, 12); ctx.stroke();
                  } else if (responseTool === 'wick') {
                    ctx.strokeStyle = wickPreviewValid ? '#99f6e4' : '#fbbf24';
                    ctx.setLineDash(wickPreviewValid ? [] : [3, 2]);
                    ctx.strokeRect(-11, -7, 22, 14);
                    for (var wickFiber = -7; wickFiber <= 7; wickFiber += 7) {
                      ctx.beginPath(); ctx.moveTo(wickFiber - 4, -6); ctx.lineTo(wickFiber + 4, 6); ctx.stroke();
                    }
                    ctx.setLineDash([]);
                  } else if (responseTool === 'dropper') {
                    ctx.strokeStyle = dropperTechniqueValid ? '#7dd3fc' : '#fbbf24'; ctx.setLineDash(dropperTechniqueValid ? [] : [3, 2]);
                    for (var dropRippleIdx = 0; dropRippleIdx < 2; dropRippleIdx++) {
                      ctx.globalAlpha = (0.58 - dropRippleIdx * 0.2) * (1 - pulseProgress * 0.55);
                      ctx.beginPath(); ctx.ellipse(0, 0, 8 + dropRippleIdx * 7 + pulseProgress * 8, 4 + dropRippleIdx * 4 + pulseProgress * 4, 0, 0, Math.PI * 2); ctx.stroke();
                    }
                    ctx.setLineDash([]);
                  } else if (responseTool === 'scalpel' || responseTool === 'scissors') {
                    var strokeAngle = canvas._toolVector ? Math.atan2(canvas._toolVector.y * H, canvas._toolVector.x * W) : -0.72;
                    ctx.rotate(strokeAngle); ctx.beginPath(); ctx.moveTo(-13, 0); ctx.lineTo(13, 0); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(9, -3); ctx.lineTo(13, 0); ctx.lineTo(9, 3); ctx.stroke();
                  } else {
                    ctx.strokeStyle = responseTool === 'probe' && probeTechniqueValid ? '#99f6e4' : (responseTool === 'probe' && probePreviewValid ? '#fbbf24' : '#f8fafc');
                    ctx.beginPath(); ctx.arc(0, 0, 6 + surfacePressure * 4, 0, Math.PI * 2); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(-4, 0); ctx.lineTo(4, 0); ctx.moveTo(0, -4); ctx.lineTo(0, 4); ctx.stroke();
                    if (responseTool === 'probe' && probePreviewValid) {
                      for (var probePreviewCorner = 0; probePreviewCorner < 4; probePreviewCorner++) {
                        var probeCornerAngle = probePreviewCorner * Math.PI / 2;
                        ctx.beginPath(); ctx.arc(0, 0, 14, probeCornerAngle + 0.12, probeCornerAngle + 0.72); ctx.stroke();
                      }
                    }
                  }
                  if (activeContactPulse) {
                    ctx.globalAlpha = Math.max(0, 0.72 * (1 - pulseProgress));
                    ctx.beginPath(); ctx.arc(0, 0, 10 + pulseProgress * 22, 0, Math.PI * 2); ctx.stroke();
                  }
                  if (cuttingAtProtectedStructure) {
                    ctx.globalAlpha = 0.92; ctx.strokeStyle = '#fb7185'; ctx.lineWidth = 2; ctx.setLineDash([3, 2]);
                    ctx.beginPath(); ctx.arc(0, 0, 16, 0, Math.PI * 2); ctx.stroke(); ctx.setLineDash([]);
                    ctx.beginPath(); ctx.moveTo(-5, -5); ctx.lineTo(5, 5); ctx.moveTo(5, -5); ctx.lineTo(-5, 5); ctx.stroke();
                  }
                  ctx.restore();

                  if (canvas._toolDrawing || activeContactPulse || ['forceps', 'pin', 'probe', 'dropper'].indexOf(responseTool) >= 0) {
                    var responseLabelX = pointerScreenX > W - 150 ? pointerScreenX - 138 : pointerScreenX + 14;
                    var responseLabelY = pointerScreenY > H - 58 ? pointerScreenY - 43 : pointerScreenY + 14;
                    ctx.save(); ctx.fillStyle = 'rgba(15,23,42,0.90)';
                    if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(responseLabelX, responseLabelY, 132, 43, 6); ctx.fill(); }
                    else ctx.fillRect(responseLabelX, responseLabelY, 132, 43);
                    ctx.font = 'bold 8px Inter, system-ui'; ctx.fillStyle = contactAccent;
                    var materialResponseLabel = contactOnSpecimen ? toolMaterial.response.toUpperCase() : 'TRAY SURFACE';
                    ctx.fillText(materialResponseLabel, responseLabelX + 7, responseLabelY + 12);
                    ctx.font = '7px Inter, system-ui'; ctx.fillStyle = '#e2e8f0';
                    var resistanceWord = contactResistance < 0.34 ? 'low' : (contactResistance < 0.68 ? 'moderate' : 'high');
                    var contactCalibration = instrumentCalibrationAssessment(responseTool, toolCalibration);
                    ctx.fillText(contactOnSpecimen ? (responseTool + ' | ' + contactCalibration.label.toLowerCase() + ' ' + contactCalibration.score + '% | ' + Math.round(surfacePressure * 100) + '%') : (responseTool + ' | no tissue response'), responseLabelX + 7, responseLabelY + 24);
                    ctx.fillStyle = cuttingAtProtectedStructure ? '#fecdd3' : '#cbd5e1';
                    var activeDepthLabel = (d.incisionDepth || 'shallow').toUpperCase();
                    var contactContextLabel = interactionPreviewLabel || (cuttingAtProtectedStructure
                      ? activeDepthLabel + ' / PROTECT ' + underTipOrgan.name
                      : (underTipOrgan ? 'Under tip: ' + underTipOrgan.name : (contactOnSpecimen ? activeDepthLabel + ' / ' + ((spec.layers[currentLayerIdx] || {}).name || activeLayer) : 'Move tip onto specimen')));
                    ctx.fillText(contactContextLabel.slice(0, 31), responseLabelX + 7, responseLabelY + 36);
                    ctx.restore();
                  }
                }

                var lensTargetPoint = !d.quizMode && d.lensPinned && d.lensPinnedPoint ? d.lensPinnedPoint : screenPointer;
                var lensTargetStructure = null;
                if (!d.quizMode && d.lensPinnedOrganId) {
                  lensTargetStructure = organs.find(function (organ) { return organ.id === d.lensPinnedOrganId; }) || null;
                  if (lensTargetStructure) lensTargetPoint = variedOrganPoint(lensTargetStructure);
                }
                if (!d.quizMode && !lensTargetStructure) {
                  lensTargetStructure = organs.find(function (organ) { return organ.id === d.hoveredOrgan; })
                    || organs.find(function (organ) { return organ.id === d.selectedOrgan; })
                    || (lensTargetPoint ? closestVisibleOrganAt(lensTargetPoint.x, lensTargetPoint.y, 0.075, canvas) : null);
                }
                var lensTargetScreenX = pointerScreenX, lensTargetScreenY = pointerScreenY;
                if (lensTargetPoint && lensTargetPoint !== screenPointer) {
                  var lensTargetScale = specimenScaleFactors();
                  var displayedLensTargetX = (lensTargetPoint.x - 0.5) * lensTargetScale.x + 0.5;
                  var displayedLensTargetY = (lensTargetPoint.y - 0.45) * lensTargetScale.y + 0.45;
                  lensTargetScreenX = (displayedLensTargetX * W + parallaxX - W / 2) * zoom + W / 2 + panX;
                  lensTargetScreenY = (displayedLensTargetY * H + parallaxY - H / 2) * zoom + H / 2 + panY;
                }
                var targetEase = dissMotionReduced ? 1 : 0.28;
                if (!isFinite(Number(canvas._lensDisplayX))) canvas._lensDisplayX = lensTargetScreenX;
                if (!isFinite(Number(canvas._lensDisplayY))) canvas._lensDisplayY = lensTargetScreenY;
                canvas._lensDisplayX += (lensTargetScreenX - canvas._lensDisplayX) * targetEase;
                canvas._lensDisplayY += (lensTargetScreenY - canvas._lensDisplayY) * targetEase;
                var lensDisplayX = canvas._lensDisplayX, lensDisplayY = canvas._lensDisplayY;
                var lensTargetInFrame = lensDisplayX > 0 && lensDisplayX < W && lensDisplayY > 0 && lensDisplayY < H;
                var lensBuffer = null, lensCtx = null;
                var lensSourceScale = Number(canvas._dpr) || 1;
                if (!d.quizMode && (d.inspectionLens || macroInset) && lensTargetInFrame) {
                  lensBuffer = canvas._dissLensBuffer;
                  if (!lensBuffer) { lensBuffer = document.createElement('canvas'); lensBuffer.setAttribute('aria-hidden', 'true'); canvas._dissLensBuffer = lensBuffer; }
                  if (lensBuffer.width !== canvas.width || lensBuffer.height !== canvas.height) { lensBuffer.width = canvas.width; lensBuffer.height = canvas.height; }
                  lensCtx = lensBuffer.getContext('2d');
                  if (lensCtx) {
                    lensCtx.setTransform(1, 0, 0, 1, 0, 0); lensCtx.clearRect(0, 0, lensBuffer.width, lensBuffer.height); lensCtx.drawImage(canvas, 0, 0);
                  }
                }
                var inspectionMagnification = [2, 4, 6].indexOf(Number(d.lensMagnification)) >= 0 ? Number(d.lensMagnification) : 2;
                var inspectionDepth = ['surface', 'structure', 'deep'].indexOf(d.lensFocusDepth) >= 0 ? d.lensFocusDepth : 'structure';
                var inspectionAccent = inspectionDepth === 'surface' ? '#fbbf24' : (inspectionDepth === 'deep' ? '#a78bfa' : '#22d3ee');
                var liveOpticalInspection = opticalInspectionData(lensTargetStructure, inspectionDepth, inspectionMagnification, !!d.lensPinned, liveProcedureState);
                var inspectionDetail = liveOpticalInspection.detail;
                var inspectionFocusBlur = liveVisualMode === 'accessible' ? 0 : Math.max(0, Math.min(2.6, (82 - liveOpticalInspection.quality) / 18));
                var inspectionQualityColor = liveOpticalInspection.quality >= 82 ? '#4ade80' : (liveOpticalInspection.quality >= 65 ? '#38bdf8' : (liveOpticalInspection.quality >= 45 ? '#fbbf24' : '#fb7185'));
                function drawOpticalDepthCues(regionX, regionY, regionW, regionH, depth, quality) {
                  ctx.save(); ctx.globalAlpha = 0.10 + quality / 650; ctx.strokeStyle = inspectionAccent; ctx.fillStyle = inspectionAccent; ctx.lineWidth = 0.75;
                  var cueCenterX = regionX + regionW / 2, cueCenterY = regionY + regionH / 2;
                  if (depth === 'surface') {
                    for (var surfaceCueIdx = 0; surfaceCueIdx < 12; surfaceCueIdx++) {
                      var surfaceCueAngle = surfaceCueIdx * 2.17;
                      var surfaceCueRadius = 8 + (surfaceCueIdx % 5) * Math.min(regionW, regionH) * 0.065;
                      ctx.beginPath(); ctx.arc(cueCenterX + Math.cos(surfaceCueAngle) * surfaceCueRadius, cueCenterY + Math.sin(surfaceCueAngle) * surfaceCueRadius * 0.72, 1.2 + (surfaceCueIdx % 3) * 0.45, 0, Math.PI * 2); ctx.fill();
                    }
                  } else if (depth === 'structure') {
                    for (var structureCueIdx = -3; structureCueIdx <= 3; structureCueIdx++) {
                      var structureCueY = cueCenterY + structureCueIdx * regionH * 0.085;
                      ctx.beginPath(); ctx.moveTo(regionX + regionW * 0.12, structureCueY);
                      ctx.bezierCurveTo(regionX + regionW * 0.36, structureCueY - 6, regionX + regionW * 0.64, structureCueY + 6, regionX + regionW * 0.88, structureCueY); ctx.stroke();
                    }
                  } else {
                    for (var deepCueIdx = 1; deepCueIdx <= 4; deepCueIdx++) {
                      ctx.beginPath(); ctx.ellipse(cueCenterX, cueCenterY, regionW * 0.11 * deepCueIdx, regionH * 0.085 * deepCueIdx, -0.18, 0, Math.PI * 2); ctx.stroke();
                    }
                    ctx.beginPath(); ctx.moveTo(regionX + regionW * 0.18, regionY + regionH * 0.72);
                    ctx.bezierCurveTo(regionX + regionW * 0.38, regionY + regionH * 0.34, regionX + regionW * 0.62, regionY + regionH * 0.68, regionX + regionW * 0.82, regionY + regionH * 0.28); ctx.stroke();
                  }
                  ctx.restore();
                }
                if (lensTargetInFrame && lensCtx) {
                  var sourceGuideRadius = Math.max(7, (liveVisualMode === 'accessible' ? 27 : 22) / Math.sqrt(inspectionMagnification / 2));
                  ctx.save(); ctx.strokeStyle = inspectionAccent; ctx.lineWidth = liveVisualMode === 'accessible' ? 3 : 1.5; ctx.setLineDash(d.lensPinned ? [] : [4, 3]);
                  ctx.beginPath(); ctx.arc(lensDisplayX, lensDisplayY, sourceGuideRadius, 0, Math.PI * 2); ctx.stroke();
                  ctx.beginPath(); ctx.moveTo(lensDisplayX - sourceGuideRadius - 5, lensDisplayY); ctx.lineTo(lensDisplayX + sourceGuideRadius + 5, lensDisplayY);
                  ctx.moveTo(lensDisplayX, lensDisplayY - sourceGuideRadius - 5); ctx.lineTo(lensDisplayX, lensDisplayY + sourceGuideRadius + 5); ctx.stroke();
                  if (d.lensPinned) {
                    ctx.fillStyle = inspectionAccent; ctx.beginPath(); ctx.arc(lensDisplayX, lensDisplayY, 3.5, 0, Math.PI * 2); ctx.fill();
                  }
                  ctx.setLineDash([]); ctx.restore();
                }
                if (!d.quizMode && d.inspectionLens && lensTargetInFrame && lensCtx) {
                  var lensRadius = liveVisualMode === 'accessible' ? 72 : 58;
                  var sourceRadius = lensRadius / inspectionMagnification;
                  ctx.save(); ctx.beginPath(); ctx.arc(lensDisplayX, lensDisplayY, lensRadius, 0, Math.PI * 2); ctx.clip();
                  ctx.filter = inspectionFocusBlur > 0 ? 'blur(' + inspectionFocusBlur.toFixed(2) + 'px)' : 'none';
                  ctx.drawImage(lensBuffer, (lensDisplayX - sourceRadius) * lensSourceScale, (lensDisplayY - sourceRadius) * lensSourceScale, sourceRadius * 2 * lensSourceScale, sourceRadius * 2 * lensSourceScale, lensDisplayX - lensRadius, lensDisplayY - lensRadius, lensRadius * 2, lensRadius * 2);
                  ctx.filter = 'none';
                  drawOpticalDepthCues(lensDisplayX - lensRadius, lensDisplayY - lensRadius, lensRadius * 2, lensRadius * 2, inspectionDepth, liveOpticalInspection.quality);
                  ctx.fillStyle = inspectionDepth === 'surface' ? 'rgba(251,191,36,0.08)' : (inspectionDepth === 'deep' ? 'rgba(124,58,237,0.12)' : 'rgba(34,211,238,0.035)');
                  ctx.fillRect(lensDisplayX - lensRadius, lensDisplayY - lensRadius, lensRadius * 2, lensRadius * 2);
                  var lensShade = ctx.createRadialGradient(lensDisplayX - 15, lensDisplayY - 18, 4, lensDisplayX, lensDisplayY, lensRadius);
                  lensShade.addColorStop(0, 'rgba(255,255,255,0.17)'); lensShade.addColorStop(0.72, 'rgba(255,255,255,0)'); lensShade.addColorStop(1, 'rgba(2,6,23,0.24)');
                  ctx.fillStyle = lensShade; ctx.fillRect(lensDisplayX - lensRadius, lensDisplayY - lensRadius, lensRadius * 2, lensRadius * 2); ctx.restore();
                  ctx.save(); ctx.beginPath(); ctx.arc(lensDisplayX, lensDisplayY, lensRadius, 0, Math.PI * 2);
                  ctx.strokeStyle = liveVisualMode === 'accessible' ? '#f8fafc' : inspectionAccent; ctx.lineWidth = liveVisualMode === 'accessible' ? 5 : 3; ctx.shadowColor = 'rgba(2,6,23,0.75)'; ctx.shadowBlur = 8; ctx.stroke();
                  ctx.strokeStyle = inspectionQualityColor; ctx.lineWidth = liveVisualMode === 'accessible' ? 4 : 2.5; ctx.shadowBlur = 0;
                  ctx.beginPath(); ctx.arc(lensDisplayX, lensDisplayY, lensRadius - 5, -Math.PI / 2, -Math.PI / 2 + Math.PI * 2 * liveOpticalInspection.quality / 100); ctx.stroke();
                  ctx.font = 'bold 10px Inter, system-ui'; ctx.fillStyle = '#f8fafc';
                  ctx.fillText(inspectionMagnification + 'x ' + liveOpticalInspection.quality + '%' + (d.lensPinned ? ' PIN' : ''), lensDisplayX + lensRadius - 57, lensDisplayY + lensRadius - 8); ctx.restore();
                }
                if (!d.quizMode && macroInset && lensTargetInFrame && lensCtx) {
                  var macroWidth = liveVisualMode === 'accessible' ? 176 : 158;
                  var macroHeight = liveVisualMode === 'accessible' ? 142 : 124;
                  var macroX = lensDisplayX > W * 0.58 ? 12 : W - macroWidth - 12;
                  var macroY = 12;
                  var macroPadding = 5, macroLabelHeight = 34, macroFooterHeight = 17;
                  var macroSourceWidth = (macroWidth - macroPadding * 2) / inspectionMagnification;
                  var macroSourceHeight = (macroHeight - macroPadding * 2 - macroLabelHeight - macroFooterHeight) / inspectionMagnification;
                  ctx.save(); ctx.strokeStyle = 'rgba(148,163,184,0.5)'; ctx.lineWidth = 1; ctx.setLineDash([3, 3]);
                  ctx.beginPath(); ctx.moveTo(lensDisplayX, lensDisplayY); ctx.lineTo(macroX + macroWidth / 2, macroY + macroHeight / 2); ctx.stroke(); ctx.setLineDash([]); ctx.restore();
                  ctx.save(); ctx.shadowColor = 'rgba(2,6,23,0.72)'; ctx.shadowBlur = 14; ctx.fillStyle = 'rgba(15,23,42,0.95)';
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(macroX, macroY, macroWidth, macroHeight, 9); ctx.fill(); }
                  else ctx.fillRect(macroX, macroY, macroWidth, macroHeight);
                  ctx.shadowBlur = 0; ctx.beginPath();
                  if (ctx.roundRect) ctx.roundRect(macroX + macroPadding, macroY + macroLabelHeight, macroWidth - macroPadding * 2, macroHeight - macroLabelHeight - macroFooterHeight, 5);
                  else ctx.rect(macroX + macroPadding, macroY + macroLabelHeight, macroWidth - macroPadding * 2, macroHeight - macroLabelHeight - macroFooterHeight);
                  ctx.clip();
                  ctx.filter = inspectionFocusBlur > 0 ? 'blur(' + inspectionFocusBlur.toFixed(2) + 'px)' : 'none';
                  ctx.drawImage(lensBuffer, (lensDisplayX - macroSourceWidth / 2) * lensSourceScale, (lensDisplayY - macroSourceHeight / 2) * lensSourceScale, macroSourceWidth * lensSourceScale, macroSourceHeight * lensSourceScale, macroX + macroPadding, macroY + macroLabelHeight, macroWidth - macroPadding * 2, macroHeight - macroLabelHeight - macroFooterHeight);
                  ctx.filter = 'none';
                  drawOpticalDepthCues(macroX + macroPadding, macroY + macroLabelHeight, macroWidth - macroPadding * 2, macroHeight - macroLabelHeight - macroFooterHeight, inspectionDepth, liveOpticalInspection.quality);
                  ctx.fillStyle = inspectionDepth === 'surface' ? 'rgba(251,191,36,0.09)' : (inspectionDepth === 'deep' ? 'rgba(124,58,237,0.13)' : 'rgba(34,211,238,0.04)');
                  ctx.fillRect(macroX + macroPadding, macroY + macroLabelHeight, macroWidth - macroPadding * 2, macroHeight - macroLabelHeight - macroFooterHeight);
                  ctx.restore();
                  ctx.save(); ctx.strokeStyle = liveVisualMode === 'accessible' ? '#facc15' : inspectionQualityColor; ctx.lineWidth = liveVisualMode === 'accessible' ? 3 : 1.5;
                  if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(macroX, macroY, macroWidth, macroHeight, 9); ctx.stroke(); }
                  else ctx.strokeRect(macroX, macroY, macroWidth, macroHeight);
                  var macroCenterX = macroX + macroWidth / 2, macroCenterY = macroY + macroLabelHeight + (macroHeight - macroLabelHeight - macroFooterHeight) / 2;
                  ctx.beginPath(); ctx.moveTo(macroCenterX - 8, macroCenterY); ctx.lineTo(macroCenterX + 8, macroCenterY); ctx.moveTo(macroCenterX, macroCenterY - 8); ctx.lineTo(macroCenterX, macroCenterY + 8); ctx.stroke();
                  ctx.font = 'bold 9px Inter, system-ui'; ctx.fillStyle = '#f8fafc';
                  ctx.fillText('MACRO ' + inspectionMagnification + 'x / ' + inspectionDepth.toUpperCase(), macroX + 8, macroY + 12);
                  ctx.font = '8px Inter, system-ui'; ctx.fillStyle = '#cbd5e1';
                  var macroStructureName = d.quizMode ? 'Unlabeled tissue field' : (lensTargetStructure ? lensTargetStructure.name : 'Unlabeled tissue field');
                  ctx.fillText((d.lensPinned ? 'PINNED / ' : '') + macroStructureName.slice(0, 24), macroX + 8, macroY + 25);
                  ctx.fillStyle = inspectionQualityColor;
                  ctx.fillText('FOCUS: ' + liveOpticalInspection.label.toUpperCase() + ' ' + liveOpticalInspection.quality + '%', macroX + 8, macroY + macroHeight - 6);
                  ctx.restore();
                }
                function instrumentPitchData(toolId) {
                  var pitchDefinitions = {
                    scalpel: { degrees: toolCalibration.bladeAngle, min: 20, max: 35, label: 'BLADE' },
                    scissors: { degrees: 18, min: 12, max: 28, label: 'JAW' },
                    forceps: { degrees: 24, min: 16, max: 34, label: 'TIP' },
                    pin: { degrees: toolCalibration.pinAngle, min: 55, max: 75, label: 'ANCHOR' },
                    probe: { degrees: 12, min: 8, max: 22, label: 'PROBE' },
                    dropper: { degrees: 70, min: 58, max: 82, label: 'DROPPER' }
                  };
                  var pitch = pitchDefinitions[toolId] || pitchDefinitions.probe;
                  var degrees = Math.max(5, Math.min(90, Number(pitch.degrees) || 12));
                  return {
                    degrees: degrees,
                    radians: degrees * Math.PI / 180,
                    min: pitch.min,
                    max: pitch.max,
                    label: pitch.label,
                    calibrated: degrees >= pitch.min && degrees <= pitch.max,
                    adjustable: toolId === 'scalpel' || toolId === 'pin'
                  };
                }
                function drawInstrumentPitchGauge(toolId, angle, pitchData, engaged, onSpecimen) {
                  // The posture reticle separates planar heading from above-surface pitch using angle, foreshortening, and non-color status geometry.
                  var pitchAccessible = highContrastEnabled || liveVisualMode === 'accessible';
                  var pitchRadius = 14;
                  var pitchRayLength = 24;
                  var pitchRayX = -Math.cos(pitchData.radians) * pitchRayLength;
                  var pitchRayY = -Math.sin(pitchData.radians) * pitchRayLength;
                  var pitchStatusColor = pitchData.calibrated ? '#5eead4' : '#fb7185';
                  ctx.save(); ctx.translate(pointerScreenX, pointerScreenY); ctx.rotate(angle);
                  ctx.globalAlpha = onSpecimen ? (engaged ? 0.96 : 0.82) : 0.54;
                  ctx.strokeStyle = pitchAccessible ? '#ffffff' : 'rgba(203,213,225,0.82)';
                  ctx.lineWidth = pitchAccessible ? 2 : 1;
                  ctx.beginPath(); ctx.moveTo(-28, 0); ctx.lineTo(4, 0); ctx.stroke();
                  ctx.strokeStyle = pitchAccessible ? (pitchData.calibrated ? '#ffffff' : '#facc15') : pitchStatusColor;
                  ctx.lineWidth = pitchAccessible ? 2.4 : 1.5;
                  ctx.setLineDash(pitchData.calibrated ? [] : [3, 2]);
                  ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(pitchRayX, pitchRayY); ctx.stroke(); ctx.setLineDash([]);
                  ctx.beginPath(); ctx.arc(0, 0, pitchRadius, Math.PI, Math.PI + pitchData.radians); ctx.stroke();
                  if (pitchData.calibrated) {
                    ctx.beginPath(); ctx.moveTo(pitchRayX - 4, pitchRayY - 1); ctx.lineTo(pitchRayX - 1, pitchRayY + 3); ctx.lineTo(pitchRayX + 5, pitchRayY - 5); ctx.stroke();
                  } else {
                    ctx.beginPath(); ctx.moveTo(pitchRayX - 5, pitchRayY + 4); ctx.lineTo(pitchRayX, pitchRayY - 5); ctx.lineTo(pitchRayX + 5, pitchRayY + 4); ctx.closePath(); ctx.stroke();
                  }
                  var pitchText = pitchData.label + ' ' + Math.round(pitchData.degrees) + '\u00B0';
                  ctx.font = 'bold 7px Inter, system-ui';
                  var pitchTextWidth = Math.ceil(ctx.measureText(pitchText).width) + 10;
                  ctx.save(); ctx.translate(pitchRayX - 4, pitchRayY - 10); ctx.rotate(-angle);
                  var pitchTextX = -pitchTextWidth / 2, pitchTextY = -7.5;
                  ctx.fillStyle = pitchAccessible ? '#000000' : 'rgba(15,23,42,0.90)';
                  ctx.beginPath();
                  if (ctx.roundRect) ctx.roundRect(pitchTextX, pitchTextY, pitchTextWidth, 15, 4); else ctx.rect(pitchTextX, pitchTextY, pitchTextWidth, 15);
                  ctx.fill(); ctx.strokeStyle = pitchStatusColor; ctx.lineWidth = 0.9; ctx.stroke();
                  ctx.fillStyle = pitchAccessible ? '#ffffff' : (pitchData.calibrated ? '#99f6e4' : '#fecdd3');
                  ctx.fillText(pitchText, pitchTextX + 5, pitchTextY + 10);
                  ctx.restore(); ctx.restore();
                }
                function instrumentLightingData(angle, pitchData) {
                  // One lab-light model drives metal highlights, instrument shadows, and the projected tip-to-surface elevation cue.
                  var lightVector = {
                    overhead: { x: 0.08, y: -0.62, z: 1 },
                    left: { x: -0.92, y: -0.22, z: 0.72 },
                    right: { x: 0.92, y: -0.22, z: 0.72 },
                    raking: { x: -0.72, y: 0.58, z: 0.38 }
                  }[lightDirection] || { x: 0.08, y: -0.62, z: 1 };
                  var shadowDirection = {
                    overhead: { x: 0.16, y: 0.52 },
                    left: { x: 0.74, y: 0.24 },
                    right: { x: -0.74, y: 0.24 },
                    raking: { x: 0.58, y: -0.38 }
                  }[lightDirection] || { x: 0.16, y: 0.52 };
                  var longitudinalX = Math.cos(angle), longitudinalY = Math.sin(angle);
                  var crossX = -longitudinalY, crossY = longitudinalX;
                  var lateralLight = crossX * lightVector.x + crossY * lightVector.y;
                  var pitchFacing = Math.max(0.12, Math.sin(pitchData.radians) * lightVector.z);
                  return {
                    shadowDirection: shadowDirection,
                    shadowLocalX: shadowDirection.x * longitudinalX + shadowDirection.y * longitudinalY,
                    shadowLocalY: shadowDirection.x * crossX + shadowDirection.y * crossY,
                    glintSide: lateralLight >= 0 ? 1 : -1,
                    highlightPosition: Math.max(0.18, Math.min(0.68, 0.43 - lateralLight * 0.16 - pitchFacing * 0.08)),
                    specularStrength: Math.max(0.28, Math.min(1, 0.30 + Math.abs(lateralLight) * 0.46 + pitchFacing * 0.34))
                  };
                }
                function drawInstrumentElevationTether(angle, engaged, onSpecimen, pitchData, lightingData, tipPoint) {
                  // The screen-space tether turns projected shadow separation into a redundant height cue: dashed above the surface, solid at contact.
                  var approachHeight = onSpecimen ? (engaged ? 0.05 : 0.28) : 1;
                  var pitchLift = Math.sin(pitchData.radians) * (onSpecimen ? 2.4 : 5.4);
                  var shadowDistance = 2.5 + approachHeight * 8.5 + pitchLift;
                  var shadowDirection = lightingData.shadowDirection;
                  var tipX = tipPoint ? tipPoint.x : pointerScreenX, tipY = tipPoint ? tipPoint.y : pointerScreenY;
                  var endX = pointerScreenX + shadowDirection.x * shadowDistance;
                  var endY = pointerScreenY + shadowDirection.y * shadowDistance;
                  var dx = endX - tipX, dy = endY - tipY;
                  var tetherLength = Math.max(0.001, Math.sqrt(dx * dx + dy * dy));
                  var normalX = -dy / tetherLength, normalY = dx / tetherLength;
                  var accessibleTether = highContrastEnabled || liveVisualMode === 'accessible';
                  ctx.save();
                  ctx.globalAlpha = accessibleTether ? 1 : (engaged && onSpecimen ? 0.86 : 0.66);
                  ctx.strokeStyle = accessibleTether ? (onSpecimen ? '#facc15' : '#ffffff') : (onSpecimen ? '#67e8f9' : '#cbd5e1');
                  ctx.lineWidth = accessibleTether ? 2.2 : 1.25;
                  ctx.setLineDash(engaged && onSpecimen ? [] : (onSpecimen ? [2, 2] : [4, 3]));
                  ctx.beginPath(); ctx.moveTo(tipX, tipY); ctx.lineTo(endX, endY); ctx.stroke();
                  ctx.setLineDash([]);
                  var capSize = accessibleTether ? 4.5 : 3.2;
                  ctx.beginPath();
                  ctx.moveTo(tipX - normalX * capSize, tipY - normalY * capSize);
                  ctx.lineTo(tipX + normalX * capSize, tipY + normalY * capSize);
                  ctx.moveTo(endX - normalX * capSize, endY - normalY * capSize);
                  ctx.lineTo(endX + normalX * capSize, endY + normalY * capSize);
                  ctx.stroke();
                  if (engaged && onSpecimen) {
                    ctx.beginPath(); ctx.arc(tipX, tipY, accessibleTether ? 5.5 : 4.2, -0.7, 0.7); ctx.stroke();
                  } else if (accessibleTether && tetherLength > 8) {
                    var tetherLabel = onSpecimen ? 'ABOVE' : 'TRAY';
                    ctx.font = 'bold 7px Inter, system-ui';
                    ctx.fillStyle = '#000000';
                    var tetherTextWidth = ctx.measureText(tetherLabel).width + 8;
                    ctx.fillRect((tipX + endX) / 2 - tetherTextWidth / 2, (tipY + endY) / 2 - 12, tetherTextWidth, 10);
                    ctx.fillStyle = '#ffffff';
                    ctx.fillText(tetherLabel, (tipX + endX) / 2 - tetherTextWidth / 2 + 4, (tipY + endY) / 2 - 4);
                  }
                  ctx.restore();
                }
                function drawInstrumentSpecularEdge(toolId, lightingData) {
                  // Tool-specific edge glints keep the illumination readable after foreshortening and never carry status by color alone.
                  var side = lightingData.glintSide;
                  var accessibleGlint = highContrastEnabled || liveVisualMode === 'accessible';
                  ctx.save(); ctx.shadowBlur = 0;
                  ctx.globalAlpha = accessibleGlint ? 0.92 : 0.32 + lightingData.specularStrength * 0.48;
                  ctx.strokeStyle = accessibleGlint ? '#ffffff' : 'rgba(255,255,255,0.96)';
                  ctx.lineWidth = accessibleGlint ? 1.35 : 0.8 + lightingData.specularStrength * 0.45;
                  ctx.lineCap = 'round';
                  ctx.beginPath();
                  if (toolId === 'scalpel') {
                    ctx.moveTo(-44, side * 4.2); ctx.lineTo(-12, side * 4.2);
                    ctx.moveTo(-7, side * 3.4); ctx.lineTo(12, side * 1.7);
                  } else if (toolId === 'scissors') {
                    ctx.moveTo(-6, 0); ctx.lineTo(26, side * (7 + toolCalibration.scissorsAperture / 100 * 6));
                    ctx.arc(-19, side * 9, 6.2, 0.18, Math.PI - 0.18);
                  } else if (toolId === 'forceps') {
                    ctx.moveTo(-36, side * 5.1); ctx.quadraticCurveTo(-3, side * 2.4, 19, side * 1.5);
                  } else if (toolId === 'pin') {
                    ctx.moveTo(-22, side * 0.7); ctx.lineTo(14, side * 0.7);
                  } else if (toolId === 'dropper') {
                    ctx.moveTo(-25, side * 2.8); ctx.lineTo(9, side * 2.8);
                    ctx.arc(-32, 0, 8.8, side > 0 ? 0.15 : Math.PI + 0.15, side > 0 ? Math.PI - 0.15 : Math.PI * 2 - 0.15);
                  } else {
                    ctx.moveTo(-41, side * 0.8); ctx.lineTo(17, side * 0.8);
                    ctx.moveTo(-42, side * 3.8); ctx.lineTo(-18, side * 3.8);
                  }
                  ctx.stroke(); ctx.restore();
                }
                function drawInstrumentContactShadow(toolId, contactContext, angle, engaged, scale, material, pitchData, lightingData) {
                  // Directional contact shadow and tool-specific footprint distinguish approach, contact, and engagement before the instrument body is drawn.
                  var onSpecimen = !!(contactContext && contactContext.onSpecimen);
                  var shadowDirection = lightingData ? lightingData.shadowDirection : { x: 0.16, y: 0.52 };
                  var approachHeight = onSpecimen ? (engaged ? 0.05 : 0.28) : 1;
                  var pitchLift = pitchData ? Math.sin(pitchData.radians) * (onSpecimen ? 2.4 : 5.4) : 0;
                  var shadowDistance = 2.5 + approachHeight * 8.5 + pitchLift;
                  var contactScale = engaged && onSpecimen ? 0.88 : (onSpecimen ? 1 : 1.12);
                  var accessibleFootprint = highContrastEnabled || liveVisualMode === 'accessible';
                  var footprintColor = onSpecimen ? (material.accent || '#22d3ee') : '#94a3b8';
                  var contactOpacity = onSpecimen ? (engaged ? 0.86 : 0.58) : 0.30;
                  ctx.save();
                  ctx.translate(pointerScreenX + shadowDirection.x * shadowDistance, pointerScreenY + shadowDirection.y * shadowDistance);
                  ctx.rotate(angle);
                  ctx.scale(scale * contactScale, scale * contactScale);
                  if (!accessibleFootprint) {
                    var contactShadowGradient = ctx.createRadialGradient(-2, -1, 1, 0, 0, onSpecimen ? 16 : 20);
                    contactShadowGradient.addColorStop(0, 'rgba(2,6,23,' + Math.min(0.54, contactOpacity + 0.08).toFixed(2) + ')');
                    contactShadowGradient.addColorStop(0.58, 'rgba(15,23,42,' + contactOpacity.toFixed(2) + ')');
                    contactShadowGradient.addColorStop(1, 'rgba(15,23,42,0)');
                    ctx.fillStyle = contactShadowGradient;
                    ctx.beginPath(); ctx.ellipse(0, 0, onSpecimen ? 17 : 21, onSpecimen ? 6 : 8, 0, 0, Math.PI * 2); ctx.fill();
                  }
                  ctx.globalAlpha = accessibleFootprint ? 1 : Math.min(0.94, contactOpacity + 0.24);
                  ctx.strokeStyle = accessibleFootprint ? (onSpecimen ? '#facc15' : '#ffffff') : footprintColor;
                  ctx.fillStyle = accessibleFootprint ? (onSpecimen ? 'rgba(250,204,21,0.18)' : 'rgba(255,255,255,0.08)') : (onSpecimen ? 'rgba(255,255,255,0.10)' : 'rgba(148,163,184,0.06)');
                  ctx.lineWidth = accessibleFootprint ? 2.2 : (engaged && onSpecimen ? 1.8 : 1.2);
                  ctx.setLineDash(onSpecimen ? [] : [4, 3]);
                  if (toolId === 'scalpel' || toolId === 'scissors') {
                    if (toolId === 'scissors') {
                      ctx.beginPath(); ctx.moveTo(-10, -2.5); ctx.lineTo(9, -1); ctx.moveTo(-10, 2.5); ctx.lineTo(9, 1); ctx.stroke();
                    } else {
                      ctx.beginPath(); ctx.ellipse(0, 0, 11, engaged && onSpecimen ? 1.6 : 2.5, 0, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                    }
                  } else if (toolId === 'forceps') {
                    ctx.beginPath(); ctx.ellipse(1, -3.1, 5.8, 1.8, 0, 0, Math.PI * 2); ctx.ellipse(1, 3.1, 5.8, 1.8, 0, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                  } else if (toolId === 'pin') {
                    ctx.beginPath(); ctx.arc(0, 0, engaged && onSpecimen ? 2.6 : 4, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(-5, 0); ctx.lineTo(5, 0); ctx.moveTo(0, -5); ctx.lineTo(0, 5); ctx.stroke();
                  } else if (toolId === 'dropper') {
                    ctx.beginPath(); ctx.ellipse(0, 0, engaged && onSpecimen ? 5.2 : 7, engaged && onSpecimen ? 3.4 : 4.8, 0, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                  } else {
                    ctx.beginPath(); ctx.arc(0, 0, engaged && onSpecimen ? 3.2 : 5.2, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                  }
                  ctx.setLineDash([]);
                  if (engaged && onSpecimen) {
                    ctx.strokeStyle = accessibleFootprint ? '#ffffff' : footprintColor;
                    ctx.lineWidth = accessibleFootprint ? 1.7 : 1;
                    ctx.beginPath(); ctx.arc(0, 0, 7.5 + contactPressure * 2.5, -0.78, 0.78); ctx.stroke();
                  }
                  ctx.restore();
                }
                function drawInstrumentTissueOcclusion(toolId, angle, contactContext, engaged, material, pitchData) {
                  // A foreground tissue lip briefly covers the engaged tip so depth reads through occlusion as well as shadow and pitch.
                  if (!engaged || !contactContext || !contactContext.onSpecimen || toolId === 'dropper') return;
                  var occlusionAccessible = highContrastEnabled || liveVisualMode === 'accessible';
                  var openingOcclusion = !!(canvasProcedure.incisionStarted && activeOpeningPath && activeOpeningPath.length > 1 && distanceToGuide(screenPointer, activeOpeningPath) <= 0.055);
                  var occlusionLayer = (spec.layers[currentLayerIdx] || {}).color || '#b87972';
                  var toolDepthFactor = { scalpel: 0.62, scissors: 0.52, forceps: 0.46, pin: 0.92, probe: 0.38, wick: 0.24 }[toolId] || 0.32;
                  var pitchDepth = Math.max(0.18, Math.sin((pitchData && pitchData.radians) || 0.24));
                  var occlusionDepth = Math.max(0.18, Math.min(1, toolDepthFactor * (0.62 + contactPressure * 0.58) * (0.74 + pitchDepth * 0.38)));
                  var lipLength = 6 + occlusionDepth * 5.5;
                  var lipThickness = 2.4 + occlusionDepth * 3.4;
                  var shaftGap = toolId === 'scissors' ? 3.7 : (toolId === 'forceps' ? 2.8 : (toolId === 'wick' ? 4.1 : 1.7));
                  ctx.save(); ctx.translate(pointerScreenX, pointerScreenY); ctx.rotate(angle);
                  var lipGradient = ctx.createLinearGradient(0, -lipThickness * 2, 0, lipThickness * 2);
                  lipGradient.addColorStop(0, occlusionAccessible ? '#000000' : occlusionLayer);
                  lipGradient.addColorStop(0.48, occlusionAccessible ? '#000000' : 'rgba(248,250,252,0.34)');
                  lipGradient.addColorStop(1, occlusionAccessible ? '#000000' : 'rgba(64,24,34,0.96)');
                  ctx.fillStyle = lipGradient;
                  ctx.strokeStyle = occlusionAccessible ? '#ffffff' : (material.accent || '#fda4af');
                  ctx.lineWidth = occlusionAccessible ? 2.1 : 0.9;
                  ctx.globalAlpha = occlusionAccessible ? 1 : 0.94;
                  if (openingOcclusion) {
                    ctx.beginPath(); ctx.moveTo(-lipLength, -shaftGap);
                    ctx.quadraticCurveTo(0, -shaftGap + lipThickness * 0.34, lipLength, -shaftGap);
                    ctx.quadraticCurveTo(0, -shaftGap - lipThickness, -lipLength, -shaftGap); ctx.closePath(); ctx.fill(); ctx.stroke();
                    ctx.beginPath(); ctx.moveTo(-lipLength, shaftGap);
                    ctx.quadraticCurveTo(0, shaftGap - lipThickness * 0.34, lipLength, shaftGap);
                    ctx.quadraticCurveTo(0, shaftGap + lipThickness, -lipLength, shaftGap); ctx.closePath(); ctx.fill(); ctx.stroke();
                  } else {
                    ctx.beginPath(); ctx.moveTo(-lipLength, 1.2);
                    ctx.quadraticCurveTo(0, 1.2 - lipThickness * 0.46, lipLength, 1.2);
                    ctx.quadraticCurveTo(0, 1.2 + lipThickness, -lipLength, 1.2); ctx.closePath(); ctx.fill(); ctx.stroke();
                  }
                  // Paired depth notches make partial insertion legible without relying on the lip color.
                  ctx.strokeStyle = occlusionAccessible ? '#facc15' : '#f8fafc';
                  ctx.lineWidth = occlusionAccessible ? 2 : 1.1;
                  ctx.beginPath();
                  ctx.moveTo(-lipLength - 2, -3); ctx.lineTo(-lipLength - 2, 3);
                  ctx.moveTo(lipLength + 2, -3); ctx.lineTo(lipLength + 2, 3);
                  ctx.stroke(); ctx.restore();
                }
                if (instrumentVisuals && (!pointerIsSynthetic || (activeContactPulse && activeContactPulse.replay)) && !d.quizMode && !d.annotateMode && !d.rulerMode) {
                  var cursorTool = activeContactPulse && pointerIsSynthetic ? activeContactPulse.tool : activePointerTool;
                  var cursorVector = activeContactPulse && pointerIsSynthetic && activeContactPulse.vector ? activeContactPulse.vector : canvas._toolVector;
                  var desiredCursorAngle = -0.72;
                  if (cursorVector && Math.abs(cursorVector.x) + Math.abs(cursorVector.y) > 0.001) {
                    desiredCursorAngle = Math.atan2(cursorVector.y * pointerScale.y * H, cursorVector.x * pointerScale.x * W);
                  }
                  var previousCursorAngle = Number(canvas._toolAngle);
                  if (!isFinite(previousCursorAngle)) previousCursorAngle = desiredCursorAngle;
                  var cursorAngleDelta = Math.atan2(Math.sin(desiredCursorAngle - previousCursorAngle), Math.cos(desiredCursorAngle - previousCursorAngle));
                  var cursorAngle = dissMotionReduced ? desiredCursorAngle : previousCursorAngle + cursorAngleDelta * 0.28;
                  canvas._toolAngle = cursorAngle;
                  var cursorPitch = instrumentPitchData(cursorTool);
                  var cursorLighting = instrumentLightingData(cursorAngle, cursorPitch);
                  var cursorLongitudinalScale = 0.62 + Math.cos(cursorPitch.radians) * 0.38;
                  var cursorMaterial = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
                  var cursorContactContext = specimenContactContext(screenPointer);
                  var replayInstrumentActive = !!(activeContactPulse && pointerIsSynthetic && activeContactPulse.replay);
                  var replayContactPhase = !replayInstrumentActive || (contactPulseProgress >= 0.28 && contactPulseProgress <= 0.78);
                  var replayLift = replayInstrumentActive && !dissMotionReduced
                    ? (contactPulseProgress < 0.28
                      ? (1 - contactPulseProgress / 0.28) * 24
                      : (contactPulseProgress > 0.78 ? ((contactPulseProgress - 0.78) / 0.22) * 18 : 0))
                    : 0;
                  var cursorTipPoint = { x: pointerScreenX, y: pointerScreenY - replayLift };
                  var toolVisuallyEngaged = !!canvas._toolDrawing || (!!activeContactPulse && replayContactPhase);
                  var cursorScale = (liveVisualMode === 'accessible' ? 1.28 : 1) * (1 + contactPressure * 0.035 + (toolVisuallyEngaged ? 0.018 : 0));
                  var toolTipOffset = cursorTool === 'scissors' ? 29 : (cursorTool === 'forceps' ? 21 : (cursorTool === 'probe' ? 20 : (cursorTool === 'wick' ? 18 : (cursorTool === 'pin' ? 17 : 15))));
                  var cursorProjectedOffset = toolTipOffset * cursorLongitudinalScale * cursorScale;
                  drawInstrumentElevationTether(cursorAngle, toolVisuallyEngaged, cursorContactContext.onSpecimen, cursorPitch, cursorLighting, cursorTipPoint);
                  drawInstrumentContactShadow(cursorTool, cursorContactContext, cursorAngle, toolVisuallyEngaged, cursorScale, cursorMaterial, cursorPitch, cursorLighting);
                  drawInstrumentPitchGauge(cursorTool, cursorAngle, cursorPitch, toolVisuallyEngaged, cursorContactContext.onSpecimen);
                  if (replayInstrumentActive) {
                    var replayStage = contactPulseProgress < 0.28 ? '1 APPROACH' : (contactPulseProgress <= 0.78 ? '2 CONTACT' : '3 RELEASE');
                    var replayBadgeX = cursorTipPoint.x > W - 92 ? cursorTipPoint.x - 84 : cursorTipPoint.x + 12;
                    var replayBadgeY = Math.max(8, cursorTipPoint.y - 29);
                    ctx.save(); ctx.font = 'bold 8px Inter, system-ui';
                    var replayBadgeWidth = Math.ceil(ctx.measureText(replayStage).width) + 12;
                    ctx.fillStyle = highContrastEnabled || liveVisualMode === 'accessible' ? '#000000' : 'rgba(15,23,42,0.92)';
                    ctx.beginPath();
                    if (ctx.roundRect) ctx.roundRect(replayBadgeX, replayBadgeY, replayBadgeWidth, 17, 5); else ctx.rect(replayBadgeX, replayBadgeY, replayBadgeWidth, 17);
                    ctx.fill(); ctx.strokeStyle = '#ffffff'; ctx.lineWidth = 1; ctx.stroke();
                    ctx.fillStyle = '#ffffff'; ctx.fillText(replayStage, replayBadgeX + 6, replayBadgeY + 11.5); ctx.restore();
                  }
                  ctx.save();
                  if (replayInstrumentActive) ctx.globalAlpha = dissMotionReduced ? 0.96 : Math.max(0.54, 1 - Math.abs(contactPulseProgress - 0.5) * 0.52);
                  ctx.translate(cursorTipPoint.x - Math.cos(cursorAngle) * cursorProjectedOffset, cursorTipPoint.y - Math.sin(cursorAngle) * cursorProjectedOffset); ctx.rotate(cursorAngle); ctx.scale(cursorScale * cursorLongitudinalScale, cursorScale);
                  ctx.shadowColor = toolVisuallyEngaged ? (cursorContactContext.onSpecimen ? cursorMaterial.accent : '#94a3b8') : 'rgba(2,6,23,0.76)';
                  ctx.shadowBlur = toolVisuallyEngaged ? 10 : 5 + (1 - contactPressure) * 7;
                  var bodyShadowDistance = toolVisuallyEngaged ? 2.2 : 5.5 + (1 - contactPressure) * 4;
                  ctx.shadowOffsetX = cursorLighting.shadowLocalX * bodyShadowDistance;
                  ctx.shadowOffsetY = cursorLighting.shadowLocalY * bodyShadowDistance;
                  var metalGradient = ctx.createLinearGradient(-44, -9 * cursorLighting.glintSide, 28, 9 * cursorLighting.glintSide);
                  var highlightShoulderA = Math.max(0.06, cursorLighting.highlightPosition - 0.16);
                  var highlightShoulderB = Math.min(0.92, cursorLighting.highlightPosition + 0.18);
                  metalGradient.addColorStop(0, '#334155');
                  metalGradient.addColorStop(highlightShoulderA, '#94a3b8');
                  metalGradient.addColorStop(cursorLighting.highlightPosition, '#ffffff');
                  metalGradient.addColorStop(highlightShoulderB, '#cbd5e1');
                  metalGradient.addColorStop(1, '#475569');
                  if (cursorTool === 'scalpel') {
                    var handleGradient = ctx.createLinearGradient(-48, -7, -7, 7);
                    handleGradient.addColorStop(0, '#0f766e'); handleGradient.addColorStop(0.5, '#5eead4'); handleGradient.addColorStop(1, '#115e59');
                    ctx.fillStyle = handleGradient; ctx.beginPath();
                    if (ctx.roundRect) ctx.roundRect(-48, -6, 40, 12, 4); else ctx.rect(-48, -6, 40, 12); ctx.fill();
                    ctx.fillStyle = metalGradient; ctx.beginPath(); ctx.moveTo(-9, -5); ctx.lineTo(16, -2); ctx.lineTo(4, 5); ctx.lineTo(-9, 5); ctx.closePath(); ctx.fill();
                    ctx.strokeStyle = '#ffffff'; ctx.lineWidth = 0.9; ctx.stroke();
                    ctx.strokeStyle = 'rgba(15,118,110,0.8)'; ctx.lineWidth = 1;
                    for (var gripIdx = -42; gripIdx < -12; gripIdx += 6) { ctx.beginPath(); ctx.moveTo(gripIdx, -4); ctx.lineTo(gripIdx, 4); ctx.stroke(); }
                  } else if (cursorTool === 'scissors') {
                    var jawBase = 0.07 + toolCalibration.scissorsAperture / 100 * 0.38;
                    var jaw = canvas._toolDrawing && !dissMotionReduced ? jawBase + Math.abs(Math.sin(dissTick * 0.16)) * 0.07 : jawBase;
                    ctx.strokeStyle = metalGradient; ctx.lineWidth = 3.2; ctx.lineCap = 'round';
                    ctx.beginPath(); ctx.moveTo(-7, 0); ctx.lineTo(29, -7 - jaw * 17); ctx.moveTo(-7, 0); ctx.lineTo(29, 7 + jaw * 17); ctx.stroke();
                    ctx.strokeStyle = '#cbd5e1'; ctx.lineWidth = 3;
                    ctx.beginPath(); ctx.arc(-19, -9, 8, 0, Math.PI * 2); ctx.arc(-19, 9, 8, 0, Math.PI * 2); ctx.stroke();
                    ctx.beginPath(); ctx.arc(-7, 0, 3.3, 0, Math.PI * 2); ctx.fillStyle = '#334155'; ctx.fill(); ctx.strokeStyle = '#f8fafc'; ctx.lineWidth = 1; ctx.stroke();
                  } else if (cursorTool === 'forceps') {
                    var forcepsClose = toolCalibration.forcepsGrip / 100 * 6.2 + contactPressure * 1.1;
                    ctx.strokeStyle = metalGradient; ctx.lineWidth = 2.7; ctx.lineCap = 'round';
                    ctx.beginPath(); ctx.moveTo(-39, -6); ctx.quadraticCurveTo(-3, -3, 21, -2 + forcepsClose * 0.25); ctx.moveTo(-39, 6); ctx.quadraticCurveTo(-3, 3, 21, 2 - forcepsClose * 0.25); ctx.stroke();
                    ctx.strokeStyle = '#64748b'; ctx.lineWidth = 5; ctx.beginPath(); ctx.moveTo(-39, 0); ctx.lineTo(-25, 0); ctx.stroke();
                    ctx.strokeStyle = '#f8fafc'; ctx.lineWidth = 0.8;
                    for (var toothIdx = 15; toothIdx <= 20; toothIdx += 2.5) { ctx.beginPath(); ctx.moveTo(toothIdx, -2); ctx.lineTo(toothIdx, 2); ctx.stroke(); }
                  } else if (cursorTool === 'pin') {
                    ctx.strokeStyle = metalGradient; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(-29, 0); ctx.lineTo(17, 0); ctx.stroke();
                    var cursorPinGrad = ctx.createRadialGradient(-32, -3, 1, -29, 0, 8); cursorPinGrad.addColorStop(0, '#fff'); cursorPinGrad.addColorStop(0.35, '#f9a8d4'); cursorPinGrad.addColorStop(1, '#be185d');
                    ctx.beginPath(); ctx.arc(-29, 0, 7, 0, Math.PI * 2); ctx.fillStyle = cursorPinGrad; ctx.fill();
                    ctx.fillStyle = '#f8fafc'; ctx.beginPath(); ctx.moveTo(17, 0); ctx.lineTo(12, -2); ctx.lineTo(12, 2); ctx.closePath(); ctx.fill();
                  } else if (cursorTool === 'dropper') {
                    var dropperGradient = ctx.createLinearGradient(-29, -5, 12, 5);
                    dropperGradient.addColorStop(0, '#475569'); dropperGradient.addColorStop(0.25, '#e0f2fe'); dropperGradient.addColorStop(0.8, '#7dd3fc'); dropperGradient.addColorStop(1, '#0369a1');
                    ctx.fillStyle = dropperGradient; ctx.fillRect(-29, -4, 40, 8);
                    ctx.fillStyle = '#334155'; ctx.beginPath(); ctx.ellipse(-32, 0, 12, 9, 0, 0, Math.PI * 2); ctx.fill();
                    ctx.fillStyle = 'rgba(125,211,252,0.9)'; ctx.beginPath(); ctx.moveTo(15, 0); ctx.quadraticCurveTo(20 + toolCalibration.salineDose * 2, 6 + toolCalibration.salineDose * 2, 15, 11 + toolCalibration.salineDose * 2); ctx.quadraticCurveTo(10 - toolCalibration.salineDose, 6 + toolCalibration.salineDose * 2, 15, 0); ctx.fill();
                  } else if (cursorTool === 'wick') {
                    var wickHandleGradient = ctx.createLinearGradient(-43, -5, 10, 5);
                    wickHandleGradient.addColorStop(0, '#475569'); wickHandleGradient.addColorStop(0.48, '#e2e8f0'); wickHandleGradient.addColorStop(1, '#94a3b8');
                    ctx.fillStyle = wickHandleGradient;
                    if (ctx.roundRect) { ctx.beginPath(); ctx.roundRect(-43, -4, 48, 8, 3); ctx.fill(); } else ctx.fillRect(-43, -4, 48, 8);
                    ctx.fillStyle = '#f8fafc'; ctx.strokeStyle = '#334155'; ctx.lineWidth = 1;
                    ctx.beginPath(); if (ctx.roundRect) ctx.roundRect(3, -8, 15, 16, 3); else ctx.rect(3, -8, 15, 16); ctx.fill(); ctx.stroke();
                    ctx.strokeStyle = '#64748b'; ctx.lineWidth = 0.8;
                    for (var wickFiber = 5; wickFiber <= 16; wickFiber += 3) { ctx.beginPath(); ctx.moveTo(wickFiber, -6); ctx.lineTo(wickFiber - 3, 6); ctx.stroke(); }
                  } else {
                    ctx.fillStyle = 'rgba(56,189,248,0.52)'; ctx.beginPath(); ctx.ellipse(17, 0, 3.2, 5.4, 0, 0, Math.PI * 2); ctx.fill();
                    ctx.strokeStyle = metalGradient; ctx.lineWidth = 2.15 + toolCalibration.probePressure / 100 * 1.25; ctx.beginPath(); ctx.moveTo(-43, 0); ctx.lineTo(20, 0); ctx.stroke();
                    var probeHandleGradient = ctx.createLinearGradient(-45, -6, -15, 6);
                    probeHandleGradient.addColorStop(0, '#115e59'); probeHandleGradient.addColorStop(0.5, '#2dd4bf'); probeHandleGradient.addColorStop(1, '#134e4a');
                    ctx.fillStyle = probeHandleGradient; ctx.beginPath();
                    if (ctx.roundRect) ctx.roundRect(-45, -5, 29, 10, 4); else ctx.rect(-45, -5, 29, 10); ctx.fill();
                    ctx.beginPath(); ctx.arc(20, 0, 2.5 + toolCalibration.probePressure / 100 * 2.1, 0, Math.PI * 2); ctx.fillStyle = '#f8fafc'; ctx.fill();
                  }
                  ctx.shadowBlur = 0;
                  drawInstrumentSpecularEdge(cursorTool, cursorLighting);
                  ctx.beginPath(); ctx.arc(toolTipOffset, 0, 1.8, 0, Math.PI * 2); ctx.fillStyle = 'rgba(255,255,255,0.92)'; ctx.fill();
                  if (toolVisuallyEngaged) {
                    ctx.globalAlpha = dissMotionReduced ? 0.84 : 0.64 + Math.sin(dissTick * 0.12) * 0.18;
                    ctx.fillStyle = cursorMaterial.accent; ctx.strokeStyle = '#f8fafc'; ctx.lineWidth = 0.8;
                    ctx.beginPath(); ctx.arc(-38, -9, 3.4, 0, Math.PI * 2); ctx.fill(); ctx.stroke();
                    ctx.font = 'bold 6px Inter, system-ui'; ctx.fillStyle = '#f8fafc'; ctx.fillText('ACTIVE', -33, -7);
                  }
                  ctx.restore();
                  drawInstrumentTissueOcclusion(cursorTool, cursorAngle, cursorContactContext, toolVisuallyEngaged, cursorMaterial, cursorPitch);
                }
              }
              } catch (e) { console.error('[DissectionLab] render error:', e); try { ctx.restore(); ctx.restore(); } catch (_) {} }

              var frameEndedAt = typeof performance !== 'undefined' && performance.now ? performance.now() : Date.now();
              var frameCost = Math.max(0, frameEndedAt - frameStartedAt);
              dissFrameCostAverage = dissFrameCostAverage ? dissFrameCostAverage * 0.88 + frameCost * 0.12 : frameCost;
              if (liveRenderQuality === 'auto') {
                canvas._adaptiveFrameFloor = dissFrameCostAverage > 28 ? 66 : (dissFrameCostAverage > 18 ? 50 : (dissFrameCostAverage > 11 ? 33 : 0));
                canvas._performanceStatus = { averageFrameCost: Math.round(dissFrameCostAverage), frameInterval: Math.max(minFrameMs, canvas._adaptiveFrameFloor || 0), constrainedDevice: constrainedDevice, saveData: saveDataRequested };
              }
              scheduleDissectionFrame();

            }

            drawDissectionFrame();

          };



          // Save current-specimen progress with a short debounce. Each specimen owns
          // its own layer, notes, assessment, and annotation state.
          var activeDissectionSaveKey = 'dissection_progress_' + specimen;
          var activeDissectionSaveAccess = currentDissectionSaveAccess(activeDissectionSaveKey, dissectionSaveOwner);
          if (d._dissLoadedSpec === specimen && activeDissectionSaveAccess.state === 'unknown') {
            prepareDissectionSaveAccess(activeDissectionSaveKey, spec.name, dissectionSaveOwner);
            activeDissectionSaveAccess = currentDissectionSaveAccess(activeDissectionSaveKey, dissectionSaveOwner);
          }
          if (d._dissLoadedSpec === specimen && activeDissectionSaveAccess.state === 'writable') {
            scheduleDissectionSave(activeDissectionSaveKey, {
              schemaVersion: DISSECTION_SAVE_SCHEMA_VERSION,
              exploredOrgans: d.exploredOrgans || {},
              verifiedIdentifications: d.verifiedIdentifications || {},
              revealedLayers: d.revealedLayers || {},
              quizScore: d.quizScore || 0,
              quizTotal: d.quizTotal || 0,
              quizFirstAttemptScore: Number(d.quizFirstAttemptScore) || 0,
              quizFirstAttemptTotal: Number(d.quizFirstAttemptTotal) || 0,
              quizSupportedCount: Number(d.quizSupportedCount) || 0,
              assessmentCompletedAt: Number(d.assessmentCompletedAt) || 0,
              assessmentRecordedScore: Number(d.assessmentRecordedScore) || 0,
              assessmentRecordedTotal: Number(d.assessmentRecordedTotal) || 0,
              assessmentEvidence: d.assessmentEvidence || {},
              quizReviewQueue: Array.isArray(d.quizReviewQueue) ? d.quizReviewQueue : [],
              completedObjectives: d.completedObjectives || {},
              organNotes: d.organNotes || {},
              organConfidence: d.organConfidence || {},
              annotations: d.annotations || [],
              dissInquiry: normalizeDissectionInquiry(d.dissInquiry),
              activeLayer: activeLayer,
              timeSpent: d.timeSpent || 0,
              guidedComplete: !!d.guidedComplete,
              procedureByLayer: d.procedureByLayer || {},
              attemptArchive: d.attemptArchive || {},
              adaptiveGuidance: d.adaptiveGuidance !== false,
              procedureMode: d.procedureMode === 'independent' ? 'independent' : 'guided',
              activeInstrument: activeInstrument,
              livingFunctionEnabled: livingFunctionEnabled,
              livingFunctionPaused: livingFunctionPaused,
              livingFunctionSpeed: livingFunctionSpeed,
              livingFunctionReplayToken: Number(d.livingFunctionReplayToken) || 0,
              incisionDepth: d.incisionDepth || 'shallow',
              toolCalibration: toolCalibration,
              visualRealism: visualRealism,
              labLight: labLight,
              lightIntensity: lightIntensity,
              inspectionLens: inspectionLens,
              lensPinned: lensPinned,
              lensPinnedPoint: d.lensPinnedPoint || null,
              lensPinnedOrganId: d.lensPinnedOrganId || null,
              lensMagnification: lensMagnification,
              lensFocusDepth: lensFocusDepth,
              instrumentVisuals: instrumentVisuals,
              macroInset: macroInset,
              sceneDetail: sceneDetail,
              depthAtlas: depthAtlasEnabled,
              tissueRelief: tissueReliefEnabled,
              relationshipMotion: relationshipMotion,
              focusMode: focusMode,
              parallaxDepth: parallaxDepth,
              visualEvidence: visualEvidence,
              splitComparison: splitComparison,
              referenceEvidenceId: d.referenceEvidenceId || null,
              lightDirection: lightDirection,
              variationSeed: variationSeed,
              tactileFeedback: d.tactileFeedback !== false,
              highContrast: highContrastEnabled,
              reducedMotion: reducedMotionEnabled,
              largeText: largeTextEnabled,
              simplifiedInstructions: simplifiedInstructions,
              anatomicalView: anatomicalView,
              crossSectionMode: crossSectionMode,
              specimenCondition: specimenCondition,
              relationshipMode: relationshipMode,
              relationshipTraceIndex: Number(d.relationshipTraceIndex) || 0,
              relationshipTraceCount: Number(d.relationshipTraceCount) || 0,
              renderQuality: renderQuality,
              procedureScenario: d.procedureScenario || 'precision-access',
              scenarioStartedAt: Number(d.scenarioStartedAt) || 0,
              scenarioTimeRemaining: Math.max(0, Number(d.scenarioTimeRemaining) || 0),
              scenarioCompletedAt: Math.max(0, Number(d.scenarioCompletedAt) || 0),
              instructorTarget: instructorTarget,
              instructorMaxCautions: instructorMaxCautions,
              instructorRequiredStructures: instructorRequiredStructures
            }, dissectionSaveOwner);
          }

          // Load the selected specimen after render; defaults deliberately clear
          // progress from the previously viewed specimen.
          if (!d._dissLoadedSpec || d._dissLoadedSpec !== specimen) {
            var loadSpecimenKey = specimen;
            var loadSpecimenDef = spec;
            var loadIsProtectedRetry = !!d._dissSaveRetrying;
            setTimeout(function () {
              var loadSaveKey = 'dissection_progress_' + loadSpecimenKey;
              var loadInspection = prepareDissectionSaveAccess(loadSaveKey, loadSpecimenDef.name, dissectionSaveOwner);
              if (loadIsProtectedRetry && !loadInspection.ok) {
                updManyForSpecimen(loadSpecimenKey, {
                  _dissLoadedSpec: loadSpecimenKey,
                  _dissSaveRetrying: false,
                  resetConfirmPending: false,
                  procedureFeedback: { message: 'Saved progress is still unavailable. Temporary-session changes remain in memory and saving stays paused.', tone: 'caution', at: Date.now() }
                });
                if (typeof announceToSR === 'function') announceToSR('Saved progress is still unavailable. Temporary-session changes remain and saving stays paused.');
                focusResetControl('diss-protected-save-retry');
                return;
              }
              if (loadIsProtectedRetry && loadInspection.kind === 'empty') {
                updManyForSpecimen(loadSpecimenKey, {
                  _dissLoadedSpec: loadSpecimenKey,
                  _dissSaveRetrying: false,
                  resetConfirmPending: false,
                  procedureFeedback: { message: 'No stored record was found. Temporary-session work was kept, and saving resumed from it.', tone: 'success', at: Date.now() }
                });
                if (typeof announceToSR === 'function') announceToSR('No stored record was found. Temporary-session work was kept, and saving resumed from it.');
                focusResetControl('diss-reset-specimen');
                return;
              }
              var data = loadInspection.ok ? loadInspection.data : {};
              var savedLayer = data.activeLayer && loadSpecimenDef.layers.some(function (layer) { return layer.id === data.activeLayer; })
                ? data.activeLayer
                : ((loadSpecimenDef.layers[0] || {}).id || 'skin');
              updManyForSpecimen(loadSpecimenKey, {
                exploredOrgans: data.exploredOrgans || {},
                verifiedIdentifications: data.verifiedIdentifications || {},
                revealedLayers: data.revealedLayers || {},
                quizScore: Number(data.quizScore) || 0,
                quizTotal: Number(data.quizTotal) || 0,
                quizFirstAttemptScore: Number(data.quizFirstAttemptScore) || 0,
                quizFirstAttemptTotal: Number(data.quizFirstAttemptTotal) || 0,
                quizSupportedCount: Number(data.quizSupportedCount) || 0,
                assessmentCompletedAt: Number(data.assessmentCompletedAt) || 0,
                assessmentRecordedScore: data.assessmentRecordedScore == null ? (Number(data.quizFirstAttemptScore) || 0) : (Number(data.assessmentRecordedScore) || 0),
                assessmentRecordedTotal: data.assessmentRecordedTotal == null ? (Number(data.quizFirstAttemptTotal) || 0) : (Number(data.assessmentRecordedTotal) || 0),
                assessmentEvidence: data.assessmentEvidence || {},
                quizReviewQueue: Array.isArray(data.quizReviewQueue) ? data.quizReviewQueue : [],
                completedObjectives: data.completedObjectives || {},
                organNotes: data.organNotes || {},
                organConfidence: data.organConfidence || {},
                annotations: Array.isArray(data.annotations) ? data.annotations : [],
                dissInquiry: normalizeDissectionInquiry(data.dissInquiry),
                activeLayer: savedLayer,
                timeSpent: Number(data.timeSpent) || 0,
                guidedComplete: !!data.guidedComplete,
                procedureByLayer: normalizeSavedProcedureByLayer(data.procedureByLayer || {}, {
                  specimenCondition: data.specimenCondition,
                  variationSeed: data.variationSeed
                }),
                attemptArchive: data.attemptArchive || {},
                adaptiveGuidance: data.adaptiveGuidance !== false,
                compareTechniqueAttempts: false,
                procedureMode: data.procedureMode === 'independent' ? 'independent' : 'guided',
                activeInstrument: data.activeInstrument || 'probe',
                livingFunctionEnabled: !!data.livingFunctionEnabled,
                livingFunctionPaused: !!data.livingFunctionPaused,
                livingFunctionSpeed: ['slow', 'normal', 'fast'].indexOf(data.livingFunctionSpeed) >= 0 ? data.livingFunctionSpeed : 'normal',
                livingFunctionReplayToken: Number(data.livingFunctionReplayToken) || 0,
                incisionDepth: data.incisionDepth === 'deep' ? 'deep' : 'shallow',
                toolCalibration: normalizeToolCalibration(data.toolCalibration),
                visualRealism: ['guided', 'realistic', 'accessible'].indexOf(data.visualRealism) >= 0 ? data.visualRealism : 'guided',
                labLight: ['neutral', 'warm', 'cool'].indexOf(data.labLight) >= 0 ? data.labLight : 'neutral',
                lightIntensity: Math.max(20, Math.min(100, Number(data.lightIntensity) || 68)),
                inspectionLens: !!data.inspectionLens,
                lensPinned: !!data.lensPinned,
                lensPinnedPoint: data.lensPinnedPoint && isFinite(Number(data.lensPinnedPoint.x)) && isFinite(Number(data.lensPinnedPoint.y)) ? { x: Number(data.lensPinnedPoint.x), y: Number(data.lensPinnedPoint.y) } : null,
                lensPinnedOrganId: data.lensPinnedOrganId || null,
                lensMagnification: [2, 4, 6].indexOf(Number(data.lensMagnification)) >= 0 ? Number(data.lensMagnification) : 2,
                lensFocusDepth: ['surface', 'structure', 'deep'].indexOf(data.lensFocusDepth) >= 0 ? data.lensFocusDepth : 'structure',
                instrumentVisuals: data.instrumentVisuals !== false,
                macroInset: data.macroInset !== false,
                sceneDetail: data.sceneDetail !== false,
                depthAtlas: data.depthAtlas !== false,
                tissueRelief: data.tissueRelief !== false,
                relationshipMotion: data.relationshipMotion !== false,
                focusMode: data.focusMode !== false,
                parallaxDepth: data.parallaxDepth !== false,
                visualEvidence: Array.isArray(data.visualEvidence) ? data.visualEvidence.filter(function (entry) { return entry && entry.image; }).slice(-6) : [],
                splitComparison: !!data.splitComparison,
                referenceEvidenceId: data.referenceEvidenceId || null,
                lightDirection: ['overhead', 'left', 'right', 'raking'].indexOf(data.lightDirection) >= 0 ? data.lightDirection : 'overhead',
                variationSeed: Number(data.variationSeed) || 1,
                tactileFeedback: data.tactileFeedback !== false,
                highContrast: storedAccessibilityPreferences.highContrast != null ? !!storedAccessibilityPreferences.highContrast : !!data.highContrast,
                reducedMotion: storedAccessibilityPreferences.reducedMotion != null ? !!storedAccessibilityPreferences.reducedMotion : !!data.reducedMotion,
                largeText: storedAccessibilityPreferences.largeText != null ? !!storedAccessibilityPreferences.largeText : !!data.largeText,
                simplifiedInstructions: storedAccessibilityPreferences.simplifiedInstructions != null ? !!storedAccessibilityPreferences.simplifiedInstructions : !!data.simplifiedInstructions,
                anatomicalView: ['dorsal', 'ventral', 'lateral', 'internal'].indexOf(data.anatomicalView) >= 0 ? data.anatomicalView : 'dorsal',
                crossSectionMode: !!data.crossSectionMode,
                specimenCondition: ['standard', 'preserved', 'dehydrated', 'cloudy', 'swollen'].indexOf(data.specimenCondition) >= 0 ? data.specimenCondition : 'standard',
                relationshipMode: !!data.relationshipMode,
                relationshipTraceIndex: Math.max(0, Number(data.relationshipTraceIndex) || 0),
                relationshipTraceCount: Math.max(0, Number(data.relationshipTraceCount) || 0),
                renderQuality: ['auto', 'high', 'balanced'].indexOf(data.renderQuality) >= 0 ? data.renderQuality : 'auto',
                procedureScenario: ADVANCED_SCENARIOS.some(function (scenario) { return scenario.id === data.procedureScenario; }) ? data.procedureScenario : 'precision-access',
                scenarioStartedAt: Math.max(0, Number(data.scenarioStartedAt) || 0),
                scenarioTimeRemaining: Math.max(0, Math.min(240, Number(data.scenarioTimeRemaining) || 0)),
                scenarioCompletedAt: Math.max(0, Number(data.scenarioCompletedAt) || 0),
                instructorTarget: Math.max(60, Math.min(95, Number(data.instructorTarget) || 75)),
                instructorMaxCautions: normalizeInstructorMaxCautions(data.instructorMaxCautions),
                instructorRequiredStructures: Math.max(1, Math.min(5, Number(data.instructorRequiredStructures) || 2)),
                procedureFeedback: loadIsProtectedRetry ? { message: 'Protected saved progress reloaded safely. Saving has resumed from that saved version.', tone: 'success', at: Date.now() } : null,
                selectedOrgan: null,
                _dissLoadedSpec: loadSpecimenKey,
                _dissSaveRetrying: false
              });
              if (loadIsProtectedRetry) {
                if (typeof announceToSR === 'function') announceToSR('Protected saved progress reloaded safely. Saving has resumed from that saved version.');
                focusResetControl('diss-reset-specimen');
              }
            }, 0);
          }
          // Canvas navigation shares one constrained zoom model across keyboard, wheel, buttons, and drag panning.
          function clampCanvasPan(value, zoomLevel, logicalSize) {
            var maxPan = Math.max(0, (Math.max(1, zoomLevel) - 1) * logicalSize / 2);
            return Math.max(-maxPan, Math.min(maxPan, Number(value) || 0));
          }
          function applyCanvasZoom(canvas, requestedZoom, anchorX, anchorY, announce) {
            if (!canvas) return;
            var logicalWidth = canvas._logicalW || canvas.width || 500;
            var logicalHeight = canvas._logicalH || canvas.height || 600;
            var currentZoom = Math.max(0.5, Math.min(3, Number(canvas._zoom) || Number(d.canvasZoom) || 1));
            var nextZoom = Math.round(Math.max(0.5, Math.min(3, Number(requestedZoom) || 1)) * 100) / 100;
            var currentPanX = Number(canvas._panX);
            var currentPanY = Number(canvas._panY);
            if (!isFinite(currentPanX)) currentPanX = Number(d.canvasPanX) || 0;
            if (!isFinite(currentPanY)) currentPanY = Number(d.canvasPanY) || 0;
            var focusX = isFinite(Number(anchorX)) ? Number(anchorX) : logicalWidth / 2;
            var focusY = isFinite(Number(anchorY)) ? Number(anchorY) : logicalHeight / 2;
            var worldOffsetX = (focusX - logicalWidth / 2 - currentPanX) / currentZoom;
            var worldOffsetY = (focusY - logicalHeight / 2 - currentPanY) / currentZoom;
            var nextPanX = clampCanvasPan(focusX - logicalWidth / 2 - worldOffsetX * nextZoom, nextZoom, logicalWidth);
            var nextPanY = clampCanvasPan(focusY - logicalHeight / 2 - worldOffsetY * nextZoom, nextZoom, logicalHeight);
            canvas._cameraTransition = null; canvas._zoom = nextZoom; canvas._panX = nextPanX; canvas._panY = nextPanY; canvas._interactionZoom = nextZoom; canvas._interactionPanX = nextPanX; canvas._interactionPanY = nextPanY;
            updMany({ canvasZoom: nextZoom, canvasPanX: nextPanX, canvasPanY: nextPanY });
            if (announce) {
              var zoomMessage = 'Specimen zoom ' + Math.round(nextZoom * 100) + ' percent' + (nextZoom === 1 ? '. View centered.' : '. Pan remains constrained to the specimen stage.');
              setProcedureFeedback(zoomMessage, 'working');
            }
          }
          function panCanvasByControl(e, horizontalStep, verticalStep, label) {
            var root = e.currentTarget && e.currentTarget.closest ? e.currentTarget.closest('[data-dissection-root]') : null;
            var canvas = root && root.querySelector ? root.querySelector('[data-diss-canvas]') : null;
            if (!canvas) return;
            var zoomLevel = Math.max(1, Number(canvas._zoom) || Number(d.canvasZoom) || 1);
            var logicalWidth = canvas._logicalW || canvas.width || 500, logicalHeight = canvas._logicalH || canvas.height || 600;
            var currentPanX = isFinite(Number(canvas._interactionPanX)) ? Number(canvas._interactionPanX) : (Number(d.canvasPanX) || 0);
            var currentPanY = isFinite(Number(canvas._interactionPanY)) ? Number(canvas._interactionPanY) : (Number(d.canvasPanY) || 0);
            var nextPanX = clampCanvasPan(currentPanX + horizontalStep, zoomLevel, logicalWidth);
            var nextPanY = clampCanvasPan(currentPanY + verticalStep, zoomLevel, logicalHeight);
            animateCanvasCamera(canvas, zoomLevel, nextPanX, nextPanY, 'Specimen view moved ' + label + '.');
          }

          function animateCanvasCamera(canvas, nextZoom, nextPanX, nextPanY, message) {
            if (!canvas) return;
            var logicalWidth = canvas._logicalW || canvas.width || 500, logicalHeight = canvas._logicalH || canvas.height || 600;
            nextZoom = Math.max(0.5, Math.min(3, Number(nextZoom) || 1));
            nextPanX = clampCanvasPan(nextPanX, nextZoom, logicalWidth);
            nextPanY = clampCanvasPan(nextPanY, nextZoom, logicalHeight);
            canvas._cameraTransition = {
              startedAt: Date.now(),
              duration: 520,
              fromZoom: Number(canvas._interactionZoom) || Number(canvas._zoom) || 1,
              fromPanX: Number(canvas._interactionPanX) || Number(canvas._panX) || 0,
              fromPanY: Number(canvas._interactionPanY) || Number(canvas._panY) || 0,
              toZoom: nextZoom,
              toPanX: nextPanX,
              toPanY: nextPanY
            };
            canvas._zoom = nextZoom; canvas._panX = nextPanX; canvas._panY = nextPanY;
            updMany({ canvasZoom: nextZoom, canvasPanX: nextPanX, canvasPanY: nextPanY });
            if (message) {
              setProcedureFeedback(message, 'working');
            }
          }
          function frameSelectedStructure(e) {
            if (!sel) { setProcedureFeedback('Select a visible structure before using camera focus.', 'caution'); return; }
            var root = e.currentTarget && e.currentTarget.closest ? e.currentTarget.closest('[data-dissection-root]') : null;
            var canvas = root && root.querySelector ? root.querySelector('[data-diss-canvas]') : null;
            if (!canvas) return;
            var logicalWidth = canvas._logicalW || canvas.width || 500, logicalHeight = canvas._logicalH || canvas.height || 600;
            var selectedPoint = variedOrganPoint(sel);
            var selectedScale = specimenScaleFactors();
            var displayX = (selectedPoint.x - 0.5) * selectedScale.x + 0.5;
            var displayY = (selectedPoint.y - 0.45) * selectedScale.y + 0.45;
            var focusZoom = Math.max(1.65, Math.min(2.4, Number(d.canvasZoom) || 1.8));
            var focusPanX = -(displayX - 0.5) * logicalWidth * focusZoom;
            var focusPanY = -(displayY - 0.5) * logicalHeight * focusZoom;
            animateCanvasCamera(canvas, focusZoom, focusPanX, focusPanY, 'Camera framed on ' + sel.name + ' at ' + Math.round(focusZoom * 100) + ' percent zoom.');
          }

          function adjustCanvasZoomFromControl(e, delta) {
            var root = e.currentTarget && e.currentTarget.closest ? e.currentTarget.closest('[data-dissection-root]') : null;
            var canvas = root && root.querySelector ? root.querySelector('[data-diss-canvas]') : null;
            applyCanvasZoom(canvas, (canvas && canvas._zoom || d.canvasZoom || 1) + delta, null, null, true);
          }
          function resetCanvasView(e) {
            var root = e && e.currentTarget && e.currentTarget.closest ? e.currentTarget.closest('[data-dissection-root]') : null;
            var canvas = root && root.querySelector ? root.querySelector('[data-diss-canvas]') : null;
            animateCanvasCamera(canvas, 1, 0, 0, 'Camera returned to the full specimen at 100 percent zoom.');
          }
          function canvasWheelZoom(e) {
            if (!e.ctrlKey && !e.metaKey) return;
            e.preventDefault();
            var canvas = e.currentTarget, rect = canvas.getBoundingClientRect();
            var logicalWidth = canvas._logicalW || canvas.width || 500, logicalHeight = canvas._logicalH || canvas.height || 600;
            var anchorX = (e.clientX - rect.left) / Math.max(1, rect.width) * logicalWidth;
            var anchorY = (e.clientY - rect.top) / Math.max(1, rect.height) * logicalHeight;
            var delta = e.deltaY < 0 ? 0.15 : -0.15;
            applyCanvasZoom(canvas, (canvas._zoom || d.canvasZoom || 1) + delta, anchorX, anchorY, false);
            if (canvas._wheelZoomTimer) clearTimeout(canvas._wheelZoomTimer);
            canvas._wheelZoomTimer = setTimeout(function () {
              var wheelMessage = 'Specimen zoom ' + Math.round((canvas._zoom || 1) * 100) + ' percent.';
              setProcedureFeedback(wheelMessage, 'working');
              canvas._wheelZoomTimer = null;
            }, 220);
          }

          // Keyboard shortcuts stay inside the active lab and never intercept form controls.
          var dissectionKeyHandler = function (e) {
            var target = e.target;
            var tag = target && target.tagName ? target.tagName.toLowerCase() : '';
            if (tag === 'input' || tag === 'textarea' || tag === 'select' || (target && target.isContentEditable)) return;
            var root = target && target.closest ? target.closest('[data-dissection-root]') : null;
            if (!root) return;
            var isCanvas = !!(target && target.matches && target.matches('[data-diss-canvas]'));
            if (!isCanvas && e.key !== 'Escape') return;
            var keyboardOrgans = organs.filter(function (organ) { return structureExposureState(organ, currentProcedure) === 'visible'; });
            function keyboardPreview(organ) {
              if (!organ) return;
              target._keyboardPreviewOrganId = organ.id;
              // Arrow-key browsing previews a structure without awarding exploration
              // progress. Enter or Space commits the inspection.
              var previewPosition = keyboardOrgans.indexOf(organ) + 1;
              var keyboardMessage;
              if (d.quizMode) {
                updMany({ selectedOrgan: null, hoveredOrgan: organ.id });
                keyboardMessage = effectiveQuizAnswerMode === 'hotspot'
                  ? 'Candidate ' + previewPosition + ' of ' + keyboardOrgans.length + ' focused. Press Enter to submit this unlabeled structure.'
                  : 'Specimen candidate focused. This question uses answer choices; press Tab to reach them.';
              } else {
                upd('selectedOrgan', organ.id);
                keyboardMessage = 'Previewing ' + organ.name + '. Press Enter to inspect this structure.';
              }
              setProcedureFeedback(keyboardMessage, 'working');
            }

            if (e.shiftKey && (e.key === 'ArrowRight' || e.key === 'ArrowDown' || e.key === 'ArrowLeft' || e.key === 'ArrowUp')) {
              e.preventDefault();
              var panStep = 54;
              var panHorizontal = e.key === 'ArrowLeft' ? -panStep : (e.key === 'ArrowRight' ? panStep : 0);
              var panVertical = e.key === 'ArrowUp' ? -panStep : (e.key === 'ArrowDown' ? panStep : 0);
              var panLabel = e.key.replace('Arrow', '').toLowerCase();
              var syntheticPanEvent = { currentTarget: target };
              panCanvasByControl(syntheticPanEvent, panHorizontal, panVertical, panLabel);
            } else if (e.key === 'ArrowRight' || e.key === 'ArrowDown' || e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
              e.preventDefault();
              if (!keyboardOrgans.length) return;
              var direction = (e.key === 'ArrowRight' || e.key === 'ArrowDown') ? 1 : -1;
              var keyboardPreviewId = target._keyboardPreviewOrganId;
              var organIndex = keyboardOrgans.findIndex(function (organ) { return organ.id === keyboardPreviewId; });
              if (organIndex < 0) organIndex = direction > 0 ? -1 : 0;
              keyboardPreview(keyboardOrgans[(organIndex + direction + keyboardOrgans.length) % keyboardOrgans.length]);
            } else if (e.key === 'Home' || e.key === 'End') {
              e.preventDefault();
              keyboardPreview(e.key === 'Home' ? keyboardOrgans[0] : keyboardOrgans[keyboardOrgans.length - 1]);
            } else if (e.key === 'Enter' || e.key === ' ') {
              e.preventDefault();
              if (d.quizMode && effectiveQuizAnswerMode !== 'hotspot') {
                setProcedureFeedback('This assessment uses the answer choices. Press Tab to choose an answer in the assessment panel.', 'working');
                focusDissectionTarget('diss-quiz-panel');
                return;
              }
              var keyboardCommitId = target._keyboardPreviewOrganId;
              var keyboardSelected = keyboardOrgans.find(function (organ) { return organ.id === keyboardCommitId; });
              if (!keyboardSelected) {
                setProcedureFeedback('Use an Arrow key, Home, or End to preview a structure before pressing Enter.', 'working');
              target._keyboardPreviewOrganId = null;
                return;
              }
              if (d.quizMode) {
                if (effectiveQuizAnswerMode === 'hotspot' && !d.quizFeedback) submitQuizAnswer(keyboardSelected.id);
                else {
                  setProcedureFeedback('This assessment uses the answer choices. Press Tab to choose an answer in the assessment panel.', 'working');
                  focusDissectionTarget('diss-quiz-panel');
                }
              } else if (activeInstrument === 'probe' && procedureToolReadinessData('probe', currentProcedure).safeToAct) {
                var keyboardProbeResult = performProcedureAction('probe', { organ: keyboardSelected });
                if (keyboardProbeResult && keyboardProbeResult.ok) queueProcedureInstrumentReplay('probe', { point: keyboardProbeResult.point || variedOrganPoint(keyboardSelected) });
              } else {
                chooseOrganFromDirectory(keyboardSelected);
                setProcedureFeedback('Inspected ' + keyboardSelected.name + '. Structure details and evidence notes are available beside the specimen.', 'success');
              }
            } else if (e.key === 'Escape') {
              e.preventDefault();
              if (cancelActiveCanvasGesture('Instrument gesture canceled with Escape; no action was recorded.', target, e)) return;
              if (d.resetConfirmPending) cancelSpecimenReset();
              else upd('selectedOrgan', null);
            } else if (e.key === '0' || e.key === 'r' || e.key === 'R') {
              e.preventDefault();
              applyCanvasZoom(target, 1, null, null, true);
            } else if (e.key === '+' || e.key === '=') {
              e.preventDefault();
              applyCanvasZoom(target, (target._zoom || d.canvasZoom || 1) + 0.25, null, null, true);
            } else if (e.key === '-' || e.key === '_') {
              e.preventDefault();
              applyCanvasZoom(target, (target._zoom || d.canvasZoom || 1) - 0.25, null, null, true);
            } else if (e.key === 'v' || e.key === 'V') {
              e.preventDefault();
              var keyboardViews = ['dorsal', 'ventral', 'lateral', 'internal'];
              changeAnatomicalView(keyboardViews[(keyboardViews.indexOf(d.anatomicalView || 'dorsal') + 1) % keyboardViews.length], 'keyboard shortcut V');
            } else if (advancedWorkspace && (e.key === 'x' || e.key === 'X')) {
              e.preventDefault(); upd('crossSectionMode', !d.crossSectionMode);
            } else if (advancedWorkspace && (e.key === 'm' || e.key === 'M')) {
              e.preventDefault(); cycleLensMagnification();
            } else if (advancedWorkspace && (e.key === 'p' || e.key === 'P')) {
              e.preventDefault(); toggleInspectionPin();
            } else if (advancedWorkspace && (e.key === 'f' || e.key === 'F')) {
              e.preventDefault(); cycleLensFocusDepth();
            } else if (/^[1-7]$/.test(e.key)) {
              var keyboardTool = PROCEDURE_INSTRUMENTS[Number(e.key) - 1];
              if (keyboardTool) { e.preventDefault(); selectProcedureInstrument(keyboardTool.id, 'keyboard shortcut ' + e.key); }
            }
          };
          // Retain the legacy hook for integrations that invoke the active lab
          // directly. Normal key dispatch below resolves the originating root.
          window._dissectionKeyHandler = dissectionKeyHandler;

          if (!window._dissectionKeyBound) {
            window._dissectionKeyBound = true;
            window.addEventListener('keydown', function (e) {
              var eventTarget = e && e.target;
              var eventRoot = eventTarget && eventTarget.closest ? eventTarget.closest('[data-dissection-root]') : null;
              var eventCanvas = eventRoot && eventRoot.querySelector ? eventRoot.querySelector('[data-diss-canvas]') : null;
              var eventHandler = eventCanvas && eventCanvas._dissKeyHandler;
              if (eventHandler) eventHandler(e);
            });
          }


          // Simple sound effects via Web Audio API

          var audioCtx = null;

          function playDissectSound(type) {

            if (!disSoundEnabled()) return;
            try {

              if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();

              var osc = audioCtx.createOscillator();

              var gain = audioCtx.createGain();

              osc.connect(gain); gain.connect(audioCtx.destination);

              gain.gain.setValueAtTime(0.05, audioCtx.currentTime);

              if (type === 'pin') { osc.frequency.setValueAtTime(880, audioCtx.currentTime); osc.type = 'sine'; gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.1); osc.start(); osc.stop(audioCtx.currentTime + 0.1); }

              else if (type === 'peel') { osc.frequency.setValueAtTime(220, audioCtx.currentTime); osc.frequency.linearRampToValueAtTime(440, audioCtx.currentTime + 0.2); osc.type = 'sawtooth'; gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.25); osc.start(); osc.stop(audioCtx.currentTime + 0.25); }

              else if (type === 'success') { osc.frequency.setValueAtTime(523, audioCtx.currentTime); osc.type = 'sine'; gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.15); osc.start(); osc.stop(audioCtx.currentTime + 0.15); var osc2 = audioCtx.createOscillator(); var g2 = audioCtx.createGain(); osc2.connect(g2); g2.connect(audioCtx.destination); g2.gain.setValueAtTime(0.05, audioCtx.currentTime + 0.12); osc2.frequency.setValueAtTime(659, audioCtx.currentTime + 0.12); osc2.type = 'sine'; g2.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.3); osc2.start(audioCtx.currentTime + 0.12); osc2.stop(audioCtx.currentTime + 0.3); }

            } catch (e) { }

          }



          function canvasPointerModeData(canvas, hit) {
            canvas = canvas || {};
            if (canvas._cameraTransition) return { mode: 'wait', cursor: 'wait', tone: 'caution', icon: '?', label: 'Camera moving', instruction: 'Wait for the specimen view to settle before contact.' };
            if (d.quizMode) {
              if (d.quizFeedback) return { mode: 'answered', cursor: 'not-allowed', tone: 'blocked', icon: '?', label: 'Answer recorded', instruction: 'Use Next question before selecting another candidate.' };
              return { mode: 'identify', cursor: 'crosshair', tone: 'assessment', icon: '?', label: 'Identify a structure', instruction: effectiveQuizAnswerMode === 'hotspot' ? 'Select an unlabeled visible candidate; Arrow keys preview and Enter submits.' : 'Use the answer choices below the specimen.' };
            }
            if (d.annotateMode) return { mode: 'annotate', cursor: 'crosshair', tone: 'ready', icon: '?', label: 'Add observation mark', instruction: 'Select the specimen to place a visible observation mark.' };
            if (d.rulerMode) return { mode: 'measure', cursor: 'crosshair', tone: 'ready', icon: '?', label: 'Measure', instruction: d.rulerStart ? 'Select the second endpoint to complete the measurement.' : 'Select the first measurement endpoint.' };
            if (guidedMode) return { mode: 'locate', cursor: hit ? 'pointer' : 'crosshair', tone: 'ready', icon: '?', label: 'Locate the guided target', instruction: 'Select a visible structure; procedure tools are paused during Guided investigation.' };
            if (canvas._isPanning) return { mode: 'panning', cursor: 'grabbing', tone: 'ready', icon: '?', label: 'Panning specimen', instruction: 'Release to keep this view position.' };
            if ((d.canvasZoom || 1) > 1.01 && activeInstrument === 'probe') return { mode: 'pan-or-probe', cursor: 'grab', tone: 'ready', icon: '?', label: 'Pan or trace', instruction: 'Drag the background to pan; drag a visible structure to trace it.' };
            var next = nextProcedureInfo();
            var techniqueActive = !revealedLayers[activeLayer] && next.action !== 'complete';
            if (!techniqueActive) return { mode: 'explore', cursor: hit ? 'pointer' : 'crosshair', tone: 'ready', icon: '?', label: 'Inspect anatomy', instruction: 'Select a visible structure, or use Arrow keys to preview and Enter to inspect.' };
            var readiness = procedureToolReadinessData(activeInstrument, currentProcedure);
            var intent = canvas._toolIntentState && canvas._toolIntentState.tool === activeInstrument ? canvas._toolIntentState : null;
            if (!readiness.safeToAct || (intent && !intent.valid)) {
              return { mode: 'blocked', cursor: 'not-allowed', tone: 'blocked', icon: '!', label: readiness.label || 'Contact blocked', instruction: intent && intent.targetLabel ? 'Move toward ' + intent.targetLabel + '. ' + readiness.cue : readiness.cue };
            }
            if (next.action === 'inspect' && activeInstrument === 'probe') return { mode: 'inspect', cursor: 'crosshair', tone: 'ready', icon: '?', label: 'Confirm orientation', instruction: 'Select the specimen only after the recommended view and landmarks are confirmed.' };
            var toolModes = {
              scalpel: { mode: 'cut', cursor: 'crosshair', icon: '?', label: 'Draw shallow entry', instruction: 'Press and drag along the planned corridor, then release.' },
              scissors: { mode: 'extend', cursor: 'crosshair', icon: '?', label: 'Extend opening', instruction: 'Press and drag within the established opening, then release.' },
              forceps: { mode: 'lift', cursor: 'grab', icon: '?', label: 'Lift tissue edge', instruction: 'Press the opened edge, drag with controlled tension, then release.' },
              pin: { mode: 'anchor', cursor: 'crosshair', icon: '?', label: 'Place stabilizer', instruction: 'Press an endpoint, drag to set the angle, then release.' },
              probe: { mode: 'trace', cursor: 'crosshair', icon: '?', label: 'Trace visible boundary', instruction: 'Press a visible structure, trace a short controlled path, then release.' },
              dropper: { mode: 'hydrate', cursor: 'crosshair', icon: '?', label: 'Apply controlled film', instruction: 'Press the specimen, drag a short flow-aligned path, then release.' },
              wick: { mode: 'wick', cursor: 'crosshair', icon: '?', label: 'Recover pooled saline', instruction: 'Press the pool edge, drag outward, then release.' }
            };
            var modeData = toolModes[activeInstrument] || { mode: 'inspect', cursor: 'crosshair', icon: '?', label: 'Interact with specimen', instruction: readiness.cue };
            if (intent && intent.label && intent.label !== 'READY') modeData = Object.assign({}, modeData, { label: intent.label + ' ? ' + modeData.label });
            return Object.assign({ tone: 'ready' }, modeData);
          }
          function syncCanvasPointerPresentation(canvas, hit) {
            if (!canvas) return canvasPointerModeData(null, hit);
            var pointerMode = canvasPointerModeData(canvas, hit);
            canvas.style.cursor = pointerMode.cursor;
            canvas.setAttribute('data-cursor-mode', pointerMode.mode);
            canvas.setAttribute('data-cursor-tone', pointerMode.tone);
            var pointerGuide = queryDissectionNode('[data-diss-pointer-guide]');
            if (pointerGuide) {
              pointerGuide.setAttribute('data-tone', pointerMode.tone);
              pointerGuide.setAttribute('data-mode', pointerMode.mode);
              var pointerGuideIcon = pointerGuide.querySelector('[data-pointer-guide-icon]');
              var pointerGuideLabel = pointerGuide.querySelector('[data-pointer-guide-label]');
              var pointerGuideInstruction = pointerGuide.querySelector('[data-pointer-guide-instruction]');
              if (pointerGuideIcon) pointerGuideIcon.textContent = pointerMode.icon;
              if (pointerGuideLabel) pointerGuideLabel.textContent = 'Pointer mode ? ' + pointerMode.label;
              if (pointerGuideInstruction) pointerGuideInstruction.textContent = pointerMode.instruction;
            }
            return pointerMode;
          }
          var canvasClick = function (e) {

            var canvas = e.target;
            if (canvas._cameraTransition) {
              canvas._suppressToolClick = false;
              setProcedureFeedback('Wait for the specimen camera movement to finish before selecting or making contact.', 'caution');
              syncCanvasPointerPresentation(canvas, null);
              return;
            }
            if (canvas._wasPanning) { canvas._wasPanning = false; return; }
            if (canvas._suppressToolClick) { canvas._suppressToolClick = false; return; }

            var rect = canvas.getBoundingClientRect();

            var rawX = (e.clientX - rect.left) / rect.width, rawY = (e.clientY - rect.top) / rect.height;
            canvas._parallaxTargetX = parallaxDepth ? (rawX - 0.5) * 8 : 0;
            canvas._parallaxTargetY = parallaxDepth ? (rawY - 0.5) * 6 : 0;

            // Inverse-transform for zoom + pan

            var _z = Number(canvas._interactionZoom) || d.canvasZoom || 1, _px = Number(canvas._interactionPanX), _py = Number(canvas._interactionPanY); if (!isFinite(_px)) _px = d.canvasPanX || 0; if (!isFinite(_py)) _py = d.canvasPanY || 0;

            var clickLogicalWidth = canvas._logicalW || canvas.width || 500, clickLogicalHeight = canvas._logicalH || canvas.height || 600;
            var mx = (rawX - 0.5 - _px / clickLogicalWidth) / _z + 0.5;

            var my = (rawY - 0.5 - _py / clickLogicalHeight) / _z + 0.5;
            mx -= (Number(canvas._parallaxX) || 0) / clickLogicalWidth;
            my -= (Number(canvas._parallaxY) || 0) / clickLogicalHeight;
            var mappedPoint = inverseSpecimenVariation({ x: mx, y: my });
            mx = mappedPoint.x; my = mappedPoint.y;
            var techniqueContactActive = !guidedMode && !revealedLayers[activeLayer] && nextProcedureInfo().action !== 'complete';
            if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniqueContactActive) {
              if (activeInstrument === 'probe' && nextProcedureInfo().action === 'inspect') {
                if (!canBeginDirectInstrument('probe')) return;
                if (!specimenContactContext({ x: mx, y: my }).onSpecimen) {
                  setProcedureFeedback('Move the orientation marker onto the specimen before recording the view.', 'caution');
                  return;
                }
                var inspectResult = performProcedureAction('inspect', { point: { x: mx, y: my }, inputType: 'pointer' });
                if (inspectResult && inspectResult.ok) queueProcedureInstrumentReplay('probe', { point: { x: mx, y: my } });
                return;
              }
              if (activeInstrument === 'dropper' || activeInstrument === 'wick') {
                setProcedureFeedback((activeInstrument === 'dropper' ? 'Apply saline' : 'Recover pooled saline') + ' with a short press-drag-release path; a tap does not record technique.', 'working');
                return;
              }
              if (activeInstrument === 'scalpel' || activeInstrument === 'scissors' || activeInstrument === 'forceps' || activeInstrument === 'pin') {
                setProcedureFeedback('Use the visible pointer mode: press, drag through the planned motion, then release. A tap does not record technique.', 'working');
                return;
              }
            }

            var clickPointerCoarse = false;
            try { clickPointerCoarse = !!(window.matchMedia && window.matchMedia('(pointer: coarse)').matches); } catch (e) {}
            var clickDisplayMin = Math.max(1, Math.min(rect.width || 1, rect.height || 1));
            var minimumCssHitRadius = (clickPointerCoarse ? 22 : 14) / clickDisplayMin / Math.max(1, _z);
            var clickHitRadius = Math.max(minimumCssHitRadius, 0.028, (visualRealism === 'accessible' ? 0.065 : 0.05) / Math.sqrt(Math.max(1, Number(d.canvasZoom) || 1)));
            var hit = closestVisibleOrganAt(mx, my, clickHitRadius, canvas);
            var hitHandledByProcedure = false;
            if (hit && d.quizMode && effectiveQuizAnswerMode === 'hotspot' && !d.quizFeedback) submitQuizAnswer(hit.id);
            if (!d.quizMode) upd('selectedOrgan', hit ? (hit.id === d.selectedOrgan ? null : hit.id) : null);
            else upd('hoveredOrgan', null);

            if (hit) {
              playDissectSound('pin'); sfxDisPin();
              if (window._alloHaptic) window._alloHaptic('click');
              if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniqueContactActive && activeInstrument === 'probe' && nextProcedureInfo().action === 'probe') {
                hitHandledByProcedure = true;
                setProcedureFeedback('Target previewed: ' + hit.name + '. Press and trace its visible boundary before releasing; a tap does not record the probe step.', 'working');
              } else if (!d.quizMode && !d.annotateMode && !d.rulerMode && activeInstrument === 'probe') {
                setProcedureFeedback('Selected ' + hit.name + '. ' + procedureInstrumentStatus('probe').instruction, 'working');
              }

            }

            // Annotation mode: add to drawing

            if (!d.quizMode && d.annotateMode) {

              var annots = d.annotations || [];

              annots.push({ x: mx, y: my, type: 'dot' });

              upd('annotations', annots);

              if (annots.length > 1) {

                // Connect to previous dot

                annots[annots.length - 1].prevX = annots[annots.length - 2].x;

                annots[annots.length - 1].prevY = annots[annots.length - 2].y;

                upd('annotations', annots.slice());

              }
              setProcedureFeedback('Annotation point ' + annots.length + ' added. Select another point to continue the line.', 'working');

            }

            // Ruler mode: set start/end points

            if (!d.quizMode && d.rulerMode) {

              if (!d.rulerStart || d.rulerEnd) {

                upd('rulerStart', { x: mx, y: my });

                upd('rulerEnd', null);
                setProcedureFeedback('Ruler start point set. Select a second point to complete the measurement.', 'working');

              } else {

                upd('rulerEnd', { x: mx, y: my });
                var measureDx = mx - d.rulerStart.x, measureDy = my - d.rulerStart.y;
                var measureScale = spec.bodyShape === 'worm' ? 15 : spec.bodyShape === 'pig' ? 25 : spec.bodyShape === 'fish' ? 20 : spec.bodyShape === 'crayfish' ? 12 : spec.bodyShape === 'frog' ? 8 : 3;
                setProcedureFeedback('Measurement complete: ' + (Math.sqrt(measureDx * measureDx + measureDy * measureDy) * measureScale).toFixed(1) + ' cm. Select again to start a new measurement.');

              }

            }

            // Track explored organs for progress

            if (hit && !hitHandledByProcedure && !d.quizMode && !d.annotateMode && !d.rulerMode) {

              var explored = Object.assign({}, d.exploredOrgans || {});

              explored[specimen + '|' + hit.id] = true;

              upd('exploredOrgans', explored);

              // Locating a Guided target marks it viewed. A matching observation
              // must be chosen before the step earns verification credit.
              if (guidedMode && currentGuided && hit.id === currentGuided.organId) {
                beginGuidedObservation(hit);
              }

            }

          };



          // Hover handler for canvas tooltips

          var canvasHover = function (e) {

            var canvas = e.target; var rect = canvas.getBoundingClientRect();

            var rawX = (e.clientX - rect.left) / rect.width, rawY = (e.clientY - rect.top) / rect.height;
            canvas._parallaxTargetX = parallaxDepth ? (rawX - 0.5) * 8 : 0;
            canvas._parallaxTargetY = parallaxDepth ? (rawY - 0.5) * 6 : 0;

            // Inverse-transform for zoom + pan

            var _z = Number(canvas._interactionZoom) || d.canvasZoom || 1, _px = Number(canvas._interactionPanX), _py = Number(canvas._interactionPanY); if (!isFinite(_px)) _px = d.canvasPanX || 0; if (!isFinite(_py)) _py = d.canvasPanY || 0;

            var hoverLogicalWidth = canvas._logicalW || canvas.width || 500, hoverLogicalHeight = canvas._logicalH || canvas.height || 600;
            var mx = (rawX - 0.5 - _px / hoverLogicalWidth) / _z + 0.5;

            var my = (rawY - 0.5 - _py / hoverLogicalHeight) / _z + 0.5;
            mx -= (Number(canvas._parallaxX) || 0) / hoverLogicalWidth;
            my -= (Number(canvas._parallaxY) || 0) / hoverLogicalHeight;
            var mappedPoint = inverseSpecimenVariation({ x: mx, y: my });
            mx = mappedPoint.x; my = mappedPoint.y;
            var priorToolPointer = canvas._toolPointer;
            if (priorToolPointer) {
              var vectorX = mx - priorToolPointer.x, vectorY = my - priorToolPointer.y;
              if (Math.abs(vectorX) + Math.abs(vectorY) > 0.0015) canvas._toolVector = { x: vectorX, y: vectorY };
            }
            canvas._toolPointer = { x: mx, y: my, at: Date.now() };
            canvas._toolPressure = Number(e.pressure) || (e.buttons ? 0.5 : 0.12);

            var hoverHitRadius = Math.max(0.024, (visualRealism === 'accessible' ? 0.06 : 0.04) / Math.sqrt(Math.max(1, Number(d.canvasZoom) || 1)));
            var hit = closestVisibleOrganAt(mx, my, hoverHitRadius, canvas);
            var nextHoveredOrgan = hit ? hit.id : null;
            if (nextHoveredOrgan !== d.hoveredOrgan) upd('hoveredOrgan', nextHoveredOrgan);

            // Cutting tools combine protocol readiness, path alignment, and anatomy protection before contact.
            if (!canvas._toolDrawing && !d.quizMode && !d.annotateMode && !d.rulerMode && (activeInstrument === 'scalpel' || activeInstrument === 'scissors')) {
              var hoverCuttingPoint = { x: mx, y: my };
              var hoverCuttingContact = specimenContactContext(hoverCuttingPoint);
              var hoverCuttingSafety = hoverCuttingContact.onSpecimen ? cuttingTrajectorySafety(hoverCuttingPoint, canvas._toolVector, canvas) : null;
              var hoverCuttingInstrumentState = procedureInstrumentStatus(activeInstrument);
              var hoverCuttingReadiness = procedureToolReadinessData(activeInstrument, currentProcedure);
              var hoverCuttingSequenceReady = hoverCuttingInstrumentState.readiness === 'ready';
              var hoverCuttingSetupReady = hoverCuttingReadiness.tone === 'ready';
              var hoverCuttingGuideTarget = nearestProcedureGuidePoint(hoverCuttingPoint, procedureGuidePoints());
              var hoverCuttingAligned = hoverCuttingGuideTarget.distance <= 0.085;
              var hoverCuttingValid = hoverCuttingSequenceReady && hoverCuttingSetupReady && hoverCuttingContact.onSpecimen && !hoverCuttingSafety && hoverCuttingAligned;
              var hoverCuttingLabel = !hoverCuttingSequenceReady ? 'WAIT'
                : (!hoverCuttingSetupReady ? 'ADJUST'
                  : (!hoverCuttingContact.onSpecimen ? 'TRAY' : (hoverCuttingSafety ? 'PROTECT' : (!hoverCuttingAligned ? 'ALIGN' : 'READY'))));
              var cuttingSafetyKey = hoverCuttingLabel + '|' + (hoverCuttingSafety ? hoverCuttingSafety.organ.id + '|' + (hoverCuttingSafety.critical ? 'stop' : 'ahead') : 'clear');
              canvas._toolIntentState = {
                tool: activeInstrument,
                valid: hoverCuttingValid,
                label: hoverCuttingLabel,
                tone: hoverCuttingValid ? 'ready' : 'caution',
                target: hoverCuttingSequenceReady && hoverCuttingSetupReady && !hoverCuttingSafety && !hoverCuttingValid ? { x: hoverCuttingGuideTarget.x, y: hoverCuttingGuideTarget.y } : null,
                targetLabel: 'Safe path',
                at: Date.now()
              };
              if (canvas._cuttingSafetyState !== cuttingSafetyKey) {
                canvas._cuttingSafetyState = cuttingSafetyKey;
                var cuttingSafetyStatus = queryDissectionNode('[data-diss-tool-status]');
                if (cuttingSafetyStatus) {
                  cuttingSafetyStatus.setAttribute('data-tone', hoverCuttingValid ? 'working' : 'caution');
                  cuttingSafetyStatus.textContent = !hoverCuttingSequenceReady ? hoverCuttingInstrumentState.instruction
                    : (!hoverCuttingSetupReady ? hoverCuttingReadiness.cue
                      : (!hoverCuttingContact.onSpecimen
                        ? 'Cutting tool above the tray; follow the dashed arrow onto the specimen-specific access path.'
                        : (hoverCuttingSafety
                          ? (hoverCuttingSafety.critical ? 'Trajectory stop: protect ' : 'Trajectory landmark ahead: ') + hoverCuttingSafety.organ.name + '. Adjust direction before contact.'
                          : (!hoverCuttingAligned ? 'Align with the ' + procedureProtocol.route + '. The dashed arrow marks the nearest safe path.' : 'Projected cutting path clear and aligned with the teaching corridor.'))));
                }
              }
            }

            // Non-cutting previews pair sequence and calibration readiness with a visible nearest anatomical target.
            if (!canvas._toolDrawing && !d.quizMode && !d.annotateMode && !d.rulerMode && ['forceps', 'pin', 'probe', 'dropper', 'wick'].indexOf(activeInstrument) >= 0) {
              var hoverPoint = { x: mx, y: my };
              var hoverGuide = procedureGuidePoints();
              var hoverPins = currentProcedure.pins || [];
              var hoverEndpointDistance = Math.min(procedurePointDistance(hoverPoint, hoverGuide[0]), procedurePointDistance(hoverPoint, hoverGuide[hoverGuide.length - 1]));
              var hoverPinSpacing = hoverPins.length ? procedurePointDistance(hoverPoint, hoverPins[0]) : 1;
              var hoverContact = specimenContactContext(hoverPoint);
              var hoverInstrumentState = procedureInstrumentStatus(activeInstrument);
              var hoverReadiness = procedureToolReadinessData(activeInstrument, currentProcedure);
              var hoverSequenceReady = hoverInstrumentState.readiness === 'ready';
              var hoverSetupReady = hoverReadiness.tone === 'ready';
              var previewValid = false, previewMessage = '', previewTone = 'caution', previewDetailKey = '', previewTarget = null, previewTargetLabel = '';
              if (activeInstrument === 'forceps') {
                previewValid = hoverSequenceReady && currentProcedure.incisionExtended && !currentProcedure.retracted && distanceToGuide(hoverPoint, hoverGuide) <= 0.11;
                if (hoverSequenceReady && !previewValid) {
                  var hoverForcepsTarget = nearestProcedureGuidePoint(hoverPoint, hoverGuide);
                  previewTarget = { x: hoverForcepsTarget.x, y: hoverForcepsTarget.y }; previewTargetLabel = 'Lift edge';
                }
                previewMessage = previewValid ? 'Forceps lift ready. Press on the tissue edge, drag away, and release inside the solid safe envelope.' : 'Move the forceps toward the opened corridor.';
              } else if (activeInstrument === 'pin') {
                var hoverPinPrerequisite = procedureProtocol.preStabilize ? currentProcedure.inspected : currentProcedure.retracted;
                previewValid = hoverSequenceReady && hoverPinPrerequisite && hoverPins.length < 2 && hoverEndpointDistance <= 0.14 && hoverPinSpacing >= 0.18;
                if (hoverSequenceReady && hoverPinPrerequisite && hoverPins.length < 2 && !previewValid) {
                  var hoverStartPoint = hoverGuide[0], hoverEndPoint = hoverGuide[hoverGuide.length - 1];
                  var hoverPinTarget = hoverPins.length
                    ? (procedurePointDistance(hoverStartPoint, hoverPins[0]) >= procedurePointDistance(hoverEndPoint, hoverPins[0]) ? hoverStartPoint : hoverEndPoint)
                    : (procedurePointDistance(hoverPoint, hoverStartPoint) <= procedurePointDistance(hoverPoint, hoverEndPoint) ? hoverStartPoint : hoverEndPoint);
                  previewTarget = { x: hoverPinTarget.x, y: hoverPinTarget.y }; previewTargetLabel = hoverPins.length ? 'Opposite anchor' : 'Anchor point';
                }
                previewMessage = hoverPins.length >= 2 ? 'Pin placement complete: the tissue layer is stable.'
                  : (hoverPinSpacing < 0.18 ? 'Pin preview invalid: follow the arrow to the opposite anchor.'
                    : (previewValid ? 'Pin placement ready. Press the endpoint, drag inward along the shaft, and release when the anchor reads stable.' : 'Move the pin toward the indicated corridor endpoint.'));
              } else if (activeInstrument === 'probe') {
                var hoverProbePressure = Math.max(10, Math.min(80, Number(toolCalibration.probePressure) || 30));
                var hoverProbeState = hoverProbePressure < 18 ? 'too light for a clear tactile signal' : (hoverProbePressure > 38 ? 'excess pressure; tissue stress risk' : 'controlled palpation');
                var hoverProbeDepth = hit ? anatomicalDepthLabel(structureOpticalDepth(hit)) : '';
                var hoverProbeMode = nextProcedureInfo().action === 'inspect' ? 'inspect' : 'trace';
                var hoverProbePrerequisite = hoverProbeMode === 'inspect' || hoverPins.length >= 2;
                previewValid = hoverSequenceReady && hoverProbePrerequisite && !!hit;
                if (hoverSequenceReady && hoverProbePrerequisite && !hit) {
                  var hoverStructureTarget = nearestVisibleStructureTarget(hoverPoint);
                  if (hoverStructureTarget) { previewTarget = { x: hoverStructureTarget.x, y: hoverStructureTarget.y }; previewTargetLabel = hoverStructureTarget.name || 'Visible structure'; }
                }
                previewTone = previewValid && hoverProbePressure >= 18 && hoverProbePressure <= 38 ? 'working' : 'caution';
                previewDetailKey = hoverProbeMode + '|' + hoverProbeState + '|' + hoverProbeDepth + '|' + hoverProbePressure;
                previewMessage = hoverProbeMode === 'inspect'
                  ? (hit ? 'Inspection target: ' + hit.name + '. Select to review this landmark, then record orientation to advance the protocol.' : 'Move the probe toward the labeled landmark for pre-procedure inspection.')
                  : (hit ? 'Probe preview: ' + hoverProbeState + ' at ' + hoverProbePressure + '% on ' + hit.name + ', ' + hoverProbeDepth + '. ' + (SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog).response + ' response; select to record the trace.' : 'Move the probe toward the labeled visible structure.');
              } else if (activeInstrument === 'wick') {
                var hoverWickTissue = normalizeTissueState(currentProcedure.tissueState);
                var hoverPoolPoint = currentProcedure.dropperPoint;
                var hoverWickDistance = hoverPoolPoint ? procedurePointDistance(hoverPoint, hoverPoolPoint) : 1;
                previewValid = hoverSequenceReady && hoverContact.onSpecimen && !!hoverPoolPoint && hoverWickDistance <= 0.16;
                if (hoverSequenceReady && hoverPoolPoint && !previewValid) { previewTarget = { x: hoverPoolPoint.x, y: hoverPoolPoint.y }; previewTargetLabel = 'Pool edge'; }
                previewTone = previewValid ? 'working' : 'caution';
                previewDetailKey = hoverWickTissue.salineDrops + '|' + Math.round(hoverWickDistance * 100);
                previewMessage = previewValid ? 'Wick preview ready at the pool edge. Press, drag outward across a short recovery path, and release when control is stable; the equivalent button remains available.' : 'Follow the arrow toward the visible pool edge without touching exposed anatomy.';
              } else if (activeInstrument === 'dropper') {
                var hoverTissue = normalizeTissueState(currentProcedure.tissueState);
                var hoverDose = Math.max(1, Math.min(3, Math.round(Number(toolCalibration.salineDose) || 1)));
                var hoverDropperState = hoverTissue.moisture >= 96 ? 'surface already saturated' : (hoverDose === 1 ? 'controlled film' : (hoverDose === 2 ? 'broad spread; reduce dose for precision' : 'pooling risk; reduce dose'));
                var hoverHydrationProfile = specimenHydrationProfile();
                previewValid = hoverSequenceReady && hoverContact.onSpecimen && hoverTissue.moisture < 96;
                if (hoverSequenceReady && !hoverContact.onSpecimen) { previewTarget = { x: hoverGuide[1].x, y: hoverGuide[1].y }; previewTargetLabel = 'Specimen surface'; }
                previewTone = previewValid && hoverDose === 1 ? 'working' : 'caution';
                previewDetailKey = hoverDropperState + '|' + hoverDose + '|' + hoverTissue.moisture;
                previewMessage = !hoverContact.onSpecimen ? 'Follow the arrow to move the dropper over the specimen surface.'
                  : 'Dropper forecast: ' + hoverDropperState + ' at dose ' + hoverDose + '; moisture ' + hoverTissue.moisture + ' percent; ' + hoverHydrationProfile.pattern + ' flow. ' + (previewValid ? 'Select to apply.' : 'No additional saline is needed.');
              }
              if (!hoverSequenceReady) {
                previewValid = false; previewTarget = null; previewTone = 'caution'; previewMessage = hoverInstrumentState.instruction;
              } else if (!hoverSetupReady) {
                previewValid = false; previewTarget = null; previewTone = 'caution'; previewMessage = hoverReadiness.cue;
              } else if (['probe', 'dropper'].indexOf(activeInstrument) < 0) previewTone = previewValid ? 'working' : 'caution';
              var previewIntentLabel = previewValid
                ? (activeInstrument === 'probe' && hoverProbeMode === 'inspect' ? 'INSPECT' : 'READY')
                : ((!hoverContact.onSpecimen && (activeInstrument === 'dropper' || activeInstrument === 'wick')) ? 'TRAY' : (previewTarget ? 'MOVE' : 'WAIT'));
              canvas._toolIntentState = {
                tool: activeInstrument,
                valid: !!previewValid,
                label: previewIntentLabel,
                tone: previewTone,
                target: previewValid ? null : previewTarget,
                targetLabel: previewTargetLabel,
                at: Date.now()
              };
              var previewStateKey = activeInstrument + '|' + previewIntentLabel + '|' + (hit ? hit.id : '') + '|' + hoverPins.length + '|' + previewDetailKey + '|' + hoverReadiness.tone;
              if (canvas._toolPreviewState !== previewStateKey) {
                canvas._toolPreviewState = previewStateKey;
                var previewStatus = queryDissectionNode('#diss-canvas-status');
                if (previewStatus) {
                  previewStatus.setAttribute('data-tone', previewTone);
                  previewStatus.textContent = previewMessage;
                }
              }
            }
            syncCanvasPointerPresentation(canvas, hit);

          };



          // Progress calculation

          var exploredOrgans = d.exploredOrgans || {};

          var totalOrgansInSpecimen = 0;

          var exploredCount = 0;

          spec.layers.forEach(function (layer) {

            var layerOrgans = spec.organs[layer.id] || [];

            totalOrgansInSpecimen += layerOrgans.length;

            layerOrgans.forEach(function (org) {

              if (exploredOrgans[specimen + '|' + org.id]) exploredCount++;

            });

          });

          var progressPct = totalOrgansInSpecimen > 0 ? Math.round((exploredCount / totalOrgansInSpecimen) * 100) : 0;



          // Guided walkthrough data

          var guidedOrgans = organs.filter(function (org) { return structureExposureState(org, currentProcedure) === 'visible'; });
          var guidedTargetIds = guidedMode && Array.isArray(d.guidedTargetIds) && d.guidedTargetIds.length
            ? d.guidedTargetIds
            : guidedOrgans.map(function (org) { return org.id; });
          var guidedSteps = guidedTargetIds.map(function (organId) {
            return organs.find(function (org) { return org.id === organId; }) || null;
          }).filter(Boolean).map(function (org, i) {
            return { organId: org.id, name: org.name, prompt: 'Find {name} \u00B7 step {step} of {total}.'.replace('{step}', i + 1).replace('{total}', guidedTargetIds.length).replace('{name}', org.name) };
          });

          var currentGuided = guidedMode && guidedSteps.length ? guidedSteps[guidedStep % guidedSteps.length] : null;
          var guidedObservationOrgan = d.guidedObservationPending ? organs.find(function (organ) { return organ.id === d.guidedObservationPending; }) : null;
          var guidedObservationOptions = guidedObservationOrgan ? [guidedObservationOrgan].concat(organs.filter(function (organ) { return organ.id !== guidedObservationOrgan.id && structureExposureState(organ, currentProcedure) === 'visible'; }).sort(function (a, b) { return dissHash(specimen + '|' + guidedObservationOrgan.id + '|' + a.id) - dissHash(specimen + '|' + guidedObservationOrgan.id + '|' + b.id); })).slice(0, 3) : [];
          var currentGuidedOrgan = currentGuided ? organs.find(function (org) { return org.id === currentGuided.organId; }) : null;
          var currentGuidedVisible = !!currentGuidedOrgan && structureExposureState(currentGuidedOrgan, currentProcedure) === 'visible';



          var SPEC_KEYS = Object.keys(SPECIMENS);
          var currentLayerDef = spec.layers[currentLayerIdx] || spec.layers[0] || { id: activeLayer, name: activeLayer, desc: '' };
          var currentLayerDone = !!revealedLayers[activeLayer];
          var nextLayerDef = spec.layers[currentLayerIdx + 1] || null;
          var procedureStageIdx = procedureStepIndex(currentProcedure);
          var procedureNext = nextProcedureInfo();
          var activeInstrumentState = procedureInstrumentStatus(activeInstrument);
          if (simplifiedInstructions) {
            activeInstrumentState = Object.assign({}, activeInstrumentState, { instruction: procedureNext.action === 'complete' ? 'Done. Complete this layer.' : 'Next: ' + procedureNext.label + '.' });
          }
          var activeCalibrationControl = calibrationControlDefinition(activeInstrument);
          var activeCalibrationAssessment = instrumentCalibrationAssessment(activeInstrument, toolCalibration);
          var recordedCalibrationValues = Object.keys(currentProcedure.calibrationScores || {}).map(function (key) { return Number(currentProcedure.calibrationScores[key]) || 0; });
          var recordedCalibrationScore = recordedCalibrationValues.length ? Math.round(recordedCalibrationValues.reduce(function (sum, value) { return sum + value; }, 0) / recordedCalibrationValues.length) : 0;
          var techniqueScore = procedureTechniqueScore(currentProcedure);
          var currentTissueState = normalizeTissueState(currentProcedure.tissueState);
          var currentTissueStatus = tissueStatusData(currentTissueState);
          var currentTissuePhysics = tissuePhysicsData(currentTissueState);
          var currentObservationField = observationFieldData(currentProcedure);
          var currentInspectionTarget = d.quizMode ? null : ((d.lensPinnedOrganId ? organs.find(function (organ) { return organ.id === d.lensPinnedOrganId; }) : null) || sel || null);
          var currentIllumination = illuminationAssessmentData(currentProcedure, lightIntensity);
          var currentOpticalInspection = opticalInspectionData(currentInspectionTarget, lensFocusDepth, lensMagnification, lensPinned, currentProcedure);
          var tissueConsequences = currentTissueState.consequences || [];
          var latestTissueConsequence = tissueConsequences.length ? tissueConsequences[tissueConsequences.length - 1] : null;
          var latestTechniqueAction = (currentProcedure.actionLog || []).length ? currentProcedure.actionLog[currentProcedure.actionLog.length - 1] : null;
          var procedureTimelineEntries = (currentProcedure.actionLog || []).slice(-6);
          var selectedTechniqueAction = procedureTimelineEntries.find(function (entry) { return d.selectedProcedureActionAt != null && String(entry.at) === String(d.selectedProcedureActionAt); }) || latestTechniqueAction;
          var selectedTechniqueActionIsLatest = !!(selectedTechniqueAction && latestTechniqueAction && String(selectedTechniqueAction.at) === String(latestTechniqueAction.at));
          var stageTelemetryTone = currentTissueState.trauma >= 60 ? 'danger' : ((currentTissueState.risk >= 55 || currentTissueState.moisture < 38 || currentTissueState.tension > 82) ? 'caution' : 'stable');
          var stageTelemetryMetrics = [
            { id: 'exposure', label: 'Exposure', value: currentTissueState.exposure, note: 'Field aperture', risk: false },
            { id: 'clarity', label: 'Clarity', value: currentTissueState.clarity, note: currentObservationField.label, risk: currentTissueState.clarity < 45 },
            { id: 'stability', label: 'Stability', value: currentTissueState.stability, note: currentTissuePhysics.label, risk: currentTissueState.stability < 45 && currentTissueState.exposure > 35 },
            { id: 'risk', label: 'Risk', value: currentTissueState.risk, note: currentTissueStatus.label, risk: currentTissueState.risk >= 55 }
          ];
          var latestActionBefore = selectedTechniqueAction && selectedTechniqueAction.tissueBefore ? normalizeTissueState(selectedTechniqueAction.tissueBefore) : null;
          var latestActionAfter = selectedTechniqueAction && selectedTechniqueAction.tissueAfter ? normalizeTissueState(selectedTechniqueAction.tissueAfter) : null;
          var latestActionDeltaMetrics = latestActionBefore && latestActionAfter ? [
            { label: 'Clarity', before: latestActionBefore.clarity, after: latestActionAfter.clarity },
            { label: 'Exposure', before: latestActionBefore.exposure, after: latestActionAfter.exposure },
            { label: 'Stability', before: latestActionBefore.stability, after: latestActionAfter.stability },
            { label: 'Risk', before: latestActionBefore.risk, after: latestActionAfter.risk }
          ].map(function (metric) {
            var delta = metric.after - metric.before;
            var isRisk = metric.label === 'Risk';
            return Object.assign({}, metric, { delta: delta, direction: delta === 0 ? 'neutral' : ((isRisk ? delta < 0 : delta > 0) ? 'improve' : 'caution') });
          }) : [];
          var recentTechniqueMetrics = currentProcedure.extensionMetrics || currentProcedure.incisionMetrics || null;
          var scenarioDefinition = currentScenarioDefinition();
          var scenarioStatus = procedureScenarioStatus();
          var scenarioGuidance = procedureScenarioGuidance(scenarioStatus, currentProcedure);
          var procedureDebrief = procedureDebriefData();
          var attemptEntries = (((d.attemptArchive || {})[activeLayer]) || []);
          var techniqueComparison = techniqueComparisonData();
          var compareReplayProgress = Math.max(0, Math.min(1, d.compareReplayProgress == null ? 1 : Number(d.compareReplayProgress) || 0));
          var compareReplayPercent = Math.round(compareReplayProgress * 100);
          var compareReplayLabel = compareReplayPercent <= 0 ? 'Start of attempt' : (compareReplayPercent >= 100 ? 'Full paths' : 'Step ' + compareReplayPercent + '%');
          var adaptiveCoach = adaptiveCoachingData();
          var activeMaterialProfile = SPECIMEN_MATERIAL_PROFILES[spec.bodyShape] || SPECIMEN_MATERIAL_PROFILES.frog;
          var currentExposure = procedureExposureData(currentProcedure);
          var currentDecisionScore = procedureDecisionScore(currentProcedure);
          var currentMaterialInteraction = procedureMaterialInteractionData(activeInstrument, currentProcedure);
          var currentToolReadiness = procedureToolReadinessData(activeInstrument, currentProcedure);
          var nextToolReadiness = procedureNext.action === 'complete' ? null : procedureToolReadinessData(procedureNext.instrument, currentProcedure);
          var readinessForField = nextToolReadiness || currentToolReadiness;
          var fieldReadinessChecks = [
            { id: 'view', label: 'View', ready: anatomicalView === procedureProtocol.recommendedView, detail: anatomicalView === procedureProtocol.recommendedView ? 'Aligned to ' + procedureProtocol.recommendedView : 'Align to ' + procedureProtocol.recommendedView + ' view' },
            { id: 'optics', label: 'Optics', ready: currentOpticalInspection.quality >= 65 && currentIllumination.glareRisk < 58, detail: currentOpticalInspection.quality >= 65 && currentIllumination.glareRisk < 58 ? 'Focus ' + currentOpticalInspection.quality + '% · glare ' + currentIllumination.glareRisk + '%' : currentOpticalInspection.suggestion },
            { id: 'tissue', label: 'Tissue', ready: currentTissueState.trauma < 60 && currentTissueState.risk < 55 && currentTissueState.moisture >= 38, detail: currentTissueState.trauma < 60 && currentTissueState.risk < 55 && currentTissueState.moisture >= 38 ? currentTissueStatus.label + ' · preserve the corridor' : currentTissueStatus.priority },
            { id: 'tool', label: 'Tool', ready: !!readinessForField.safeToAct, detail: readinessForField.label + ' · ' + readinessForField.score + '%' }
          ];
          var fieldReadinessScore = Math.round(fieldReadinessChecks.reduce(function (sum, check) { return sum + (check.ready ? 25 : 0); }, 0));
          var fieldReadinessTone = readinessForField.tone === 'restricted' ? 'restricted' : (fieldReadinessScore === 100 ? 'ready' : 'caution');
          var fieldReadinessLabel = fieldReadinessTone === 'ready' ? 'Ready for controlled contact' : (fieldReadinessTone === 'restricted' ? 'Contact restricted' : 'Adjust before contact');
          var firstFieldReadinessIssue = fieldReadinessChecks.find(function (check) { return !check.ready; }) || null;
          var fieldReadinessCue = fieldReadinessScore === 100 ? 'All four pre-contact checks pass. Make one deliberate contact and monitor the response.' : (firstFieldReadinessIssue || {}).detail || 'Review the current tool and field state before contact.';
          var fieldReadinessActionLabel = !firstFieldReadinessIssue ? 'All checks pass' : (firstFieldReadinessIssue.id === 'view' ? 'Align view' : (firstFieldReadinessIssue.id === 'optics' ? 'Tune optics' : (firstFieldReadinessIssue.id === 'tool' ? 'Prepare tool' : 'Review tissue')));
          var activeInstrumentDefinition = PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === activeInstrument; }) || { label: activeInstrument };
          var stageProcedureStepTotal = Math.max(1, (procedureProtocol.order || []).length);
          var stageProcedureStepDisplay = procedureNext.action === 'complete' ? stageProcedureStepTotal : Math.min(stageProcedureStepTotal, procedureStageIdx + 1);
          var stageHandoffLabel = currentLayerDone ? (nextLayerDef ? 'Continue to ' + nextLayerDef.name : 'All layers revealed') : procedureNext.label;
          var stageHandoffGlyph = currentToolReadiness.tone === 'ready' || currentToolReadiness.tone === 'complete' ? '\u2713' : (currentToolReadiness.tone === 'restricted' ? '\u00D7' : (currentToolReadiness.tone === 'caution' ? '!' : '\u2192'));
          var protectedLandmarks = procedureProtectedLandmarks();
          var protectedLandmarkNames = protectedLandmarks.map(function (structure) { return structure.name; });
          var referenceEvidence = visualEvidence.find(function (entry) { return entry.id === d.referenceEvidenceId; }) || visualEvidence[visualEvidence.length - 1] || null;
          var selectedRelationships = anatomicalRelationships();
          var visibleOrgansInView = organs.filter(function (org) { return structureExposureState(org, currentProcedure) === 'visible'; });
          var revealedLayerCount = spec.layers.filter(function (layer) { return !!revealedLayers[layer.id]; }).length;
          var unlockedLayerIdx = Math.max(0, currentLayerIdx);
          spec.layers.forEach(function (layer, layerIdx) {
            if (layerIdx === 0 || revealedLayers[spec.layers[layerIdx - 1].id]) unlockedLayerIdx = Math.max(unlockedLayerIdx, layerIdx);
          });
          var remainingUnexplored = organs.find(function (org) { return !exploredOrgans[specimen + '|' + org.id]; }) || null;
          var nextUnexplored = visibleOrgansInView.find(function (org) { return !exploredOrgans[specimen + '|' + org.id]; }) || null;
          var visibilityTarget = currentGuidedOrgan && !currentGuidedVisible ? currentGuidedOrgan : remainingUnexplored;
          var visibilityTargetView = alternateVisibleViewForOrgan(visibilityTarget);
          var currentLearningGate = procedureLearningGateFor(procedureNext.action, currentProcedure);
          var layerTechniqueComplete = procedureNext.action === 'complete' && !currentLearningGate.required;
          var studyRoute = d.practicalMode ? 'study' : (d.guidedMode ? 'guided' : (d.quizMode ? 'quiz' : ((d.flashcardMode || d.compareMode || d.toolbarStudyOpen) ? 'study' : 'explore')));
          var currentLayerExploredCount = organs.filter(function (org) { return !!exploredOrgans[specimen + '|' + org.id]; }).length;
          var recordCandidate = organs.find(function (org) {
            var evidenceKey = specimen + '|' + org.id;
            return !!exploredOrgans[evidenceKey] && (!String((d.organNotes || {})[evidenceKey] || '').trim() || !(d.organConfidence || {})[evidenceKey]);
          }) || null;
          var currentLayerEvidenceCount = organs.filter(function (org) {
            var evidenceKey = specimen + '|' + org.id;
            return !!exploredOrgans[evidenceKey] && !!String((d.organNotes || {})[evidenceKey] || '').trim() && !!(d.organConfidence || {})[evidenceKey];
          }).length;
          var allSpecimenOrgans = [];
          spec.layers.forEach(function (layer) {
            allSpecimenOrgans = allSpecimenOrgans.concat(spec.organs[layer.id] || []);
          });
          var specimenEvidenceCount = allSpecimenOrgans.filter(function (org) {
            var evidenceKey = specimen + '|' + org.id;
            return !!exploredOrgans[evidenceKey] && !!String((d.organNotes || {})[evidenceKey] || '').trim() && !!(d.organConfidence || {})[evidenceKey];
          }).length;
          var validSpecimenOrganIds = Object.create(null);
          allSpecimenOrgans.forEach(function (organ) { validSpecimenOrganIds[organ.id] = true; });
          var specimenVerificationPrefix = specimen + '|';
          var verifiedIdentificationCount = Object.keys(d.verifiedIdentifications || {}).filter(function (key) {
            var verification = (d.verifiedIdentifications || {})[key];
            return key.indexOf(specimenVerificationPrefix) === 0 && Object.prototype.hasOwnProperty.call(validSpecimenOrganIds, key.slice(specimenVerificationPrefix.length)) && verification && verification.status === 'verified';
          }).length;
          var procedureLearningTotals = spec.layers.reduce(function (totals, layer) {
            var summary = procedureLearningSummary(((d.procedureByLayer || {})[layer.id]) || {});
            totals.verified += summary.verified;
            totals.total += summary.total;
            return totals;
          }, { verified: 0, total: 0 });
          var techniqueLearningPct = procedureLearningTotals.total ? Math.round(procedureLearningTotals.verified / procedureLearningTotals.total * 100) : 100;
          var evidenceProgressPct = totalOrgansInSpecimen ? Math.round(specimenEvidenceCount / totalOrgansInSpecimen * 100) : 100;
          var verifiedIdentificationGoal = Math.min(totalOrgansInSpecimen, Math.max(1, instructorRequiredStructures));
          var verifiedIdentificationPct = verifiedIdentificationGoal ? Math.min(100, Math.round(verifiedIdentificationCount / verifiedIdentificationGoal * 100)) : 100;
          var assessmentQuestionGoal = Math.min(3, Math.max(1, totalOrgansInSpecimen));
          var assessmentFirstAttemptScore = Number(d.quizFirstAttemptScore) || 0;
          var assessmentFirstAttemptTotal = Number(d.quizFirstAttemptTotal) || 0;
          var assessmentRecordedScore = d.assessmentCompletedAt ? (d.assessmentRecordedScore == null ? assessmentFirstAttemptScore : Number(d.assessmentRecordedScore) || 0) : 0;
          var assessmentRecordedTotal = d.assessmentCompletedAt ? (d.assessmentRecordedTotal == null ? assessmentFirstAttemptTotal : Number(d.assessmentRecordedTotal) || 0) : 0;
          var assessmentCoveragePct = Math.min(100, Math.round(assessmentRecordedTotal / assessmentQuestionGoal * 100));
          var assessmentAccuracyPct = assessmentRecordedTotal ? Math.round(assessmentRecordedScore / assessmentRecordedTotal * 100) : 0;
          var assessmentUnderstandingPct = Math.min(assessmentCoveragePct, Math.min(100, Math.round(assessmentAccuracyPct / 80 * 100)));
          var assessmentEvidence = d.assessmentEvidence || {};
          var objectiveEvidenceModels = (spec.objectives || []).map(function (objective, objectiveIndex) {
            var targetIds = ((spec.objectiveTargets || [])[objectiveIndex] || []).filter(function (id, idx, ids) { return ids.indexOf(id) === idx; });
            var targetMatchesById = targetIds.map(function (id) { return allSpecimenOrgans.filter(function (organ) { return organ.id === id; }); });
            var targetStructures = targetMatchesById.filter(function (matches) { return matches.length === 1; }).map(function (matches) { return matches[0]; });
            var unresolvedTargetIds = targetIds.filter(function (id, index) { return targetMatchesById[index].length === 0; });
            var ambiguousTargetIds = targetIds.filter(function (id, index) { return targetMatchesById[index].length > 1; });
            var mappingComplete = targetIds.length > 0 && unresolvedTargetIds.length === 0 && ambiguousTargetIds.length === 0;
            // Keep every configured ID in the denominator. A missing catalog
            // entry must never make an objective easier to demonstrate.
            var targetCount = Math.max(1, targetIds.length);
            var observedCount = targetStructures.filter(function (organ) {
              return !!exploredOrgans[specimen + '|' + organ.id];
            }).length;
            var recordedCount = targetStructures.filter(function (organ) {
              var key = specimen + '|' + organ.id;
              return !!String((d.organNotes || {})[key] || '').trim() && !!(d.organConfidence || {})[key];
            }).length;
            var understoodCount = targetStructures.filter(function (organ) {
              var key = specimen + '|' + organ.id;
              var verification = (d.verifiedIdentifications || {})[key];
              return !!(assessmentEvidence[key] && assessmentEvidence[key].correct) || !!(verification && verification.status === 'verified');
            }).length;
            var understandingGoal = targetCount;
            var observationPct = Math.min(100, Math.round(observedCount / targetCount * 100));
            var recordPct = Math.min(100, Math.round(recordedCount / targetCount * 100));
            var understandingPct = Math.min(100, Math.round(understoodCount / understandingGoal * 100));
            var objectivePct = Math.round((observationPct + recordPct + understandingPct) / 3);
            var objectiveComplete = mappingComplete && observedCount === targetCount && recordedCount === targetCount && understoodCount === targetCount;
            if (!objectiveComplete) objectivePct = Math.min(99, objectivePct);
            var status = objectiveComplete ? 'demonstrated' : (objectivePct > 0 ? 'developing' : 'not-started');
            var missingObserved = targetStructures.filter(function (organ) { return !exploredOrgans[specimen + '|' + organ.id]; });
            var missingRecorded = targetStructures.filter(function (organ) {
              var key = specimen + '|' + organ.id;
              return !String((d.organNotes || {})[key] || '').trim() || !(d.organConfidence || {})[key];
            });
            var missingUnderstood = targetStructures.filter(function (organ) {
              var key = specimen + '|' + organ.id;
              var verification = (d.verifiedIdentifications || {})[key];
              return !(assessmentEvidence[key] && assessmentEvidence[key].correct) && !(verification && verification.status === 'verified');
            });
            var nextEvidence = !mappingComplete
              ? 'Objective setup is incomplete, so this objective cannot yet be demonstrated. Your recorded evidence is preserved.'
              : (missingObserved.length
              ? 'Next: inspect ' + missingObserved.map(function (organ) { return organ.name; }).join(', ') + '.'
              : (missingRecorded.length
                ? 'Next: add an observation note and confidence rating for ' + missingRecorded.map(function (organ) { return organ.name; }).join(', ') + '.'
                : (understandingPct < 100
                  ? 'Next: verify ' + missingUnderstood.map(function (organ) { return organ.name; }).join(', ') + ' in Guided investigation, or answer the corresponding assessment ' + (missingUnderstood.length === 1 ? 'question' : 'questions') + ' correctly on the first attempt.'
                  : 'Evidence demonstrated: observation, recorded evidence, and verified understanding all align.')));
            return {
              objective: objective,
              status: status,
              progress: objectivePct,
              targetCount: targetCount,
              observedCount: observedCount,
              recordedCount: recordedCount,
              understoodCount: understoodCount,
              mappingComplete: mappingComplete,
              nextEvidence: nextEvidence
            };
          });
          var objectiveProgressPct = objectiveEvidenceModels.length
            ? Math.round(objectiveEvidenceModels.reduce(function (sum, objectiveModel) { return sum + objectiveModel.progress; }, 0) / objectiveEvidenceModels.length)
            : 0;
          var objectivesDemonstrated = objectiveEvidenceModels.length > 0 && objectiveEvidenceModels.every(function (objectiveModel) {
            return objectiveModel.mappingComplete && objectiveModel.status === 'demonstrated';
          });
          var understandingEvidenceComplete = verifiedIdentificationCount >= verifiedIdentificationGoal && !!d.assessmentCompletedAt && assessmentRecordedTotal >= assessmentQuestionGoal && assessmentAccuracyPct >= 80 && objectivesDemonstrated;
          var understandingProgressPct = Math.round((verifiedIdentificationPct + assessmentUnderstandingPct + objectiveProgressPct) / 3);
          if (!understandingEvidenceComplete) understandingProgressPct = Math.min(99, understandingProgressPct);
          var masteryComplete = progressPct >= 100 && evidenceProgressPct >= 100 && techniqueLearningPct >= 100 &&
            verifiedIdentificationCount >= verifiedIdentificationGoal && !!d.assessmentCompletedAt && assessmentRecordedTotal >= assessmentQuestionGoal &&
            assessmentAccuracyPct >= 80 && revealedLayerCount >= spec.layers.length && objectivesDemonstrated;
          var masteryDimensions = [
            { id: 'technique', label: 'Technique', value: techniqueLearningPct },
            { id: 'observation', label: 'Observation', value: progressPct },
            { id: 'evidence', label: 'Evidence', value: evidenceProgressPct },
            { id: 'understanding', label: 'Understanding', value: understandingProgressPct }
          ];
          var masteryOverallPct = Math.round(masteryDimensions.reduce(function (sum, dimension) { return sum + dimension.value; }, 0) / masteryDimensions.length);
          if (!masteryComplete) masteryOverallPct = Math.min(99, masteryOverallPct);
          var nextToolDefinition = PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === procedureNext.instrument; }) || { label: procedureNext.instrument || 'instrument' };
          var equivalentProcedureActionDisabled = !!(d._incisionAnim && d._incisionAnim.active) || procedureNext.action === 'complete' || currentLearningGate.required || activeInstrument !== procedureNext.instrument || !nextToolReadiness || !nextToolReadiness.safeToAct;
          var equivalentProcedureActionLabel;
          if (procedureNext.action === 'complete') {
            equivalentProcedureActionLabel = 'Technique protocol complete; use the Next best action card to inspect and record evidence';
          } else if (d._incisionAnim && d._incisionAnim.active) {
            equivalentProcedureActionLabel = 'Motor-neutral equivalent unavailable while the current technique animation finishes.';
          } else if (currentLearningGate.required) {
            equivalentProcedureActionLabel = 'Motor-neutral equivalent: ' + procedureNext.label + '. Complete the ' + (currentLearningGate.phase === 'reflect' ? 'reflection' : 'planning') + ' checkpoint first.';
          } else if (activeInstrument !== procedureNext.instrument) {
            equivalentProcedureActionLabel = 'Motor-neutral equivalent: ' + procedureNext.label + '. Select ' + nextToolDefinition.label + ' first.';
          } else if (!nextToolReadiness || !nextToolReadiness.safeToAct) {
            equivalentProcedureActionLabel = 'Motor-neutral equivalent: ' + procedureNext.label + '. ' + (nextToolReadiness ? nextToolReadiness.cue : 'Complete the readiness checks first.');
          } else {
            equivalentProcedureActionLabel = 'Motor-neutral equivalent: ' + procedureNext.label + '. Ready to record the equivalent technique action.';
          }
          var nextActionModel;
          if (d.practicalMode && quizQ) {
            nextActionModel = { step: 6, tone: 'assessment', phase: 'Timed practical', title: quizPrompt, description: 'Submit the current identification before time expires.', label: 'Go to practical question', action: 'quiz' };
          } else if (d.quizMode && d.quizComplete) {
            nextActionModel = { step: 6, tone: 'assessment', phase: d.quizReviewMode ? 'Supported review complete' : 'Assessment complete', title: d.quizReviewMode ? 'Review the supported-practice summary' : 'Review first-attempt evidence', description: 'The assessment has ended. Review the score, supported practice, and next recommendation.', label: 'Go to assessment summary', action: 'assessment-summary' };
          } else if (d.quizMode && quizQ) {
            nextActionModel = { step: 6, tone: 'assessment', phase: 'Practice assessment', title: quizPrompt, description: effectiveQuizAnswerMode === 'hotspot' ? 'Select a visible structure on the specimen, or use Arrow keys and Enter.' : 'Choose one answer in the assessment panel.', label: 'Go to assessment question', action: 'quiz' };
          } else if (d.flashcardMode) {
            nextActionModel = { step: 6, tone: 'assessment', phase: 'Study tools', title: 'Review the current structure flashcard', description: 'Reveal the function, then move at your own pace.', label: 'Go to flashcards', action: 'flashcards' };
          } else if (d.compareMode && sel) {
            nextActionModel = { step: 6, tone: 'assessment', phase: 'Compare', title: 'Compare this structure across specimens', description: 'Use the selected structure as the shared anatomical reference.', label: 'Go to comparison', action: 'comparison' };
          } else if (d.compareMode) {
            nextActionModel = { step: 4, tone: 'assessment', phase: 'Compare', title: 'Select a structure to compare', description: 'Inspect any visible structure first; the cross-specimen comparison will open automatically.', label: 'Focus the specimen', action: 'canvas' };
          } else if (d.toolbarStudyOpen) {
            nextActionModel = { step: 6, tone: 'assessment', phase: 'Study tools', title: 'Choose a study activity', description: 'Open flashcards, compare a selected structure, or start a timed specimen practical.', label: 'Choose a study activity', action: 'study-tools' };
          } else if (guidedMode && guidedObservationOrgan) {
            nextActionModel = { step: 4, tone: 'assessment', phase: 'Verify observation', title: 'Choose the observation that supports ' + guidedObservationOrgan.name, description: 'Locating the target marks it viewed; a matching specimen observation is required before the Guided step advances.', label: 'Complete evidence check', action: 'guided-observation' };
          } else if (guidedMode && currentGuided && currentGuidedVisible) {
            nextActionModel = { step: 4, tone: 'ready', phase: 'Guided investigation', title: currentGuided.prompt, description: 'Preview with Arrow keys or pointer movement, then press Enter or select the structure to inspect it.', label: 'Focus the specimen', action: 'canvas' };
          } else if (guidedMode && currentGuided) {
            nextActionModel = { step: 4, tone: 'caution', phase: 'Guided investigation', title: 'Make ' + currentGuided.name + ' visible to continue', description: visibilityTargetView ? 'Change to the ' + visibilityTargetView + ' view; the Guided target and step count will stay fixed.' : 'Improve the anatomical exposure before continuing the fixed Guided route.', label: visibilityTargetView ? 'Change to ' + visibilityTargetView + ' view' : 'Review field setup', action: 'visibility', targetView: visibilityTargetView };
          } else if (!currentLayerDone && currentLearningGate.required && currentLearningGate.definition) {
            var learningIsReflection = currentLearningGate.phase === 'reflect';
            nextActionModel = {
              step: learningIsReflection ? 3 : (currentLearningGate.action === 'inspect' ? 1 : 2),
              tone: learningIsReflection ? 'assessment' : 'caution',
              phase: learningIsReflection ? 'Explain the response' : 'Predict before contact',
              title: learningIsReflection ? currentLearningGate.definition.reflectionPrompt : currentLearningGate.definition.predictionPrompt,
              description: learningIsReflection ? 'Interpret the tissue response before another protocol action unlocks.' : 'Choose the safest plan. A correct plan unlocks the simulation action but never performs it for you.',
              label: learningIsReflection ? 'Explain what changed' : 'Answer planning checkpoint',
              action: 'learning'
            };
          } else if (!currentLayerDone && !layerTechniqueComplete) {
            if (procedureNext.action !== 'complete' && nextToolReadiness && !nextToolReadiness.safeToAct) {
              nextActionModel = { step: procedureNext.action === 'inspect' ? 1 : 2, tone: 'caution', phase: 'Set up safely', title: 'Prepare ' + nextToolDefinition.label + ' to ' + procedureNext.label.toLowerCase(), description: nextToolReadiness.cue, label: 'Set up ' + nextToolDefinition.label, action: 'prepare' };
            } else {
              nextActionModel = { step: procedureNext.action === 'inspect' ? 1 : 3, tone: 'ready', phase: procedureNext.action === 'inspect' ? 'Orient' : 'Demonstrate technique', title: procedureNext.label, description: 'Your plan is confirmed. Choose the required instrument, perform the action on the specimen, then explain the observed response.', label: activeInstrument === procedureNext.instrument ? 'Demonstrate on specimen' : 'Choose the instrument', action: 'procedure' };
            }
          } else if (nextUnexplored) {
            nextActionModel = { step: 4, tone: 'ready', phase: 'Identify', title: 'Find and inspect ' + nextUnexplored.name, description: currentLayerExploredCount + ' of ' + organs.length + ' structures inspected in this layer.', label: 'Focus the specimen', action: 'canvas' };
          } else if (remainingUnexplored) {
            nextActionModel = { step: 4, tone: 'caution', phase: 'Identify', title: 'Make ' + remainingUnexplored.name + ' visible', description: visibilityTargetView ? 'Change anatomical view before recording this structure as explored.' : 'Review exposure and field clarity before recording this structure.', label: visibilityTargetView ? 'Change to ' + visibilityTargetView + ' view' : 'Review field setup', action: 'visibility', targetView: visibilityTargetView };
          } else if (recordCandidate) {
            nextActionModel = { step: 5, tone: 'ready', phase: 'Record evidence', title: 'Finish the note for ' + recordCandidate.name, description: currentLayerEvidenceCount + ' of ' + currentLayerExploredCount + ' inspected structures have a note and confidence rating.', label: 'Open evidence note', action: 'evidence' };
          } else if (!currentLayerDone && layerTechniqueComplete) {
            nextActionModel = { step: 3, tone: 'ready', phase: 'Reveal', title: nextLayerDef ? 'Complete ' + currentLayerDef.name + ' and reveal ' + nextLayerDef.name : 'Complete the ' + currentLayerDef.name + ' layer', description: 'The accessible structures are inspected and documented. Complete this layer when you are ready to continue.', label: nextLayerDef ? 'Reveal ' + nextLayerDef.name : 'Complete layer', action: 'peel-layer' };
          } else if (nextLayerDef) {
            nextActionModel = { step: 3, tone: 'ready', phase: 'Continue', title: 'Continue to the ' + nextLayerDef.name + ' layer', description: 'This layer is revealed, inspected, and documented.', label: 'Continue to ' + nextLayerDef.name, action: 'next-layer' };
          } else {
            nextActionModel = { step: 6, tone: 'assessment', phase: 'Review', title: 'Specimen investigation complete', description: 'Review your evidence or check understanding with a short practice assessment.', label: 'Start practice assessment', action: 'start-assessment' };
          }
          var missionText = 'Work through the specimen one clear action at a time. Current focus: ' + nextActionModel.phase + '.';
          stageHandoffLabel = nextActionModel.title;
          var stageHandoffUsesTool = nextActionModel.action === 'prepare' || nextActionModel.action === 'procedure';
          var stageHandoffTone = nextActionModel.tone;
          var stageHandoffDetail = stageHandoffUsesTool && nextToolReadiness
            ? nextToolDefinition.label + ' \u00B7 ' + nextToolReadiness.label
            : nextActionModel.phase;
          stageHandoffGlyph = stageHandoffTone === 'ready' ? '\u2713' : (stageHandoffTone === 'caution' ? '!' : '\u2192');
          var pointerGuideData = canvasPointerModeData(null, null);
          var workflowSteps = [
            { label: __alloT('stem.dissection.orient', 'Orient') },
            { label: 'Prepare' },
            { label: __alloT('stem.dissection.reveal_a_layer', 'Reveal') },
            { label: __alloT('stem.dissection.identify_structures', 'Identify') },
            { label: __alloT('stem.dissection.record_evidence', 'Record evidence') },
            { label: __alloT('stem.dissection.compare', 'Review') }
          ];

          function focusDissectionTarget(targetId, message, originatingRoot) {
            var focusRoot = originatingRoot || currentDissectionRootNode();
            var remainingAttempts = 2;
            function focusWhenRendered() {
              if (!focusRoot || !focusRoot.isConnected) focusRoot = currentDissectionRootNode();
              var target = scopedDissectionQuery(focusRoot, '#' + targetId);
              if (!target && remainingAttempts > 0) {
                remainingAttempts -= 1;
                setTimeout(focusWhenRendered, 0);
                return false;
              }
              if (!target) return false;
              if (target.scrollIntoView) target.scrollIntoView({ block: 'center', behavior: reducedMotionEnabled ? 'auto' : 'smooth' });
              if (target.focus) target.focus();
              return true;
            }
            if (focusWhenRendered()) setTimeout(focusWhenRendered, 0);
            if (message) setProcedureFeedback(message, 'working');
          }
          function focusStructureDirectoryItem(organId) {
            var focusRoot = currentDissectionRootNode();
            var remainingAttempts = 2;
            function focusWhenRendered() {
              var target = scopedDissectionQuery(focusRoot, '#diss-organ-' + organId) || scopedDissectionQuery(focusRoot, '#diss-organ-search');
              if (!target && remainingAttempts > 0) {
                remainingAttempts -= 1;
                setTimeout(focusWhenRendered, 0);
                return;
              }
              if (!target) return;
              if (target.scrollIntoView) target.scrollIntoView({ block: 'nearest', behavior: reducedMotionEnabled ? 'auto' : 'smooth' });
              if (target.focus) target.focus();
            }
            setTimeout(focusWhenRendered, 0);
          }
          function performPrimaryNextAction() {
            if (nextActionModel.action === 'guided-observation') {
              focusDissectionTarget('diss-guided-observation-check', 'Guided evidence check focused. Choose the observation that supports the identification.');
            } else if (nextActionModel.action === 'learning') {
              focusDissectionTarget('diss-learning-checkpoint', 'Learning checkpoint focused. Choose one response; the simulation will not perform the action for you.');
            } else if (nextActionModel.action === 'prepare') {
              upd('techniquePanelOpen', true);
              setProcedureFeedback('Choose and prepare the required instrument yourself. The planner will focus the tray without changing the tool, view, calibration, or specimen.', 'working');
              focusDissectionTarget('diss-instrument-' + procedureNext.instrument);
            } else if (nextActionModel.action === 'procedure') {
              upd('techniquePanelOpen', true);
              if (activeInstrument !== procedureNext.instrument) {
                setProcedureFeedback('Choose the instrument that matches your confirmed plan. No technique action has been recorded.', 'working');
                focusDissectionTarget('diss-instrument-' + procedureNext.instrument);
              } else {
                focusDissectionTarget('diss-canvas', 'Plan confirmed. Demonstrate ' + procedureNext.label.toLowerCase() + ' on the specimen. The action occurs only through your canvas gesture or the motor-neutral equivalent control.');
              }
            } else if (nextActionModel.action === 'peel-layer') {
              performNextProcedureStep();
            } else if (nextActionModel.action === 'study-tools') {
              focusDissectionTarget('diss-study-tools', 'Study tools focused. Choose flashcards, comparison, or a timed practical.');
            } else if (nextActionModel.action === 'visibility') {
              if (nextActionModel.targetView) {
                updMany({ toolbarViewOpen: true, toolbarToolsOpen: false, toolbarStudyOpen: false });
                focusDissectionTarget('diss-view-cycle', 'Choose the anatomical view that makes the target visible. The view was not changed automatically.');
              } else {
                upd('techniquePanelOpen', true);
                focusDissectionTarget('diss-procedure-summary', 'Review the field setup and decide what to adjust before continuing.');
              }
            } else if (nextActionModel.action === 'canvas') {
              focusDissectionTarget('diss-canvas', nextActionModel.title + '. Use the pointer, or Arrow keys followed by Enter.');
            } else if (nextActionModel.action === 'quiz') {
              focusDissectionTarget('diss-quiz-panel');
            } else if (nextActionModel.action === 'assessment-summary') {
              focusDissectionTarget('diss-assessment-summary');
            } else if (nextActionModel.action === 'flashcards') {
              focusDissectionTarget('diss-flashcard-panel');
            } else if (nextActionModel.action === 'comparison') {
              focusDissectionTarget('diss-comparison-panel');
            } else if (nextActionModel.action === 'evidence' && recordCandidate) {
              chooseOrganFromDirectory(recordCandidate);
              focusDissectionTarget('diss-note-' + recordCandidate.id, 'Record an observation and confidence rating for ' + recordCandidate.name + '.');
            } else if (nextActionModel.action === 'next-layer' && nextLayerDef) {
              selectLayer(nextLayerDef.id);
            } else if (nextActionModel.action === 'start-assessment') {
              setStudyRoute('quiz');
              setTimeout(function () { focusDissectionTarget('diss-quiz-panel'); }, 60);
            }
          }

          function renderNextActionCard() {
            return React.createElement("section", {
              className: "diss-next-action",
              "data-tone": nextActionModel.tone,
              "data-dissection-next-action": true,
              "data-next-action": nextActionModel.action,
              role: "region",
              "aria-labelledby": "diss-next-action-title"
            },
              React.createElement("div", { className: "diss-next-action__step", "aria-label": "Workflow step " + nextActionModel.step + " of " + workflowSteps.length }, 'Step ' + nextActionModel.step + '\n' + 'of ' + workflowSteps.length),
              React.createElement("div", { className: "diss-next-action__body" },
                React.createElement("span", { className: "diss-next-action__eyebrow" }, 'Next best action \u00B7 ' + nextActionModel.phase),
                React.createElement("h3", { id: "diss-next-action-title", tabIndex: -1 }, nextActionModel.title),
                React.createElement("p", null, nextActionModel.description),
                React.createElement("div", { className: "diss-next-action__meta", "aria-label": "Current lab context" },
                  React.createElement("span", null, spec.icon + ' ' + spec.name),
                  React.createElement("span", null, currentLayerDef.icon + ' ' + currentLayerDef.name + ' \u00B7 layer ' + (currentLayerIdx + 1) + '/' + spec.layers.length),
                  nextToolReadiness && stageHandoffUsesTool && React.createElement("span", null, 'Tool: ' + nextToolDefinition.label + ' \u00B7 ' + nextToolReadiness.label)
                )
              ),
              React.createElement("div", { className: "diss-next-action__actions" },
                React.createElement("button", { type: "button", className: "diss-next-action__primary", disabled: !!(d._incisionAnim && d._incisionAnim.active), onClick: performPrimaryNextAction }, d._incisionAnim && d._incisionAnim.active ? 'Technique in progress...' : nextActionModel.label),
                nextActionModel.action !== 'canvas' && React.createElement("button", { type: "button", className: "diss-next-action__secondary", onClick: function () { focusDissectionTarget('diss-canvas', 'Interactive specimen focused.'); } }, 'View specimen')
              )
            );
          }

          function renderProcedureLearningCheckpoint() {
            if (studyRoute !== 'explore' || d.quizMode || guidedMode || currentLayerDone || !currentLearningGate.definition) return null;
            var learningAction = currentLearningGate.action;
            var definition = currentLearningGate.definition;
            var entry = procedureLearningEntry(learningAction, currentProcedure);
            var phase = currentLearningGate.phase === 'reflect' ? 'reflect' : (currentLearningGate.required ? 'predict' : 'perform');
            var responseKey = phase === 'reflect' ? 'reflection' : 'prediction';
            var selectedChoice = entry[responseKey + 'Choice'];
            var responseCorrect = !!entry[responseKey + 'Correct'];
            var options = phase === 'reflect' ? definition.reflectionOptions : (phase === 'predict' ? definition.predictionOptions : []);
            var feedbackValue = entry[responseKey + 'Feedback'];
            var feedback = typeof feedbackValue === 'string' ? feedbackValue : '';
            var actionToolId = procedureInstrumentForAction(learningAction);
            var actionTool = PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === actionToolId; }) || { label: actionToolId };
            var actionComplete = procedureActionComplete(currentProcedure, learningAction);
            var phaseStates = [
              { id: 'predict', label: '1 Predict', state: entry.predictionCorrect ? 'complete' : (phase === 'predict' ? 'current' : 'upcoming') },
              { id: 'perform', label: '2 Perform', state: actionComplete ? 'complete' : (phase === 'perform' ? 'current' : 'upcoming') },
              { id: 'reflect', label: '3 Explain', state: entry.reflectionCorrect ? 'complete' : (phase === 'reflect' ? 'current' : 'upcoming') }
            ];
            return React.createElement("section", {
              id: "diss-learning-checkpoint",
              className: "diss-learning-check",
              "data-phase": phase,
              "data-learning-action": learningAction,
              tabIndex: -1,
              role: "region",
              "aria-labelledby": "diss-learning-check-title"
            },
              React.createElement("div", { className: "diss-learning-check__head" },
                React.createElement("p", { className: "diss-learning-check__eyebrow" }, phase === 'reflect' ? 'Reason from evidence' : (phase === 'perform' ? 'Plan confirmed' : 'Pause before contact')),
                React.createElement("h3", { id: "diss-learning-check-title" }, phase === 'reflect' ? 'Explain what changed' : (phase === 'perform' ? 'Demonstrate your plan' : definition.title)),
                React.createElement("p", { className: "diss-learning-check__prompt" },
                  phase === 'reflect' ? definition.reflectionPrompt : (phase === 'perform' ? 'Use ' + actionTool.label + ' to ' + procedureNext.label.toLowerCase() + '. The simulation will score the action you perform; it will not execute the step from the Next card.' : definition.predictionPrompt)
                )
              ),
              React.createElement("div", { className: "diss-learning-check__phases", "aria-label": "Learning cycle progress" },
                phaseStates.map(function (phaseState) {
                  return React.createElement("span", { key: phaseState.id, className: "diss-learning-check__phase", "data-state": phaseState.state, "aria-current": phaseState.state === 'current' ? 'step' : undefined }, (phaseState.state === 'complete' ? '\u2713 ' : '') + phaseState.label);
                })
              ),
              options.length ? React.createElement("div", { className: "diss-learning-check__options", role: "group", "aria-label": phase === 'reflect' ? "Choose the best evidence-based explanation" : "Choose the safest plan" },
                options.map(function (option) {
                  var isSelected = selectedChoice === option.id;
                  return React.createElement("button", {
                    type: "button",
                    key: option.id,
                    className: "diss-learning-check__option",
                    "aria-pressed": isSelected,
                    "data-result": isSelected ? (responseCorrect ? "correct" : "incorrect") : undefined,
                    onClick: function () { recordProcedureLearningChoice(learningAction, responseKey, option.id); }
                  }, option.label);
                })
              ) : React.createElement("div", { className: "diss-learning-check__perform" }, 'Make the contact yourself, then return here to explain the observed tissue response. A motor-neutral equivalent is available in Technique controls after all readiness checks pass.'),
              feedback && React.createElement("p", { className: "diss-learning-check__feedback", "data-result": responseCorrect ? "correct" : "incorrect" }, feedback)
            );
          }
          function renderGuidedObservationCheck() {
            if (!guidedMode || !guidedObservationOrgan || !currentGuided) return null;
            var guidedFeedback = d.guidedObservationFeedback;
            return React.createElement("section", {
              id: "diss-guided-observation-check",
              className: "diss-learning-check",
              "data-phase": "reflect",
              "data-guided-observation": true,
              tabIndex: -1,
              role: "region",
              "aria-labelledby": "diss-guided-observation-title"
            },
              React.createElement("div", { className: "diss-learning-check__head" },
                React.createElement("p", { className: "diss-learning-check__eyebrow" }, 'Guided evidence check'),
                React.createElement("h3", { id: "diss-guided-observation-title" }, 'What observation supports this identification?'),
                React.createElement("p", { className: "diss-learning-check__prompt" }, 'You located ' + guidedObservationOrgan.name + '. Choose the description that matches what this structure looks like or does. Locating alone does not advance the step.')
              ),
              React.createElement("div", { className: "diss-learning-check__phases", "aria-label": "Guided evidence progress" },
                React.createElement("span", { className: "diss-learning-check__phase", "data-state": "complete" }, '\u2713 Locate'),
                React.createElement("span", { className: "diss-learning-check__phase", "data-state": "current", "aria-current": "step" }, 'Observe'),
                React.createElement("span", { className: "diss-learning-check__phase", "data-state": "upcoming" }, 'Verify')
              ),
              React.createElement("div", { className: "diss-learning-check__options", role: "group", "aria-label": "Choose the matching specimen observation" },
                guidedObservationOptions.map(function (option) {
                  var isChosen = guidedFeedback && guidedFeedback.choiceId === option.id;
                  return React.createElement("button", {
                    type: "button",
                    key: option.id,
                    className: "diss-learning-check__option",
                    "aria-pressed": !!isChosen,
                    "data-result": isChosen ? (guidedFeedback.correct ? "correct" : "incorrect") : undefined,
                    onClick: function () { recordGuidedObservationChoice(option.id); }
                  }, String(option.fn || 'No observation available.').split('.')[0] + '.');
                })
              ),
              guidedFeedback && React.createElement("p", { className: "diss-learning-check__feedback", "data-result": guidedFeedback.correct ? "correct" : "incorrect" }, guidedFeedback.message)
            );
          }
          function evidenceLayerLabel(evidence) {
            var explicitLabel = evidence && typeof evidence.layerName === 'string' ? evidence.layerName.trim() : '';
            if (explicitLabel) return explicitLabel;
            var layerId = evidence && evidence.layer;
            var matchedLayer = spec.layers.find(function (layer) { return layer && layer.id === layerId; });
            return matchedLayer ? matchedLayer.name : 'Saved specimen view';
          }
          function evidenceViewLabel(evidence) {
            var explicitView = evidence && typeof evidence.view === 'string' ? evidence.view.trim() : '';
            return explicitView || 'recorded';
          }
          function evidenceConditionLabel(evidence) {
            var explicitCondition = evidence && typeof evidence.condition === 'string' ? evidence.condition.trim() : '';
            return explicitCondition || 'saved condition';
          }
          function evidenceFileToken(value, fallback) {
            var token = value == null ? '' : String(value).trim().replace(/[^a-zA-Z0-9_-]+/g, '_').replace(/^_+|_+$/g, '');
            return token || fallback;
          }
          function evidenceDomToken(evidenceId) {
            return evidenceFileToken(evidenceId, 'frame');
          }
          function focusEvidenceControl(controlId) {
            setTimeout(function () {
              var notebook = dissectionNodeById('diss-evidence-notebook');
              if (notebook) notebook.open = true;
              var target = controlId ? dissectionNodeById(controlId) : null;
              if (!target && notebook) target = notebook.querySelector('summary');
              if (target && typeof target.focus === 'function') target.focus();
            }, 0);
          }
          function captureVisualEvidence() {
            if (visualEvidence.length >= evidenceFrameLimit) {
              setProcedureFeedback('Evidence notebook is full. Download or remove a frame before capturing another; no saved frame was replaced.', 'caution');
              if (addToast) addToast('Evidence notebook full — manage frames before capturing.', 'error');
              if (typeof announceToSR === 'function') announceToSR('Evidence notebook full. No frame was replaced. Download or remove a frame before capturing another.');
              focusEvidenceControl();
              return false;
            }
            try {
              var sourceCanvas = queryDissectionNode('[data-diss-canvas]');
              if (!sourceCanvas) throw new Error('Canvas unavailable');
              var thumbnail = document.createElement('canvas');
              thumbnail.setAttribute('aria-hidden', 'true');
              thumbnail.width = 220; thumbnail.height = 264;
              var thumbnailContext = thumbnail.getContext('2d');
              if (!thumbnailContext) throw new Error('Thumbnail canvas unavailable');
              thumbnailContext.drawImage(sourceCanvas, 0, 0, thumbnail.width, thumbnail.height);
              var evidenceId = Date.now();
              while (visualEvidence.some(function (entry) { return String(entry.id) === String(evidenceId); })) evidenceId += 1;
              var selectedEvidenceOrgan = d.selectedOrgan ? organs.find(function (organ) { return organ.id === d.selectedOrgan; }) : null;
              var evidenceEntry = {
                id: evidenceId,
                image: thumbnail.toDataURL('image/jpeg', 0.72),
                capturedAt: new Date(evidenceId).toISOString(),
                layer: activeLayer,
                layerName: currentLayerDef.name,
                view: anatomicalView,
                condition: specimenCondition,
                depthAtlas: depthAtlasEnabled,
                tissueRelief: tissueReliefEnabled,
                selectedOrganId: selectedEvidenceOrgan ? selectedEvidenceOrgan.id : null,
                selectedOrganName: selectedEvidenceOrgan ? selectedEvidenceOrgan.name : '',
                techniqueScore: techniqueScore,
                calibrationScore: recordedCalibrationScore,
                toolCalibration: Object.assign({}, toolCalibration),
                observationField: Object.assign({}, currentObservationField),
                observationQuality: currentObservationField.quality,
                anatomicalExposure: Object.assign({}, currentExposure),
                decisionQuality: currentDecisionScore,
                materialInteraction: Object.assign({}, currentMaterialInteraction),
                toolReadiness: { score: currentToolReadiness.score, tone: currentToolReadiness.tone, label: currentToolReadiness.label, cue: currentToolReadiness.cue, checks: currentToolReadiness.checks.map(function (check) { return { id: check.id, label: check.label, ready: check.ready }; }) },
                scenario: { id: scenarioDefinition.id, label: scenarioDefinition.label, difficulty: scenarioDefinition.difficulty, status: scenarioStatus.complete ? 'complete' : (scenarioStatus.expired ? 'expired' : 'active'), progress: scenarioStatus.progress, detail: scenarioStatus.detail, elapsedSeconds: d.scenarioStartedAt ? Math.max(0, Math.round(((Number(d.scenarioCompletedAt) || Date.now()) - Number(d.scenarioStartedAt)) / 1000)) : 0 },
                opticalInspection: Object.assign({}, currentOpticalInspection),
                illumination: Object.assign({}, currentIllumination),
                lightIntensity: lightIntensity,
                opticalQuality: currentOpticalInspection.quality,
                livingFunction: livingFunctionEnabled ? {
                  title: specimenLivingFunctionProfile().title,
                  speed: livingFunctionSpeed,
                  disclaimer: 'In-life physiology model; not preserved specimen motion.'
                } : null,
                tissuePreservation: currentTissueStatus.preservation,
                tissueState: normalizeTissueState(currentTissueState),
                consequenceSummary: latestTissueConsequence ? latestTissueConsequence.message : '',
                procedureProtocol: {
                  title: procedureProtocol.title,
                  recommendedView: procedureProtocol.recommendedView,
                  route: procedureProtocol.route,
                  protectedLandmarks: protectedLandmarkNames.slice()
                },
                specimen: specimen
              };
              var nextEvidence = visualEvidence.concat([evidenceEntry]);
              updMany({ visualEvidence: nextEvidence, referenceEvidenceId: evidenceId, splitComparison: true });
              setProcedureFeedback('Evidence frame captured. It is now the active reference in split comparison.');
              if (addToast) addToast('Evidence frame captured and opened for comparison.', 'success');
              if (typeof announceToSR === 'function') announceToSR('Captured evidence frame for ' + currentLayerDef.name + '. Split comparison is on.');
            } catch (error) {
              setProcedureFeedback('Evidence capture is unavailable in this view.', 'caution');
              if (addToast) addToast('Evidence capture is unavailable in this view.', 'error');
            }
          }

          function selectEvidenceReference(evidenceId) {
            var evidence = visualEvidence.find(function (entry) { return entry.id === evidenceId; });
            if (!evidence) return;
            updMany({ referenceEvidenceId: evidenceId, splitComparison: true });
            var layerLabel = evidenceLayerLabel(evidence);
            setProcedureFeedback('Reference frame selected from ' + layerLabel + '. Split comparison is on.');
            if (typeof announceToSR === 'function') announceToSR('Reference frame selected from ' + layerLabel + '.');
          }

          function downloadEvidence(evidence) {
            if (!evidence || !evidence.image) return;
            try {
              var link = document.createElement('a');
              link.download = evidenceFileToken(specimen, 'specimen') + '_' + evidenceFileToken(evidence.layer, 'evidence') + '_' + evidenceFileToken(evidence.id, 'frame') + '.jpg';
              link.href = evidence.image; link.click();
              setProcedureFeedback('Evidence frame downloaded as ' + link.download + '.');
              if (addToast) addToast('Evidence frame downloaded.', 'success');
            } catch (error) {
              setProcedureFeedback('Evidence download failed in this view.', 'caution');
              if (addToast) addToast('Evidence download failed.', 'error');
            }
          }

          function requestEvidenceRemoval(evidenceId) {
            var evidence = visualEvidence.find(function (entry) { return entry.id === evidenceId; });
            if (!evidence) return;
            updMany({ evidenceRemovePendingId: evidenceId, evidenceClearPending: false });
            setProcedureFeedback('Removal confirmation opened for ' + evidenceLayerLabel(evidence) + '. Download the frame first if you need a separate copy.', 'caution');
            focusEvidenceControl('diss-evidence-confirm-remove-' + evidenceDomToken(evidenceId));
          }
          function cancelEvidenceRemoval(evidenceId) {
            upd('evidenceRemovePendingId', null);
            setProcedureFeedback('Evidence-frame removal canceled.', 'working');
            focusEvidenceControl('diss-evidence-remove-' + evidenceDomToken(evidenceId));
          }
          function confirmEvidenceRemoval(evidenceId) {
            var evidence = visualEvidence.find(function (entry) { return entry.id === evidenceId; });
            if (!evidence) { upd('evidenceRemovePendingId', null); return; }
            var remainingEvidence = visualEvidence.filter(function (entry) { return entry.id !== evidenceId; });
            var preferredReferenceId = referenceEvidence ? referenceEvidence.id : null;
            var nextReference = remainingEvidence.find(function (entry) { return entry.id === preferredReferenceId; }) || remainingEvidence[remainingEvidence.length - 1] || null;
            cancelDissectionSave('dissection_progress_' + specimen, dissectionSaveOwner);
            updMany({
              visualEvidence: remainingEvidence,
              referenceEvidenceId: nextReference ? nextReference.id : null,
              splitComparison: !!(d.splitComparison && nextReference),
              evidenceRemovePendingId: null,
              evidenceClearPending: false
            });
            var remainingCount = remainingEvidence.length;
            setProcedureFeedback('Removed the ' + evidenceLayerLabel(evidence) + ' evidence frame. ' + remainingCount + ' frame' + (remainingCount === 1 ? ' remains.' : 's remain.') + ' Saving the smaller notebook now.', 'success');
            if (typeof announceToSR === 'function') announceToSR('Evidence frame removed. ' + remainingCount + ' frame' + (remainingCount === 1 ? ' remains.' : 's remain.'));
            focusEvidenceControl();
          }
          function requestClearVisualEvidence() {
            if (!visualEvidence.length) return;
            updMany({ evidenceClearPending: true, evidenceRemovePendingId: null });
            setProcedureFeedback('Clear-all confirmation opened. Download any evidence frames you need before continuing.', 'caution');
            focusEvidenceControl('diss-evidence-confirm-clear');
          }
          function cancelClearVisualEvidence() {
            upd('evidenceClearPending', false);
            setProcedureFeedback('Clear-all evidence action canceled.', 'working');
            focusEvidenceControl('diss-evidence-clear-all');
          }
          function confirmClearVisualEvidence() {
            var removedCount = visualEvidence.length;
            if (!removedCount) { upd('evidenceClearPending', false); return; }
            cancelDissectionSave('dissection_progress_' + specimen, dissectionSaveOwner);
            updMany({ visualEvidence: [], referenceEvidenceId: null, splitComparison: false, evidenceRemovePendingId: null, evidenceClearPending: false });
            setProcedureFeedback('Cleared ' + removedCount + ' evidence frame' + (removedCount === 1 ? '' : 's') + '. Notes and other specimen progress were preserved; saving the smaller record now.', 'success');
            if (typeof announceToSR === 'function') announceToSR('All ' + removedCount + ' evidence frames cleared. Notes and other progress were preserved.');
            focusEvidenceControl();
          }

          function selectSpecimen(sk) {
            var sp = SPECIMENS[sk];
            if (!sp || sk === specimen) return;
            if (d.quizMode) {
              setProcedureFeedback('Specimen selection is locked during an assessment. Return to the lab before changing specimens.', 'caution');
              return;
            }
            // Persist the departing specimen synchronously so the incoming
            // specimen's debounced save cannot supersede its latest evidence.
            var departureSaveKey = 'dissection_progress_' + specimen;
            var departureSaveAccess = currentDissectionSaveAccess(departureSaveKey, dissectionSaveOwner);
            if (departureSaveAccess.state !== 'writable') {
              setProcedureFeedback('The current ' + spec.name + ' save is protected or unavailable, so the specimen was not changed. Reload the protected record, restore browser storage, or confirm a reset before switching specimens.', 'caution');
              focusResetControl('diss-protected-save-retry');
              return;
            }
            var departureHadPendingSave = !!_disSaveQueue[dissectionSaveSlot(departureSaveKey, dissectionSaveOwner)];
            var departureSaved = flushDissectionSave(departureSaveKey, true, dissectionSaveOwner);
            if (departureHadPendingSave && !departureSaved) {
              setProcedureFeedback('Could not save the latest ' + spec.name + ' work, so the specimen was not changed. Restore browser storage or copy your report, then try again.', 'caution');
              return;
            }
            clearAllDissectionCanvasTimers(canvasRefNode);
            cancelActiveCanvasGesture('Specimen gesture canceled before changing specimens; no action was recorded.');
            closeTimedPractical();
            updMany({ guidedMode: false, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, evidenceRemovePendingId: null, evidenceClearPending: false });
            var viewed = Object.assign({}, d.specimensViewed || {});
            viewed[sk] = true;
            updMany({ specimen: sk, activeLayer: (sp.layers && sp.layers[0] ? sp.layers[0].id : 'skin'), selectedOrgan: null, lensPinned: false, lensPinnedPoint: null, lensPinnedOrganId: null, guidedStep: 0, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, organSearch: '', exploredOrgans: {}, verifiedIdentifications: {}, revealedLayers: {}, quizScore: 0, quizTotal: 0, quizFirstAttemptScore: 0, quizFirstAttemptTotal: 0, quizSupportedCount: 0, quizComplete: false, quizReviewMode: false, assessmentCompletedAt: 0, assessmentRecordedScore: 0, assessmentRecordedTotal: 0, assessmentEvidence: {}, quizFeedback: null, quizRetry: null, quizReviewQueue: [], completedObjectives: {}, organNotes: {}, organConfidence: {}, annotations: [], dissInquiry: defaultDissectionInquiry(), timeSpent: 0, guidedComplete: false, procedureByLayer: {}, attemptArchive: {}, compareTechniqueAttempts: false, compareReplayProgress: 0, compareReplayPlaying: false, visualEvidence: [], referenceEvidenceId: null, splitComparison: false, procedureFeedback: null, scenarioStartedAt: 0, scenarioTimeRemaining: 0, scenarioCompletedAt: 0, resetConfirmPending: false, activeInstrument: 'probe', livingFunctionEnabled: false, livingFunctionPaused: false, livingFunctionSpeed: 'normal', livingFunctionReplayToken: 0, incisionDepth: 'shallow', toolCalibration: normalizeToolCalibration(), specimensViewed: viewed, _dissLoadedSpec: null, _incisionAnim: null, _layerTransition: null, _viewTransition: null, _layerBrowseTransition: null, _procedureDemo: null, _procedureReplay: null });
            if (typeof announceToSR === 'function') announceToSR('Selected ' + sp.name + '. Loading saved progress for the ' + ((sp.layers[0] || {}).name || 'first') + ' layer.');
            if (typeof canvasNarrate === 'function') canvasNarrate('dissection', 'specimenSelect', 'Selected ' + sp.name + '. ' + sp.desc, { debounce: 500 });
          }
          function focusResetControl(controlId) {
            var focusRoot = currentDissectionRootNode();
            var remainingAttempts = 2;
            function focusWhenRendered() {
              if ((!focusRoot || !focusRoot.isConnected) && typeof document !== 'undefined') focusRoot = currentDissectionRootNode();
              var target = scopedDissectionQuery(focusRoot, '#' + controlId);
              if (target) {
                target.focus();
                return;
              }
              if (remainingAttempts > 0) {
                remainingAttempts -= 1;
                setTimeout(focusWhenRendered, 0);
                return;
              }
              if (focusRoot && focusRoot.querySelector) target = focusRoot.querySelector('[data-dissection-specimens] [role="tab"][aria-selected="true"]');
              if (target) target.focus();
            }
            setTimeout(focusWhenRendered, 0);
          }
          function requestSpecimenReset() {
            upd('resetConfirmPending', true);
            var message = activeDissectionSaveIssue
              ? 'Reset confirmation opened. This will permanently delete the protected saved record for ' + spec.name + ' and replace it with a fresh lab.'
              : 'Reset confirmation opened. This will remove viewed structures, verified identifications, notes, inquiry records, evidence frames, quiz progress, and technique attempts for ' + spec.name + '.';
            setProcedureFeedback(message, 'caution');
            focusResetControl('diss-reset-confirm');
          }
          function retryProtectedDissectionSave() {
            cancelDissectionSave(activeDissectionSaveKey, dissectionSaveOwner);
            updMany({
              _dissLoadedSpec: null,
              _dissSaveRetrying: true,
              resetConfirmPending: false,
              procedureFeedback: { message: 'Checking protected saved progress. A successful check reloads the saved version before saving resumes.', tone: 'working', at: Date.now() }
            });
            if (typeof announceToSR === 'function') announceToSR('Checking protected saved progress. Temporary-session changes will remain unless the saved version can be read safely.');
          }
          function cancelSpecimenReset() {
            upd('resetConfirmPending', false);
            setProcedureFeedback('Specimen reset canceled. Your progress is unchanged.', 'working');
            focusResetControl(activeDissectionSaveIssue ? 'diss-protected-save-reset' : 'diss-reset-specimen');
          }
          function confirmSpecimenReset() {
            var resetSaveKey = 'dissection_progress_' + specimen;
            // A pending pre-reset snapshot must never recreate progress after
            // the learner confirms deletion. The reset render may then queue a
            // clean default snapshot for this specimen.
            clearAllDissectionCanvasTimers(canvasRefNode);
            cancelDissectionSave(resetSaveKey, dissectionSaveOwner);
            try {
              localStorage.removeItem(resetSaveKey);
            } catch (e) {
              blockDissectionSave(resetSaveKey, { kind: 'storage-unavailable', error: e }, spec.name, dissectionSaveOwner);
              publishDissectionSaveStatus('error', 'The protected ' + spec.name + ' save could not be removed and remains unchanged. Check browser storage access, then try again.', resetSaveKey, dissectionSaveOwner);
              setProcedureFeedback('Reset could not continue because the protected save could not be removed.', 'caution');
              focusResetControl('diss-reset-confirm');
              return;
            }
            allowDissectionSave(resetSaveKey, null, spec.name, dissectionSaveOwner);
            closeTimedPractical();
            updMany({ activeLayer: (spec.layers[0] || {}).id || 'skin', selectedOrgan: null, lensPinned: false, lensPinnedPoint: null, lensPinnedOrganId: null, guidedMode: false, guidedStep: 0, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, exploredOrgans: {}, verifiedIdentifications: {}, revealedLayers: {}, quizScore: 0, quizTotal: 0, quizFirstAttemptScore: 0, quizFirstAttemptTotal: 0, quizSupportedCount: 0, quizComplete: false, quizReviewMode: false, assessmentCompletedAt: 0, assessmentRecordedScore: 0, assessmentRecordedTotal: 0, assessmentEvidence: {}, quizFeedback: null, quizRetry: null, quizReviewQueue: [], completedObjectives: {}, organNotes: {}, organConfidence: {}, annotations: [], dissInquiry: defaultDissectionInquiry(), timeSpent: 0, guidedComplete: false, procedureByLayer: {}, attemptArchive: {}, compareTechniqueAttempts: false, compareReplayProgress: 0, compareReplayPlaying: false, visualEvidence: [], referenceEvidenceId: null, splitComparison: false, procedureFeedback: { message: 'Progress reset for ' + spec.name + '. Accessibility preferences were preserved.', tone: 'success', at: Date.now() }, scenarioStartedAt: 0, scenarioTimeRemaining: 0, scenarioCompletedAt: 0, activeInstrument: 'probe', livingFunctionEnabled: false, livingFunctionPaused: false, livingFunctionSpeed: 'normal', livingFunctionReplayToken: 0, incisionDepth: 'shallow', toolCalibration: normalizeToolCalibration(), canvasZoom: 1, canvasPanX: 0, canvasPanY: 0, traceNervous: false, traceCirculation: false, traceDigestion: false, traceRespiration: false, traceExcretory: false, showEndocrine: false, rulerMode: false, annotateMode: false, labelMode: 'show', resetConfirmPending: false, _incisionAnim: null, _layerTransition: null, _viewTransition: null, _layerBrowseTransition: null, _procedureDemo: null, _procedureReplay: null });
            updMany({ practicalMode: false, quizMode: false, practicalTimer: 0, practicalEndsAt: 0, practicalTargetIds: [], quizExplanation: null, guidedMode: false, guidedTargetIds: [], evidenceRemovePendingId: null, evidenceClearPending: false, labelMode: d.practicalMode ? (d._prePracticalLabelMode || 'show') : (d.labelMode || 'show') });
            if (addToast) addToast('\u21BA Progress reset for ' + spec.name, 'info');
            if (typeof announceToSR === 'function') announceToSR('Progress reset for ' + spec.name + '. Accessibility preferences were preserved.');
            focusResetControl('diss-reset-specimen');
          }

          function runFieldReadinessCheck() {
            var failedChecks = fieldReadinessChecks.filter(function (check) { return !check.ready; });
            var message = 'Field check: ' + fieldReadinessLabel + ' (' + fieldReadinessScore + '/100). ' + (failedChecks.length ? failedChecks.map(function (check) { return check.label + ': ' + check.detail; }).join(' ') : fieldReadinessCue);
            setProcedureFeedback(message, fieldReadinessTone === 'ready' ? 'success' : 'caution');
          }
          function resolveFieldReadinessIssue() {
            var issue = fieldReadinessChecks.find(function (check) { return !check.ready; });
            if (!issue) { setProcedureFeedback('Field check passed. The current setup is ready for one deliberate contact.', 'success'); return; }
            if (issue.id === 'view') {
              updMany({ toolbarViewOpen: true, toolbarToolsOpen: false, toolbarStudyOpen: false });
              focusDissectionTarget('diss-view-cycle', 'Choose the ' + procedureProtocol.recommendedView + ' view yourself, using the specimen landmarks as evidence.');
              return;
            }
            if (issue.id === 'optics') {
              updMany({ toolbarViewOpen: true, toolbarToolsOpen: false, toolbarStudyOpen: false });
              focusDissectionTarget('diss-view-tools', 'Adjust focus and illumination yourself. ' + currentOpticalInspection.suggestion);
              return;
            }
            if (issue.id === 'tool') {
              prepareNextProcedureStep();
              return;
            }
            setProcedureFeedback('Tissue needs attention before contact. Decide which intervention matches this evidence: ' + issue.detail, 'caution');
          }
          function prepareNextProcedureStep() {
            var next = nextProcedureInfo();
            if (next.action === 'complete') {
              var completionLearningGate = procedureLearningGateFor(next.action, currentProcedure);
              if (completionLearningGate.required) {
                focusDissectionTarget('diss-learning-checkpoint', 'Explain the final observed response before completing this layer.');
                return;
              }
              setProcedureFeedback('This layer protocol is complete. Review the debrief or continue to the next layer.', 'success');
              return;
            }
            var nextLearningGate = procedureLearningGateFor(next.action, currentProcedure);
            if (nextLearningGate.required) {
              focusDissectionTarget('diss-learning-checkpoint', nextLearningGate.phase === 'reflect' ? 'Explain the observed response before preparing another tool.' : 'Choose a safe plan before preparing the instrument.');
              return;
            }
            var nextTool = procedureInstrumentForAction(next.action);
            var tool = PROCEDURE_INSTRUMENTS.find(function (entry) { return entry.id === nextTool; }) || { label: nextTool };
            upd('techniquePanelOpen', true);
            setProcedureFeedback('Planner opened. Choose ' + tool.label + ', verify the view and calibration, then demonstrate ' + next.label.toLowerCase() + '. No setup was changed automatically.', 'working');
            focusDissectionTarget('diss-instrument-' + nextTool);
          }
          function selectProcedureInstrument(toolId, inputMethod) {
            var tool = PROCEDURE_INSTRUMENTS.find(function (entry) { return entry.id === toolId; });
            if (!tool) return;
            var toolState = procedureInstrumentStatus(tool.id);
            var readiness = procedureToolReadinessData(tool.id, currentProcedure);
            if (readiness.tone === 'restricted') {
              setProcedureFeedback(tool.label + ' is unavailable in the current scenario. Choose an instrument that remains in the tray.', 'caution');
              return false;
            }
            if (tool.id !== activeInstrument) cancelActiveCanvasGesture('Instrument gesture canceled before switching to ' + tool.label + '; no action was recorded.');
            updMany({ activeInstrument: tool.id, annotateMode: false, rulerMode: false });
            var selectionMessage = tool.label + ' selected' + (inputMethod ? ' with ' + inputMethod : '') + '. ' + readiness.label + ', ' + readiness.score + ' percent. ' + readiness.cue;
            setProcedureFeedback(selectionMessage, readiness.tone === 'ready' || readiness.tone === 'complete' ? 'success' : 'caution');
            return true;
          }
          function onInstrumentKeyDown(e, toolId) {
            var navigationKeys = ['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown', 'Home', 'End'];
            if (navigationKeys.indexOf(e.key) < 0) return;
            var availableTools = PROCEDURE_INSTRUMENTS.filter(function (tool) {
              return procedureToolReadinessData(tool.id, currentProcedure).tone !== 'restricted';
            });
            var currentIndex = availableTools.findIndex(function (tool) { return tool.id === toolId; });
            if (!availableTools.length) return;
            if (currentIndex < 0) currentIndex = 0;
            var forward = e.key === 'ArrowRight' || e.key === 'ArrowDown';
            var nextIndex = e.key === 'Home' ? 0 : (e.key === 'End' ? availableTools.length - 1 : (currentIndex + (forward ? 1 : -1) + availableTools.length) % availableTools.length);
            var nextTool = availableTools[nextIndex];
            e.preventDefault();
            selectProcedureInstrument(nextTool.id, 'arrow-key navigation');
            setTimeout(function () {
              var target = dissectionNodeById('diss-instrument-' + nextTool.id);
              if (target) target.focus();
            }, 0);
          }

          function onCompositeToolbarKeyDown(e) {
            var keys = ['ArrowLeft', 'ArrowRight', 'Home', 'End'];
            if (keys.indexOf(e.key) < 0) return;
            var controls = Array.prototype.slice.call(e.currentTarget.querySelectorAll('button:not([disabled])'));
            if (!controls.length) return;
            var currentIndex = controls.indexOf(e.target);
            if (currentIndex < 0) return;
            var nextIndex = e.key === 'Home' ? 0 : (e.key === 'End' ? controls.length - 1 : (currentIndex + (e.key === 'ArrowRight' ? 1 : -1) + controls.length) % controls.length);
            e.preventDefault();
            controls[nextIndex].focus();
          }

          function onSpecimenKeyDown(e, sk) {
            var specimenRoot = e.currentTarget && e.currentTarget.closest ? e.currentTarget.closest('[data-dissection-root]') : null;
            var idx = SPEC_KEYS.indexOf(sk);
            var nextIdx = idx;
            if (e.key === 'ArrowRight' || e.key === 'ArrowDown') nextIdx = (idx + 1) % SPEC_KEYS.length;
            else if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') nextIdx = (idx - 1 + SPEC_KEYS.length) % SPEC_KEYS.length;
            else if (e.key === 'Home') nextIdx = 0;
            else if (e.key === 'End') nextIdx = SPEC_KEYS.length - 1;
            else return;
            e.preventDefault();
            var nextKey = SPEC_KEYS[nextIdx];
            selectSpecimen(nextKey);
            setTimeout(function () {
              var target = scopedDissectionQuery(specimenRoot, '#diss-specimen-tab-' + nextKey);
              if (target) target.focus();
            }, 0);
          }

          function activeDissectionFullscreenElement() {
            if (typeof document === 'undefined') return null;
            return document.fullscreenElement || document.webkitFullscreenElement || document.mozFullScreenElement || null;
          }
          function focusDissectionFullscreenExit(stage) {
            var exitControl = stage && stage.querySelector ? stage.querySelector('[data-diss-fullscreen-exit]') : null;
            if (!exitControl || typeof exitControl.focus !== 'function') return;
            try { exitControl.focus({ preventScroll: true }); } catch (e) { exitControl.focus(); }
          }
          function restoreDissectionFullscreenFocus(stage) {
            var returnTarget = stage && stage._dissFullscreenReturn;
            if (!returnTarget) {
              try { returnTarget = window.__alloDissectionFullscreenReturn; } catch (e) {}
            }
            if (stage && stage._dissFullscreenEscapeHandler && typeof document !== 'undefined') {
              document.removeEventListener('keydown', stage._dissFullscreenEscapeHandler);
              stage._dissFullscreenEscapeHandler = null;
            }
            if (stage) stage._dissFullscreenReturn = null;
            try {
              if (window.__alloDissectionFullscreenReturn === returnTarget) window.__alloDissectionFullscreenReturn = null;
              if (window.__alloDissectionFullscreenStage === stage) window.__alloDissectionFullscreenStage = null;
            } catch (e) {}
            if (!returnTarget || !returnTarget.isConnected || typeof returnTarget.focus !== 'function') return;
            try { returnTarget.focus({ preventScroll: true }); } catch (e) { returnTarget.focus(); }
            setProcedureFeedback('Exited fullscreen specimen mode. Focus returned to the fullscreen control.');
          }
          function enterDissectionFullscreen(control) {
            try {
              var root = control && control.closest ? control.closest('[data-dissection-root]') : null;
              var stage = root && root.querySelector ? root.querySelector('[data-diss-fullscreen-stage]') : null;
              if (!stage || !window.__alloStemFS) throw new Error('Fullscreen unavailable');
              stage._dissFullscreenReturn = control;
              window.__alloDissectionFullscreenReturn = control;
              window.__alloDissectionFullscreenStage = stage;
              if (stage._dissFullscreenEscapeHandler && typeof document !== 'undefined') document.removeEventListener('keydown', stage._dissFullscreenEscapeHandler);
              stage._dissFullscreenEscapeHandler = function (event) {
                if (!event || event.key !== 'Escape') return;
                setTimeout(function () {
                  if (!stage.__alloFsOn && activeDissectionFullscreenElement() !== stage) restoreDissectionFullscreenFocus(stage);
                }, 0);
              };
              if (typeof document !== 'undefined') document.addEventListener('keydown', stage._dissFullscreenEscapeHandler);
              window.__alloStemFS(stage);
              setTimeout(function () {
                if (stage.__alloFsOn || activeDissectionFullscreenElement() === stage) focusDissectionFullscreenExit(stage);
              }, 0);
              setProcedureFeedback('Fullscreen specimen mode opened with view and tool controls. Use Exit fullscreen or press Escape to return.');
            } catch (e) {
              setProcedureFeedback('Fullscreen is unavailable in this view.', 'caution');
            }
          }
          function exitDissectionFullscreen(control) {
            try {
              var root = control && control.closest ? control.closest('[data-dissection-root]') : null;
              var stage = root && root.querySelector ? root.querySelector('[data-diss-fullscreen-stage]') : null;
              if (!stage || !window.__alloStemFS) throw new Error('Fullscreen unavailable');
              window.__alloStemFS(stage);
              setTimeout(function () {
                if (!stage.__alloFsOn && activeDissectionFullscreenElement() !== stage) restoreDissectionFullscreenFocus(stage);
              }, 0);
            } catch (e) {
              setProcedureFeedback('Use Escape to exit fullscreen.', 'caution');
            }
          }

          // View changes retain spatial context with a specimen-aware turn cue and an equivalent spoken announcement.
          function changeAnatomicalView(nextView, source) {
            var allowedViews = ['dorsal', 'ventral', 'lateral', 'internal'];
            if (allowedViews.indexOf(nextView) < 0) return;
            if (d.quizMode) {
              setProcedureFeedback('The anatomical view is locked during an assessment so the current question stays stable.', 'caution');
              return;
            }
            var fromView = d.anatomicalView || anatomicalView || 'dorsal';
            if (fromView === nextView) {
              var alignedMessage = 'Specimen is already in the ' + nextView + ' view.';
              setProcedureFeedback(alignedMessage, 'success');
              return;
            }
            cancelActiveCanvasGesture('Specimen gesture canceled before changing anatomical view; no action was recorded.');
            var viewMotionReduced = reducedMotionEnabled;
            try { viewMotionReduced = reducedMotionEnabled || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) { viewMotionReduced = reducedMotionEnabled; }
            var viewTransitionDuration = viewMotionReduced ? 1 : 820;
            var viewTransitionStartedAt = Date.now();
            var viewTransitionCanvas = canvasRefNode;
            clearDissectionCanvasTimer(viewTransitionCanvas, 'layerBrowseTransition');
            updMany({ anatomicalView: nextView, selectedOrgan: null, hoveredOrgan: null,
              _layerBrowseTransition: null,
              _viewTransition: { active: true, fromView: fromView, toView: nextView, source: source || 'view control', startedAt: viewTransitionStartedAt, duration: viewTransitionDuration, reducedMotion: viewMotionReduced, specimenShape: spec.bodyShape }
            });
            var viewMessage = 'View changed from ' + fromView + ' to ' + nextView + '. ' + (viewMotionReduced ? 'Reduced motion is active; the specimen switched instantly.' : 'A spatial turn cue shows the orientation change.');
            setProcedureFeedback(viewMessage, 'success');
            scheduleDissectionCanvasTimer(viewTransitionCanvas, 'viewTransition', function () {
              clearOwnedTimedDissectionState('_viewTransition', viewTransitionStartedAt, specimen);
            }, viewMotionReduced ? 90 : viewTransitionDuration + 120);
          }

          function selectLayer(layerId) {
            if (d.quizMode) {
              setProcedureFeedback('Layer navigation is locked during an assessment so the current question stays stable.', 'caution');
              return;
            }
            var layerIdx = spec.layers.findIndex(function (layer) { return layer.id === layerId; });
            if (layerIdx < 0 || layerIdx > unlockedLayerIdx) return;
            var fromLayerIdx = Math.max(0, currentLayerIdx);
            var fromLayer = spec.layers[fromLayerIdx] || spec.layers[0];
            var layer = spec.layers[layerIdx];
            if (layer.id === activeLayer) {
              if (typeof announceToSR === 'function') announceToSR('Already viewing ' + layer.name + ' layer, step ' + (layerIdx + 1) + ' of ' + spec.layers.length + '.');
              return;
            }
            cancelActiveCanvasGesture('Specimen gesture canceled before changing layers; no action was recorded.');
            var layerMotionReduced = reducedMotionEnabled;
            try { layerMotionReduced = reducedMotionEnabled || !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); } catch (e) { layerMotionReduced = reducedMotionEnabled; }
            var layerBrowseDuration = layerMotionReduced ? 1 : 720;
            var layerBrowseStartedAt = Date.now();
            var layerBrowseCanvas = canvasRefNode;
            clearDissectionCanvasTimer(layerBrowseCanvas, 'viewTransition');
            updMany({ activeLayer: layerId, selectedOrgan: null, hoveredOrgan: null, guidedMode: false, guidedStep: 0, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, quizFeedback: null, quizExplanation: null, _viewTransition: null,
              _layerBrowseTransition: { active: true, fromId: fromLayer.id, fromName: fromLayer.name, fromIcon: fromLayer.icon, fromIndex: fromLayerIdx, toId: layer.id, toName: layer.name, toIcon: layer.icon, toIndex: layerIdx, direction: layerIdx > fromLayerIdx ? 'deeper' : 'superficial', startedAt: layerBrowseStartedAt, duration: layerBrowseDuration, reducedMotion: layerMotionReduced, specimenShape: spec.bodyShape }
            });
            var layerMessage = 'Viewing ' + layer.name + ' layer, step ' + (layerIdx + 1) + ' of ' + spec.layers.length + '. Moving ' + (layerIdx > fromLayerIdx ? 'deeper' : 'toward the surface') + ' from ' + fromLayer.name + '.';
            if (typeof announceToSR === 'function') announceToSR(layerMessage + (layerMotionReduced ? ' Reduced motion is active; the layer switched instantly.' : ' Concentric depth rings show the change.'));
            scheduleDissectionCanvasTimer(layerBrowseCanvas, 'layerBrowseTransition', function () {
              clearOwnedTimedDissectionState('_layerBrowseTransition', layerBrowseStartedAt, specimen);
            }, layerMotionReduced ? 90 : layerBrowseDuration + 120);
          }
          function moveThroughLayer() {
            if (d._incisionAnim && d._incisionAnim.active) return;
            if (currentLayerDone && nextLayerDef) { selectLayer(nextLayerDef.id); return; }
            if (!currentLayerDone) performPrimaryNextAction();
          }

          function closeTimedPractical() {
            stopCanvasPracticalCountdown(canvasRefNode);
            try { window.__alloDissectionPracticalScore = 0; } catch (e) {}
            if (!d.practicalMode) return;
            updMany({ practicalMode: false, quizMode: false, practicalTimer: 0, practicalEndsAt: 0, practicalTargetIds: [], quizFeedback: null, quizExplanation: null, hoveredOrgan: null, guidedObservationPending: null, guidedObservationFeedback: null, labelMode: d._prePracticalLabelMode || 'show' });
          }
          function stopCanvasPracticalCountdown(canvas) {
            if (canvas && canvas._dissPracticalImmediateTimer) {
              clearTimeout(canvas._dissPracticalImmediateTimer);
              canvas._dissPracticalImmediateTimer = null;
            }
            if (canvas && canvas._dissPracticalTimer) {
              clearInterval(canvas._dissPracticalTimer);
              if (typeof window !== 'undefined' && window.__alloDissectionPracticalInterval === canvas._dissPracticalTimer) window.__alloDissectionPracticalInterval = null;
              canvas._dissPracticalTimer = null;
            }
          }
          function finishCanvasPractical(canvas, latest, timingUnavailable) {
            latest = latest || {};
            var practicalDeadline = Number(latest.practicalEndsAt) || 0;
            if (canvas && canvas._settledPracticalDeadline === practicalDeadline && practicalDeadline) return;
            if (canvas) canvas._settledPracticalDeadline = practicalDeadline || -1;
            stopCanvasPracticalCountdown(canvas);
            var finalScore = Number(latest.quizScore) || 0;
            var finalTotal = Math.max(finalScore, Number(latest.quizTotal) || 0);
            updMany({ practicalTimer: 0, practicalEndsAt: 0, practicalMode: false, quizMode: false, practicalTargetIds: [], quizFeedback: null, quizExplanation: null, hoveredOrgan: null, labelMode: latest._prePracticalLabelMode || 'show' });
            var completionMessage = timingUnavailable
              ? 'Timed practical ended because its timing record was unavailable. No additional response was recorded.'
              : (finalTotal ? 'Time up! Score: ' + finalScore + '/' + finalTotal : 'Time up! No responses recorded.');
            if (addToast) addToast(completionMessage, 'info');
            if (typeof announceToSR === 'function') announceToSR(timingUnavailable ? completionMessage : (finalTotal ? 'Practical assessment complete. Score ' + finalScore + ' out of ' + finalTotal + '.' : 'Practical assessment complete. No responses were recorded.'));
          }
          function tickCanvasPracticalCountdown(canvas) {
            if (!canvas || canvas._dissAlive === false || !canvas.isConnected) return;
            var latest = canvas._drawD || {};
            if (!latest.practicalMode) {
              stopCanvasPracticalCountdown(canvas);
              return;
            }
            var practicalDeadline = Number(latest.practicalEndsAt) || 0;
            if (!practicalDeadline) {
              finishCanvasPractical(canvas, latest, true);
              return;
            }
            var remaining = Math.max(0, Math.ceil((practicalDeadline - Date.now()) / 1000));
            if (remaining <= 0) {
              finishCanvasPractical(canvas, latest, false);
              return;
            }
            if (remaining !== Number(latest.practicalTimer)) upd('practicalTimer', remaining);
          }
          function syncCanvasPracticalCountdown(canvas) {
            if (!canvas) return;
            var latest = canvas._drawD || {};
            if (!latest.practicalMode) {
              stopCanvasPracticalCountdown(canvas);
              return;
            }
            if (!canvas._dissPracticalTimer) {
              canvas._dissPracticalTimer = setInterval(function () { tickCanvasPracticalCountdown(canvas); }, 1000);
              try { window.__alloDissectionPracticalInterval = canvas._dissPracticalTimer; } catch (e) {}
            }
            if (canvas._dissPracticalImmediateTimer) clearTimeout(canvas._dissPracticalImmediateTimer);
            canvas._dissPracticalImmediateTimer = null;
            tickCanvasPracticalCountdown(canvas);
          }

          function clearAssessmentTeachingTimers(canvas) {
            var teachingCanvas = canvas || canvasRefNode;
            clearDissectionCanvasTimer(teachingCanvas, 'procedureDemo');
            clearDissectionCanvasTimer(teachingCanvas, 'procedureReplay');
            clearDissectionCanvasTimer(teachingCanvas, 'compareReplay');
          }

          function restartAssessmentSession(reviewOnly) {
            var reviewIds = quizReviewIds.slice();
            if (reviewOnly && !reviewIds.length) {
              if (Array.isArray(d.quizReviewQueue) && d.quizReviewQueue.length) upd('quizReviewQueue', []);
              setProcedureFeedback('No missed structures are waiting for supported review.', 'working');
              return false;
            }
            clearAssessmentTeachingTimers();
            updMany({ flashcardMode: false, compareMode: false, guidedMode: false, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null,
              quizMode: true, quizIdx: 0, quizScore: 0, quizTotal: 0,
              quizFirstAttemptScore: reviewOnly ? assessmentRecordedScore : 0,
              quizFirstAttemptTotal: reviewOnly ? assessmentRecordedTotal : 0,
              quizSupportedCount: reviewOnly ? (Number(d.quizSupportedCount) || 0) : 0,
              quizComplete: false, quizReviewMode: !!reviewOnly,
              assessmentCompletedAt: Number(d.assessmentCompletedAt) || 0,
              quizFeedback: null, quizExplanation: null, quizRetry: null,
              quizReviewQueue: reviewIds, selectedOrgan: null, hoveredOrgan: null, quizSeed: Date.now(),
              toolbarStudyOpen: false, toolbarViewOpen: false, toolbarToolsOpen: false });
            if (typeof announceToSR === 'function') announceToSR(reviewOnly ? 'Supported review started for missed structures.' : 'New practice assessment started with up to five encountered structures. First-attempt evidence has been reset for this set.');
            return true;
          }
          function setStudyRoute(route) {
            if (route === 'quiz' && d._incisionAnim && d._incisionAnim.active) {
              setProcedureFeedback('Finish the current layer transition before starting an assessment.', 'caution');
              return;
            }
            if (d.practicalMode && route !== 'study') {
              setProcedureFeedback('The timed practical is still active. Open Study tools to exit it before changing learning routes.', 'caution');
              return;
            }
            var secondaryStudyOpen = !!(d.flashcardMode || d.compareMode || d.practicalMode);
            var routeIsClean = route === 'explore' ? !d.guidedMode && !d.quizMode && !secondaryStudyOpen && !d.toolbarStudyOpen : (route === 'guided' ? !!d.guidedMode && !d.quizMode && !d.toolbarStudyOpen : (route === 'quiz' ? !!d.quizMode && !d.guidedMode && !d.practicalMode && !d.toolbarStudyOpen : !!d.toolbarStudyOpen && !d.guidedMode && (d.practicalMode ? !!d.quizMode : !d.quizMode)));
            if (routeIsClean) return;
            cancelActiveCanvasGesture('Specimen gesture canceled before changing learning routes; no action was recorded.');
            if (route === 'study') {
              updMany({ guidedMode: false, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, quizMode: d.practicalMode ? true : false, quizFeedback: d.practicalMode ? d.quizFeedback : null, toolbarStudyOpen: true, toolbarViewOpen: false, toolbarToolsOpen: false });
              if (typeof announceToSR === 'function') announceToSR(secondaryStudyOpen ? 'Study tools open. The active study activity is unchanged.' : 'Study tools open. Choose flashcards, comparison, or a timed practical.');
              return;
            }
            closeTimedPractical();
            if (route === 'guided') {
              var firstGuided = guidedSteps[0];
              if (!firstGuided) {
                updMany({ flashcardMode: false, compareMode: false, quizMode: false, guidedMode: false, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, toolbarStudyOpen: false });
                setProcedureFeedback('Guided investigation is not ready yet. Complete the current reveal step or change view until at least one structure is visible.', 'caution');
                return;
              }
              updMany({ flashcardMode: false, compareMode: false, quizMode: false, guidedMode: true, guidedStep: 0, guidedTargetIds: guidedOrgans.map(function (org) { return org.id; }), guidedObservationPending: null, guidedObservationFeedback: null, toolbarStudyOpen: false });
              if (typeof announceToSR === 'function') announceToSR('Guided investigation started. ' + firstGuided.prompt);
              clearAssessmentTeachingTimers();
            } else if (route === 'quiz') {
              clearAssessmentTeachingTimers();
              updMany({ flashcardMode: false, compareMode: false, guidedMode: false, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, traceNervous: false, traceCirculation: false, traceDigestion: false, traceRespiration: false, traceExcretory: false, showEndocrine: false, livingFunctionEnabled: false, livingFunctionPaused: false, quizMode: true, quizIdx: 0, quizScore: 0, quizTotal: 0, quizFirstAttemptScore: 0, quizFirstAttemptTotal: 0, quizSupportedCount: 0, quizComplete: false, quizReviewMode: false, quizFeedback: null, quizExplanation: null, quizRetry: null, quizReviewQueue: Array.isArray(d.quizReviewQueue) ? d.quizReviewQueue : [], selectedOrgan: null, hoveredOrgan: null, quizSeed: Date.now(), quizAnswerMode: d.quizAnswerMode || 'choices', toolbarStudyOpen: false, toolbarViewOpen: false, toolbarToolsOpen: false, rulerMode: false, annotateMode: false, rulerStart: null, rulerEnd: null, compareTechniqueAttempts: false, compareReplayPlaying: false, splitComparison: false, beforeTechniqueView: false });
              updMany({ inspectionLens: false, lensPinned: false, lensPinnedPoint: null, lensPinnedOrganId: null, macroInset: false, _procedureDemo: null, _procedureReplay: null });
              if (typeof announceToSR === 'function') announceToSR('Practice assessment started. Choose multiple choice or answer directly on the specimen.');
            } else {
              updMany({ flashcardMode: false, compareMode: false, guidedMode: false, guidedTargetIds: [], guidedObservationPending: null, guidedObservationFeedback: null, quizMode: false, quizComplete: false, quizReviewMode: false, quizFeedback: null, toolbarStudyOpen: false });
              if (typeof announceToSR === 'function') announceToSR(advancedWorkspace ? 'Free explore mode active.' : 'Procedure practice mode active.');
            }
          }
          function chooseOrganFromDirectory(org, preserveFocus) {
            var exposureState = structureExposureState(org, currentProcedure);
            var evidenceKey = specimen + '|' + org.id;
            var alreadyExplored = !!(d.exploredOrgans || {})[evidenceKey];
            if (exposureState !== 'visible' && !alreadyExplored) {
              var recoveryView = alternateVisibleViewForOrgan(org);
              setProcedureFeedback(org.name + ' is ' + (exposureState === 'occluded' ? 'occluded in this view' : 'not yet exposed') + '. ' + (recoveryView ? 'Change to the ' + recoveryView + ' view before recording it.' : 'Improve the field before recording it.'), 'caution');
              return false;
            }
            if (exposureState !== 'visible') {
              upd('selectedOrgan', org.id);
              setProcedureFeedback('Reviewing previously recorded evidence for ' + org.name + '. The structure is not currently visible, so no new viewing progress was added.', 'working');
              if (!preserveFocus) focusDissectionTarget('diss-selection-title');
              return true;
            }
            var explored = Object.assign({}, d.exploredOrgans || {});
            explored[specimen + '|' + org.id] = true;
            var exploredForSpecimen = Object.keys(explored).filter(function (key) { return key.indexOf(specimen + '|') === 0 && explored[key]; }).length;
            var completesStructureScenario = currentScenarioDefinition().id === 'structure-trace' && exploredForSpecimen >= instructorRequiredStructures;
            updMany({ selectedOrgan: org.id, exploredOrgans: explored, scenarioCompletedAt: completesStructureScenario ? Date.now() : (Number(d.scenarioCompletedAt) || 0) });
            if (guidedMode && currentGuided && org.id === currentGuided.organId) {
              beginGuidedObservation(org);
            } else if (!preserveFocus) {
              focusDissectionTarget('diss-selection-title');
            }


            return true;
          }





          // ── Render ──

          var dissectionSaveStatus = currentDissectionSaveStatus(activeDissectionSaveKey, dissectionSaveOwner);
          var activeDissectionSaveIssue = activeDissectionSaveAccess && activeDissectionSaveAccess.state === 'blocked' ? activeDissectionSaveAccess.issue : null;
          var dissectionRootRefNode = null;

          return React.createElement("div", {
            className: "space-y-3",
            "data-dissection-root": true,
            "data-dissection-save-key": activeDissectionSaveKey,
            "data-dissection-save-owner": dissectionSaveOwner,
            ref: function (root) {
              if (!root) {
                var detachedRoot = dissectionRootRefNode;
                dissectionRootRefNode = null;
                if (detachedRoot) detachedRoot._alloDissectionRefreshSaveAccess = null;
                Promise.resolve().then(function () {
                  if (detachedRoot && !detachedRoot.isConnected) releaseDissectionSaveOwner(dissectionSaveOwner);
                });
                return;
              }
              dissectionRootRefNode = root;
              root._alloDissectionRefreshSaveAccess = function () {
                setLabToolData(function (previousToolData) {
                  var previousDissection = previousToolData.dissection || {};
                  var previousKey = 'dissection_progress_' + (previousDissection.specimen || 'frog');
                  if (previousKey !== activeDissectionSaveKey) return previousToolData;
                  return Object.assign({}, previousToolData, { dissection: Object.assign({}, previousDissection, { _dissSaveAccessRefreshAt: Date.now() }) });
                });
              };
            },
            "data-high-contrast": highContrastEnabled ? "true" : "false",
            "data-reduced-motion": reducedMotionEnabled ? "true" : "false",
            "data-text-size": largeTextEnabled ? "large" : "default",
            "data-simplified-instructions": simplifiedInstructions ? "true" : "false",
            "data-workspace-mode": workspaceMode,
            "data-assessment-mode": d.quizMode || d.practicalMode ? "true" : "false",
          },
            React.createElement("a", { className: "diss-skip-link", href: "#diss-canvas", onClick: function (event) { event.preventDefault(); focusDissectionTarget('diss-canvas', 'Interactive specimen focused.', event.currentTarget.closest('[data-dissection-root]')); } }, 'Skip to interactive specimen'),
            React.createElement("a", { className: "diss-skip-link", href: "#diss-structure-directory", onClick: function (event) { event.preventDefault(); focusDissectionTarget('diss-structure-directory', 'Structure directory focused.', event.currentTarget.closest('[data-dissection-root]')); } }, 'Skip to structure directory and notes'),

            React.createElement("div", {
              className: "diss-save-status",
              "data-dissection-save-status": true,
              "data-state": dissectionSaveStatus.state,
              role: "status",
              "aria-live": "polite",
              "aria-atomic": "true"
            },
              React.createElement("span", { className: "diss-save-status__glyph", "data-dissection-save-glyph": true, "aria-hidden": "true" }, dissectionSaveStatus.state === 'error' ? '\u00D7' : (dissectionSaveStatus.state === 'warning' ? '!' : '\u2713')),
              React.createElement("span", { "data-dissection-save-message": true }, dissectionSaveStatus.message)
            ),
            activeDissectionSaveIssue && React.createElement("section", {
              className: "rounded-xl border border-amber-400 bg-amber-50 p-3 text-amber-950",
              "data-dissection-protected-save": true,
              role: "region",
              "aria-labelledby": "diss-protected-save-title"
            },
              React.createElement("h3", { id: "diss-protected-save-title", className: "text-sm font-black" }, 'Protected saved progress'),
              React.createElement("p", { className: "mt-1 text-xs" }, dissectionSaveIssueMessage(activeDissectionSaveIssue, spec.name) + ' Changes in this temporary session will not replace the protected record.'),
              React.createElement("p", { className: "mt-1 text-xs" }, 'Retry safely reloads the saved version and replaces temporary-session edits only after the record can be read and validated.'),
              !d.resetConfirmPending && React.createElement("button", { id: "diss-protected-save-retry", type: "button", disabled: !!d._dissSaveRetrying, className: "mt-2 mr-2 min-h-11 rounded-lg border border-amber-600 bg-white px-3 py-2 text-xs font-bold disabled:opacity-60", onClick: retryProtectedDissectionSave }, d._dissSaveRetrying ? 'Checking saved progress…' : 'Retry saved progress safely'),
              !d.resetConfirmPending && React.createElement("button", { id: "diss-protected-save-reset", type: "button", disabled: !!d._dissSaveRetrying, className: "mt-2 min-h-11 rounded-lg border border-amber-600 bg-white px-3 py-2 text-xs font-bold disabled:opacity-60", onClick: requestSpecimenReset }, 'Start fresh with confirmed reset'),
              d.resetConfirmPending && React.createElement("div", { id: "diss-reset-confirmation", className: "diss-reset-confirm mt-2", role: "group", "aria-labelledby": "diss-reset-confirm-title" },
                React.createElement("p", { id: "diss-reset-confirm-title", role: "alert", "aria-live": "assertive", "aria-atomic": "true" }, 'Reset ' + spec.name + '? This permanently removes the protected saved record and starts a fresh lab. Accessibility preferences are preserved.'),
                React.createElement("button", { id: "diss-reset-confirm", type: "button", "data-confirm": "true", onClick: confirmSpecimenReset }, 'Confirm reset'),
                React.createElement("button", { id: "diss-reset-cancel", type: "button", onClick: cancelSpecimenReset }, 'Cancel')
              )
            ),

            React.createElement("section", { className: "diss-mission", "data-dissection-mission": true, "aria-labelledby": "diss-mission-title" },
              React.createElement("div", { className: "diss-mission__main" },
                React.createElement("p", { className: "diss-mission__eyebrow" }, __alloT('stem.dissection.virtual_biology_laboratory', 'Virtual Biology Laboratory')),
                React.createElement("h2", { id: "diss-mission-title" }, __alloT('stem.dissection.virtual_dissection_lab', 'Virtual Dissection Lab')),
                React.createElement("p", { className: "diss-mission__copy" }, missionText),
                React.createElement("ol", { className: "diss-workflow", "aria-label": "Dissection workflow" },
                  workflowSteps.map(function (step, stepIdx) {
                    var workflowPosition = stepIdx + 1;
                    var workflowState = workflowPosition < nextActionModel.step ? 'complete' : (workflowPosition === nextActionModel.step ? 'current' : 'upcoming');
                    return React.createElement("li", { key: step.label, "data-state": workflowState, "aria-current": workflowState === 'current' ? 'step' : undefined },
                      React.createElement("span", { className: "diss-workflow__number", "aria-hidden": "true" }, String(stepIdx + 1)),
                      step.label
                    );
                  })
                ),
                React.createElement("p", { className: "diss-mission__copy" }, __alloT('stem.dissection.virtual_practice_notice', 'Virtual practice supports observation and comparison. Follow your instructor’s safety, ethics, handling, and disposal procedures for any physical specimen.'))
              ),
              React.createElement("aside", { className: "diss-mission__action", "aria-label": "Current workflow position" },
                React.createElement("span", { className: "diss-mission__action-label" }, 'Current workflow position'),
                React.createElement("h3", null, nextActionModel.phase),
                React.createElement("p", null, 'Step ' + nextActionModel.step + ' of ' + workflowSteps.length)
              ),
              React.createElement("div", { className: "diss-mission__stats", "aria-label": "Current lab status" },
                [
                  { label: 'Specimen', value: spec.icon + ' ' + spec.name },
                  { label: 'Layer', value: (currentLayerIdx + 1) + ' of ' + spec.layers.length + ' · ' + currentLayerDef.name },
                  { label: 'Structures viewed', value: exploredCount + ' of ' + totalOrgansInSpecimen }
                ].map(function (stat) {
                  return React.createElement("div", { className: "diss-stat", key: stat.label },
                    React.createElement("span", { className: "diss-stat__label" }, stat.label),
                    React.createElement("span", { className: "diss-stat__value", title: stat.value }, stat.value)
                  );
                })
              )
            ),

            renderNextActionCard(),
            renderProcedureLearningCheckpoint(),
            renderGuidedObservationCheck(),

            React.createElement("section", { className: "diss-workspace-mode", "data-dissection-workspace-mode": true, "aria-labelledby": "diss-workspace-mode-title" },
              React.createElement("div", { className: "diss-workspace-mode__copy" },
                React.createElement("strong", { id: "diss-workspace-mode-title" }, workspaceMode === 'advanced' ? 'Advanced workspace' : 'Essentials workspace'),
                React.createElement("span", null, workspaceMode === 'advanced'
                  ? 'Adds scenarios, detailed telemetry, technique comparison, instructor settings, and extended visualization tools.'
                  : 'Keeps the procedure, instruments, observation, evidence, assessment, and accessibility controls in focus.')
              ),
              React.createElement("div", { className: "diss-workspace-mode__choices", role: "group", "aria-label": "Workspace detail" },
                React.createElement("button", { type: "button", "aria-pressed": workspaceMode === 'essentials', onClick: function () { setDissectionWorkspaceMode('essentials'); } }, 'Essentials'),
                React.createElement("button", { type: "button", "aria-pressed": workspaceMode === 'advanced', onClick: function () { setDissectionWorkspaceMode('advanced'); } }, 'Advanced')
              )
            ),

            React.createElement("div", { className: "diss-mode-rail", role: "toolbar", "aria-label": "Learning route", "aria-orientation": "horizontal", onKeyDown: onCompositeToolbarKeyDown },
              [
                { id: 'explore', icon: '🔎', label: advancedWorkspace ? __alloT('stem.dissection.free_explore', 'Free explore') : 'Procedure practice' },
                { id: 'guided', icon: '🧭', label: __alloT('stem.dissection.guided_investigation', 'Guided investigation') },
                { id: 'study', icon: '\uD83D\uDCDA', label: 'Study tools' },
                { id: 'quiz', icon: '🧠', label: __alloT('stem.dissection.practice_assessment', 'Practice assessment') }
              ].map(function (route) {
                var active = studyRoute === route.id;
                var routeLocked = !!d.practicalMode && route.id !== 'study';
                return React.createElement("button", {
                  type: "button",
                  key: route.id,
                  disabled: routeLocked,
                  "aria-label": route.label + (routeLocked ? ', locked during timed practical' : ''),
                  className: "diss-route-button",
                  "data-active": active ? "true" : "false",
                  "aria-pressed": active,
                  tabIndex: active && !routeLocked ? 0 : -1,
                  onClick: function () { setStudyRoute(route.id); }
                }, route.icon + ' ' + route.label);
              })
            ),

            React.createElement("section", { className: "diss-picker", "data-dissection-specimens": true, "aria-labelledby": "diss-specimen-heading" },
              React.createElement("div", { className: "diss-section-heading" },
                React.createElement("h3", { id: "diss-specimen-heading" }, 'Choose a specimen'),
                React.createElement("p", null, SPEC_KEYS.length + ' comparative anatomy models')
              ),
              React.createElement("div", { className: "diss-specimen-rail", role: "tablist", "aria-label": "Dissection specimens" },
                SPEC_KEYS.map(function (sk) {
                  var sp = SPECIMENS[sk];
                  var isActive = sk === specimen;
                  return React.createElement("button", {
                    type: "button",
                    key: sk,
                    id: 'diss-specimen-tab-' + sk,
                    role: "tab",
                    disabled: !!d.quizMode,
                    "aria-label": d.quizMode ? "Specimen locked during assessment: " + sp.name : "Select specimen: " + sp.name,
                    "aria-selected": isActive,
                    "aria-controls": "diss-workspace",
                    tabIndex: isActive ? 0 : -1,
                    onKeyDown: function (e) { onSpecimenKeyDown(e, sk); },
                    onClick: function () { selectSpecimen(sk); },
                    className: "diss-specimen-tab"
                  }, sp.icon + ' ' + sp.name);
                })
              )
            ),

            React.createElement("nav", { className: "diss-layer-stepper", "data-dissection-layer-stepper": true, "aria-labelledby": "diss-layer-heading" },
              React.createElement("div", { className: "diss-section-heading" },
                React.createElement("h3", { id: "diss-layer-heading" }, 'Anatomical layers'),
                React.createElement("p", null, revealedLayerCount + ' of ' + spec.layers.length + ' completed')
              ),
              React.createElement("div", { className: "diss-layer-list" },
                spec.layers.map(function (layer, layerIdx) {
                  var isCurrent = layer.id === activeLayer;
                  var isDone = !!revealedLayers[layer.id];
                  var isUnlocked = layerIdx <= unlockedLayerIdx;
                  var state = isCurrent ? 'current' : (isDone ? 'revealed' : (isUnlocked ? 'available' : 'locked'));
                  return React.createElement("button", {
                    type: "button",
                    key: layer.id,
                    className: "diss-layer-button",
                    "data-state": state,
                    disabled: !isUnlocked || !!d.quizMode,
                    "aria-current": isCurrent ? "step" : undefined,
                    "aria-label": 'Layer ' + (layerIdx + 1) + ': ' + layer.name + (d.quizMode ? ', locked during assessment' : isCurrent ? ', current' : isDone ? ', revealed' : isUnlocked ? ', available' : ', locked'),
                    onClick: function () { selectLayer(layer.id); }
                  },
                    React.createElement("span", { className: "diss-layer-index", "aria-hidden": "true" }, String(layerIdx + 1)),
                    React.createElement("span", { className: "diss-layer-name" }, layer.icon + ' ' + layer.name),
                    React.createElement("span", { className: "diss-layer-state", "aria-hidden": "true" }, isCurrent ? '●' : isDone ? '✓' : isUnlocked ? '→' : '🔒')
                  );
                })
              )
            ),

            // Toolbar: compact secondary controls
            React.createElement("div", { className: "diss-toolbar flex flex-wrap items-center bg-slate-50 border border-slate-400", role: "toolbar", "aria-label": "Secondary lab controls", "aria-orientation": "horizontal", onKeyDown: onCompositeToolbarKeyDown },

              // ── View toggle ──
              React.createElement("button", { type: "button", tabIndex: !d.toolbarToolsOpen ? 0 : -1, "aria-label": d.quizMode ? "Toggle accessibility toolbar" : "Toggle View toolbar", "aria-expanded": !!d.toolbarViewOpen, "aria-controls": "diss-view-tools",
                onClick: function () { upd('toolbarViewOpen', !d.toolbarViewOpen); upd('toolbarToolsOpen', false); upd('toolbarStudyOpen', false); },
                className: "flex items-center gap-1 px-3 py-1.5 rounded-lg text-xs font-bold transition-all " + (d.toolbarViewOpen ? 'bg-blue-600 text-white shadow-md' : 'transition-colors bg-white text-slate-600 border border-slate-400 hover:bg-blue-50 active:scale-[0.97]')
              }, (d.quizMode ? '\u267F Accessibility ' : '\uD83D\uDC41 View ') + (d.toolbarViewOpen ? '\u25B2' : '\u25BC')),

              // ── Tools toggle ──
              !d.quizMode && React.createElement("button", { type: "button", tabIndex: d.toolbarToolsOpen ? 0 : -1, "aria-label": "Toggle Tools toolbar", "aria-expanded": !!d.toolbarToolsOpen, "aria-controls": "diss-lab-tools",
                onClick: function () { upd('toolbarToolsOpen', !d.toolbarToolsOpen); upd('toolbarViewOpen', false); upd('toolbarStudyOpen', false); },
                className: "diss-advanced-only flex items-center gap-1 px-3 py-1.5 rounded-lg text-xs font-bold transition-all " + (d.toolbarToolsOpen ? 'bg-emerald-700 text-white shadow-md' : 'transition-colors bg-white text-slate-600 border border-slate-400 hover:bg-emerald-50 active:scale-[0.97]')
              }, '\uD83D\uDEE0 Tools ' + (d.toolbarToolsOpen ? '\u25B2' : '\u25BC')),

              // ── Study toggle ──

            ),

            // ── View group expanded ──
            d.toolbarViewOpen && React.createElement("div", { id: "diss-view-tools", className: "diss-tool-panel flex flex-wrap bg-blue-50 rounded-xl border border-blue-200 animate-[fadeIn_0.2s_ease-out]", role: "region", tabIndex: -1, "aria-label": "View and accessibility controls" },
              React.createElement("button", { disabled: !!d.quizMode, "aria-label": d.quizMode ? "Organ name labels hidden during assessment" : "Toggle organ name labels", "aria-pressed": !d.quizMode && d.labelMode !== 'hidden', onClick: function () { upd('labelMode', d.labelMode === 'show' ? 'hidden' : 'show'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (!d.quizMode && d.labelMode !== 'hidden' ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\uD83C\uDFF7 Labels'),
              React.createElement("button", { "aria-label": "Toggle high contrast mode", "aria-pressed": highContrastEnabled, onClick: function () { setAccessibilityPreference('highContrast', !highContrastEnabled, 'High contrast'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (highContrastEnabled ? 'bg-yellow-500 text-black' : 'bg-white text-blue-700 border border-blue-200') }, '\u2600 High contrast'),
              React.createElement("button", { "aria-label": "Toggle additional reduced motion; operating system reduced motion settings are always honored", "aria-pressed": reducedMotionEnabled, onClick: function () { setAccessibilityPreference('reducedMotion', !reducedMotionEnabled, 'Reduced motion'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (reducedMotionEnabled ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, reducedMotionEnabled ? '\u23F8 Reduced motion on' : '\u25B6 Reduced motion off'),
              React.createElement("button", { "aria-label": "Toggle larger interface text", "aria-pressed": largeTextEnabled, onClick: function () { setAccessibilityPreference('largeText', !largeTextEnabled, 'Larger interface text'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (largeTextEnabled ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, largeTextEnabled ? 'A+ Larger text on' : 'A Larger text off'),
              React.createElement("button", { "aria-label": "Toggle simplified step instructions", "aria-pressed": simplifiedInstructions, onClick: function () { setAccessibilityPreference('simplifiedInstructions', !simplifiedInstructions, 'Simplified instructions'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (simplifiedInstructions ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, simplifiedInstructions ? 'Simple steps on' : 'Simple steps off'),
              React.createElement("button", { "aria-label": "Toggle dissection sound effects", "aria-pressed": d.soundEnabled !== false, onClick: function () { var enabled = d.soundEnabled === false; try { window.__alloDissectionSoundEnabled = enabled; } catch (e) {} upd('soundEnabled', enabled); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.soundEnabled !== false ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, d.soundEnabled !== false ? '\uD83D\uDD0A Sound on' : '\uD83D\uDD07 Sound off'),
              React.createElement("button", { "aria-label": "Toggle tactile instrument feedback", "aria-pressed": d.tactileFeedback !== false, onClick: function () { upd('tactileFeedback', d.tactileFeedback === false); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.tactileFeedback !== false ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, d.tactileFeedback !== false ? '\u223F Tactile on' : '\u223F Tactile off'),
              React.createElement("button", { "aria-label": "Visual presentation: cycle guided, realistic, and accessible", onClick: function () { setVisualRealism(visualRealism === 'guided' ? 'realistic' : (visualRealism === 'realistic' ? 'accessible' : 'guided')); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u2726 Visuals: ' + (visualRealism === 'realistic' ? 'Realistic' : visualRealism === 'accessible' ? 'Accessible' : 'Guided')),
              !d.quizMode && React.createElement(React.Fragment, null,
              React.createElement("button", { id: "diss-view-cycle", disabled: !!d.practicalMode, "aria-label": d.practicalMode ? "Anatomical view locked during timed practical" : "Anatomical view: cycle dorsal, ventral, lateral, and internal", onClick: function () { var views = ['dorsal', 'ventral', 'lateral', 'internal']; changeAnatomicalView(views[(views.indexOf(anatomicalView) + 1) % views.length], 'view toolbar'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u21BB View: ' + anatomicalView),
              React.createElement("button", { "aria-label": "Toggle layer cross-section", "aria-pressed": crossSectionMode, onClick: function () { upd('crossSectionMode', !crossSectionMode); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (crossSectionMode ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u25A4 Cross-section ' + (crossSectionMode ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Specimen condition: cycle standard, preserved, dehydrated, cloudy, and swollen", onClick: function () { var conditions = ['standard', 'preserved', 'dehydrated', 'cloudy', 'swollen']; upd('specimenCondition', conditions[(conditions.indexOf(specimenCondition) + 1) % conditions.length]); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u25C9 Condition: ' + specimenCondition),
              React.createElement("button", { "aria-label": "Toggle curated anatomical relationships", "aria-pressed": relationshipMode, onClick: function () { upd('relationshipMode', !relationshipMode); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (relationshipMode ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u2194 Relationships ' + (relationshipMode ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Rendering quality: cycle auto, high, and balanced", onClick: function () { var qualities = ['auto', 'high', 'balanced']; upd('renderQuality', qualities[(qualities.indexOf(renderQuality) + 1) % qualities.length]); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u2699 Quality: ' + renderQuality),
              !d.quizMode && React.createElement("button", { "aria-label": "Toggle movable inspection lens", "aria-pressed": inspectionLens, onClick: function () { upd('inspectionLens', !inspectionLens); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (inspectionLens ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\uD83D\uDD0D Lens ' + (inspectionLens ? 'on' : 'off')),
              !d.quizMode && React.createElement("button", { "aria-label": lensPinned ? "Release pinned inspection target" : "Pin inspection lens to selected structure or current pointer", "aria-pressed": lensPinned, onClick: toggleInspectionPin, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (lensPinned ? 'bg-cyan-700 text-white' : 'bg-white text-blue-700 border border-blue-200') }, lensPinned ? '\u2316 Unpin lens' : '\u2316 Pin lens'),
              !d.quizMode && React.createElement("button", { "aria-label": "Cycle inspection magnification: 2, 4, or 6 times", onClick: cycleLensMagnification, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u2295 Magnify ' + lensMagnification + 'x'),
              !d.quizMode && React.createElement("button", { "aria-label": "Cycle inspection focus depth: surface, structure, or deep", onClick: cycleLensFocusDepth, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u25C9 Focus: ' + lensFocusDepth),
              React.createElement("button", { "aria-label": "Toggle detailed pointer-following instrument visuals and contact response", "aria-pressed": instrumentVisuals, onClick: function () { upd('instrumentVisuals', !instrumentVisuals); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (instrumentVisuals ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u2692 Visual tools ' + (instrumentVisuals ? 'on' : 'off')),
              !d.quizMode && React.createElement("button", { "aria-label": "Toggle docked macro inspection view", "aria-pressed": macroInset, onClick: function () { upd('macroInset', !macroInset); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (macroInset ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u25A3 Macro view ' + (macroInset ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle specimen-specific surface and tray depth details", "aria-pressed": sceneDetail, onClick: function () { upd('sceneDetail', !sceneDetail); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (sceneDetail ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u2726 Scene detail ' + (sceneDetail ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle anatomical depth atlas with shape-coded landmarks", "aria-pressed": depthAtlasEnabled, onClick: function () { var nextDepthAtlas = !depthAtlasEnabled; upd('depthAtlas', nextDepthAtlas); setProcedureFeedback('Depth atlas ' + (nextDepthAtlas ? 'enabled. Surface, mid-depth, and deep structures now use distinct landmark shapes.' : 'disabled. Standard landmark pins remain visible.'), 'working'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (depthAtlasEnabled ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u25C8 Depth atlas ' + (depthAtlasEnabled ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle responsive tissue relief lighting around visible structures", "aria-pressed": tissueReliefEnabled, onClick: function () { var nextTissueRelief = !tissueReliefEnabled; upd('tissueRelief', nextTissueRelief); setProcedureFeedback('Tissue relief ' + (nextTissueRelief ? 'enabled. Structure elevation and recess cues now respond to the inspection light.' : 'disabled. Flat anatomical shading remains active.'), 'working'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (tissueReliefEnabled ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u25D2 Tissue relief ' + (tissueReliefEnabled ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle animated direction markers for curated anatomical relationships", "aria-pressed": relationshipMotion, onClick: function () { upd('relationshipMotion', !relationshipMotion); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (relationshipMotion ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u2192 Flow motion ' + (relationshipMotion ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle selected-structure focus isolation", "aria-pressed": focusMode, onClick: function () { upd('focusMode', !focusMode); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (focusMode ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, '\u25CE Focus mode ' + (focusMode ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle subtle pointer-responsive specimen depth", "aria-pressed": parallaxDepth, onClick: function () { upd('parallaxDepth', !parallaxDepth); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (parallaxDepth ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, 'Depth motion ' + (parallaxDepth ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Toggle live and reference split comparison", "aria-pressed": splitComparison && !!referenceEvidence, disabled: !referenceEvidence, onClick: function () { upd('splitComparison', !splitComparison); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (splitComparison && referenceEvidence ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') + (!referenceEvidence ? ' opacity-50 cursor-not-allowed' : '') }, 'Split compare ' + (splitComparison && referenceEvidence ? 'on' : 'off')),
              React.createElement("button", { "aria-label": "Lab light: cycle neutral, warm, and cool", onClick: function () { upd('labLight', labLight === 'neutral' ? 'warm' : (labLight === 'warm' ? 'cool' : 'neutral')); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\uD83D\uDCA1 ' + labLight + ' light'),
              React.createElement("label", { className: "diss-light-intensity", "data-tone": currentIllumination.tone, htmlFor: "diss-light-intensity-range" },
                React.createElement("span", null, 'Illumination intensity · ' + currentIllumination.label),
                React.createElement("output", { htmlFor: "diss-light-intensity-range" }, lightIntensity + '%'),
                React.createElement("input", {
                  disabled: !!d.practicalMode, id: "diss-light-intensity-range", type: "range", min: 20, max: 100, step: 2, value: lightIntensity,
                  "aria-valuetext": lightIntensity + ' percent, ' + currentIllumination.label + ', glare risk ' + currentIllumination.glareRisk + ' percent',
                  onChange: function (event) { upd('lightIntensity', Number(event.target.value)); },
                  onBlur: function () { var result = illuminationAssessmentData(currentProcedure, lightIntensity); setProcedureFeedback('Illumination ' + lightIntensity + ' percent: ' + result.label + '. ' + result.suggestion, result.score >= 72 ? 'success' : 'caution'); }
                })
              ),
              React.createElement("button", { "aria-label": "Laboratory light direction: cycle overhead, left, right, and raking", onClick: function () { var directions = ['overhead', 'left', 'right', 'raking']; upd('lightDirection', directions[(directions.indexOf(lightDirection) + 1) % directions.length]); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u2198 Light angle: ' + lightDirection),
              React.createElement("button", { "aria-label": "Generate another deterministic specimen variation", onClick: function () { upd('variationSeed', variationSeed + 1); setProcedureFeedback('Loaded specimen variation ' + (variationSeed + 1) + '. Landmark shifts are small and deterministic.'); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u21BB Variation ' + variationSeed),
              React.createElement("button", { "aria-label": "Toggle before and after technique view", "aria-pressed": !!d.beforeTechniqueView, onClick: function () { upd('beforeTechniqueView', !d.beforeTechniqueView); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold " + (d.beforeTechniqueView ? 'bg-blue-600 text-white' : 'bg-white text-blue-700 border border-blue-200') }, d.beforeTechniqueView ? '\u25C0 Before view' : '\u25B6 After view')
              ),
              React.createElement("button", { "aria-label": "Enter fullscreen specimen mode with view and tool controls", "aria-controls": "diss-fullscreen-dock", onClick: function (event) { enterDissectionFullscreen(event.currentTarget); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u26F6 Fullscreen'),
              React.createElement("button", { "aria-label": "Animation speed: cycle normal, slow, and fast", onClick: function () { var s = d.animSpeed === 'fast' ? 'normal' : (d.animSpeed === 'normal' ? 'slow' : 'fast'); upd('animSpeed', s); }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\u23E9 ' + (d.animSpeed === 'slow' ? 'Slow' : d.animSpeed === 'fast' ? 'Fast' : 'Normal')),
              React.createElement("button", { "aria-label": "Print clean dissection report", onClick: function () { try { setProcedureFeedback('Print view opened. Choose a printer or save the report as a PDF.'); window.print(); } catch (e) { if (addToast) addToast('Print is unavailable in this view.', 'error'); setProcedureFeedback('Print is unavailable in this view.', 'caution'); } }, className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-blue-700 border border-blue-200" }, '\uD83D\uDDA8 Print')
            ),

            // Tools group expanded
            !d.quizMode && d.toolbarToolsOpen && React.createElement("div", { id: "diss-lab-tools", className: "diss-advanced-only diss-tool-panel flex flex-wrap bg-emerald-50 rounded-xl border border-emerald-200 animate-[fadeIn_0.2s_ease-out]", role: "region", "aria-label": "Lab tools" },

              React.createElement("button", { "aria-label": "Ruler", "aria-pressed": !!d.rulerMode,
                onClick: function () { var enabled = !d.rulerMode; upd('rulerMode', enabled); if (enabled) upd('annotateMode', false); setProcedureFeedback(enabled ? 'Ruler active. Select two points on the specimen to measure their distance.' : 'Ruler off.'); },
                title: 'Ruler' + ' — Measure distances on the specimen',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.rulerMode ? 'bg-emerald-700 text-white' : 'transition-colors bg-white text-emerald-700 border border-emerald-200 hover:bg-emerald-100 active:scale-[0.97]')
              }, '\uD83D\uDCCF ' + 'Ruler'),

              React.createElement("button", { "aria-label": "Annotate", "aria-pressed": !!d.annotateMode,
                onClick: function () { var enabled = !d.annotateMode; upd('annotateMode', enabled); if (enabled) upd('rulerMode', false); setProcedureFeedback(enabled ? 'Annotation active. Select points on the specimen to draw a connected evidence line.' : 'Annotation off.'); },
                title: 'Annotate' + ' — Draw annotations on the canvas',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.annotateMode ? 'bg-emerald-700 text-white' : 'transition-colors bg-white text-emerald-700 border border-emerald-200 hover:bg-emerald-100 active:scale-[0.97]')
              }, '\u270F ' + 'Annotate'),

              React.createElement("button", { "aria-label": "Screenshot",
                onClick: function () {
                  try {
                    var c = queryDissectionNode('[data-diss-canvas]');
                    if (c) { var link = document.createElement('a'); link.download = specimen + '_dissection.png'; link.href = c.toDataURL(); link.click(); if (addToast) addToast('\uD83D\uDCF8 Screenshot saved!', 'success'); setProcedureFeedback('Screenshot saved as ' + link.download + '.'); }
                  } catch (e) { if (addToast) addToast('Screenshot failed', 'error'); setProcedureFeedback('Screenshot failed in this view.', 'caution'); }
                },
                title: 'Screenshot' + ' — Save the current canvas view',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-emerald-700 border border-emerald-200 hover:bg-emerald-100 transition-all active:scale-[0.97]"
              }, '\uD83D\uDCF8 ' + 'Screenshot'),

              React.createElement("button", { "aria-label": "Clear annotations", disabled: !(d.annotations || []).length,
                onClick: function () { var count = (d.annotations || []).length; upd('annotations', []); setProcedureFeedback('Cleared ' + count + ' annotation point' + (count === 1 ? '' : 's') + '.'); },
                title: 'Clear annotations \u2014 Remove annotation marks from the current specimen',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-emerald-700 border border-emerald-200 disabled:opacity-50 disabled:cursor-not-allowed"
              }, '\u232B Clear annotations'),

              React.createElement("button", { "aria-label": visualEvidence.length >= evidenceFrameLimit ? "Evidence notebook full; manage frames before capturing another" : "Capture compressed visual evidence frame",
                "aria-describedby": "diss-evidence-capacity",
                "data-evidence-capture": true,
                onClick: captureVisualEvidence,
                title: visualEvidence.length >= evidenceFrameLimit ? 'Evidence notebook full — download or remove a frame first' : 'Capture evidence — Save a lightweight frame in the specimen notebook',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-emerald-700 border border-emerald-200 hover:bg-emerald-100 transition-all active:scale-[0.97]"
              }, visualEvidence.length >= evidenceFrameLimit ? 'Evidence full · manage notebook' : 'Capture evidence'),

              React.createElement("button", { "aria-label": "Copy accurate lab report to clipboard",
                onClick: function () {
                  var report = 'Virtual Dissection Lab Report: ' + spec.name + '\n' + '\u2500'.repeat(36) + '\n';
                  report += 'Specimen: ' + spec.name + '\nLayers revealed: ' + revealedLayerCount + '/' + spec.layers.length + '\n';
                  report += 'Structures viewed: ' + exploredCount + '/' + totalOrgansInSpecimen + '\nVerified identifications: ' + verifiedIdentificationCount + '\nCompleted first-attempt assessment: ' + assessmentRecordedScore + '/' + assessmentRecordedTotal + '; supported practice: ' + (Number(d.quizSupportedCount) || 0) + '\n';
                  report += 'Active investigation time: ' + (d.timeSpent || 0) + ' seconds\n';
                  var techniqueLayers = spec.layers.map(function (layer) { return (d.procedureByLayer || {})[layer.id] || {}; });
                  var completedTechniques = techniqueLayers.filter(function (state) { return procedureStepIndex(state) >= (procedureProtocol.order || []).length; }).length;
                  var techniqueCautions = techniqueLayers.reduce(function (sum, state) { return sum + (Number(state.errors) || 0); }, 0);
                  report += 'Technique sequences complete: ' + completedTechniques + '/' + spec.layers.length + '\n';
                  report += 'Technique cautions reviewed: ' + techniqueCautions + '\n';
                  report += 'Technique support: ' + (procedureMode === 'independent' ? 'Independent' : 'Guided') + '\n';
                  report += 'Current layer technique score: ' + procedureTechniqueScore((d.procedureByLayer || {})[activeLayer] || {}) + '/100\n';
                  var reportMetrics = currentProcedure.extensionMetrics || currentProcedure.incisionMetrics;
                  report += 'Current tool control: ' + (reportMetrics ? (reportMetrics.control == null ? reportMetrics.smoothness : reportMetrics.control) + '%' : 'Not scored') + '\n';
                  report += 'Observation field: ' + currentObservationField.label + ' (' + currentObservationField.quality + '/100); limiting factor: ' + currentObservationField.limiter + '\n';
                  report += 'Optical inspection: ' + currentOpticalInspection.label + ' (' + currentOpticalInspection.quality + '/100); target: ' + currentOpticalInspection.targetName + '; focus ' + lensFocusDepth + ' (ideal ' + currentOpticalInspection.idealDepth + '); magnification ' + lensMagnification + 'x; illumination ' + lightIntensity + '% (' + currentIllumination.label + '); glare risk ' + currentIllumination.glareRisk + '%\n';
                  report += 'Living function model: ' + (livingFunctionEnabled ? specimenLivingFunctionProfile().title + '; speed ' + livingFunctionSpeed + '; in-life physiology, not preserved specimen motion' : 'off') + '\n';
                  report += 'Instrument calibration score: ' + (recordedCalibrationValues.length ? recordedCalibrationScore + '%' : 'Not scored') + '\n';
                  report += 'Calibration settings: blade ' + toolCalibration.bladeAngle + '\u00B0; scissors aperture ' + toolCalibration.scissorsAperture + '%; forceps grip ' + toolCalibration.forcepsGrip + '%; pin angle ' + toolCalibration.pinAngle + '\u00B0; probe pressure ' + toolCalibration.probePressure + '%; saline dose ' + toolCalibration.salineDose + '\n';
                  report += 'Technique input: ' + (currentProcedure.inputType || 'Not recorded') + '\n';
                  if (currentProcedure.forcepsDragMetrics) report += 'Direct forceps lift: ' + currentProcedure.forcepsDragMetrics.liftPercent + '% lift; ' + currentProcedure.forcepsDragMetrics.control + '% control; ' + currentProcedure.forcepsDragMetrics.projectedTension + '% projected tension; ' + currentProcedure.forcepsDragMetrics.durationMs + ' ms; input ' + currentProcedure.forcepsDragMetrics.inputType + '\n';
                  if (currentProcedure.pinDragMetrics) report += 'Direct pin placement: ' + currentProcedure.pinDragMetrics.insertionPercent + '% insertion; ' + currentProcedure.pinDragMetrics.alignment + '% alignment; ' + currentProcedure.pinDragMetrics.control + '% control; angle ' + currentProcedure.pinDragMetrics.angle + ' degrees; input ' + currentProcedure.pinDragMetrics.inputType + '\n';
                  if (currentProcedure.probeDragMetrics) report += 'Direct probe palpation: ' + currentProcedure.probeDragMetrics.contactPercent + '% contact; ' + currentProcedure.probeDragMetrics.alignment + '% alignment; ' + currentProcedure.probeDragMetrics.control + '% control; resistance ' + currentProcedure.probeDragMetrics.resistance + '/100; deformation ' + currentProcedure.probeDragMetrics.deformation + '; input ' + currentProcedure.probeDragMetrics.inputType + '\n';
                  if (currentProcedure.dropperDragMetrics) report += 'Direct dropper hydration: ' + currentProcedure.dropperDragMetrics.contactPercent + '% contact; ' + currentProcedure.dropperDragMetrics.flowAlignment + '% flow alignment; ' + currentProcedure.dropperDragMetrics.control + '% control; dose ' + currentProcedure.dropperDragMetrics.dose + '; pooling risk ' + currentProcedure.dropperDragMetrics.poolingRisk + '/100; input ' + currentProcedure.dropperDragMetrics.inputType + '\n';
                  if (currentProcedure.wickDragMetrics) report += 'Direct wick recovery: ' + currentProcedure.wickDragMetrics.recoveryPercent + '% recovery; ' + currentProcedure.wickDragMetrics.edgeAlignment + '% edge alignment; ' + currentProcedure.wickDragMetrics.contactPercent + '% contact; ' + currentProcedure.wickDragMetrics.control + '% control; input ' + currentProcedure.wickDragMetrics.inputType + '\n';
                  report += 'Saved attempts for current layer: ' + attemptEntries.length + '\n';
                  if (techniqueComparison) report += 'Attempt score change: ' + (techniqueComparison.scoreDelta >= 0 ? '+' : '') + techniqueComparison.scoreDelta + ' points\n';
                  report += 'Adaptive coaching: ' + adaptiveCoach.focus + ' — ' + adaptiveCoach.suggestion + '\n';
                  report += 'Caution history: ' + ((currentProcedure.cautionLog || []).map(function (entry) { return entry.message; }).join(' | ') || 'None') + '\n';
                      report += 'Tissue preservation: ' + currentTissueStatus.preservation + '/100; state: ' + currentTissueStatus.label + '; preparation: ' + currentTissueState.variantLabel + '\n';
                      report += 'Tissue metrics: moisture ' + currentTissueState.moisture + '%, clarity ' + currentTissueState.clarity + '%, exposure ' + currentTissueState.exposure + '%, stability ' + currentTissueState.stability + '%, tension ' + currentTissueState.tension + '%, trauma ' + currentTissueState.trauma + '%, risk ' + currentTissueState.risk + '%\n';
                      report += 'Hydration response: ' + specimenHydrationProfile().label + '; saline remaining: ' + currentTissueState.salineDrops + ' drop-equivalent(s); absorbent wicking: ' + ((currentProcedure.actionLog || []).filter(function (entry) { return entry.action === 'wick'; }).length ? 'recorded' : 'not used') + '\n';
                      report += 'Tissue consequence history: ' + (tissueConsequences.map(function (entry) { return procedureActionLabel(entry.action) + ': ' + entry.message; }).join(' | ') || 'No tissue-changing actions recorded') + '\n';
                  report += 'Technique timeline: ' + ((currentProcedure.actionLog || []).map(function (entry) { return entry.label || procedureActionLabel(entry.action); }).join(' > ') || 'Not started') + '\n';
                  report += 'Specimen-specific procedure: ' + procedureProtocol.title + '; recommended view: ' + procedureProtocol.recommendedView + '; access route: ' + procedureProtocol.route + '; protected landmarks: ' + (protectedLandmarkNames.join(', ') || 'underlying anatomy') + '\n';
                  report += 'Dynamic simulation: exposure ' + currentExposure.score + '/100 (' + currentExposure.label + '); decision quality ' + currentDecisionScore + '/100; material resistance ' + currentMaterialInteraction.resistance + '/100; protocol deviations ' + (currentProcedure.protocolDeviations || 0) + '\n';
                  report += 'Active tool readiness: ' + currentToolReadiness.label + ' (' + currentToolReadiness.score + '/100); ' + currentToolReadiness.cue + '; checks: ' + currentToolReadiness.checks.map(function (check) { return check.label + ' ' + (check.ready ? 'ready' : 'needs attention'); }).join(', ') + '\n';
                  report += 'Visual presentation: ' + visualRealism + '; lab light: ' + labLight + '; intensity: ' + lightIntensity + '%; light direction: ' + lightDirection + '; inspection lens: ' + (inspectionLens ? 'on' : 'off') + '; visual tools: ' + (instrumentVisuals ? 'on' : 'off') + '; access corridor: ' + (procedureMode === 'guided' && !currentProcedure.incisionStarted ? 'on' : 'off') + '; trajectory safety: ' + (instrumentVisuals ? 'on' : 'off') + '; instrument pitch: ' + (instrumentVisuals ? 'on' : 'off') + '; instrument lighting: ' + (instrumentVisuals ? 'light-aware elevation feedback on' : 'off') + '; instrument-tissue occlusion: foreground lip and paired depth notches encoded' + '; equivalent action replay: ' + (instrumentVisuals ? 'on' : 'off') + '; technique replay: pressure and alignment encoded' + '; forceps direct manipulation: press-drag-release lift, safe envelope, grip, slip, speed, control, and tension encoded' + '; pin stability preview: angle, depth, spacing, and tension encoded' + '; direct pin manipulation: press-drag-release insertion, angle, depth, spacing, alignment, and control encoded' + '; probe palpation preview: pressure, resistance, depth, and deformation encoded' + '; direct probe manipulation: press-drag-release trace, alignment, contact coverage, resistance, deformation, and control encoded' + '; direct dropper manipulation: press-drag-release hydration, dose, contact coverage, flow alignment, moisture, and pooling control encoded' + '; dropper spread forecast: dose, flow, saturation, and pooling encoded' + '; guided handoff cue: next instrument, anatomical target, and ' + (procedureProtocol.order || []).length + '-step progress encoded' + '; localized technique evidence: incision edge, grip, anchor, probe, and pooling consequences encoded' + '; localized action outcome: success or caution shape and changed tissue metric encoded' + '; tissue-state transitions: opening, retraction, stabilization, hydration, and risk interpolate spatially; Undo reverses opening, flap, pin, hydration, wick, and trace geometry; reduced motion snaps to final state' + '; macro view: ' + (macroInset ? 'on' : 'off') + '; scene detail: ' + (sceneDetail ? 'on' : 'off') + '; depth atlas: ' + (depthAtlasEnabled ? 'on' : 'off') + '; structure inspection footprint: front and back depth edges, exposure line styles, and boundary-attached relationships encoded; morphology contours: tubular, lobed, chambered, layered, and compact patterns encoded; relationship pathways: type-specific line rhythms and marker shapes encoded' + '; anatomical orientation axis: circle marks ' + anatomicalOrientationData().start.toLowerCase() + ', diamond marks ' + anatomicalOrientationData().end.toLowerCase() + ', view ' + anatomicalOrientationData().view + ', target ' + anatomicalOrientationData().targetView + '; tissue relief: ' + (tissueReliefEnabled ? 'on' : 'off') + '; relationship motion: ' + (relationshipMotion ? 'on' : 'off') + '; focus isolation: ' + (focusMode ? 'on' : 'off') + '; spatial navigation transitions: orientation sweep, circle start, diamond target, and layer depth rings encoded; reduced motion switches instantly' + '; depth motion: ' + (parallaxDepth ? 'on' : 'off') + '; evidence frames: ' + visualEvidence.length + '; split comparison: ' + (splitComparison && referenceEvidence ? 'on' : 'off') + '; tactile feedback: ' + (d.tactileFeedback !== false ? 'on' : 'off') + '\n';
                  report += 'Specimen material model: ' + activeMaterialProfile.label + '; contact response: ' + activeMaterialProfile.response + '\n';
                  report += 'Surface microdetail: ' + ((visualRealism === 'accessible' || !sceneDetail) ? 'suppressed for visual clarity' : activeMaterialProfile.pattern + '; directional light ' + lightDirection + '; condition ' + specimenCondition + '; moisture ' + currentTissueState.moisture + '%; roughness and sheen respond to tissue state') + '\n';
                  report += 'Specimen variation: ' + variationSeed + '\n';
                  report += 'Anatomical view: ' + anatomicalView + '; cross-section: ' + (crossSectionMode ? 'on' : 'off') + '; condition: ' + specimenCondition + '\n';
                  report += 'Scenario: ' + scenarioDefinition.label + ' (' + scenarioDefinition.difficulty + '); status: ' + (scenarioStatus.complete ? 'complete' : (scenarioStatus.expired ? 'expired' : 'in progress')) + ' (' + scenarioStatus.progress + '%); ' + scenarioStatus.detail + '; elapsed ' + (d.scenarioStartedAt ? Math.max(0, Math.round(((Number(d.scenarioCompletedAt) || Date.now()) - Number(d.scenarioStartedAt)) / 1000)) : 0) + ' seconds\n';
                  var reportInquiry = normalizeDissectionInquiry(d.dissInquiry);
                  var reportInquiryMetrics = dissectionInquiryMetrics(reportInquiry);
                  report += 'Inquiry model (pedagogical heuristic): ' + dissectionInquiryStateLabel(reportInquiryMetrics.state) + '; specimen size ' + reportInquiry.specimenSize + ' cm; depth ' + reportInquiry.layerDepth + '/5; care ' + reportInquiry.careLevel + '/10; time pressure ' + reportInquiry.timePress + '/10; damage index ' + reportInquiryMetrics.damage.toFixed(1) + '; insight score ' + reportInquiryMetrics.insight.toFixed(1) + '; logged approaches ' + reportInquiry.log.length + '\n';
                  if (reportInquiry.hypothesis.trim()) report += 'Inquiry hypothesis: ' + reportInquiry.hypothesis.replace(/\s+/g, ' ').trim() + '\n';
                  if (reportInquiry.explanation.trim()) report += 'Inquiry explanation: ' + reportInquiry.explanation.replace(/\s+/g, ' ').trim() + '\n';
                  report += 'Instructor thresholds: score ' + instructorTarget + ', max cautions ' + instructorMaxCautions + ', required structures ' + instructorRequiredStructures + '\n';
                  report += 'Curated relationship overlay: ' + (relationshipMode ? 'on' : 'off') + '; directly visible structures: ' + visibleOrgansInView.length + '/' + organs.length + '; rendering quality: ' + renderQuality + '\n';
                  if (d.selectedOrgan && selectedRelationships.length) report += 'Selected structure relationships: ' + selectedRelationships.map(function (item) { return item.type + ': ' + item.relation + ' ' + item.organ.name; }).join('; ') + '\n';
                  spec.layers.forEach(function (layer) {
                    var examined = (spec.organs[layer.id] || []).filter(function (o) { return !!(d.exploredOrgans || {})[specimen + '|' + o.id]; });
                    if (!examined.length) return;
                    report += '\n' + layer.name + '\n';
                    examined.forEach(function (o) {
                      var noteKey = specimen + '|' + o.id;
                      report += '\u2022 ' + o.name + ': ' + o.fn + '\n';
                      var confidenceRating = Number((d.organConfidence || {})[noteKey]) || 0;
                      if (confidenceRating) {
                        report += '  Confidence self-rating: ' + dissectionConfidenceLabel(confidenceRating) + ' (' + confidenceRating + ' of 3)\n';
                      }
                      if ((d.organNotes || {})[noteKey]) report += '  Evidence note: ' + (d.organNotes || {})[noteKey] + '\n';
                    });
                  });
                  copyDissectionText(report, 'Lab report copied to the clipboard.', 'The lab report could not be copied in this view.');
                },
                "aria-label": "Copy lab report to clipboard",
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-emerald-700 border border-emerald-200"
              }, '\uD83D\uDCCB Lab Report'),
              React.createElement("button", { id: "diss-reset-specimen", "aria-label": "Reset progress for this specimen", "aria-expanded": !!d.resetConfirmPending, "aria-controls": "diss-reset-confirmation", onClick: requestSpecimenReset,
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold bg-white text-red-700 border border-red-300"
              }, '\u21BA Reset specimen'),
              d.resetConfirmPending && !activeDissectionSaveIssue && React.createElement("div", { id: "diss-reset-confirmation", className: "diss-reset-confirm", role: "group", "aria-labelledby": "diss-reset-confirm-title" },
                React.createElement("p", { id: "diss-reset-confirm-title", role: "alert", "aria-live": "assertive", "aria-atomic": "true" }, 'Reset ' + spec.name + '? This permanently removes viewed structures, verified identifications, notes, inquiry records, evidence frames, quiz progress, and technique attempts. Accessibility preferences are preserved.'),
                React.createElement("button", { id: "diss-reset-confirm", type: "button", "data-confirm": "true", onClick: confirmSpecimenReset }, 'Confirm reset'),
                React.createElement("button", { id: "diss-reset-cancel", type: "button", onClick: cancelSpecimenReset }, 'Cancel')
              )
            ),

            // Study group expanded
            d.toolbarStudyOpen && React.createElement("div", { id: "diss-study-tools", tabIndex: -1, className: "diss-tool-panel flex flex-wrap bg-amber-50 rounded-xl border border-amber-200 animate-[fadeIn_0.2s_ease-out]", role: "region", "aria-label": "Study tools" },

              React.createElement("button", { "aria-label": "Flashcard", "aria-pressed": !!d.flashcardMode,
                onClick: function () { if (d.flashcardMode) upd('flashcardMode', false); else { closeTimedPractical(); upd('guidedMode', false); upd('quizMode', false); upd('compareMode', false); upd('flashcardMode', true); upd('flashcardIdx', 0); upd('flashcardFlipped', false); } },
                title: 'Flashcard' + ' — Review organs with flip cards',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.flashcardMode ? 'bg-violet-600 text-white' : 'transition-colors bg-white text-amber-700 border border-amber-200 hover:bg-amber-100 active:scale-[0.97]')
              }, '\uD83C\uDCCF ' + 'Flashcard'),

              React.createElement("button", { "aria-label": "Compare", "aria-pressed": !!d.compareMode,
                onClick: function () { if (d.compareMode) upd('compareMode', false); else { closeTimedPractical(); upd('guidedMode', false); upd('quizMode', false); upd('flashcardMode', false); upd('compareMode', true); } },
                title: 'Compare' + ' — Compare organs across specimens',
                className: "px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.compareMode ? 'bg-cyan-700 text-white' : 'transition-colors bg-white text-amber-700 border border-amber-200 hover:bg-amber-100 active:scale-[0.97]')
              }, '\uD83D\uDD0D ' + 'Compare'),

              React.createElement("button", { "aria-label": d.practicalMode ? "End timed practical" : "Start 2-minute timed practical", "aria-describedby": d.practicalMode ? "diss-practical-time-status" : undefined, "aria-pressed": !!d.practicalMode,
                onClick: function () {
                    if (d._incisionAnim && d._incisionAnim.active) {
                      setProcedureFeedback('Finish the current layer transition before starting the timed practical.', 'caution');
                      return;
                    }
                    clearAssessmentTeachingTimers();
                  if (!d.practicalMode) {
                    var practicalIds = visibleQuizPool.map(function (organ) { return organ.id; });
                    if (practicalIds.length < 2) {
                      setProcedureFeedback('Timed practical unavailable: at least two structures must be visible. Improve the field or change view first.', 'caution');
                      return;
                    }
                    var previousLabelMode = d.labelMode || 'show';
                    var practicalEndsAt = Date.now() + 120000;
                    stopCanvasPracticalCountdown(canvasRefNode);
                    try { window.__alloDissectionPracticalScore = 0; } catch (e) {}
                    updMany({ _prePracticalLabelMode: previousLabelMode, guidedMode: false, flashcardMode: false, compareMode: false, traceNervous: false, traceCirculation: false, traceDigestion: false, traceRespiration: false, traceExcretory: false, showEndocrine: false, livingFunctionEnabled: false, livingFunctionPaused: false, practicalMode: true, labelMode: 'hidden', quizMode: true, quizComplete: false, quizReviewMode: false, quizAnswerMode: 'hotspot', quizSeed: Date.now(), quizIdx: 0, quizScore: 0, quizTotal: 0, quizFeedback: null, quizExplanation: null, quizRetry: null, practicalTimer: 120, practicalEndsAt: practicalEndsAt, practicalTargetIds: practicalIds, selectedOrgan: null, hoveredOrgan: null, toolbarViewOpen: false, toolbarToolsOpen: false, rulerMode: false, annotateMode: false, rulerStart: null, rulerEnd: null, compareTechniqueAttempts: false, compareReplayPlaying: false, splitComparison: false, beforeTechniqueView: false });
                    updMany({ inspectionLens: false, lensPinned: false, lensPinnedPoint: null, lensPinnedOrganId: null, macroInset: false, _procedureDemo: null, _procedureReplay: null });
                  } else closeTimedPractical();
                },
                title: 'Practical — timed identification with a specimen hotspot and accessible answer choices',
                className: "diss-advanced-only px-2.5 py-1 rounded-lg text-[11px] font-semibold transition-all " + (d.practicalMode ? 'bg-red-600 text-white' + (!reducedMotionEnabled ? ' animate-pulse' : '') : 'transition-colors bg-white text-orange-700 border border-orange-200 hover:bg-orange-100 active:scale-[0.97]')
              }, d.practicalMode ? '\u23F0 ' + formatScenarioTime(d.practicalTimer || 0) : '\u23F1 Practical'),
              d.practicalMode && React.createElement("output", { id: "diss-practical-time-status", className: "sr-only", role: "timer", "aria-live": "off", "aria-atomic": "true" }, 'Timed practical: ' + formatAccessibleDuration(d.practicalTimer || 0))

            ),

            // Main: Canvas + sidebar


            React.createElement("div", { id: "diss-workspace", className: "diss-workspace", "data-dissection-workspace": true, role: "tabpanel", "aria-labelledby": "diss-specimen-tab-" + specimen },

              // Canvas + peel button

              React.createElement("div", { className: "diss-primary-column" },

                React.createElement("section", { className: "diss-stage", "data-dissection-stage": true, "aria-labelledby": "diss-stage-title" },
                  React.createElement("div", { className: "diss-stage__header" },
                    React.createElement("div", null,
                      React.createElement("div", { className: "diss-stage__eyebrow" }, 'Interactive specimen'),
                      React.createElement("h3", { id: "diss-stage-title", className: "diss-stage__title" }, spec.icon + ' ' + spec.name + ' · ' + currentLayerDef.name)
                    ),
                    React.createElement("div", { className: "diss-stage__status" }, sel ? ('Selected: ' + sel.name) : (currentObservationField.label + ' \u00B7 ' + currentObservationField.quality + '%'))
                  ),
                  React.createElement("div", {
                    className: "diss-stage__handoff",
                    "data-tone": stageHandoffTone,
                    role: "group",
                    "aria-label": "Current action: " + stageHandoffLabel + ". " + stageHandoffDetail + ". Workflow step " + nextActionModel.step + " of " + workflowSteps.length
                  },
                    React.createElement("span", { className: "diss-stage__handoff-glyph", "aria-hidden": "true" }, stageHandoffGlyph),
                    React.createElement("strong", null, 'Next \u00B7 ' + stageHandoffLabel),
                    React.createElement("span", { className: "diss-stage__handoff-tool" }, stageHandoffDetail),
                    React.createElement("span", { className: "diss-stage__handoff-progress" }, 'Workflow ' + nextActionModel.step + '/' + workflowSteps.length)
                  ),

                  React.createElement("details", { className: "diss-field-monitor" },
                    React.createElement("summary", { className: "diss-field-monitor__summary" },
                      React.createElement("strong", null, 'Field monitor'),
                      React.createElement("span", null, stageHandoffUsesTool ? fieldReadinessLabel : currentObservationField.label),
                      React.createElement("small", null, stageHandoffUsesTool ? fieldReadinessCue : 'Exposure and tissue-response details')
                    ),
                    React.createElement("div", { className: "diss-field-monitor__body" },
                  React.createElement("section", {
                    className: "diss-stage__telemetry",
                    "data-tone": stageTelemetryTone,
                    role: "group",
                    "aria-label": "Live specimen response monitor"
                  },
                    React.createElement("div", { className: "diss-stage__telemetry-head" },
                      React.createElement("div", null,
                        React.createElement("span", { className: "diss-stage__telemetry-kicker" }, 'Live specimen monitor'),
                        React.createElement("strong", null, currentTissueStatus.label + ' · ' + currentTissueStatus.preservation + '% preservation')
                      ),
                      React.createElement("span", { className: "diss-stage__telemetry-phase" }, 'Phase · ' + stageHandoffLabel)
                    ),
                    React.createElement("div", { className: "diss-stage__telemetry-grid" },
                      stageTelemetryMetrics.map(function (metric) {
                        return React.createElement("div", { className: "diss-stage__telemetry-metric", "data-risk": metric.risk ? "true" : "false", key: metric.id },
                          React.createElement("div", { className: "diss-stage__telemetry-metric-head" },
                            React.createElement("span", null, metric.label),
                            React.createElement("b", null, metric.value + '%')
                          ),
                          React.createElement("progress", { max: 100, value: metric.value, "aria-label": metric.label + ' ' + metric.value + ' percent' }),
                          React.createElement("span", { className: "diss-stage__telemetry-metric-note" }, metric.note)
                        );
                      })
                    ),
                    React.createElement("div", { className: "diss-stage__telemetry-foot" },
                      React.createElement("span", { className: "diss-stage__telemetry-action" }, latestTechniqueAction
                        ? 'Last response · ' + (latestTechniqueAction.outcome || 'RECORDED') + (latestTechniqueAction.outcomeDetail ? ' · ' + latestTechniqueAction.outcomeDetail : '')
                        : 'Awaiting first recorded technique response'),
                      React.createElement("span", { className: "diss-stage__telemetry-priority" }, currentTissueStatus.priority),
                      React.createElement("button", {
                        type: "button",
                        className: "diss-stage__telemetry-prepare",
                        disabled: currentLayerDone || procedureNext.action === 'complete',
                        onClick: prepareNextProcedureStep,
                        "aria-label": procedureNext.action === 'complete' ? 'Current layer protocol complete' : 'Prepare next technique step: ' + procedureNext.label
                      }, procedureNext.action === 'complete' ? 'Protocol complete' : 'Prepare next')
                    )
                  ),
                  selectedTechniqueAction && React.createElement("details", { className: "diss-stage__telemetry-detail", open: !!(selectedTechniqueAction && !selectedTechniqueActionIsLatest), "aria-label": selectedTechniqueActionIsLatest ? "Inspect the latest tissue response" : "Inspect the selected tissue response" },
                    React.createElement("summary", null, selectedTechniqueActionIsLatest ? 'Inspect last response · why did the field change?' : 'Inspect selected response · why did the field change?'),
                    React.createElement("div", { className: "diss-stage__telemetry-detail-body" },
                      React.createElement("p", null, (selectedTechniqueAction.label || procedureActionLabel(selectedTechniqueAction.action)) + ' produced ' + (selectedTechniqueAction.outcome || 'a recorded response') + (selectedTechniqueAction.outcomeDetail ? '. ' + selectedTechniqueAction.outcomeDetail + '.' : '.')),
                      latestActionDeltaMetrics.length
                        ? React.createElement("div", { className: "diss-stage__telemetry-deltas", role: "group", "aria-label": selectedTechniqueActionIsLatest ? "Latest tissue response metric changes" : "Selected tissue response metric changes" },
                          latestActionDeltaMetrics.map(function (metric) {
                            return React.createElement("div", { className: "diss-stage__telemetry-delta", "data-direction": metric.direction, key: metric.label },
                              React.createElement("span", null, metric.label),
                              React.createElement("strong", null, metric.before + '% → ' + metric.after + '%'),
                              React.createElement("small", null, (metric.delta > 0 ? '+' : '') + metric.delta + '% ' + (metric.direction === 'improve' ? 'improved' : (metric.direction === 'caution' ? 'needs review' : 'unchanged')))
                            );
                          })
                        )
                        : React.createElement("p", null, 'This older action record does not include before-and-after tissue metrics. New actions show the exact state change here.'),
                      React.createElement("button", { type: "button", className: "diss-stage__telemetry-replay", onClick: showProcedureLastResponse, "aria-label": selectedTechniqueActionIsLatest ? "Replay the latest technique response" : "Replay the selected technique response" }, (selectedTechniqueActionIsLatest ? 'Replay last response' : 'Replay selected response'))
                    )
                  ),
                  stageHandoffUsesTool && React.createElement("section", { className: "diss-field-readiness", "data-tone": fieldReadinessTone, role: "group", "aria-label": "Pre-contact field readiness" },
                    React.createElement("div", { className: "diss-field-readiness__header" },
                      React.createElement("strong", null, 'Pre-contact check · ' + fieldReadinessLabel),
                      React.createElement("span", { className: "diss-field-readiness__score" }, fieldReadinessScore + '/100')
                    ),
                    React.createElement("p", { className: "diss-field-readiness__cue" }, fieldReadinessCue),
                    React.createElement("div", { className: "diss-field-readiness__checks" },
                      fieldReadinessChecks.map(function (check) {
                        return React.createElement("div", { className: "diss-field-readiness__check", "data-ready": check.ready ? "true" : "false", key: check.id },
                          React.createElement("div", { className: "diss-field-readiness__check-head" },
                            React.createElement("b", { "aria-hidden": "true" }, check.ready ? '✓' : '!'),
                            React.createElement("span", null, check.label)
                          ),
                          React.createElement("span", { className: "diss-field-readiness__check-detail" }, check.detail)
                        );
                      })
                    ),
                    React.createElement("div", { className: "diss-field-readiness__actions" },
                      React.createElement("button", { type: "button", className: "diss-field-readiness__action", onClick: runFieldReadinessCheck, "aria-label": "Run pre-contact field check" }, 'Run field check'),
                      React.createElement("button", { type: "button", className: "diss-field-readiness__action", onClick: resolveFieldReadinessIssue, disabled: !firstFieldReadinessIssue, "aria-label": firstFieldReadinessIssue ? 'Fix first readiness issue: ' + firstFieldReadinessIssue.label : 'All readiness checks pass' }, fieldReadinessActionLabel)
                    )
                  ),
                    )
                  ),
                  React.createElement("div", { className: "diss-pointer-guide", "data-diss-pointer-guide": true, "data-tone": pointerGuideData.tone, "data-mode": pointerGuideData.mode, role: "group", "aria-label": "Current pointer mode: " + pointerGuideData.label + ". " + pointerGuideData.instruction },
                    React.createElement("span", { className: "diss-pointer-guide__cursor", "data-pointer-guide-icon": true, "aria-hidden": "true" }, pointerGuideData.icon),
                    React.createElement("span", { className: "diss-pointer-guide__body" },
                      React.createElement("strong", { "data-pointer-guide-label": true }, 'Pointer mode ? ' + pointerGuideData.label),
                      React.createElement("span", { "data-pointer-guide-instruction": true }, pointerGuideData.instruction)
                    )
                  ),
                  React.createElement("div", { className: "diss-canvas-layout", "data-split": !d.quizMode && splitComparison && referenceEvidence ? "true" : "false" },
                    React.createElement("div", { className: "diss-canvas-frame", "data-diss-fullscreen-stage": true },
                      React.createElement("div", { id: "diss-fullscreen-dock", className: "diss-fullscreen-dock", role: "toolbar", "aria-label": "Fullscreen specimen view and tools" },
                      React.createElement("button", { type: "button", className: "diss-fullscreen-dock__exit", "data-diss-fullscreen-exit": true, "aria-label": "Exit fullscreen specimen mode", "aria-keyshortcuts": "Escape", title: "Exit fullscreen (Escape)", onClick: function (event) { exitDissectionFullscreen(event.currentTarget); } }, 'Exit fullscreen'),
                        React.createElement("button", { type: "button", disabled: !!d.quizMode, onClick: function () { var views = ['dorsal', 'ventral', 'lateral', 'internal']; changeAnatomicalView(views[(views.indexOf(anatomicalView) + 1) % views.length], 'fullscreen dock'); }, "aria-label": d.quizMode ? "Fullscreen anatomical view locked during assessment" : "Fullscreen anatomical view: cycle dorsal, ventral, lateral, and internal" }, 'View: ' + anatomicalView),
                        React.createElement("button", { type: "button", className: "diss-advanced-only", onClick: function () { upd('labLight', labLight === 'neutral' ? 'warm' : (labLight === 'warm' ? 'cool' : 'neutral')); }, "aria-label": "Fullscreen illumination color: cycle neutral, warm, and cool" }, 'Light: ' + labLight),
                        React.createElement("label", null,
                          React.createElement("span", null, 'Illumination ' + lightIntensity + '%'),
                          React.createElement("input", { type: "range", min: 20, max: 100, step: 2, value: lightIntensity, disabled: !!d.practicalMode, onChange: function (event) { upd('lightIntensity', Number(event.target.value)); }, "aria-label": d.practicalMode ? "Fullscreen illumination locked during timed practical" : "Fullscreen illumination intensity", "aria-valuetext": lightIntensity + ' percent, ' + currentIllumination.label })
                        ),
                        React.createElement("button", { type: "button", className: "diss-advanced-only", onClick: function () { var directions = ['overhead', 'left', 'right', 'raking']; upd('lightDirection', directions[(directions.indexOf(lightDirection) + 1) % directions.length]); }, "aria-label": "Fullscreen light direction: cycle overhead, left, right, and raking" }, 'Angle: ' + lightDirection),
                        React.createElement("button", { type: "button", className: "diss-advanced-only", "aria-pressed": !!d.beforeTechniqueView, onClick: function () { upd('beforeTechniqueView', !d.beforeTechniqueView); } }, d.beforeTechniqueView ? 'Before view' : 'After view'),
                        React.createElement("button", { type: "button", className: "diss-advanced-only", onClick: function () { var speed = d.animSpeed === 'fast' ? 'normal' : (d.animSpeed === 'normal' ? 'slow' : 'fast'); upd('animSpeed', speed); }, "aria-label": "Fullscreen animation speed: cycle normal, slow, and fast" }, 'Speed: ' + (d.animSpeed === 'slow' ? 'slow' : d.animSpeed === 'fast' ? 'fast' : 'normal')),
                        !d.quizMode && React.createElement("label", null,
                          React.createElement("span", null, 'Tool'),
                          React.createElement("select", { value: activeInstrument, onChange: function (event) { selectProcedureInstrument(event.target.value, 'fullscreen tool dock'); }, "aria-label": "Fullscreen active instrument" },
                            PROCEDURE_INSTRUMENTS.map(function (tool) {
                              var restrictedTool = scenarioDefinition.id === 'restricted-tray' && (tool.id === 'dropper' || tool.id === 'wick');
                              return React.createElement("option", { key: tool.id, value: tool.id, disabled: restrictedTool }, tool.label + (restrictedTool ? ' \u2014 unavailable' : ''));
                            })
                          )
                        ),
                        React.createElement("span", {
                          className: "diss-fullscreen-dock__handoff",
                          "data-tone": stageHandoffTone,
                          "aria-label": "Fullscreen next action: " + stageHandoffLabel + ". " + stageHandoffDetail
                        }, 'Next: ' + stageHandoffLabel + ' \u00B7 ' + stageHandoffDetail)
                      ),
                      React.createElement("span", { id: "diss-canvas-label", className: "diss-sr-only" },
                        d.quizMode
                          ? spec.name + ' practice assessment specimen'
                          : spec.name + ' interactive specimen \u00B7 ' + currentLayerDef.name + ' layer, ' + anatomicalView + ' view'
                      ),
                      React.createElement("canvas", {

                      key: specimen,
                      role: "application",
                      // This interactive canvas has a dynamic aria-label later in the same props object.
                      id: "diss-canvas",
                      ref: canvasRef, onClick: canvasClick, onWheel: canvasWheelZoom,
                      onFocus: function (e) {
                        var canvas = e.currentTarget; canvas._keyboardFocus = true;
                        if (!canvas._keyboardFocusAnnounced) {
                          canvas._keyboardFocusAnnounced = true;
                          var focusMessage = d.quizMode
                            ? (effectiveQuizAnswerMode === 'hotspot'
                              ? 'Assessment canvas ready. Arrow keys preview visible structures; Enter submits the selected answer; plus and minus zoom.'
                              : 'This assessment uses multiple choice. Press Tab to reach the answer panel; Arrow keys only preview the specimen.')
                            : 'Specimen canvas ready. Arrow keys preview visible structures; Enter inspects; plus and minus zoom.';
                          setProcedureFeedback(focusMessage, 'working');
                        }
                      },
                      onBlur: function (e) { e.currentTarget._keyboardFocus = false; e.currentTarget._keyboardPreviewOrganId = null; },
                      'data-diss-canvas': true,
                      'data-cursor-mode': pointerGuideData.mode,
                      'data-cursor-tone': pointerGuideData.tone,
                      'aria-roledescription': 'interactive specimen canvas',
                      'aria-labelledby': 'diss-canvas-label',
                      'aria-keyshortcuts': 'ArrowUp ArrowDown ArrowLeft ArrowRight Shift+ArrowUp Shift+ArrowDown Shift+ArrowLeft Shift+ArrowRight Home End Enter Space 0 R V' + (advancedWorkspace ? ' X M P F' : '') + ' 1 2 3 4 5 6 7',
                      tabIndex: 0,
                      'aria-label': d.quizMode
                        ? spec.name + ' practice assessment, ' + currentLayerDef.name + ' layer. ' + (effectiveQuizAnswerMode === 'hotspot' ? 'Select a structure on the specimen, or switch to the keyboard-accessible multiple-choice answers.' : 'Answer with the keyboard-accessible choices in the assessment panel.')
                        : spec.name + ' virtual dissection. Specimen-specific procedure: ' + procedureProtocol.title + '; recommended view ' + procedureProtocol.recommendedView + '; access route ' + procedureProtocol.route + '; protected landmarks ' + (protectedLandmarkNames.join(', ') || 'underlying anatomy') + '. Current view: ' + anatomicalView + ', ' + specimenCondition + ' condition, ' + currentLayerDef.name + ' layer. ' + (livingFunctionEnabled ? 'Living function model active: ' + specimenLivingFunctionProfile().title + '; this represents in-life physiology, not preserved specimen motion. ' : '') + 'Progressive exposure: ' + currentExposure.label + ', ' + currentExposure.score + ' percent; decision quality ' + currentDecisionScore + ' percent. Observation field: ' + currentObservationField.label + ', ' + currentObservationField.quality + ' percent; limiting factor: ' + currentObservationField.limiter + '. Surface model: ' + activeMaterialProfile.label + '. Surface microtexture ' + ((visualRealism === 'accessible' || !sceneDetail) ? 'is suppressed for visual clarity' : 'uses ' + activeMaterialProfile.pattern + ' detail aligned to ' + lightDirection + ' illumination, with sheen adjusted for ' + currentTissueState.moisture + ' percent moisture and ' + specimenCondition + ' condition') + (currentTissueState.salineDrops ? '. Hydration model: ' + specimenHydrationProfile().label + ', ' + currentTissueState.salineDrops + ' saline ' + (currentTissueState.salineDrops === 1 ? 'drop' : 'drops') + ' applied' : '') + '. Tool contact response: ' + activeMaterialProfile.response + '. Light direction: ' + lightDirection + '. Illumination ' + lightIntensity + ' percent: ' + currentIllumination.label + '; glare risk ' + currentIllumination.glareRisk + ' percent. Focus isolation ' + (focusMode ? 'on' : 'off') + (opticalPlaneMapActive ? '. Focus-plane map active at ' + lensFocusDepth + ' depth: ' + opticalPlaneCounts.resolved + ' resolved, ' + opticalPlaneCounts.soft + ' soft, and ' + opticalPlaneCounts.unresolved + ' unresolved structures' : '') + '. Tissue relief ' + (tissueReliefEnabled ? 'on and responsive to the inspection light' : 'off') + '. Depth atlas ' + (depthAtlasEnabled ? 'on, with circle surface markers, diamond mid-depth markers, and double-ring deep markers' : 'off') + '. Selected and hovered structures use a labeled depth footprint with front and back edges, visibility-specific line styles, and boundary-attached relationship paths. Relationship types use distinct line patterns and moving marker shapes, with fixed markers when motion is reduced. Tubular, lobed, chambered, layered, and compact structure classes use distinct contours, internal patterns, and text labels' + (sceneDetail ? '. Anatomical orientation axis uses a circle for ' + anatomicalOrientationData().start.toLowerCase() + ' and a diamond for ' + anatomicalOrientationData().end.toLowerCase() + '; current and target views are stated in text' : '') + '. View and layer navigation uses a labeled orientation sweep with circle start and diamond target markers, plus concentric depth rings for deeper or superficial movement; reduced motion switches instantly and uses text announcements. Depth motion ' + (parallaxDepth ? 'on' : 'off') + '. ' + visibleOrgansInView.length + ' directly visible structures. Active instrument: ' + activeInstrument + '. Action readiness: ' + currentToolReadiness.label + ', ' + currentToolReadiness.score + ' percent. ' + currentToolReadiness.cue + (instrumentVisuals ? '. Directional contact footprint feedback is active. Engaged instrument tips use foreground tissue lips and paired depth notches to show partial insertion and opening-edge occlusion' : '') + ((procedureMode === 'guided' && !currentProcedure.incisionStarted) ? '. Guided anatomical access corridor active; circle marks start and diamond marks finish' : '') + ((instrumentVisuals && (activeInstrument === 'scalpel' || activeInstrument === 'scissors')) ? '. Predictive cutting trajectory safety feedback is active' : '') + (instrumentVisuals ? '. Directional target guidance uses a dashed arrow, labeled endpoint, and shape-coded reticle when an instrument is spatially misaligned' : '') + '. Localized action outcomes use a one-shot check or warning symbol with the changed tissue metric. Tissue opening, retraction, stabilization, hydration, and risk metrics transition spatially after each action; Undo reverses openings, retraction, pin placement, hydration, wick evidence, and probe traces; reduced motion snaps to the final state' + (instrumentVisuals ? '. Three-dimensional instrument pitch feedback is active. Light-aware instrument elevation feedback is active. Equivalent keyboard and button actions include an instrument approach, contact, and release replay. Recorded attempt replay progressively traces pressure with line width and states path alignment in text and shape. Direct forceps press-drag-release manipulation shows live lift direction, safe range, calibrated grip, speed, control, slip risk, and excess tension with text and geometry; the technique action button remains equivalent. Live pin stability preview shows endpoint spacing, calibrated angle, insertion depth, and flap tension with text and geometry. Direct pin press-drag-release placement adds shaft alignment, insertion travel, safe depth, and control feedback. Live probe palpation preview shows calibrated pressure, material resistance, anatomical depth, and tissue deformation with text and shape. Direct probe press-drag-release tracing adds contact coverage, alignment, resistance, deformation, and control feedback. Direct dropper press-drag-release hydration adds contact coverage, flow alignment, moisture, dose, and pooling feedback. Direct wick press-drag-release recovery adds pool-edge alignment, contact coverage, recovery distance, and control feedback. Live dropper spread forecast shows dose count, organism-specific flow direction, current saturation, and pooling risk with text and geometry. Absorbent-wick feedback shows pool-edge targeting, fluid recovery, and a localized balanced-film marker with text and cross-hatched geometry. Guided procedure handoff cue connects the next required instrument to its anatomical target and ' + (procedureProtocol.order || []).length + '-step progress rail with text, shape, and line style. Persistent localized technique evidence maps edge stress, grip compression, anchor tension, probe pressure, and saline pooling to actual contact locations with text and distinct geometry; direct wick recovery maps pool-edge contact, recovery distance, and control' : '') + '. ' + ((activeInstrument === 'scalpel' || activeInstrument === 'scissors') ? 'Drag on the canvas to practice a stroke, or use the equivalent technique action button.' : (activeInstrument === 'forceps' ? 'Press the opened tissue edge, drag away while monitoring lift and tension, then release inside the solid safe envelope; the equivalent technique action button remains available.' : (activeInstrument === 'pin' ? 'Press an indicated endpoint, drag inward along the shaft while monitoring insertion and alignment, then release when stable; the equivalent technique action button remains available.' : (activeInstrument === 'probe' ? 'Press a visible structure, trace a short path while monitoring pressure, resistance, and contact, then release when controlled; the equivalent technique action button remains available.' : (activeInstrument === 'dropper' ? 'Press the specimen surface, drag a short flow-aligned path while monitoring dose, contact, moisture, and pooling, then release when controlled; the equivalent technique action button remains available.' : (activeInstrument === 'wick' ? 'Press the saline pool edge, drag outward across a short recovery path while monitoring contact, alignment, and control, then release when controlled; the equivalent technique action button remains available.' : 'Select on the canvas or use the equivalent technique action button.')))))) + ' Occluded structures remain available in the structure directory.',
                      'aria-describedby': 'diss-canvas-status diss-canvas-equivalent',

                  onPointerMove: function (e) {

                    var canvas = e.currentTarget;
                    if (canvas._forcepsDrag && canvas._forcepsDrag.active) { appendForcepsDrag(e); return; }
                    if (canvas._pinDrag && canvas._pinDrag.active) { appendPinDrag(e); return; }
                    if (canvas._probeDrag && canvas._probeDrag.active) { appendProbeDrag(e); return; }
                    if (canvas._dropperDrag && canvas._dropperDrag.active) { appendDropperDrag(e); return; }
                    if (canvas._wickDrag && canvas._wickDrag.active) { appendWickDrag(e); return; }
                    if (canvas._toolDrawing) { appendProcedureStroke(e); return; }

                    // Pan via drag when zoomed > 1

                    if (canvas._isPanning) {
                      if (canvas._panPointerId != null && e.pointerId != null && canvas._panPointerId !== e.pointerId) return;

                      var dx = e.clientX - canvas._panStartX;

                      var dy = e.clientY - canvas._panStartY;

                      if (Math.abs(dx) > 3 || Math.abs(dy) > 3) canvas._wasPanning = true;

                      var livePanZoom = Math.max(1, Number(canvas._zoom) || Number(d.canvasZoom) || 1);
                      var livePanWidth = canvas._logicalW || canvas.width || 500, livePanHeight = canvas._logicalH || canvas.height || 600;
                      var newPanX = clampCanvasPan((canvas._panOrigX || 0) + dx, livePanZoom, livePanWidth);
                      var newPanY = clampCanvasPan((canvas._panOrigY || 0) + dy, livePanZoom, livePanHeight);
                      canvas._panX = newPanX; canvas._panY = newPanY;
                      upd('canvasPanX', newPanX); upd('canvasPanY', newPanY);
                    }
                    canvasHover(e);
                  },
                  onPointerDown: function (e) {
                    var canvas = e.currentTarget;
                    var techniquePointerActive = !guidedMode && !revealedLayers[activeLayer] && nextProcedureInfo().action !== 'complete';
                    if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniquePointerActive && activeInstrument === 'pin') { beginPinDrag(e); return; }
                    if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniquePointerActive && activeInstrument === 'forceps') { beginForcepsDrag(e); return; }
                    if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniquePointerActive && activeInstrument === 'probe') {
                      if (beginProbeDrag(e)) return;
                      if ((d.canvasZoom || 1) <= 1.01) {
                        if (procedureToolReadinessData('probe', currentProcedure).safeToAct) setProcedureFeedback('Move the probe tip onto a visible structure before tracing.', 'caution');
                        return;
                      }
                      canvas._suppressToolClick = false;
                    }
                    if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniquePointerActive && activeInstrument === 'dropper') { beginDropperDrag(e); return; }
                    if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniquePointerActive && activeInstrument === 'wick') { beginWickDrag(e); return; }
                    if (!d.quizMode && !d.annotateMode && !d.rulerMode && techniquePointerActive && (activeInstrument === 'scalpel' || activeInstrument === 'scissors')) {
                      beginProcedureStroke(e); return;
                    }
                    if ((d.canvasZoom || 1) > 1.01 && activeInstrument === 'probe' && !d.annotateMode && !d.rulerMode) {
                      if (!canClaimDirectGesture(canvas, e)) return;
                      canvas._panPointerId = e.pointerId;
                      canvas._isPanning = true;
                      canvas._panStartX = e.clientX;
                      canvas._panStartY = e.clientY;
                      canvas._cameraTransition = null;
                      canvas._panOrigX = isFinite(Number(canvas._interactionPanX)) ? Number(canvas._interactionPanX) : (d.canvasPanX || 0);
                      canvas._panOrigY = isFinite(Number(canvas._interactionPanY)) ? Number(canvas._interactionPanY) : (d.canvasPanY || 0);
                      canvas._wasPanning = false;
                      if (canvas.setPointerCapture && e.pointerId != null) canvas.setPointerCapture(e.pointerId);
                      canvas.style.cursor = 'grabbing';
                      e.preventDefault();
                    }
                  },

                  onPointerUp: function (e) {

                    var canvas = e.currentTarget;
                    if (finishPinDrag(e, false)) return;
                    if (finishForcepsDrag(e, false)) return;
                    if (finishProbeDrag(e, false)) return;
                    if (finishDropperDrag(e, false)) return;
                    if (finishWickDrag(e, false)) return;
                    if (finishProcedureStroke(e)) return;

                    if (canvas._isPanning && canvas._panPointerId != null && e.pointerId != null && canvas._panPointerId !== e.pointerId) return;
                    canvas._isPanning = false;
                    if (canvas.releasePointerCapture && e.pointerId != null && canvas.hasPointerCapture && canvas.hasPointerCapture(e.pointerId)) canvas.releasePointerCapture(e.pointerId);
                    canvas._panPointerId = null;
                    canvas._panStartX = null;
                    canvas._panStartY = null;
                    canvas._panOrigX = null;
                    canvas._panOrigY = null;
                    syncCanvasPointerPresentation(canvas, null);

                  },

                  onPointerCancel: cancelCanvasPointerInteraction,
                  onLostPointerCapture: cancelCanvasPointerInteraction,
                  onPointerLeave: function (e) {
                    if (e.currentTarget._toolDrawing || (e.currentTarget._forcepsDrag && e.currentTarget._forcepsDrag.active) || (e.currentTarget._pinDrag && e.currentTarget._pinDrag.active) || (e.currentTarget._probeDrag && e.currentTarget._probeDrag.active) || (e.currentTarget._dropperDrag && e.currentTarget._dropperDrag.active) || (e.currentTarget._wickDrag && e.currentTarget._wickDrag.active)) return;
                    e.currentTarget._toolPointer = null;
                    e.currentTarget._toolIntentState = null;
                    e.currentTarget._parallaxTargetX = 0;
                    e.currentTarget._parallaxTargetY = 0;
                    e.currentTarget._toolVector = null;
                    e.currentTarget._toolPressure = null;
                    upd('hoveredOrgan', null);
                    syncCanvasPointerPresentation(e.currentTarget, null);
                  },

                  width: 500, height: 600,

                      className: "diss-canvas w-full rounded-xl border border-slate-400 cursor-crosshair",

                      style: { aspectRatio: '5/6', background: 'var(--allo-stem-canvas, #0f172a)', cursor: pointerGuideData.cursor, touchAction: (d.canvasZoom || 1) <= 1.01 && (d.quizMode || guidedMode || currentLearningGate.required || !currentToolReadiness.safeToAct || procedureNext.action === 'complete') ? 'pan-y' : (((d.canvasZoom || 1) > 1.01 || d.annotateMode || d.rulerMode || activeInstrument === 'scalpel' || activeInstrument === 'pin' || ((activeInstrument === 'probe' || activeInstrument === 'dropper' || activeInstrument === 'wick') && (d.canvasZoom || 1) <= 1.01)) ? 'none' : ((activeInstrument === 'scissors' || activeInstrument === 'forceps') ? 'none' : 'pan-y')) }

                      })
                    ),
                    !d.quizMode && splitComparison && referenceEvidence ? React.createElement("section", { className: "diss-split-reference", "aria-label": "Selected dissection reference frame" },
                      React.createElement("div", { className: "diss-split-reference__header" },
                        React.createElement("div", null,
                          React.createElement("strong", null, 'Reference frame'),
                          React.createElement("span", null, evidenceLayerLabel(referenceEvidence) + ' · ' + evidenceViewLabel(referenceEvidence) + ' view')
                        ),
                        React.createElement("button", { type: "button", "aria-label": "Close split comparison", onClick: function () { upd('splitComparison', false); } }, 'Close')
                      ),
                      React.createElement("img", { src: referenceEvidence.image, alt: spec.name + ' evidence reference, ' + evidenceLayerLabel(referenceEvidence) + ', ' + evidenceViewLabel(referenceEvidence) + ' view, ' + evidenceConditionLabel(referenceEvidence) }),
                      React.createElement("p", null, (referenceEvidence.selectedOrganName ? 'Focused on ' + referenceEvidence.selectedOrganName + ' · ' : '') + (isFinite(Number(referenceEvidence.techniqueScore)) ? 'Technique ' + Math.max(0, Math.min(100, Math.round(Number(referenceEvidence.techniqueScore)))) + '/100' : 'Technique not scored') + (referenceEvidence.opticalQuality != null ? ' · optics ' + referenceEvidence.opticalQuality + '%' : '') + ' · captured ' + (typeof referenceEvidence.capturedAt === 'string' && referenceEvidence.capturedAt ? referenceEvidence.capturedAt.slice(0, 16).replace('T', ' ') : 'earlier'))
                    ) : null
                  ),

                  // Zoom control bar
                React.createElement("div", { className: "diss-zoom-bar flex items-center justify-center gap-2 py-1 px-2 rounded-lg bg-slate-100 border border-slate-400" },
                  React.createElement("button", { "aria-label": "Zoom out canvas",
                    onClick: function (e) { adjustCanvasZoomFromControl(e, -0.25); },
                    className: "transition-colors px-2 py-0.5 rounded text-xs font-bold bg-white border border-slate-400 hover:bg-slate-50 active:scale-[0.97]"
                  }, '\u2796'),
                  React.createElement("span", { className: "text-[11px] font-mono text-slate-600 min-w-[40px] text-center" }, Math.round((d.canvasZoom || 1) * 100) + '%'),
                  React.createElement("button", { "aria-label": "Zoom in canvas",
                    onClick: function (e) { adjustCanvasZoomFromControl(e, 0.25); },
                    className: "transition-colors px-2 py-0.5 rounded text-xs font-bold bg-white border border-slate-400 hover:bg-slate-50 active:scale-[0.97]"
                  }, '\u2795'),
                  (d.canvasZoom || 1) > 1.01 ? React.createElement("div", { className: "diss-pan-controls", role: "group", "aria-label": "Move the zoomed specimen without dragging" },
                    React.createElement("button", { type: "button", "aria-label": "Move specimen up", onClick: function (e) { panCanvasByControl(e, 0, -54, 'up'); } }, '\u2191'),
                    React.createElement("button", { type: "button", "aria-label": "Move specimen left", onClick: function (e) { panCanvasByControl(e, -54, 0, 'left'); } }, '\u2190'),
                    React.createElement("span", { "aria-hidden": "true" }, 'PAN'),
                    React.createElement("button", { type: "button", "aria-label": "Move specimen right", onClick: function (e) { panCanvasByControl(e, 54, 0, 'right'); } }, '\u2192'),
                    React.createElement("button", { type: "button", "aria-label": "Move specimen down", onClick: function (e) { panCanvasByControl(e, 0, 54, 'down'); } }, '\u2193')
                  ) : null,
                  (d.canvasZoom || 1) !== 1 ? React.createElement("button", { "aria-label": "100%",
                    onClick: function (e) { resetCanvasView(e); },
                    className: "transition-colors px-2 py-0.5 rounded text-[11px] font-bold bg-blue-100 text-blue-700 border border-blue-200 hover:bg-blue-200 active:scale-[0.97]"
                  }, '\u21BA 100%') : null,
                  sel ? React.createElement("button", {
                    type: "button",
                    "aria-label": "Frame selected structure " + sel.name,
                    onClick: frameSelectedStructure,
                    className: "transition-colors px-2 py-0.5 rounded text-[11px] font-bold bg-teal-100 text-teal-800 border border-teal-300 hover:bg-teal-200 active:scale-[0.97]"
                  }, '\u25CE Frame ' + sel.name) : null
                ),

                depthAtlasEnabled && React.createElement("div", { className: "diss-depth-key", role: "group", "aria-label": "Anatomical depth visual key: " + depthAtlasCounts.surface + " surface, " + depthAtlasCounts.structure + " mid-depth, and " + depthAtlasCounts.deep + " deep structures" },
                  React.createElement("span", { className: "diss-depth-key__title" }, 'Depth atlas'),
                  React.createElement("span", { className: "diss-depth-key__item" }, React.createElement("span", { className: "diss-depth-symbol", "data-depth": "surface", "aria-hidden": "true" }), 'Surface', React.createElement("span", { className: "diss-depth-key__count" }, String(depthAtlasCounts.surface))),
                  React.createElement("span", { className: "diss-depth-key__item" }, React.createElement("span", { className: "diss-depth-symbol", "data-depth": "structure", "aria-hidden": "true" }), 'Mid-depth', React.createElement("span", { className: "diss-depth-key__count" }, String(depthAtlasCounts.structure))),
                  React.createElement("span", { className: "diss-depth-key__item" }, React.createElement("span", { className: "diss-depth-symbol", "data-depth": "deep", "aria-hidden": "true" }), 'Deep', React.createElement("span", { className: "diss-depth-key__count" }, String(depthAtlasCounts.deep)))
                ),

                React.createElement("p", { id: "diss-canvas-equivalent", className: "diss-sr-only" }, 'Every canvas selection and drag action has a keyboard or button alternative. Use the structure directory for equivalent anatomical descriptions, the technique action button instead of drawing a stroke, the pin action button instead of dragging a pin, the probe action button instead of tracing a structure, and the pan controls instead of dragging a zoomed view. When the depth atlas is enabled, surface landmarks use circles, mid-depth landmarks use diamonds, and deep landmarks use double rings.'),
                React.createElement("details", { id: "diss-evidence-notebook", className: "diss-evidence", open: splitComparison && !!referenceEvidence },
                  React.createElement("summary", null, 'Evidence notebook · ' + visualEvidence.length + '/' + evidenceFrameLimit + ' frames'),
                  React.createElement("p", { id: "diss-evidence-capacity", className: "diss-evidence__capacity", "data-full": visualEvidence.length >= evidenceFrameLimit ? "true" : "false", role: "status", "aria-live": "polite" },
                    visualEvidence.length >= evidenceFrameLimit
                      ? 'Notebook full. Download or remove a frame before capturing another; no frame will be replaced automatically.'
                      : 'Frames use browser storage. Download important frames, and remove frames you no longer need if saving becomes limited.'
                  ),
                  visualEvidence.length ? React.createElement("div", { className: "diss-evidence__manage" },
                    React.createElement("button", { id: "diss-evidence-clear-all", type: "button", "aria-expanded": !!d.evidenceClearPending, "aria-controls": "diss-evidence-clear-confirmation", onClick: requestClearVisualEvidence }, 'Clear all evidence frames'),
                    d.evidenceClearPending && React.createElement("div", { id: "diss-evidence-clear-confirmation", className: "diss-evidence__confirm", role: "group", "aria-labelledby": "diss-evidence-clear-title" },
                      React.createElement("p", { id: "diss-evidence-clear-title", role: "status", "aria-live": "polite" }, 'Clear all ' + visualEvidence.length + ' evidence frames? Download any frames you need first. Notes, procedure work, and assessment progress will remain.'),
                      React.createElement("button", { id: "diss-evidence-confirm-clear", type: "button", "data-confirm": "true", onClick: confirmClearVisualEvidence }, 'Confirm clear frames'),
                      React.createElement("button", { type: "button", onClick: cancelClearVisualEvidence }, 'Cancel')
                    )
                  ) : null,
                  visualEvidence.length ? React.createElement("div", { className: "diss-evidence__grid" },
                    visualEvidence.map(function (evidence, evidenceIndex) {
                      var isReferenceEvidence = !!referenceEvidence && evidence.id === referenceEvidence.id;
                      var evidenceLayerName = evidenceLayerLabel(evidence);
                      var evidenceView = evidenceViewLabel(evidence);
                      var evidenceCondition = evidenceConditionLabel(evidence);
                      var evidenceTechniqueScore = isFinite(Number(evidence.techniqueScore)) ? Math.max(0, Math.min(100, Math.round(Number(evidence.techniqueScore)))) : null;
                      var evidenceTime = typeof evidence.capturedAt === 'string' && evidence.capturedAt.length >= 16 ? evidence.capturedAt.slice(11, 16) : '';
                      var removalPending = d.evidenceRemovePendingId === evidence.id;
                      var evidenceToken = evidenceDomToken(evidence.id);
                      return React.createElement("article", { className: "diss-evidence__item", key: String(evidence.id) + '-' + evidenceIndex, "data-evidence-id": String(evidence.id), "data-reference": isReferenceEvidence ? "true" : "false" },
                        React.createElement("img", { src: evidence.image, alt: spec.name + ' evidence thumbnail, ' + evidenceLayerName + ', ' + evidenceView + ' view' }),
                        React.createElement("strong", null, evidenceLayerName),
                        React.createElement("span", null, evidenceView + ' · ' + evidenceCondition + (evidence.selectedOrganName ? ' · ' + evidence.selectedOrganName : '')),
                        React.createElement("span", null, (evidenceTechniqueScore == null ? 'Technique not scored' : 'Technique ' + evidenceTechniqueScore + '/100') + (evidence.opticalQuality != null ? ' · optics ' + evidence.opticalQuality + '%' : '') + (evidenceTime ? ' · ' + evidenceTime : '')),
                        removalPending && React.createElement("p", { className: "diss-evidence__confirm-copy", role: "status" }, 'Remove this frame? Download it first if you need a separate copy.'),
                        React.createElement("div", { className: "diss-evidence__actions" },
                          removalPending
                            ? React.createElement("button", { id: "diss-evidence-confirm-remove-" + evidenceToken, type: "button", "data-confirm": "true", "data-evidence-confirm-remove-id": String(evidence.id), onClick: function () { confirmEvidenceRemoval(evidence.id); } }, 'Confirm remove')
                            : React.createElement("button", { type: "button", "aria-label": (isReferenceEvidence ? 'Current reference: ' : 'Use as reference: ') + evidenceLayerName, "aria-pressed": isReferenceEvidence, onClick: function () { selectEvidenceReference(evidence.id); } }, isReferenceEvidence ? 'Reference selected' : 'Use as reference'),
                          removalPending
                            ? React.createElement("button", { type: "button", onClick: function () { cancelEvidenceRemoval(evidence.id); } }, 'Cancel')
                            : React.createElement("button", { type: "button", onClick: function () { downloadEvidence(evidence); }, "aria-label": "Download evidence from " + evidenceLayerName }, 'Download'),
                          !removalPending && React.createElement("button", { id: "diss-evidence-remove-" + evidenceToken, type: "button", "data-danger": "true", "data-evidence-remove-id": String(evidence.id), onClick: function () { requestEvidenceRemoval(evidence.id); }, "aria-label": "Remove evidence frame " + (evidenceIndex + 1) + ", " + evidenceLayerName }, 'Remove')
                        )
                      );
                    })
                  ) : React.createElement("p", null, 'Capture a frame from Lab tools to build a visual record of layers, views, and technique progress.')
                ),

                  React.createElement("p", { id: "diss-canvas-status", className: "diss-stage__live", "data-tool-status": "true", "data-tone": stageHandoffTone, role: "status", "aria-live": "polite", "aria-atomic": "true" },
                    d.quizMode
                      ? (effectiveQuizAnswerMode === 'hotspot'
                        ? 'Practice assessment: ' + quizPrompt + ' Select a visible structure to submit. Arrow keys preview and Enter submits.'
                        : 'Practice assessment: ' + quizPrompt + ' Choose an answer in the assessment panel. Canvas selection will not submit in multiple-choice mode.')
                      : (stageHandoffUsesTool ? ('Tool ' + nextToolDefinition.label + ' · ' + (nextToolReadiness ? nextToolReadiness.label + '. ' + nextToolReadiness.cue : nextActionModel.description) + ' ' + (sel
                        ? ('Selected ' + sel.name + ' in the ' + currentLayerDef.name + ' layer. ' + sel.fn.split('.')[0] + '.')
                        : 'Select a structure on the specimen or use the accessible structure directory. Arrow keys move between structures when the canvas is focused.'))
                        : (nextActionModel.phase + ': ' + nextActionModel.title + '. ' + nextActionModel.description
                          + (sel ? ' Selected ' + sel.name + ' in the ' + currentLayerDef.name + ' layer.' : '')))
                  ),

                  React.createElement("details", { className: "diss-shortcuts" },
                    React.createElement("summary", null, 'Keyboard and precision controls'),
                    React.createElement("div", { className: "diss-shortcuts__grid" },
                      [
                        ['Arrows', 'Browse visible structures'],
                        ['Shift + Arrows', 'Pan the zoomed specimen without dragging'],
                        ['Home / End', 'First or last structure'],
                        ['Enter', 'Inspect or submit hotspot answer'],
                        ['+ / -', 'Zoom while keeping the stage constrained'],
                        ['Ctrl + wheel', 'Zoom around the pointer'],
                        ['0 / R', 'Center and reset to 100%'],
                        ['V / X', 'Cycle view / cross-section'],
                        ['M / P / F', 'Lens magnification / pin / depth'],
                        ['1-7', 'Select an instrument']
                      ].map(function (shortcut) {
                        return React.createElement("span", { key: shortcut[0] },
                          React.createElement("kbd", null, shortcut[0]),
                          shortcut[1]
                        );
                      })
                    )
                  ),

                  React.createElement("details", { id: "diss-procedure-panel", className: "diss-procedure", open: !!d.techniquePanelOpen, onToggle: function (event) { var open = event.currentTarget.open; if (open !== !!d.techniquePanelOpen) upd('techniquePanelOpen', open); }, "aria-labelledby": "diss-procedure-title" },
                    React.createElement("summary", { id: "diss-procedure-summary", className: "diss-procedure__summary" },
                      React.createElement("strong", null, 'Technique controls'),
                      React.createElement("span", null, layerTechniqueComplete ? 'Protocol complete' : procedureNext.label)
                    ),
                    React.createElement("div", { className: "diss-procedure__header" },
                      React.createElement("div", null,
                        React.createElement("h4", { id: "diss-procedure-title" }, 'Technique practice \u00B7 ' + (procedureStageIdx >= PROCEDURE_STEPS.length ? 'ready to complete' : 'step ' + (procedureStageIdx + 1) + ' of ' + PROCEDURE_STEPS.length)),
                        React.createElement("p", null, procedureProtocol.title + '. ' + (procedureMode === 'guided' ? 'Follow the specimen-specific corridor or use the equivalent action button.' : 'Choose instruments yourself; protocol safeguards and feedback remain active.'))
                      ),
                      React.createElement("div", { className: "diss-procedure__mode", role: "group", "aria-label": "Technique support level" },
                        ['guided', 'independent'].map(function (mode) {
                          return React.createElement("button", { type: "button", key: mode, "aria-pressed": procedureMode === mode, onClick: function () { upd('procedureMode', mode); setProcedureFeedback(mode === 'guided' ? 'Guided technique shows the teaching access path.' : 'Independent technique hides the access path while preserving safety feedback.'); } }, mode === 'guided' ? 'Guided' : 'Independent');
                        })
                      )
                    ),
                    React.createElement("dl", { className: "diss-protocol", "aria-label": "Specimen-specific procedure protocol" },
                      React.createElement("div", { "data-aligned": anatomicalView === procedureProtocol.recommendedView ? "true" : "false" },
                        React.createElement("dt", null, 'Orientation'),
                        React.createElement("dd", null,
                          procedureProtocol.orientation + ' Recommended view: ' + procedureProtocol.recommendedView + '. Current view: ' + anatomicalView + '.',
                          anatomicalView !== procedureProtocol.recommendedView
                            ? React.createElement("button", { type: "button", className: "diss-protocol__align", onClick: function () { changeAnatomicalView(procedureProtocol.recommendedView, 'procedure alignment'); }, "aria-label": 'Align specimen to recommended ' + procedureProtocol.recommendedView + ' view' }, 'Align to ' + procedureProtocol.recommendedView + ' view')
                            : React.createElement("span", { className: "diss-sr-only" }, 'View aligned to procedure recommendation')
                        )
                      ),
                      React.createElement("div", null,
                        React.createElement("dt", null, 'Access route'),
                        React.createElement("dd", null, procedureProtocol.route)
                      ),
                      React.createElement("div", null,
                        React.createElement("dt", null, 'Protected landmarks'),
                        React.createElement("dd", null, protectedLandmarkNames.join(', ') || 'Underlying anatomy')
                      )
                    ),
                    React.createElement("div", { className: "diss-active-tool", "data-readiness": activeInstrumentState.readiness, "aria-label": "Active instrument status" },
                      React.createElement("span", { className: "diss-active-tool__icon", "aria-hidden": "true" }, (PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === activeInstrument; }) || {}).icon || '\u2022'),
                      React.createElement("div", null,
                        React.createElement("strong", null, 'Active tool: ' + ((PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === activeInstrument; }) || {}).label || activeInstrument)),
                        React.createElement("span", { id: "diss-active-tool-help" }, activeInstrumentState.instruction)
                      ),
                      React.createElement("span", { className: "diss-active-tool__badge" }, activeInstrumentState.label)
                    ),
                    React.createElement("section", { className: "diss-readiness", "data-tone": currentToolReadiness.tone, role: "region", "aria-label": "Active instrument action readiness" },
                      React.createElement("div", { className: "diss-readiness__header" },
                        React.createElement("strong", null, 'Action readiness \u00B7 ' + currentToolReadiness.label),
                        React.createElement("span", { className: "diss-readiness__score" }, currentToolReadiness.score + '/100')
                      ),
                      React.createElement("div", { className: "diss-readiness__meter", role: "progressbar", "aria-label": "Action readiness for " + currentToolReadiness.tool.label, "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": currentToolReadiness.score, "aria-valuetext": currentToolReadiness.label + ', ' + currentToolReadiness.score + ' percent' },
                        React.createElement("span", { style: { width: currentToolReadiness.score + '%' } })
                      ),
                      React.createElement("div", { className: "diss-readiness__checks", role: "group", "aria-label": "Readiness checks" },
                        currentToolReadiness.checks.map(function (check) {
                          return React.createElement("div", { className: "diss-readiness__check", key: check.id },
                            React.createElement("b", { "aria-hidden": "true" }, check.ready ? '\u2713' : '!'),
                            React.createElement("span", null, check.label + ' \u00B7 ' + (check.ready ? 'ready' : 'needs attention'))
                          );
                        })
                      ),
                      React.createElement("p", { className: "diss-readiness__cue" }, currentToolReadiness.cue)
                    ),
                    React.createElement("div", { className: "diss-instruments", role: "radiogroup", "aria-label": "Dissection instruments" },
                      PROCEDURE_INSTRUMENTS.map(function (tool) {
                        var toolState = procedureInstrumentStatus(tool.id);
                        var toolIsNext = procedureNext.action !== 'complete' && tool.id === procedureNext.instrument;
                        var toolRestricted = scenarioDefinition.id === 'restricted-tray' && (tool.id === 'dropper' || tool.id === 'wick');
                        return React.createElement("button", {
                          type: "button", role: "radio", key: tool.id, disabled: toolRestricted,
                          id: 'diss-instrument-' + tool.id,
                          className: "diss-instrument", "aria-checked": activeInstrument === tool.id,
                          tabIndex: activeInstrument === tool.id ? 0 : -1,
                          "data-readiness": toolState.readiness,
                          "data-next": toolIsNext ? 'true' : 'false',
                          "aria-current": toolIsNext ? 'step' : undefined,
                          "aria-label": (toolIsNext ? 'Next required instrument. ' : '') + tool.label + ', ' + toolState.label + '. Interaction: ' + (tool.inputHint || 'canvas gesture') + '. ' + toolState.instruction,
                          "aria-describedby": activeInstrument === tool.id ? "diss-active-tool-help" : undefined,
                          title: toolState.label + '. Interaction: ' + (tool.inputHint || 'canvas gesture') + '. ' + toolState.instruction,
                          onKeyDown: function (e) { onInstrumentKeyDown(e, tool.id); },
                          onClick: function () { selectProcedureInstrument(tool.id, 'pointer or keyboard activation'); }
                        },
                          React.createElement("span", null, tool.icon + ' ' + tool.label),
                          React.createElement("span", { className: "diss-instrument__state" }, activeInstrument === tool.id ? 'Selected \u00B7 ' + toolState.label : (toolIsNext ? 'Next \u00B7 ' + toolState.label : toolState.label)),
                          React.createElement("span", { className: "diss-instrument__hint" }, tool.inputHint || 'Canvas gesture')
                        );
                      })
                    ),
                    activeInstrument === 'scalpel' && React.createElement("div", { className: "diss-procedure__controls", role: "group", "aria-label": "Scalpel depth" },
                      React.createElement("span", { className: "text-[11px] font-bold text-slate-600" }, 'Depth:'),
                      ['shallow', 'deep'].map(function (depth) {
                        return React.createElement("button", { type: "button", key: depth, "aria-pressed": (d.incisionDepth || 'shallow') === depth, onClick: function () { upd('incisionDepth', depth); setProcedureFeedback(depth === 'shallow' ? 'Shallow depth selected. The scalpel is ready when orientation is complete.' : 'Deep practice depth selected. It will be blocked to protect underlying structures.', depth === 'shallow' ? 'success' : 'caution'); } }, depth === 'shallow' ? 'Shallow' : 'Deep (practice warning)');
                      })
                    ),
                    activeCalibrationControl && React.createElement("div", {
                      className: "diss-calibration",
                      "data-tone": activeCalibrationAssessment.tone,
                      role: "group",
                      "aria-labelledby": "diss-calibration-label",
                      "aria-describedby": "diss-calibration-status"
                    },
                      React.createElement("label", { className: "diss-calibration__title", id: "diss-calibration-label", htmlFor: "diss-calibration-range" },
                        React.createElement("strong", null, 'Instrument calibration \u00B7 ' + activeCalibrationControl.label),
                        React.createElement("span", null, 'Adjust before contact; this setting changes tissue response and scoring.')
                      ),
                      React.createElement("input", {
                        id: "diss-calibration-range", type: "range",
                        min: activeCalibrationControl.min, max: activeCalibrationControl.max, step: activeCalibrationControl.step,
                        value: toolCalibration[activeCalibrationControl.key],
                        "aria-valuetext": toolCalibration[activeCalibrationControl.key] + activeCalibrationControl.unit + ', ' + activeCalibrationAssessment.label,
                        onChange: function (event) {
                          var nextCalibration = Object.assign({}, toolCalibration);
                          nextCalibration[activeCalibrationControl.key] = Number(event.target.value);
                          upd('toolCalibration', normalizeToolCalibration(nextCalibration));
                        },
                        onBlur: function () {
                          var result = instrumentCalibrationAssessment(activeInstrument, toolCalibration);
                          setProcedureFeedback(((PROCEDURE_INSTRUMENTS.find(function (tool) { return tool.id === activeInstrument; }) || {}).label || activeInstrument) + ' calibration: ' + result.label + '. ' + result.detail, result.tone === 'ready' ? 'success' : 'caution');
                        }
                      }),
                      React.createElement("output", { htmlFor: "diss-calibration-range" }, toolCalibration[activeCalibrationControl.key] + activeCalibrationControl.unit + (activeInstrument === 'dropper' && toolCalibration.salineDose !== 1 ? 's' : '')),
                      React.createElement("p", { className: "diss-calibration__status", id: "diss-calibration-status" },
                        React.createElement("b", null, activeCalibrationAssessment.label + ' ' + activeCalibrationAssessment.score + '%'),
                        React.createElement("span", null, activeCalibrationAssessment.detail)
                      )
                    ),
                    React.createElement("div", { className: "diss-procedure__steps", "aria-label": procedureStageIdx + ' of ' + PROCEDURE_STEPS.length + ' technique steps complete' },
                      PROCEDURE_STEPS.map(function (step, idx) {
                        return React.createElement("span", { key: step.id, className: "diss-procedure__step", "data-complete": procedureStageIdx > idx ? "true" : "false", "data-current": procedureStageIdx === idx ? "true" : "false" }, (procedureStageIdx > idx ? '\u2713 ' : '') + step.label);
                      })
                    ),
                    React.createElement("div", { className: "diss-procedure__metrics", role: "group", "aria-label": "Technique feedback metrics" },
                      [
                        { label: 'Technique score', value: techniqueScore + '/100' },
                        { label: 'Decision quality', value: currentDecisionScore + '/100' },
                        { label: 'Anatomical exposure', value: currentExposure.score + '%' },
                        { label: 'Stroke precision', value: recentTechniqueMetrics ? recentTechniqueMetrics.precision + '%' : 'Not scored' },
                        { label: 'Path coverage', value: recentTechniqueMetrics ? recentTechniqueMetrics.coverage + '%' : 'Not scored' },
                        { label: 'Tool control', value: recentTechniqueMetrics ? (recentTechniqueMetrics.control == null ? recentTechniqueMetrics.smoothness : recentTechniqueMetrics.control) + '%' : 'Not scored' },
                        { label: 'Instrument angle', value: recentTechniqueMetrics && recentTechniqueMetrics.angleControl != null ? recentTechniqueMetrics.angleControl + '%' : 'Not scored' },
                        { label: 'Calibration', value: recordedCalibrationValues.length ? recordedCalibrationScore + '%' : activeCalibrationAssessment.label },
                        { label: 'Input', value: currentProcedure.inputType || 'Not recorded' },
                        { label: 'Cautions', value: String(currentProcedure.errors || 0) },
                        { label: 'Tissue preservation', value: currentTissueStatus.preservation + '%' },
                        { label: 'Field exposure', value: currentTissueState.exposure + '%' },
                        { label: 'Observation quality', value: currentObservationField.quality + '%' }
                      ].map(function (metric) {
                        return React.createElement("div", { className: "diss-procedure__metric", key: metric.label },
                          React.createElement("span", null, metric.label),
                          React.createElement("strong", null, metric.value)
                        );
                      })
                    ),
                    React.createElement("div", { className: "diss-advanced-row" },
                      React.createElement("div", {
                        className: "diss-advanced-card diss-tissue-monitor",
                        "data-tone": currentTissueState.trauma >= 60 ? "danger" : ((currentTissueState.risk >= 55 || currentTissueState.moisture < 38 || currentTissueState.tension > 82) ? "caution" : "stable"),
                        role: "group",
                        "aria-label": "Dynamic tissue state"
                      },
                        React.createElement("strong", null, 'Dynamic tissue state \u00B7 ' + currentTissueStatus.label),
                        React.createElement("p", null, currentTissueState.variantLabel + '. ' + currentTissueStatus.priority),
                        React.createElement("div", { className: "diss-tissue-grid" },
                          [
                            { label: 'Moisture', value: currentTissueState.moisture, risk: currentTissueState.moisture < 38 },
                            { label: 'Clarity', value: currentTissueState.clarity, risk: currentTissueState.clarity < 45 },
                            { label: 'Exposure', value: currentTissueState.exposure, risk: false },
                            { label: 'Stability', value: currentTissueState.stability, risk: currentTissueState.exposure > 35 && currentTissueState.stability < 45 },
                            { label: 'Tension', value: currentTissueState.tension, risk: currentTissueState.tension > 82 },
                            { label: 'Trauma', value: currentTissueState.trauma, risk: currentTissueState.trauma > 42 },
                            { label: 'Compliance', value: currentTissuePhysics.compliance, risk: currentTissuePhysics.compliance < 28 },
                            { label: 'Motion damping', value: currentTissuePhysics.damping, risk: currentTissueState.exposure > 35 && currentTissuePhysics.damping < 35 }
                          ].map(function (metric) {
                            return React.createElement("div", { className: "diss-tissue-meter", "data-risk": metric.risk ? "true" : "false", key: metric.label },
                              React.createElement("span", null, React.createElement("span", null, metric.label), React.createElement("b", null, metric.value + '%')),
                              React.createElement("progress", { max: 100, value: metric.value, "aria-label": metric.label + ' ' + metric.value + ' percent' })
                            );
                          })
                        ),
                        latestTissueConsequence && React.createElement("p", { className: "diss-tissue-consequence" }, latestTissueConsequence.message)
                      ),
                      React.createElement("div", {
                        className: "diss-advanced-card diss-field-quality",
                        "data-tone": currentObservationField.tone,
                        role: "group",
                        "aria-label": "Observation field quality " + currentObservationField.quality + " percent"
                      },
                        React.createElement("strong", null, 'Observation field \u00B7 ' + currentObservationField.label),
                        React.createElement("p", null, 'Limiting factor: ' + currentObservationField.limiter + '. ' + currentObservationField.guidance),
                        React.createElement("progress", { max: 100, value: currentObservationField.quality, "aria-label": "Observation quality " + currentObservationField.quality + " percent" }),
                        React.createElement("div", { className: "diss-field-quality__metrics" },
                          React.createElement("span", null, 'Aperture ' + currentObservationField.aperture + '%'),
                          React.createElement("span", null, 'Clarity ' + currentObservationField.clarity + '%'),
                          React.createElement("span", null, 'Stability ' + currentObservationField.stability + '%')
                        )
                      ),
                      !d.quizMode && (inspectionLens || macroInset || lensPinned) && React.createElement("div", {
                        className: "diss-advanced-card diss-optics",
                        "data-tone": currentOpticalInspection.tone,
                        role: "group",
                        "aria-label": "Optical inspection quality " + currentOpticalInspection.quality + " percent"
                      },
                        React.createElement("strong", null, 'Optical inspection \u00B7 ' + currentOpticalInspection.label),
                        React.createElement("p", null, currentOpticalInspection.targetName + '. Detail: ' + currentOpticalInspection.detail + '. Ideal focus: ' + currentOpticalInspection.idealDepth + '. Illumination: ' + currentIllumination.label + ' at ' + lightIntensity + '%; glare risk ' + currentIllumination.glareRisk + '%. ' + currentOpticalInspection.suggestion),
                        React.createElement("div", { className: "diss-optics__meter" },
                          React.createElement("progress", { max: 100, value: currentOpticalInspection.quality, "aria-label": "Optical focus quality " + currentOpticalInspection.quality + " percent" }),
                          React.createElement("b", null, currentOpticalInspection.quality + '%')
                        ),
                        opticalPlaneMapActive && React.createElement("div", { className: "diss-optics__plane-key", role: "group", "aria-label": "Optical plane visual key: " + opticalPlaneCounts.resolved + " resolved, " + opticalPlaneCounts.soft + " soft, and " + opticalPlaneCounts.unresolved + " unresolved structures" },
                          React.createElement("strong", null, 'Focus plane'),
                          React.createElement("span", { className: "diss-optics__plane-item" }, React.createElement("span", { className: "diss-focus-symbol", "data-focus": "resolved", "aria-hidden": "true" }), 'Resolved ' + opticalPlaneCounts.resolved),
                          React.createElement("span", { className: "diss-optics__plane-item" }, React.createElement("span", { className: "diss-focus-symbol", "data-focus": "soft", "aria-hidden": "true" }), 'Soft ' + opticalPlaneCounts.soft),
                          React.createElement("span", { className: "diss-optics__plane-item" }, React.createElement("span", { className: "diss-focus-symbol", "data-focus": "unresolved", "aria-hidden": "true" }), 'Unresolved ' + opticalPlaneCounts.unresolved)
                        ),
                        React.createElement("div", { className: "diss-optics__controls", role: "group", "aria-label": "Inspection focus depth" },
                          React.createElement("span", null, 'Focus'),
                          ['surface', 'structure', 'deep'].map(function (depth) {
                            return React.createElement("button", { type: "button", key: depth, "aria-pressed": lensFocusDepth === depth, onClick: function () {
                              updMany({ inspectionLens: true, lensFocusDepth: depth });
                              var result = opticalInspectionData(currentInspectionTarget, depth, lensMagnification, lensPinned, currentProcedure);
                              setProcedureFeedback('Focus set to ' + depth + '. ' + result.label + ' at ' + result.quality + '%. ' + result.suggestion, result.quality >= 65 ? 'success' : 'caution');
                            } }, depth);
                          }),
                          React.createElement("span", null, 'Magnify'),
                          [2, 4, 6].map(function (level) {
                            return React.createElement("button", { type: "button", key: level, "aria-pressed": lensMagnification === level, onClick: function () {
                              updMany({ inspectionLens: true, lensMagnification: level });
                              var result = opticalInspectionData(currentInspectionTarget, lensFocusDepth, level, lensPinned, currentProcedure);
                              setProcedureFeedback('Magnification set to ' + level + 'x. ' + result.label + ' at ' + result.quality + '%. ' + result.suggestion, result.quality >= 65 ? 'success' : 'caution');
                            } }, level + 'x');
                          })
                        )
                      ),
                      React.createElement("section", { className: "diss-scenario-console", role: "region", "aria-labelledby": "diss-scenario-title" },
                        React.createElement("div", { className: "diss-scenario-console__header" },
                          React.createElement("strong", { id: "diss-scenario-title" }, 'Scenario lab \u00B7 ' + scenarioDefinition.label),
                          React.createElement("span", { className: "diss-scenario-console__state", "data-state": scenarioStatus.complete ? "complete" : (scenarioStatus.expired ? "expired" : "active"), role: "status", "aria-live": "polite" }, scenarioStatus.complete ? '\u2713 Complete' : (scenarioStatus.expired ? 'Time expired' : scenarioDefinition.difficulty + ' \u00B7 ' + scenarioStatus.progress + '%'))
                        ),
                        React.createElement("p", { className: "diss-scenario-console__objective" }, scenarioDefinition.prompt),
                        React.createElement("div", { className: "diss-scenario-console__progress", role: "progressbar", "aria-label": scenarioDefinition.label + " progress", "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": scenarioStatus.progress, "aria-valuetext": scenarioStatus.progress + ' percent. ' + scenarioStatus.detail },
                          React.createElement("span", { style: { width: scenarioStatus.progress + '%' } })
                        ),
                        React.createElement("p", { className: "diss-scenario-console__detail" },
                          scenarioStatus.detail + '. ' + scenarioGuidance,
                          scenarioDefinition.id === 'timed-practical' && React.createElement("strong", { className: "diss-scenario-console__timer", role: "timer", "aria-label": formatScenarioTime(scenarioStatus.remaining) + " remaining" }, ' Time: ' + formatScenarioTime(scenarioStatus.remaining))
                        ),
                        React.createElement("div", { className: "diss-scenario-console__controls" },
                          React.createElement("label", null,
                            React.createElement("span", null, 'Choose scenario'),
                            React.createElement("select", { id: "diss-scenario-select", value: scenarioDefinition.id, onChange: function (event) { var chosen = ADVANCED_SCENARIOS.find(function (scenario) { return scenario.id === event.target.value; }); activateProcedureScenario(chosen, false); }, "aria-label": "Choose procedure scenario" },
                              ADVANCED_SCENARIOS.map(function (scenario) { return React.createElement("option", { key: scenario.id, value: scenario.id }, scenario.label + ' \u00B7 ' + scenario.difficulty); })
                            )
                          ),
                          React.createElement("button", { type: "button", onClick: function () { activateProcedureScenario(scenarioDefinition, true); }, "aria-label": "Restart " + scenarioDefinition.label + " with a fresh procedure attempt" }, '\u21BB Restart scenario')
                        )
                      ),
                      React.createElement("div", { className: "diss-advanced-card" },
                        React.createElement("strong", null, anatomicalView + ' view \u00B7 ' + specimenCondition),
                        React.createElement("p", null, specimenConditionDescription() + '. ' + visibleOrgansInView.length + '/' + organs.length + ' structures directly visible in this orientation. Material model: ' + activeMaterialProfile.label + '. Contact response: ' + activeMaterialProfile.response + '. Light: ' + lightDirection + '; focus isolation ' + (focusMode ? 'on' : 'off') + '.')
                      ),
                      React.createElement("div", { className: "diss-advanced-card", "data-tone": currentExposure.score >= 58 ? "clear" : "developing" },
                        React.createElement("strong", null, 'Progressive exposure \u00B7 ' + currentExposure.label + ' ' + currentExposure.score + '%'),
                        React.createElement("p", null, currentExposure.next + ' Protocol progress ' + currentExposure.protocolProgress + '%. Decision quality ' + currentDecisionScore + '/100.')
                      ),
                      React.createElement("div", { className: "diss-advanced-card", "data-tone": currentMaterialInteraction.margin >= 0 ? "clear" : "caution" },
                        React.createElement("strong", null, 'Material response \u00B7 resistance ' + currentMaterialInteraction.resistance + '/100'),
                        React.createElement("p", null, currentMaterialInteraction.label + '. ' + currentMaterialInteraction.response + ' Risk if control is lost: ' + currentMaterialInteraction.failureMode)
                      ),
                      currentProcedure.forcepsDragMetrics && React.createElement("div", { className: "diss-advanced-card", "data-tone": currentProcedure.forcepsDragMetrics.control >= 72 ? "clear" : "caution", role: "status", "aria-label": "Recorded direct forceps lift evidence" },
                        React.createElement("strong", null, 'Direct lift evidence \u00B7 ' + currentProcedure.forcepsDragMetrics.liftPercent + '% lift'),
                        React.createElement("p", null, 'Control ' + currentProcedure.forcepsDragMetrics.control + '%. Projected tension ' + currentProcedure.forcepsDragMetrics.projectedTension + '%. Duration ' + currentProcedure.forcepsDragMetrics.durationMs + ' milliseconds. Input: ' + currentProcedure.forcepsDragMetrics.inputType + '.')
                      ),                      currentProcedure.pinDragMetrics && React.createElement("div", { className: "diss-advanced-card", "data-tone": currentProcedure.pinDragMetrics.control >= 72 ? "clear" : "caution", role: "status", "aria-label": "Recorded direct pin placement evidence" },
                        React.createElement("strong", null, 'Direct pin evidence \u00B7 ' + currentProcedure.pinDragMetrics.insertionPercent + '% inserted'),
                        React.createElement("p", null, 'Alignment ' + currentProcedure.pinDragMetrics.alignment + '%. Control ' + currentProcedure.pinDragMetrics.control + '%. Angle ' + currentProcedure.pinDragMetrics.angle + ' degrees. Input: ' + currentProcedure.pinDragMetrics.inputType + '.')
                      ),
                      currentProcedure.probeDragMetrics && React.createElement("div", { className: "diss-advanced-card", "data-tone": currentProcedure.probeDragMetrics.control >= 72 ? "clear" : "caution", role: "status", "aria-label": "Recorded direct probe palpation evidence" },
                        React.createElement("strong", null, 'Direct probe evidence \u00B7 ' + currentProcedure.probeDragMetrics.contactPercent + '% contact'),
                        React.createElement("p", null, 'Alignment ' + currentProcedure.probeDragMetrics.alignment + '%. Control ' + currentProcedure.probeDragMetrics.control + '%. Resistance ' + currentProcedure.probeDragMetrics.resistance + '/100. Deformation ' + currentProcedure.probeDragMetrics.deformation + '. Input: ' + currentProcedure.probeDragMetrics.inputType + '.')
                      ),                      currentProcedure.dropperDragMetrics && React.createElement("div", { className: "diss-advanced-card", "data-tone": currentProcedure.dropperDragMetrics.control >= 72 && currentProcedure.dropperDragMetrics.poolingRisk < 64 ? "clear" : "caution", role: "status", "aria-label": "Recorded direct dropper hydration evidence" },
                        React.createElement("strong", null, 'Direct dropper evidence \u00B7 ' + currentProcedure.dropperDragMetrics.contactPercent + '% contact'),
                        React.createElement("p", null, 'Dose ' + currentProcedure.dropperDragMetrics.dose + '. Flow alignment ' + currentProcedure.dropperDragMetrics.flowAlignment + '%. Control ' + currentProcedure.dropperDragMetrics.control + '%. Moisture ' + currentProcedure.dropperDragMetrics.moisture + '%. Pooling risk ' + currentProcedure.dropperDragMetrics.poolingRisk + '/100. Input: ' + currentProcedure.dropperDragMetrics.inputType + '.')
                      ),                      currentProcedure.wickDragMetrics && React.createElement("div", { className: "diss-advanced-card", "data-tone": currentProcedure.wickDragMetrics.control >= 72 && currentProcedure.wickDragMetrics.edgeAlignment >= 72 ? "clear" : "caution", role: "status", "aria-label": "Recorded direct wick recovery evidence" },
                        React.createElement("strong", null, 'Direct wick evidence \u00B7 ' + currentProcedure.wickDragMetrics.recoveryPercent + '% recovery'),
                        React.createElement("p", null, 'Edge alignment ' + currentProcedure.wickDragMetrics.edgeAlignment + '%. Contact ' + currentProcedure.wickDragMetrics.contactPercent + '%. Control ' + currentProcedure.wickDragMetrics.control + '%. Pooling before ' + currentProcedure.wickDragMetrics.poolingBefore + ' drops. Input: ' + currentProcedure.wickDragMetrics.inputType + '.')
                      ),                      d.adaptiveGuidance !== false && React.createElement("div", { className: "diss-advanced-card" },
                        React.createElement("strong", null, 'Adaptive focus: ' + adaptiveCoach.focus),
                        React.createElement("p", null, adaptiveCoach.suggestion),
                        React.createElement("button", { type: "button", onClick: applyAdaptiveCoaching }, 'Apply coaching setup')
                      ),
                      relationshipMode && d.selectedOrgan && React.createElement("div", { className: "diss-advanced-card" },
                        React.createElement("strong", null, 'Curated anatomical relationships'),
                        React.createElement("p", null, selectedRelationships.length ? selectedRelationships.map(function (item) { return item.relation + ' ' + item.organ.name + ' [' + item.type + '; ' + anatomicalRelationshipEncodingText(item.type) + ']' + (item.visible ? '' : ' (not yet exposed)'); }).join('; ') + '.' : 'No curated connection is defined for this structure.'),
                        React.createElement("p", { "aria-label": "Relationship pathway visual key" }, 'Path key: vascular solid circles; neural dash-dot diamonds; digestive short-dash chevrons; optical dotted triangles; excretory long-dash rings; respiratory long-short double chevrons; structural long-dash squares; other functional links even-dash hexagons.'),
                        React.createElement("button", { type: "button", disabled: !selectedRelationships.some(function (item) { return item.visible; }), onClick: traceNextAnatomicalRelationship }, 'Trace next exposed connection')
                      )
                    ),
                    !d.quizMode && d.compareTechniqueAttempts && techniqueComparison && React.createElement("div", { className: "diss-advanced-card diss-attempt-comparison", role: "region", "aria-label": "Technique attempt comparison" },
                      React.createElement("strong", null, 'Previous attempt vs current attempt'),
                      React.createElement("p", null, 'Saved baseline: ' + (techniqueComparison.previous.view || anatomicalView) + ' view, ' + (techniqueComparison.previous.inputType || 'input not recorded') + '. Positive values indicate improvement.'),
                      React.createElement("div", { className: "diss-attempt-comparison__legend", role: "list", "aria-label": "Technique path comparison legend" },
                        React.createElement("span", { className: "diss-attempt-comparison__legend-item", role: "listitem" }, React.createElement("i", { className: "diss-attempt-comparison__swatch", "aria-hidden": "true" }), 'Current attempt · solid cyan path'),
                        React.createElement("span", { className: "diss-attempt-comparison__legend-item", role: "listitem" }, React.createElement("i", { className: "diss-attempt-comparison__swatch", "data-style": "saved", "aria-hidden": "true" }), 'Saved baseline · dashed magenta path'),
                        React.createElement("span", { className: "diss-attempt-comparison__legend-item", role: "listitem" }, React.createElement("i", { className: "diss-attempt-comparison__swatch", "data-style": "caution", "aria-hidden": "true" }), 'Caution marker · diamond or circle')
                      ),
                      React.createElement("div", { className: "diss-attempt-comparison__scrub", role: "group", "aria-label": "Comparison replay controls" },
                        React.createElement("label", { htmlFor: "diss-compare-replay-progress" }, 'Replay point'),
                        React.createElement("input", { id: "diss-compare-replay-progress", type: "range", min: 0, max: 100, step: 1, value: compareReplayPercent, onChange: function (event) { setTechniqueCompareReplayProgress(Number(event.target.value) / 100); }, "aria-label": "Scrub comparison replay", "aria-valuetext": compareReplayLabel, "data-diss-compare-progress": true }),
                        React.createElement("output", { htmlFor: "diss-compare-replay-progress" }, compareReplayPercent + '% · ' + compareReplayLabel),
                        React.createElement("button", { type: "button", onClick: toggleTechniqueCompareReplay, "aria-pressed": !!d.compareReplayPlaying, "aria-label": d.compareReplayPlaying ? "Pause comparison replay" : "Play comparison replay" }, d.compareReplayPlaying ? 'Pause compare replay' : 'Play compare replay'),
                        React.createElement("button", { type: "button", onClick: function () { setTechniqueCompareReplayProgress(1); }, disabled: compareReplayPercent >= 100, "aria-label": "Show full comparison paths" }, 'Show full paths')
                      ),
                      React.createElement("div", { className: "diss-attempt-comparison__grid" },
                        [
                          { label: 'Score', previous: techniqueComparison.previous.score, current: techniqueComparison.current.score, delta: techniqueComparison.scoreDelta, unit: '' },
                          { label: 'Precision', previous: techniqueComparison.previous.precision, current: techniqueComparison.current.precision, delta: techniqueComparison.precisionDelta, unit: '%' },
                          { label: 'Coverage', previous: techniqueComparison.previous.coverage, current: techniqueComparison.current.coverage, delta: techniqueComparison.coverageDelta, unit: '%' },
                          { label: 'Control', previous: techniqueComparison.previous.control, current: techniqueComparison.current.control, delta: techniqueComparison.controlDelta, unit: '%' },
                          { label: 'Angle', previous: techniqueComparison.previous.angleControl, current: techniqueComparison.current.angleControl, delta: techniqueComparison.angleDelta, unit: '%' },
                          { label: 'Cautions', previous: techniqueComparison.previous.cautions, current: techniqueComparison.current.cautions, delta: techniqueComparison.cautionDelta, unit: '' },
                          { label: 'Preservation', previous: techniqueComparison.previous.preservation || 0, current: techniqueComparison.current.preservation, delta: techniqueComparison.preservationDelta, unit: '%' },
                          { label: 'Observation', previous: techniqueComparison.previous.observationQuality || 0, current: techniqueComparison.current.observationQuality, delta: techniqueComparison.observationDelta, unit: '%' }
                        ].map(function (metric) {
                          return React.createElement("div", { className: "diss-attempt-comparison__metric", key: metric.label },
                            React.createElement("span", null, metric.label),
                            React.createElement("strong", null, metric.previous + metric.unit + ' \u2192 ' + metric.current + metric.unit + ' (' + (metric.delta > 0 ? '+' : '') + metric.delta + metric.unit + ')')
                          );
                        })
                      )
                    ),
                    procedureTimelineEntries.length > 0 && React.createElement("div", { className: "diss-procedure__timeline", "aria-label": "Recorded technique timeline" },
                      procedureTimelineEntries.map(function (entry, idx) {
                        var timelineLabel = entry.label || procedureActionLabel(entry.action);
                        var timelineOutcome = entry.outcome || 'RECORDED';
                        var timelineDetail = entry.outcomeDetail ? ' ' + '\u00B7' + ' ' + entry.outcomeDetail : '';
                        var timelineSelected = !!(selectedTechniqueAction && String(selectedTechniqueAction.at) === String(entry.at));
                        return React.createElement("button", {
                          type: "button",
                          className: "diss-procedure__timeline-entry",
                          key: String(entry.at != null ? entry.at : idx) + entry.action,
                          "data-tone": entry.tone || 'success',
                          "data-selected": timelineSelected ? "true" : "false",
                          "aria-pressed": timelineSelected,
                          "aria-label": 'Review ' + timelineLabel + ': ' + timelineOutcome + (entry.outcomeDetail ? '. ' + entry.outcomeDetail : ''),
                          onClick: function () { selectProcedureTimelineEntry(entry); }
                        },
                          React.createElement("b", { className: "diss-procedure__timeline-index", "aria-hidden": "true" }, (idx + 1) + '.'),
                          React.createElement("span", { className: "diss-procedure__timeline-label" }, timelineLabel),
                          React.createElement("small", { className: "diss-procedure__timeline-outcome" }, timelineOutcome + timelineDetail)
                        );
                      })
                    ),
                    (currentProcedure.cautionLog || []).length > 0 && React.createElement("div", { className: "diss-procedure__timeline", "aria-label": "Technique caution history" },
                      (currentProcedure.cautionLog || []).slice(-3).map(function (entry, idx) {
                        return React.createElement("span", { key: String(entry.at || idx) }, 'Caution: ' + entry.message);
                      })
                    ),
                    React.createElement("div", { className: "diss-procedure__controls" },
                      React.createElement("button", { type: "button", className: "diss-procedure__next", disabled: equivalentProcedureActionDisabled, onClick: performNextProcedureStep, "aria-describedby": "diss-equivalent-technique-note", "aria-label": equivalentProcedureActionLabel }, procedureNext.action === 'complete' ? 'Technique complete' : ('Motor-neutral: ' + procedureNext.label)),
                      React.createElement("span", { id: "diss-equivalent-technique-note", className: "diss-sr-only" }, 'This control provides a motor-neutral equivalent after you choose the plan and prepare the field. It records supported technique and still requires an explanation before the next step.'),
                      React.createElement("button", { type: "button", onClick: showProcedureDemonstration, "aria-label": "Show a generalized safe-technique demonstration on the specimen" }, '\u25B6 Show technique'),
                      React.createElement("button", { type: "button", disabled: !((currentProcedure.actionLog || []).length || (currentProcedure.history || []).length), onClick: showProcedureReplay, "aria-label": "Replay the recorded technique attempt on the specimen" }, '\u21BB Replay attempt'),
                      React.createElement("button", { type: "button", className: "diss-advanced-only", disabled: procedureStageIdx === 0, onClick: saveTechniqueAttempt, "aria-label": "Save this technique attempt as a comparison baseline" }, '\u25A3 Save attempt'),
                      React.createElement("button", { type: "button", className: "diss-advanced-only", disabled: !attemptEntries.length, onClick: startNewTechniqueAttempt, "aria-label": "Start a new technique attempt for this layer" }, '\u21BB Start new attempt'),
                      React.createElement("button", { type: "button", className: "diss-advanced-only", disabled: !techniqueComparison, "aria-pressed": !!d.compareTechniqueAttempts, onClick: function () { upd('compareTechniqueAttempts', !d.compareTechniqueAttempts); }, "aria-label": "Compare the current technique with the previous saved attempt" }, '\u21C4 Compare attempts'),
                      React.createElement("button", { type: "button", className: "diss-advanced-only", "aria-pressed": d.adaptiveGuidance !== false, onClick: function () { upd('adaptiveGuidance', d.adaptiveGuidance === false); } }, 'Adaptive coaching ' + (d.adaptiveGuidance === false ? 'off' : 'on')),
                      React.createElement("button", { type: "button", className: "diss-advanced-only", onClick: function () { var select = dissectionNodeById('diss-scenario-select'); if (select) { select.scrollIntoView({ block: 'center', behavior: reducedMotionEnabled ? 'auto' : 'smooth' }); select.focus(); } }, "aria-controls": "diss-scenario-select" }, '\u2691 Scenario center'),
                      React.createElement("button", { type: "button", className: "diss-advanced-only", "aria-pressed": !!d.showProcedureDebrief, onClick: function () { upd('showProcedureDebrief', !d.showProcedureDebrief); } }, '\uD83D\uDCDD Debrief'),
                      activeInstrument === 'dropper' && React.createElement("button", { type: "button", disabled: !currentToolReadiness.safeToAct, onClick: function () { var point = procedureGuidePoints()[Math.floor((procedureGuidePoints().length - 1) / 2)]; var result = performProcedureAction('dropper', { point: point }); if (result && result.ok) queueProcedureInstrumentReplay('dropper', { point: result.point || point }); } }, '\u25C9 Apply controlled drop'),
                      activeInstrument === 'wick' && React.createElement("button", { type: "button", disabled: !currentToolReadiness.safeToAct, onClick: function () { var point = currentProcedure.dropperPoint || procedureGuidePoints()[Math.floor((procedureGuidePoints().length - 1) / 2)]; var result = performProcedureAction('wick', { point: point }); if (result && result.ok) queueProcedureInstrumentReplay('wick', { point: result.point || point }); }, "aria-label": "Wick excess saline from the visible pool edge" }, '\u25A7 Wick excess saline'),
                      React.createElement("button", { type: "button", disabled: !(currentProcedure.history || []).length, onClick: undoProcedureAction, "aria-label": "Undo the last technique action and visually restore the previous tissue state" }, '\u21A9 Undo last technique action')
                    ),
                    advancedWorkspace && d.showProcedureDebrief && React.createElement("div", { className: "diss-debrief", role: "region", "aria-label": "Technique debrief" },
                      React.createElement("strong", null, 'Strength: '), procedureDebrief.strength + '. ',
                      React.createElement("strong", null, 'Next improvement: '), procedureDebrief.improve
                    ),
                    React.createElement("details", { className: "diss-instructor" },
                      React.createElement("summary", null, 'Scenario thresholds'),
                      React.createElement("p", { className: "diss-procedure__notice" }, 'These controls tune procedure scenarios. Verified investigation completion also requires the objective, evidence, observation, technique, and first-attempt assessment targets.'),
                      React.createElement("div", { className: "diss-procedure__controls", role: "group", "aria-label": "Scenario assessment thresholds" },
                        React.createElement("button", { type: "button", onClick: function () { upd('instructorTarget', instructorTarget >= 90 ? 70 : instructorTarget + 5); } }, 'Target score: ' + instructorTarget),
                        React.createElement("button", { type: "button", onClick: function () { upd('instructorMaxCautions', instructorMaxCautions >= 2 ? 0 : instructorMaxCautions + 1); } }, 'Max cautions: ' + instructorMaxCautions),
                        React.createElement("button", { type: "button", onClick: function () { upd('instructorRequiredStructures', instructorRequiredStructures >= 4 ? 1 : instructorRequiredStructures + 1); } }, 'Required structures: ' + instructorRequiredStructures)
                      )
                    ),
                    React.createElement("p", { className: "diss-procedure__feedback", "data-diss-tool-status": true, "data-tone": (d.procedureFeedback || {}).tone || 'success', role: "status", "aria-live": "polite", "aria-atomic": "true" },
                      (d.procedureFeedback || {}).message || 'Begin by inspecting the visible layer and planning an access path.'
                    ),
                    React.createElement("p", { className: "diss-procedure__notice" }, specimenTrayNote),
                    React.createElement("p", { className: "diss-procedure__notice" }, 'This is a generalized, non-graphic teaching simulation, not a specimen-specific physical-dissection or surgical protocol. Follow instructor-approved procedures in a physical lab.')
                  ),

                  !d.quizMode && React.createElement("details", { className: "diss-overlay-actions", "data-dissection-overlays": true, "aria-label": "System overlays" },
                    React.createElement("summary", null, 'Systems and physiology tools'),
                    [
                      { key: 'traceNervous', label: 'Nervous', icon: '\u26A1', active: !!d.traceNervous, tone: 'purple' },
                      { key: 'traceCirculation', label: 'Circulatory', icon: '\u2665', active: !!d.traceCirculation, tone: 'red' },
                      { key: 'traceDigestion', label: 'Digestive', icon: '\u25C9', active: !!d.traceDigestion, tone: 'amber' },
                      { key: 'traceRespiration', label: 'Respiratory', icon: '\u2248', active: !!d.traceRespiration, tone: 'blue' },
                      { key: 'traceExcretory', label: 'Excretory', icon: '\u25C6', active: !!d.traceExcretory, tone: 'lime' },
                      { key: 'showEndocrine', label: 'Endocrine', icon: '\u2726', active: !!d.showEndocrine, tone: 'pink' }
                    ].map(function (overlay) {
                      var activeTone = { purple: 'bg-purple-700 text-white border-purple-700', red: 'bg-red-700 text-white border-red-700', amber: 'bg-amber-700 text-white border-amber-700', blue: 'bg-blue-700 text-white border-blue-700', lime: 'bg-lime-700 text-white border-lime-700', pink: 'bg-pink-700 text-white border-pink-700' }[overlay.tone];
                      return React.createElement("button", {
                        type: "button", key: overlay.key, "aria-label": "Toggle " + overlay.label + " system overlay", "aria-pressed": overlay.active,
                        onClick: function () {
                          var overlayPatch = { traceNervous: false, traceCirculation: false, traceDigestion: false, traceRespiration: false, traceExcretory: false, showEndocrine: false, systemPlaybackPaused: false, systemTraceReplayToken: Date.now() };
                          overlayPatch[overlay.key] = !overlay.active; updMany(overlayPatch);
                          setProcedureFeedback(!overlay.active ? overlay.label + ' overlay selected. ' + (activeLayer === 'organs' ? 'Directional playback started.' : 'Open the Internal Organs layer to view the pathway.') : overlay.label + ' overlay hidden.', !overlay.active && activeLayer !== 'organs' ? 'working' : 'success');
                        },
                        className: "w-full rounded-xl text-xs font-bold border transition-colors " + (overlay.active ? activeTone : 'bg-white text-slate-700 border-slate-300 hover:bg-slate-50')
                      }, overlay.icon + ' ' + (overlay.active ? 'Hide ' : '') + overlay.label);
                    }),
                    React.createElement("div", {
                      className: "diss-living-function",
                      "data-active": livingFunctionEnabled ? "true" : "false",
                      role: "group",
                      "aria-label": "Specimen-specific living function model"
                    },
                      React.createElement("div", { className: "diss-living-function__copy" },
                        React.createElement("strong", null, specimenLivingFunctionProfile().title),
                        React.createElement("span", null, specimenLivingFunctionProfile().summary),
                        React.createElement("span", { className: "diss-living-function__badge" }, livingFunctionEnabled ? (livingFunctionPaused ? 'Paused · ' + livingFunctionSpeed : 'Playing · ' + livingFunctionSpeed) : 'Off · preserved view remains static')
                      ),
                      React.createElement("div", { className: "diss-living-function__controls" },
                        React.createElement("button", { type: "button", "aria-pressed": livingFunctionEnabled, onClick: function () {
                          var enabled = !livingFunctionEnabled;
                          updMany({ livingFunctionEnabled: enabled, livingFunctionPaused: false, livingFunctionReplayToken: Date.now() });
                          setProcedureFeedback(enabled ? specimenLivingFunctionProfile().title + ' enabled as an in-life physiology model. Preserved specimens do not move.' : 'Living function model off. The preserved specimen view is static.');
                        } }, livingFunctionEnabled ? 'Function model on' : 'Enable function model'),
                        React.createElement("button", { type: "button", disabled: !livingFunctionEnabled, "aria-pressed": livingFunctionPaused, onClick: function () {
                          upd('livingFunctionPaused', !livingFunctionPaused);
                          setProcedureFeedback(specimenLivingFunctionProfile().title + (livingFunctionPaused ? ' resumed.' : ' paused on the current phase.'));
                        } }, livingFunctionPaused ? '▶ Resume' : '⏸ Pause'),
                        React.createElement("button", { type: "button", disabled: !livingFunctionEnabled, onClick: function () {
                          updMany({ livingFunctionPaused: false, livingFunctionReplayToken: Date.now() });
                          setProcedureFeedback(specimenLivingFunctionProfile().title + ' replayed from the first modeled phase.');
                        } }, '↻ Replay'),
                        React.createElement("button", { type: "button", disabled: !livingFunctionEnabled, onClick: function () {
                          var nextSpeed = livingFunctionSpeed === 'normal' ? 'slow' : (livingFunctionSpeed === 'slow' ? 'fast' : 'normal');
                          upd('livingFunctionSpeed', nextSpeed);
                          setProcedureFeedback('Living function speed set to ' + nextSpeed + '.');
                        }, "aria-label": "Cycle living function speed" }, 'Speed: ' + livingFunctionSpeed)
                      )
                    ),
                    activeFunctionalTraceKey && React.createElement("div", { className: "diss-system-playback", role: "group", "aria-label": activeFunctionalTraceLabel + " pathway playback" },
                      React.createElement("div", { className: "diss-system-playback__status", role: "status", "aria-live": "polite" },
                        React.createElement("strong", null, activeFunctionalTraceLabel + ' playback'),
                        React.createElement("span", null, activeLayer !== 'organs' ? 'Waiting for Internal Organs layer' : (d.systemPlaybackPaused ? 'Paused on the current directional frame' : 'Playing directional flow'))
                      ),
                      React.createElement("button", { type: "button", disabled: activeLayer !== 'organs', "aria-pressed": !!d.systemPlaybackPaused, onClick: function () { upd('systemPlaybackPaused', !d.systemPlaybackPaused); setProcedureFeedback(activeFunctionalTraceLabel + (d.systemPlaybackPaused ? ' playback resumed.' : ' playback paused on the current directional frame.')); } }, d.systemPlaybackPaused ? '\u25B6 Resume' : '\u23F8 Pause'),
                      React.createElement("button", { type: "button", disabled: activeLayer !== 'organs', onClick: function () { updMany({ systemPlaybackPaused: false, systemTraceReplayToken: Date.now() }); setProcedureFeedback(activeFunctionalTraceLabel + ' pathway replayed from the beginning.'); } }, '\u21BB Replay')
                    )
                  )
                ),
              // Flashcard panel
              d.flashcardMode && React.createElement("section", {
                id: "diss-flashcard-panel",
                tabIndex: -1,
                className: "diss-study-card bg-gradient-to-br from-indigo-50 to-violet-50 rounded-xl border border-indigo-200 p-4",
                "data-dissection-flashcards": true,
                "aria-labelledby": "diss-flashcard-title"
              },
                React.createElement("div", { className: "flex items-center justify-between gap-2 mb-3" },
                  React.createElement("h3", { id: "diss-flashcard-title", className: "text-sm font-black text-indigo-800" }, '🃏 Structure flashcards'),
                  React.createElement("span", { className: "text-xs font-bold text-indigo-500" }, ((d.flashcardIdx || 0) + 1) + ' / ' + organs.length)
                ),
                React.createElement("button", {
                  type: "button",
                  className: "diss-flashcard bg-white rounded-xl shadow-lg p-5 flex items-center justify-center hover:shadow-xl transition-shadow",
                  "aria-pressed": !!d.flashcardFlipped,
                  "aria-label": (d.flashcardFlipped ? 'Hide answer for ' : 'Reveal answer for ') + (organs[d.flashcardIdx || 0] ? organs[d.flashcardIdx || 0].name : 'this card'),
                  onClick: function () { upd('flashcardFlipped', !d.flashcardFlipped); }
                },
                  !d.flashcardFlipped
                    ? React.createElement("span", { className: "block text-center" },
                        React.createElement("span", { className: "block text-base font-black text-indigo-800" }, organs[d.flashcardIdx || 0] ? organs[d.flashcardIdx || 0].name : 'No structures found'),
                        React.createElement("span", { className: "block text-xs text-indigo-500 mt-2" }, 'Tap or press Enter to reveal its function')
                      )
                    : React.createElement("span", { className: "block text-left" },
                        React.createElement("span", { className: "block text-sm font-bold text-slate-700 leading-relaxed" }, organs[d.flashcardIdx || 0] ? organs[d.flashcardIdx || 0].fn : ''),
                        organs[d.flashcardIdx || 0] && organs[d.flashcardIdx || 0].clinical && React.createElement("span", { className: "block text-xs text-amber-700 mt-2 italic" }, organs[d.flashcardIdx || 0].clinical)
                      )
                ),
                React.createElement("div", { className: "flex items-center justify-between gap-3 mt-3" },
                  React.createElement("button", {
                    type: "button",
                    "aria-label": "Previous flashcard",
                    disabled: (d.flashcardIdx || 0) <= 0,
                    onClick: function () { upd('flashcardIdx', Math.max(0, (d.flashcardIdx || 0) - 1)); upd('flashcardFlipped', false); },
                    className: "px-4 py-2 rounded-lg text-xs font-bold bg-indigo-100 text-indigo-800 disabled:opacity-40"
                  }, '◀ Previous'),
                  React.createElement("button", {
                    type: "button",
                    "aria-label": "Next flashcard",
                    disabled: (d.flashcardIdx || 0) >= organs.length - 1,
                    onClick: function () { upd('flashcardIdx', Math.min(organs.length - 1, (d.flashcardIdx || 0) + 1)); upd('flashcardFlipped', false); },
                    className: "px-4 py-2 rounded-lg text-xs font-bold bg-indigo-100 text-indigo-800 disabled:opacity-40"
                  }, 'Next ▶')
                )
              ),
              // Comparison panel

              d.compareMode && sel && React.createElement("div", { id: "diss-comparison-panel", tabIndex: -1, className: "mt-2 bg-gradient-to-r from-cyan-50 to-blue-50 rounded-xl border border-cyan-200 p-3" },

                React.createElement("div", { className: "text-xs font-bold text-cyan-800 mb-2" }, '\uD83D\uDD0D ' + 'Comparing across specimens' + ': ' + sel.name + ' across specimens'),

                React.createElement("div", { className: "space-y-1.5 max-h-48 overflow-y-auto", tabIndex: 0, role: "region", "aria-label": "Specimen list" },

                  SPEC_KEYS.map(function (sk) {

                    var sp = SPECIMENS[sk];

                    // Search for matching organ name in all layers

                    var match = null;

                    sp.layers.forEach(function (layer) {

                      if (match) return;

                      var layerOrgans = sp.organs[layer.id] || [];

                      layerOrgans.forEach(function (org) {

                        if (!match && org.name.toLowerCase() === sel.name.toLowerCase()) match = { organ: org, layer: layer };

                      });

                    });

                    // Also try partial match (e.g. "Heart" matches "3-Chamber Heart")

                    if (!match) {

                      var searchWord = sel.name.split(' ').pop().toLowerCase();

                      sp.layers.forEach(function (layer) {

                        if (match) return;

                        var layerOrgans = sp.organs[layer.id] || [];

                        layerOrgans.forEach(function (org) {

                          if (!match && org.name.toLowerCase().indexOf(searchWord) >= 0) match = { organ: org, layer: layer };

                        });

                      });

                    }

                    if (!match) return null;

                    var isCurrent = sk === specimen;

                    return React.createElement("div", {

                      key: sk,

                      className: "p-2 rounded-lg text-xs " + (isCurrent ? 'bg-cyan-100 border border-cyan-300' : 'bg-white border border-slate-400')

                    },

                      React.createElement("div", { className: "font-bold " + (isCurrent ? 'text-cyan-800' : 'text-slate-700') }, sp.icon + ' ' + sp.name.split('(')[0].trim() + ': ' + match.organ.name),

                      React.createElement("p", { className: "text-[11px] text-slate-600 mt-0.5 leading-relaxed" }, match.organ.fn.substring(0, 120) + (match.organ.fn.length > 120 ? '...' : '')),

                      match.organ.clinical && React.createElement("p", { className: "text-[11px] text-amber-600 mt-0.5 italic" }, '\uD83C\uDFEB ' + match.organ.clinical.substring(0, 80) + '...')

                    );

                  })

                ),

                !sel && React.createElement("p", { className: "text-xs text-cyan-600 italic" }, 'Click organ to compare')

              ),



              ),

              // Sidebar

              React.createElement("aside", { id: "diss-structure-directory", tabIndex: -1, className: "diss-sidebar space-y-3", "data-dissection-sidebar": true, "aria-label": "Structure directory and lab notes" },
                React.createElement("a", { href: "#diss-canvas", className: "diss-skip-return", onClick: function (event) { event.preventDefault(); focusDissectionTarget('diss-canvas', 'Interactive specimen focused.', event.currentTarget.closest('[data-dissection-root]')); } }, 'Return to interactive specimen'),

                // Selected organ detail

                sel && React.createElement("section", { className: "diss-selection-card bg-white rounded-xl border p-4", "data-dissection-selection": true, role: "region", "aria-labelledby": "diss-selection-title" },

                  React.createElement("div", { className: "flex items-center justify-between mb-1" },

                    React.createElement("div", { className: "min-w-0" },
                      React.createElement("h4", { id: "diss-selection-title", tabIndex: -1, className: "text-base font-black text-slate-800" }, sel.name),
                      React.createElement("span", {
                        className: "diss-selection-position", role: "status", "aria-live": "polite", "aria-atomic": "true",
                        "aria-label": sel.name + ', structure ' + (selectedOrganIndex + 1) + ' of ' + organs.length
                      }, (selectedOrganIndex + 1) + ' of ' + organs.length)
                    ),

                    React.createElement("div", { className: "diss-selection-nav flex gap-1" },

                      React.createElement("button", { type: "button", "aria-label": "Previous structure", disabled: selectedOrganIndex <= 0,

                        onClick: function () {

                          var previousOrgan = organs[selectedOrganIndex - 1];
                          if (previousOrgan) chooseOrganFromDirectory(previousOrgan, true);

                        },

                        className: "transition-colors w-10 h-10 rounded-lg bg-slate-100 text-slate-700 text-xs hover:bg-slate-200 flex items-center justify-center active:scale-[0.97] disabled:opacity-35"

                      }, '\u25C0'),

                      React.createElement("button", { type: "button", "aria-label": "Next structure", disabled: selectedOrganIndex >= organs.length - 1,

                        onClick: function () {

                          var nextOrgan = organs[selectedOrganIndex + 1];
                          if (nextOrgan) chooseOrganFromDirectory(nextOrgan, true);

                        },

                        className: "transition-colors w-10 h-10 rounded-lg bg-slate-100 text-slate-700 text-xs hover:bg-slate-200 flex items-center justify-center active:scale-[0.97] disabled:opacity-35"

                      }, '▶'),
                      React.createElement("button", {
                        type: "button",
                        "aria-label": "Back to structure directory",
                        onClick: function () {
                          var closingOrganId = sel.id;
                          upd('selectedOrgan', null);
                          focusStructureDirectoryItem(closingOrganId);
                        },
                        className: "transition-colors w-10 h-10 rounded-lg bg-slate-100 text-slate-700 text-sm hover:bg-slate-200 flex items-center justify-center active:scale-[0.97]"
                      }, '✕')
                    )
                  ),

                  React.createElement("div", { id: 'diss-observe-first-' + sel.id, className: "diss-observe-first", role: "note" },
                    React.createElement("strong", null, __alloT('stem.dissection.observe_first', 'Observe first')),
                    React.createElement("p", null, __alloT('stem.dissection.observe_first_prompt', 'Record one visible feature and where it is in the evidence note below. Open the reference whenever you need support, then revise anything the comparison changes.'))
                  ),
                  React.createElement("details", {
                    key: specimen + '|' + sel.id,
                    className: "diss-selection-reference diss-disclosure",
                    "data-selection-reference": true
                  },
                    React.createElement("summary", null, __alloT('stem.dissection.check_reference', 'Check reference and connections')),
                    React.createElement("div", { className: "diss-disclosure__body" },
                      React.createElement("p", { className: "diss-selection-reference__intro" }, __alloT('stem.dissection.reference_context_help', 'Reference context can extend your learning. Base the evidence note on features and anatomical relationships you can observe in this specimen.')),
                  React.createElement("div", { className: "diss-selection-summary" },
                    React.createElement("span", { className: "diss-selection-summary__label" }, __alloT('stem.dissection.specimen_anatomy_and_function', 'Specimen anatomy and function')),
                    React.createElement("p", null, sel.fn)
                  ),
                  // Embryological origin badge

                  (function () {

                    var devMap = {

                      heart: 'Mesoderm', liver: 'Endoderm', brain: 'Ectoderm', kidney: 'Mesoderm',

                      lung: 'Endoderm', stomach: 'Endoderm', intestine: 'Endoderm', spleen: 'Mesoderm',

                      skin: 'Ectoderm', bone: 'Mesoderm', muscle: 'Mesoderm', nerve: 'Ectoderm',

                      eye: 'Ectoderm (lens) + mesoderm', retina: 'Ectoderm', pancreas: 'Endoderm',

                      blood: 'Mesoderm', thyroid: 'Endoderm'

                    };

                    var sn = sel.name.toLowerCase(); var dev = null;

                    Object.keys(devMap).forEach(function (k) { if (sn.indexOf(k) >= 0) dev = devMap[k]; });

                    var dColors = { Ectoderm: 'bg-blue-50 text-blue-600 border-blue-200', Mesoderm: 'bg-red-50 text-red-700 border-red-200', Endoderm: 'bg-yellow-50 text-yellow-800 border-yellow-200' };

                    var dcKey = dev ? (dev.indexOf('Ecto') >= 0 ? 'Ectoderm' : dev.indexOf('Meso') >= 0 ? 'Mesoderm' : 'Endoderm') : null;

                    return dev ? React.createElement("span", { className: "inline-block px-2 py-0.5 rounded-full text-[11px] font-bold mr-1 mb-1 border " + (dColors[dcKey] || 'bg-slate-50 text-slate-600 border-slate-200') }, '\uD83E\uDDEC ' + dev) : null;

                  })(),

                  // Tissue type badge

                  (function () {

                    var tissueMap = {

                      heart: 'Cardiac muscle', liver: 'Epithelial', brain: 'Nervous', kidney: 'Epithelial',

                      lung: 'Epithelial', stomach: 'Smooth muscle', intestine: 'Epithelial', spleen: 'Lymphoid',

                      bone: 'Connective', cartilage: 'Connective', skin: 'Epithelial', muscle: 'Skeletal muscle',

                      nerve: 'Nervous', tendon: 'Connective', blood: 'Connective (fluid)', pancreas: 'Glandular',

                      eye: 'Mixed', lens: 'Epithelial', retina: 'Nervous', esophagus: 'Smooth muscle'

                    };

                    var sn = sel.name.toLowerCase(); var tissue = null;

                    Object.keys(tissueMap).forEach(function (k) { if (sn.indexOf(k) >= 0) tissue = tissueMap[k]; });

                    var tColors = {

                      'Cardiac muscle': 'bg-red-100 text-red-700', 'Epithelial': 'bg-emerald-100 text-emerald-700',

                      'Nervous': 'bg-purple-100 text-purple-600', 'Smooth muscle': 'bg-rose-100 text-rose-700',

                      'Lymphoid': 'bg-amber-100 text-amber-800', 'Connective': 'bg-blue-100 text-blue-700',

                      'Skeletal muscle': 'bg-red-100 text-red-700', 'Glandular': 'bg-teal-100 text-teal-700',

                      'Mixed': 'bg-slate-100 text-slate-600', 'Connective (fluid)': 'bg-blue-100 text-blue-700'

                    };

                    return tissue ? React.createElement("span", { className: "inline-block px-2 py-0.5 rounded-full text-[11px] font-bold mr-1 mb-1 " + (tColors[tissue] || 'bg-slate-100 text-slate-600') }, '\uD83E\uDDA0 ' + tissue) : null;

                  })(),

                  // Organ weight estimate

                  (function () {

                    var weightMap = { heart: '250-350g', liver: '1.4-1.5kg', brain: '1.3-1.4kg', kidney: '120-170g', lung: '0.5-0.6kg', stomach: '150g', spleen: '170g', pancreas: '80g', eye: '7.5g', thyroid: '20-25g', adrenal: '4-5g', gallbladder: '30-50ml' };

                    var w = null; var sn = sel.name.toLowerCase();

                    Object.keys(weightMap).forEach(function (k) { if (sn.indexOf(k) >= 0) w = weightMap[k]; });

                    return w ? React.createElement("span", { className: "inline-block px-2 py-0.5 rounded-full text-[11px] font-bold bg-slate-100 text-slate-600 border border-slate-400 mr-1 mb-1" }, '\u2696\uFE0F ' + w + ' ' + 'Human') : null;

                  })(),

                  // System badge

                  (function () {

                    var sn = sel.name.toLowerCase();

                    var sysN = null;

                    var sysBadges = { circulatory: '\u2764\uFE0F Circulatory', digestive: '\uD83C\uDF7D\uFE0F Digestive', respiratory: '\uD83C\uDF2C\uFE0F Respiratory', nervous: '\u26A1 Nervous', skeletal: '\uD83E\uDDB4 Skeletal', muscular: '\uD83D\uDCAA Muscular', excretory: '\uD83D\uDCA7 Excretory', reproductive: '\u2665\uFE0F Reproductive' };

                    var sysCols = { circulatory: 'bg-red-50 text-red-700 border-red-200', digestive: 'bg-amber-50 text-amber-700 border-amber-200', respiratory: 'bg-blue-50 text-blue-700 border-blue-200', nervous: 'bg-purple-50 text-purple-700 border-purple-200', skeletal: 'bg-slate-50 text-slate-700 border-slate-200', muscular: 'bg-red-50 text-red-700 border-red-200', excretory: 'bg-lime-50 text-lime-700 border-lime-200', reproductive: 'bg-pink-50 text-pink-700 border-pink-200' };

                    var sysKW = { circulatory: ['heart', 'aorta', 'artery', 'vein', 'atrium', 'ventricle', 'blood', 'aortic'], digestive: ['stomach', 'liver', 'intestin', 'gizzard', 'crop', 'pancreas', 'gallbladder'], respiratory: ['lung', 'gill', 'trachea', 'swim bladder'], nervous: ['brain', 'nerve', 'spinal', 'eye', 'optic'], skeletal: ['bone', 'skull', 'vertebr', 'femur'], muscular: ['muscle', 'rectus'], excretory: ['kidney', 'nephri'], reproductive: ['gonad', 'ovary', 'testi'] };

                    Object.keys(sysKW).forEach(function (sk) { sysKW[sk].forEach(function (kw) { if (!sysN && sn.indexOf(kw) >= 0) sysN = sk; }); });

                    return sysN ? React.createElement("span", { className: "inline-block px-2 py-0.5 rounded-full text-[11px] font-bold border mb-1 " + sysCols[sysN] }, sysBadges[sysN]) : null;

                  })(),



                  sel.clinical && React.createElement("div", { className: "bg-amber-50 rounded-lg p-2 border border-amber-200" },

                    React.createElement("span", { className: "text-[11px] font-bold text-amber-700" }, '\uD83C\uDFE5 ' + __alloT('stem.dissection.human_clinical_connection', 'Human/clinical connection')),

                    React.createElement("p", { className: "text-[11px] text-amber-600 leading-relaxed mt-0.5" }, sel.clinical)

                  ),

                  // Position info

                  React.createElement("div", { className: "diss-selection-reference__layer" },
                    React.createElement("strong", null, __alloT('stem.dissection.specimen_layer', 'Specimen layer') + ':'),
                    React.createElement("span", null, currentLayerDef.name)
                  ),

                  // Clinical correlations

                  (function () {

                    var clinMap = {

                      heart: '\u26A0 Myocardial infarction, arrhythmia, heart murmur',

                      liver: '\u26A0 Hepatitis, cirrhosis, fatty liver disease',

                      lung: '\u26A0 Pneumonia, asthma, COPD, pulmonary embolism',

                      brain: '\u26A0 Stroke (CVA), concussion, meningitis',

                      kidney: '\u26A0 Renal calculi (stones), UTI, nephritis',

                      stomach: '\u26A0 Gastric ulcer, GERD, H. pylori infection',

                      pancreas: '\u26A0 Pancreatitis, diabetes mellitus',

                      spleen: '\u26A0 Splenomegaly, splenic rupture (trauma)',

                      gallbladder: '\u26A0 Cholelithiasis (gallstones), cholecystitis',

                      thyroid: '\u26A0 Hypothyroidism, hyperthyroidism, goiter',

                      eye: '\u26A0 Cataracts, glaucoma, retinal detachment',

                      retina: '\u26A0 Macular degeneration, diabetic retinopathy'

                    };

                    var sn = sel.name.toLowerCase(); var clin = null;

                    Object.keys(clinMap).forEach(function (k) { if (sn.indexOf(k) >= 0) clin = clinMap[k]; });

                    return clin ? React.createElement("div", { className: "text-[11px] text-amber-500 mt-1 italic border-l-2 border-amber-300 pl-2" }, clin) : null;

                  })(),

                  // Related organs info

                  (function () {

                    var relMap = {

                      heart: ['lungs', 'aorta', 'blood vessels'],

                      lungs: ['heart', 'trachea', 'diaphragm'],

                      liver: ['gallbladder', 'stomach', 'intestine'],

                      stomach: ['esophagus', 'liver', 'intestine'],

                      brain: ['spinal cord', 'nerves', 'eyes'],

                      kidneys: ['bladder', 'ureters', 'adrenals']

                    };

                    var sn = sel.name.toLowerCase();

                    var related = null;

                    Object.keys(relMap).forEach(function (k) { if (sn.indexOf(k) >= 0) related = relMap[k]; });

                    return related ? React.createElement("div", { className: "text-[11px] text-slate-600 mt-1" },

                      React.createElement("span", { className: "font-bold" }, '\uD83D\uDD17 ' + 'Related' + ': '),

                      related.join(', ')

                    ) : null;

                  })()
                    )
                  ),

                  (function () {
                    var evidenceKey = specimen + '|' + sel.id;
                    var noteValue = (d.organNotes || {})[evidenceKey] || '';
                    var confidenceValue = (d.organConfidence || {})[evidenceKey] || 0;
                    var confidenceChoices = [
                      { level: 1, label: dissectionConfidenceLabel(1), cue: __alloT('stem.dissection.confidence_not_sure_yet_cue', 'Inspect one more visible feature and its location.') },
                      { level: 2, label: dissectionConfidenceLabel(2), cue: __alloT('stem.dissection.confidence_somewhat_sure_cue', 'Check one more distinguishing feature or anatomical relationship.') },
                      { level: 3, label: dissectionConfidenceLabel(3), cue: __alloT('stem.dissection.confidence_confident_cue', 'Check that each claim in your note matches what you actually observed.') }
                    ];
                    var selectedConfidence = confidenceChoices.find(function (choice) { return choice.level === confidenceValue; }) || null;
                    var confidenceHelpId = 'diss-confidence-help-' + sel.id;
                    var evidenceHelpId = 'diss-evidence-help-' + sel.id;
                    var observePromptId = 'diss-observe-first-' + sel.id;
                    var countercheckStatusId = 'diss-countercheck-status-' + sel.id;
                    var noteText = String(noteValue).trim();
                    var noteLower = noteText.toLowerCase();
                    var countercheckStarter = {
                      id: 'countercheck',
                      label: __alloT('stem.dissection.countercheck_starter', 'Add a countercheck starter'),
                      text: __alloT('stem.dissection.countercheck_starter_text', 'I would revise this identification if I observed ')
                    };
                    var hasCountercheck = noteLower.indexOf(String(countercheckStarter.text).trim().toLowerCase()) >= 0;
                    var evidenceChecks = [
                      { id: 'observation', label: __alloT('stem.dissection.evidence_observation', 'Observable feature'), complete: noteText.length >= 12, cue: __alloT('stem.dissection.evidence_observation_cue', 'Describe a visible color, shape, texture, size, or movement.') },
                      { id: 'location', label: __alloT('stem.dissection.evidence_location', 'Location or relationship'), complete: /\b(located|location|dorsal|ventral|anterior|posterior|left|right|above|below|beneath|near|between|inside|outside|adjacent|connect(?:s|ed)?|cavity|toward)\b/.test(noteLower) || /\b(?:on|at|within)\s+(?:the\s+)?surface\b/.test(noteLower), cue: __alloT('stem.dissection.evidence_location_cue', 'Add where it is or what structure it touches or connects to.') },
                      { id: 'reasoning', label: __alloT('stem.dissection.evidence_reasoning', 'Reasoning link'), complete: /\b(because|therefore|supports?|indicates?|shows?|consistent with|so that|which means)\b/.test(noteLower), cue: __alloT('stem.dissection.evidence_reasoning_cue', 'Explain why those observations support this identification.') },
                      { id: 'confidence', label: __alloT('stem.dissection.evidence_confidence_check', 'Confidence rating'), complete: confidenceValue >= 1 && confidenceValue <= 3, cue: __alloT('stem.dissection.evidence_confidence_cue', 'Choose a confidence level from 1 to 3.') }
                    ];
                    var evidenceCompleteCount = evidenceChecks.filter(function (check) { return check.complete; }).length;
                    var evidenceReady = evidenceCompleteCount === evidenceChecks.length;
                    var nextEvidenceCheck = evidenceChecks.find(function (check) { return !check.complete; });
                    var evidenceStarters = [
                      { id: 'observation', label: __alloT('stem.dissection.evidence_starter_observation', '+ Observation'), text: __alloT('stem.dissection.evidence_starter_observation_text', 'I observed ') },
                      { id: 'location', label: __alloT('stem.dissection.evidence_starter_location', '+ Location'), text: __alloT('stem.dissection.evidence_starter_location_text', 'It is located ') },
                      { id: 'reasoning', label: __alloT('stem.dissection.evidence_starter_reasoning', '+ Reasoning'), text: __alloT('stem.dissection.evidence_starter_reasoning_text', 'This supports the identification because ') }
                    ];
                    function appendEvidenceStarter(starter) {
                      if (noteLower.indexOf(String(starter.text).trim().toLowerCase()) >= 0) return;
                      var separator = noteText ? (/\n$/.test(noteValue) ? '' : '\n') : '';
                      var notes = Object.assign({}, d.organNotes || {});
                      notes[evidenceKey] = String(noteValue) + separator + starter.text;
                      upd('organNotes', notes);
                      if (starter.id === 'countercheck' && typeof announceToSR === 'function') {
                        announceToSR(__alloT('stem.dissection.countercheck_starter_added_announcement', 'Countercheck starter added. Finish the sentence in your evidence note.'));
                      }
                      if (typeof requestAnimationFrame === 'function') requestAnimationFrame(function () {
                        var noteField = dissectionNodeById('diss-note-' + sel.id);
                        if (noteField) { noteField.focus(); noteField.setSelectionRange(noteField.value.length, noteField.value.length); }
                      });
                    }
                    return React.createElement("div", { className: "mt-3 rounded-lg border border-blue-200 bg-blue-50 p-3", "data-dissection-evidence": true },
                      React.createElement("label", { htmlFor: 'diss-note-' + sel.id, className: "block text-xs font-black text-blue-900" }, __alloT('stem.dissection.evidence_note', 'Evidence note')),
                      React.createElement("p", { id: evidenceHelpId, className: "text-xs text-blue-700 mt-1" }, __alloT('stem.dissection.evidence_note_help', 'Record what you observed, where it is located, and how that evidence supports your identification.')),
                      React.createElement("div", { className: "diss-evidence-starters", role: "group", "aria-label": __alloT('stem.dissection.evidence_sentence_starters', 'Evidence sentence starters') },
                        evidenceStarters.map(function (starter) {
                          return React.createElement("button", { type: "button", key: starter.id, "data-evidence-starter": starter.id, "aria-controls": 'diss-note-' + sel.id, onClick: function () { appendEvidenceStarter(starter); } }, starter.label);
                        })
                      ),
                      React.createElement("textarea", { id: 'diss-note-' + sel.id, className: "diss-evidence-note", rows: 3, value: noteValue, "aria-describedby": observePromptId + ' ' + evidenceHelpId, placeholder: __alloT('stem.dissection.evidence_note_placeholder', 'I identified this structure because…'), onChange: function (e) { var notes = Object.assign({}, d.organNotes || {}); notes[evidenceKey] = e.target.value; upd('organNotes', notes); } }),
                      React.createElement("fieldset", { className: "diss-confidence-scale", "aria-describedby": confidenceHelpId },
                        React.createElement("legend", { className: "diss-confidence-scale__label" }, __alloT('stem.dissection.confidence_question', 'How sure are you, based on your evidence?')),
                        React.createElement("div", { className: "diss-confidence-scale__choices" },
                          confidenceChoices.map(function (choice) {
                            return React.createElement("label", { key: choice.level, className: "diss-confidence-scale__option", "data-selected": confidenceValue === choice.level },
                              React.createElement("input", {
                                type: "radio", name: 'diss-confidence-' + evidenceKey, value: choice.level,
                                checked: confidenceValue === choice.level, "data-confidence-level": choice.level,
                                "aria-label": 'Confidence ' + choice.level + ' of 3: ' + choice.label,
                                onChange: function () { var confidence = Object.assign({}, d.organConfidence || {}); confidence[evidenceKey] = choice.level; upd('organConfidence', confidence); }
                              }),
                              React.createElement("span", null, choice.level + ' \u00B7 ' + choice.label)
                            );
                          })
                        )
                      ),
                      React.createElement("p", { id: confidenceHelpId, className: "diss-confidence-cue" },
                        React.createElement("strong", null, __alloT('stem.dissection.confidence_calibration_cue', 'Calibration cue') + ': '),
                        selectedConfidence ? selectedConfidence.cue : __alloT('stem.dissection.confidence_default_cue', 'Choose based on the evidence in your note, not on how familiar the answer feels.')
                      ),
                      React.createElement("div", { className: "diss-evidence-coach", "data-evidence-readiness": evidenceCompleteCount, "data-ready": evidenceReady, role: "group", "aria-label": __alloT('stem.dissection.evidence_quality_coach', 'Evidence quality coach') },
                        React.createElement("div", { className: "diss-evidence-coach__head" },
                          React.createElement("strong", null, __alloT('stem.dissection.evidence_self_check', 'Evidence self-check')),
                          React.createElement("span", { className: "diss-evidence-coach__score" }, evidenceCompleteCount + ' of ' + evidenceChecks.length + ' elements included')
                        ),
                        React.createElement("div", { className: "diss-evidence-meter", role: "progressbar", "aria-label": __alloT('stem.dissection.evidence_elements_included', 'Evidence elements included'), "aria-valuemin": 0, "aria-valuemax": evidenceChecks.length, "aria-valuenow": evidenceCompleteCount }, React.createElement("span", { style: { width: Math.round((evidenceCompleteCount / evidenceChecks.length) * 100) + '%' } })),
                        React.createElement("ul", { className: "diss-evidence-checks" }, evidenceChecks.map(function (check) {
                          return React.createElement("li", { key: check.id, "data-complete": check.complete }, (check.complete ? '\u2713 ' : '\u25CB ') + check.label);
                        })),
                        React.createElement("p", { className: "diss-evidence-coach__cue" }, evidenceReady ? __alloT('stem.dissection.evidence_complete_message', 'Your observation, location or relationship, reasoning, and confidence are included. Compare your note with the reference, revise anything it changes, or try the optional countercheck below.') : __alloT('stem.dissection.evidence_next_prefix', 'Next:') + ' ' + nextEvidenceCheck.cue),
                        React.createElement("p", { className: "diss-evidence-coach__note" }, __alloT('stem.dissection.evidence_coaching_limit', 'This checklist detects writing elements, not scientific accuracy or a grade. Revise it when new evidence changes your thinking.'))
                      ),
                      evidenceReady && React.createElement("details", { className: "diss-countercheck diss-disclosure", "data-evidence-countercheck": true },
                        React.createElement("summary", null, __alloT('stem.dissection.countercheck_title', 'Optional challenge: test your identification')),
                        React.createElement("div", { className: "diss-disclosure__body" },
                          React.createElement("p", null, __alloT('stem.dissection.countercheck_prompt', 'What different feature, location, or connection would make this identification less likely\u2014or suggest a different structure?')),
                          React.createElement("button", {
                            type: "button", "data-evidence-countercheck-action": true,
                            "aria-controls": 'diss-note-' + sel.id, "aria-describedby": countercheckStatusId,
                            disabled: hasCountercheck, onClick: function () { appendEvidenceStarter(countercheckStarter); }
                          }, hasCountercheck ? __alloT('stem.dissection.countercheck_starter_added', 'Countercheck starter added') : countercheckStarter.label),
                          React.createElement("p", { id: countercheckStatusId, className: "diss-countercheck__status" },
                            hasCountercheck
                              ? __alloT('stem.dissection.countercheck_finish', 'Finish the countercheck sentence in your evidence note.')
                              : __alloT('stem.dissection.countercheck_unscored', 'This reflection does not change your 4-of-4 evidence status.')
                          )
                        )
                      )
                    );
                  })(),
                  // Action buttons

                  React.createElement("div", { className: "mt-2 flex gap-1" },

                    React.createElement("button", { onClick: function () {

                        if (typeof callGemini === 'function') {

                          var _aiPrompt = 'Explain the ' + sel.name + ' in a ' + spec.name + ' for a biology student. Clearly separate specimen-specific anatomy from any human comparison. Include function, location, one observable identification clue, and cite uncertainty when species details vary.';
                          callGemini(_aiPrompt).then(function(res) {
                            if (addToast) addToast('\uD83E\uDD16 ' + sel.name + ': ' + (res || 'Response received'), 'info', 8000);
                          }).catch(function(err) {
                            if (addToast) addToast('\uD83E\uDD16 AI unavailable: ' + (err.message || err), 'error');
                          });

                        } else if (addToast) {

                          addToast('\uD83E\uDD16 AI not available. ' + sel.name + ': ' + sel.fn.split('.')[0] + '.', 'info');

                        }

                      },

                      className: "flex-1 px-2 py-1.5 rounded-lg text-[11px] font-bold bg-gradient-to-r from-violet-600 to-purple-600 text-white hover:from-violet-600 hover:to-purple-600"

                    }, '\uD83E\uDD16 ' + 'AI Explain'),

                    React.createElement("button", { onClick: function () {

                        var text = sel.name + '\n' + sel.fn;

                        if (sel.clinical) text += '\n\nFun Fact: ' + sel.clinical;

                        copyDissectionText(text, sel.name + ' information copied to the clipboard.', 'Could not copy ' + sel.name + ' information in this view.');

                      },

                      "aria-label": "Copy " + sel.name + " information to clipboard",

                      className: "transition-colors px-2 py-1.5 rounded-lg text-[11px] font-bold bg-slate-100 text-slate-600 hover:bg-slate-200 active:scale-[0.97]"

                    }, '\uD83D\uDCCB ' + 'Copy')

                  )

                ),



                // Organ list with search

                !sel && !d.quizMode && React.createElement("section", { className: "diss-structure-list bg-white rounded-xl border p-3", "data-dissection-directory": true, "aria-labelledby": "diss-directory-title" },

                  React.createElement("h3", { id: "diss-directory-title", className: "text-sm font-black text-slate-800 mb-2" }, (spec.layers[currentLayerIdx] || {}).icon + ' ' + (spec.layers[currentLayerIdx] || {}).name + ' structures (' + organs.length + ')'),

                  React.createElement("input", {

                    id: "diss-organ-search",

                    type: "text",

                    placeholder: 'Search organs...',

                    "aria-label": "Search organs in this layer",
                    "aria-controls": "diss-directory-results",
                    "aria-describedby": "diss-directory-count",

                    value: d.organSearch || '',

                    onChange: function (e) { upd('organSearch', e.target.value); },

                    className: "w-full min-h-11 px-3 py-2 rounded-lg border border-slate-400 text-sm mb-2 focus:outline-none focus:ring-2 focus:ring-blue-400"

                  }),

                  React.createElement("p", { id: "diss-directory-count", className: "diss-directory-status", role: "status", "aria-live": "polite", "aria-atomic": "true" },
                    organSearchText ? (filteredOrgans.length + ' matching ' + (filteredOrgans.length === 1 ? 'structure' : 'structures') + ' in ' + currentLayerDef.name + '.') : (organs.length + ' structures available in ' + currentLayerDef.name + '.')
                  ),
                  React.createElement("div", { id: "diss-directory-results", className: "space-y-1 max-h-72 overflow-y-auto", "aria-busy": "false", tabIndex: 0, role: "region", "aria-label": "Directory results" },

                    filteredOrgans.length === 0
                      ? React.createElement("p", { className: "diss-directory-empty" }, 'No structures match “' + (d.organSearch || '') + '”. Try a structure name, system, or function.')
                      : filteredOrgans.map(function (org) {

                      var orgSys = null;

                      var orgName = org.name.toLowerCase();

                      var sysKeys = ['circulatory', 'digestive', 'respiratory', 'nervous', 'skeletal', 'muscular', 'excretory', 'reproductive'];

                      var sysKws = { circulatory: ['heart', 'aorta', 'artery', 'vein', 'atrium', 'ventricle', 'blood', 'aortic'], digestive: ['stomach', 'liver', 'intestin', 'gizzard', 'crop', 'pancreas', 'gallbladder'], respiratory: ['lung', 'gill', 'trachea', 'swim bladder'], nervous: ['brain', 'nerve', 'spinal', 'eye', 'optic'], skeletal: ['bone', 'skull', 'vertebr', 'femur'], muscular: ['muscle', 'rectus'], excretory: ['kidney', 'nephri'], reproductive: ['gonad', 'ovary', 'testi', 'oviduct'] };

                      for (var si = 0; si < sysKeys.length; si++) {

                        var kws = sysKws[sysKeys[si]];

                        for (var ki = 0; kws && ki < kws.length; ki++) {

                          if (orgName.indexOf(kws[ki]) >= 0) { orgSys = sysKeys[si]; break; }

                        }

                        if (orgSys) break;

                      }

                      var sysColorsMap = { circulatory: '#ef4444', digestive: '#f59e0b', respiratory: '#3b82f6', nervous: '#8b5cf6', skeletal: '#94a3b8', muscular: '#dc2626', excretory: '#84cc16', reproductive: '#ec4899' };

                      var dotColor = orgSys ? sysColorsMap[orgSys] : '#94a3b8';

                      var isExplored = (d.exploredOrgans || {})[specimen + '|' + org.id];
                      var directoryExposureState = structureExposureState(org, currentProcedure);
                      var directoryVisible = directoryExposureState === 'visible';

                      var directoryMarkerNumber = Math.max(1, organs.indexOf(org) + 1);
                      return React.createElement("button", { key: org.id,

                        id: 'diss-organ-' + org.id,

                        type: "button",
                        "aria-pressed": d.selectedOrgan === org.id,
                        "aria-disabled": directoryVisible || isExplored ? undefined : true,
                        "data-exposure-state": directoryExposureState,
                        "aria-label": org.name + (directoryVisible ? (isExplored ? ', viewed' : ', not yet viewed') : (isExplored ? ', previously viewed; review evidence' : ', not currently visible; preview only')),
                        title: directoryVisible ? (isExplored ? 'Explored' : 'Visible and ready to inspect') : (isExplored ? 'Review previously recorded evidence; no new exploration credit.' : 'Not currently visible. Activate for a recovery cue.'),
                        onClick: function () { chooseOrganFromDirectory(org); },

                        className: "w-full min-h-11 text-left px-3 py-2 rounded-lg text-xs hover:bg-slate-50 transition-all flex items-center gap-2 active:scale-[0.99] " + (!directoryVisible ? 'bg-slate-50 text-slate-500 opacity-75 ' : '') + (d.selectedOrgan === org.id ? 'bg-amber-50 border border-amber-200 font-bold text-amber-800' : 'text-slate-700')

                      },

                        React.createElement("span", { className: "diss-directory-index", style: { '--diss-system-color': dotColor }, "aria-hidden": "true" }, String(directoryMarkerNumber)),

                        React.createElement("span", { className: "flex-1" }, org.name),

                        !directoryVisible ? React.createElement("span", { className: "text-[11px] font-bold text-slate-500" }, isExplored ? 'Prior evidence' : 'Not visible')
                          : isExplored && React.createElement("span", { className: "text-[11px] text-green-500" }, '\u2713')

                      );

                    })

                  )

                ),



                // Quiz card

                d.quizMode && quizQ && React.createElement("section", { id: "diss-quiz-panel", tabIndex: -1, className: "bg-gradient-to-br from-amber-50 to-yellow-50 rounded-xl border border-amber-200 p-4", "data-dissection-quiz": true, "aria-labelledby": "diss-quiz-title" },

                  React.createElement("div", { className: "flex items-center gap-2 mb-2" },

                    React.createElement("h3", { id: "diss-quiz-title", className: "text-sm font-black text-amber-900" }, '\uD83E\uDDE0 ' + 'Identify'),

                    React.createElement("span", { className: "text-[11px] font-bold text-green-700 ml-auto" }, d.practicalMode
                      ? ('Recorded ' + (d.quizTotal || 0))
                      : ('First attempt ' + assessmentFirstAttemptScore + '/' + assessmentFirstAttemptTotal + ' · Question ' + Math.min(quizSessionLimit, (Number(d.quizTotal) || 0) + 1) + '/' + quizSessionLimit))

                  ),

                  React.createElement("p", { className: "text-xs text-amber-700 mb-2" }, quizPrompt),
                  React.createElement("div", { className: "flex flex-wrap gap-2 mb-3", role: "group", "aria-label": "Answer method" },
                    React.createElement("button", { type: "button", "aria-pressed": effectiveQuizAnswerMode === 'choices', onClick: function () { upd('quizAnswerMode', 'choices'); }, className: "px-3 py-1 rounded-lg text-xs font-bold border " + (effectiveQuizAnswerMode === 'choices' ? 'bg-amber-700 text-white border-amber-700' : 'bg-white text-amber-800 border-amber-300') }, d.practicalMode ? 'Accessible choices' : 'Multiple choice'),
                    React.createElement("button", { type: "button", disabled: !hotspotQuizAvailable, title: hotspotQuizAvailable ? 'Answer directly on the visible specimen' : 'The current question is not visible on the specimen; use multiple choice', "aria-pressed": effectiveQuizAnswerMode === 'hotspot', onClick: function () { upd('quizAnswerMode', 'hotspot'); }, className: "px-3 py-1 rounded-lg text-xs font-bold border " + (effectiveQuizAnswerMode === 'hotspot' ? 'bg-amber-700 text-white border-amber-700' : 'bg-white text-amber-800 border-amber-300') }, hotspotQuizAvailable ? 'Select on specimen' : 'Specimen answer unavailable')
                  ),
                  d.quizAnswerMode === 'hotspot' && !hotspotQuizAvailable && React.createElement("p", { className: "text-xs font-bold text-amber-800 mb-2", role: "status" }, 'This question is not visible in the current field, so multiple choice is active.'),
                  effectiveQuizAnswerMode === 'hotspot' && !d.quizFeedback && React.createElement("p", { className: "text-xs font-bold text-blue-700 mb-2", role: "status" }, 'Select the matching visible structure to submit. Keyboard: use Arrow keys to move an unlabeled candidate marker, then Enter to submit. Accessible answer choices remain available.'),

                  effectiveQuizAnswerMode === 'choices' && React.createElement("div", { className: "grid grid-cols-1 sm:grid-cols-2 gap-2" },

                    quizOptions.map(function (opt) {

                      var fb = d.quizFeedback;

                      var isCorrect = fb && !d.practicalMode && opt.id === quizQ.id;

                      var isChosen = (fb && fb.chosen === opt.id) || (!fb && d.quizRetry && d.quizRetry.questionId === quizQ.id && d.quizRetry.chosen === opt.id);

                      var isWrong = !d.practicalMode && isChosen && !isCorrect;

                      return React.createElement("button", { type: "button", key: opt.id, disabled: !!fb, "aria-label": opt.name + (isCorrect ? ', correct answer' : isWrong ? ', selected answer, incorrect' : ''),

                        onClick: function () { submitQuizAnswer(opt.id); },


                        className: "min-h-11 px-3 py-2 rounded-lg text-xs font-bold border transition-all " + (isCorrect ? 'border-green-400 bg-green-50 text-green-700' : isWrong ? 'border-red-400 bg-red-50 text-red-700' : fb ? 'border-slate-200 bg-slate-50 text-slate-600' : 'transition-colors border-amber-200 bg-white text-slate-700 hover:border-amber-400')

                      }, opt.name);

                    })

                  ),
                  !d.practicalMode && d.quizRetry && !d.quizFeedback && React.createElement("div", { className: "mt-3 rounded-lg border border-amber-300 bg-amber-50 p-2.5 text-amber-900", role: "status" },
                    React.createElement("p", { className: "text-xs font-semibold" }, d.quizRetry.message),
                    React.createElement("button", {
                      type: "button",
                      onClick: revealQuizAnswer,
                      className: "mt-2 min-h-11 w-full rounded-lg border border-amber-500 bg-white px-3 py-2 text-xs font-bold text-amber-900 hover:bg-amber-100"
                    }, 'Show answer \u00B7 supported practice')
                  ),

                  !d.practicalMode && d.quizFeedback && React.createElement("div", {
                    className: "mt-3 p-2.5 rounded-lg border " + (d.quizFeedback.correct ? 'bg-emerald-50 border-emerald-300 text-emerald-800' : 'bg-rose-50 border-rose-300 text-rose-800'),
                    role: "status",
                    "aria-live": "assertive"
                  }, d.quizFeedback.correct ? ((d.quizFeedback.supported ? 'Correct with support — ' : 'Correct — ') + quizQ.name + '.') : ('Not quite. The correct structure is ' + quizQ.name + '.')),
                  d.practicalMode && d.quizFeedback && React.createElement("div", {
                    className: "mt-3 rounded-lg border border-blue-300 bg-blue-50 p-2.5 text-blue-900",
                    role: "status",
                    "aria-live": "assertive"
                  }, 'Response recorded. Correctness and the running score stay hidden while the timed practical is active.'),

                  d.quizFeedback && React.createElement("button", { type: "button", "aria-label": !d.practicalMode && (Number(d.quizTotal) || 0) >= quizSessionLimit ? "View assessment summary" : "Next Question",

                    onClick: function () {
                      var sessionFinished = !d.practicalMode && (Number(d.quizTotal) || 0) >= quizSessionLimit;
                      var completedReviewIds = d.quizReviewMode ? orderedQuizPool.slice(0, quizSessionLimit).map(function (organ) { return organ.id; }) : [];
                      var remainingReviewIds = d.quizReviewMode ? quizReviewIds.filter(function (id) { return completedReviewIds.indexOf(id) < 0; }) : quizReviewIds;
                      if (sessionFinished) updMany({ quizComplete: true, quizFeedback: null, quizExplanation: null, quizRetry: null, selectedOrgan: null, hoveredOrgan: null, quizReviewQueue: remainingReviewIds, assessmentCompletedAt: d.quizReviewMode ? (Number(d.assessmentCompletedAt) || 0) : Date.now(), assessmentRecordedScore: d.quizReviewMode ? assessmentRecordedScore : assessmentFirstAttemptScore, assessmentRecordedTotal: d.quizReviewMode ? assessmentRecordedTotal : assessmentFirstAttemptTotal });
                      else updMany({ quizIdx: (d.quizIdx || 0) + 1, quizFeedback: null, quizExplanation: null, quizRetry: null, selectedOrgan: null, hoveredOrgan: null });
                      setTimeout(function () { var panel = dissectionNodeById(sessionFinished ? 'diss-assessment-summary' : 'diss-quiz-panel'); if (panel) panel.focus(); }, 0);
                    },

                    className: "transition-colors w-full min-h-11 mt-2 py-2 rounded-lg text-xs font-bold bg-amber-700 text-white hover:bg-amber-800 active:scale-[0.97]"

                  }, (!d.practicalMode && (Number(d.quizTotal) || 0) >= quizSessionLimit ? 'View assessment summary' : 'Next Question') + ' \u2192'),

                  d.quizExplanation && React.createElement("div", { className: "mt-2 p-2 rounded-lg bg-white border border-amber-200" },
                    React.createElement("span", { className: "text-[11px] font-bold text-amber-600" }, '\uD83D\uDCA1 '),
                    React.createElement("span", { className: "text-[11px] text-slate-600 leading-relaxed" }, d.quizExplanation)
                  )

                ),



                d.quizMode && !d.practicalMode && d.quizComplete && React.createElement("section", { id: "diss-assessment-summary", tabIndex: -1, className: "rounded-xl border border-emerald-300 bg-gradient-to-br from-emerald-50 to-cyan-50 p-4", "data-dissection-assessment-summary": true, "aria-labelledby": "diss-assessment-summary-title" },
                  React.createElement("h3", { id: "diss-assessment-summary-title", className: "text-sm font-black text-emerald-900" }, d.quizReviewMode ? (quizReviewIds.length ? 'Supported review set complete' : 'Supported review complete') : 'Assessment complete'),
                  React.createElement("p", { className: "mt-1 text-xs font-bold text-emerald-800" }, 'First-attempt evidence: ' + (d.quizReviewMode ? assessmentRecordedScore : assessmentFirstAttemptScore) + '/' + (d.quizReviewMode ? assessmentRecordedTotal : assessmentFirstAttemptTotal) + ' · ' + assessmentAccuracyPct + '% accuracy'),
                  React.createElement("p", { className: "mt-1 text-xs text-slate-700" }, (Number(d.quizSupportedCount) || 0) + ' question' + ((Number(d.quizSupportedCount) || 0) === 1 ? '' : 's') + ' completed with retry or answer support. Supported practice is reported separately from first-attempt mastery.'),
                  quizReviewIds.length
                    ? React.createElement("p", { className: "mt-2 text-xs text-amber-900" }, 'Review next: ' + quizReviewIds.map(function (id) { var organ = specimenQuizPool.find(function (candidate) { return candidate.id === id; }); return organ ? organ.name : id; }).join(', ') + '.')
                    : React.createElement("p", { className: "mt-2 text-xs text-emerald-800" }, 'No missed structures are waiting for supported review.'),
                  React.createElement("p", { className: "mt-2 text-xs font-semibold text-slate-700" }, assessmentAccuracyPct >= 80 && assessmentFirstAttemptTotal >= assessmentQuestionGoal
                    ? 'Evidence meets the current first-attempt understanding target. Continue linking answers to notes and verified identifications.'
                    : 'Revisit missed structures, record evidence, then try a new scored set when ready.'),
                  React.createElement("div", { className: "mt-3 grid gap-2 sm:grid-cols-2" },
                    React.createElement("button", { type: "button", className: "min-h-11 rounded-lg border border-emerald-600 bg-white px-3 py-2 text-xs font-bold text-emerald-800", onClick: function () { setStudyRoute('explore'); } }, 'Return to lab'),
                    quizReviewIds.length > 0 && React.createElement("button", { type: "button", className: "min-h-11 rounded-lg border border-amber-600 bg-white px-3 py-2 text-xs font-bold text-amber-900", onClick: function () { restartAssessmentSession(true); } }, d.quizReviewMode ? 'Review remaining structures' : 'Review missed structures'),
                    React.createElement("button", { type: "button", className: "min-h-11 rounded-lg bg-emerald-700 px-3 py-2 text-xs font-bold text-white", onClick: function () { restartAssessmentSession(false); } }, 'Try a new assessment')
                  )
                ),

                // Layer + specimen info

                React.createElement("div", { className: "bg-slate-50 rounded-xl border p-3" },

                  React.createElement("div", { className: "text-[11px] font-bold text-slate-600 mb-1" }, 'Layer Progress'),

                  spec.layers.map(function (layer) {

                    var done = !!revealedLayers[layer.id];

                    return React.createElement("div", { key: layer.id, className: "flex items-center gap-2 py-0.5" },

                      React.createElement("span", { className: "text-[11px] " + (done ? 'line-through text-slate-600' : 'text-slate-600') }, layer.icon + ' ' + layer.name),

                      done && React.createElement("span", { className: "text-[11px] text-green-500 ml-auto" }, '\u2713')

                    );

                  }),

                  React.createElement("div", { className: "mt-2 pt-2 border-t border-slate-200" },

                    React.createElement("p", { className: "text-[11px] text-slate-600 leading-relaxed mb-1" }, __alloT('stem.dissection.' + (specimen) + '_desc', spec.desc)),
                    React.createElement("p", { className: "diss-science-scope" }, __alloT('stem.dissection.comparative_model_notice', 'Comparative learning model: specimen observations and human clinical connections are labeled separately. Anatomy varies by species, age, preservation, and individual.')),

                    React.createElement("div", { className: "grid grid-cols-2 gap-1 mt-1" },

                      spec.kingdom && React.createElement("div", null,

                        React.createElement("span", { className: "text-[11px] font-bold text-slate-600 uppercase" }, 'Kingdom'),

                        React.createElement("p", { className: "text-[11px] text-slate-600" }, spec.kingdom)

                      ),

                      spec.phylum && React.createElement("div", null,

                        React.createElement("span", { className: "text-[11px] font-bold text-slate-600 uppercase" }, 'Phylum'),

                        React.createElement("p", { className: "text-[11px] text-slate-600" }, spec.phylum)

                      ),

                      spec.habitat && React.createElement("div", null,

                        React.createElement("span", { className: "text-[11px] font-bold text-slate-600 uppercase" }, 'Habitat'),

                        React.createElement("p", { className: "text-[11px] text-slate-600" }, spec.habitat)

                      ),

                      spec.lifespan && React.createElement("div", null,

                        React.createElement("span", { className: "text-[11px] font-bold text-slate-600 uppercase" }, 'Lifespan'),

                        React.createElement("p", { className: "text-[11px] text-slate-600" }, spec.lifespan)

                      )

                    )

                  )

                ),




                // "Did You Know?" rotating trivia panel
                (function () {
                  var facts = SPECIMEN_TRIVIA[specimen] || [];
                  if (facts.length === 0) return null;
                  var factIdx = Math.floor((Date.now() / 8000)) % facts.length;
                  return React.createElement("details", { className: "diss-disclosure diss-disclosure--inset bg-gradient-to-r from-amber-50 to-orange-50 border border-amber-200" }, React.createElement("summary", { className: "text-amber-800" }, 'Biology fact'),
                    React.createElement("div", { className: "flex items-center gap-1.5 mb-1" },
                      React.createElement("span", { className: "text-sm" }, '\uD83D\uDCA1'),
                      React.createElement("span", { className: "text-[11px] font-bold text-amber-700" }, 'Did You Know?')
                    ),
                    React.createElement("p", { className: "text-[11px] text-amber-600 leading-relaxed" }, facts[factIdx]),
                    React.createElement("div", { className: "flex gap-0.5 mt-1.5 justify-center" },
                      facts.map(function (_, fi) {
                        return React.createElement("div", {
                          key: fi,
                          className: "w-1 h-1 rounded-full " + (fi === factIdx ? 'bg-amber-500' : 'bg-amber-200')
                        });
                      })
                    )
                  );
                })(),

                // Progress card

                React.createElement("div", { className: "bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl border border-blue-200 p-3" },

                  React.createElement("div", { className: "flex items-center justify-between mb-1" },

                    React.createElement("span", { className: "text-[11px] font-bold text-blue-700" }, '\uD83D\uDCCA Learning evidence'),

                    React.createElement("span", { className: "text-[11px] font-bold " + (masteryComplete ? 'text-green-700' : 'text-blue-700') }, masteryOverallPct + '%')

                  ),

                  React.createElement("div", { className: "w-full h-2 bg-blue-100 rounded-full overflow-hidden" },

                    React.createElement("div", { role: "progressbar", "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": progressPct, "aria-label": "Observation: structures viewed " + exploredCount + " of " + totalOrgansInSpecimen, className: "h-full rounded-full transition-all duration-500 " + (progressPct >= 100 ? 'bg-green-700' : 'bg-blue-500'), style: { width: progressPct + '%' } })

                  ),

                  React.createElement("div", { className: "mt-1 text-[11px] text-blue-700" }, 'Observation: ' + exploredCount + ' of ' + totalOrgansInSpecimen + ' structures viewed'),
                  React.createElement("div", { className: "mt-2 grid grid-cols-2 gap-2", "aria-label": "Learning evidence dimensions" },
                    masteryDimensions.map(function (dimension) {
                      return React.createElement("div", { key: dimension.id, className: "rounded-lg border border-blue-100 bg-white/80 p-2" },
                        React.createElement("div", { className: "flex items-center justify-between gap-2 text-[11px]" },
                          React.createElement("span", { className: "font-semibold text-slate-700" }, dimension.label),
                          React.createElement("span", { className: "font-bold text-blue-800" }, dimension.value + '%')
                        ),
                        React.createElement("div", { className: "mt-1 h-1.5 overflow-hidden rounded-full bg-blue-100" },
                          React.createElement("div", { role: "progressbar", "aria-label": dimension.label + ' evidence', "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": dimension.value, className: "h-full rounded-full " + (dimension.value >= 100 ? 'bg-green-700' : 'bg-blue-600'), style: { width: dimension.value + '%' } })
                        )
                      );
                    })
                  ),
                  React.createElement("div", { className: "mt-1 text-[11px] text-slate-600" }, verifiedIdentificationCount + '/' + verifiedIdentificationGoal + ' guided identifications verified \u00B7 ' + specimenEvidenceCount + '/' + totalOrgansInSpecimen + ' evidence records \u00B7 completed first-attempt assessment ' + assessmentRecordedScore + '/' + assessmentRecordedTotal),
                  !d.quizMode && quizReviewIds.length > 0 && React.createElement("button", { type: "button", className: "mt-2 min-h-11 w-full rounded-lg border border-amber-500 bg-amber-50 px-3 py-2 text-xs font-bold text-amber-900 hover:bg-amber-100", onClick: function () { restartAssessmentSession(true); } }, 'Review ' + quizReviewIds.length + ' missed ' + (quizReviewIds.length === 1 ? 'structure' : 'structures')),

                  React.createElement("div", { className: "mt-2" },

                    React.createElement("div", { className: "text-[11px] font-bold " + (masteryComplete ? 'text-green-700' : 'text-amber-800') }, masteryComplete ? '\u2713 Verified investigation complete' : (progressPct >= 100 ? 'Viewing milestone reached' : 'Practice in progress')),

                    React.createElement("div", { className: "text-[11px] text-emerald-700 mt-0.5" },

                      masteryComplete ? 'Technique, observation, evidence, and understanding all meet the practice criteria.' : (progressPct >= 100 ? 'Viewing every structure is a milestone, not proof of identification. Complete the remaining evidence and understanding checks.' : 'Continue the investigation; the log distinguishes viewing from verified understanding.')

                    ),

                    React.createElement("button", { type: "button", "aria-label": masteryComplete ? "Copy verified dissection completion summary" : "Copy dissection practice log",

                      onClick: function () {

                        var cert = (masteryComplete ? 'Verified Dissection Completion Summary' : 'Dissection Practice Log') + '\n';

                        cert += '\u2500'.repeat(40) + '\n';

                        cert += 'Specimen' + ': ' + spec.icon + ' ' + spec.name + '\n';

                        cert += 'Observation: ' + progressPct + '% (' + exploredCount + '/' + totalOrgansInSpecimen + ' viewed)\n';
                        cert += 'Technique: ' + techniqueLearningPct + '%\n';
                        cert += 'Evidence: ' + evidenceProgressPct + '% (' + specimenEvidenceCount + '/' + totalOrgansInSpecimen + ' records)\n';
                        cert += 'Understanding: ' + understandingProgressPct + '% (' + verifiedIdentificationCount + ' guided identifications verified)\n';

                        cert += 'Layers revealed: ' + revealedLayerCount + '/' + spec.layers.length + '\n';

                        cert += 'Completed first-attempt assessment: ' + assessmentRecordedScore + '/' + assessmentRecordedTotal + '; supported practice: ' + (Number(d.quizSupportedCount) || 0) + '\n';

                        cert += 'Date' + ': ' + new Date().toLocaleDateString() + '\n';

                        cert += '\u2500'.repeat(40) + '\n';

                        cert += masteryComplete ? 'Verified completion criteria met' : 'Practice in progress; completion criteria not yet met';

                        copyDissectionText(cert, masteryComplete ? 'Verified completion summary copied to the clipboard.' : 'Practice log copied to the clipboard.', masteryComplete ? 'Could not copy the verified completion summary in this view.' : 'Could not copy the practice log in this view.');

                      },

                      "aria-label": masteryComplete ? "Copy verified completion summary to clipboard" : "Copy practice log to clipboard",

                      className: "mt-1 px-2 py-1 rounded-lg text-[11px] font-bold bg-gradient-to-r from-green-700 to-emerald-700 text-white"

                    }, masteryComplete ? '\uD83C\uDF93 Copy verified summary' : 'Copy practice log')

                  )

                ),



                // Specimen stats card

                React.createElement("details", { className: "diss-disclosure diss-disclosure--inset bg-gradient-to-br from-slate-50 to-gray-50 border border-slate-400" }, React.createElement("summary", { className: "text-slate-800" }, 'Specimen stats'),

                  React.createElement("div", { className: "text-[11px] font-bold text-slate-700 mb-1" }, '\uD83D\uDCC8 ' + 'Specimen Stats'),

                  React.createElement("div", { className: "grid grid-cols-3 gap-2 text-center" },

                    React.createElement("div", null,

                      React.createElement("div", { className: "text-lg font-bold text-blue-600 tracking-tight" }, String(totalOrgansInSpecimen)),

                      React.createElement("div", { className: "text-[11px] text-slate-600" }, 'Structures')

                    ),

                    React.createElement("div", null,

                      React.createElement("div", { className: "text-lg font-bold text-emerald-600 tracking-tight" }, String(spec.layers.length)),

                      React.createElement("div", { className: "text-[11px] text-slate-600" }, 'Layers')

                    ),

                    React.createElement("div", null,

                      React.createElement("div", { className: "text-lg font-bold text-amber-600 tracking-tight" }, String(assessmentRecordedScore)),

                      React.createElement("div", { className: "text-[11px] text-slate-600" }, 'First-attempt score')

                    ),

                    React.createElement("div", null,

                      React.createElement("div", { className: "text-lg font-bold text-violet-600 tracking-tight" }, (function () {

                        var ts = d.timeSpent || 0;

                        return ts < 60 ? ts + 's' : Math.floor(ts / 60) + 'm';

                      })()),

                      React.createElement("div", { className: "text-[11px] text-slate-600" }, 'Time')

                    )

                  )

                ),

                // Learning Objectives

                React.createElement("details", { className: "diss-disclosure bg-gradient-to-br from-emerald-50 to-teal-50 border border-emerald-200" },

                  React.createElement("summary", { className: "text-emerald-800" }, 'Learning objectives'),
                  React.createElement("div", { className: "diss-disclosure__body space-y-1" },

                    (spec.objectives || [

                      'Identify major organs and their functions',

                      'Compare organ systems across body layers',

                      'Trace the digestive pathway from ingestion to excretion',

                      'Trace the respiratory pathway and gas exchange',

                      'Locate and name all structures in each layer',

                      'Explain how structure relates to function',

                      'Compare homologous organs across specimens'

                    ]).map(function (obj, oi) {

                      var objectiveModel = objectiveEvidenceModels[oi] || { status: 'not-started', progress: 0, observedCount: 0, recordedCount: 0, understoodCount: 0, targetCount: 1, nextEvidence: 'Inspect and record evidence for the objective target.' };
                      var objectiveTargetNoun = objectiveModel.targetCount === 1 ? 'target' : 'targets';
                      var objectiveRecordNoun = objectiveModel.targetCount === 1 ? 'evidence record' : 'evidence records';

                      return React.createElement("article", { key: oi, className: "diss-objective-item", "data-objective-status": objectiveModel.status },
                        React.createElement("div", { className: "diss-objective-item__head" },
                          React.createElement("strong", null, obj),
                          React.createElement("span", { className: "diss-objective-item__status" }, objectiveModel.status === 'demonstrated' ? '✓ Demonstrated' : (objectiveModel.status === 'developing' ? 'Developing' : 'Not started'))
                        ),
                        React.createElement("div", { className: "diss-objective-item__meter", role: "progressbar", "aria-label": "Evidence progress for objective " + (oi + 1), "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": objectiveModel.progress },
                          React.createElement("span", { style: { width: objectiveModel.progress + '%' } })
                        ),
                        React.createElement("p", null, objectiveModel.observedCount + '/' + objectiveModel.targetCount + ' ' + objectiveTargetNoun + ' observed · ' + objectiveModel.recordedCount + '/' + objectiveModel.targetCount + ' ' + objectiveRecordNoun + ' · ' + objectiveModel.understoodCount + '/' + objectiveModel.targetCount + ' ' + objectiveTargetNoun + ' verified for understanding.'),
                        React.createElement("p", null, objectiveModel.nextEvidence)
                      );

                    })

                  )

                ),

                // ══ DISSECTION INQUIRY widget (H7b'') ══
                (function() {
                  var iq = normalizeDissectionInquiry(d.dissInquiry);
                  function setIQ(patch) { upd('dissInquiry', normalizeDissectionInquiry(Object.assign({}, iq, patch))); }
                  function setKey(k, v) { var p = {}; p[k] = v; setIQ(p); }
                  var inquiryMetrics = dissectionInquiryMetrics(iq);
                  var damage = inquiryMetrics.damage;
                  var insight = inquiryMetrics.insight;
                  var state = inquiryMetrics.state;
                  var sm = ({
                    destroyed: { label: dissectionInquiryStateLabel('destroyed'), color: '#fca5a5', bg: '#2a0a0a', border: '#dc2626', desc: 'The heuristic predicts severe damage and obscured relationships. Compare a smaller depth or lower time pressure.' },
                    damaged: { label: dissectionInquiryStateLabel('damaged'), color: '#fdba74', bg: '#2a1a0a', border: '#ea580c', desc: 'The heuristic predicts that damage may obscure key relationships. Compare a slower or more careful approach.' },
                    compromised: { label: dissectionInquiryStateLabel('compromised'), color: '#fde047', bg: '#2a2410', border: '#eab308', desc: 'The heuristic predicts some visible relationships alongside substantial technique risk.' },
                    surface: { label: dissectionInquiryStateLabel('surface'), color: '#cbd5e1', bg: '#1e293b', border: '#64748b', desc: 'The heuristic predicts low damage risk but limited access beyond external anatomy.' },
                    good: { label: dissectionInquiryStateLabel('good'), color: '#67e8f9', bg: '#0a1f2e', border: '#0891b2', desc: 'The heuristic predicts a useful balance of access, care, and time pressure.' },
                    excellent: { label: dissectionInquiryStateLabel('excellent'), color: '#86efac', bg: '#0a2e1a', border: '#16a34a', desc: 'The heuristic predicts high insight with controlled technique; compare whether the assumptions are realistic.' }
                  })[state];
                  return React.createElement("details", { className: "diss-advanced-only diss-disclosure diss-disclosure--inset rounded-xl", "data-dissection-inquiry": true, style: { background: sm.bg, border: '1px solid ' + sm.border, color: '#e8f0f5' } }, React.createElement("summary", { style: { color: sm.color } }, 'Advanced inquiry simulator'),
                    React.createElement("h4", { id: "diss-inquiry-title", className: "text-xs font-black uppercase tracking-wider mb-1", style: { color: sm.color } }, '🔬 Dissection Inquiry — Explore the Outcome'),
                    React.createElement("p", { className: "text-[10px] opacity-85 mb-2 leading-snug" }, 'Set specimen size, dissection depth, care level, and time pressure, then observe how the modeled outcome changes. The result updates live; record a hypothesis or pattern you notice.'),
                    React.createElement("div", { id: "diss-inquiry-result", role: "status", "aria-live": "polite", "aria-atomic": "true" },
                      React.createElement("div", { className: "inline-block px-2 py-1 rounded-full text-[10px] font-bold mb-2", style: { background: sm.color, color: '#000' } }, 'Modeled outcome: ' + sm.label),
                      React.createElement("p", { className: "text-[10px] mb-2" }, sm.desc),
                      React.createElement("div", { className: "grid grid-cols-2 gap-2 mb-2" },
                        [
                          { label: 'Damage index', val: damage.toFixed(1) },
                          { label: 'Insight score', val: insight.toFixed(1) }
                        ].map(function(m) {
                          return React.createElement("div", { key: m.label, className: "p-1 rounded text-center", style: { background: '#0a0a1a', border: '1px solid ' + sm.border } },
                            React.createElement("div", { className: "text-[10px]" }, m.label),
                            React.createElement("div", { className: "text-[11px] font-bold font-mono", style: { color: sm.color } }, m.val)
                          );
                        })
                      )
                    ),
                    React.createElement("svg", { role: 'img', 'aria-label': 'Modeled outcome chart: damage index ' + damage.toFixed(1) + ', insight score ' + insight.toFixed(1), width: '100%', height: 100, viewBox: '0 0 320 100', style: { background: '#0a0a1a', borderRadius: 6, marginBottom: 8 } },
                      React.createElement("line", { x1: 30, y1: 80, x2: 310, y2: 80, stroke: '#1e293b' }),
                      React.createElement("rect", { x: 50, y: 80 - Math.max(0, Math.min(60, damage * 6)), width: 60, height: Math.max(0, Math.min(60, damage * 6)), fill: '#f87171', opacity: 0.85 }),
                      React.createElement("text", { x: 80, y: 95, fill: '#94a3b8', fontSize: 9, textAnchor: 'middle' }, 'damage'),
                      React.createElement("rect", { x: 200, y: 80 - Math.max(0, Math.min(60, insight * 5)), width: 60, height: Math.max(0, Math.min(60, insight * 5)), fill: '#4ade80', opacity: 0.85 }),
                      React.createElement("text", { x: 230, y: 95, fill: '#94a3b8', fontSize: 9, textAnchor: 'middle' }, 'insight'),
                      React.createElement("text", { x: 8, y: 14, fill: '#475569', fontSize: 8 }, 'high'),
                      React.createElement("text", { x: 8, y: 78, fill: '#475569', fontSize: 8 }, 'low')
                    ),
                    React.createElement("div", { className: "grid grid-cols-2 gap-2 mb-2" },
                      React.createElement("div", { className: "text-[10px]" },
                        React.createElement("div", { className: "flex justify-between mb-0.5" }, React.createElement("label", { htmlFor: "diss-inquiry-size" }, 'Specimen size (cm)'), React.createElement("output", { htmlFor: "diss-inquiry-size", className: "font-mono font-bold", style: { color: sm.color } }, iq.specimenSize)),
                        React.createElement("input", { id: "diss-inquiry-size", type: 'range', min: 2, max: 30, step: 1, value: iq.specimenSize, "aria-valuetext": iq.specimenSize + ' centimeters', "aria-describedby": "diss-inquiry-disclaimer", onChange: function(e) { setKey('specimenSize', parseInt(e.target.value, 10)); }, className: "w-full min-h-11" })
                      ),
                      React.createElement("div", { className: "text-[10px]" },
                        React.createElement("div", { className: "flex justify-between mb-0.5" }, React.createElement("label", { htmlFor: "diss-inquiry-depth" }, 'Dissection depth'), React.createElement("output", { htmlFor: "diss-inquiry-depth", className: "font-mono font-bold", style: { color: sm.color } }, iq.layerDepth + '/5')),
                        React.createElement("input", { id: "diss-inquiry-depth", type: 'range', min: 1, max: 5, step: 1, value: iq.layerDepth, "aria-valuetext": 'level ' + iq.layerDepth + ' of 5', "aria-describedby": "diss-inquiry-disclaimer", onChange: function(e) { setKey('layerDepth', parseInt(e.target.value, 10)); }, className: "w-full min-h-11" })
                      ),
                      React.createElement("div", { className: "text-[10px]" },
                        React.createElement("div", { className: "flex justify-between mb-0.5" }, React.createElement("label", { htmlFor: "diss-inquiry-care" }, 'Care level'), React.createElement("output", { htmlFor: "diss-inquiry-care", className: "font-mono font-bold", style: { color: sm.color } }, iq.careLevel + '/10')),
                        React.createElement("input", { id: "diss-inquiry-care", type: 'range', min: 1, max: 10, step: 1, value: iq.careLevel, "aria-valuetext": 'level ' + iq.careLevel + ' of 10', "aria-describedby": "diss-inquiry-disclaimer", onChange: function(e) { setKey('careLevel', parseInt(e.target.value, 10)); }, className: "w-full min-h-11" })
                      ),
                      React.createElement("div", { className: "text-[10px]" },
                        React.createElement("div", { className: "flex justify-between mb-0.5" }, React.createElement("label", { htmlFor: "diss-inquiry-time" }, 'Time pressure'), React.createElement("output", { htmlFor: "diss-inquiry-time", className: "font-mono font-bold", style: { color: sm.color } }, iq.timePress + '/10')),
                        React.createElement("input", { id: "diss-inquiry-time", type: 'range', min: 1, max: 10, step: 1, value: iq.timePress, "aria-valuetext": 'level ' + iq.timePress + ' of 10', "aria-describedby": "diss-inquiry-disclaimer", onChange: function(e) { setKey('timePress', parseInt(e.target.value, 10)); }, className: "w-full min-h-11" })
                      )
                    ),
                    React.createElement("div", { className: "flex gap-2 mb-2" },
                      React.createElement("button", { type: "button", onClick: function() {
                        var t = new Date().toISOString().slice(11, 19);
                        setIQ({ log: iq.log.concat([{ t: t, sz: iq.specimenSize, dp: iq.layerDepth, c: iq.careLevel, tp: iq.timePress }]) });
                        var logMessage = 'Inquiry approach logged for ' + spec.name + ': ' + sm.label + ', damage index ' + damage.toFixed(1) + ', insight score ' + insight.toFixed(1) + '.';
                        setProcedureFeedback(logMessage, 'success');
                      }, className: "flex-1 min-h-11 px-2 py-1 rounded text-[10px] font-bold", style: { background: sm.bg, color: sm.color, border: '1px solid ' + sm.border, cursor: 'pointer' } }, '📋 Log this approach'),
                      React.createElement("button", { type: "button", "aria-label": "Reset inquiry simulator inputs", onClick: function() {
                        setIQ({ specimenSize: 8, layerDepth: 1, careLevel: 5, timePress: 5 });
                        setProcedureFeedback('Inquiry simulator inputs reset. Saved approaches and written responses were preserved.', 'working');
                      }, className: "min-h-11 px-2 py-1 rounded text-[10px]", style: { background: '#0a0a1a', color: '#cbd5e1', border: '1px solid #64748b', cursor: 'pointer' } }, 'Reset inputs')
                    ),
                    iq.log.length > 0 && React.createElement("section", { className: "p-1.5 rounded text-[10px] mb-2", role: "log", "aria-label": "Saved inquiry approaches", "aria-live": "polite", "aria-relevant": "additions", tabIndex: 0, style: { background: '#0a0a1a', maxHeight: 110, overflow: 'auto', border: '1px solid #64748b' } },
                      React.createElement("h5", { className: "font-bold mb-1" }, 'Saved approaches (' + iq.log.length + '/20)'),
                      React.createElement("ol", { className: "m-0 pl-4 font-mono" },
                        iq.log.slice(-5).map(function(e, i) { return React.createElement("li", { key: e.t + '|' + e.sz + '|' + e.dp + '|' + i }, (e.t ? e.t + ' · ' : '') + e.state + ' · size ' + e.sz + ' cm · depth ' + e.dp + '/5 · care ' + e.c + '/10 · time ' + e.tp + '/10 · damage ' + e.dmg + ' · insight ' + e.ins); })
                      )
                    ),
                    React.createElement("label", { htmlFor: "diss-inquiry-hypothesis", className: "block text-[10px] font-bold mb-1" }, 'Your hypothesis: which factor will matter most, and why?'),
                    React.createElement("textarea", { id: "diss-inquiry-hypothesis", value: iq.hypothesis, maxLength: 2000, onChange: function(e) { setIQ({ hypothesis: e.target.value }); }, rows: 2, placeholder: 'Predict how size, depth, care, or time pressure will change the modeled outcome...', className: "w-full p-1.5 rounded text-[10px] mb-2", style: { background: '#0a0a1a', border: '1px solid ' + sm.border, color: '#e8f0f5', resize: 'vertical' } }),
                    React.createElement("button", { type: "button", "aria-expanded": !!iq.stuckRevealed, "aria-controls": "diss-inquiry-open-questions", onClick: function() { setIQ({ stuckRevealed: !iq.stuckRevealed }); }, className: "min-h-11 px-2 py-1 rounded text-[10px] font-bold mb-2", style: { background: '#0a0a1a', color: sm.color, border: '1px solid #64748b', cursor: 'pointer' } }, iq.stuckRevealed ? 'Hide open questions' : "🤔 I'm stuck — show open questions"),
                    iq.stuckRevealed && React.createElement("div", { id: "diss-inquiry-open-questions", className: "p-2 rounded text-[10px] mb-2", style: { background: '#0a0a1a', border: '1px dashed ' + sm.border, lineHeight: 1.5 } },
                      React.createElement("div", { className: "font-bold mb-1", style: { color: sm.color } }, 'Open questions (no answer key)'),
                      React.createElement("ul", { className: "pl-4 m-0" },
                        React.createElement("li", null, 'Which assumptions make specimen size affect the model, and would they apply equally to an earthworm and a pig?'),
                        React.createElement("li", null, 'When does added depth reduce useful observation because relationships become obscured?'),
                        React.createElement("li", null, 'What can virtual practice teach, and what tactile or specimen-specific evidence does it omit?'),
                        React.createElement("li", null, 'What assumptions would make depth 5, care 10, and time pressure 1 realistic or unrealistic?')
                      )
                    ),
                    React.createElement("label", { htmlFor: "diss-inquiry-understood", className: "flex items-center gap-2 text-[10px] font-bold cursor-pointer mb-1" },
                      React.createElement("input", { id: "diss-inquiry-understood", type: 'checkbox', checked: iq.understood, "aria-controls": "diss-inquiry-explanation", onChange: function(e) { setIQ({ understood: e.target.checked }); } }),
                      React.createElement("span", null, 'I can explain why this combination of size, depth, care, and time pressure yields this outcome.')
                    ),
                    iq.understood && React.createElement("div", { id: "diss-inquiry-explanation" },
                      React.createElement("label", { htmlFor: "diss-inquiry-explanation-text", className: "block text-[10px] font-bold mb-1" }, 'Explain the modeled relationship in your own words'),
                      React.createElement("textarea", { id: "diss-inquiry-explanation-text", value: iq.explanation, maxLength: 3000, onChange: function(e) { setIQ({ explanation: e.target.value }); }, rows: 2, placeholder: 'Explain how the four inputs affect the model, then name one limitation...', className: "w-full p-1.5 rounded text-[10px] mb-1", style: { background: '#0a0a1a', border: '1px solid ' + sm.border, color: '#e8f0f5', resize: 'vertical' } })
                    ),
                    React.createElement("p", { id: "diss-inquiry-disclaimer", role: "note", className: "m-0 text-[10px] italic" }, 'Inquiry widget — no score, no reveal, no answer dump. Damage and insight indices are pedagogical heuristics, not lab-grade rubrics or predictions of an actual specimen. Real outcomes depend on preservation, instruments, anatomy, and technique.')
                  );
                })(),

                // Glossary panel

                React.createElement("details", { className: "diss-disclosure bg-gradient-to-br from-violet-50 to-purple-50 border border-violet-200" },

                  React.createElement("summary", { className: "text-violet-800" }, 'Key terms'),
                  React.createElement("div", { className: "diss-disclosure__body space-y-1 max-h-48 overflow-y-auto", tabIndex: 0, role: "region", "aria-label": "Structure details" },

                    [

                      { term: 'Dorsal', def: 'Back/upper surface of the organism' },

                      { term: 'Ventral', def: 'Belly/lower surface of the organism' },

                      { term: 'Anterior', def: 'Front/head end of the organism' },

                      { term: 'Posterior', def: 'Rear/tail end of the organism' },

                      { term: 'Lateral', def: 'Side of the organism' },

                      { term: 'Medial', def: 'Toward the midline of the organism' },

                      { term: 'Proximal', def: 'Closer to the point of attachment' },

                      { term: 'Distal', def: 'Further from the point of attachment' },

                      { term: 'Sagittal', def: 'Plane dividing body into left/right' },

                      { term: 'Transverse', def: 'Plane dividing body into top/bottom' },

                      { term: 'Homologous', def: 'Structures with shared evolutionary origin' },

                      { term: 'Analogous', def: 'Similar function but different origin' }

                    ].map(function (g) {

                      return React.createElement("div", { key: g.term, className: "text-[11px]" },

                        React.createElement("span", { className: "font-bold text-violet-700" }, g.term + ': '),

                        React.createElement("span", { className: "text-slate-600" }, g.def)

                      );

                    })

                  )

                )

              )

            )

          );
      })();
    }
  });
