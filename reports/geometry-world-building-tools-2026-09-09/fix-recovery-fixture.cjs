const fs=require('node:fs'),path=require('node:path');const file=path.join(__dirname,'verify-import-recovery-browser.cjs');let text=fs.readFileSync(file,'utf8');const from='e._popBlocks.forEach(m=>m.scale.set(1,1,1));e._popBlocks=[];';if(!text.includes(from))throw Error('Missing optional animation fixture');text=text.replace(from,'');const fd=fs.openSync(file,'r+');fs.writeFileSync(fd,text);fs.ftruncateSync(fd,Buffer.byteLength(text));fs.closeSync(fd);
