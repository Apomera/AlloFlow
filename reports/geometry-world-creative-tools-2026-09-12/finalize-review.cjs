const fs=require('node:fs'),assert=require('node:assert/strict'),file=__dirname+'/CREATIVE-TOOLS-REVIEW.md';
const raw=fs.readFileSync(file,'utf8'),eol=raw.includes('\r\n')?'\r\n':'\n';let text=raw.replace(/\r\n/g,'\n');
const before='- Browser verification uses actual React and Three.js with Chromium WebGL. Raw checkpoints, screenshots, and scripts are retained in this folder; final browser results are recorded separately after the targeted pass.';
const after='- Browser verification uses actual React and Three.js with Chromium WebGL. Verified workflows include native Home entry and first placement, keyboard and pointer construction, single-step Undo/Redo, whole-selection edits, stale-collision rejection, stamp placement, selected camera views and orbit, ordinary-motion print camera restoration, print-scale overflow/recovery, Print Lab revision round-trip, a 12-to-24-block lesson revision with complete evidence downloads, touch Wall placement, mobile Position/drawing controls, and drawing from a fitted Top view of a 20×20 creation. See `BROWSER-REVIEW.md` for final layout results, superseded fixture failures, and screenshots.';
assert.equal(text.split(before).length,2);text=text.replace(before,after);const bytes=Buffer.from(text.replace(/\n/g,eol)),fd=fs.openSync(file,'r+');try{fs.writeSync(fd,bytes);fs.ftruncateSync(fd,bytes.length);}finally{fs.closeSync(fd);}
console.log('Review now includes final native browser workflow evidence.');
