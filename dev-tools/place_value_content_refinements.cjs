const {session}=require('./science_refinement_session.cjs');exports.refine=p=>{const {E,get,replaceAll,finish}=session(p);
replaceAll('In math, where you stand changes what you are worth.','For a digit, its place changes the value it represents.');replaceAll('In 305 there are no tens at all.','In the usual hundreds-tens-ones form of 305, the tens digit is zero. You can also regroup 305 as 30 tens and 5 ones.');
E('glossary','data.1.def','In a whole number, the rightmost place; it counts single units.');E('glossary','data.9.def','The number zero means none; the digit 0 can mark a place with no groups in the usual form.');
E('anchor','data.sections.0.label','ONES');E('anchor','data.sections.1.label','TENS');E('anchor','data.sections.2.label','HUNDREDS');E('anchor','data.sections.2.bullets.2','Two hundred flats, each with 100 small squares');E('anchor','data.sections.3.bullets.1','Compare: align the same places, then work from the greatest place');
const M=(i,k,v)=>E('memory',`data.cards.${i}.${k}`,v);M(0,'essentialFacts.0','For these whole numbers, ones is the rightmost place.');M(0,'essentialFacts.3','Each place to the left has ten times the unit value; a zero digit still represents zero.');M(0,'aiExample','Draw three columns: hundreds on the left, tens in the middle, ones on the right. Put a hundred flat, ten rod and single square in the matching columns. Read 245 as two hundreds, four tens and five ones.');M(0,'mapping','Columns keep places aligned without depending on which hand you use or how it faces. Blocks connect each place to its unit value.');M(0,'scaffoldStarter','Left column: ____. Middle: ____. Right: ____.');M(0,'scaffoldSteps.1','Draw three columns or point to a place-value mat.');M(0,'scaffoldSteps.2','Show 245 in the columns.');M(0,'coachPrompts.0','Which column holds the greatest place value?');M(1,'aiExample','ALIGN, THEN COMPARE. Line up ones with ones, tens with tens and hundreds with hundreds. Compare the greatest place first, moving right only when digits tie.');M(1,'mapping','Alignment matters when numbers have different numbers of digits: 99 has zero hundreds, so it is smaller than 100.');
E('faq','data.0.answer','Base ten groups units by tens. Other number systems use different bases, such as base two. Our lesson uses base ten throughout.');E('faq','data.1.answer','Yes. Zero can mean no objects, and the digit 0 also keeps place values clear. In 305 it marks zero tens in the usual place-value form, so the number is different from 35.');E('faq','data.4.answer','Align equal places first. For whole numbers, compare the greatest place where they differ. For example, 99 has no hundreds and 100 has one, so 100 is greater even though its first digit is smaller.');
E('challenge','data.supports.parallelExample.move','She drew an empty box in the ONES column of 230, with two hundred flats and three ten rods in the other columns. Her buddy noticed the empty ones place first.');E('challenge','data.brief.seedDirection','Try a number with a zero. Show the empty place clearly and check that its column matches the digit.');E('challenge','data.coachHint','For 230, the ones place is empty; the tens digit is three. One number, three matching representations.');
return finish();};
