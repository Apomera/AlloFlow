/**
 * AlloFlow Adventure UI Module
 *
 * Contains: MissionReportCard, ClimaxProgressBar, AdventureAmbience,
 * InventoryGrid, DiceOverlay, AdventureShop, and CastLobby.
 * Utilities: playAdventureEventSound, playGenerativeSoundscape,
 * playDiceSound, and getD20Rotation.
 *
 * Auto-generated by _build_adventure_module.js - DO NOT EDIT MANUALLY.
 * Source: adventure_source.jsx
 */
(function() {
  'use strict';

  window.AlloModules = window.AlloModules || {};
  if (window.__adventureModuleLoaded || window.AlloModules.AdventureModule) {
    console.warn('[AdventureModule] Already loaded - skipping');
    return;
  }
  window.__adventureModuleLoaded = true;

  // WCAG 2.1 AA: accessibility support shared by all Adventure overlays.
  if (!document.getElementById('adv-a11y-css')) {
    var advA11yStyle = document.createElement('style');
    advA11yStyle.id = 'adv-a11y-css';
    advA11yStyle.textContent = '@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; } }';
    document.head.appendChild(advA11yStyle);
  }

  var STYLE_IMAGE_PIXELATED = { imageRendering: 'pixelated' };

  // Fallback: define ADVENTURE_SHOP_ITEMS if not available from main app
  if (typeof ADVENTURE_SHOP_ITEMS === 'undefined') {
    var ADVENTURE_SHOP_ITEMS = window.ADVENTURE_SHOP_ITEMS || [
      { id: 'ration', name: 'Emergency Ration', cost: 50, description: 'Restores 20 Energy.', effectType: 'energy', effectValue: 20, icon: '🍎' },
      { id: 'feast', name: 'Field Feast', cost: 120, description: 'Fully restores energy.', effectType: 'energy', effectValue: 100, icon: '🍱' },
      { id: 'hint', name: 'Oracle Whisper', cost: 75, description: 'Rewinds your previous turn.', effectType: 'hint', effectValue: 1, icon: '🔮' },
      { id: 'charm', name: 'Luck Charm', cost: 100, description: '+5 to next roll.', effectType: 'modifier', effectValue: 5, icon: '🍀' },
      { id: 'journal', name: "Scholar's Journal", cost: 100, description: 'Double XP next turn.', effectType: 'xp_boost', effectValue: 2, icon: '📔' },
      { id: 'detector', name: 'Metal Detector', cost: 50, description: 'More gold for 3 scenes.', effectType: 'gold_boost', effectValue: 3, icon: '💰' },
      { id: 'guiding_hand', name: 'Guiding Hand', cost: 125, description: 'A timely intervention resolves the current obstacle and teaches useful knowledge for later scenes. No XP or Gold is awarded.', effectType: 'story_assist', effectValue: 1, icon: '\u{1F91D}' },
    ];
  }

  // WCAG 4.1.3: shared status live region for dynamic announcements.
  if (!document.getElementById('allo-live-adventure')) {
    var liveRegion = document.createElement('div');
    liveRegion.id = 'allo-live-adventure';
    liveRegion.setAttribute('aria-live', 'polite');
    liveRegion.setAttribute('aria-atomic', 'true');
    liveRegion.setAttribute('role', 'status');
    liveRegion.className = 'sr-only';
    liveRegion.style.cssText = 'position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);border:0';
    document.body.appendChild(liveRegion);
  }

  var React = window.React;
  if (!React) {
    console.error('[AdventureModule] React not found on window');
    return;
  }
  var useState = React.useState;
  var useEffect = React.useEffect;
  var useRef = React.useRef;
  var useCallback = React.useCallback;
  var useMemo = React.useMemo;
  var useContext = React.useContext;

  var LanguageContext = window.AlloLanguageContext;
  var _shared = window.__alloShared || {};
  var warnLog = _shared.warnLog || function() { console.warn.apply(console, arguments); };
  var debugLog = window.__alloDebugLog || function() {};
  var getGlobalAudioContext = window.getGlobalAudioContext || function() { return null; };
  var isGlobalMuted = window.__alloIsGlobalMuted || function() { return false; };
  var useFocusTrap = (window.__alloHooks && window.__alloHooks.useFocusTrap) || function() {};

  var _icons = window.AlloIcons || {};
  var Backpack = _icons.Backpack || function() { return null; };
  var BookOpen = _icons.BookOpen || function() { return null; };
  var Key = _icons.Key || function() { return null; };
  var MapIcon = _icons.MapIcon || function() { return null; };
  var RefreshCw = _icons.RefreshCw || function() { return null; };
  var ShoppingBag = _icons.ShoppingBag || function() { return null; };
  var Trophy = _icons.Trophy || function() { return null; };
  var X = _icons.X || function() { return null; };
  var Zap = _icons.Zap || function() { return null; };

var MissionReportCard = React.memo(({ adventureState, globalLevel, onClose, onExport, onContinue, onNewGame, isProcessing }) => {
  const { t } = useContext(LanguageContext);
  const reportRef = useRef(null);
  useFocusTrap(reportRef, true, onClose);
  const { stats, climax, xp, level } = adventureState;
  const safeStats = stats || { successes: 0, failures: 0, decisions: 0, partials: 0, conceptsFound: [] };
  const totalDecisions = Math.max(1, safeStats.decisions);
  const efficiency = Math.round(safeStats.successes / totalDecisions * 100);
  const proficiency = Math.max(0, Math.min(100, Number(climax?.masteryScore) || 0));
  const performanceLabel = t("adventure.mission_report.performance_rating") || "Story Performance";
  let ratingLabel = t("adventure.mission_report.rating2_beginning") || "Just Beginning";
  if (proficiency >= 90) ratingLabel = t("adventure.mission_report.rating2_strong") || "Strong Command";
  else if (proficiency >= 70) ratingLabel = t("adventure.mission_report.rating2_solid") || "Solid Command";
  else if (proficiency >= 50) ratingLabel = t("adventure.mission_report.rating2_developing") || "Developing";
  return /* @__PURE__ */ React.createElement("div", { ref: reportRef, role: "dialog", "aria-modal": "true", "aria-labelledby": "adventure-mission-report-title", className: "fixed inset-0 z-[200] flex items-center justify-center bg-black/80 backdrop-blur-sm animate-in fade-in duration-700 p-4" }, /* @__PURE__ */ React.createElement("div", { className: "bg-slate-900 w-full max-w-md rounded-3xl border-4 border-yellow-500 shadow-[0_0_50px_rgba(234,179,8,0.3)] overflow-hidden relative transform scale-100 animate-in zoom-in-95 duration-500 flex flex-col max-h-[90vh]" }, /* @__PURE__ */ React.createElement("div", { className: "bg-yellow-500 p-4 text-center shrink-0" }, /* @__PURE__ */ React.createElement("h2", { id: "adventure-mission-report-title", className: "text-3xl font-black text-slate-900 uppercase tracking-widest drop-shadow-sm flex items-center justify-center gap-2" }, /* @__PURE__ */ React.createElement(Trophy, { size: 32, "aria-hidden": "true" }), " ", t("adventure.mission_report.title")), /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-center gap-2 text-slate-900 font-bold text-sm opacity-80 mt-1" }, /* @__PURE__ */ React.createElement("span", null, t("adventure.mission_report.status_success")), /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true" }, "\u2022"), /* @__PURE__ */ React.createElement("span", null, (/* @__PURE__ */ new Date()).toLocaleDateString()))), /* @__PURE__ */ React.createElement("div", { className: "p-8 space-y-6 relative z-10 text-white overflow-y-auto custom-scrollbar flex-grow" }, /* @__PURE__ */ React.createElement("div", { className: "absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.05)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.05)_1px,transparent_1px)] bg-[size:20px_20px] pointer-events-none" }), /* @__PURE__ */ React.createElement("div", { className: "text-center mb-2 relative z-20" }, /* @__PURE__ */ React.createElement("div", { className: "text-xs font-bold text-yellow-500 uppercase tracking-widest mb-1" }, t("adventure.mission_report.final_score")), /* @__PURE__ */ React.createElement("div", { className: "text-6xl font-black text-white tracking-tighter drop-shadow-lg" }, xp), /* @__PURE__ */ React.createElement("div", { className: "inline-block bg-indigo-600 px-3 py-1 rounded-full border border-indigo-400 shadow-sm text-sm font-bold mt-2" }, t("adventure.mission_report.level_achieved", { level: level || 1 }))), /* @__PURE__ */ React.createElement("div", { className: "relative z-20" }, /* @__PURE__ */ React.createElement("div", { className: "flex justify-between text-xs font-bold mb-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-cyan-400 uppercase" }, performanceLabel), /* @__PURE__ */ React.createElement("span", { className: "text-white" }, proficiency, "/100 (", ratingLabel, ")")), /* @__PURE__ */ React.createElement("div", { role: "progressbar", "aria-label": performanceLabel, "aria-valuemin": 0, "aria-valuemax": 100, "aria-valuenow": proficiency, className: "h-4 bg-slate-800 rounded-full overflow-hidden border border-slate-700 relative" }, /* @__PURE__ */ React.createElement(
    "div",
    {
      className: "h-full bg-gradient-to-r from-cyan-600 to-cyan-400 transition-all duration-1000 ease-out",
      style: { width: `${proficiency}%` }
    }
  )), /* @__PURE__ */ React.createElement("p", { className: "text-[10px] text-slate-400 mt-1.5 leading-snug" }, t("adventure.mission_report.ai_estimate_note") || "Estimated by AI from story decisions \u2014 a reflection prompt, not a formal assessment.")), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-3 gap-2 relative z-20" }, /* @__PURE__ */ React.createElement("div", { className: "bg-slate-800/50 p-2.5 rounded-xl border border-slate-700 flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold uppercase text-green-400" }, t("adventure.mission_report.band_strong") || "Strong moves"), /* @__PURE__ */ React.createElement("span", { className: "text-xl font-black text-white" }, safeStats.successes || 0)), /* @__PURE__ */ React.createElement("div", { className: "bg-slate-800/50 p-2.5 rounded-xl border border-slate-700 flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold uppercase text-yellow-400" }, t("adventure.mission_report.band_partial") || "Partial credit"), /* @__PURE__ */ React.createElement("span", { className: "text-xl font-black text-white" }, safeStats.partials || 0)), /* @__PURE__ */ React.createElement("div", { className: "bg-slate-800/50 p-2.5 rounded-xl border border-slate-700 flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement("span", { className: "text-[10px] font-bold uppercase text-rose-400" }, t("adventure.mission_report.band_misconception") || "Misconceptions"), /* @__PURE__ */ React.createElement("span", { className: "text-xl font-black text-white" }, safeStats.failures || 0))), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-2 gap-4 relative z-20" }, /* @__PURE__ */ React.createElement("div", { className: "bg-slate-800/50 p-4 rounded-xl border border-slate-700 flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-2 text-yellow-400" }, /* @__PURE__ */ React.createElement(Zap, { size: 16, "aria-hidden": "true" }), " ", /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase" }, t("adventure.mission_report.efficiency"))), /* @__PURE__ */ React.createElement("div", { className: "text-3xl font-black" }, efficiency, "%")), /* @__PURE__ */ React.createElement("div", { className: "bg-slate-800/50 p-4 rounded-xl border border-slate-700 flex flex-col items-center justify-center" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 mb-2 text-green-400" }, /* @__PURE__ */ React.createElement(Key, { size: 16, "aria-hidden": "true" }), " ", /* @__PURE__ */ React.createElement("span", { className: "text-[11px] font-bold uppercase" }, t("adventure.mission_report.concepts"))), /* @__PURE__ */ React.createElement("div", { className: "text-3xl font-black" }, safeStats.conceptsFound.length))), safeStats.conceptsFound.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "mt-4 pt-4 border-t border-slate-700/50 relative z-20" }, /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-300 font-bold uppercase mb-2" }, t("adventure.mission_report.concepts_secured")), /* @__PURE__ */ React.createElement("div", { className: "flex flex-wrap gap-2" }, safeStats.conceptsFound.map((c, i) => /* @__PURE__ */ React.createElement("span", { key: i, className: "px-2 py-1 rounded-md bg-cyan-900/30 text-cyan-200 text-xs border border-cyan-800/50" }, c))))), /* @__PURE__ */ React.createElement("div", { className: "p-4 bg-slate-800 border-t border-slate-700 flex flex-col gap-3 relative z-20 shrink-0" }, /* @__PURE__ */ React.createElement(
    "button",
    {
      "aria-label": t("common.create_storybook"),
      onClick: onExport,
      disabled: isProcessing,
      className: "w-full min-h-11 py-3 rounded-xl font-bold bg-indigo-600 text-white hover:bg-indigo-500 transition-colors shadow-lg flex items-center justify-center gap-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-slate-800"
    },
    isProcessing ? /* @__PURE__ */ React.createElement(RefreshCw, { size: 18, className: "animate-spin", "aria-hidden": "true" }) : /* @__PURE__ */ React.createElement(BookOpen, { size: 18, "aria-hidden": "true" }),
    isProcessing ? t("adventure.storybook_writing") : t("adventure.storybook")
  ), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-2 gap-3" }, /* @__PURE__ */ React.createElement(
    "button",
    {
      onClick: () => {
        onClose();
        if (onContinue) onContinue();
      },
      className: "w-full min-h-11 py-3 rounded-xl font-bold bg-green-700 text-white hover:bg-green-600 transition-colors shadow-lg flex items-center justify-center gap-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-slate-800"
    },
    /* @__PURE__ */ React.createElement(MapIcon, { size: 18, "aria-hidden": "true" }),
    " ",
    t("adventure.start_sequel") || "Continue"
  ), /* @__PURE__ */ React.createElement(
    "button",
    {
      "aria-label": t("adventure.new_game") || "New Game",
      onClick: () => {
        onClose();
        if (onNewGame) onNewGame();
      },
      className: "w-full min-h-11 py-3 rounded-xl font-bold bg-slate-700 text-slate-200 hover:bg-slate-600 hover:text-white transition-colors border border-slate-500 flex items-center justify-center gap-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-slate-800"
    },
    /* @__PURE__ */ React.createElement(RefreshCw, { size: 18, "aria-hidden": "true" }),
    " ",
    t("adventure.new_game") || "New Game"
  )), /* @__PURE__ */ React.createElement(
    "button",
    {
      "aria-label": t("common.close"),
      onClick: onClose,
      className: "w-full min-h-11 py-2 text-sm font-bold text-slate-200 hover:text-white transition-colors rounded-lg focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-slate-800"
    },
    t("adventure.mission_report.confirm_exit")
  ))));
});
var playAdventureEventSound = (type) => {
  const ctx = getGlobalAudioContext();
  if (!ctx) return;
  if (ctx.state === "suspended") ctx.resume();
  const now = ctx.currentTime;
  const playTone = (wave, freq, startTime, duration, vol = 0.1) => {
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.type = wave;
    osc.frequency.setValueAtTime(freq, startTime);
    gain.gain.setValueAtTime(vol, startTime);
    gain.gain.exponentialRampToValueAtTime(1e-3, startTime + duration);
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.start(startTime);
    osc.stop(startTime + duration);
  };
  switch (type) {
    case "transition":
      const tNoise = ctx.createBufferSource();
      const tBuffer = ctx.createBuffer(1, ctx.sampleRate * 0.5, ctx.sampleRate);
      const tData = tBuffer.getChannelData(0);
      for (let i = 0; i < tBuffer.length; i++) tData[i] = Math.random() * 2 - 1;
      tNoise.buffer = tBuffer;
      const tFilter = ctx.createBiquadFilter();
      tFilter.type = "lowpass";
      tFilter.frequency.setValueAtTime(200, now);
      tFilter.frequency.linearRampToValueAtTime(1200, now + 0.2);
      const tGain = ctx.createGain();
      tGain.gain.setValueAtTime(0.05, now);
      tGain.gain.linearRampToValueAtTime(0, now + 0.4);
      tNoise.connect(tFilter);
      tFilter.connect(tGain);
      tGain.connect(ctx.destination);
      tNoise.start(now);
      break;
    case "critical_success":
      playTone("square", 523.25, now, 0.4);
      playTone("square", 659.25, now + 0.1, 0.4);
      playTone("square", 783.99, now + 0.2, 0.4);
      playTone("square", 1046.5, now + 0.3, 0.8, 0.15);
      break;
    case "success":
      playTone("sine", 880, now, 0.3);
      playTone("sine", 1760, now + 0.1, 0.4);
      break;
    case "failure":
      playTone("sawtooth", 100, now, 0.4, 0.15);
      playTone("sawtooth", 92, now, 0.4, 0.15);
      break;
    case "damage":
      const dOsc = ctx.createOscillator();
      const dGain = ctx.createGain();
      dOsc.type = "sawtooth";
      dOsc.frequency.setValueAtTime(150, now);
      dOsc.frequency.exponentialRampToValueAtTime(40, now + 0.2);
      dGain.gain.setValueAtTime(0.2, now);
      dGain.gain.exponentialRampToValueAtTime(0.01, now + 0.2);
      dOsc.connect(dGain);
      dGain.connect(ctx.destination);
      dOsc.start(now);
      dOsc.stop(now + 0.2);
      break;
    case "item_get":
      playTone("sine", 1500, now, 0.1, 0.05);
      playTone("sine", 2e3, now + 0.1, 0.1, 0.05);
      playTone("sine", 2500, now + 0.2, 0.3, 0.05);
      break;
    case "decision_select":
      playTone("triangle", 800, now, 0.05, 0.05);
      break;
  }
};
var playGenerativeSoundscape = (ctx, dest, params) => {
  const safeParams = params || {};
  const atmosphere = safeParams.atmosphere || "Calm";
  const element = safeParams.element || "Wind";
  const now = ctx.currentTime;
  const activeNodes = [];
  const atmOsc1 = ctx.createOscillator();
  const atmOsc2 = ctx.createOscillator();
  const atmGain = ctx.createGain();
  const atmFilter = ctx.createBiquadFilter();
  atmOsc1.connect(atmFilter);
  atmOsc2.connect(atmFilter);
  atmFilter.connect(atmGain);
  atmGain.connect(dest);
  let baseFreq = 220;
  let detune = 4;
  let waveType = "sine";
  atmFilter.type = "lowpass";
  atmFilter.frequency.value = 800;
  switch (atmosphere?.toLowerCase()) {
    case "tense":
      baseFreq = 110;
      detune = 100;
      waveType = "sawtooth";
      atmFilter.frequency.value = 2e3;
      const tenseLFO = ctx.createOscillator();
      tenseLFO.frequency.value = 5;
      const tenseLFOGain = ctx.createGain();
      tenseLFOGain.gain.value = 50;
      tenseLFO.connect(tenseLFOGain);
      tenseLFOGain.connect(atmOsc1.frequency);
      tenseLFO.start(now);
      activeNodes.push(tenseLFO, tenseLFOGain);
      break;
    case "dark":
      baseFreq = 55;
      detune = 12;
      waveType = "triangle";
      atmFilter.frequency.value = 300;
      break;
    case "ethereal":
      baseFreq = 440;
      detune = 700;
      waveType = "sine";
      atmFilter.type = "highpass";
      atmFilter.frequency.value = 600;
      break;
    case "joyful":
      baseFreq = 261.63;
      detune = 400;
      waveType = "triangle";
      atmFilter.frequency.value = 1500;
      break;
  }
  atmOsc1.type = waveType;
  atmOsc1.frequency.value = baseFreq;
  atmOsc2.type = waveType;
  atmOsc2.frequency.value = baseFreq;
  atmOsc2.detune.value = detune;
  atmGain.gain.setValueAtTime(0, now);
  atmGain.gain.linearRampToValueAtTime(0.1, now + 2);
  atmOsc1.start(now);
  atmOsc2.start(now);
  activeNodes.push(atmOsc1, atmOsc2, atmGain, atmFilter);
  if (element && element.toLowerCase() !== "silence") {
    const bufferSize = ctx.sampleRate * 2;
    const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
    const data = buffer.getChannelData(0);
    for (let i = 0; i < bufferSize; i++) data[i] = Math.random() * 2 - 1;
    const noise = ctx.createBufferSource();
    noise.buffer = buffer;
    noise.loop = true;
    const eleFilter = ctx.createBiquadFilter();
    const eleGain = ctx.createGain();
    noise.connect(eleFilter);
    eleFilter.connect(eleGain);
    eleGain.connect(dest);
    eleGain.gain.value = 0.05;
    switch (element.toLowerCase()) {
      case "fire":
        eleFilter.type = "lowpass";
        eleFilter.frequency.value = 800;
        const crackleInterval = setInterval(() => {
          if (Math.random() > 0.8) {
            const spike = ctx.currentTime;
            eleGain.gain.setValueAtTime(0.05, spike);
            eleGain.gain.linearRampToValueAtTime(0.2, spike + 0.05);
            eleGain.gain.linearRampToValueAtTime(0.05, spike + 0.1);
          }
        }, 100);
        activeNodes.push({ stop: () => clearInterval(crackleInterval), disconnect: () => {
        } });
        break;
      case "water":
        eleFilter.type = "lowpass";
        eleFilter.frequency.value = 400;
        const waveLFO = ctx.createOscillator();
        waveLFO.frequency.value = 0.2;
        const waveGain = ctx.createGain();
        waveGain.gain.value = 300;
        waveLFO.connect(waveGain);
        waveGain.connect(eleFilter.frequency);
        waveLFO.start(now);
        activeNodes.push(waveLFO, waveGain);
        break;
      case "machinery":
        const clank = ctx.createOscillator();
        clank.type = "square";
        clank.frequency.value = 100;
        const clankGain = ctx.createGain();
        clank.connect(eleFilter);
        const rhythm = ctx.createOscillator();
        rhythm.type = "square";
        rhythm.frequency.value = 4;
        const rhythmGain = ctx.createGain();
        rhythmGain.gain.value = 1;
        rhythm.connect(rhythmGain);
        rhythmGain.connect(eleGain.gain);
        clank.start(now);
        rhythm.start(now);
        activeNodes.push(clank, rhythm, rhythmGain);
        break;
      case "wind":
        eleFilter.type = "bandpass";
        eleFilter.Q.value = 1;
        const windLFO = ctx.createOscillator();
        windLFO.frequency.value = 0.1;
        const windGain = ctx.createGain();
        windGain.gain.value = 600;
        windLFO.connect(windGain);
        windGain.connect(eleFilter.frequency);
        eleFilter.frequency.value = 800;
        windLFO.start(now);
        activeNodes.push(windLFO, windGain);
        break;
      case "nature":
        eleFilter.type = "highpass";
        eleFilter.frequency.value = 3e3;
        eleGain.gain.value = 0.02;
        const birdInterval = setInterval(() => {
          if (Math.random() > 0.7) {
            const osc = ctx.createOscillator();
            osc.frequency.setValueAtTime(2e3 + Math.random() * 1e3, ctx.currentTime);
            osc.frequency.linearRampToValueAtTime(3e3, ctx.currentTime + 0.1);
            const g = ctx.createGain();
            g.gain.setValueAtTime(0.05, ctx.currentTime);
            g.gain.linearRampToValueAtTime(0, ctx.currentTime + 0.1);
            osc.connect(g);
            g.connect(dest);
            osc.start();
            osc.stop(ctx.currentTime + 0.2);
          }
        }, 800);
        activeNodes.push({ stop: () => clearInterval(birdInterval), disconnect: () => {
        } });
        break;
    }
    if (element.toLowerCase() !== "machinery") {
      noise.start(now);
      activeNodes.push(noise, eleFilter, eleGain);
    }
  }
  return activeNodes;
};
var ClimaxProgressBar = React.memo(({ climaxState }) => {
  const { t } = useContext(LanguageContext);
  if (!climaxState || !climaxState.isActive) return null;
  const { masteryScore, archetype } = climaxState;
  let typeKey = (archetype || "default").toLowerCase();
  if (typeKey === "auto") typeKey = "default";
  const label = t("adventure.climax_archetypes." + typeKey + ".label") || t("adventure.climax_archetypes.default.label");
  const leftLabel = t("adventure.climax_archetypes." + typeKey + ".left") || t("adventure.climax_archetypes.default.left");
  const rightLabel = t("adventure.climax_archetypes." + typeKey + ".right") || t("adventure.climax_archetypes.default.right");
  const normalizedScore = Math.max(0, Math.min(100, Number(masteryScore) || 0));
  let icon = "\u2694\uFE0F";
  switch (archetype) {
    case "Antagonist":
      icon = "\u2694\uFE0F";
      break;
    case "Catastrophe":
      icon = "\u26A0\uFE0F";
      break;
    case "Masterpiece":
      icon = "\u{1F3A8}";
      break;
    case "Discovery":
      icon = "\u{1F5FA}\uFE0F";
      break;
  }
  let barColor = "bg-yellow-500";
  let textColor = "text-yellow-400";
  let borderColor = "border-yellow-600";
  if (normalizedScore >= 80) {
    barColor = "bg-green-500 shadow-[0_0_15px_rgba(34,197,94,0.6)]";
    textColor = "text-green-400";
    borderColor = "border-green-600";
  } else if (normalizedScore <= 30) {
    barColor = "bg-red-500 shadow-[0_0_15px_rgba(239,68,68,0.6)]";
    textColor = "text-red-400";
    borderColor = "border-red-600";
  }
  return /* @__PURE__ */ React.createElement("div", { className: "w-full bg-slate-900/95 backdrop-blur-md border-y-4 " + borderColor + " p-4 shadow-2xl animate-in slide-in-from-top-4 motion-reduce:animate-none relative z-40 mb-2 transition-colors duration-500 motion-reduce:transition-none" }, /* @__PURE__ */ React.createElement("div", { className: "flex justify-between items-end mb-2 px-1" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-xl animate-pulse motion-reduce:animate-none", "aria-hidden": "true" }, icon), /* @__PURE__ */ React.createElement("span", { className: "text-xs font-black text-indigo-200 uppercase tracking-widest" }, label)), /* @__PURE__ */ React.createElement("span", { className: "text-2xl font-black " + textColor + " drop-shadow-sm font-mono transition-colors duration-500 motion-reduce:transition-none" }, Math.round(normalizedScore), "%")), /* @__PURE__ */ React.createElement(
    "div",
    {
      className: "relative h-6 w-full bg-slate-800 rounded-full border-2 border-slate-600 overflow-hidden shadow-inner",
      role: "progressbar",
      "aria-label": label,
      "aria-valuemin": 0,
      "aria-valuemax": 100,
      "aria-valuenow": Math.round(normalizedScore),
      "aria-valuetext": Math.round(normalizedScore) + "%; " + leftLabel + " to " + rightLabel
    },
    /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "h-full " + barColor + " transition-all duration-1000 ease-out motion-reduce:transition-none relative",
        style: { width: normalizedScore + "%" },
        "aria-hidden": "true"
      },
      /* @__PURE__ */ React.createElement("div", { className: "absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent animate-[shimmer_2s_infinite] motion-reduce:animate-none" })
    ),
    /* @__PURE__ */ React.createElement("div", { className: "absolute top-0 bottom-0 left-1/2 w-0.5 bg-white/30 z-10 border-l border-black/20", "aria-hidden": "true" }),
    /* @__PURE__ */ React.createElement("div", { className: "absolute top-0 bottom-0 left-[25%] w-px bg-white/10 z-0", "aria-hidden": "true" }),
    /* @__PURE__ */ React.createElement("div", { className: "absolute top-0 bottom-0 left-[75%] w-px bg-white/10 z-0", "aria-hidden": "true" })
  ), /* @__PURE__ */ React.createElement("div", { className: "flex justify-between text-[11px] font-bold uppercase mt-1.5 px-1" }, /* @__PURE__ */ React.createElement("span", { className: "text-red-300" }, leftLabel, " (0%)"), /* @__PURE__ */ React.createElement("span", { className: "text-green-400" }, rightLabel, " (100%)")));
});
var AdventureAmbience = React.memo(({ sceneText, soundParams, active, volume = 0.3 }) => {
  const ctxRef = useRef(null);
  const masterGainRef = useRef(null);
  const activeNodesRef = useRef([]);
  const intervalsRef = useRef([]);
  const cleanup = () => {
    const nodesToStop = [...activeNodesRef.current];
    const intervalsToClear = [...intervalsRef.current];
    const oldGain = masterGainRef.current;
    activeNodesRef.current = [];
    intervalsRef.current = [];
    masterGainRef.current = null;
    intervalsToClear.forEach(clearInterval);
    if (oldGain && ctxRef.current) {
      try {
        oldGain.gain.cancelScheduledValues(ctxRef.current.currentTime);
        oldGain.gain.setValueAtTime(oldGain.gain.value, ctxRef.current.currentTime);
        oldGain.gain.linearRampToValueAtTime(0, ctxRef.current.currentTime + 1);
      } catch (e) {
        warnLog("Caught error:", e?.message || e);
      }
    }
    setTimeout(() => {
      nodesToStop.forEach((node) => {
        try {
          node.stop();
        } catch (e) {
          warnLog("Caught error:", e?.message || e);
        }
        try {
          node.disconnect();
        } catch (e) {
          warnLog("Caught error:", e?.message || e);
        }
      });
      if (oldGain) {
        try {
          oldGain.disconnect();
        } catch (e) {
          warnLog("Caught error:", e?.message || e);
        }
      }
    }, 1100);
  };
  useEffect(() => {
    if (!active || !sceneText) {
      cleanup();
      return;
    }
    const ctx = getGlobalAudioContext();
    if (!ctx) return;
    ctxRef.current = ctx;
    if (ctx.state === "suspended") ctx.resume();
    cleanup();
    playAdventureEventSound("transition");
    const masterGain = ctx.createGain();
    const now = ctx.currentTime;
    const fadeInTime = 2;
    const sustainTime = 6;
    const fadeOutTime = 4;
    const totalDuration = fadeInTime + sustainTime + fadeOutTime;
    masterGain.gain.setValueAtTime(0, now);
    masterGain.gain.linearRampToValueAtTime(volume, now + fadeInTime);
    masterGain.gain.setValueAtTime(volume, now + fadeInTime + sustainTime);
    masterGain.gain.linearRampToValueAtTime(0, now + totalDuration);
    masterGain.connect(ctx.destination);
    masterGainRef.current = masterGain;
    const autoKillTimer = setTimeout(() => {
      cleanup();
    }, totalDuration * 1e3 + 200);
    intervalsRef.current.push(autoKillTimer);
    try {
      if (soundParams && typeof soundParams === "object") {
        debugLog("DJ Gemini playing:", soundParams);
        const nodes = playGenerativeSoundscape(ctx, masterGain, soundParams);
        activeNodesRef.current.push(...nodes);
      }
    } catch (err) {
      warnLog("Audio generation failed, attempting fallback.", err);
      const nodes = playGenerativeSoundscape(ctx, masterGain, { atmosphere: "Calm", element: "Wind" });
      activeNodesRef.current.push(...nodes);
    }
    return () => cleanup();
  }, [sceneText, soundParams, active]);
  useEffect(() => {
    if (masterGainRef.current && ctxRef.current) {
      try {
      } catch (e) {
        warnLog("Caught error:", e?.message || e);
      }
    }
  }, [volume]);
  return null;
});
var playDiceSound = () => {
  const ctx = getGlobalAudioContext();
  if (!ctx) return;
  if (ctx.state === "suspended") {
    ctx.resume();
  }
  const now = ctx.currentTime;
  const playClack = (time, velocity) => {
    const bufferSize = ctx.sampleRate * 0.1;
    const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
    const data = buffer.getChannelData(0);
    for (let i = 0; i < bufferSize; i++) {
      data[i] = Math.random() * 2 - 1;
    }
    const noise = ctx.createBufferSource();
    noise.buffer = buffer;
    const filter = ctx.createBiquadFilter();
    filter.type = "bandpass";
    filter.frequency.value = 1200 + Math.random() * 1e3;
    filter.Q.value = 1.5;
    const gain = ctx.createGain();
    gain.gain.setValueAtTime(0, time);
    gain.gain.linearRampToValueAtTime(velocity, time + 1e-3);
    gain.gain.exponentialRampToValueAtTime(0.01, time + 0.08);
    noise.connect(filter);
    filter.connect(gain);
    gain.connect(ctx.destination);
    noise.start(time);
    noise.stop(time + 0.1);
  };
  playClack(now, 0.8);
  playClack(now + 0.06, 0.7);
  playClack(now + 0.13, 0.6);
  playClack(now + 0.25 + Math.random() * 0.05, 0.5);
  playClack(now + 0.45 + Math.random() * 0.1, 0.3);
  playClack(now + 0.7 + Math.random() * 0.1, 0.15);
};
var getD20Rotation = (result, spins = 5) => {
  const index = result - 1;
  const faceTransforms = [
    [0, 52.62, 0],
    [72, 52.62, 0],
    [144, 52.62, 0],
    [216, 52.62, 0],
    [288, 52.62, 0],
    [0, 10.81, 180],
    [72, 10.81, 180],
    [144, 10.81, 180],
    [216, 10.81, 180],
    [288, 10.81, 180],
    [36, -10.81, 0],
    [108, -10.81, 0],
    [180, -10.81, 0],
    [252, -10.81, 0],
    [324, -10.81, 0],
    [36, -52.62, 180],
    [108, -52.62, 180],
    [180, -52.62, 180],
    [252, -52.62, 180],
    [324, -52.62, 180]
  ];
  if (index < 0 || index >= faceTransforms.length) return "rotateX(0deg) rotateY(0deg) rotateZ(0deg)";
  const [y, x, zOffset] = faceTransforms[index];
  const xRot = -x + spins * 360;
  const yRot = -y + spins * 360;
  const zRot = 360 * 2 - zOffset;
  return `rotateZ(${zRot}deg) rotateX(${xRot}deg) rotateY(${yRot}deg)`;
};
var InventoryGrid = React.memo(({ inventory, onSelect }) => {
  const [dismissedTooltip, setDismissedTooltip] = useState(null);
  if (!inventory || inventory.length === 0) return null;
  return /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 bg-indigo-800/50 px-3 py-1.5 rounded-2xl border border-indigo-500" }, /* @__PURE__ */ React.createElement(Backpack, { size: 18, className: "text-yellow-300 shrink-0", "aria-hidden": "true" }), /* @__PURE__ */ React.createElement("ul", { className: "flex flex-wrap gap-2" }, inventory.map((item, idx) => /* @__PURE__ */ React.createElement("li", { key: item.id || idx, className: "group relative hover:z-20 focus-within:z-20" }, /* @__PURE__ */ React.createElement(
    "button",
    {
      type: "button",
      "data-help-key": "inventory_item",
      onClick: () => onSelect(item),
      onMouseEnter: () => setDismissedTooltip(null),
      onFocus: () => setDismissedTooltip(null),
      onKeyDown: (event) => {
        if (event.key === "Escape") {
          event.stopPropagation();
          setDismissedTooltip(idx);
        }
      },
      className: "w-11 h-11 rounded-lg bg-indigo-950 border-2 border-indigo-300 flex items-center justify-center overflow-hidden shadow-sm relative transition-transform hover:scale-105 motion-reduce:transform-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-yellow-300 focus-visible:ring-offset-2 focus-visible:ring-offset-indigo-900",
      "aria-label": item.name,
      "aria-describedby": item.effectType && item.description ? "inventory-item-description-" + idx : void 0,
      "aria-busy": item.isLoading || void 0
    },
    item.image ? /* @__PURE__ */ React.createElement(
      "img",
      {
        loading: "lazy",
        src: item.image,
        alt: "",
        className: "w-full h-full object-contain pixelated",
        style: STYLE_IMAGE_PIXELATED,
        decoding: "async"
      }
    ) : item.icon ? /* @__PURE__ */ React.createElement("span", { className: "text-lg", "aria-hidden": "true" }, item.icon) : item.isLoading ? /* @__PURE__ */ React.createElement(RefreshCw, { size: 16, className: "text-indigo-200 animate-spin motion-reduce:animate-none", "aria-hidden": "true" }) : /* @__PURE__ */ React.createElement("span", { className: "text-sm font-bold text-indigo-200", "aria-hidden": "true" }, item.name.charAt(0))
  ), /* @__PURE__ */ React.createElement("div", { className: dismissedTooltip === idx ? "hidden" : "absolute top-full left-1/2 -translate-x-1/2 mt-2 hidden group-hover:flex group-focus-within:flex flex-col items-center z-50 w-36", role: "tooltip" }, /* @__PURE__ */ React.createElement("div", { className: "w-2 h-2 bg-black/95 rotate-45 -mb-1 border-t border-l border-white/30", "aria-hidden": "true" }), /* @__PURE__ */ React.createElement("div", { className: "bg-black/95 text-white text-xs px-2 py-1.5 rounded shadow-lg font-bold border border-white/30 text-center" }, item.name, item.effectType && item.description && /* @__PURE__ */ React.createElement("div", { id: "inventory-item-description-" + idx, className: "font-normal text-slate-100 text-xs mt-1 border-t border-white/20 pt-1" }, item.description)))))));
}, (prevProps, nextProps) => {
  if (prevProps.inventory === nextProps.inventory) return true;
  if (!prevProps.inventory || !nextProps.inventory) return false;
  if (prevProps.inventory.length !== nextProps.inventory.length) return false;
  for (let i = 0; i < prevProps.inventory.length; i++) {
    const prev = prevProps.inventory[i];
    const next = nextProps.inventory[i];
    if (!prev || !next) return false;
    if (prev.id !== next.id) return false;
    if (prev.image !== next.image) return false;
    if (prev.icon !== next.icon) return false;
    if (prev.name !== next.name) return false;
    if (prev.description !== next.description) return false;
    if (prev.effectType !== next.effectType) return false;
    if (prev.isLoading !== next.isLoading) return false;
  }
  return true;
});
var DiceOverlay = React.memo(({ result, onComplete }) => {
  const { t } = useContext(LanguageContext);
  const diceRef = useRef(null);
  const reduceMotion = typeof window !== "undefined" && window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  useFocusTrap(diceRef, true, onComplete);
  const onCompleteRef = useRef(onComplete);
  useEffect(() => {
    onCompleteRef.current = onComplete;
  }, [onComplete]);
  const [currentRotation, setCurrentRotation] = useState(() => {
    const rX = Math.floor(Math.random() * 360);
    const rY = Math.floor(Math.random() * 360);
    return `rotateX(${rX}deg) rotateY(${rY}deg) rotateZ(0deg)`;
  });
  useEffect(() => {
    playDiceSound();
    const spinCount = 4 + Math.floor(Math.random() * 2);
    const targetRotation = getD20Rotation(result, spinCount);
    const rollTimer = setTimeout(() => {
      setCurrentRotation(targetRotation);
    }, reduceMotion ? 0 : 50);
    const endTimer = setTimeout(() => onCompleteRef.current(), reduceMotion ? 1e3 : 3500);
    return () => {
      clearTimeout(rollTimer);
      clearTimeout(endTimer);
    };
  }, [reduceMotion, result]);
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      ref: diceRef,
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": "adventure-dice-result",
      className: "fixed inset-0 bg-black/60 z-[200] backdrop-blur-sm flex items-center justify-center",
      onClick: onComplete
    },
    /* @__PURE__ */ React.createElement("p", { id: "adventure-dice-result", className: "sr-only", role: "status", "aria-live": "assertive" }, t("adventure.dice_roll_result", { result })),
    /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: (e) => {
          e.stopPropagation();
          onComplete();
        },
        className: "absolute top-6 right-6 min-w-11 min-h-11 text-white hover:text-white bg-black/50 hover:bg-black/70 p-2 rounded-full transition-colors z-[202] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black",
        title: t("common.skip_animation"),
        "aria-label": t("common.skip_animation"),
        "data-alloflow-close-on-escape": "true"
      },
      /* @__PURE__ */ React.createElement(X, { size: 32, "aria-hidden": "true" })
    ),
    /* @__PURE__ */ React.createElement("div", { className: "dice-container", "aria-hidden": "true", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "dice",
        style: { transform: currentRotation }
      },
      Array.from({ length: 20 }, (_, i) => {
        const num = i + 1;
        const isResult = num === result;
        return /* @__PURE__ */ React.createElement(
          "div",
          {
            key: i,
            className: `face face-${num} ${isResult ? "bg-yellow-400 text-indigo-900 border-yellow-500 shadow-[inset_0_0_40px_rgba(255,255,255,0.4)]" : ""}`
          },
          num
        );
      })
    ))
  );
});
var AdventureShop = React.memo(({ gold, globalXP, onClose, onPurchase }) => {
  const shopRef = useRef(null);
  useFocusTrap(shopRef, true, onClose);
  const { t } = useContext(LanguageContext);
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      ref: shopRef,
      role: "dialog",
      "aria-modal": "true",
      "aria-label": t("adventure.shop"),
      className: "fixed inset-0 z-[150] bg-black/80 backdrop-blur-sm flex items-center justify-center p-4 animate-in fade-in duration-200",
      onClick: onClose
    },
    /* @__PURE__ */ React.createElement("div", { className: "bg-slate-900 border-4 border-indigo-500 rounded-3xl shadow-2xl max-w-2xl w-full overflow-hidden relative flex flex-col max-h-[90vh]", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("div", { className: "bg-indigo-600 p-3 sm:p-6 text-white flex justify-between items-center shrink-0 shadow-lg relative z-10" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h2", { className: "text-2xl font-black uppercase tracking-widest flex items-center gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "bg-yellow-400 text-indigo-900 p-2 rounded-lg shadow-inner border-2 border-indigo-800" }, /* @__PURE__ */ React.createElement(ShoppingBag, { size: 24, "aria-hidden": "true" })), t("adventure.shop")), /* @__PURE__ */ React.createElement("p", { className: "text-white text-sm font-bold mt-1 ml-1" }, t("adventure.shop_desc"))), /* @__PURE__ */ React.createElement("button", { onClick: onClose, className: "min-w-11 min-h-11 bg-indigo-800 hover:bg-indigo-700 text-white p-2 rounded-full transition-colors border-2 border-indigo-400 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-white", autoFocus: true, "aria-label": t("adventure.close_shop_aria") }, /* @__PURE__ */ React.createElement(X, { size: 24, "aria-hidden": "true" }))), /* @__PURE__ */ React.createElement("div", { className: "bg-slate-800 p-2 sm:p-4 flex justify-between items-center border-b border-slate-700 shrink-0 gap-2 sm:gap-4 flex-wrap sm:flex-nowrap" }, /* @__PURE__ */ React.createElement("div", { className: "flex gap-6" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 bg-slate-700 px-4 py-2 rounded-xl border border-slate-600" }, /* @__PURE__ */ React.createElement("span", { className: "text-2xl", "aria-hidden": "true" }, "\u{1F4B0}"), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-200 font-bold uppercase tracking-wider" }, t("adventure.gold")), /* @__PURE__ */ React.createElement("div", { className: "text-xl font-black text-yellow-400 leading-none" }, gold))), /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-2 bg-slate-700 px-4 py-2 rounded-xl border border-slate-600" }, /* @__PURE__ */ React.createElement("span", { className: "text-2xl", "aria-hidden": "true" }, "\u{1F3C6}"), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-[11px] text-slate-200 font-bold uppercase tracking-wider" }, t("adventure.global_xp")), /* @__PURE__ */ React.createElement("div", { className: "text-xl font-black text-green-400 leading-none" }, globalXP)))), /* @__PURE__ */ React.createElement("div", { className: "text-xs text-slate-300 italic text-right ml-auto" }, t("adventure.xp_earn_tip"))), /* @__PURE__ */ React.createElement("div", { className: "p-3 sm:p-6 overflow-y-auto custom-scrollbar bg-slate-900 grid grid-cols-1 sm:grid-cols-2 gap-3 sm:gap-4 flex-1 min-h-0" }, ADVENTURE_SHOP_ITEMS.map((item) => /* @__PURE__ */ React.createElement("div", { key: item.id, className: "bg-slate-800 border-2 border-slate-700 rounded-2xl p-3 sm:p-4 flex flex-col hover:border-indigo-500 transition-colors group relative overflow-hidden" }, /* @__PURE__ */ React.createElement("div", { className: "flex justify-between items-start mb-2 relative z-10" }, /* @__PURE__ */ React.createElement("div", { className: "text-2xl sm:text-4xl bg-slate-700 w-10 h-10 sm:w-16 sm:h-16 rounded-xl flex items-center justify-center shadow-inner border border-slate-600", "aria-hidden": "true" }, item.icon), /* @__PURE__ */ React.createElement("div", { className: "text-right" }, /* @__PURE__ */ React.createElement("div", { className: "text-yellow-400 font-black text-lg" }, item.cost, " G"), /* @__PURE__ */ React.createElement("span", { className: "inline-block max-w-36 truncate whitespace-nowrap text-[11px] font-bold uppercase text-slate-300 bg-slate-900 px-2 py-0.5 rounded border border-slate-700", title: t(`adventure.effects.${item.effectType}_label`) || t(`adventure.effects.${item.effectType}`) || item.effectType }, t(`adventure.effects.${item.effectType}_label`) || t(`adventure.effects.${item.effectType}`) || item.effectType))), /* @__PURE__ */ React.createElement("div", { className: "relative z-10 flex flex-col flex-1" }, /* @__PURE__ */ React.createElement("h3", { className: "text-white font-bold text-lg mb-1 group-hover:text-indigo-300 transition-colors" }, t(`adventure.shop_items.${item.id}_name`) || item.name), /* @__PURE__ */ React.createElement("p", { className: "text-slate-300 text-xs leading-relaxed mb-2 sm:mb-4 min-h-[2em] flex-1 overflow-y-auto custom-scrollbar" }, t(`adventure.shop_items.${item.id}_desc`) || item.description), /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: () => onPurchase(item),
        disabled: gold < item.cost,
        className: `w-full py-2 sm:py-2.5 rounded-xl font-bold text-sm sm:text-base flex items-center justify-center gap-2 transition-all active:scale-95 shrink-0 min-h-11 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-yellow-300 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-800 ${gold >= item.cost ? "bg-yellow-500 hover:bg-yellow-400 text-indigo-900 shadow-lg shadow-yellow-500/30 border-2 border-yellow-300 ring-1 ring-yellow-400/50" : "bg-slate-700 text-slate-300 cursor-not-allowed"}`
      },
      gold >= item.cost ? t("adventure.buy_now") : t("adventure.no_gold")
    )), /* @__PURE__ */ React.createElement("div", { className: "absolute inset-0 bg-gradient-to-br from-indigo-500/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity pointer-events-none" })))))
  );
});
var sanitizeAdventurePortraitFile = (file, { maxDimension = 1024, quality = 0.88 } = {}) => new Promise((resolve, reject) => {
  if (!file) {
    reject(new Error("No portrait file was selected."));
    return;
  }
  const reader = new FileReader();
  reader.onerror = () => reject(reader.error || new Error("The portrait file could not be read."));
  reader.onload = () => {
    const image = new Image();
    image.onerror = () => reject(new Error("The portrait image could not be decoded."));
    image.onload = () => {
      try {
        const sourceWidth = Number(image.naturalWidth || image.width);
        const sourceHeight = Number(image.naturalHeight || image.height);
        if (!sourceWidth || !sourceHeight) throw new Error("The portrait has invalid dimensions.");
        const boundedDimension = Math.max(320, Math.min(2048, Number(maxDimension) || 1024));
        const scale = Math.min(1, boundedDimension / Math.max(sourceWidth, sourceHeight));
        const canvas = document.createElement("canvas");
        canvas.width = Math.max(1, Math.round(sourceWidth * scale));
        canvas.height = Math.max(1, Math.round(sourceHeight * scale));
        const ctx = canvas.getContext("2d");
        if (!ctx) throw new Error("Portrait processing is unavailable in this browser.");
        ctx.fillStyle = "#ffffff";
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        ctx.drawImage(image, 0, 0, canvas.width, canvas.height);
        const sanitized = canvas.toDataURL("image/jpeg", Math.max(0.7, Math.min(0.95, Number(quality) || 0.88)));
        if (!sanitized?.startsWith("data:image/jpeg;base64,")) throw new Error("The sanitized portrait could not be encoded.");
        resolve(sanitized);
      } catch (error) {
        reject(error);
      }
    };
    image.src = String(reader.result || "");
  };
  reader.readAsDataURL(file);
});
var CastLobby = React.memo(({ characters, onUpdateCharacter, onConfirm, onGeneratePortrait, onRefinePortrait, onAddCharacter, onRemoveCharacter, onUploadPortrait, t }) => {
  const [editIdx, setEditIdx] = useState(null);
  const [editPrompt, setEditPrompt] = useState("");
  const [isAdding, setIsAdding] = useState(false);
  const [newName, setNewName] = useState("");
  const [newRole, setNewRole] = useState("");
  const [newAppearance, setNewAppearance] = useState("");
  const [editingField, setEditingField] = useState(null);
  const [editFieldValue, setEditFieldValue] = useState("");
  const [portraitUploadError, setPortraitUploadError] = useState(null);
  const [pendingPortraitUpload, setPendingPortraitUpload] = useState(null);
  const [sanitizingPortraitIdx, setSanitizingPortraitIdx] = useState(null);
  const isPortraitSanitizing = sanitizingPortraitIdx !== null;
  const hasTriggeredAutoGen = useRef(false);
  const portraitFileRefs = useRef({});
  const portraitUploadButtonRefs = useRef({});
  const portraitConsentButtonRef = useRef(null);
  const castRef = useRef(null);
  const castTitleRef = useRef(null);
  const charactersRef = useRef(characters || []);
  const portraitQueueRef = useRef([]);
  const activePortraitJobsRef = useRef(/* @__PURE__ */ new Map());
  const portraitQueueMountedRef = useRef(true);
  const [portraitQueueState, setPortraitQueueState] = useState({ activeKeys: [], queuedKeys: [] });
  charactersRef.current = characters || [];
  const getPortraitJobKey = (character, index) => String(character?.id || index + ":" + String(character?.name || "") + ":" + String(character?.role || ""));
  const syncPortraitQueueState = () => {
    if (!portraitQueueMountedRef.current) return;
    setPortraitQueueState({
      activeKeys: Array.from(activePortraitJobsRef.current.keys()),
      queuedKeys: portraitQueueRef.current.map((job) => job.key)
    });
  };
  const pumpPortraitQueue = () => {
    if (!portraitQueueMountedRef.current) return;
    while (activePortraitJobsRef.current.size < 2 && portraitQueueRef.current.length > 0) {
      const job = portraitQueueRef.current.shift();
      if (!job || activePortraitJobsRef.current.has(job.key)) continue;
      const currentCharacters = charactersRef.current || [];
      const currentIndex = job.characterId ? currentCharacters.findIndex((character) => character?.id === job.characterId) : currentCharacters.findIndex((character, index) => getPortraitJobKey(character, index) === job.key);
      const currentCharacter = currentCharacters[currentIndex];
      if (!currentCharacter || currentCharacter.isGenerating || !job.force && (currentCharacter.portrait || currentCharacter.isUserUploaded)) continue;
      activePortraitJobsRef.current.set(job.key, true);
      let generationPromise;
      try {
        generationPromise = onGeneratePortrait(currentIndex);
      } catch (error) {
        generationPromise = Promise.reject(error);
      }
      Promise.resolve(generationPromise).catch(() => {
      }).finally(() => {
        activePortraitJobsRef.current.delete(job.key);
        syncPortraitQueueState();
        pumpPortraitQueue();
      });
    }
    syncPortraitQueueState();
  };
  const enqueuePortrait = (index, { force = false } = {}) => {
    const character = charactersRef.current?.[index];
    if (!character || character.isGenerating || !force && (character.portrait || character.isUserUploaded)) return false;
    const key = getPortraitJobKey(character, index);
    if (activePortraitJobsRef.current.has(key) || portraitQueueRef.current.some((job) => job.key === key)) return false;
    portraitQueueRef.current.push({ key, characterId: character.id || null, force });
    syncPortraitQueueState();
    Promise.resolve().then(pumpPortraitQueue);
    return true;
  };
  const isPortraitQueued = (index) => portraitQueueState.queuedKeys.includes(getPortraitJobKey(characters?.[index], index));
  const isCharacterPortraitBusy = (index) => {
    const key = getPortraitJobKey(characters?.[index], index);
    return !!characters?.[index]?.isGenerating || portraitQueueState.activeKeys.includes(key);
  };
  const cancelQueuedPortrait = (index) => {
    const key = getPortraitJobKey(charactersRef.current?.[index], index);
    const nextQueue = portraitQueueRef.current.filter((job) => job.key !== key);
    if (nextQueue.length === portraitQueueRef.current.length) return false;
    portraitQueueRef.current = nextQueue;
    syncPortraitQueueState();
    return true;
  };
  const portraitReadyCount = (characters || []).filter((character) => !!character?.portrait).length;
  const portraitTotal = (characters || []).length;
  const portraitProgressPercent = portraitTotal > 0 ? Math.round(portraitReadyCount / portraitTotal * 100) : 0;
  const isPortraitQueueBusy = portraitQueueState.activeKeys.length > 0 || portraitQueueState.queuedKeys.length > 0 || (characters || []).some((character) => character?.isGenerating);
  useFocusTrap(castRef, true);
  useEffect(() => {
    portraitQueueMountedRef.current = true;
    const frame = requestAnimationFrame(() => castTitleRef.current?.focus());
    return () => {
      portraitQueueMountedRef.current = false;
      portraitQueueRef.current = [];
      cancelAnimationFrame(frame);
      window.AlloModules?.AdventureHandlers?.cancelAdventureEstablishingShot?.();
    };
  }, []);
  useEffect(() => {
    if (!pendingPortraitUpload) return void 0;
    const frame = requestAnimationFrame(() => portraitConsentButtonRef.current?.focus());
    return () => cancelAnimationFrame(frame);
  }, [pendingPortraitUpload]);
  const clearPendingPortraitUpload = (returnFocus = false) => {
    const pending = pendingPortraitUpload;
    if (pending?.input) pending.input.value = "";
    setPendingPortraitUpload(null);
    if (returnFocus && pending) {
      requestAnimationFrame(() => portraitUploadButtonRefs.current[pending.charIdx]?.focus());
    }
  };
  const acceptPendingPortraitUpload = async () => {
    const pending = pendingPortraitUpload;
    if (!pending) return;
    setPendingPortraitUpload(null);
    setSanitizingPortraitIdx(pending.charIdx);
    try {
      const sanitizedPortrait = await sanitizeAdventurePortraitFile(pending.file);
      if (onUploadPortrait) onUploadPortrait(pending.charIdx, sanitizedPortrait);
    } catch (error) {
      setPortraitUploadError({ charIdx: pending.charIdx, message: "This image could not be prepared securely. Please choose a different image." });
    } finally {
      pending.input.value = "";
      setSanitizingPortraitIdx(null);
    }
  };
  const handlePortraitFileChange = (charIdx, e) => {
    const file = e.target.files?.[0];
    if (!file) return;
    const input = e.target;
    const rejectUpload = (message) => {
      setPortraitUploadError({ charIdx, message });
      input.value = "";
    };
    if (!file.type.startsWith("image/")) {
      rejectUpload("Only image files can be used for character portraits.");
      return;
    }
    if (file.size > 5 * 1024 * 1024) {
      rejectUpload("Image too large (max 5MB). Please use a smaller image.");
      return;
    }
    cancelQueuedPortrait(charIdx);
    setPortraitUploadError(null);
    setPendingPortraitUpload({ charIdx, file, input });
  };
  useEffect(() => {
    if (!hasTriggeredAutoGen.current && characters?.length > 0) {
      hasTriggeredAutoGen.current = true;
      const prioritizedIndexes = characters.map((character, index) => ({ character, index })).sort((left, right) => {
        const leftIsLead = /protagonist|player/i.test(String(left.character?.role || ""));
        const rightIsLead = /protagonist|player/i.test(String(right.character?.role || ""));
        return Number(rightIsLead) - Number(leftIsLead);
      }).map((entry) => entry.index);
      prioritizedIndexes.forEach((index) => enqueuePortrait(index));
    }
  }, [characters?.length]);
  const startFieldEdit = (idx, field) => {
    setEditingField({ idx, field });
    setEditFieldValue(characters[idx]?.[field] || "");
  };
  const saveFieldEdit = () => {
    if (editingField && editFieldValue.trim()) {
      onUpdateCharacter(editingField.idx, { [editingField.field]: editFieldValue.trim() });
    }
    setEditingField(null);
    setEditFieldValue("");
  };
  return /* @__PURE__ */ React.createElement("div", { ref: castRef, role: "dialog", "aria-modal": "true", "aria-labelledby": "adventure-cast-lobby-title", className: "fixed inset-0 z-[250] bg-gradient-to-br from-violet-950/95 to-indigo-950/95 backdrop-blur-sm flex items-center justify-center p-4 animate-in fade-in duration-500" }, /* @__PURE__ */ React.createElement("div", { className: "bg-white rounded-3xl shadow-2xl max-w-4xl w-full max-h-[90vh] overflow-y-auto p-4 sm:p-8" }, /* @__PURE__ */ React.createElement("div", { className: "text-center mb-6" }, /* @__PURE__ */ React.createElement("span", { className: "text-4xl mb-2 block", "aria-hidden": "true" }, "\u{1F3AD}"), /* @__PURE__ */ React.createElement("h2", { ref: castTitleRef, id: "adventure-cast-lobby-title", tabIndex: -1, className: "text-2xl font-bold text-slate-800 focus:outline-none" }, t("adventure.cast_lobby") || "Meet Your Cast"), /* @__PURE__ */ React.createElement("p", { className: "text-sm text-slate-600 mt-1" }, t("adventure.cast_lobby_desc") || "Select any name, role, or description to edit. Portraits generate automatically.")), /* @__PURE__ */ React.createElement("div", { className: "grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-6" }, characters.map((char, i) => /* @__PURE__ */ React.createElement("div", { key: char.id || i, className: "bg-gradient-to-br from-slate-50 to-violet-50 rounded-2xl border border-violet-100 p-4 flex flex-col items-center text-center transition-all hover:shadow-lg hover:border-violet-300 relative group/card" }, /* @__PURE__ */ React.createElement("button", { type: "button", disabled: isPortraitSanitizing || isPortraitQueueBusy, onClick: () => {
    setPortraitUploadError(null);
    clearPendingPortraitUpload();
    onRemoveCharacter(i);
  }, className: "absolute top-2 right-2 w-6 h-6 rounded-full bg-red-100 text-red-700 hover:bg-red-200 hover:text-red-700 text-xs font-bold opacity-0 group-hover/card:opacity-100 group-focus-within/card:opacity-100 focus:opacity-100 transition-opacity flex items-center justify-center focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-700 focus-visible:ring-offset-2", title: t("adventure.remove_character"), "aria-label": (t("adventure.remove_character") || "Remove character") + ": " + (char.name || i + 1) }, "\u2715"), /* @__PURE__ */ React.createElement("div", { className: "w-24 h-24 rounded-full bg-violet-100 border-2 border-violet-200 flex items-center justify-center overflow-hidden mb-3 shadow-inner", "aria-busy": !!char.isGenerating, "aria-label": char.isGenerating ? t("adventure.generating_portrait_aria") || "Generating portrait for " + (char.name || "character") : void 0 }, char.isGenerating ? /* @__PURE__ */ React.createElement("div", { className: "animate-spin w-6 h-6 border-2 border-violet-400 border-t-transparent rounded-full", "aria-hidden": "true" }) : char.portrait ? /* @__PURE__ */ React.createElement("img", { src: char.portrait, alt: char.name, className: "w-full h-full object-cover rounded-full" }) : /* @__PURE__ */ React.createElement("span", { className: "text-3xl", "aria-hidden": "true" }, "\u{1F3AD}")), editingField?.idx === i && editingField?.field === "name" ? /* @__PURE__ */ React.createElement("input", { type: "text", "aria-label": t("adventure.edit_name") || "Edit character name", value: editFieldValue, onChange: (e) => setEditFieldValue(e.target.value), onBlur: saveFieldEdit, onKeyDown: (e) => {
    if (e.key === "Enter") saveFieldEdit();
    if (e.key === "Escape") {
      setEditingField(null);
      setEditFieldValue("");
    }
  }, autoFocus: true, className: "font-bold text-slate-800 text-sm text-center w-full px-2 py-0.5 border border-violet-300 rounded-lg focus:ring-2 focus:ring-violet-600 focus:outline-none bg-white" }) : /* @__PURE__ */ React.createElement("h3", null, /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => startFieldEdit(i, "name"), className: "min-w-11 min-h-11 px-2 font-bold text-slate-800 text-sm cursor-pointer hover:text-violet-700 hover:underline decoration-dashed underline-offset-2 transition-colors rounded focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2", title: t("adventure.edit_name"), "aria-label": (t("adventure.edit_name") || "Edit character name") + ": " + char.name }, char.name)), editingField?.idx === i && editingField?.field === "role" ? /* @__PURE__ */ React.createElement("input", { type: "text", "aria-label": t("adventure.edit_role") || "Edit character role", value: editFieldValue, onChange: (e) => setEditFieldValue(e.target.value), onBlur: saveFieldEdit, onKeyDown: (e) => {
    if (e.key === "Enter") saveFieldEdit();
    if (e.key === "Escape") {
      setEditingField(null);
      setEditFieldValue("");
    }
  }, autoFocus: true, className: "text-xs text-violet-600 font-medium text-center w-full px-2 py-0.5 border border-violet-300 rounded-lg focus:ring-2 focus:ring-violet-600 focus:outline-none bg-white mt-0.5" }) : /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => startFieldEdit(i, "role"), className: "min-w-11 min-h-11 px-2 text-xs text-violet-700 font-medium cursor-pointer hover:text-violet-800 hover:underline decoration-dashed underline-offset-2 transition-colors rounded focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2", title: t("adventure.edit_role"), "aria-label": (t("adventure.edit_role") || "Edit character role") + ": " + char.role }, char.role), editingField?.idx === i && editingField?.field === "appearance" ? /* @__PURE__ */ React.createElement("textarea", { "aria-label": t("adventure.edit_appearance") || "Edit character appearance", value: editFieldValue, onChange: (e) => setEditFieldValue(e.target.value), onBlur: saveFieldEdit, onKeyDown: (e) => {
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      saveFieldEdit();
    }
    if (e.key === "Escape") {
      setEditingField(null);
      setEditFieldValue("");
    }
  }, autoFocus: true, rows: 3, className: "text-[11px] text-slate-600 mt-1 leading-relaxed w-full px-2 py-1 border border-violet-300 rounded-lg focus:ring-2 focus:ring-violet-600 focus:outline-none bg-white resize-none" }) : /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => startFieldEdit(i, "appearance"), className: "min-w-11 min-h-11 px-2 text-[11px] text-slate-700 mt-1 leading-relaxed cursor-pointer hover:text-slate-800 hover:underline decoration-dashed underline-offset-2 transition-colors rounded focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2", title: t("adventure.edit_appearance"), "aria-label": (t("adventure.edit_appearance") || "Edit character appearance") + ": " + char.appearance }, char.appearance), char.portrait && !char.isGenerating && /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex flex-wrap gap-1.5 justify-center" }, char.isUserUploaded && /* @__PURE__ */ React.createElement("span", { className: "text-[11px] px-2 py-0.5 bg-emerald-50 text-emerald-700 rounded-full border border-emerald-600 font-medium" }, "\u{1F4F7} User Photo"), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => enqueuePortrait(i, { force: true }), className: "min-h-11 px-3 py-2 bg-violet-700 text-white rounded-full hover:bg-violet-800 transition-all font-bold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2", title: t("adventure.regen_portrait") }, "\u{1F504} ", char.isUserUploaded ? "AI Generate" : "Regenerate"), /* @__PURE__ */ React.createElement("input", { type: "file", "aria-label": t("adventure.upload_portrait") || "Upload portrait image", "aria-describedby": portraitUploadError?.charIdx === i ? `adventure-portrait-upload-error-${i}` : void 0, accept: "image/*", ref: (el) => portraitFileRefs.current[i] = el, onChange: (e) => handlePortraitFileChange(i, e), className: "hidden" }), /* @__PURE__ */ React.createElement("button", { type: "button", disabled: isPortraitSanitizing || isCharacterPortraitBusy(i), ref: (el) => portraitUploadButtonRefs.current[i] = el, "aria-describedby": portraitUploadError?.charIdx === i ? `adventure-portrait-upload-error-${i}` : void 0, onClick: () => {
    setPortraitUploadError(null);
    clearPendingPortraitUpload();
    portraitFileRefs.current[i]?.click();
  }, className: "min-h-11 px-3 py-2 bg-sky-50 text-sky-700 rounded-full hover:bg-sky-100 transition-all font-medium border border-sky-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sky-700 focus-visible:ring-offset-2", title: t("adventure.upload_portrait") || "Upload your own portrait image" }, "\u{1F4F7} Upload"), editIdx === i ? /* @__PURE__ */ React.createElement("div", { className: "w-full flex gap-1 mt-1" }, /* @__PURE__ */ React.createElement("input", { type: "text", "aria-label": t("adventure.portrait_refine_aria") || "Describe how to refine portrait", value: editPrompt, onChange: (e) => setEditPrompt(e.target.value), placeholder: t("adventure.portrait_refine_placeholder") || "e.g. Add green glasses", className: "flex-1 text-xs px-2 py-1 border border-violet-600 rounded-lg focus:ring-2 focus:ring-violet-400 focus:outline-none", onKeyDown: (e) => {
    if (e.key === "Enter" && editPrompt.trim()) {
      onRefinePortrait(i, editPrompt.trim());
      setEditIdx(null);
      setEditPrompt("");
    }
  } }), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": t("common.confirm") || "Apply portrait refinement", onClick: () => {
    if (editPrompt.trim()) {
      onRefinePortrait(i, editPrompt.trim());
      setEditIdx(null);
      setEditPrompt("");
    }
  }, className: "min-w-11 min-h-11 px-2 py-2 bg-violet-700 text-white rounded-lg font-bold hover:bg-violet-800 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2" }, "\u2713"), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": t("common.cancel") || "Cancel portrait refinement", onClick: () => {
    setEditIdx(null);
    setEditPrompt("");
  }, className: "min-w-11 min-h-11 px-2 py-2 bg-slate-200 text-slate-700 rounded-lg font-bold hover:bg-slate-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-700 focus-visible:ring-offset-2" }, "\u2717")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { type: "button", "aria-label": (t("adventure.edit_nanobanana") || "Refine portrait") + ": " + char.name, onClick: () => setEditIdx(i), className: "min-h-11 px-3 py-2 bg-slate-100 text-slate-700 rounded-full hover:bg-slate-200 transition-all font-medium border border-slate-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-700 focus-visible:ring-offset-2", title: t("adventure.edit_nanobanana") }, "\u270F\uFE0F Refine"), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => {
    const link = document.createElement("a");
    link.href = char.portrait;
    link.download = `${(char.name || "character").replace(/\s+/g, "_")}_portrait.png`;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  }, className: "min-h-11 px-3 py-2 bg-emerald-50 text-emerald-700 rounded-full hover:bg-emerald-100 transition-all font-medium border border-emerald-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-700 focus-visible:ring-offset-2" }, "\u{1F4BE} Save"))), !char.portrait && !char.isGenerating && !isPortraitQueued(i) && /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex flex-wrap gap-1.5 justify-center" }, /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => enqueuePortrait(i, { force: true }), className: "min-h-11 px-3 py-2 bg-violet-700 text-white rounded-full hover:bg-violet-800 transition-all font-bold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2" }, "\u{1F3A8} Generate Portrait"), /* @__PURE__ */ React.createElement("input", { type: "file", "aria-label": t("adventure.upload_portrait") || "Upload portrait image", "aria-describedby": portraitUploadError?.charIdx === i ? `adventure-portrait-upload-error-${i}` : void 0, accept: "image/*", ref: (el) => portraitFileRefs.current["new-" + i] = el, onChange: (e) => handlePortraitFileChange(i, e), className: "hidden" }), /* @__PURE__ */ React.createElement("button", { type: "button", disabled: isPortraitSanitizing || isCharacterPortraitBusy(i), ref: (el) => portraitUploadButtonRefs.current[i] = el, "aria-describedby": portraitUploadError?.charIdx === i ? `adventure-portrait-upload-error-${i}` : void 0, onClick: () => {
    setPortraitUploadError(null);
    clearPendingPortraitUpload();
    portraitFileRefs.current["new-" + i]?.click();
  }, className: "min-h-11 px-3 py-2 bg-sky-50 text-sky-700 rounded-full hover:bg-sky-100 transition-all font-medium border border-sky-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sky-700 focus-visible:ring-offset-2", title: t("adventure.upload_portrait") || "Upload your own portrait image" }, "\u{1F4F7} Upload Photo")), portraitUploadError?.charIdx === i && /* @__PURE__ */ React.createElement("p", { id: `adventure-portrait-upload-error-${i}`, role: "alert", "aria-atomic": "true", className: "w-full mt-2 rounded-lg border border-red-300 bg-red-50 px-3 py-2 text-xs font-semibold text-red-800" }, portraitUploadError.message), pendingPortraitUpload?.charIdx === i && /* @__PURE__ */ React.createElement("div", { role: "group", "aria-labelledby": `adventure-portrait-consent-${i}`, className: "w-full mt-2 rounded-xl border border-amber-400 bg-amber-50 px-3 py-3 text-left" }, /* @__PURE__ */ React.createElement("p", { id: `adventure-portrait-consent-${i}`, className: "text-xs font-semibold leading-relaxed text-amber-950" }, "Uploaded images are used by AI to keep this character consistent \u2014 the image is sent to the AI provider configured for this app with each scene. Only upload images you have permission to use this way (for photos of students, check your school's AI/data agreement)."), /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex flex-wrap gap-2" }, /* @__PURE__ */ React.createElement("button", { ref: portraitConsentButtonRef, type: "button", onClick: acceptPendingPortraitUpload, className: "min-h-11 px-4 py-2 rounded-lg bg-amber-800 text-white font-bold hover:bg-amber-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-800 focus-visible:ring-offset-2" }, "Use image"), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => clearPendingPortraitUpload(true), className: "min-h-11 px-4 py-2 rounded-lg border border-slate-500 bg-white text-slate-800 font-bold hover:bg-slate-100 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-700 focus-visible:ring-offset-2" }, "Cancel"))), sanitizingPortraitIdx === i && /* @__PURE__ */ React.createElement("p", { id: "adventure-portrait-sanitizing-status", className: "mt-2 text-[11px] text-sky-800 font-semibold", role: "status", "aria-live": "polite" }, "Preparing image securely\u2026"), isPortraitQueued(i) && !char.isGenerating && /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-[11px] text-violet-700 font-medium", role: "status", "aria-live": "polite" }, "Waiting its turn\u2026"), char.isGenerating && /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-[11px] text-violet-700 animate-pulse font-medium", role: "status", "aria-live": "polite" }, "Creating portrait\u2026"))), isAdding ? /* @__PURE__ */ React.createElement("div", { className: "bg-gradient-to-br from-violet-50 to-indigo-50 rounded-2xl border-2 border-dashed border-violet-300 p-4 flex flex-col items-center text-center" }, /* @__PURE__ */ React.createElement("span", { className: "text-2xl mb-2", "aria-hidden": "true" }, "\u2728"), /* @__PURE__ */ React.createElement("input", { type: "text", "aria-label": t("adventure.char_name_placeholder") || "Character name", value: newName, onChange: (e) => setNewName(e.target.value), placeholder: t("adventure.char_name_placeholder"), className: "w-full text-sm px-3 py-1.5 mb-2 border border-violet-600 rounded-lg focus:ring-2 focus:ring-violet-600 focus:outline-none text-center font-bold" }), /* @__PURE__ */ React.createElement("input", { type: "text", "aria-label": t("adventure.role_placeholder") || "Character role", value: newRole, onChange: (e) => setNewRole(e.target.value), placeholder: t("adventure.role_placeholder"), className: "w-full text-xs px-3 py-1.5 mb-2 border border-violet-600 rounded-lg focus:ring-2 focus:ring-violet-600 focus:outline-none text-center" }), /* @__PURE__ */ React.createElement("input", { type: "text", "aria-label": t("adventure.appearance_placeholder") || "Character appearance", value: newAppearance, onChange: (e) => setNewAppearance(e.target.value), placeholder: t("adventure.appearance_placeholder") || "Appearance (e.g. tall, silver hair, blue robe)", className: "w-full text-xs px-3 py-1.5 mb-2 border border-violet-600 rounded-lg focus:ring-2 focus:ring-violet-400 focus:outline-none text-center", onKeyDown: (e) => {
    if (e.key === "Enter" && newName.trim()) {
      onAddCharacter({ name: newName.trim(), role: newRole.trim() || "Character", appearance: newAppearance.trim() || newName.trim(), portrait: null, isGenerating: false });
      setNewName("");
      setNewRole("");
      setNewAppearance("");
      setIsAdding(false);
    }
  } }), /* @__PURE__ */ React.createElement("div", { className: "flex gap-1.5 mt-1" }, /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => {
    if (newName.trim()) {
      onAddCharacter({ name: newName.trim(), role: newRole.trim() || "Character", appearance: newAppearance.trim() || newName.trim(), portrait: null, isGenerating: false });
      setNewName("");
      setNewRole("");
      setNewAppearance("");
      setIsAdding(false);
    }
  }, className: "min-h-11 px-3 py-2 bg-violet-700 text-white rounded-lg font-bold hover:bg-violet-800 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2" }, "Add"), /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => {
    setIsAdding(false);
    setNewName("");
    setNewRole("");
    setNewAppearance("");
  }, className: "min-h-11 px-3 py-2 bg-slate-200 text-slate-700 rounded-lg font-bold hover:bg-slate-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-700 focus-visible:ring-offset-2" }, "Cancel"))) : /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => setIsAdding(true), className: "bg-gradient-to-br from-slate-50 to-violet-50 rounded-2xl border-2 border-dashed border-violet-600 p-4 flex flex-col items-center justify-center text-center transition-all hover:shadow-lg hover:border-violet-700 hover:from-violet-50 hover:to-indigo-50 min-h-[180px] cursor-pointer group focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-4xl mb-2 group-hover:scale-110 transition-transform", "aria-hidden": "true" }, "\u2795"), /* @__PURE__ */ React.createElement("span", { className: "font-bold text-sm text-violet-600" }, t("adventure.add_character")), /* @__PURE__ */ React.createElement("span", { className: "text-[11px] text-slate-600 mt-0.5" }, t("adventure.create_cast_member")))), /* @__PURE__ */ React.createElement("div", { className: "mb-4 max-w-xl mx-auto", role: "status", "aria-live": "polite", "aria-atomic": "true" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between gap-3 text-xs font-bold text-slate-700 mb-1.5" }, /* @__PURE__ */ React.createElement("span", null, "Portraits ready"), /* @__PURE__ */ React.createElement("span", null, portraitReadyCount, " of ", portraitTotal)), /* @__PURE__ */ React.createElement("div", { className: "h-2.5 overflow-hidden rounded-full bg-slate-200 border border-slate-300", role: "progressbar", "aria-label": "Cast portraits ready", "aria-valuemin": 0, "aria-valuemax": portraitTotal, "aria-valuenow": portraitReadyCount }, /* @__PURE__ */ React.createElement("div", { className: "h-full bg-gradient-to-r from-violet-600 to-indigo-600 transition-all duration-500 motion-reduce:transition-none", style: { width: portraitProgressPercent + "%" } })), /* @__PURE__ */ React.createElement("p", { className: "mt-1.5 text-[11px] text-slate-600 text-center" }, portraitReadyCount === portraitTotal ? "Your cast is ready." : isPortraitQueueBusy ? "Creating up to two portraits at a time. You can begin whenever you are ready." : "You can begin now or generate the remaining portraits for stronger visual consistency.")), /* @__PURE__ */ React.createElement("div", { className: "flex flex-wrap justify-center gap-3" }, /* @__PURE__ */ React.createElement("button", { type: "button", disabled: isPortraitQueueBusy || portraitReadyCount === portraitTotal, onClick: () => {
    characters.forEach((character, i) => {
      if (!character.portrait && !character.isGenerating && !character.isUserUploaded) enqueuePortrait(i);
    });
  }, className: "min-h-11 px-5 py-2.5 bg-violet-100 text-violet-700 font-bold rounded-xl hover:bg-violet-200 transition-all text-sm border border-violet-600 disabled:opacity-50 disabled:cursor-not-allowed focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2" }, /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true" }, "\u{1F3A8}"), " ", isPortraitQueueBusy ? "Creating portraits\u2026" : portraitReadyCount === portraitTotal ? "Portraits Ready" : t("adventure.generate_all") || "Generate Missing Portraits"), /* @__PURE__ */ React.createElement("button", { type: "button", disabled: isPortraitSanitizing, "aria-describedby": isPortraitSanitizing ? "adventure-portrait-sanitizing-status" : void 0, onClick: () => {
    window.AlloModules?.AdventureHandlers?.cancelAdventureEstablishingShot?.();
    onConfirm();
  }, className: "min-h-11 px-6 py-2.5 bg-violet-700 text-white font-bold rounded-xl hover:bg-violet-800 shadow-lg hover:shadow-xl transition-all text-sm hover:scale-105 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-700 focus-visible:ring-offset-2" }, /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true" }, "\u2694\uFE0F"), " ", t("adventure.begin_adventure") || "Begin Adventure"))));
});

  window.AlloModules.MissionReportCard = MissionReportCard;
  window.AlloModules.ClimaxProgressBar = ClimaxProgressBar;
  window.AlloModules.AdventureAmbience = AdventureAmbience;
  window.AlloModules.InventoryGrid = InventoryGrid;
  window.AlloModules.DiceOverlay = DiceOverlay;
  window.AlloModules.AdventureShop = AdventureShop;
  window.AlloModules.CastLobby = CastLobby;
  window.AlloModules.sanitizeAdventurePortraitFile = sanitizeAdventurePortraitFile;
  window.AlloModules.playAdventureEventSound = playAdventureEventSound;
  window.AlloModules.playGenerativeSoundscape = playGenerativeSoundscape;
  window.AlloModules.playDiceSound = playDiceSound;
  window.AlloModules.getD20Rotation = getD20Rotation;
  window.AlloModules.AdventureModule = true;

  console.log('[AdventureModule] 7 components + 4 utilities registered');
})();
