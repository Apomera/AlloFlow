ed labels from scratch on the diagram.";             const analysisPrompt = `You are an educational assessment evaluator reviewing a student diagram labeling exercise. Concept: "${visualPlan.title || "diagram"}
 React.useState(false);     const [isMinimized, setIsMinimized] = React.useState(false);     const [assessmentCenterTab, setAssessmentCenterTab] = React.useState("assessments");     const [researchStudent, setR
    const [isMinimized, setIsMinimized] = React.useState(false);     const [assessmentCenterTab, setAssessmentCenterTab] = React.useState("assessments");     const [researchStudent, setResearchStudent] = React.
 = React.useState(false);     const [assessmentCenterTab, setAssessmentCenterTab] = React.useState("assessments");     const [researchStudent, setResearchStudent] = React.useState(null);     const [showCBMImpor
 }             if (er.puzzlesSolved > 0) stats.totalActivities++;         }         if (data.fluencyAssessments && data.fluencyAssessments.length > 0) {             const latest = data.fluencyAssessments[data.f
esSolved > 0) stats.totalActivities++;         }         if (data.fluencyAssessments && data.fluencyAssessments.length > 0) {             const latest = data.fluencyAssessments[data.fluencyAssessments.length - 
.fluencyAssessments && data.fluencyAssessments.length > 0) {             const latest = data.fluencyAssessments[data.fluencyAssessments.length - 1];             stats.fluencyWCPM = latest.wcpm || 0;            
ata.fluencyAssessments.length > 0) {             const latest = data.fluencyAssessments[data.fluencyAssessments.length - 1];             stats.fluencyWCPM = latest.wcpm || 0;             stats.totalActivities +
             stats.fluencyWCPM = latest.wcpm || 0;             stats.totalActivities += data.fluencyAssessments.length;         }         if (data.personaState) {             stats.interviewXP = data.personaSta
y below ${t3.fluencyMin} WCPM`);             recs.push('Implement repeated reading with the fluency assessment tool; track WCPM trend weekly');         }          if (stats.labelChallengeAvg > 0 && stats.labelC
luency-record' && h.data?.metrics);         if (fluencyRecords.length === 0) { addToast('No fluency assessments to export', 'warning'); return; }         const headers = ['Date', 'Passage', 'WCPM', 'Accuracy %'
createElement('a');         link.href = URL.createObjectURL(blob);         link.download = 'Fluency_Assessments_' + new Date().toISOString().split('T')[0] + '.csv';         document.body.appendChild(link);     
 },             { id: 'efficiency', text: 'How much time does AlloFlow save compared to traditional assessment?', labels: ['None', 'A little', 'Some', 'A lot', 'Significant'] },             { id: 'satisfaction'
 <div class="sheet"><div class="hdr"><div><h1>\ud83d\udcca Oral Fluency Score Sheet</h1><p>AlloFlow Assessment Record</p></div><div style="text-align:right;font-size:11px;"><div>Generated: ${new Date().toLocale
/div>                 </div>`;         };          let runningRecordHtml = '';         const fluencyAssessments = student.data?.fluencyAssessments;         if (fluencyAssessments?.length > 0) {             cons
    };          let runningRecordHtml = '';         const fluencyAssessments = student.data?.fluencyAssessments;         if (fluencyAssessments?.length > 0) {             const latest = fluencyAssessments[fluen
dHtml = '';         const fluencyAssessments = student.data?.fluencyAssessments;         if (fluencyAssessments?.length > 0) {             const latest = fluencyAssessments[fluencyAssessments.length - 1];      
fluencyAssessments;         if (fluencyAssessments?.length > 0) {             const latest = fluencyAssessments[fluencyAssessments.length - 1];             if (latest?.wordData && typeof calculateRunningRecordM
         if (fluencyAssessments?.length > 0) {             const latest = fluencyAssessments[fluencyAssessments.length - 1];             if (latest?.wordData && typeof calculateRunningRecordMetrics === 'functio
 ${metricBar('Games Played', s.gamesPlayed, 10, '', '🎮')}              <div class="section-title">📋 Assessment Basis</div>             <div style="display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 16px;"
return;         if (trendChartInstance.current) trendChartInstance.current.destroy();         const assessments = selectedStudent.data?.fluencyAssessments;         if (!assessments || assessments.length < 2) re
ent) trendChartInstance.current.destroy();         const assessments = selectedStudent.data?.fluencyAssessments;         if (!assessments || assessments.length < 2) return;         const labels = assessments.ma
rrent.destroy();         const assessments = selectedStudent.data?.fluencyAssessments;         if (!assessments || assessments.length < 2) return;         const labels = assessments.map((a, i) => a.date ? new D
;         const assessments = selectedStudent.data?.fluencyAssessments;         if (!assessments || assessments.length < 2) return;         const labels = assessments.map((a, i) => a.date ? new Date(a.date).toL
encyAssessments;         if (!assessments || assessments.length < 2) return;         const labels = assessments.map((a, i) => a.date ? new Date(a.date).toLocaleDateString() : `#${i + 1}`);         const wcpmDat
p((a, i) => a.date ? new Date(a.date).toLocaleDateString() : `#${i + 1}`);         const wcpmData = assessments.map(a => a.wcpm || 0);         trendChartInstance.current = new Chart(trendChartRef.current, {    
ssName="text-xl font-bold text-slate-800">{isIndependentMode ? '\u{1F4CA} My Learning Journey' : '🎯 Assessment Center'}</h2>                             {importedStudents.length > 0 && (                        
te-200 bg-slate-50/50 px-4 shrink-0">                         {[                             { id: 'assessments', label: '🎯 Assessments', desc: 'Probes & benchmarks' },                             { id: 'resear
-4 shrink-0">                         {[                             { id: 'assessments', label: '🎯 Assessments', desc: 'Probes & benchmarks' },                             { id: 'research', label: '📊 Research 
                ].map(tab => (                             <button key={tab.id} onClick={() => { setAssessmentCenterTab(tab.id); if (tab.id === 'research' && researchFirstVisit) { setResearchFirstVisit(false); 
      className={`flex items-center gap-2 px-4 py-2.5 text-sm font-bold border-b-2 transition-all ${assessmentCenterTab === tab.id ? 'border-indigo-600 text-indigo-700 bg-white' : 'border-transparent text-slate
span>{tab.label}</span>                                 <span className={`text-[10px] font-normal ${assessmentCenterTab === tab.id ? 'text-indigo-400' : 'text-slate-400'}`}>{tab.desc}</span>                    
               <div className="flex-1 overflow-y-auto p-4" style={{ display: (!isIndependentMode && assessmentCenterTab === 'research') ? 'none' : undefined }}>                     {!isIndependentMode && import
-4 bg-gradient-to-r from-violet-50 to-indigo-50 border border-violet-200 rounded-xl" data-help-key="assessment_probe_launcher">                         <div className="flex items-center gap-2 mb-3">            
mb-3">Curated word lists with fixed activity order per grade. No gamification — designed for formal assessment.</p>                         <div className="flex gap-2 items-center flex-wrap">                   
 mb-3">Nonsense Word Fluency (NWF), Letter Naming Fluency (LNF), and Rapid Automatized Naming (RAN) assessments.</p>                                 <div className="grid grid-cols-1 sm:grid-cols-3 gap-2">      
                ];                                 const fluencyData = selectedStudent.data?.fluencyAssessments?.map(a => a.wcpm || 0) || [];                                 const gameScores = selectedStudent.d
          <div style={{ fontSize: '11px', fontWeight: 700, color: '#475569', marginBottom: '4px' }}>Assessment Basis:</div>                                             <div className="flex flex-wrap gap-1">    
];                                                 const fluencyData = selectedStudent.data?.fluencyAssessments?.map(a => ({ value: a.wcpm || 0, date: a.timestamp || a.date })) || [];                           
    </div>                             )}                             {selectedStudent.data?.fluencyAssessments?.length > 0 && (                                 <div className="mb-4 space-y-3">                 
       {(() => {                                         const latest = selectedStudent.data.fluencyAssessments[selectedStudent.data.fluencyAssessments.length - 1];                                         if (!
                 const latest = selectedStudent.data.fluencyAssessments[selectedStudent.data.fluencyAssessments.length - 1];                                         if (!latest?.wordData) return null;          
                             })()}                                     {selectedStudent.data.fluencyAssessments.length >= 2 && (                                         <div className="bg-white border border-sl
       {(() => {                                         const latest = selectedStudent.data.fluencyAssessments[selectedStudent.data.fluencyAssessments.length - 1];                                         if (!
                 const latest = selectedStudent.data.fluencyAssessments[selectedStudent.data.fluencyAssessments.length - 1];                                         if (!latest?.wcpm || typeof getBenchmarkCompa
    {t('class_analytics.no_transcript')}                                     {!isIndependentMode && assessmentCenterTab === 'research' && (                                         <div className="flex-1 overflo
                                          <p className="text-sm text-slate-500 mb-4">Import student assessment data to unlock research insights and growth tracking.</p>                                          
wth tracking.</p>                                                         <button onClick={() => setAssessmentCenterTab('assessments')}                                                             className="px-
                                                     <button onClick={() => setAssessmentCenterTab('assessments')}                                                             className="px-4 py-2 bg-indigo-600 
-lg text-sm hover:bg-indigo-700">                                                             Go to Assessments →                                                         </button>                               
mes': usage = "(Writing Support / Independent Practice)"; break;             case 'quiz': usage = "(Assessment / Closure)"; break;             case 'math': usage = "(STEM Problem Solving)"; break;             c
gn with this simplified content.",         simplified_teacher_tools: "Access teacher-only tools for assessment and customization options.",         wizard_back_results_btn: "Return to the search results to sele
studentProgressLog = [], pointHistory = [], wordSoundsBadges = {},     gameCompletions = [], fluencyAssessments = [], labelChallengeResults = [],     wordSoundsScore = { streak: 0 },     isParentMode = false, i
Activities,             gamesPlayed: gameCompletions?.length || 0,             fluencyTests: fluencyAssessments?.length || 0,             labelChallenges: labelChallengeResults?.length || 0,             session
       trend         };     }, [history, wordSoundsHistory, phonemeMastery, gameCompletions, fluencyAssessments, labelChallengeResults, studentProgressLog]);      const heading = isParentMode         ? (selecte
}</p>`}             <h2 style="color: #1e293b; margin-top: 40px; margin-bottom: 20px;">📊 Research & Assessment Data</h2>             <div style="display: flex; gap: 20px; margin-bottom: 20px;">                 
ry || selectedStudent.interventionLogs || selectedStudent.surveyResponses || selectedStudent.fluencyAssessments) && (                      <div className="bg-white p-6 rounded-xl border border-slate-200 shadow-
xt-slate-500 uppercase tracking-wider mb-4 flex items-center gap-2">                              📊 Assessment & Research Data                          </h4>                          <div className="grid grid-c
                   )}                             </div>                             {/* Research & Assessment Analytics */}                             {(() => {                                 const allProbes
ord_sounds_module.js');   }, []);    const [stemLabTool, setStemLabTool] = useState(null);   const [assessmentBlocks, setAssessmentBlocks] = useState([]);   const [toolSnapshots, setToolSnapshots] = useState([]
);   }, []);    const [stemLabTool, setStemLabTool] = useState(null);   const [assessmentBlocks, setAssessmentBlocks] = useState([]);   const [toolSnapshots, setToolSnapshots] = useState([]);   const [showAsses
smentBlocks] = useState([]);   const [toolSnapshots, setToolSnapshots] = useState([]);   const [showAssessmentBuilder, setShowAssessmentBuilder] = useState(false);   const [stemLabCreateMode, setStemLabCreateMo
);   const [toolSnapshots, setToolSnapshots] = useState([]);   const [showAssessmentBuilder, setShowAssessmentBuilder] = useState(false);   const [stemLabCreateMode, setStemLabCreateMode] = useState('topic');  
{ warnLog("Failed to load escape completions", e); }       }       return [];   });   const [fluencyAssessments, setFluencyAssessments] = useState(() => {       if (typeof window !== 'undefined') {           tr
ad escape completions", e); }       }       return [];   });   const [fluencyAssessments, setFluencyAssessments] = useState(() => {       if (typeof window !== 'undefined') {           try {               const
eof window !== 'undefined') {           try {               const saved = safeGetItem('allo_fluency_assessments');               if (saved) return JSON.parse(saved);           } catch (e) { warnLog("Failed to l
       if (saved) return JSON.parse(saved);           } catch (e) { warnLog("Failed to load fluency assessments", e); }       }       return [];   });   const [flashcardEngagement, setFlashcardEngagement] = use
 lzLoaded]);   useEffect(() => {       if (!lzLoaded) return;       try { safeSetItem('allo_fluency_assessments', JSON.stringify(fluencyAssessments)); } catch(e) { warnLog('localStorage write failed', e); }    
   if (!lzLoaded) return;       try { safeSetItem('allo_fluency_assessments', JSON.stringify(fluencyAssessments)); } catch(e) { warnLog('localStorage write failed', e); }    }, [fluencyAssessments, lzLoaded]); 
ringify(fluencyAssessments)); } catch(e) { warnLog('localStorage write failed', e); }    }, [fluencyAssessments, lzLoaded]);   useEffect(() => {       if (!lzLoaded) return;       try { safeSetItem('allo_flashc
}       });       const result = {           id: 'math-result-' + Date.now(),           type: 'stem-assessment',           title: (generatedContent.title || 'Math Assessment') + ' — Results',           timestam
 + Date.now(),           type: 'stem-assessment',           title: (generatedContent.title || 'Math Assessment') + ' — Results',           timestamp: Date.now(),           data: {               score: correct, 
neratedContent.id           }       };       setHistory(prev => [...prev, result]);       addToast('Assessment submitted: ' + correct + '/' + total + ' (' + Math.round((correct/total)*100) + '%)', 'success');  
                quizAvg,                   wsAccuracy: wsAcc,                   fluencyWCPM: fluencyAssessments?.length > 0 ? (fluencyAssessments[fluencyAssessments.length - 1]?.wcpm || 0) : 0,                 
         wsAccuracy: wsAcc,                   fluencyWCPM: fluencyAssessments?.length > 0 ? (fluencyAssessments[fluencyAssessments.length - 1]?.wcpm || 0) : 0,                   gamesPlayed: gameCompletions?.le
: wsAcc,                   fluencyWCPM: fluencyAssessments?.length > 0 ? (fluencyAssessments[fluencyAssessments.length - 1]?.wcpm || 0) : 0,                   gamesPlayed: gameCompletions?.length || 0,         
History?.filter(h => h.correct)?.length || 0               },               fluencyHistory: (fluencyAssessments || []).slice(-10).map(a => ({ wcpm: a.wcpm, date: a.timestamp || a.date })),               gameSco
[isCanvas, activeSessionCode, studentNickname, history, studentResponses, wordSoundsHistory, fluencyAssessments, gameCompletions, labelChallengeResults, globalPoints]);    React.useEffect(() => {       if (isCa
elationship (max 10 words)"}   ],   "overallScore": 1-5,   "summary": "one-sentence overall quality assessment (max 25 words)" } Keep coverageGaps to max 3 suggestions. Keep conceptConnections to max 2.`;      
le="margin: 0 0 5px 0; color: #475569; font-size: 0.9em; text-transform: uppercase;">${t('alignment.assessment')}</h4>                                   <div style="background: #f8fafc; padding: 10px; border-ra
x solid #e2e8f0;">                                       <strong>Status:</strong> ${report.analysis.assessmentAlignment.status}<br/>                                       <strong>Evidence:</strong> "<em>${repor
tatus}<br/>                                       <strong>Evidence:</strong> "<em>${report.analysis.assessmentAlignment.evidence}</em>"                                   </div>                               </d
estId = `MANIFEST-${generateUUID()}`;       const resourceId = `RES-${generateUUID()}`;       const assessmentId = `QU-${generateUUID()}`;       const title = escapeXml(generatedContent.title || t('export.qti_d
<organizations/>   <resources>     <resource identifier="${resourceId}" type="imsqti_xmlv1p2" href="assessment.xml">       <file href="assessment.xml"/>     </resource>   </resources> </manifest>`;       zip.fi
<resource identifier="${resourceId}" type="imsqti_xmlv1p2" href="assessment.xml">       <file href="assessment.xml"/>     </resource>   </resources> </manifest>`;       zip.file("imsmanifest.xml", manifestXml);
/resource>   </resources> </manifest>`;       zip.file("imsmanifest.xml", manifestXml);       const assessmentXmlHeader = `<?xml version="1.0" encoding="UTF-8"?> <questestinterop xmlns="http://www.imsglobal.org
"http://www.imsglobal.org/xsd/ims_qtiasiv1p2 http://www.imsglobal.org/xsd/ims_qtiasiv1p2p1.xsd">   <assessment ident="${assessmentId}" title="${title}">     <section ident="root_section">`;       let itemsXml =
l.org/xsd/ims_qtiasiv1p2 http://www.imsglobal.org/xsd/ims_qtiasiv1p2p1.xsd">   <assessment ident="${assessmentId}" title="${title}">     <section ident="root_section">`;       let itemsXml = "";       const que
    </render_fib>         </response_str>       </presentation>     </item>`;       });       const assessmentXmlFooter = `     </section>   </assessment> </questestinterop>`;       const assessmentXmlContent =
     </presentation>     </item>`;       });       const assessmentXmlFooter = `     </section>   </assessment> </questestinterop>`;       const assessmentXmlContent = assessmentXmlHeader + itemsXml + assessmen
      const assessmentXmlFooter = `     </section>   </assessment> </questestinterop>`;       const assessmentXmlContent = assessmentXmlHeader + itemsXml + assessmentXmlFooter;       zip.file("assessment.xml", 
mlFooter = `     </section>   </assessment> </questestinterop>`;       const assessmentXmlContent = assessmentXmlHeader + itemsXml + assessmentXmlFooter;       zip.file("assessment.xml", assessmentXmlContent); 
ssessment> </questestinterop>`;       const assessmentXmlContent = assessmentXmlHeader + itemsXml + assessmentXmlFooter;       zip.file("assessment.xml", assessmentXmlContent);       try {           const conte
 const assessmentXmlContent = assessmentXmlHeader + itemsXml + assessmentXmlFooter;       zip.file("assessment.xml", assessmentXmlContent);       try {           const content = await zip.generateAsync({ type: 
XmlContent = assessmentXmlHeader + itemsXml + assessmentXmlFooter;       zip.file("assessment.xml", assessmentXmlContent);       try {           const content = await zip.generateAsync({ type: "blob" });       
act.duration} `;               });               textContent += ` `;           }           if (data.assessmentIdeas && data.assessmentIdeas.length > 0) {               textContent += `${t('lesson_plan.assessmen
       });               textContent += ` `;           }           if (data.assessmentIdeas && data.assessmentIdeas.length > 0) {               textContent += `${t('lesson_plan.assessment_header')}: `;         
ssessmentIdeas && data.assessmentIdeas.length > 0) {               textContent += `${t('lesson_plan.assessment_header')}: `;               data.assessmentIdeas.forEach(idea => textContent += `• ${idea} `);     
h > 0) {               textContent += `${t('lesson_plan.assessment_header')}: `;               data.assessmentIdeas.forEach(idea => textContent += `• ${idea} `);           }           await navigator.clipboard.
AudioLibrary,                   sessionScore: wordSoundsScore               },               fluencyAssessments: fluencyAssessments,               flashcardEngagement: flashcardEngagement,               timeOnT
            sessionScore: wordSoundsScore               },               fluencyAssessments: fluencyAssessments,               flashcardEngagement: flashcardEngagement,               timeOnTask: timeOnTask,    
.wordSoundsState.confusionPatterns);                      }                      if (rawData.fluencyAssessments) setFluencyAssessments(rawData.fluencyAssessments);                      if (rawData.flashcardEnga
ionPatterns);                      }                      if (rawData.fluencyAssessments) setFluencyAssessments(rawData.fluencyAssessments);                      if (rawData.flashcardEngagement) setFlashcardEng
        }                      if (rawData.fluencyAssessments) setFluencyAssessments(rawData.fluencyAssessments);                      if (rawData.flashcardEngagement) setFlashcardEngagement(rawData.flashcardEn
('scaffolds.title');           case 'adventure': return t('adventure.title');            case 'stem-assessment': return 'STEM Assessment';       case 'explore-challenge': return `🎯 ${item.label || 'Explore Chal
     case 'adventure': return t('adventure.title');            case 'stem-assessment': return 'STEM Assessment';       case 'explore-challenge': return `🎯 ${item.label || 'Explore Challenge'}`;           case '
ual Anchor.\n`;       }       if (quizItem?.data?.questions) {           context += `\n--- CONTEXT: ASSESSMENT (Exit Ticket) ---\nHas ${quizItem.data.questions.length} Multiple Choice Questions and ${quizItem.d
ference** the "Writing Scaffolds" or "Worksheets" (Leveled Text) if available.         8. Closure & Assessment: A closing discussion script. **Explicitly reference** the "Exit Ticket" (Quiz) provided.         9
se if (type === 'quiz') {               contextInstruction = `                   Focus on formative assessment or checking for understanding (e.g., "Use this as an exit ticket...").               `;            
de. Explain concepts simply. Focus on fun, bonding activities, and reinforcement rather than strict assessment. If the parent mentions an IEP, explain the goals in plain English.";           const teacherSystem
tline' type based on topology (e.g. Comparative -> Venn, Procedural -> Flow Chart).             - **Assessment**: Quiz should test the Golden Thread.             ${allowDuplicates ? '**HIGH VOLUME STRATEGY**: S
dPractice}                      INDEPENDENT PRACTICE: ${d.independentPractice}                      ASSESSMENT/CLOSURE: ${d.closure}                      `;                  case 'quiz':                      if
zers, and Games (Timeline/Sorts) force students to practice the specific skills?                - **Assessment Alignment:** Does the Quiz or Adventure outcome verify mastery of the standard?             3. GAP 
se activities practice the standard's skills.",                         },                         "assessmentAlignment": {                             "status": "Aligned" | "Partially Aligned" | "Not Aligned",
setIsGeneratingRubric(true);     try {         const rubricType = isIndependentMode ? "Student Self-Assessment Checklist" : "Grading Rubric";         const scaleDesc = isIndependentMode             ? "Use a sim
er-white/10 hover:border-white/30 ring-1 ring-violet-400/40"                                 title="Assessment Center"                                 data-help-key="header_analytics"                           
   >                                 <ClipboardList size={14} /> <span className="hidden lg:inline">Assessment Center</span>                             </button>                         {!isTeacherMode && (   
               <div className="space-y-8">                             {['creation', 'activities', 'assessment', 'platform'].map(catKey => {                                 const featuresList = t('about.feature
ex-grow"><strong>UDL Goal:</strong> Providing options for action and expression. Frequent formative assessments help track progress and adjust instruction.</p>                         <div className="flex items
slate-800 flex items-center gap-2"><CheckSquare size={18} className="text-teal-600"/> {t('alignment.assessment')}</h3>                                       <span className={`text-[10px] uppercase font-bold px-
              <span className={`text-[10px] uppercase font-bold px-2 py-1 rounded ${report.analysis.assessmentAlignment.status === 'Aligned' ? 'bg-green-100 text-green-700' : 'bg-orange-100 text-orange-700'}`}>
0' : 'bg-orange-100 text-orange-700'}`}>                                           {report.analysis.assessmentAlignment.status}                                       </span>                                   </
                        <p className="italic text-slate-700 pl-6 leading-relaxed">"{report.analysis.assessmentAlignment.evidence}"</p>                                       </div>                               
                                     <p className="text-slate-600 leading-relaxed">{report.analysis.assessmentAlignment.notes}</p>                                       </div>                                   
emerald-600 hover:to-teal-600 transition-all">                                             📊 Submit Assessment                                         </button>                                     )}           
oundsBadges={wordSoundsBadges}               gameCompletions={gameCompletions}               fluencyAssessments={fluencyAssessments}               labelChallengeResults={labelChallengeResults}               wor
ndsBadges}               gameCompletions={gameCompletions}               fluencyAssessments={fluencyAssessments}               labelChallengeResults={labelChallengeResults}               wordSoundsScore={wordSo
parkles, X, addToast, angleChallenge, angleFeedback, angleValue, areaModelDims, areaModelHighlight, assessmentBlocks, base10Challenge, base10Feedback, base10Value, cubeAnswer, cubeBuilderChallenge, cubeBuilderF
alue, setAreaAnswer, setAreaChallenge, setAreaFeedback, setAreaModelDims, setAreaModelHighlight, setAssessmentBlocks, setBase10Challenge, setBase10Feedback, setBase10Value, setCubeAnswer, setCubeBuilderChalleng
nswer, setNlChallenge, nlChallenge, setNlFeedback, setNumberLineMarkers, setNumberLineRange, setShowAssessmentBuilder, setShowStemLab, setStemLabCreateMode, setStemLabTab, setStemLabTool, setToolSnapshots, show
Builder, setShowStemLab, setStemLabCreateMode, setStemLabTab, setStemLabTool, setToolSnapshots, showAssessmentBuilder, showStemLab, startMathFluencyProbe, stemLabCreateMode, stemLabTab, stemLabTool, submitExplo