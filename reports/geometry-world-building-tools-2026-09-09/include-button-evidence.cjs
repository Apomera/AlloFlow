const fs=require('node:fs'),path=require('node:path');const file=path.join(__dirname,'finalize-building-tools.cjs');let text=fs.readFileSync(file,'utf8');const from="'import-presentation-tests.json'])";if(!text.includes(from))throw Error('Missing evidence list');text=text.replace(from,"'import-presentation-tests.json','recovery-button-ui-tests.json'])");const fd=fs.openSync(file,'r+');fs.writeFileSync(fd,text);fs.ftruncateSync(fd,Buffer.byteLength(text));fs.closeSync(fd);
