/**
 * AlloFlow Confirm Dialog Module
 *
 * Auto-generated by _build_view_confirm_dialog_module.js — DO NOT EDIT MANUALLY.
 * Source: view_confirm_dialog_source.jsx
 */
(function() {
  'use strict';
  if (window.AlloModules && window.AlloModules.ConfirmDialog) {
    console.log('[CDN] ConfirmDialog already loaded, skipping');
    return;
  }
  var React = window.React;
  if (!React) { console.error('[ConfirmDialog] React not found on window'); return; }

function ConfirmDialog({ confirmDialog, setConfirmDialog, t }) {
  const AlertCircle = window.AlertCircle || (() => null);
  const AlertTriangle = window.AlertTriangle || AlertCircle;
  const Info = window.Info || AlertCircle;
  const tone = confirmDialog.tone || "warning";
  const iconMap = { danger: AlertTriangle, warning: AlertCircle, info: Info };
  const colorMap = {
    danger: { iconBg: "bg-red-100", iconText: "text-red-600", btn: "bg-red-600 hover:bg-red-700 shadow-red-500/25" },
    warning: { iconBg: "bg-amber-100", iconText: "text-amber-600", btn: "bg-red-600 hover:bg-red-700 shadow-red-500/25" },
    info: { iconBg: "bg-indigo-100", iconText: "text-indigo-600", btn: "bg-indigo-600 hover:bg-indigo-700 shadow-indigo-500/25" }
  };
  const Icon = iconMap[tone] || AlertCircle;
  const palette = colorMap[tone] || colorMap.warning;
  const handleCancel = React.useCallback(() => {
    if (typeof confirmDialog.onCancel === "function") {
      try {
        confirmDialog.onCancel();
      } catch (_) {
      }
    }
    setConfirmDialog(null);
  }, [confirmDialog, setConfirmDialog]);
  const handleConfirm = React.useCallback(() => {
    try {
      confirmDialog.onConfirm();
    } catch (_) {
    }
    setConfirmDialog(null);
  }, [confirmDialog, setConfirmDialog]);
  const dialogRef = React.useRef(null);
  const cancelBtnRef = React.useRef(null);
  React.useEffect(() => {
    const dialog = dialogRef.current;
    if (!dialog) return void 0;
    const previousFocus = document.activeElement;
    const trapStack = window.__alloFocusTrapStack || (window.__alloFocusTrapStack = []);
    const trap = { root: dialog };
    trapStack.push(trap);
    (cancelBtnRef.current || dialog).focus();
    const getFocusable = () => Array.from(dialog.querySelectorAll('button:not([disabled]), [href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])'));
    const onKeyDown = (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        handleCancel();
        return;
      }
      if (event.key !== "Tab") return;
      const focusable = getFocusable();
      if (!focusable.length) {
        event.preventDefault();
        dialog.focus();
        return;
      }
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    };
    dialog.addEventListener("keydown", onKeyDown);
    return () => {
      dialog.removeEventListener("keydown", onKeyDown);
      const trapIndex = trapStack.indexOf(trap);
      if (trapIndex !== -1) trapStack.splice(trapIndex, 1);
      if (previousFocus && typeof previousFocus.focus === "function") previousFocus.focus();
    };
  }, [handleCancel]);
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      role: "presentation",
      className: "fixed inset-0 z-[9999] bg-black/60 backdrop-blur-sm flex items-center justify-center p-4 animate-in fade-in duration-200 motion-reduce:animate-none",
      onClick: (e) => {
        if (e.target === e.currentTarget) handleCancel();
      }
    },
    /* @__PURE__ */ React.createElement(
      "div",
      {
        ref: dialogRef,
        tabIndex: -1,
        role: "alertdialog",
        "aria-modal": "true",
        "aria-labelledby": "alloflow-confirm-title",
        "aria-describedby": confirmDialog.detail ? "alloflow-confirm-message alloflow-confirm-detail" : "alloflow-confirm-message",
        className: "bg-white rounded-2xl shadow-2xl max-w-md w-full max-h-[calc(100vh-2rem)] overflow-y-auto p-6 animate-in zoom-in-95 duration-200 motion-reduce:animate-none border-2 border-slate-200"
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3 mb-4" }, /* @__PURE__ */ React.createElement("div", { className: `w-10 h-10 rounded-full ${palette.iconBg} flex items-center justify-center shrink-0` }, /* @__PURE__ */ React.createElement(Icon, { size: 20, className: palette.iconText, "aria-hidden": "true" })), /* @__PURE__ */ React.createElement("h2", { id: "alloflow-confirm-title", className: "text-lg font-bold text-slate-800" }, confirmDialog.title || t("common.confirm") || "Confirm")),
      /* @__PURE__ */ React.createElement("p", { id: "alloflow-confirm-message", className: "text-sm text-slate-700 leading-relaxed mb-2 whitespace-pre-wrap" }, confirmDialog.message),
      confirmDialog.detail ? /* @__PURE__ */ React.createElement("p", { id: "alloflow-confirm-detail", className: "text-xs text-slate-600 leading-relaxed mb-6 whitespace-pre-wrap" }, confirmDialog.detail) : /* @__PURE__ */ React.createElement("div", { className: "mb-6" }),
      /* @__PURE__ */ React.createElement("div", { className: "flex flex-col sm:flex-row gap-3 justify-end" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          ref: cancelBtnRef,
          type: "button",
          onClick: handleCancel,
          className: "px-5 py-2.5 bg-slate-100 hover:bg-slate-200 text-slate-700 font-semibold rounded-xl transition-colors focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-slate-600"
        },
        confirmDialog.cancelText || t("common.cancel") || "Cancel"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: handleConfirm,
          className: `px-5 py-2.5 ${palette.btn} text-white font-semibold rounded-xl transition-colors shadow-lg focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-offset-2 focus-visible:ring-indigo-700`
        },
        confirmDialog.confirmText || t("common.confirm_action") || "Confirm"
      ))
    )
  );
}

  window.AlloModules = window.AlloModules || {};
  window.AlloModules.ConfirmDialog = { ConfirmDialog: ConfirmDialog };
  console.log('[CDN] ConfirmDialog loaded');
})();
