import {test,expect} from '@playwright/test';
import {GlHarness} from './helpers/stem_gl_harness';
const harness=new GlHarness({toolFile:'stem_lab/stem_tool_ecosystem.js',toolId:'ecosystem',width:1100,height:900,appStyles:true});
test.beforeAll(async()=>harness.start());test.afterAll(async()=>harness.stop());test.afterEach(async({page})=>harness.destroy(page));
async function setup(page:any){
  await page.setViewportSize({width:1140,height:1050});await harness.mount(page,{ecosystem:{tab:'foodweb',tutorialDismissed:true}},undefined,{expectCanvas:false});
  await page.evaluate(()=>{document.body.className='theme-default';document.getElementById('wrap')!.style.cssText='width:100%;height:auto;display:block;padding:16px;background:white';});
  await page.evaluate(()=>{const w=window as any,p=w.THREE.WebGLRenderer.prototype,render=p.render;w.__invalidVisibility=[];p.render=function(scene:any,camera:any){const invalid:any[]=[];scene.traverse((o:any)=>{if(typeof o.visible!=='boolean')invalid.push(o.type);});w.__invalidVisibility=invalid;return render.call(this,scene,camera);};});
  await page.getByRole('button',{name:'Insect food shortage',exact:true}).click();await page.getByRole('button',{name:'Run food-web comparison',exact:true}).click();
  const meadow=page.locator('[data-efw-meadow]'),canvas=meadow.locator('canvas'),timeline=meadow.getByLabel('Meadow timeline',{exact:true});await meadow.getByRole('button',{name:'Inspect selected group',exact:true}).click();return {meadow,canvas,timeline,tracking:meadow.getByLabel('Animal camera tracking',{exact:true})};
}
test('steady camera reveals turns and leaps, rewinds deterministically and retains heading mode',async({page})=>{
  const {meadow,canvas,timeline,tracking}=await setup(page);await meadow.getByRole('button',{name:/Red foxes\s/}).click();await timeline.fill('0');
  const data=await page.evaluate(()=>{const w=window as any,a=w.StemLab.ecosystemFoodWeb,c=w.__toolData.ecosystem.foodWeb.run.config,f=a.behaviorTimeline(c,a.compare(c).experiment);let leap:any=null;for(let step=0;step<f.length&&!leap;step++)for(let index=0;index<16;index++)if(f[step].foxes[index].active&&f[step].foxes[index].altitude>0.30){leap={step,index,rise:f[step].foxes[index].altitude-f[0].foxes[index].altitude};break;}return {turn:f.findIndex((x:any)=>Math.abs(x.foxes[0].yaw-f[0].foxes[0].yaw)>0.8),leap,run:JSON.stringify(w.__toolData.ecosystem.foodWeb.run)};});
  expect(data.turn).toBeGreaterThan(0);expect(data.leap).toBeTruthy();await expect(tracking).toHaveValue('steady');const bearing=await canvas.getAttribute('data-camera-bearing');await timeline.fill(String(data.turn));await expect(canvas).toHaveAttribute('data-camera-bearing',bearing!);const target=await canvas.getAttribute('data-camera-target');
  await tracking.selectOption('heading');await expect(canvas).toHaveAttribute('data-camera-tracking','heading');await expect(canvas).not.toHaveAttribute('data-camera-bearing',bearing!);await expect(timeline).toHaveValue(String(data.turn));await tracking.selectOption('steady');await expect(canvas).toHaveAttribute('data-camera-bearing',bearing!);
  await meadow.getByRole('button',{name:'Rotate left',exact:true}).click();expect(Number(await canvas.getAttribute('data-camera-bearing'))).toBeCloseTo(Number(bearing)-Math.PI/6,7);await meadow.getByRole('button',{name:'Rotate right',exact:true}).click();
  await timeline.fill('0');await timeline.fill(String(data.turn));await expect(canvas).toHaveAttribute('data-camera-target',target!);
  await timeline.fill(String(data.leap.step));await meadow.getByLabel('Representative animal',{exact:true}).selectOption(String(data.leap.index));await meadow.getByRole('button',{name:'Isolate specimen',exact:true}).click();await expect(canvas).toHaveAttribute('data-isolated-specimen','true');
  const steadyTarget=JSON.parse((await canvas.getAttribute('data-camera-target'))!);await tracking.selectOption('heading');const headingTarget=JSON.parse((await canvas.getAttribute('data-camera-target'))!);expect(headingTarget[1]-steadyTarget[1]).toBeCloseTo(data.leap.rise,7);await tracking.selectOption('steady');
  await expect(canvas).toHaveAttribute('data-isolated-specimen','true');await expect(canvas).toHaveAttribute('data-visible-glyphs-foxes','1');await expect(canvas).toHaveAttribute('data-visible-glyphs-rabbits','0');expect(await page.evaluate(()=>(window as any).__invalidVisibility)).toEqual([]);
  await meadow.locator('.efw-meadow-stage').screenshot({path:'reports/ecosystem-steady-camera/fox-leap.jpg',type:'jpeg',quality:90});
  await page.setViewportSize({width:390,height:844});await meadow.locator('.efw-meadow-stage').screenshot({path:'reports/ecosystem-steady-camera/mobile-leap.jpg',type:'jpeg',quality:90});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);
  await page.emulateMedia({reducedMotion:'reduce'});await expect(canvas).toHaveAttribute('data-behavior','Starting pose');const frozen=await canvas.getAttribute('data-camera-target');await timeline.fill('0');await expect(canvas).toHaveAttribute('data-camera-target',frozen!);await expect(canvas).toHaveAttribute('data-behavior','Starting pose');
  expect(await page.evaluate(()=>JSON.stringify((window as any).__toolData.ecosystem.foodWeb.run))).toBe(data.run);await tracking.selectOption('heading');await meadow.getByRole('button',{name:'Reset camera',exact:true}).click();await meadow.getByRole('button',{name:'Inspect selected group',exact:true}).click();await expect(tracking).toHaveValue('steady');expect(await page.evaluate(()=>(window as any).__events.errors)).toEqual([]);
});
test('interaction preference holds its bearing when a cue disappears and restores the chosen tracking mode',async({page})=>{
  const {meadow,canvas,timeline,tracking}=await setup(page);await timeline.fill('0');await tracking.selectOption('heading');const reason=meadow.locator('[data-efw-action-reason]');await reason.locator('summary').click();const interaction=reason.getByRole('button',{name:'Show behavior interaction',exact:true});await interaction.click();await expect(tracking).toBeDisabled();await expect(canvas).toHaveAttribute('data-camera-tracking','steady');const bearing=await canvas.getAttribute('data-camera-bearing');
  const noCue=await page.evaluate(()=>{const w=window as any,a=w.StemLab.ecosystemFoodWeb,c=w.__toolData.ecosystem.foodWeb.run.config;return a.behaviorTimeline(c,a.compare(c).experiment).findIndex((f:any)=>f.bluetits[0].active&&!a.interaction(f,'bluetits',0));});expect(noCue).toBeGreaterThan(0);await timeline.fill(String(noCue));await expect(canvas).toHaveAttribute('data-interaction-view','false');await expect(canvas).toHaveAttribute('data-camera-bearing',bearing!);await interaction.click();await expect(tracking).toBeEnabled();await expect(tracking).toHaveValue('heading');await expect(canvas).toHaveAttribute('data-camera-tracking','heading');expect(await page.evaluate(()=>(window as any).__events.errors)).toEqual([]);
});
