const fs=require('node:fs');const file='stem_lab/stem_tool_anatomy.js';let s=fs.readFileSync(file,'utf8');const a="key: option.id, type: 'button', disabled: chosen !== null, 'aria-pressed': wasChosen,";if(s.split(a).length!==2)throw Error('Tour option target');s=s.replace(a,"key: option.id, type: 'button', 'data-anatomy-tour-option':option.id, disabled: chosen !== null, 'aria-pressed': wasChosen,");fs.writeFileSync(file,s);fs.copyFileSync(file,'desktop/web-app/public/'+file);
