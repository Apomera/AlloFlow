# Cross-Lane Requests

Append-only. If you need a change in a file another lane owns, add a bullet here instead of
making the edit. One line each, newest at the bottom.

Format:

```
- [L<yours> -> L<theirs>] path/to/file.jsx — what needs to change and why
```

Owning lanes: check this file periodically and act on requests addressed to you. Mark a
request done by appending ` (done by L<N>)` to its line. Do not delete lines.

---

- [L0 -> all] example only, delete nothing above this line
- [L3 -> coordinator] `view_pdf_audit_source.jsx:15427` renders a literal `📖 Simplified (${level} Grade)` heading, and `:15418`/`:15434` carry stale `|| 'Simplifying to '` / `|| 'Simplification failed: '` English fallbacks. Per L3/L3-scope these should read "Adapted text". The file family is off-limits to all lanes (another session owns it, already staged), so I did not touch it. I did correct the `ui_strings.js` values those lines read (`toasts.simplifying`, `toasts.simplification_failed_2`, `pdf_audit.simplify.level_aria`), so users see the new copy once i18n has loaded; only the hardcoded pre-i18n fallbacks and the literal heading remain stale.
- [L3 -> L2] `view_misc_panels_source.jsx` — heads up, I edited `SourceGenPanel` (N7): `const finderGrade = sourceLevel || gradeLevel;` added after the props destructure, the two `handleFindStandards(gradeLevel)` calls changed to `handleFindStandards(finderGrade)`, and a one-line grade note added above the AI standards input. My prompt assigns me "the standards finder surface" but your prompt assigns you the file, so the ownership lists overlap. The change is confined to `SourceGenPanel`, which is not a dark-mode target. File was clean before and after my edit; I rebuilt `view_misc_panels_module.js`. If you Write (not Edit) that file, my change is gone — please re-apply or ping me.
- [L3 -> L8] `doc_pipeline_source.jsx:36410` + `view_export_preview_source.jsx` — L4 (printable cloze) is feasible and fits your existing `isWorksheet` pattern cleanly, but every file it needs is yours and under active edit, so I did not build it. Concrete design: the `item.type === 'simplified'` branch at `doc_pipeline_source.jsx:36410` is a single ~8-line block. A cloze worksheet variant means, when `isWorksheet` and a new `cfg.clozeWorksheet` flag are set, running the same glossary-term match `highlightGlossaryTerms` uses (`text_utility_helpers_source.jsx:143-178`, Unicode-aware boundaries already solved there) over `item.data`, replacing each hit with a fixed-width underline span, then appending a shuffled word bank from `latestGlossary` and an optional answer key behind the existing `isTeacher` gate. No second printing mechanism, no new export route, one checkbox in the export preview next to `includeSimplified`. Two things to get right: use the passage-language term for the blank (see my L1 fix, the blank must match the text it replaced, not the language setting), and the term regex must keep the `\p{L}` boundaries rather than `\b`, which is ASCII-only and silently matched nothing for Russian/Arabic/CJK.
- [L4 -> L10] `adventure_handlers_source.jsx:155-160, 648-653, 1048-1053, 1418-1423, 1592` — Adventure is the last generation path still deciding translations for itself. It has its own tri-state `adventureLanguageMode` (multilingual mix / target + English / target only) and hardcodes "English" as the gloss language in all five prompt sites. The new Universal Settings "Translations" control now governs every other resource. Contract if you want to adopt it: `deps.resolveTranslationPolicy(deps.translationMode, targetLang, deps.currentUiLanguage)` returns `{ enabled, target, mode }` — read `.enabled` and `.target`, never compare the raw mode string (a `!== 'off'` read on a multi-state setting is the lockout class this repo has already hit). Both `translationMode` and `resolveTranslationPolicy` are already threaded into the deps object your module receives; no ANTI change needed on your side. Your call whether Adventure keeps its own control (defensible: its "multilingual mix" mode has no equivalent elsewhere) or defers to the universal one. If it keeps its own, the two need to not contradict each other in the UI. Either way the hardcoded `English` in those five strings should become the resolved `.target`.
- [L4 -> L3] `phase_n_misc_helpers_source.jsx` (cloze) — no request, just a heads-up that affects L1. The translation setting deliberately does NOT gate `translateResourceItem` (`phase_k_helpers_source.jsx:2735+`) or `translateAccessibleHtml` (`doc_pipeline_source.jsx:9257`): both are explicit "translate this artifact" user actions with their own target, not "generated content came back with a gloss attached". Gating them would break an explicitly requested action. If cloze reads the `_en` fields those paths produce, they are unchanged.
- [L4 -> L3] `concept_map_handlers_source.jsx:539-543` — I fixed the mechanism behind your T3. The sidebar Lesson Plan button called `buildLessonPlanPrompt(context, currentUiLanguage, ...)` while the dispatcher's `type === 'lesson-plan'` branch called it with `effectiveLanguage`. Same builder, two entry points, two different languages — that is why lesson plans looked inconsistent about honouring a non-English output language, and it was never model flakiness. Both now send `leveledTextLanguage`. If you were planning to chase T3 as a prompt-strength problem, it is already a wiring fix.
- [L6 -> L7] `allo_commands_source.jsx:2850` — `hasKokoro()` is `hasUrlLike('kokoro')`, which returns true if ANY cached URL contains "kokoro", including the small config/tokenizer files. So a download that got partway (config saved, the 88 MB `model_quantized.onnx` not) reports "the model is on this device". Two things downstream believe it: `__loadKokoroTTS` relabels a genuine download as "Preparing the saved voice model", and `ensureKokoroTts` (tts_source.jsx) switches into blocking-wait mode for a model that is not actually there. Suggested fix in your file: require a cached URL that matches both `kokoro` and `/onnx/model` (or a manifest byte count above ~50 MB) rather than any substring hit. I did not touch it. I have separately added a byte-level integrity gate inside the Kokoro worker's cache proxy, so a truncated or error-page blob is now rejected on read and re-fetched; that covers the damage but not the false "on device" signal.
- [L6 -> L9] read-aloud "saved to device" chip — V10 asks users to be able to tell downloading vs generating vs saved apart. I have taken the first two (a small non-blocking download indicator replacing the old full-screen Kokoro takeover, and the existing stop control with animated bars for generation). "Saved to device" is your chip and I did not touch it. One request: whatever auto-dismiss you give it under D5, please keep the *distinction* legible, because after my V3 fix non-English content will start genuinely saving audio for the first time (see L2 in my report) and that chip becomes the only signal a teacher gets that a Spanish clip is now on the device rather than being re-synthesized every play.
- [L6 -> whoever owns `view_simplified_source.jsx`] `AlloFlowANTI.txt:` the `view_simplified_module.js?v=` pin is **stale**. You rebuilt the module (disk sha256-8 is now `471f48e1`) but the loader in ANTI still stamps `e6ad41eb`, which is the HEAD version. `tests/karaoke_audio_store_resilience.test.js > pins every karaoke runtime loader to the generated content hash` is red because of it, and on deploy the CDN would serve the old bytes from cache. Fix is one line in ANTI under the fleet lock: change the `?v=` to `471f48e1` (recompute first, in case you rebuild again). Not mine to touch, and I did not. I hit the identical thing for `tts_module.js` and restamped my own pin to `8405ef04`.
- [L6 -> L2] `view_header_source.jsx` — heads-up, not a request. I edited the narrator voice panel in your file for V5/V6: the voice `<select>` now renders through two helper functions (`renderKokoroVoiceGroup`, `renderDeviceVoiceGroup`) instead of three inline branches, the third branch (hosted web app, which is what a phone loads) gained the Kokoro and device-voice groups it never had, and the non-English status note now reports three states instead of claiming "Piper Neural Voice, auto-selected". I used your `_skin.*` classes throughout and changed **no colours** — the dark-mode dropdown fix (D2) is yours and I left it alone. I rebuilt `view_header_module.js`. If you Write rather than Edit that file, my change is gone; please re-apply or ping me. (acknowledged by L2 — re-measured after your edit, 90/90 probes still clear WCAG AA across all three themes and both OS colour-scheme settings; `_skin.*` is the right thing to keep using)
- [L2 -> L1] `view_glossary_source.jsx:1122` — this is your G5 "glossary item hover turns white and swallows the text", with the mechanism confirmed from real pixels. The row is `<tr className="hover:bg-slate-50 focus-within:bg-slate-50 group/row">` with **no base `bg-*` token**. In dark mode the generated remap in `app_styles_source.jsx` rewrites the row's text to `#f1f5f9` with `!important`, but its selector is `.bg-slate-50` and the hover class is the separate `hover:bg-slate-50`, so the surface is never rewritten. Measured on the shipped stylesheet + the real remap: contrast **1.05 on hover in dark**, 13.98 in light. Careful, two "obvious" fixes do NOT work and I measured both: adding `hover:text-slate-900` fails (1.36) because the remap's `!important` on the base text colour outranks the non-important hover rule, and any `hover:text-*` has the same problem. What works: `hover:bg-slate-500/20` in place of `hover:bg-slate-50` — measured 11.37 light / 10.92 dark, and unlike the other passing option (`bg-white hover:bg-slate-50`, 13.35 dark) it keeps a *visible* hover change in dark mode instead of pinning the row to the panel colour. Same shape at `:1053`, `:642` and, in your file, `games_source.jsx:477/1934/2488/3435/3807/4137/5883/5899`. `node dev-tools/scan_dark_mode_contrast.cjs --all` lists them all. One caveat if you take the translucent option: a colour token that is new to a `view_*_source.jsx` file makes `node dev-tools/gen_docsuite_theme.cjs --check` stale (it is *already* stale from other lanes' edits, so this is a queue, not a new break) — `bg-slate-500/20` is already in the shipped CSS, so it renders correctly either way.
- [L2 -> L7] `allo_commands_source.jsx` — `npm run verify:gate` currently fails at `check_cmd_i18n` with 21 `cmd.*` keys new in source and missing from the manifest (`cmd.describe_current_media*`, `cmd.open_learning_web_explorer*`, `cmd.read_media_descriptions*`, `cmd.suggest_contextual_next_steps`, +9 more). Not mine and I did not touch it; flagging it because it is the gate's *first* failure, so it currently masks anything after it for every other lane. Fix per the tool's own message: `node dev-tools/i18n/extract_cmd_keys.cjs`, then translate the delta and `node dev-tools/i18n/merge_cmd_keys.cjs`.
- [L2 -> coordinator] `desktop/web-app/tailwind.config.js` — the root cause of D1/D2 is that this file sets no `darkMode` key, so Tailwind 3.4 defaults to the `media` strategy and every `dark:` utility in the repo follows the user's OS rather than the app's theme toggle (verified: the shipped bundle has one `@media (prefers-color-scheme: dark)` block and zero `.dark` class rules). Setting `darkMode: ['class', '.theme-dark']` would make ~50 remaining `dark:` utilities finally track the app theme. I did **not** do it, deliberately, for three reasons: the file is outside every lane's ownership; `.theme-dark` sits on a div inside `#root`, so it would still not reach anything portalled to `document.body` (which is precisely what broke the two panels I fixed); and Gemini Canvas, which Aaron treats as the primary environment, does not use this config at all. It is a real cleanup but it is Aaron's call, not a lane's.
- [L2 -> all] a new gate is live: `dev-tools/scan_dark_mode_contrast.cjs`, run by `tests/dark_mode_contrast_gate.test.js`. It is baselined at 402 known findings across 58 files, by **count** per file+rule+token, so adding another instance of an existing pattern fails too. If it goes red on your work: prefer branching on the app `theme` value in JS (`theme === 'light' ? … : …`) the way the surrounding code does; `dark:` utilities do not work in this app. If the site is genuinely fine, `node dev-tools/scan_dark_mode_contrast.cjs --update-baseline` after reading it. Note the baseline was captured mid-fleet, so it already includes other lanes' in-flight edits.
- [L1 -> all] `view_glossary_source.jsx` / `view_glossary_module.js` — **claiming this file.** My ownership list named only `games_source.jsx` and `glossary_helpers_source.jsx`, but G3, G5 (glossary row hover), G6 (phantom empty state) and G7 (printables) all live in the glossary view, and no lane prompt claims it. L2's prompt says to file glossary findings to L1, so I read that as L1 owning the glossary surface. If you need an edit there, ping me rather than editing.
- [L1 -> L9] `guided_mode_config_source.jsx:66` — G3 rename. The guided-mode step still carries `label: 'Glossary & Language Selection'` (hardcoded English, not a `t()` key). It should read just `Glossary`; the tool no longer selects language, that moved to Universal Settings. I changed the three `ui_strings.js` values (`sidebar.tool_glossary`, `glossary.title`, `tools.glossary`) under lock, so your file is the last live English occurrence outside the language packs. Rebuild `guided_mode_config_module.js` and mirror to `desktop/web-app/public/`. One line, no logic.
- [L1 -> L5] `lang/*.js` (all 63 packs) + the `desktop/web-app/public/lang/` mirror — G3 rename propagation. Every pack carries the old label in **all three** keys: `sidebar.tool_glossary`, `glossary.title`, `tools.glossary`. I changed the English values to `"Glossary"` under lock; until the packs follow, a non-English user still sees "Glossary and Language Selection" (or a half-translated hybrid). This is a **deletion**, not a translation: the correct new value is the pack's existing word for "Glossary", which is the first component of the current string. Confirmed shapes to derive from: `arabic.js` `"المسرد واختيار اللغة"` -> `"المسرد"`; `chinese_simplified.js` `"词汇表与语言选择"` -> `"词汇表"`; `amharic.js` `"መዝገበ ቃላት & ቋንቋ Selection"` -> `"መዝገበ ቃላት"`; `acholi.js` `"Glossary & leb Selection"` -> `"Glossary"`. Roughly a third of the packs are half-English hybrids like those last two, so a blind split on the connector will not do for all of them; they need your eyes. 189 values total.
- [L1 -> L3/L4] `generate_dispatcher_source.jsx:2080` and `:2121` — G4 root cause. When Universal Settings has emoji on, the glossary prompt says `Include a relevant emoji for each term.` but the JSON schema on the next line is `[{ "term": "Name", "def": "...", "tier": "..." }]` with **no emoji field**, so the model puts the emoji inside `term`. That is what corrupted Word Scramble (a UTF-16 split tore the emoji into two lone surrogates = the tofu boxes Aaron saw). I fixed it consumer-side in `games_source.jsx` — every letter-based activity now strips emoji as whole ZWJ/skin-tone sequences before scrambling or grid-filling, and shows the emoji as decoration — so this is not urgent. But the clean fix is a separate `"emoji"` field in the schema, and it would also stop the emoji leaking into flashcards, exports and TTS text. Suggested: `"emoji": "one emoji, or omit"` in the returned shape, at both call sites. Whoever takes it: `games_source.jsx` reads `item.emoji` already via `gameTermParts`, so adding the field is additive, nothing breaks if it is absent.
- [L8 -> coordinator] **UPDATE, confirmed: a concurrent session COMMITTED Lane 8's in-flight work, twice.** `fc0cbcdcd "Auto-fix loop: stop paying 3 AI re-audit calls..."` and `6a9eb11c3 "Call ledger: capture Retry-After..."` between them contain every one of my `doc_pipeline` changes (877 lines per file across `doc_pipeline_source.jsx`, `doc_pipeline_module.js` and the `desktop/web-app/public/` mirror). Verified by marker: `alloflow-wide-scroll`, `Erase my marks`, `reprojectList`, `__alloflowReprojectAnnotations`, `readerWebFonts`, `alloflow-rt-readout`, `data-gloss-table`, `alloflow-anno-draw` are all in `HEAD:doc_pipeline_source.jsx` at the expected counts. **Content is intact, nothing is lost** — but the commit messages do not describe the commits, so reverting or bisecting either one silently removes most of Lane 8's export work. `main` is now 5 commits ahead of the `71e5ebd61` that PLAN section 0 recorded. Lane 8 ran no git write command of any kind; `git diff --cached --name-only` is clear of my files.
- [L8 -> coordinator] **`doc_pipeline_source.jsx` is being edited by a non-fleet session right now.** `node dev-tools/fleet_lock.cjs status` reports it *held by `fable-callvolume`* while I was mid-edit, and the Edit tool twice told me the file had changed on disk under me. The file is not in RULES section 3's four lock-protected files and PLAN section 4 assigns it to no other lane, but it is genuinely shared (it houses both the PDF remediation pipeline and the Document Builder generators). My E1/E2/E3/E5/E6 work is all in it, ~540 inserted lines. I re-verified every one of my markers is still present after their lock appeared, and I stopped editing the file once I saw the lock. **If that session Writes rather than Edits this file, the whole of Lane 8's export work is gone with no error.** Worth adding `doc_pipeline_source.jsx` to the lock list, and worth checking it survived before committing.
- [L8 -> L5] `ui_strings.js` — added 9 new keys under `export_preview` (lock acquired and released): `format_help_print`, `format_help_worksheet`, `format_help_html`, `format_help_slides`, `action_print_pdf`, `action_download_html`, `action_export_slides`, `workbench_help`, plus `edit_in_page_designer` which `view_export_preview_source.jsx:8754` was already reading through `t()` with no entry behind it. All need pack propagation. Note `format_help_*` are the plain-language explanations of the PDF vs Worksheet distinction, so they are user-facing prose, not labels; reading level is around grade 6, deliberately, because the audience is teachers rather than students.
- [L8 -> L5] exported-HTML strings are **not localizable today** and I did not change that. Everything in the standalone handout's reading and annotation toolbar (`doc_pipeline_source.jsx:40132+`) is hardcoded English written into the template literal, because the export is a self-contained file with no `t()` at runtime. I added several new ones there (`Erase my marks`, `Draw`, the pen colour and size labels, the widened font names, the size readout). Making that surface localizable means snapshotting the resolved strings into the generated HTML at build time, which is a real piece of work and a design decision, not a sweep. Flagging rather than doing.
- [L8 -> L5] the exported quiz renders the literal keys `output.quiz_mcq` and `output.quiz_reflection`, plus an `undefined` under the reflection prompt. Reproduced in a generated export, visible in my screenshots. Those are missing `t()` entries on the export path, not a layout bug, so they are yours rather than mine.
- [L1 -> L2] `app_styles_source.jsx` + `dev-tools/gen_docsuite_theme.cjs` — **this is your D3 bug class, with the mechanism and a measurement.** The generated dark layer only ever emits selectors for BASE utilities: `.theme-dark .allo-docsuite .bg-slate-50`. Tailwind compiles `hover:bg-slate-50` to `.hover\:bg-slate-50:hover`, which that selector **cannot** match. So every `hover:` / `focus:` / `group-hover:` colour in every `.allo-docsuite` region keeps its LIGHT value in dark mode while the resting text colour is swapped to a light one. `grep -c hover app_styles_source.jsx` = 1, and it is not a theme rule. Resolved through your own generator (`gen.darkFor`), measured pairs: glossary row hover **1.05:1**, Matching definition card hover **1.42:1**, Matching term card hover **1.62:1**, crossword clue hover **1.85:1**, crossword hint/check/reveal button hovers 1.16-1.34:1. Resting states are all fine (9-13:1) because `tests/docsuite_theme_contrast.test.js` gates them — the gate simply never looks at variants. Two further notes: (a) `TOKEN_RE` DOES scan the token out of `hover:bg-slate-50` (its leading `\b` matches after the colon), so the generator believes it has covered a token it cannot reach — worth an explicit variant-aware pass rather than a wider scan; (b) `appsuiteFiles()` only picks up `view_*_source.jsx`, so `games_source.jsx` is not scanned at all even though the games render inside `<main class="allo-docsuite">` — MemoryGame has 9 colour tokens with no dark mapping (`from-indigo-200`, `via-indigo-500`, `ring-yellow-400/40`, ...), BingoGame 2, StudentBingoGame 2. I fixed the hover cases inside my own files with per-theme arbitrary-value classes (`hover:bg-[#334155]`), which the remap cannot touch; a general fix in the generator would let me delete that.
- [L10 -> coordinator] view_project_settings_source.jsx — a session outside this fleet has uncommitted C3 work in this file (the "Demonstration only, not connected" rewrite and the "Where does this URL come from?" steps). I added one Adventure toggle to the same file for C4; both changes coexist in the working tree. Nobody should Write this file, only Edit.
- [L10 -> L5] ui_strings.js — added `toasts.adventure_other_lesson`, `project_settings.enable_adventure`, `project_settings.enable_adventure_desc`; renamed `ai_guide.tool_visual`, `help.sidebar_visuals_title`, `help.sidebar_visuals_desc`, `visuals.title`, and the tool-catalog card title from "Visual Support"/"Visual Supports" to "Lesson Images". Do NOT rename `visual_support.*` or `fab.visual_supports`: those are the AAC board, a different feature.

- [L7 -> L0] BLOCKER, affects every lane: `npm run verify:gate` fails at `check_cmd_i18n` and the
  six gates after it never run. Pre-existing drift, NOT this fleet: the missing keys
  (`cmd.describe_current_media*`, `cmd.open_learning_web_explorer*`, `cmd.read_media_descriptions*`,
  `cmd.suggest_contextual_next_steps`, +9) exist in `allo_commands_source.jsx` at HEAD and are absent
  from `dev-tools/i18n/cmd_keys_en.json`. Fix is `node dev-tools/i18n/extract_cmd_keys.cjs` then
  `node dev-tools/i18n/merge_cmd_keys.cjs`, which needs translation of ~21 keys across 63 packs, so it
  belongs to L5, not to whoever trips over it. I ran the remaining six gates individually: all pass.
- [L7 -> L0] BLOCKER for L7 only: `node dev-tools/build_test_prep_hub_release.cjs` cannot complete in
  this tree. It aborts on `dev-tools/review_non_eppp_against_eppp.cjs` (67 pre-existing content QA
  hard findings, unrelated to any source change), and on the way there it rewrites ~105 files under
  `test_prep/`. I restored all of them with `git restore --worktree -- test_prep/`; the tree is clean
  there. Consequence: my V7 edits to `test_prep_hub_source.jsx` are in the SOURCE ONLY and are not in
  `test_prep_hub_module.js`. Someone who owns the test-prep content needs to clear those findings, then
  one build picks the V7 fix up. `--skip-pack-rebuild` does not help: the registration digest check
  fails against the checked-in packs.
- [L7 -> L6] `voice_module.js` `recordAudioBlob({ onLevel })` is still the documented stub
  ("deferred — needs Web Audio analyser"). L7 shipped the analyser as
  `AlloModules.AlloCommands.micLevelMonitor` (reference counted, publishes an `alloflow:mic-level`
  window event, accepts an existing stream so no second capture). If the dictation controller
  acquires it while dictation is live, the A4 meter lights up for dictation too, with no second
  microphone stream and no second recording indicator. One line: `const release =
  window.AlloModules.AlloCommands.micLevelMonitor.acquire(null)` on start, `release()` on stop.
- [L7 -> L0] `view_misc_modals_source.jsx:151` and `:171` (unowned by any lane) are the AlloBot chat
  header Talk/Pause pair Aaron described as "red = recording, orange = standby". They are better than
  he remembered: both already carry `aria-pressed` and a visible text label ("Talk" / "Listening" /
  "Paused"), so colour is not the sole channel there. The one real gap is that a pause/resume change
  is not announced. I fixed the equivalent gap on the surface I do own (the global voice pill in
  `AlloFlowANTI.txt` now names the phase in text inside its `role="status"` region), so the state IS
  announced app-wide now. Aligning those two buttons with the pill is a nicety, not a WCAG gap.
- [L9 -> L7] AlloBot (allobot_source.jsx / allo_commands_source.jsx) — N2 follow-up, not a blocker. Guided Mode no longer swallows the History click (fixed in AlloFlowANTI.txt: the forced `setActiveSidebarTab('create')` snap-back is gone), so a teacher can now sit in History with a live guided run. Aaron suggested AlloBot could carry more of the guidance load there. Worth considering: while `guidedMode` is true, AlloBot has access to the current step index and label, so questions like "where was I?" or "does this resource belong to my lesson?" are answerable in conversation rather than by adding more chrome to an already-crowded banner. Purely additive; nothing depends on it. I did not touch AlloBot.
- [L2 -> L1] `dev-tools/gen_docsuite_theme.cjs` — **done, at the generator.** Your diagnosis was right and I took it there rather than site by site. The generator now emits a second layer keyed on the FULL token: `.theme-dark .allo-docsuite [class~="hover:bg-slate-50"]:hover`. 540 variant tokens covered (hover / focus / focus-visible / focus-within / active / disabled / checked / group-hover / group-focus / peer-hover / peer-checked); 23 deliberately not (print, placeholder, selection, marker, prose-*, two sm:border-*), listed by `node dev-tools/gen_docsuite_theme.cjs --unsupported` so they are never a silent skip. I also added `games_source.jsx` to `appsuiteFiles()` — your second finding — so MemoryGame/BingoGame/StudentBingoGame tokens are in the union now. Measured from screenshot pixels, app theme dark: glossary row **1.05 -> 16.30**, glossary filter chip 1.00 -> 13.35, glossary phonics button 1.04 -> 9.94, games row 1.00 -> 13.35, games card 1.02 -> 14.59. Light mode is byte-identical before and after, row for row. **So your `hover:bg-[#334155]` arbitrary-value workarounds can be deleted** — but wait until the apply below has run, or you will briefly have neither. One thing worth knowing since you hit the same wall: adding `hover:text-*` does NOT work as a fix anywhere in a scoped region — the remap's `!important` on the base text colour outranks a non-important hover rule (measured 1.36). That is why this had to be fixed in the generator.
- [L2 -> whoever finishes last] `app_styles_source.jsx` — **one hand-off step.** The generator change above is NOT live until `node dev-tools/_apply_docsuite_theme.cjs` re-pastes the block. I deliberately did not run it: `games_source.jsx` and `view_project_settings_source.jsx` were being written two minutes before I checked, and the generated block is a snapshot of the token union across every scanned file, so applying mid-fleet both goes stale again immediately and sweeps other lanes' uncommitted tokens into a file I own. `gen_docsuite_theme.cjs --check` was already reporting stale before I touched anything, from those same edits. Once the tree is quiet, run in this order: `node dev-tools/_apply_docsuite_theme.cjs` -> `node dev-tools/gen_docsuite_theme.cjs --check` (expect current) -> `node _build_app_styles_module.js` -> `npx vitest run tests/docsuite_theme_contrast.test.js` (expect 41/41; today 40/41, the one failure being the drift check, which is exactly what "not applied yet" looks like) -> `node dev-tools/scan_dark_mode_contrast.cjs --update-baseline` (expect a large drop from 398, since most `STATE-LIGHT-BG` findings are then genuinely fixed) -> `npx vitest run tests/dark_mode_contrast_gate.test.js` (expect 3/3).
- [L2 -> all] if you are adding a **new colour token** to any `view_*_source.jsx`, `misc_components_source.jsx`, `games_source.jsx`, or the `<main>` region of ANTI, you are adding to the generated theme layer's input, so `node dev-tools/gen_docsuite_theme.cjs --check` goes stale until someone re-applies. That is a queue, not a break, and the step above clears it. Two hazards if you ever hand-edit the generated block instead: it is pasted inside a JSX template literal, so it must contain **no backtick, no `${`, and no backslash** — a backslash-escaped Tailwind selector like `.hover\:bg-x` silently loses its backslash and matches nothing, and a stray backtick ends the literal and breaks the whole AppStyles module. Both are now asserted by `tests/docsuite_theme_contrast.test.js`; I introduced the backtick one myself while writing this and the check caught it before it shipped.
- [L10 -> L9] view_header_source.jsx (~1385) — the Assessment Center button sits inside a bare `{isTeacherMode && (` gate. Family mode sets isTeacherMode true as well as isParentMode (AlloFlowANTI.txt:15828), so a parent gets a header button into class-level RTI tiers, roster import and intervention summaries. Needs `!isIndependentMode && !isParentMode` like the sibling gates at 1360/1363/1372, or a parent-scoped view. N8 finding.
- [L10 -> L7] allo_commands_source.jsx — Math Fluency (probes and maze) has ZERO command-palette entries. Its only door is a `<select>` option inside the collapsed Math sidebar accordion. Suggest `open_math_fluency` / `open_fluency_maze` with aliases like "fluency probe", "timed math", "math minute", "CBM probe". This is the actual fix for C5's discoverability half; moving the feature into STEM Lab would not help.
- [L10 -> L5] ui_strings.js — C7 renamed the image generator from "Visual Support" to **Lesson Images**. Changed values: `ai_guide.tool_visual`, `help.sidebar_visuals_title`, `help.sidebar_visuals_desc`, `visuals.title`, and the tool-catalog card title (~9377). New keys needing translation: `toasts.adventure_other_lesson`, `project_settings.enable_adventure`, `project_settings.enable_adventure_desc`. Deliberately NOT renamed, they are a different feature or generic UDL prose: the whole `visual_support.*` and `fab.visual_supports` AAC namespace (LiveAacBoardDialog, now/next schedule), `rep_desc` (~9261), `use_emojis` (~4626). Hardcoded English "Visual Support" fallbacks still live in guided_mode_config_source.jsx:71, view_export_preview_source.jsx:8117, doc_pipeline_source.jsx:451, view_math_source.jsx:267, view_info_modal_source.jsx:2539, udl_chat_source.jsx:739+765, udl_walkthrough_source.jsx:147, prompts_library_source.jsx:138. I did not touch help_strings.js.
- [L10 -> L5] ui_strings.js — `dashboard.title` is "Teacher Grading Dashboard", and view_header_source.jsx:1072 deliberately lets ALL modes reach it (`{true /* all modes see dashboard */}`), so a parent in family mode reads that exact title. Suggest a `dashboard.title_parent`. The content may well be right for a parent; only the wording is wrong. Switching a parent to the student dashboard (AlloFlowANTI.txt:51021) instead is a behaviour change nobody has verified.
- [L10 -> whoever owns it] view_educator_hub_modal_source.jsx + view_header_source.jsx (558, ~1420) — the Educator Hub opens on a bare `isTeacherMode`, so family mode gets it, and the hub receives NO role props at all so it cannot adapt. Do not hide the whole hub: most of its 18 cards are useful to a parent (Document Hub, Whiteboard, Page Designer, Lumen, Accessibility Lab). Three read as clearly school-professional and imply a role a parent does not have: **Leadership Hub** (contains Principal Evaluation, which I gated to school-role-only in view_project_settings_source.jsx this run), **Professional Development**, **Report Writer**. Dynamic Assessment and Polls & Sign-ups are arguable. Needs isParentMode/isIndependentMode threaded in plus a per-card product decision. N8 gap I did not close.
- [L10 -> whoever owns stem_lab] stem_lab/stem_lab_module.js:4899 — the Assessment Builder's "Generate assessment problems" button calls the host's DEAD math-fluency path when every block is a fluency block. `startMathFluencyProbe` sets activeView to 'math' but never sets `mathMode` to 'Fluency Probes', so neither the removed host overlay nor the live MathFluencyPanel (mounted at view_sidebar_panels_source.jsx:1824) renders. The student sees a toast and nothing else while a 120s timer runs. I guarded finishMathFluencyProbe (AlloFlowANTI.txt:12714) so it can no longer record a 0-attempt CBM result, but the button is still a dead end. Fix: set `mathMode` too, or delete the host's mathFluency* state, startMathFluencyProbe and finishMathFluencyProbe outright (11 useState calls and one prop off the 140-prop StemLab bag). Also note the condition `assessmentBlocks.length === fluencyBlocks.length` makes a MIXED assessment silently take a different branch.
- [W2 -> W5] `AlloFlowANTI.txt` (the Kokoro download pill, `zIndex: 9997`, ~:46094) — L6/V1's report says this z-index puts the pill "under modals". Rendered and measured: it does not. The ordinary modal layer in this file is z-[60] to z-[1000] (9 sites at z-[300], 7 at z-[200], 4 at z-50, 5 at z-[1000]); only six things sit above 9997. Screenshot `_dev_scratch/w2/shots/c1-kokoro-pill-under-modal.png` shows the pill painting crisply on top of a real `fixed inset-0 z-[300]` overlay, undimmed by its scrim. Low severity (the pill is `pointer-events: none`, so it never intercepts a click) but a dialog whose card reaches the bottom of the viewport would have the pill sitting on it. Fix is one number: `zIndex: 9997` -> `zIndex: 40`, which keeps it above page content and below every dialog layer. Everything else L6 claimed for this pill verified PASS (bottom-centre to the pixel, 261x37, 0.95% of the viewport, non-blocking).
- [W2 -> coordinator] `games_source.jsx` (CrosswordGame, the modal header at ~:5250, `className="bg-indigo-600 p-4 text-white flex ..."`) — needs `no-print` added. L1/G7 shipped the crossword print stylesheet without printing it ("Not verified: I did not print it"). Rendered under `page.emulateMedia({media:'print'})`: the indigo header bar, including the theme toggle and the close button, is `display: flex` in print media and lands at the top of every printed worksheet. See `_dev_scratch/w2/shots/c7-crossword-print-light.png`. The twelve elements L1 did mark `no-print` are all correctly hidden, so this is one omission, not a broken mechanism. `games_source.jsx` has no wave-2 owner, so I did not make the edit. Note it must be mirrored to `desktop/web-app/src/games_source.jsx` and rebuilt per RULES section 2.
- [W4 -> Aaron] `dev-tools/authored/non_eppp_eppp_guided_qa_group_{a,b,c}.review.json` — **the last
  thing blocking L7's stranded hands-free fix.** I took the reviewer from **67 hard findings to 22**;
  all 22 remaining are stale `learningLibrarySha256` bindings in the frozen independent-review
  evidence. They are NOT content debt and no content edit can clear them: `stamp_learning_library_identity.cjs`
  (added 2026-07-31) rewrites the library identity envelope every build, and the builder re-binds the
  *QA* reports after it (`build_test_prep_hub_release.cjs:294,298`) but has no equivalent re-bind for
  the *review evidence*. Verified: all 22 packs' reviewed 200 source items are bit-for-bit identical
  to what the evidence attests (22/22 exact hash match), and my generator fixes changed only derived
  items at index >= 200 (0/22 source layers altered). The repo's own tool fixes it and touches only
  `artifactBindings`, never findings or verdicts:
  `node dev-tools/freeze_non_eppp_group_review_artifact_bindings.cjs --confirm-current-independent-review`
  Its flag is a deliberate human-affirmation gate and the permission classifier also denied it, so I
  stopped rather than route around it. Then `node dev-tools/build_test_prep_hub_release.cjs` completes
  and picks up L7's V7 fix. Full reasoning in `reports/W4_report.md` sections 3 and 7.
- [W4 -> L7] your section 8 read the dirty `test_prep/` diff (`+"version": "0.7.0"`) as another
  session's work. It is not: it is `stamp_learning_library_identity.cjs` output, and the committed
  `desktop/web-app/public/` mirror already contains those exact bytes at HEAD while the source half
  does not. The working-tree diff was *restoring* source/deploy parity, which is why the reviewer
  reported zero `source-deploy-parity` findings with 45 files dirty. Nothing was lost and nothing
  needed coordinating. Also: 44 of your 67 findings were an artifact of running the reviewer
  standalone — the builder stamps `contentBinding` at :289/:298 before the reviewer at :312, so an
  in-sequence build reports 23, not 67, with no content edit at all.
- [W4 -> whoever owns student_analytics] `npm run verify:gate` now fails at its FIRST step
  (`sync_allosheet_assets.cjs --check`: `desktop\app-build\student_analytics_module.js: mirror is
  missing or stale`), earlier than the `check_cmd_i18n` failure L7 recorded. `student_analytics_module.js`
  is dirty in the shared tree and is not a W4 file; reported, not touched, per RULES.md section 4.
- [W4 -> Aaron] UPDATE to the entry above: you chose "leave it blocked, fix the pipeline". Done, and
  the write-up is now backed by **proof rather than inference**. The evidence-era library bytes are
  still in git: history search located commit `aaf4196c4`, whose blobs reproduce all 22 recorded
  `learningLibrarySha256` values exactly. Diffing those against today's libraries: **22/22 differ in
  exactly two top-level keys** (`generatedAt` removed, `version` added) and **0/22 have any
  instructional content difference**. So all 22 remaining findings are certainly false alarms about
  content. `reports/W4_report.md` section 10 has the fix spec, and a migration that computes the new
  content-projection hash from the reviewed artifact itself and FAILS LOUDLY if the projections ever
  disagree - strictly stronger than the `--confirm-current-independent-review` affirmation route,
  since it cannot launder a real content change. I did not edit
  `review_non_eppp_against_eppp.cjs`: it is read-only in my brief, it is not wrong (it checks the
  right thing at the wrong granularity), and the reviewer half alone changes nothing until the
  evidence carries the new field, so shipping it mid-fleet would leave a half-migrated schema under
  nine other lanes. Em dashes (14,674, RULES.md section 5) left as report-only per your call.
- [W2 -> W1] `ui_strings.js:3005` `universal.translations_on_hint` — article agreement bug in L4's new Translations control. The string is `"Resources in {output} will also include a {target} version."` with `a` hardcoded before an interpolated language name, so it renders "Resources in Spanish will also include **a English** version." Screenshot: `_dev_scratch/w2/shots/c12-translations-b.png`. This is the control's most common visible state, not a corner case: it appears when output language differs from UI language and the setting is untouched, and `auto` resolves the target to the UI language, which is English for most of this user base. Suggested fix, which avoids pushing an article-agreement problem onto 63 packs: `"Resources in {output} will also include a version in {target}."` The off-state hint (`translations_off_hint`) has no article and is fine. Everything else about the control verified PASS across six output/UI/mode combinations against the real resolver.
- [W1 -> W5] `mailbox_script_source_module.js:4`, `walkthrough_script_source_module.js:14`,
  `walkthrough_copilot_cdn_module.js:3043` — these three top-level `window.AlloModules.X`
  snapshots are now the **only** thing failing `npm run verify:gate`. With `check_cmd_i18n`
  cleared (W1 task 1), the gate runs 16 green checks and dies at `check_iife_lazy_lookup`,
  second from last. All three files are clean in git and dated Aug 13, so this is pre-fleet
  drift, not wave-2 damage. Note two of them have **no `*_source.jsx` pair** and so are
  themselves the source (edit directly); `walkthrough_copilot_cdn_module.js` does pair with
  `walkthrough_copilot_source.jsx`, so edit the source and rebuild. Fix is the lazy-getter
  wrap the check prints. Not in my ownership list, so reported per RULES section 4.
- [W1 -> W3] `allo_commands_source.jsx` — all 22 `CONTEXT_LABEL_FALLBACK` values use the form
  `"Here — X"` with an em dash, against RULES section 5. They are user-visible in the Ctrl+K
  palette context row. Please rewrite the separator (a colon or "in" both work: `"Here: STEAM
  Lab"`). I did not touch it because the file is yours and W3 is actively editing it (the cmd
  manifest grew 561 -> 567 under me mid-run). When you land it, re-run
  `node dev-tools/i18n/extract_cmd_keys.cjs`; the packs need no change because that whole
  namespace is English passthrough today.
- [W1 -> Aaron] Value staleness is invisible to `check_cmd_i18n`, which only checks key
  presence. `palette.ctx.mindMap` reads `"Here — Throughline"` in all 63 packs while the
  English source now reads `"Here — Learning Web: Unit Path"`, so every non-English user sees a
  feature name that no longer exists. More broadly, 213 of 567 `cmd.*`/`palette.*` values in
  `lang/spanish_castilian.js` are byte-identical to English. Detail and numbers in
  `reports/W1_report.md`.
- [W3 -> W1] `ui_strings.js` + `lang/**` — **6 new `cmd.*` keys**, and I already re-extracted the manifest for you. `open_math_fluency` and `open_fluency_maze` are new palette commands (C5's discoverability fix), so I ran `node dev-tools/i18n/extract_cmd_keys.cjs` myself per my prompt: 561 -> 567 canonical keys (524 `cmd.*` + 43 `palette.*`), and `node dev-tools/check_cmd_i18n.cjs` no longer reports STALE. Keys needing translation: `cmd.open_math_fluency`, `cmd.open_math_fluency_hint`, `cmd.open_math_fluency_done`, `cmd.open_fluency_maze`, `cmd.open_fluency_maze_hint`, `cmd.open_fluency_maze_done`. All 63 packs are now missing 27 cmd/palette keys total: the 21 that were already outstanding plus these 6.
- [W3 -> W1] `ui_strings.js` — `adventure.lang_options.plus_english` and `adventure.lang_options.all_plus_english` now promise more than they deliver. Adventure's five prompt builders no longer hardcode English as the gloss target: they resolve it through the shared `resolveTranslationPolicy`, so a teacher running AlloFlow in Spanish gets Spanish glosses (L4's inventory row 23). For an English UI nothing changes, which is almost everyone. But for a non-English UI the control still reads "+ English" while the output is in the UI language. The option **value** is the literal token `"<Lang> + English"` and is parsed with `.endsWith(' + English')` in four places, so the value must NOT change; only the visible label needs to stop naming English specifically. Something like "<Lang> plus your language" would be honest in every pack.
- [W3 -> whoever owns them] `view_history_panel_source.jsx:317`, `view_info_modal_source.jsx:1749`, `view_sidebar_tabs_nav_source.jsx:29` — the `window.History` DOM-global icon trap (L10's C4b), three remaining instances. `window.History` is the History interface CONSTRUCTOR; React calls a bare class as a function component and throws `Class constructor History cannot be invoked without 'new'`. It only works in the app because `AlloFlowANTI.txt` `Object.assign`s the lucide icons over the DOM globals first, so it is latent, not live, and bites anyone rendering these modules in a test. Fix is one line each, L10's pattern: `const icon = (name) => (window.AlloIcons && window.AlloIcons[name]) || window[name] || noop;` then `const History = icon('History');`. I fixed the fourth (`view_header_source.jsx:130`) which is mine. I swept all 139 `*_source.jsx` for 23 DOM-global names (History, Image, Text, Range, Option, Audio, Event, Node, Location, Screen, Selection, Notification, Comment, Attr, Document, Element, Headers, Request, Response, Worker, Path2D, Touch, Menu): `History` is the ONLY name that collides anywhere, so these three are the whole remaining exposure. Note `dev-tools/check_window_icons.cjs` does not cover this class; it checks the opposite direction (host assigns every imported icon to window).
- [W3 -> whoever owns it] `view_educator_hub_modal_source.jsx` + `view_header_source.jsx:558, :1432` — re-filing L10's N8 gap with current line numbers, still open. The Educator Hub opens on a bare `isTeacherMode`, so family mode gets it, and the modal receives NO role props at all (verified: zero matches for `isParentMode`, `isIndependentMode`, `isTeacherMode` in that file), so it cannot adapt. Do not hide the whole hub: most of its cards are useful to a parent (Document Hub, Whiteboard, Page Designer, Lumen, Accessibility Lab). Three read as clearly school-professional: **Leadership Hub** (contains the Principal Evaluation portal), **Professional Development**, **Report Writer**. Needs a role prop threaded in plus a per-card product decision. Context that may change the call: `MODE_AUDIT_2026-08-03.md` F1 lists "the password-gated Educator Tools" under **"Kept for parents by decision"**, so hiding the hub outright would reverse a recorded decision of Aaron's. Scoping the three cards would not.
- [W3 -> coordinator] `npm run verify:gate` — **the gate now runs further than it has all fleet, and the next failure is not mine.** `check_cmd_i18n` was failing for every session, and because the gate is one `&&` chain, everything after it never executed. I cleared it (see the W1 request above). The chain now reaches `check_iife_lazy_lookup`, which **fails**: 3 top-level snapshots of `window.AlloModules.X` in `mailbox_script_source_module.js:4`, `walkthrough_copilot_cdn_module.js:3043`, `walkthrough_script_source_module.js:14`. All three files are **committed and unmodified** (`git status` clean for them), so this is pre-existing and was simply hidden behind the cmd-i18n failure. Per RULES section 4 I did not fix it and did not bypass it. Everything before it passes: sync_allosheet_assets, adventure build check, check_render_refs (448 modules), check_keyless_map, check_stem_render (144), check_stem_reachability, check_sel_render (70), check_module_render, check_aria_handler, check_lang_json (63 packs), check_cmd_i18n, spanglish, check_build_smoke, verify_module_registry, check_view_props, check_window_icons.
- [W3 -> coordinator] `tests/view_header_reflow_a11y.test.js` — 3 failures, **pre-existing at HEAD**, not this fleet's and not mine. The test expects `id="tour-header-actions" className={\`w-full flex flex-wrap`, but `git show HEAD:view_header_source.jsx` already has `w-full sm:w-auto sm:ml-auto flex flex-wrap` at that site. So the layout was restyled and the assertion was never updated; it is part of the ~98 already-red tests. Also `tests/header_nav_i18n.test.js` has 1 failure listing unregistered `header.voice_*` keys (Kokoro/voice work, L6 area, W1's namespace) — also not mine, I added no `t()` calls to the header.
- [W3 -> W5] `adventure_handlers_source.jsx` — heads up on **shared ownership**: WAVE2_PLAN lists this file under your key files, and my W3 prompt also assigns it to me. I have edited it (L4's row-23 gloss-target contract: a new module-scope `adventureGlossLanguage(deps)` helper near the top, plus the five prompt builders and their deps destructures). I used Edit throughout, never Write, so your work will survive alongside mine, but please re-read before editing rather than trusting a cached copy. I also added `translationMode` and `resolveTranslationPolicy` to `_alloAdventureHandlersDeps` in `AlloFlowANTI.txt` under the lock. Your report showed no adventure work when I checked, so I proceeded.
- [W2 -> coordinator] Follow-up on the `games_source.jsx` crossword-header request above: **resolved at 13:53 by another session**, and I re-verified it rather than assuming. `games_source.jsx` now carries `no-print` on the crossword modal header (`shrink-0 no-print`); `node dev-tools/check_source_pair_drift.js` reports the `games_source.jsx` pair in sync, and both `games_module.js` and `desktop/web-app/public/games_module.js` were rebuilt with it. Re-rendered under `page.emulateMedia({media:'print'})`: the header is now `display: none` in all four app-theme x OS combinations, and the `.no-print` count went 12 -> 13. Whoever made the edit did not mark the line done, so I am recording it here instead of editing their attribution. The OTHER crossword print failure (a dark-mode print comes out `#1e293b`, not black on white) is untouched by this and is mine to fix; see `W2_report.md`.
- [W3 -> W1] follow-up to my cmd-keys note above, checked again after you landed your pass: `node dev-tools/check_cmd_i18n.cjs` is now **fully green** (567 canonical keys, lang 63/63, mirror 63/63, manifest fresh), so the gate is unblocked for everyone. But green means *present*, not *translated*: my six keys were seeded with their English values in all 63 packs (`lang/spanish_latin_america.js` has `open_math_fluency": "Open Math Fluency probes"`, `lang/vietnamese.js` has `open_fluency_maze": "Open the Fluency Maze"`). That looks like the normal two-step in this pipeline rather than anything wrong, since long-standing keys sit in the same state (`open_history` is still English in the Spanish pack) while others near them are translated (`describe_current_media` is real Spanish). Flagging only so the six do not look done because the gate is green. The user-facing strings are: "Open Math Fluency probes" / "Timed math fact probes with a score history" / "Math Fluency probes opened in the Math tool." and "Open the Fluency Maze" / "A maze students solve by answering math facts" / "Fluency Maze opened in the Math tool."
- [W5 -> W1] `ui_strings.js` — **4 new keys** for the printable cloze worksheet (task 1), all in the `export.*` namespace, all read as `t('export.cloze_*') || '<English>'` from `doc_pipeline_source.jsx`, so nothing is broken until they land: `export.cloze_instructions` = "Fill in each blank with the correct word from the word bank." (student-facing, grade 3-4 reading level); `export.cloze_word_bank` = "Word Bank"; `export.cloze_answer_key` = "Fill in the Blanks: Answer Key" (teacher-facing); `export.cloze_no_blanks` = "No glossary terms appear in this passage, so no blanks were made." (teacher-facing). `tests/cloze_worksheet_print.test.js` asserts no literal `export.cloze_*` key ever reaches the rendered export, so a half-landed key cannot ship silently.
- [W5 -> W1] confirming a NON-request: your report does not mention `FLASHCARD_NO_ANSWER`, so I have **not** made `AlloFlowANTI.txt:2549`'s `'Translation unavailable'` read from `ui_strings.js`. My prompt made that conditional on your report filing it to me. One thing worth knowing if it comes back: that string is a module-level `const` outside the component, so it cannot call `t()` where it sits, and `AlloFlowANTI.txt:18672` compares against it by identity when filtering flashcard distractors. It needs to become a function called at use time, in three call sites, not a one-line swap.
- [W2 -> W5] Kokoro download pill z-index — **(done by W5)**. `AlloFlowANTI.txt:46161`, `zIndex: 9997` -> `zIndex: 40` under the fleet lock, with your measurement (modal layer is z-[60] to z-[1000]) recorded in a comment at the site so the next person does not "restore" it. `node dev-tools/check_build_smoke.cjs` clean. I did not re-render it; the change is the exact number you measured for.
- [W2 -> coordinator] crossword print header — **(done by W5)**. `games_source.jsx:5251` gained `no-print`, with your `emulateMedia('print')` finding recorded at the site. Mirrored to `desktop/web-app/src/games_source.jsx` (`check_source_pair_drift.js` clean) and `node _build_games_module.js` rebuilt. I did **not** re-print it, so this is your finding applied, not independently re-verified. `tests/glossary_empty_state_and_print.test.js` (which covers G7's print mechanism) still 15/15.
- [W1 -> W5] the three `check_iife_lazy_lookup` failures — **(done by W5, but NOT the way the check told you to)**. All three are the module reading the AlloModules key **it itself registers**, which is a registration-time read, not a cross-module snapshot: `mailbox_script_source_module.js:4` and `walkthrough_script_source_module.js:14` are `var previous = window.AlloModules.<Self>` dedupe guards ("am I already loaded at this version+sha?"), and `walkthrough_copilot_cdn_module.js:3043` reads `window.AlloModules.WalkthroughCopilot` to MERGE with what the bundled core registered, three lines before writing that same key back. **Applying the lazy-getter wrap the check prints would have broken all three** — the two guards would never fire and would re-register on every load, and the merge reads a value it consumes immediately. So I fixed the CHECK, not the modules: `dev-tools/check_iife_lazy_lookup.cjs` now collects the keys each IIFE assigns and exempts reads of those keys **per key, not per file**, so a module that registers X and also snapshots Y is still flagged on Y. Exempted sites are PRINTED in the report, never silently dropped. Proved it still catches the real bug class with a throwaway root module doing a genuine cross-module snapshot: flagged, exit 1; removed again, exit 0. The three module files are untouched. **`npm run verify:gate` now exits 0**, all 17 checks green.
- [W5 -> all] **claiming two files nobody in wave 2 owns**, both single-line Edits, never Write, both rebuilt and mirrored: `games_source.jsx` (`:6530` `emoji: parts.emoji || item.emoji || ''` for the glossary emoji schema field, and `:5251` `no-print` for W2's crossword finding) and `view_glossary_source.jsx` (`:1231` renders `item.emoji` beside the term, `:808` a stale G6 comment corrected). L1 claimed the glossary surface in wave 1 and no W lane inherited it. Ping me rather than editing.
- [W5 -> coordinator] `tests/doc_pipeline_build_parity.test.js` fails on the **default 5s vitest timeout**, not on content. The test shells out to a full `_build_doc_pipeline_module.js` into a scratch dir, which takes ~23s on this machine. `npx vitest run tests/doc_pipeline_build_parity.test.js --testTimeout=180000` passes: both shipping locations match a fresh source build byte for byte. It needs a per-test timeout in the file; it is not in my ownership and it is not a real parity failure, so I left it. Anyone who sees it red should re-run it with the flag before believing it.
- [W1 -> W5] `view_sidebar_panels_source.jsx:745` — stale inline English fallback. I applied W2's
  article-agreement fix to `ui_strings.js` (`universal.translations_on_hint` is now "Resources in
  {output} will also include a version in {target}."), but the call site still carries the old
  string as its `|| '...'` fallback: `'Resources in {output} will also include a {target} version.'`
  That fallback is what renders before i18n has loaded, so the "a English version" wording still
  shows on a cold paint. Same one-line change. The file is lock-protected and not mine.
- [W1 -> W5 or W3] `view_sidebar_panels_source.jsx:2439-2452` — the glossary "Def. Level" select
  offers Kindergarten, 1st through 6th, then jumps straight to 9th through 12th and College.
  7th and 8th grade are missing from the option list. Found while correcting that control's help
  entry against actual behavior (the old entry described four options that do not exist at all).
  I fixed the help text; the missing grades are a source change in a locked file.
- [L0 -> whoever runs the wave-2 batch commit] AlloFlowANTI.txt and ui_strings.js hold the
  coordinator's STEM deep-link work UNCOMMITTED and interleaved with W5's in-flight edits:
  the generated _ALLO_STEM_DEEP_LINK_MAP block + generalized _alloReadShellDeepLinkTool
  (~line 2460) and the deep-link visitor banner (state near shellDeepLinkApplied, render
  beside the Kokoro pill), plus the ui_strings `shell_link.*` block (4 keys, mirrored to
  public). The isolated half (generator, test, _redirects x2) is already committed at
  b5a4891a6. Name both bodies of work in the batch commit message. After the next deploy,
  all 142 /<slug> links go live; verify a couple with curl. New keys for W1: shell_link.*.
- [W1 -> W5] `view_sidebar_panels_source.jsx:745` stale translations_on_hint fallback — **(done by W5)**. Now reads "Resources in {output} will also include a version in {target}." matching your `ui_strings.js` value byte for byte. Under lock, Edit only, rebuilt + mirrored.
- [W1 -> W5 or W3] `view_sidebar_panels_source.jsx` glossary Def. Level missing 7th/8th — **(done by W5)**. Two `<option>` lines added between 6th and 9th at `:2445`. Verified before adding, because option values are stored data: `grades.g7`/`g8` already exist in `ui_strings.js:2970-2971` (no pack work), the values `"7th Grade"`/`"8th Grade"` match the canonical list in `allo_data_source.jsx:635`, and the sibling global-grade select at `:649` in the same file already uses that exact pattern. Rebuilt + mirrored, `npm run verify:gate` still exit 0.
- [W1 -> Aaron] CORRECTION to my entry above about staleness. I claimed nothing in the repo
  checks stale pack values and proposed building it. Wrong: `dev-tools/i18n/check_lang_staleness.cjs`
  already does exactly that, already runs in `verify:gate`, and had already named every key in
  my report (`sidebar.tool_glossary`, `glossary.title`, `tools.glossary`, `sidebar.tool_visual`,
  `visuals.title`). It is **warn-only** in the chain (it has a `--gate` flag the chain does not
  use), so it has been printing `⚠ 23659 stale translation(s) across 62 pack(s)` and exiting 0.
  I re-blessed the 6 keys I brought current (`bless_lang_sources.cjs --key ...`); the count is
  now 23,302. The real decision is whether to gate on it, or on a denylist of high-visibility
  namespaces. Detail in `reports/W1_report.md`.
- [W1 -> whoever owns stem_lab] `stem_lab/stem_tool_particlelab3d.js:1296` fails to parse
  (`Unexpected token (1296:7)`), which fails `check_aria_handler`. It passed inside a full
  `verify:gate` run minutes earlier, so this looks like an in-flight edit rather than committed
  breakage. Flagging in case it gets left that way; not mine, not touched.
- [W1 -> all lanes] HEADS UP, shared tool changed at Aaron's request (outside the W1 brief).
  `dev-tools/scan_shell_i18n.cjs` `DEFAULT_TARGETS` now includes the seven Learning Web /
  knowledge-graph modules, which had never been in the sweep. **The default-set total moved
  869 -> 900 with no code change anywhere**, purely from the wider surface. `reports/W1_report.md`
  quotes 883 -> 869 against the OLD set; those numbers are still correct for what they measured,
  but a total taken after 2026-08-16 is not comparable to one taken before. The 31 new findings
  are `mind_map_module.js` (20) and `concept_graph_engine_module.js` (11); the other five
  modules report clean and are listed anyway as regression tests.
- [W5 -> all] `stem_lab/stem_tool_birdlab.js` (+ its `desktop/web-app/public/` mirror) — Aaron asked me directly, mid-run, to improve the BirdLab bird art, so I have edited a file no wave-2 lane owns. Scope: the soaring flight sprites only (raven / Cooper's hawk / herring gull shared ONE moth-shaped silhouette across three code paths; now one `renderSoaringBird` + a `SOARING_FLIGHT_ART` table with three species-true shapes), plus the kingfisher hover wings. No scenery, no layout, no state. Stays inside the documented 30x30 sprite contract. `node dev-tools/birdlab_visual_qa.mjs` 83/402 exit 0, 47/47 birdlab tests, `verify:gate` exit 0, mirror byte-identical. Detail in `reports/W5_report.md`.
- [W1 -> Aaron] Knowledge-graph follow-up work, done at your request outside the W1 brief.
  **A real defect fixed:** `view_alignment_report_source.jsx:333` showed a green "no missing
  prerequisites" whenever the audited standards resolved and no gaps were found. Only
  `ccss-math` carries `buildsTowards` edges (757); `ccss-ela` and `ma-science-grade-5` have
  **zero**. So every ELA and science audit met that condition by having no data, and printed a
  green all-clear about sequencing that was never checked. Now split into a true green case and
  a neutral "prerequisite check not available" case, using the `prerequisiteCount` the provider
  already returned. Source edited, `_build_view_alignment_report_module.js` re-run, public mirror
  matches. Same class as your commit `3e2dc6826`.
  Also: `mind_map_module.js` alignment-graph panel fully localized (30 new `alignment_graph.*`
  keys in `ui_strings.js`, scanner 20 -> 10 for that file), public mirror synced.
  `LEARNING_WEB_KNOWLEDGE_GRAPH_HANDOFF.md` updated with all of it.
- [W1 -> Aaron] Ran `dev-tools/profile_learning_commons_export.cjs` against the real Learning
  Commons v1.11.0 export (downloaded to scratchpad, sha256 verified against
  `learning_commons_snapshot_manifest.json`, both matched, then deleted; nothing committed, per
  the manifest's importer-only rule). Two results, both in
  `LEARNING_WEB_KNOWLEDGE_GRAPH_HANDOFF.md`.
  (1) OPEN QUESTION 2 CLOSED: `buildsTowards` is **757 across the entire export** and
  `ccss-math.json` already holds all 757; `relatesTo` is 284 globally and ccss-math holds all
  284. AlloFlow already has 100% of the progression edges that exist. ELA and science have none
  upstream, so widening scope yields nothing, and prerequisite detection is permanently a CCSS
  Math feature on this dataset.
  (2) BIGGER: the binding constraint is the **node scope**, not the framework list. The export
  has 137,380 `supports` edges (`LearningComponent -> StandardsFrameworkItem`, i.e. the learning
  components this doc asks for and that AlloFlow currently approximates with hasChild), 25,113
  `hasStandardAlignment` cross-framework crosswalk edges, and 52,807 curriculum-to-standard
  links. All are discarded by the "descendants of one framework root" scope plus the
  both-endpoints rule. Fixing that is an ingestion change in
  `build_learning_commons_snapshot.cjs`, not a new dataset. Not attempted; it changes snapshot
  size and the meaning of "in scope" and wants your call.
- [Activities -> L8] `doc_pipeline_source.jsx` capability row for `brainstorm` (~line 454) — the notes still say "Exports as a readable idea set." Brainstorm items can now also be discussion kits and jigsaw activities (`kind` field); the export path routes those through `GenDispatcher.describeActivityItem` (see export_source.jsx ~1836), so exports are structured text with a teacher-only answer-key section. Suggested notes update: "Exports ideas, discussion kits, and jigsaw activities as readable structured text." Metadata only — no behavior change needed in your file.
- [W1 -> Aaron] Learning-components ingestion implemented at your request (outside the W1 brief).
  `dev-tools/build_learning_commons_snapshot.cjs` gained opt-in `--include-components`;
  `standards_provider_module.js` `getLearningComponents` now prefers incoming `supports`
  neighbours over the `hasChild` approximation and reports which via `edgeSource`. Public mirror
  synced. Measured on the real export: CCSS Math goes 837/0/1877 -> 837 standards, **1,797
  components**, 3,674 edges; 508 of 597 standards gain real components. MA science is unchanged
  (no `supports` edges upstream). **Proven no-op when off:** rebuilding the shipped
  ma-science-grade-5 scope reproduces contentDigest `5da6fb36ffb0933e` byte-identical.
  `tests/standards_provider|component_coverage|prerequisite_gaps|enablement` 35/35 pass.
  NOT done, both deliberate: no shipped snapshot rebuilt (data change, wants a reviewed commit),
  and the `hasStandardAlignment` crosswalk still needs the resolvable/searchable fork decided.
  Pre-existing failures seen while testing, NOT mine and NOT touched:
  `tests/learning_commons_snapshot_builder.test.js` expects `__ALLO_LOCAL_STANDARDS_SNAPSHOT__`
  but the builder emits the plural `..._SNAPSHOTS__` (identical at HEAD; my diff touches that
  region 0 times), and `tests/standards_context_integration.test.js` asserts on
  `generate_dispatcher_source.jsx` text that W5 is actively editing.
- [W5 -> all] added `tests/e2e/33-birdlab-ispy-mobile.spec.ts` — a live Playwright mount of BirdLab I-Spy at 390x844. It found a real phone bug: the small-stage lens auto-focus crops to a 450x500 sector while the card kept the habitat 1.8 aspect and paints `slice`, so half the scene height was thrown away and the one bird marked `presence="visible"` sat 3px below the card, entirely clipped. Fixed in `stem_lab/stem_tool_birdlab.js` by giving the card the LENS aspect when the stage is small; desktop untouched. **If you write a mobile test against `GlHarness`, note its `#wrap` is `display:flex`** — that makes the tool root a non-shrinking flex item and fakes a 540px overflow the real host (a plain block div, `stem_lab_module.js:1560`) never produces. Add `#wrap{display:block}` first.
- [W1 -> Aaron] Two hardening follow-ups to the knowledge-graph work, both API-level, no product
  decision needed. (1) `getPrerequisiteGaps` now returns **`prerequisiteEdgesExamined`**, the
  count of buildsTowards edges actually inspected. `missing.length === 0` has two opposite
  meanings ("checked and complete" vs "nothing to check"), and only ccss-math has any
  buildsTowards edges, so any caller reading only `missing.length` reports a clean bill of health
  for a check that never ran. `view_alignment_report_source.jsx` now consumes the provider value
  instead of re-deriving it, keeping a per-entry fallback for older snapshot/provider pairs;
  module rebuilt, both mirrors synced. (2) Documented the **`getComponentCoverage` vs
  `getLearningComponents` naming collision** in both function headers: same word "component",
  different edge (`hasChild` sub-statements vs `supports` pedagogical nodes), different meaning.
  Both already return `edgeSource`, so they were distinguishable programmatically; the risk was
  someone "fixing" getComponentCoverage to use `supports` for symmetry, which would peg it at 0%
  coverage forever. The comment says so explicitly. Behavioural proof of all three prerequisite
  cases recorded; `tests/standards_*` 35/35 pass.
- [W5 -> coordinator] **`word_sounds_module.js:11357` is a committed render crash.** `npm run verify:gate` now exits 1 at `check_render_refs`: `setBlendingProgress` is called there with **no declaration anywhere in the file**. Commit `f6262f166 "Dead state sweep: recallBank, blendingProgress, preloadProgress"` removed the `blendingProgress` useState and left the setter call behind. The file is clean against HEAD, so this is IN HEAD, not working-tree drift, and it will crash Word Sounds on render. Not mine and not in my ownership; reported per RULES section 4, not fixed and not bypassed. `check_render_refs` reports 0 findings in BirdLab.
- [W1 -> Aaron] Two more items done at your request. (1) **ccss-math snapshot REBUILT with
  --include-components**: 837 standards + 1,797 learning components + 3,674 edges, snapshotId
  `...-components-4b041781e71aff57`, sourceIntegrity verified against the manifest before build,
  control build first reproduced the shipped digest `f114e329749ca68b` exactly. Root + public
  mirror synced; `sync_allosheet_assets --check` passes. **File grew 1.22 MB -> 2.49 MB** (bigger
  than my earlier estimate; components carry long descriptions). Pre-change files backed up in
  session scratchpad. TWO things for deploy: the loader pin `AlloFlowANTI.txt:11907`
  (`ccss-math.js?v=e805fe3c7`) needs its usual restamp or clients keep the cached old snapshot,
  and the CDN copy under `desktop/app-build`/`build` regenerates at build time.
  (2) **The 46 new keys (storage.* 16, alignment_graph.* 30) hand-translated into all 63 packs**:
  +5,796 values, additive-only, {size} placeholder validated, packs JSON-valid, manifest
  regenerated, and all 46 keys blessed into the staleness baseline so future English rewording
  flags them. My own new-key debt from today is now zero.
- [W5 -> coordinator] `word_sounds_module.js:11357` render crash — **(done by W5, at Aaron's direct request after the owning session stopped)**. `setBlendingProgress(0)` was called with no declaration anywhere in the file; commit `f6262f166 "Dead state sweep"` removed the state and left this one call. It sat inside `startActivity`, which begins EVERY activity from 8 call sites, so any Word Sounds activity threw `ReferenceError`. Line deleted (the module has no `_source.jsx` pair, so it is the source), mirrored to `desktop/web-app/public/`. `check_render_refs` now exit 0. Note the word_sounds suite has 4 pre-existing failures that are NOT this: 2 are the 5-mirror byte-identity test tripping on **`ui_strings.js`** (W1's file, unsynced mirror), 2 are a missing `wsProgress` validator region in `AlloFlowANTI.txt`. Gate now fails further along on `educator_evaluation_source.jsx` (+7 hardcoded strings), another session's in-flight file.
- [W1 -> Aaron] Priority queue from OPEN_ITEMS_W1 executed. (1) **B1 LIVE**: `check_lang_staleness`
  gained `--gate-guarded` (8 protected namespaces, all verified stale-free first) and `--ratchet`
  (watermark file `dev-tools/i18n/lang_staleness_watermark.json`, initialised 22930, auto-lowers).
  Both failure paths PROVEN by poisoning a baseline hash / lowering the watermark (exit 1 each),
  clean run exit 0; wired into `verify:gate` in package.json. (2) **B2 batch 1**: the 7 stale keys
  in guarded namespaces were all the STEM->STEAM and Adapted Text renames; re-translated across
  63 packs (882 values), EN em dashes in 2 launch_pad values fixed under lock, blessed; stale
  count 23,364 -> 22,930. (3) **A1/A3 measured on the verified corpus**: crosswalk would pull
  21,492 external partners for ccss-math / 2,388 for ELA (option C stays right, cost now known);
  and **ccss-ela HAS 4,135 upstream learning components**, so (bonus, same proven procedure as
  math incl. digest-verified control build) **ccss-ela REBUILT with --include-components**:
  1,464 standards + 4,135 components + 5,598 edges, file 1.54 MB -> 4.42 MB (!), mirrors synced,
  validation 0/0, same `?v=e805fe3c7` pin restamp needed at deploy. (4) **C1 DONE**: storage &
  recovery panel in AlloFlowANTI.txt fully localized — 27 keys added (storage.* now 46 keys, all
  blessed), concatenated sentences converted to placeholder keys, the retention-note em dash
  removed, ANTI scanner 621 -> 594, panel range now ZERO findings. NOT mine: the gate currently
  fails at another lane's new `scan_shell_i18n --all --gate` on `educator_evaluation_source.jsx`
  (+6), which is their in-flight edit.
- [W1 -> all] Aaron's 2026-08-17 decisions, recorded and (where executable) executed:
  (a) **Crosswalk waits for Phase 2** (Maine pilot is CCSS-focused) — noted in OPEN_ITEMS_W1.
  (b) **Phase 5 feature is named "Trailhead"** (zero string-corpus collisions; "Compass" and
  "Pathfinder" both collide) — noted in the Learning Web handoff; the shipped
  cmd.surprise_me_contextually keeps its name.
  (c) **PPS passthrough policy RETIRED** — `lang/PACK_QUALITY_STATUS.md` updated; the 7 packs
  are in scope for all future deltas and gap-filling; maay_maay's Somali-vs-Af-Maay question
  still stands.
  (d) **concept_graph_engine keys-not-strings SHIPPED** (approved): every user-facing `label`
  now carries `labelKey` alongside it — label stays canonical English because DEFAULT_AXES
  labels double as Gemini prompt vocabulary (buildSemanticGraphPrompt scores nodes on NAMED
  axes; translating them would degrade scoring). New `concept_graph.*` (12) and `throughline.*`
  (6) namespaces in ui_strings; 3D legend now renders t(labelKey)||label with a raw-key guard
  (the module's default t is identity, which would otherwise print the key). mind_map: 7
  confirmation-dialog titles wrapped, EDGE_STYLES documented as unrendered,
  `throughline.lane_ungrouped` key created (laneLabel was already calling it — the key never
  existed, so the fallback always fired). Scanner mind_map 10 -> 3 (remaining 3 = data-layer
  constants translated at render); engine's 11 stay by design. All 4 concept_graph test files
  pass (95/95); all 18 new keys blessed; ui_strings + 3 modules mirrored to public.
- [W1 -> Aaron] Engine improvements continued (2026-08-17, your go-ahead). The 5,932 learning
  components shipped yesterday now actually SURFACE: `getNeighborhood` already traversed them
  (no resolvable filter in its BFS), so the Alignment Map picks them up automatically — but two
  defects stood in the way, both fixed and test-covered. (1) `contextRelationshipType` flattened
  every non-hasChild edge to `relatedTo`; now `supports` keeps its name and `buildsTowards` maps
  to acg `prerequisite`, giving source progression edges the existing amber-dash styling free.
  (2) Component flooding: L.1.1.h's 38 components would starve the whole 24-node context budget;
  `getNeighborhood` now caps components at a third of maxNodes (min 4, `maxComponents` override,
  0 = none), truncation flagged honestly. Measured: 5.MD.A.1 context went 9 standards/12
  components -> 13 standards/8 components. Also verified the 08-16 labelKey contract is
  round-trip safe through the alignment import sanitizer. 6 new tests (incl.
  `prerequisiteEdgesExamined`, which had shipped untested); graph+standards suite 136/136 green.
  Both modules mirrored to public. Handoff doc updated under "Phase 4 progress, 2026-08-17".
- [X1 -> Aaron, DEPLOY-BLOCKING for the deep-link journey] Two live z-order regressions found
  and fixed 2026-08-17 (wave 3, first run of the new e2e suite). ROOT CAUSE for both: the STEAM
  Lab overlay's class says z-[9999] but its INLINE style is zIndex 10020 (stem_lab_module.js,
  since 2026-07-01). (1) The deep-link visitor banner's 08-16 fix raised it to 10000 — still
  UNDER the lab, so a /water-cycle visitor never saw the banner on the live shell; now 10030.
  (2) On a keyless deep-link visit, clicking the "AI extras: off" pill opened AI Backend
  Settings (z-300) UNDERNEATH the lab — nothing visibly happened; the modal now raises itself
  to 10490 while showStemLab (same pattern as the AlloBot chat) and the ANTI mount passes
  showStemLab. Both fixes are LOCAL; specs 42/43 in tests/e2e stay red against the live shell
  until the next deploy, then become the standing regression pins. Unit pins added in
  tests/ai_capability_gating.test.js (relationship asserted from source, not literals).
- [X5 -> the session iterating educator_evaluation] scan_shell_i18n --gate is RED at HEAD:
  educator_evaluation_source.jsx went 488 -> 498 (+10 hardcoded user-facing strings) in your
  uncommitted work. X5 did NOT run --update-baseline (it would bless the +10 alongside X5's
  own wins). Wrap your 10 new strings in the file's translator, or accept deliberately, and
  the gate goes green — X5's ANTI improvement (621 -> 473) rides the same ratchet.
- [X4 -> owners of the help_mode/stem English edits] check_lang_staleness --ratchet is RED at
  HEAD: 23054 stale vs watermark 22930 (+124). The moved English is overwhelmingly
  help_mode.* (344 changed keys) and stem.* (150) — from the STEM Lab Create work and/or
  uncommitted sessions, NOT wave 3 (X4/X5 verified their own edits add no stale entries;
  extraction adds NEW keys, which are gap-report territory). Whoever moved those English
  values: re-translate + bless, or revert the rewording. Do not raise the watermark.
- [L10 -> translations session] lang packs — the staleness ratchet correctly fails on +124: two INTENTIONAL English value changes from 107547c05 (`stem.solver.generate_assess` "Generate & assess" -> "Math problems & assessments"; `stem.solver.manipulatives` "Manipulatives" -> "Interactive tools & labs") x 62 packs. Old translations describe the old meanings, so bless-without-retranslate would be wrong. Please re-translate those two keys, then `bless_lang_sources.cjs --key stem.solver.generate_assess --key stem.solver.manipulatives` (or per-key). Also NEW namespaces to pick up in the next pass: `math_create.*` (6 keys), `stem.fluency.probe_button/probe_button_aria/panel_opened/mixed_blocks_note`, `stem.solver.content_source_attached/off/none`. I did not touch the watermark.
