const fs=require('fs'),assert=require('assert/strict');const file='stem_lab/stem_tool_anatomy.js';let s=fs.readFileSync(file,'utf8');assert.ok(s.includes('.anatomy-compare-tray button{min-height:44px;'));s=s.replace('.anatomy-compare-tray button{min-height:44px;','.anatomy-tool-shell .anatomy-compare-tray [type=button]{min-height:44px;');s=s.replace('.anatomy-compare-tray>div:last-child{flex-wrap:wrap}', '.anatomy-tool-shell .anatomy-compare-tray p,.anatomy-tool-shell .anatomy-compare-tray strong{font-size:14px;line-height:1.5}.anatomy-compare-tray>div:last-child{flex-wrap:wrap}');fs.writeFileSync(file,s);fs.writeFileSync('desktop/web-app/public/'+file,s);
const test='tests/anatomy_lab_science.test.js';let tests=fs.readFileSync(test,'utf8');assert.ok(tests.includes('aria-label="Use Skull (Cranium) as comparison target"'));tests=tests.replace('aria-label="Use Skull (Cranium) as comparison target"','aria-label="Compare Skull (Cranium)"');fs.writeFileSync(test,tests);
console.log('Improved tray readability and updated the renamed-control assertion.');
