# Tree Life Lab improvements

Implemented from the September 4 engagement review.

- The default Grow view now offers a short drought discovery: predict, compare two copies of the same starting tree over three years, interpret evidence, and try recovery. Only water differs between the two trials. Saved comparisons survive subsequent exploration.
- A shared-scale schematic shows starting height and both outcomes. Small field-notebook milestones recognize a first ring, a fair comparison, and reflection.
- Free exploration remains available. An ordinary time jump can now be explained directly without starting another experiment or losing its result. Saved explanations retain the associated time-jump evidence.
- Completed actions survive a drought ending. A completed trial alone no longer counts as a submitted explanation.
- The header is smaller and the scene has more width. Seasons and time-scale explanations expand on demand. K–2 starts with food indicators; detailed carbon quantities, allocation, and temperature remain available in expandable sections.
- Intervention panels and the small scene badge explicitly label earlier changes. Their wording no longer conflicts with the live conditions.
- Very small maintenance costs remain visibly nonzero, including in the seasonal ledger. Limiting-factor explanations now acknowledge interacting inputs instead of promising that every other input has no effect.

The second pass connects root investment and shade tolerance to the annual biological equations. This remains a qualitative teaching model; the new coefficients are explicit teaching assumptions rather than measurements.

- Root mass relative to leaf area now affects a bounded water-access index. Additional roots help an undersupplied canopy, with diminishing returns and an ongoing carbon cost. Roots cannot extract water from completely dry soil. The former minimum root-mass floor was removed because it could create roots without surplus carbon.
- Shade tolerance improves low-light capture and carries a small maximum-capacity cost. Photosynthesis remains nondecreasing as light increases. Other species traits still matter, so the trait does not guarantee a winning species.
- A keyboard-accessible section beside the allocation controls explains the tradeoff. It shows current water access, root upkeep, and a one-year preview that responds to allocation changes without advancing the tree. The preview explicitly holds the current weather steady and includes changes in both roots and leaves.
- Live readouts, condition-change feedback, annual steps, controlled trials, response curves and the seasonal ledger all use the same physiology function. Wilting uses the same root-dependent water access. Earlier saved observations remain historical results.

### Model assumptions and provenance

For root carbon mass R and leaf area A, uptake capacity is 1.25R / (R + 0.025A), or zero without roots. The effective water-access index is the soil-water setting multiplied by capacity, clamped to 0–1. A root-to-leaf-area ratio of 0.1 kg C/m² gives capacity 1; unlimited roots approach 1.25. This index feeds both stomatal opening and the existing water multiplier. It is not a water volume, a new soil-moisture measurement, or a soil-water balance. Soil depth, root architecture, rainfall, hydraulic failure and depletion are not simulated.

The concept of uptake capacity relative to canopy demand follows the root-to-leaf relationships discussed by [Comas et al., Root traits contributing to plant productivity under drought](https://www.frontiersin.org/journals/plant-science/articles/10.3389/fpls.2013.00442/full). That review emphasizes functional root area/length and morphology; using root carbon mass as a proxy here is a deliberate simplification, not a coefficient obtained from that study.

For shade tolerance s, the half-saturation parameter in the light response is 0.22(1 − 0.65s); maximum photosynthetic capacity is multiplied by 1 − 0.12s. This is a stylized low-light efficiency versus capacity tradeoff. Real shade tolerance involves multiple mechanisms: for example, the [USDA Forest Service account of American beech](https://www.srs.fs.usda.gov/pubs/misc/ag_654/volume_2/fagus/grandifolia.htm) describes low respiration and responsive stomata. That source does not validate this equation or its species coefficients. The model retains its existing species respiration rates and does not simulate acclimation.

Validation covers zero-water and zero-root boundaries, diminishing returns, scale consistency, delayed allocation effects, carbon upkeep, excessive-root tradeoffs, monotonic light response, isolated shade-trait comparisons, and agreement between live, annual, seasonal and trial calculations. The original 60-year growth and longer survival checks still pass. Browser checks cover the allocation preview and learning workflows. These checks establish internal consistency, not empirical biological accuracy or measured learning gains.

Source and deployment-mirror modules were updated together. No deployment was performed.

Validation includes the existing tree unit suite, new deterministic comparison and saved-state regressions, and browser checks of the guided path, recovery, direct reflection, phone layout, keyboard operation, full screen, and accessibility of the new discovery in light, dark, and high-contrast themes. Review captures and execution logs are in `.tmp/tree-review/`.

Final second-pass verification: 193 unit tests passed across the tree, discovery and physiology suites. All six unique browser checks passed (the five existing workflow checks plus the new root-preview check). The root-preview test also checks keyboard use, zero-water feedback, light/dark/high-contrast accessibility and phone overflow. Source and deployment mirror are byte-identical; scoped `git diff --check` passed. No deployment was performed.

### Third pass: evidence and scene continuity

The guided result now puts the measured food balance first, on one shared signed scale. Shortfalls extend left of zero, surpluses right, and zero remains zero. Height, age and detailed carbon quantities are available in a native disclosure. K–2 sees the visual amounts first, with numeric evidence available in the disclosure and to screen readers. Saved evidence remains independent of later live-tree growth. Unequal trial survival times receive an explicit note.

The desktop scene column now stretches across the control area, allowing its existing sticky wrapper to follow scrolling. Sticky positioning is released on short windows, single-column layouts, and when the season guide is expanded so lower content remains reachable. A conflicting tablet grid rule was corrected so widths between 761 and 960 pixels use the full available column. Biological equations are unchanged in this pass.

Third-pass verification: all 197 unit tests and all eight unique browser checks passed. Browser coverage includes signed evidence, optional detail via keyboard, light/dark/high-contrast accessibility, stable saved results, desktop scrolling, expanded-guide reachability, short windows, 820-pixel tablet layout and phone overflow. The desktop fixture now specifies a tall viewport; the signed-chart fixture uses a simulated drought that actually produces a deficit. Final unit verification ran separately from the browser checks after a contention-related timeout. Source and mirror match, and scoped whitespace checks pass. Screenshots: `.tmp/tree-review/refined-food-evidence.png`, `refined-food-mobile.png` and `refined-sticky-scene.png`. Changes remain local.

### Fourth pass: visual scene polish

The focal tree now sits against smaller, softer woodland silhouettes and smoother overlapping hills. Layered conifer crowns and irregular broadleaf crowns replace single cones and spheres, while the woodland and hills retain four instanced batches. Distant trunks extend into their crowns rather than stopping below them.

Meadow blades are smaller and use a neutral per-instance brightness multiplier. This fixes double application of the green material tint. Their material uses the existing sampled floor-light illustration directly, avoiding a second lighting penalty that made the grass look like dark scribbles. Ground cover still responds to canopy shade and soil/season palettes.

Fill lighting makes the focal foliage easier to read, especially in dark mode. A neutral leaf fill avoids adding a green cast to autumn colours. High contrast keeps its existing simplified scene, and reduced motion continues to suppress animated scenery. The camera scale and simulation equations are unchanged. Visual captures cover a seedling, mature autumn oak, winter pine, dark-mode willow and a high-contrast oak.

Grass cards now use their texture as an opacity mask, keeping transparent-edge RGB values from muddying the meadow colour. Reduced motion reuses the resting leaf and clone poses; changing the season or stress still updates those poses, and returning from motion restores the resting pose.

Fourth-pass verification: all 197 focused unit tests passed. Eleven unique focused browser checks passed across the completed runs: five visual contexts, two camera checks, ground-cover response, scene picking/ownership, resting reduced-motion reuse and seasonal updates while reduced motion is enabled. The final five-context capture run passed with video and tracing disabled after an earlier run encountered local browser startup/teardown errors. All five final captures were visually inspected. Decorative woodland and hills stay in four instanced batches below 25,000 triangles. Source and mirror are byte-identical and scoped whitespace checks pass. Captures: `.tmp/tree-review/scene-seedling.png`, `scene-autumn-oak.png`, `scene-winter-pine.png`, `scene-dark-willow.png` and `scene-contrast-oak.png`. Changes remain local; no deployment was performed.

### Fifth pass: Grove Journey

Added an optional eight-year grove campaign with a persistent nine-patch map, authored event draws or procedural weather, resource-priority cards, carbon-funded reproduction, establishment milestones, replay and a lasting journal. The lab tree and its experiments remain separate. Gene editing, within-species evolution, invasive-species dynamics and old-growth chapters remain future extensions, with explicit scientific boundaries in the interface. See `tree-life-lab-grove-implementation-2026-09-04.md` for delivered scope, assumptions and validation. Across completed runs, 210 focused unit tests and both final campaign browser checks passed. Changes remain local.

### Sixth pass: campaign 3D close-up

Grove Journey gained an optional real 3D close-up of one selected campaign tree. A pressed-button switch above the habitat map chooses between the 2D whole-grove map, which stays the default, and the 3D view; a selected-patch action also opens that patch in 3D. The close-up offers patch and individual-tree selectors, species, age, height and diameter, camera rotate/tilt/zoom/reset buttons, and leaves/trunk/roots inspection. The rendered tree is the actual campaign individual with its local light, soil moisture and temperature taken from the last completed year, or the starting patch conditions at year zero, and viewing does not advance time. Empty patches explain that no tree has arrived rather than showing a fabricated specimen. The whole-grove map remains 2D; no full 3D forest or new fullscreen mode was added.

The existing module-level 3D viewer is reused with a stable attach point and subject key, so one canvas persists across yearly growth and tree changes and is released when returning to the map or selecting an empty patch. Returning to Grow restores the independent lab tree. Grove part selection and viewer status are kept separate from lab state. If 3D fails on a device, the map, measurements and navigation remain available and the camera buttons are disabled. The illustration is labelled as a summer view; roots are a schematic cutaway, and the background woodland is scenery while the map records the actual grove.

Sixth-pass verification: the 13 campaign tests and 4 new scene tests passed (17 across two files); a React list-key warning seen in the earlier run was resolved by keying the close-up section, and the scene file passed cleanly afterwards. All four campaign and 3D browser tests passed earlier in the pass, and the dark/high-contrast/phone capture test was rerun after a year-zero caption fix and passed again. The refreshed light, dark, high-contrast and phone captures were visually inspected. Source and desktop mirror are byte-identical; syntax and scoped whitespace checks pass. Captures: `.tmp/tree-review/grove-3d-oak.png`, `grove-3d-light.png`, `grove-3d-dark.png`, `grove-3d-contrast.png` and `grove-3d-phone.png`. Changes remain local; no deployment was performed.

### Seventh pass: evidence, prediction, mortality and replay comparison

A probe of 48 worlds showed that no campaign tree ever died, so "trees lost", the lineage-record wording and the Keep reserves card carried no real consequence. One explicit scenario rule now adds that consequence: trees three years old or younger face an extra mortality draw when their patch's soil moisture falls below 35 percent that year, scaled by how dry the patch is and reduced by the species' drought tolerance. The draw is keyed on year and individual, so it is independent of the weather and of other trees, and the same code still replays identically. Older trees still die only through the existing food-reserve rule. The forecast warns about the risk in dry years and the disclosure states the assumption.

Each year's receipt now records every landing (patch, route, outcome and the condition that most reduced the odds: dry soil, shade, no space or chance alone) and every loss (patch, species, age and cause). A "Where it happened" list in the evidence reads those records patch by patch. On the habitat map, dead trees stay as bare grey snag glyphs behind the living crowns, patch counts include snags, and a badge on each patch marks this year's arrivals (+) and losses (−); patch labels for screen readers carry the same information.

Before living through a year, an optional predict-then-check step asks how many arrivals to expect and whether the grove will make more food than it uses. The evidence then reports Matched or Missed with a one-sentence reason drawn from the year's conditions. Predictions live beside the run, not inside the save format; rewinding drops the prediction for the undone year, and replaying or returning to setup clears them.

Completed runs are added to a small ledger (at most twelve, keyed by code, mode and choices). The ending card lists other completed runs of the same code and mode, showing the priority sequence, established descendants, patches, living trees and whether the goal was reached, with a note that identical weather makes the comparison fair. Setup shows how many runs of the current code are remembered. This supports the recommended learner check about fair replays without adding genetics, invasive species or a full forest view.

Seventh-pass verification: a new unit file, `tests/tree_lab_grove_evidence.test.js`, adds five tests covering landing/loss record consistency across 24 worlds, the young-tree mortality rule (young only, dry patches only, founders untouched, weather unchanged), the rendered "Where it happened" list with snags and badges, prediction checking and its removal on rewind, and the ledger comparison. The final unit run passed 18 tests across the campaign and evidence files; the four scene tests passed on the source immediately before two cosmetic edits (loss pluralisation and snag size). A new browser test, `tests/e2e/treelab-grove-evidence.spec.ts`, predicts, lives through a year with a loss, checks the receipt, badge and snag, rewinds and replays, completes two runs of one code, verifies the ledger, and reports zero axe violations in light, dark and high contrast; it passed twice, the second time after the cosmetic edits. Map, receipt and three theme captures were visually inspected: `.tmp/tree-review/grove-evidence-map.png`, `grove-evidence-receipt.png`, `grove-evidence-light.png`, `grove-evidence-dark.png`, `grove-evidence-contrast.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Eighth pass: stored food, grouped evidence, discoveries and a journey chart

Each receipt now records the whole grove's stored food and reproductive savings across living trees, and the evidence card reports them with the change from the previous year. The Keep reserves card therefore leaves a visible trail even in years when nothing dies, which the seventh-pass ledger comparison had shown to be its weak point.

"Where it happened" groups patches whose landings had identical outcomes onto one line ("Sunny ridge, Open meadow, Stream bend: 1 failed each, dry soil lowered the odds."), so a dry year reads as one fact instead of six near-identical lines. Losses keep their own lines.

Three discoveries join the field journal: losing a young tree to dry soil (derived from the run), making a prediction that matched the evidence, and completing two runs of the same grove code. The last two are earned when a year or run is saved and, like the others, recognise observations rather than granting bonuses.

The field journal opens with a small line chart of living trees and established descendants by year. It follows the shared chart rules: one axis, two-pixel lines, eight-pixel markers with a surface ring, a legend plus end labels, hover titles on every point, and the existing timeline list as the text and table alternative. The green and amber pair was checked with the palette validator against the light card and the dark card surfaces; the second series is dashed with square markers so identity never depends on colour alone, and high contrast uses white and yellow. The chart is text tokens and marks only; no value wears a series colour.

Eighth-pass verification: four tests were added to `tests/tree_lab_grove_evidence.test.js` (stored-food receipt fields and their rendered line, the dry-soil discovery plus the three new journal names, grouped where-lines found by searching worlds for a receipt with matching patch outcomes, and the two-series chart with legend, twelve hover titles, marker shapes and text alternative). The combined run passed 22 tests across the campaign and evidence files; the scene file's worker was killed by the machine's memory limit in that run and passed all four tests when rerun alone. The browser test now chooses the prediction that will match, asserts the stored-food line, opens the field journal, checks both earned discoveries and the chart, and still reports zero axe violations in light, dark and high contrast; it passed. Captures inspected: `.tmp/tree-review/grove-journal-chart.png`, `grove-evidence-receipt.png`, `grove-evidence-dark.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Ninth pass: forecast risk preview and reflection notes

The forecast now says what next year's weather means on the ground. Every patch whose prescribed soil moisture will fall below 35 percent is listed under "Dry next year", the number of young trees standing in those patches is counted, and the card reminds learners that new arrivals rarely take root in dry soil. On the map those patches get a dashed border and a small "dry next" chip, their labels say so for screen readers, and the selected-patch panel shows next year's moisture beside the light setting. The preview disappears once the run has ended. Nothing here changes the simulation; it reads the same environment function the engine uses.

The ending card gains a reflection field for the completed run, asking what the learner would try differently and why. The note is saved into that run's ledger entry, so later comparisons of the same grove code show it beside the numbers, and a fresh replay starts with an empty field. Journal discovery and timeline lists now show their bullets and numbers.

Ninth-pass verification: two unit tests were added (dry-next markers, counts and captions found by searching worlds for a forecast that puts young trees at risk, with calm and ended cases; reflection field presence and earlier-run notes in the comparison). The campaign and evidence files passed 24 tests together; the four scene tests were last run on the eighth-pass source and the ninth pass does not touch the close-up. The browser test now asserts the dry-next count against the engine before advancing, types a reflection, checks its persistence and its appearance in the second run's comparison, and confirms the empty field and absent markers on the finished replay; it passed with zero axe violations across light, dark and high contrast. Captures inspected: `.tmp/tree-review/grove-forecast-risk.png`, `grove-map-dry-next.png`, `grove-evidence-light.png`, `grove-evidence-contrast.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Tenth pass: K-2 wording layer

Grove Journey now has a younger-reader layer for the K-2 band. A single helper swaps short wording for the full text in about thirty places: the stats row, the selected-patch summary and tree list, setup and ending copy, the reflection field, the forecast (with plain-language event descriptions and a simpler conditions line), the card names and descriptions (Grow roots, Save food, Make new trees), the reproduction route, the prediction options, the evidence card, the grouped "Where it happened" lines and loss sentences, the prediction check, and the timeline. The facts, numbers and evidence structure are identical across bands; only the words change. Grades 3 and up keep the full text, and the science disclosure stays unchanged for every band.

Tenth-pass verification: one unit test renders the same loss year in K-2 and in the default band and checks that the short wording appears only in K-2, that the numbers and the count of evidence lines match, that setup and forecast copy switch, and that grade 3–5 keeps the full forecast text. All 29 units across the three grove files passed in one run. A second browser test mounts a K-2 campaign at a loss year, checks the wording in the stats, cards, receipt and evidence, and reports zero axe violations; both browser tests passed. Captures inspected: `.tmp/tree-review/grove-k2-decisions.png`, `grove-k2-receipt.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Eleventh pass: visual cues, engagement and accessibility

The year tiles above the map now carry each completed year's event icon with a hover title, so the run's weather history is readable at a glance. Map symbols scale with the modelled tree height (square-root, clamped, still not to scale), so growth is visible from year to year rather than only at the juvenile/adult switch. Storm-opened patches show a pale light wedge behind the trees. Every patch has a thin soil-moisture bar for the completed year, and its accessible label now includes the moisture percentage. Newly arrived trees pop in with a short scale animation on a wrapper group that has no transform attribute, so the keyframe cannot wipe the SVG positioning; the class is only added when the host allows motion, and the reduced-motion media query disables the keyframe as a second guard. The evidence card opens with a "New discovery" banner when the completed year earned a journal entry, including a first matched prediction; it uses a status role so screen readers hear it. The legend explains the bar and the wedge in both bands.

Eleventh-pass verification: two unit tests were added (tile icons and titles against the receipts, nine moisture bars whose widths match the engine's patch moisture, moisture in every patch label, one light wedge per storm gap, symbol scale varying within bounds, and blank tiles before the run starts; the pop-in class matching the year's arrivals and absent under reduced motion, and the discovery banner on the first year). All 31 units across the three grove files passed in one run. The browser test asserts no pop-in class under reduced motion, nine moisture bars and the numbered tile after advancing; both browser tests passed with zero axe violations across the three themes and the K-2 view. Captures inspected: `.tmp/tree-review/grove-evidence-map.png`, `grove-evidence-receipt.png`, `grove-evidence-light.png`, `grove-evidence-contrast.png`. A final tile font-size adjustment was syntax-checked and mirrored after those runs. Source and mirror are byte-identical; scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Twelfth pass: keyboard flow, goal tracker and sharing

Focus now follows the action when the control that was pressed disappears: starting a journey moves focus to the first forecast heading, finishing a run moves it to the ending heading, and returning to setup moves it to the setup heading. Those headings are programmatically focusable and show a visible focus ring. A skip link at the top of the grove, hidden until focused, jumps keyboard users past the nine patch buttons and the 3D controls to the decision column. The stats row shows the two-patch goal as two dots that fill as patches gain established descendants, alongside the existing count.

Two share buttons sit with the replay controls. "Copy grove code" copies the code so a classmate can play the same weather; "Copy run summary" copies a plain-text account of the run (code, mode, each year's event, card and outcome, the result, and the reflection if one was written). Copying goes through the shell's copy helper first, because some hosts block the clipboard API, then falls back to the older copy command; when every path fails the text appears in a read-only, pre-selected box with a status message suggesting Ctrl+C, and the outcome is announced to screen readers.

Twelfth-pass verification: two unit tests were added (skip link target and focusable headings, goal dots and their fill count against the engine, copy buttons per state, copied and failed status rendering, the fallback text box, and K-2 labels). All 33 units across the three grove files passed in one run. The browser test now checks that focus lands on the ending heading when the run finishes, that the shell copy hook receives a summary beginning with the code and year one, that a failing host shows the selectable fallback with the code, and that the skip link is visible when focused and moves focus to the decision column; both browser tests passed with zero axe violations across the three themes and the K-2 view. The ending capture was inspected. Source and mirror are byte-identical; scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Thirteenth pass: habitat detail, evidence link and one announcement

Each map patch now carries a small decorative ground detail that matches its habitat: ripples for damp patches, stones for exposed ones and low ferns for sheltered ones, drawn inside the aria-hidden symbol so the three habitat types are distinguishable beyond tint, including in high contrast where the detail is grey on black. Patch buttons gained a hover tooltip with the patch name, habitat, soil moisture and any storm opening, matching what screen readers already hear. Under the advance button, a link reads "Read the year N evidence" (K-2: "See what happened in year N"); activating it scrolls to the evidence card and moves focus there, which matters on phones where the card sits well below the decision column. The evidence card is now a labelled, focusable region rather than a live region, because the year change is already announced once when it is saved; screen readers no longer hear the whole card twice.

Thirteenth-pass verification: one unit test was added (habitat detail counts per type, tooltips with moisture on every patch, the link's target and wording in both bands, and the receipt's id, focus target and absence of a live-region attribute). All 34 units across the three grove files passed in one run. The browser test activates the evidence link and confirms focus lands on the receipt and that nine habitat details render; both browser tests passed with zero axe violations across the three themes and the K-2 view. Captures inspected: `.tmp/tree-review/grove-evidence-map.png`, `grove-evidence-contrast.png`, `grove-k2-decisions.png`. Source and mirror are byte-identical; scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Fourteenth pass: a section navigator for the Grow view

This pass moved off the campaign and back to the single-tree views, beginning with fresh captures and an axe scan of Grow, Chemistry, Spread and Check rather than assumptions. That scan found no accessibility violations in any of the four views and no horizontal overflow on a 390-pixel phone. Two apparent defects in the first capture, a narrow scene column and a garbled Tree Memory heading, were both artifacts of viewing a 4500-pixel image scaled down to 607 pixels wide; measuring showed the canvas at 667 by 578 in a 695/620 split and the heading as one clean string. Sticky positioning of the scene was also verified directly: the canvas moved 293 pixels while the page scrolled 1400.

What measurement did find is length. The Grow view is 4500 pixels tall on a 1365-pixel desktop and 7015 pixels on a 390-pixel phone, roughly eighteen screens, with no way to reach a particular step except scrolling. Chemistry is 2174 pixels and the knowledge check is 1124, so Grow is the outlier.

Grow now opens with a "Jump to a step" navigator: five links to Run the clock, Carbon budget, Tree memory, Conditions, and Where the surplus goes. Each target is a labelled section element that is programmatically focusable, so activating a link both scrolls the step into view and moves focus there, and screen readers can reach the same five landmarks by region navigation. Scrolling is instant when the host asks for reduced motion. The navigator uses the same theme tokens as the cards, so it follows light, dark and high contrast, and its pills wrap to two rows on a phone while staying above the minimum tap height.

Fourteenth-pass verification: a new unit file, `tests/tree_lab_grow_nav.test.js`, checks that the five links match five existing, labelled, focusable, uniquely identified sections in document order, that each section still contains its own panel heading, that each link's text matches the section it points at so the two cannot drift apart, that the navigator is absent from the other views, and that it survives the K-2 band where the panels are folded differently. All three tests passed. A new browser file, `tests/e2e/treelab-grow-nav.spec.ts`, confirms the view is long enough for jumping to matter, then activates all five links and asserts that focus lands on the target and the target reaches the upper viewport, reports zero axe violations on the navigator in light, dark and high contrast, and on a 390-pixel phone checks no horizontal overflow, five tap targets of at least 24 pixels, and a working jump. Both tests passed. As a regression guard the main Tree Lab suite was rerun: 180 tests passed. Captures inspected: `.tmp/tree-review/grow-nav-light.png`, `grow-nav-dark.png`, `grow-nav-contrast.png`, `grow-nav-phone.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Fifteenth pass: the navigator generalised to Transport

Transport is the second-longest view, at 3236 pixels on a desktop and 6172 on a phone. Probing it first showed no accessibility violations and no horizontal overflow, and revealed a different structure from Grow: a plain card list rather than the workbench layout, with one dominant card of 1268 pixels followed by three shorter ones.

Rather than copy the navigator built for Grow, this pass generalised it. The single section list became a map keyed by view, the wrapper became a shared section helper, and the navigator builder now takes a view identifier. Grow's five links are unchanged; Transport gains four, for Two separate plumbing systems, Where the carbon goes, Inside the trunk, and Cutting a ring of bark. Both views now share one implementation, so a future long view needs only a list entry and a wrapper call. Each Transport card keeps its own class inside the new section wrapper, so existing styling and tests are unaffected.

Fifteenth-pass verification: the unit file grew from three tests to five, adding coverage that Transport's four links match four labelled, focusable sections in document order, that each card class still resolves inside its wrapper, and that neither view leaks the other's section identifiers. All five passed. The browser file grew from two tests to three; the Transport test activates all four links, asserts focus and viewport position for each, and reports zero axe violations across light, dark and high contrast. All three passed. One flaky step was fixed rather than retried: screenshotting the navigator on every iteration of the Grow theme loop raced the 3D scene's settle after each rerender and timed out under load, so the capture moved outside the loop while the axe assertions stayed inside it. As a regression guard the main Tree Lab suite and the navigator units were run together: 185 tests passed. Capture inspected: `.tmp/tree-review/transport-nav.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Sixteenth pass: Chemistry navigator and the tab strip verified

Chemistry now carries the shared navigator too, but it needed a different design from Grow and Transport. Two of its cards are gated by grade band and the K-2 path returns before them, so a fixed list of links would point at cards that never rendered. The view therefore collects its sections as it builds them, and the navigator is inserted at the front once the list is complete. Grades 9-12 see five links, grades 6-8 see four, grades 3-5 see three, and K-2 sees none. The navigator now also hides itself when it would have fewer than two destinations, since one link is not navigation.

This pass also closed a coverage gap rather than a code gap. The seven-tab strip implements the full ARIA tabs pattern, with a roving tabindex and arrow, Home and End keys, but nothing in the repository tested it, and axe cannot check keyboard behaviour. Tracing every key press confirmed the implementation is correct: arrows move and wrap, Home and End reach the ends, focus follows the selection, and exactly one tab stays reachable by Tab throughout. That behaviour is now asserted by a test.

Sixteenth-pass verification: the unit file grew from five tests to eight, adding coverage that Chemistry's links match exactly the sections that rendered in each of three bands, that every link resolves to a real focusable section with no dangling targets, that K-2 gets no navigator, and that the strip is placed first with each card still inside its wrapper. All eight passed. The browser file grew from three tests to five, adding a Chemistry jump test and the tab keyboard test. All five passed. Two failures during development were diagnosed rather than retried: the Chemistry assertion that a section reaches the top of the viewport is impossible on a 2174-pixel page, so it now asserts the section is in view; and the tab test's fixed 300-millisecond waits were too short while a heavy view re-rendered, so it polls instead. An earlier assertion that the navigator appears only in Grow was updated, since Chemistry and Transport now have their own. As a regression guard the main suite, the navigator units and the grove evidence units were run together: 205 tests passed. Capture inspected: `.tmp/tree-review/chem-nav.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Seventeenth pass: the Compare chart's five species lines

Compare had never been measured in earlier passes. It is 3118 pixels tall, the third-longest view, and it is accessibility-clean, but its growth chart carries a real defect that no automated gate had caught: five species lines told apart by colour alone.

The palette was validated rather than judged by eye. Across all pairs, which is the right test when five lines share one chart and any two can be confused, the dark magenta and aqua steps are 1.6 apart under deuteranopia, effectively identical for a red-green colour-blind reader, and the dark yellow and orange are 10.6 apart even with full colour vision. In light mode magenta and orange are 12.9 apart, below the 15 floor. The palette is the shared categorical order, whose own documentation notes that only its first three slots clear the all-pairs gates and that beyond three the remedy is secondary encoding rather than re-stepping.

The tool already had a set of five dash patterns, but applied them only in high contrast, where every hue collapses onto a single accent. Those patterns now apply in every theme, on the chart lines and on the matching legend swatches, so each species is identified by pattern first and colour second. High contrast is unchanged in appearance and now rests on the same mechanism everywhere else relies on. No colour values changed, so nothing else that uses the species hues is affected.

Seventeenth-pass verification: a new unit file, `tests/tree_lab_compare_series.test.js`, checks in light, dark and high contrast that all five lines carry distinct dash patterns with exactly one solid, that every legend swatch matches the dash of the line it names, and that colour remains a second channel in the themes that have colour while high contrast relies on the dash alone. All three tests passed. The chart was captured in dark and high contrast and inspected: the two lines that were indistinguishable under deuteranopia now read as dash-dot and dotted with no reliance on hue. Compare reports no axe violations. As a regression guard the main suite, the new tests and the navigator units were run together: 191 tests passed. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Eighteenth pass: a colour-encoding sweep and the species comparison grid

The previous pass found a defect that automated accessibility checking cannot see, so this pass swept the rest of the tool for the same class rather than assuming it was isolated. Three sets were validated by running the checker, not by eye.

The four factor hues, which carry light, carbon dioxide, water and temperature across the Chemistry curves, the limit bars, the condition sliders and the ring strip, pass every gate in both light and dark modes across all pairs. No change was needed. The five allocation hues fail colour-blind separation across all pairs, with amber and green 5.7 apart under protanopia, but the strip carries a full text alternative naming every part and its percentage, and five labelled sliders with names and values sit directly beneath it, so identity never rests on colour. Five categories cannot clear the all-pairs gates under any palette, and the shared guidance names direct labels as the remedy, which this already has. The quiz was checked against the rule that status must never be colour alone: correct and incorrect options carry a tick or cross glyph, a spoken announcement, an inset bar and, for a wrong choice, a dashed border. No change was needed in either place.

The sweep did surface a layout problem in Compare, measured rather than eyeballed. The species grid used automatic fitting with a 285-pixel minimum, which lands on four columns at desktop width. Since there are always exactly five species, the fifth card was stranded on its own row beside three empty slots. Explicit column counts now skip four entirely: five across above 1180 pixels, three between 900 and 1180, two down to 620, and one below that. At desktop width all five cards now share a row at equal height, so the shade tolerance, drought tolerance and bark defence bars line up and can be read across every species at once, which is what a controlled comparison is for.

Eighteenth-pass verification: a new browser file, `tests/e2e/treelab-compare-layout.spec.ts`, pins the column count at five widths, asserts that no row ever holds four cards, that no card overflows its column, that the page never scrolls sideways, and that the five cards share one height at desktop width. It passed. The five-across layout was captured and inspected: nothing is cramped or clipped and the pills wrap cleanly at 258 pixels per card. As a regression guard the main suite, the compare series tests and the navigator units were run together: 191 tests passed. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. Changes remain local; no deployment was performed.

### Nineteenth pass: fixed-count grids and a meaningless axis on first load

The previous pass established that a card set with a fixed member count should not use automatic column fitting, because the browser cannot know that five items make a four-column row look broken. This pass applied that rule to the rest of the tool by measuring every candidate grid at seven widths.

The measuring tool itself needed correcting first. Grouping cards into rows by their exact top coordinate reported a one-then-three split for grids that were plainly a single row, because a highlighted panel sits two pixels proud of its neighbours. Bucketing tops within a small tolerance fixed it, and the corrected numbers cleared several grids that the first run had wrongly flagged. Two-item and four-item grids behave well at every width.

Two real orphans remained, both near 860 pixels, which is a small laptop, a landscape tablet or a split-screen window. The four response curves in Chemistry split three and one, and the six chapters in the knowledge check split five and one. Both now use explicit column counts that skip the awkward numbers: the curves step from four to two, and the chapters from six to three. At 860 pixels the curves read as a two-by-two grid and the chapters as two rows of three, both verified by capture.

Measuring also exposed a defect in the default state of the Chemistry view. The response curve axis maximum was rounded to one decimal, and a seedling's whole-year gross photosynthesis is a small fraction of a kilogram, so it rendered as zero. Both ends of the axis therefore read zero on first load and the curve floated in an unlabelled box. Maxima below one now keep two decimals, matching the headroom annotation beside them, so a new tree shows 0.04 where it used to show 0.

Nineteenth-pass verification: a new browser file, `tests/e2e/treelab-grid-orphans.spec.ts`, pins the per-row counts for both grids at five widths and asserts the general property that a last row never holds a single item while an earlier row holds more, alongside per-card and page overflow checks. Both tests passed. A unit test covering the axis labels was added to `tests/tree_lab_compare_series.test.js`, asserting for both a default seedling and a grown oak that the top of every curve axis is greater than zero and never equal to its floor. All four tests in that file passed. Three test-authoring mistakes were corrected rather than worked around: a callback passed to the browser as a string silently dropped its argument, the axis labels could not be selected by document order because the limiting panel adds an annotation, and a shell heredoc ate the backslashes in a regular expression. As a regression guard the main suite, the compare series tests and the navigator units were run together: 192 tests passed. Captures inspected: `.tmp/tree-review/curves-860.png`, `quizpath-860.png`. Source and mirror are byte-identical; syntax and scoped whitespace checks pass.

A note on repository state: a concurrent session committed and deployed a broad change at 13:02 on September 5 that swept in the Tree Lab work up to and including the Grow section navigator. Everything from the shared navigator rename onwards, which is the Transport and Chemistry navigators, the colour-blind dash fix, the species grid and the changes in this pass, remains local and uncommitted.

### Twentieth pass: printing

The pass before last found a defect that only appeared in the state a learner starts in, so this pass first swept every view in its true first-load state, with no tree supplied and nothing clicked. That sweep came back clean: the only zeros are honest ones for a brand-new seedling, and the corrected curve axis holds.

The real gap was printing. Thirty-eight sibling tools in this collection carry print rules; Tree Life Lab had none, so a teacher printing a lab got interactive chrome, a black rectangle where the live scene sits, and ink-heavy panels. Paper now keeps the readings, the habitat map and the explanations, and drops only what needs a pointer: the tab strip, the jump navigator, the camera and view controls, the sliders and the action buttons. Backgrounds, gradients and shadows are cleared for ink, cards get a plain rule instead of a coloured shadow, sticky positioning is released so it cannot repeat across pages, and cards and headings avoid breaking across a page.

Two mistakes in the first version were caught by looking at the rendered page rather than trusting the tests. Hiding every button would have removed the habitat patches and the quiz answers, which are content that happens to be a button; those now stay, flattened. And clearing only the background colour left the gradient washes untouched, because a gradient is a background image.

The third mistake was in the test rather than the code, and it matters more. A collapsed disclosure is not laid out at all in this browser, so no print stylesheet can reveal it, yet asking for its child's computed display returns block and the assertion passed while nothing rendered. The field journal, the saved evidence and the science boundaries would all have printed as bare one-line summaries. The open state is now set before the print and restored afterwards, by a single pair of listeners registered once for the page lifetime, and the test fires the real event and measures that the content occupies space.

Twentieth-pass verification: a new browser file, `tests/e2e/treelab-print.spec.ts`, checks under print emulation that the pointer-only controls and the scene disappear while the readings, the sections and the cards remain, that every disclosure genuinely opens and then returns to how the learner left it, that all nine habitat patches and at least four quiz options still print, and that the view controls do not. Both tests passed. The printed grove was captured and inspected: the field journal with its chart and timeline, the species traits and the full science boundaries with their citations are all on the page. As a regression guard the main suite, the compare series tests, the navigator units and the grove evidence units were run together: 209 tests passed. Source and mirror are byte-identical; syntax and scoped whitespace checks pass. This pass is local and uncommitted.

### Twenty-first pass: read-aloud for the year evidence

Comparing this tool against its 146 siblings on a handful of feature markers put one gap in front of everything else. Seventeen sibling tools offer read-aloud through the browser's speech engine and Tree Life Lab offered none, which matters for a tool that explicitly supports a K-2 band. The other markers were either already covered here, with eleven forced-colours rules and twelve reduced-motion rules, or not applicable, since this tool persists through the platform's own save rather than browser storage and already has its own full-screen stage.

The year evidence in Grove Journey now carries a read-aloud control, following the same shape the sibling tools use: a guarded utterance, a rate, handlers that reset the state when speech ends or fails, and a pressed state on the button. The spoken text is composed from the receipt rather than scraped from the page, so it says the year, the card used, the arrivals, the descendants, the losses and every where-it-happened line. Because it is built from the same strings the view renders, a K-2 learner hears the short wording, and the rate slows for that band. The control only appears where the browser can actually speak, and it is hidden when printing.

Building it required one refactor worth noting. The where-it-happened lines were composed inside the function that rendered them, so the spoken summary would have needed a second copy of that logic. The line-building is now its own function feeding both the list and the speech, keeping one derivation behind both.

Twenty-first-pass verification: a new browser file, `tests/e2e/treelab-read-aloud.spec.ts`, stubs the speech engine and checks that pressing the control speaks text beginning with the year and naming the card, that pressing again stops and cancels, that the pressed state tracks both, that the K-2 band speaks its own wording at a slower rate, and that the control is absent where the browser cannot speak. All three tests passed, with no accessibility violations in the evidence card. The full unit suites and the print and grove browser suites were rerun: 209 unit tests and 4 browser tests passed.

Two mistakes in this pass are worth recording. A script wrote real line breaks into the middle of a stylesheet string and broke the file; it was repaired by converting exactly the two stray line breaks back into escapes, and the whole unit suite was run to confirm the repair before continuing. And the first version of the test appeared to prove the feature did nothing, when in fact the test was at fault: the browser's speech engine is exposed as a read-only property, so assigning a stub to it silently does nothing and the tool talks to the real engine, which is mute in a headless browser. Defining the property properly made all three tests pass unchanged. The lesson is the general one: when a new feature appears completely inert, suspect the test's stub before the code.

### Twenty-second pass: a jump strip for the longest view that lacked one

The section navigator built in an earlier pass serves Grow, Chemistry and Transport, but nothing decided which views deserved it beyond the order they were worked on. Measuring every view at a desktop width settled it: Grow is 4584px tall, Transport 3302px, Compare 2663px, Chemistry 2240px, Spread 1928px and Check 1124px. Compare was the tallest view with no way to skip through it, taller than Chemistry, which has one.

Compare now collects its own destinations the way Chemistry does, so a block that a grade band does not render never becomes a dangling link. Four regions are exposed: the shared experiment with its chart and slider, the reasoning trail, the five species strategies, and the closing suggestion. The species stage was already a labelled landmark, so it became the jump target itself rather than gaining a second wrapper that repeated its name, and its link wears that same name so a screen-reader user hears one place, not two. Spread and Check stay as they are: both are short, and Check is a single question at a time.

Twenty-second-pass verification: three new unit tests and one new browser test. The unit tests check the four links resolve to focusable labelled sections in document order, that each block survived the wrapping intact with its five species cards, that no Grow or Transport identifiers leak in, and that every grade band from grades 3-5 upward lists only what it rendered. K-2 has no Compare tab at all, so asking for the view lands back in Grow, and the test states that rather than asserting an empty page. The browser test clicks all four links, confirms focus and viewport follow each one, confirms the species landmark is still single, and finds no accessibility violations in light, dark or high contrast. It also asserts every pill shares one computed weight, colour and background, so none can read as pre-selected. Nine browser tests across the navigator, compare layout and grid suites passed, and 246 unit tests passed across nine files. Source and mirror are byte-identical. This pass is local and uncommitted.

### Twenty-third pass: the heading outline, and a guard that was lying

Reading the heading outline of all seven views turned up a defect no automated rule reports. The tool's own name, "Tree Life Lab", was an h3, and the chapter title directly beneath it was an h2. Someone moving through the page by heading level therefore started one level down and then jumped back up, and every card in the view read as a peer of the lab itself rather than as part of the chapter it belonged to. The accessibility scanner is silent here by design: its heading rule reports levels skipped on the way down, and a heading whose level rises is legal to it.

Two more headings claimed the top level for themselves: the Grove Journey tagline and the closing card of the knowledge check, both marked as h2 and so reading as further chapters. The tool name is now the highest heading in every view, the chapter title is its only peer, and those two are cards at the level their content actually sits. Nothing moved on screen: the sizes and weights of all three come from class rules, and the two stylesheet rules that selected the old tags were renamed with them. That was measured rather than assumed, by building a twin of the old tag beside the retagged heading and comparing what the cascade gave each: the retagged title keeps its 42px, its weight and its margins, while the old tag would now fall back to the app's 14px body heading.

Twenty-third-pass verification: a new unit file, `tests/tree_lab_headings.test.js`, walks the outline of all seven views in all four grade bands and holds four properties: every view opens with the tool name at the highest level, no level is skipped going down, no heading is empty, and each view has exactly two top-level headings, the lab and the chapter it is showing. All four tests pass, and they fail on the old markup. Regression: 250 unit tests across ten files, and the accessibility, grove, grove evidence, print and evidence browser suites.

One of those browser tests was failing before this pass, and its failure was false. A guard meant to prove the app stylesheet had loaded read the background colour of the tool's root and found it transparent. The screenshot showed a fully painted page: the root wears a gradient, and a gradient is a background image, so the colour is transparent on a perfectly styled page. Checking it against the last committed build confirmed the guard, not this work, was at fault. It now accepts either a fill or an image, and still fails on a genuinely unstyled page. This is the third time in this project that reading only `background-color` has produced a wrong conclusion about a gradient.

### Twenty-fourth pass: answer positions that a student cannot learn

The knowledge check already had a guard against scoring by position, and it passed. It measured the whole bank of sixteen questions: the first option was correct twice, the others four or five times each, which is uneven but unremarkable. The guard was measuring a set no student ever sees. A grade band only shows the questions written at or below its level, and inside those pools the picture was worse: of the twelve questions a grades 6-8 student works through, the first option was correct exactly once. "It is never A" was close to a winning strategy.

The cause was a rotation that was deterministic but blind to the bands. Authored answers sit mostly at the first or second option, and shifting them by a fixed per-question cycle happened to push them away from the first slot in the pools that matter. The bank now places each answer in whichever position is carrying the lightest load across the pools that will show that question. Grades 3-5, 6-8 and 9-12 each come out exactly even across the four positions, K-2 is even to within one question because five questions cannot divide by four, and no position ever repeats three questions running. Because the placement is computed rather than hand-tuned, a question added later keeps the spread even without anyone remembering to rebalance.

Twenty-fourth-pass verification: two new unit tests sit beside the guard that missed this. One requires every band's pool to be even to within a single question and to use every position at least once, so "never pick the first one" cannot be a strategy in any band. The other forbids the same position three questions running, since evenness alone still permits a bank that reads as a pattern. Both fail on the old placement. One existing test had written a wrong answer as the literal index zero, which is now the correct answer to that question; it derives the wrong pick from the bank instead. All ten unit files pass, 252 tests, along with the print, grid and knowledge-check accessibility browser suites.

One consequence worth stating: a saved pick from an earlier build refers to a position that may now hold a different option. Recorded results are unaffected, since the check stores whether each question was answered correctly rather than which slot was clicked.

### Twenty-fifth pass: the phone view, and one stranded chip

Looking at the knowledge check on a phone rather than a desktop turned up two candidates. Only one was real.

The progress trail — the little numbered chips that show which questions have been answered — wrapped by row, and at a phone width twelve chips became eleven on one row and a single chip stranded on a second. It reads as a mistake rather than as a row. The trail is now laid out in explicit columns on narrow screens, with the column count chosen from the number of questions the band shows: the widest row that fits, stepping down until the last row would hold at least two. The four pool sizes in the bank now give rows of five; six and two; six and six; and six, six and four. Because the count is computed, a question added later cannot reintroduce the orphan.

The second candidate was not a defect. In the capture, the tab strip was scrolled to the start with the active tab out of sight on a narrow screen. Measuring it showed the strip already scrolls the selected tab into view on a phone: on load, after a resize from a desktop width, and when moving with the keyboard. The picture was an artefact of how the capture resized the page, and the honest answer is no change.

Twenty-fifth-pass verification: a unit test walks the column choice for every question count from one to twenty-four and requires that the last row never holds exactly one chip, and it states the four pool sizes the bank produces so a bank change surfaces here. A browser test measures the real rows in all four bands at five widths from 1365px down to 360px and fails on the old markup, where the phone widths produced eleven and one. Regression: 253 unit tests across ten files, plus the knowledge-check accessibility and print browser suites.

The heading tests added in the previous pass now carry explicit time limits. Each renders every view, and under a loaded machine they were exceeding the five-second default and reporting as failures when nothing was wrong.

### Twenty-sixth pass: the contrast nobody was checking

The accessibility suite asserts that axe reports no violations, and it never has. Running axe on the surfaces it did not yet cover — Grove Journey in four variants, Transport and the knowledge check in dark and high contrast — also produced no violations. What it produced instead was between 85 and 261 colour-contrast results marked *incomplete* on every surface. Incomplete means axe could not decide: the text sits on a gradient, so the rule cannot resolve a single background colour and declines to judge. A suite that filters to violations drops all of those silently. Most of the tool's text had never actually been checked.

So it was measured. Every glyph on a page is made invisible, the page is captured once, and the pixel under each text box is read back as the colour that text is really painted on, judged at the tightest point of a small patch so a gradient is graded where it is worst. Across five surfaces that decided 977 text nodes, and twenty of them were below the AA floor.

Nineteen were the same fault. The light theme's accent green reaches only 3.77:1 on white, and it was the colour of small text throughout: section eyebrows, the numbered step markers, the "limiting" badge, the year label in the tree memory, the reproduction bank figure in the header. Text now uses a darker green of the same hue, which reaches 5.48:1. The accent *fill* is untouched, and deliberately so: the near-black ink on those filled buttons sits at 4.95:1 against the lighter green and would have been the thing broken by darkening it. Two other faults were separate: an eight-pixel state label in the tree memory used the muted grey on a green chip at 3.16:1 in dark and now uses the ink colour at 6.57:1, and the reproduction figure in the header set its colour inline rather than through the stylesheet, so it needed the same fix in its own place.

One node still measures below 4.5 and is correct as it stands: the trend arrow in the memory comparison badge. It is a decorative glyph, hidden from screen readers and stated in words beside it, so the 3:1 floor for graphical objects applies and it measures 3.46:1.

Twenty-sixth-pass verification: this measurement is now a spec of its own rather than a one-off. It runs five surfaces, requires that axe still leaves a substantial number of nodes undecided — otherwise the spec would pass while checking nothing — and fails with the specific selector, colours and ratio for anything below its floor. Confirming it is not vacuous, forcing the old accent back in produced eight failures on the Grow view alone. Reading the pixels needed no new dependency: a small PNG reader built on the platform's own decompression sits beside the spec. Regression: 253 unit tests and the full 24-test accessibility suite.

Five of the twenty original failures turned out to be my own measurement error, and the correction matters. SVG text is painted with a fill rather than a colour, so it survived the rule that hid the rest of the text, and the sample under an axis label was the label itself: text measured against its own glyph, reported at 1.00:1. The chart was fine all along. The spec now hides SVG text explicitly.

### Twenty-seventh pass: the rest of the surfaces, and a measurement that over-reached

The sampled contrast measurement from the previous pass covered five surfaces. Running it across sixteen — every view, in every theme it supports, and the K-2 band — measured 2,515 text nodes and reported fifteen below the floor. Two of the three clusters were real.

The season field guide was the real find. Its four identity hues are legible as swatches and borders but not as text, and they were both: the amber kicker read 2.04:1 on the light card, the summer label 1.91:1, winter 2.13:1, spring 2.26:1 and autumn 3.54:1. Every season keeps its hue for the swatch, the icon and the border, while the labels take a darker step of the same hue, all above 4.7:1 on the tinted card. The high contrast theme still collapses them onto its single accent, and the dark theme keeps the bright hue, which passes there already. The second real find was in high contrast: the selected tab's icon sat in a near-black chip while inheriting the black ink that the yellow tab gives its text, so the icon was invisible. The chip no longer paints in that theme, and the glyph sits on the tab itself.

The third cluster was my measurement over-reaching, and it is the more interesting lesson. The rule took the worst pixel in a small patch around each text box, on the reasoning that a gradient should be judged where it is tightest. In high contrast a chip border is a single white line one or two pixels from the text, and the worst-pixel rule read that line as the background: white text on white, 1.00:1, on text that is perfectly legible. It now takes the median pixel of the patch, which is what the glyph actually sits on. That change removed three phantom failures and left both real ones standing, which is the right kind of confirmation. A sticky panel can also move under a stitched full-page capture, so the measurement now un-pins sticky and fixed elements before the shot.

Twenty-seventh-pass verification: the permanent spec now covers twelve surfaces rather than five — every view at least once, each theme several times — and all twelve pass. Regression: 253 unit tests and the full 24-test accessibility suite. The season guide was also read on screen after the change, to confirm the labels still carry their season colour rather than turning into four shades of brown.

### Twenty-eighth pass: a focus ring you could barely see

Every focusable control in this tool changes on focus — a sweep across nine view-and-theme combinations found no control whose computed style stays the same. That answers the wrong question. What matters is whether a person can see the change, and in the light theme they largely could not: the ring was drawn in the same mint green the tool uses for accents, which measures 1.92:1 against white and 1.75:1 against the slate card, against the 3:1 a focus indicator needs. The dark theme's ring measures 11.4:1 and high contrast 21:1, so this was a light-theme fault, which is the default a student sees.

The ring now uses a darker step of the same hue, 5.48:1 on white. Measuring the ring on the real page then found two more faults that reasoning about colours would have missed. On the grove map, the selected patch drew its own three-pixel accent ring one pixel outside the button, so the focus ring landed directly on it: two greens, 1.46:1, and a keyboard user could not tell a selected patch from a selected-and-focused one. Selection now sits inside the patch as an inner band, leaving the outline to focus alone. And the discovery card, which is a focusable container rather than a button, had no rule of its own and fell back to the browser's default ring: near-black, 2.94:1 against the dark card. Focusable containers now get the same ring as everything else.

Twenty-eighth-pass verification: a new spec measures the ring rather than describing it. For every focusable control on a surface it reads the ring the cascade actually gives it, then samples the page pixels the ring is drawn over — its own outline offset out from the control's edge — and requires 3:1. Five surfaces, 205 rings measured, all passing. Forcing the old mint back in fails it on real pixels at 1.60 to 1.86:1, which is both a proof the gate works and independent confirmation of the original fault. Regression: 253 unit tests, the twelve sampled-contrast surfaces, the full accessibility suite and the three grove browser suites, and the grove map was read on screen to confirm a selected patch still announces itself.

One earlier reading needed correcting. A first sweep reported five controls with no focus indicator at all, including the season buttons. They sit inside a collapsed disclosure, where focus legitimately does nothing; open it and each one takes focus and shows its ring. The measurement now skips what cannot be focused rather than reporting it as unstyled.

### Twenty-ninth pass: growth lines you can actually see

A chart line carries the information, so it needs the same 3:1 against its background that any meaningful graphic does. Measuring the marks in the compare chart the way the text was measured — hide them, capture the page, read what is underneath — found that three of the five light-theme hues did not have it: the green drew at 2.82:1 against the white chart, the amber at 2.17:1 and the pink at 2.69:1. Every line has carried its own dash pattern since an earlier pass, so identity was never colour alone. A line you can barely see is not rescued by its dash.

Both palettes were rebuilt against the palette validator rather than by eye, searching hue families and steps for a set that passes every check with all pairs compared. The light five now pass the lot — lightness band, chroma, colour-blind separation, the normal-vision floor, and 4.8:1 or better against the card.

The dark five needed a judgement, and it is worth stating plainly. The old dark palette put the pink and the green 1.6 apart under deuteranopia, which is to say those two lines were one colour to a red-green colour-blind reader; it also failed the normal-vision floor. No set of five hues inside the validator's dark lightness band separates properly — a search over several thousand combinations found none. The new five sit 8.8 apart under deuteranopia and 12.2 under tritanopia, 22.6 apart in normal vision, and every one clears 3:1, at the cost of a wider spread in lightness than the band prefers. Separating two series that were previously indistinguishable is worth more than an even lightness, and that trade is recorded here rather than buried.

Twenty-ninth-pass verification: a new spec measures the five growth lines against the chart surface in all three themes and passes. It measures only the marks that carry data: the dots wear a stroke in the surface colour deliberately, as a separator where marks overlap, and the axis grid is meant to be recessive — a rule aimed at lines would fail both, and both are correct as they are. Regression: 253 unit tests, including the ones that pin each line's dash to its legend swatch, plus the twelve sampled-contrast surfaces, the full accessibility suite and the compare layout suite. Both charts were read on screen in light and dark.

### Thirtieth pass: the response curves, and a test that stopped looking

The previous pass fixed the growth lines on the comparison chart. The same measurement, pointed at the four response curves in Chemistry, found the same fault in another place: the sun curve drew at 2.94:1 against its white panel, and in the dark theme the carbon dioxide curve drew at 2.57:1 against the dark card. Both are under the 3:1 a data mark needs.

The four factor colours had been chosen once and shared between the themes, with only the sun getting a per-theme step. Each theme now has its own set, searched with the palette validator rather than nudged by eye. The light set — a deep amber, a violet, a dark blue and a crimson — passes every check with all pairs compared, separating by 11.3 under colour-blind simulation while sitting at 4.5:1 or better on the panel. The dark set does the same at 7.5, which the validator allows because each curve lives in its own labelled panel rather than relying on colour to say which factor it is.

The curve line also gained a name in the markup, so a measurement can find the line itself rather than the translucent area under it or the dot on top of it.

Thirtieth-pass verification: the chart-mark spec now covers both charts in all three themes, six tests, all passing. Regression: 253 unit tests, the twelve sampled-contrast surfaces and the full accessibility suite, and the curve panels were read on screen in both themes.

Two unit tests had to change with it, and one of them is a lesson. A test that reads the plotted curves matched on the literal text `<path d="`, so naming the path moved the attribute along and the match found nothing — and the assertion it guards, that a gated input plots as flat, would have gone quiet rather than failed loudly, because zero panels trivially satisfies nothing. It now keys off the curve's own name. The other test pinned the old hues by value, which is exactly what it is for; it now pins the new ones and additionally asserts the retired steps are gone.

### Thirty-first pass: meters against their own tracks

The measurements so far have covered text, focus rings and chart marks. The remaining family is the meters: the small bars that show establishment, reach, genetic variety, the tolerances on each species card, the factor bars in Grow, the water level on a grove patch. The pair a reader has to separate there is the filled part against its own track, which needs 3:1 like any other meaningful graphic.

Most of them did not have it. The strategy bars on Spread measured 1.53:1 and 1.44:1 against their track, the trait meters on Compare 1.44:1, the grove water bars 2.54:1, and the factor bars in dark theme about 2.5:1. Worst was the high contrast theme, which drew a yellow fill on a white track at 1.07:1 — a bar that is not there at all, in the theme chosen by people who need contrast most.

Two changes fix the family. Meters now have a track colour of their own rather than borrowing the general border colour: near-white in the light theme, near-black in dark, and black in high contrast, where the yellow fill now reads at 19.6:1. And the fills that were still using bright hues take the darker step in the light theme, through the same map introduced for the season labels, which gained the two hues the meters use. Every measured meter now sits between 3.06:1 and 8.3:1 against its track.

Thirty-first-pass verification: a new spec hides each fill, captures the page, reads the track just past the filled end, and requires 3:1 — seven surfaces covering all three themes and four kinds of meter, all passing. Regression: 253 unit tests and 47 browser tests across the chart-mark, sampled-contrast, focus-ring and accessibility suites. The strategy cards were read on screen to confirm the bars still carry their meaning at a glance.

### Thirty-second pass: the reader's own text spacing

A reader is allowed to force their own spacing on any page: line height to one and a half, letter spacing to a little over a tenth of the font size, word spacing a little more, and a blank line between paragraphs. Nothing may be lost when they do. Applying exactly those overrides to each view and then looking for text that no longer fits its box found two places where it was.

The hint under every tab label was a single line that could not wrap, clipped with an ellipsis, so "Find the bottleneck" became "Find the bottlen…" as soon as the letters were spaced out. The chips in the knowledge check's story strip did the same. Both now wrap instead of truncating. At the default spacing nothing moves — the strip looks exactly as it did — and there is a second benefit worth naming: those strings are translated, and a longer word in another language was already being cut before this criterion came into it.

Thirty-second-pass verification: a new spec applies the four overrides to all seven views, confirms they actually took effect before drawing any conclusion, and requires that no leaf text element ends up with more content than its box can show and that the page never scrolls sideways. It also asserts nothing was clipped beforehand, so the check cannot pass by grandfathering an existing fault. Seven tests, all passing. Regression: 253 unit tests and 40 browser tests across the accessibility, grid, navigator and meter suites, and the tab strip was read on screen both at normal spacing and with the overrides applied.
