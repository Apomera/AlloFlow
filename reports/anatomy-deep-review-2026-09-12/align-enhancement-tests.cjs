const fs=require('node:fs');
function edit(file,changes){let s=fs.readFileSync(file,'utf8');for(const [a,b] of changes){if(!s.includes(a))throw Error(file+': missing '+a);s=s.split(a).join(b);}fs.writeFileSync(file,s);}
edit('stem_lab/stem_tool_anatomy.js',[["var anatomyCompactMode = anatomyStudyMode || activeTab === 'explore';","var anatomyCompactMode = anatomyStudyMode || activeTab === 'explore' || activeTab === 'homeoHunt';"]]);
fs.copyFileSync('stem_lab/stem_tool_anatomy.js','desktop/web-app/public/stem_lab/stem_tool_anatomy.js');
edit('tests/anatomy_science_enhancements.test.js',[["textContent).toContain(name);","textContent.toLowerCase()).toContain(name.toLowerCase());"]]);
edit('tests/anatomy_compact_study_controls.test.js',[["limits compact controls to Quiz and Cards","offers compact controls for Explore, Quiz, Cards, and Homeostasis"],["['quiz', 'flashcards', 'explore', 'tour', 'spotter']","['quiz', 'flashcards', 'explore', 'homeoHunt', 'tour', 'spotter']"],["['quiz', 'flashcards'].includes(_activeTab)","['quiz', 'flashcards', 'explore', 'homeoHunt'].includes(_activeTab)"]]);
edit('tests/anatomy_heading_structure.test.js',[["    // Two inline value labels still read \"Belongs to: <system>\" and \"1 - Body system: <system>\".\n    // Those are not section titles and must stay as <strong>.\n    expect(source.match(/h\\('strong', null, sys\\.icon/g)).toHaveLength(2);", "    // Inline relationship values remain strong text, with scientific membership separated from the navigation label.\n    expect(source).toContain(\"h('strong', null, scientificSystemNames(sel))\");\n    expect(source.match(/h\\('strong', null, sys\\.icon/g)).toHaveLength(1);"]]);
edit('tests/anatomy_lab_science.test.js',[["const heart = renderAnatomy({ system: 'circulatory', complexity: 3, quizMode: true, quizIdx: 3, _structureConfidence: {} });","const heart = renderAnatomy({ system: 'circulatory', complexity: 1, quizMode: true, quizIdx: 3, _structureConfidence: {} });"],["System-context links can involve this structure directly", "Links below describe the selected navigation collection."],["'Mastery map'","'Study progress'"]]);
edit('tests/anatomy_mobile_a11y_polish.test.js',[["/Mastery map/","/Study progress/"]]);
edit('tests/anatomy_list_text_and_arrows.test.js',[["rowText(render(filePath, {}, { gradeLevel: '2' }))","rowText(render(filePath, { complexity: 1 }, { gradeLevel: '2' }))"],["const young = render(filePath, state, { gradeLevel: '2' });","const young = render(filePath, { ...state, complexity: 1 }, { gradeLevel: '2' });"]]);
edit('tests/anatomy_myth_hear_contrast.test.js',[["const young = render(filePath, { _activeTab: 'explore', selectedStructure: 'heart' });","const young = render(filePath, { _activeTab: 'explore', selectedStructure: 'heart', complexity: 1 });"]]);
edit('tests/anatomy_next_ui_pass.test.js',[["keeps all modes in one horizontally scrollable row at every width", "keeps a desktop mode strip and a matching mobile activity selector"],["const root = parseMarkup(renderAnatomy(filePath));\n    const strip", "const root = parseMarkup(renderAnatomy(filePath, { complexity: 1 }));\n    const strip"],["    // Harness profile is '5th Grade': Quiz joins the strip, the two clinician-level workspaces\n    // (Imaging Lab, Procedure Studio) are hidden for a known K-5 profile.","    // An explicit K-5 choice hides the two advanced workspaces.\n    expect(root.querySelectorAll('#anatomy-mobile-activity option')).toHaveLength(9);"]]);
edit('tests/anatomy_view_model_refinement.test.js',[["const youngBlueprint = parseMarkup(renderAnatomy(filePath, {\n      _bodyView3d: true,", "const youngBlueprint = parseMarkup(renderAnatomy(filePath, {\n      complexity: 1, _bodyView3d: true,"]]);
edit('tests/anatomy_review_due_homeo_recap.test.js',[["return { _structureConfidence: nextConfidence, _confidenceAt: stampConfidence(structureId) };", "return { _structureConfidence: nextConfidence, _confidenceAt: stampConfidence(structureId), _retrievalEvidence: nextEvidence };"]]);
// These two old source contracts predate marker visibility and regional camera presets.
// Retain their checks while matching the existing, more restrictive conditions.
edit('tests/anatomy_3d_visual_refinement.test.js',[["var markerItems = canvas.getAttribute('data-anatomy-3d-style') === 'clinical' && importedModel && importedModel.visible", "var markerItems = canvas.getAttribute('data-anatomy-body-markers-visible') === 'false'"],["if (blueprintStageGroup) blueprintStageGroup.visible = resolvedStyle === 'blueprint';", "if (blueprintStageGroup) blueprintStageGroup.visible = resolvedStyle === 'blueprint' && (!currentCameraPreset || currentCameraPreset === 'body');"]]);
console.log('Updated superseded test expectations and compact Homeostasis layout.');
