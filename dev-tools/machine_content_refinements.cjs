const assert=require('assert/strict');
function refine(pack){
 const changes=[],get=id=>pack.history.find(r=>r.id===id);
 const edit=(id,path,to)=>{let o=get(id);assert(o,id);for(const k of path.slice(0,-1))o=o[k];const k=path.at(-1),from=o[k];if(JSON.stringify(from)===JSON.stringify(to))return;o[k]=to;changes.push({resourceId:id,path,from,to});};
 let r=get('sk-reading').data;
 r=r.replace('and everything mechanical you have ever touched is built from them.','and many everyday tools combine them.');
 r=r.replace('Push down on one end and the other end lifts.','In one common arrangement, pushing down on one side of the fulcrum lifts the other side.');
 r=r.replace('A small turn at the wide outer edge produces a strong twist at the narrow center.','A tangential push at the wide outer edge can produce a larger force at the narrow axle; both turn through the same angle, while the rim travels farther.');
 r=r.replace('One pulley changes the direction you pull — handy, because pulling down is easier than lifting up. Add more pulleys and you also reduce the force needed, though you must pull more rope.','A single fixed pulley changes pull direction. A movable pulley attached to a load can reduce the effort force. The ideal advantage depends on the rope segments supporting the moving load, not simply on the number of wheels.');
 r=r.replace('Now the deal, which is the same in every case: **you trade distance for force.** Use half the force, move twice as far. Use a quarter of the force, move four times as far. A machine never reduces the total work — it just spreads it out into a form your body can manage.','Here is the ideal trade when a machine multiplies force: **less effort force means more effort distance.** Half the force requires twice the distance; a quarter requires four times the distance. Some arrangements instead change direction or increase movement speed. Without friction, input work equals output work.');
 r=r.replace('And every real machine charges a small extra fee called **friction**, which turns a bit of your effort into heat.','Real machines also have **friction** and other losses, which convert some mechanical energy into thermal energy and other forms. These losses can be small or large.');
 r=r.replace('Not less work. Less force at any one moment, paid for with more distance — a trade that is almost always worth making.','A machine may reduce the required effort force, change its direction, or change speed. It cannot create energy. For the same useful task, losses make real input work greater than useful output work.');
 edit('sk-reading',['data'],r);
 edit('sk-glossary',['data',8,'def'],'Energy transferred when a force acts through a displacement. Along the motion, work equals force times distance.');
 edit('sk-glossary',['data',9,'def'],'A contact force that opposes relative sliding or its tendency; sliding friction converts mechanical energy into thermal energy.');
 edit('sk-anchor',['data','sections',3,'bullets',1],'Rim travels farther than the narrower axle surface');
 edit('sk-anchor',['data','sections',4,'bullets',1],'A fixed pulley changes pull direction');
 edit('sk-anchor',['data','sections',4,'bullets',2],'Supporting rope segments determine the ideal force advantage');
 edit('sk-anchor',['data','sections',5,'bullets'],['Ideal force gain trades for longer effort travel','Ideal input work equals output work','Friction adds to the required input work']);
 edit('sk-outline',['data','branches',1,'items',2],'Pulley: a grooved rope wheel; fixed and movable arrangements have different effects.');
 edit('sk-outline',['data','branches',2,'title'],'The ideal trade when multiplying force');
 edit('sk-outline',['data','branches',2,'items'],['Less effort force requires more effort distance','Half the force, twice the distance in an ideal machine','Some machines instead change direction or increase speed; none creates energy']);
 edit('sk-outline',['data','branches',3,'items'],['Friction can convert mechanical energy into thermal energy','Lubrication and bearings can reduce losses','Real losses are not always small']);
 edit('sk-memory',['data','sourceExcerpt'],'Six classic simple machines are the lever, inclined plane, wedge, screw, wheel and axle, and pulley. When an ideal machine multiplies force, it trades longer effort travel for smaller effort force. Friction increases real input work.');
 edit('sk-memory',['data','cards',1,'essentialFacts'],['When an ideal machine multiplies force, half the effort force requires twice the effort distance.','Ideal input work equals output work; a machine cannot create energy.','Real losses convert some mechanical energy into thermal energy and other forms, so input work exceeds useful output work.']);
 edit('sk-memory',['data','cards',1,'aiExample'],'For an ideal force-multiplying machine, imagine paying the same price in smaller installments over more payments. Real machines add a friction cost that can vary.');
 edit('sk-quiz',['data','questions',3,'question'],'An ideal machine lifts the same load using half the effort force. How far must the effort move compared with the load?');
 edit('sk-quiz',['data','questions',3,'options',0],'The effort must move twice as far as the load');
 edit('sk-quiz',['data','questions',3,'correctAnswer'],'The effort must move twice as far as the load');
 edit('sk-quiz',['data','questions',4,'options',2],'Converts some mechanical energy into thermal energy, increasing required input work');
 edit('sk-quiz',['data','questions',4,'correctAnswer'],'Converts some mechanical energy into thermal energy, increasing required input work');
 edit('sk-quiz',['data','questions',6,'expectedAnswer'],'For the same useful task, an ideal machine has equal input and output work. A force-multiplying arrangement reduces effort force by increasing effort distance. Other arrangements change direction or speed. Friction means real input work is greater than useful output work; energy is not destroyed.');
 edit('sk-faq',['data',2,'answer'],'An ideal model can be 100% efficient. Ordinary real machines have friction and other losses, so useful output work is less than input work. Bearings and lubrication reduce these losses; their size depends on the machine.');
 edit('sk-challenge',['title'],'Design Challenge: Lift the Water Jug — Tabletop Model');
 edit('sk-challenge',['data','title'],'Lift the Water Jug — Tabletop Model');
 edit('sk-challenge',['data','instructions'],'Design and test a small tabletop model of a machine that could help lift a water jug. Use a sealed container with a total mass no greater than 250 grams as the model load. Raise it no more than 10 centimeters above a tray, with teacher approval. Do not lift a full water-cooler jug.');
 edit('sk-challenge',['data','brief','context'],'A water cooler needs replacement jugs, but a full jug is too heavy for this classroom experiment. Model the problem using a sealed container no heavier than 250 grams, a short ruler or craft plank, blocks, string, small classroom pulleys and a spring scale marked in newtons. Keep the setup on a tray and the rise at 10 centimeters or less.');
 edit('sk-challenge',['data','brief','drivingQuestion'],'Which approved tabletop machine lets us lift the same model load with less measured force, and how much farther does the effort travel?');
 edit('sk-challenge',['data','brief','lockedLessonFacts'],['A lever turns about a fulcrum; its arm lengths affect its force and distance trade.','A longer frictionless ramp reduces force along the ramp for the same rise.','A fixed pulley changes direction; a movable pulley can multiply force through supporting rope segments.','In an ideal machine, input work equals output work.','Real losses convert mechanical energy to other forms, increasing the required input.']);
 edit('sk-challenge',['data','brief','openQuestions'],['How does ramp length affect pull for the same height?','How do effort-arm and load-arm lengths compare?','How many rope segments support the moving load?','What difference between predicted and measured pull might friction explain?']);
 edit('sk-challenge',['data','brief','stakeholders'],['The student measuring force','Classmates observing from clear of the model','The teacher approving the setup','The team storing the materials']);
 edit('sk-challenge',['data','brief','criteria'],['The same sealed model load rises the same small height in each comparison','Measure pull in newtons within the scale range and record effort travel and load rise','Predict the trade before testing, then compare prediction with evidence','Name the machine parts and explain where friction may affect the result']);
 edit('sk-challenge',['data','brief','constraints'],['Total model-load mass is no more than 250 grams','Raise the model no more than 10 centimeters above a tray','Teacher must check supports, attachments and scale range before testing','Keep fingers clear; use no sharp tools and no full-size jug','Change only one feature between comparison trials']);
 edit('sk-challenge',['data','supports','parallelExample'],{context:'A team needed to raise a small sealed model container onto a block.',move:'They compared two ramps that reached the same 8-centimeter height. They pulled the same model steadily with a newton scale, recorded force and travel distance, and repeated each test. The longer ramp was predicted to need less pull; friction could change the measured values.',whyItHelps:'The load and rise stayed constant while ramp length changed, making the comparison useful.'});
 edit('sk-challenge',['data','supports','frameChoices'],['A longer ramp reaching the same small platform','A lever with a longer effort arm than load arm','A teacher-approved movable pulley supported by two rope segments','A ramp with a fixed pulley to change pull direction']);
 edit('sk-challenge',['data','sourceExcerpt'],'An ideal force-multiplying machine trades longer effort travel for smaller effort force. Input work equals output work in the ideal model; real friction increases required input work.');
 return changes;
}
module.exports={refine};
