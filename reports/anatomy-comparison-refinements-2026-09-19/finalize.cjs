const fs=require('fs');const result=JSON.parse(fs.readFileSync(__dirname+'/verification.json','utf8'));let readme=fs.readFileSync(__dirname+'/README.md','utf8');readme=readme.replace('Validation results will be recorded in `verification.json`.',`**${result.regression.tests} targeted regression checks pass across ${result.regression.files} files.** After the final translation and mixed-direction-text polish, all **${result.finalPolish.tests} comparison-specific checks** pass again. These are overlapping checks, not additional unique tests. The full anatomy suite was not rerun in this pass. Results and the final source hash are recorded in \`verification.json\`.`);readme=readme.replace('Accessibility scans are scoped to the comparison panel and tray; they do not certify the whole app.',`All **${result.browser.axeScans} scoped accessibility scans have zero violations and zero incomplete checks**. The browser run has no page errors and retains ${result.browser.screenshots} screenshots. Phone, dark-mode, and Arabic captures were visually reviewed. Accessibility scans are scoped to the comparison panel and tray; they do not certify the whole app. A narrow-screen caption wrapping defect found during visual review was fixed and given a geometry regression check.`);readme=readme.replace('No commit or deployment was performed.','Some legacy structure descriptions, clinical paragraphs, and source titles still use English in translated interfaces. This pass translates the comparison instructions and controls and improves mixed-direction text; it does not translate the entire anatomy catalog.\n\nNo commit or deployment was performed.');fs.writeFileSync(__dirname+'/README.md',readme);console.log('Finalized comparison refinement report.');
