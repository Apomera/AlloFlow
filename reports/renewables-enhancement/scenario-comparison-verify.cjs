const fs=require('node:fs'),assert=require('node:assert/strict'),crypto=require('node:crypto'),vm=require('node:vm');
(async()=>{
 const dir='reports/renewables-enhancement/',source=fs.readFileSync('stem_lab/stem_tool_renewables.js'),mirror=fs.readFileSync('desktop/web-app/public/stem_lab/stem_tool_renewables.js');assert.ok(source.equals(mirror));new vm.Script(source.toString());const sha=crypto.createHash('sha256').update(source).digest('hex');
 const browser=JSON.parse(fs.readFileSync(dir+'scenario-comparison-browser-results.json','utf8')),tests=JSON.parse(fs.readFileSync(dir+'scenario-comparison-vitest-results.json','utf8'));assert.equal(browser.sourceSha256,sha);assert.equal(browser.scenarios.length,18);assert.equal(browser.behavior.length,12);assert.equal(Object.keys(browser.audits).length,9);for(const [label,violations] of Object.entries(browser.audits))assert.deepEqual(violations,[],label);assert.deepEqual(browser.pageErrors,[]);assert.deepEqual(browser.consoleErrors,[]);assert.equal(tests.success,true);assert.equal(tests.numPassedTests,133);assert.equal(tests.numFailedTests,0);assert.equal(tests.testResults.length,5);
 const response=await fetch('http://127.0.0.1:8790/tool.js',{signal:AbortSignal.timeout(60000)});assert.equal(response.status,200);assert.ok(source.equals(Buffer.from(await response.arrayBuffer())));
 const result={sourceSha256:sha,sourceAndDesktopIdentical:true,previewIdentical:true,syntaxValid:true,regressionTests:133,newTests:18,testFiles:5,scenarios:18,technologies:9,browserBehaviors:12,accessibilityAudits:9,pageErrors:[],consoleErrors:[],viewportWidths:[1280,390,320],scope:'Isolated local React/Three.js browser host and running preview source; no deployment.'};fs.writeFileSync(dir+'scenario-comparison-verification.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));
})().catch(error=>{console.error(error);process.exitCode=1;});
