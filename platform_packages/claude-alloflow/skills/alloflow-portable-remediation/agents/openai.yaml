interface:
  display_name: "AlloFlow Portable Remediation"
  short_description: "Remediate PDFs locally with no extra account"
  default_prompt: "Use $alloflow-portable-remediation to remediate the attached PDF without a paid Worker, institution account, or separate API key, and return the repaired copy and report."
policy:
  allow_implicit_invocation: true
