const fs=require('fs');for(const file of process.argv.slice(2)){const r=JSON.parse(fs.readFileSync(file));console.log(JSON.stringify({file,passed:r.numPassedTests,failed:r.numFailedTests,pending:r.numPendingTests,failures:r.testResults.flatMap(t=>(t.assertionResults||[]).filter(a=>a.status==='failed').map(a=>({name:a.fullName,message:a.failureMessages.join('\n').slice(0,1800)})))},null,2));}
