exports.refine=function(p){const changes=[];function edit(id,path,to){let o=p.history.find(r=>r.id===id);const ks=path.split('.');for(const k of ks.slice(0,-1))o=o[k];const k=ks.at(-1),from=o[k];if(from!==to){o[k]=to;changes.push({resourceId:id,path,from,to});}}const get=id=>p.history.find(r=>r.id===id);
let r=get('vl-reading').data;
r=r.replace('We measure it by counting how many **unit** cubes fit inside, with no gaps and no overlaps.','For solids built from cubes, we measure it by counting how many **unit** cubes fit inside, with no gaps and no overlaps. Other shapes may require parts of cubes or other methods.');
r=r.replace('You just found the **area** of the base, and it is the number of cubes in one **layer**.','The **area** of the base is 12 square units. A **layer** one unit high above that base contains 12 unit cubes and has volume 12 cubic units.');
r=r.replace('whatever shape the base is, as long as the shape does not change as you go up.','for a prism or cylinder. Height means the perpendicular distance between the parallel bases.');
r=r.replace('What about an L-shaped solid, or a house-shaped one? Cut it into rectangular **prisms**, find each volume, and add them.','What about an L-shaped solid made by joining rectangular **prisms**? Split it into non-overlapping rectangular prisms, find each volume, and add them. Not every solid can be split into rectangular prisms; a sloping roof, for example, may need a different method.');
edit('vl-reading','data',r);
edit('vl-glossary','data.3.def','A solid with two matching, parallel polygon bases joined by parallelogram faces.');
edit('vl-glossary','data.7.def','The perpendicular distance between the parallel bases of a prism.');
edit('vl-anchor','data.sections.0.bullets.0','For a cube-built solid, fill it with unit cubes, with no gaps or overlaps');
edit('vl-anchor','data.sections.0.bullets.2','Count hidden cubes too; visible square faces are not extra cubes');
edit('vl-anchor','data.sections.1.bullets.0','A base of 12 square units supports 12 unit cubes in a layer one unit high');
edit('vl-anchor','data.sections.1.iconPrompt','Five identical cube layers laid out separately so every top grid is visible');
edit('vl-anchor','data.sections.3.bullets.0','For a solid built from rectangular prisms, split it into non-overlapping blocks');
edit('vl-sort','data.items.4.content','The area of one wall to be painted');
edit('vl-sort','data.items.5.content','The area of glass needed for one window pane');
edit('vl-sort','data.items.7.content','The length of one ribbon loop around the four sides of a box, without a bow');
edit('vl-faq','data.1.answer','For a pyramid, base area times perpendicular height is too large because parallel cross-sections shrink toward the apex. A pyramid has one third the volume of a prism with the same base area and perpendicular height. It is not one third of just any surrounding box.');
edit('vl-faq','data.3.answer','For a rectangular prism, multiplying three lengths multiplies their units too: cm × cm × cm = cubic cm, written cm³. A unit cube has one of those length units on each edge.');
edit('vl-faq','data.5.answer','Convert all lengths to the same unit before using the formula, so the result is easy to express in cubic units. Mixed units still have meaning, but need conversion: 1 cm × 1 m = 100 square cm.');
edit('vl-math','data.problems.1.steps.0.explanation','The base area is 15 square inches. A slice one inch thick has volume 15 cubic inches, even when the base cannot be tiled by whole one-inch cubes.');
edit('vl-challenge','data.brief.context','A school club ships 48 unit cubes of soap. Compare ideal closed rectangular boxes with internal volume 48 cubic units. Use total surface area as the cardboard model, ignoring wall thickness, tabs, seams and cutting waste.');
edit('vl-challenge','data.brief.constraints.0','Positive whole-number edge lengths only; rotations of the same three lengths count as one box');
edit('vl-challenge','data.brief.constraints.1','Exactly 48 cubic units inside; compare all six faces of each closed box, ignoring thickness, tabs, seams and waste');
edit('vl-memory','data.cards.0.essentialFacts.1','For a layer one unit high, the cube count has the same numerical value as the base area in square units; the units differ.');
edit('vl-memory','data.cards.0.aiExample','Picture a rectangular stack of identical slices, each one unit thick. Find the volume of one slice, then multiply by the number of slices. If thickness changes, slice count alone no longer gives height in those units.');
edit('vl-memory','data.cards.0.mapping','The stack is an ideal model with matching layers and no gaps. A real loaf may have differently shaped ends or uneven slices. Layer thickness must be included when finding height.');
edit('vl-memory','data.cards.0.scaffoldStarter','One layer has volume ____. There are ____ matching layers, so total volume is ____.');
return changes;};

