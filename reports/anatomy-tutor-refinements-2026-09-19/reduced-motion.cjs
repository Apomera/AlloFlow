const fs=require('fs'),file='stem_lab/stem_tool_anatomy.js';let s=fs.readFileSync(file,'utf8');const from='.anatomy-tutor-panel :focus-visible{outline:3px solid #0891b2;outline-offset:3px}';if(!s.includes(from))throw Error('Missing tutor style');s=s.replace(from,from+'@media(prefers-reduced-motion:reduce){.anatomy-tutor-panel,.anatomy-tutor-panel *{transition:none!important;animation:none!important}}');fs.writeFileSync(file,s);fs.writeFileSync('desktop/web-app/public/'+file,s);
const readme=__dirname+'/README.md';s=fs.readFileSync(readme,'utf8').replace('Text, controls, and wrapping support narrow screens, dark mode, and right-to-left layout.','Text, controls, and wrapping support narrow screens, dark mode, and right-to-left layout. Reduced-motion mode disables tutor transitions, including theme changes.');fs.writeFileSync(readme,s);
