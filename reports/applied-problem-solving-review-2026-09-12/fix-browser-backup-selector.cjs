const fs=require('fs');const p=__dirname+'/pass5-browser.cjs';let s=fs.readFileSync(p,'utf8');s=s.replace("name:'Download a backup of my writing'","name:'Download my work'");s=s.replace("await page.locator('input[type=file]').setInputFiles(backup);","await page.locator('input[type=file][accept=\".json,application/json\"]').setInputFiles(backup);await page.getByText('Backup restored to this workspace. Check the save status before leaving.',{exact:true}).waitFor();");fs.writeFileSync(p,s);
