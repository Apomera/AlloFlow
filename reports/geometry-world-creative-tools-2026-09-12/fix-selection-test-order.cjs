const fs=require('fs'),p='tests/geometry_world_selection_editing.test.js';let s=fs.readFileSync(p,'utf8');const a='plan.additions.slice().sort((a,b)=>a.x-b.x||a.y-b.y||a.z-b.z)';if(!s.includes(a))throw Error('Missing test order anchor');s=s.replace(a,'plan.additions.slice().sort((a,b)=>a.y-b.y||a.x-b.x||a.z-b.z)');const f=fs.openSync(p,'r+');try{fs.writeFileSync(f,s);fs.ftruncateSync(f,Buffer.byteLength(s));}finally{fs.closeSync(f);}
