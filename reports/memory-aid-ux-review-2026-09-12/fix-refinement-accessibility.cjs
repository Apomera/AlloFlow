const fs=require('fs'),p='memory_aid_source.jsx';let s=fs.readFileSync(p,'utf8');const old="aria-label={!isTeacherMode ? tr('facts_student_aria', 'Facts to remember') : card.factVerified ? (card.factReviewedAt ? tr('facts_reviewed_by_you', 'Reviewed by you') : tr('facts_ready', 'Ready to study')) : tr('facts_pending', 'Facts awaiting teacher review')}";if(!s.includes(old))throw Error('Facts label missing');s=s.replace(old,"aria-label={tr('facts_region_aria', 'Facts for target {n}: {target}', { n: index + 1, target: card.target })}");fs.writeFileSync(p,s);
