import {BoardMap} from './lesson_board_map.jsx';
import {PlanningSandbox} from './lesson_board_sandbox_ui.jsx';
import {CompletionSteps,LearningShare} from './lesson_board_sharing_ui.jsx';
import {GrowingWorld} from './lesson_board_world_ui.jsx';
import {BoardFinale} from './lesson_board_finale_ui.jsx';
import {visualActivity} from './lesson_board_visual.js';
import {VisualCue,VisualChoices,VisualStyles} from './lesson_board_visual_ui.jsx';
import {FirstBuildGuide,GuidedTools} from './lesson_board_guide_ui.jsx';
import { prepareSupport } from './lesson_board_support.js';
import { SupportStyles, SupportImage, WorldArtwork, VocabularyCards } from './lesson_board_support_ui.jsx';
import { soloWorkspace } from './lesson_board_storage.js';
import { projectPlan } from './lesson_board_strategy.js';
import { ClassroomRoles, ReportActions } from './lesson_board_followthrough.jsx';
import * as engine from './lesson_board_engine.js';
import { MovePreview, ClassProposals, TeacherLearningReview } from './lesson_board_review.jsx';
import { Amounts, MissionDashboard, RoutePlanner, LearningJournal, MoveRecap } from './lesson_board_play_extras.jsx';
import { moveDetails, responseText } from './lesson_board_insights.js';
import { tr } from './lesson_board_strings.js';
export { tr };
const React = window.React;
const { useState, useEffect, useRef } = React;
export function Styles() { return <style>{`.lb{--bg:#f6f7fc;--panel:#fff;--ink:#20243d;--muted:#535d75;--line:#778198;--accent:#5036ab;--soft:#eeebfc;color:var(--ink);background:var(--bg);font:400 1rem/1.55 system-ui,sans-serif;overflow-wrap:anywhere}.dark .lb{--bg:#171b29;--panel:#232a3c;--ink:#f4f5fb;--muted:#c3cbe0;--line:#8c99b5;--accent:#c6b9ff;--soft:#393250}.lb *{box-sizing:border-box}.lb h2{font-size:1.5em;margin:0 0 10px}.lb h3{font-size:1.12em;margin:0 0 10px}.lb h4{font-size:1em;margin:12px 0 6px}.lb p{margin:8px 0 14px}.lb button,.lb input,.lb textarea,.lb select{font:inherit;color:var(--ink);background:var(--panel);border:1px solid var(--line);border-radius:9px;padding:9px 12px;min-height:44px;min-width:0}.lb button{cursor:pointer;min-width:44px}.lb button:hover:enabled,.lb button[aria-pressed=true]{background:var(--soft);border-color:var(--accent)}.lb button:disabled,.lb button[aria-disabled=true]{opacity:.65;cursor:default}.lb :focus-visible{outline:3px solid var(--accent);outline-offset:3px}.lb button.lb-primary:hover:enabled{background:var(--accent);color:var(--panel)}.dark .lb button.lb-primary:hover:enabled{color:#171b29}.lb .lb-primary{background:var(--accent);color:var(--panel)}.dark .lb .lb-primary{color:#171b29}.lb input,.lb textarea,.lb select{width:100%}.lb textarea{min-height:90px}.lb label{display:block;margin:8px 0}.lb fieldset{border:0;margin:0;padding:0;min-width:0}.lb legend{font-weight:650;margin-bottom:8px}.lb small,.lb .lb-muted{font-size:.88em;color:var(--muted)}.lb .lb-row{display:flex;align-items:center;flex-wrap:wrap;gap:10px}.lb .lb-between{justify-content:space-between}.lb .lb-panel{padding:18px;background:var(--panel);border:1px solid var(--line);border-radius:14px;min-width:0}.lb .lb-notice{padding:12px;background:var(--soft);border:1px solid var(--line);border-radius:10px;margin:12px 0}.lb .lb-columns{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(0,1fr);gap:18px;align-items:start;margin-top:16px}.lb .lb-board{position:relative;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:20px 14px;margin:16px 0}.lb .lb-paths{position:absolute;inset:0;width:100%;height:100%;pointer-events:none}.lb .lb-paths line{stroke:var(--line);stroke-width:2}.lb .lb-tile{position:relative;z-index:1;min-height:115px;text-align:left;display:flex;flex-direction:column;align-items:flex-start;gap:5px}.lb .lb-tile small{display:block}.lb .lb-tile[data-built=true]{border:2px solid var(--accent)}.lb .lb-icon{width:30px;height:30px;fill:none;stroke:var(--accent);stroke-width:1.7;stroke-linecap:round;stroke-linejoin:round;flex-shrink:0}.lb .lb-board[data-view=list]{grid-template-columns:1fr;gap:8px}.lb .lb-board[data-view=list] .lb-tile{min-height:65px;display:block}.lb .lb-board[data-view=list] .lb-icon{float:left;margin-right:10px}.lb .lb-board[data-view=list] .lb-paths{display:none}.lb .lb-projects{display:grid;gap:10px}.lb .lb-projects button{text-align:left}.lb .lb-projects small{display:block}.lb details{border-top:1px solid var(--line);padding-top:9px;margin-top:14px}.lb summary{cursor:pointer;min-height:44px;padding:8px 0;font-weight:650}.lb .lb-order{list-style:none;padding:0}.lb .lb-order li{display:flex;gap:6px;align-items:center;margin:8px 0}.lb .lb-order span{flex:1;min-width:0}.lb .lb-form{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(180px,100%),1fr));gap:12px}.lb blockquote{border-left:3px solid var(--line);margin:12px 0;padding-left:12px;white-space:pre-wrap}.lb [tabindex],.lb button,.lb summary{scroll-margin:18px}.lb-overlay{position:fixed;inset:0;z-index:9999;overflow:auto}.lb-shell{max-width:1150px;padding:22px;margin:auto}.lb-backdrop{position:fixed;inset:0;z-index:10000;overflow:auto;background:#101528bb;padding:24px 12px;display:flex;justify-content:center;align-items:flex-start}.lb-dialog{width:min(1080px,100%);border-radius:16px;padding:24px}.lb .lb-visually-hidden{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap}.lb[data-theme=garden] .lb-tile{border-radius:18px 9px}.lb[data-theme=river] .lb-tile{border-radius:9px 18px}.lb[data-theme=space] .lb-tile{border-radius:22px}.lb .lb-progress{height:8px;background:var(--line);margin:12px 0;border-radius:5px;overflow:hidden}.lb .lb-progress span{display:block;height:100%;background:var(--accent)}@media(max-width:700px){.lb .lb-columns{grid-template-columns:1fr}.lb .lb-current{grid-row:1}.lb .lb-board{grid-template-columns:repeat(2,minmax(0,1fr))}.lb-dialog,.lb-shell{padding:15px}.lb-backdrop{padding:10px 5px}.lb .lb-panel{padding:14px}.lb input,.lb select,.lb textarea{font-size:max(1rem,16px)}}@media(forced-colors:active){.lb .lb-paths line,.lb .lb-icon{stroke:CanvasText}.lb .lb-tile[data-built=true]{border:3px solid Highlight}.lb :focus-visible{outline:3px solid Highlight}.lb .lb-progress span{background:Highlight}}
.lb{--route:#32725e;--route-soft:#e5f2ea;--gold:#80551d;--gold-soft:#fbefd9}.dark .lb{--route:#a0dfc2;--route-soft:#243d35;--gold:#efd3a1;--gold-soft:#473a25}.lb .lb-expedition-header{border-bottom:1px solid var(--line);padding-bottom:18px}.lb .lb-eyebrow{font-size:.8em;font-weight:750;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);margin:0 0 5px}.lb .lb-dashboard{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1.15fr);gap:14px;margin-top:18px}.lb .lb-supplies{display:grid;gap:10px}.lb .lb-supply{display:flex;align-items:center;gap:12px;padding:14px;border:1px solid var(--line);border-radius:14px;background:var(--panel)}.lb .lb-supply small{display:block}.lb .lb-supply-symbol{font-size:1.5em;color:var(--gold);border:2px solid currentColor;border-radius:50%;width:48px;min-width:48px;height:48px;display:grid;place-items:center;background:var(--gold-soft);box-shadow:0 3px 0 var(--gold)}.lb .lb-mission{border:1px solid var(--line);border-radius:14px;padding:14px;background:var(--panel)}.lb .lb-goals{display:grid;gap:7px}.lb .lb-goal{display:grid;grid-template-columns:1fr auto;gap:3px 12px}.lb .lb-goal[data-done=true]{color:var(--route)}.lb .lb-goal .lb-progress{grid-column:1/-1;margin:2px 0 4px;height:6px}.lb .lb-phase-strip{list-style:none;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;padding:0;margin:18px 0}.lb .lb-phase-strip li{display:flex;align-items:center;gap:9px;border:1px solid var(--line);border-radius:12px;background:var(--panel);padding:10px;font-size:.9em}.lb .lb-phase-strip li>span{display:grid;place-items:center;border:1px solid var(--line);border-radius:50%;width:28px;min-width:28px;height:28px}.lb .lb-phase-strip [aria-current=step]{border:2px solid var(--accent);background:var(--soft);font-weight:700}.lb .lb-phase-strip [aria-current=step]>span{background:var(--accent);color:var(--panel)}.dark .lb .lb-phase-strip [aria-current=step]>span{color:#171b29}.lb .lb-tabletop{padding:16px;border:2px solid var(--line);border-radius:20px;background-color:var(--soft);background-image:radial-gradient(var(--line) .65px,transparent .65px);background-size:18px 18px;box-shadow:0 5px 0 var(--line)}.lb .lb-tabletop[data-theme=river],.lb .lb-tabletop[data-theme=garden]{background-color:var(--route-soft)}.lb .lb-tabletop[data-theme=workshop],.lb .lb-tabletop[data-theme=archive]{background-color:var(--gold-soft)}.lb .lb-tabletop h3{margin:0}.lb .lb-tabletop>.lb-muted{background:var(--panel);padding:8px;border-radius:8px;margin-top:12px}.lb .lb-map-legend{display:flex;flex-wrap:wrap;gap:6px 14px;padding:8px 10px;background:var(--panel);border-radius:10px;font-size:.82em}.lb .lb-map-legend b{color:var(--accent);font-size:1.2em}.lb .lb-board{gap:32px 18px;margin:20px 0}.lb .lb-paths line{stroke:var(--line);stroke-width:2.5;stroke-dasharray:5 5}.lb .lb-paths line[data-open=true]{stroke:var(--route);stroke-width:4;stroke-dasharray:none}.lb .lb-paths line[data-selected=true]{stroke:var(--accent);stroke-width:5}.lb .lb-tile{background:var(--panel);border:2px dashed var(--line);border-radius:13px;min-height:160px;padding:11px;box-shadow:0 3px 0 var(--line);font-size:.9em;gap:7px}.lb .lb-tile[data-state=ready]{border:2px solid var(--route)}.lb .lb-tile[data-state=current]{border:3px solid var(--accent);box-shadow:0 4px 0 var(--accent)}.lb .lb-tile[data-state=explored]{border:2px solid var(--route);background:var(--route-soft)}.lb .lb-tile[aria-pressed=true]{outline:2px solid var(--accent);outline-offset:3px}.lb .lb-tile-top{width:100%;gap:5px}.lb .lb-tile-top .lb-icon{margin-left:auto;width:25px;height:25px}.lb .lb-map-number{display:grid;place-items:center;width:24px;min-width:24px;height:24px;border-radius:50%;background:var(--soft);color:var(--accent);font-size:.85em;font-weight:750}.lb .lb-pawn{font-size:1.35em;color:var(--accent);line-height:1}.lb .lb-map-state{font-size:.83em;font-weight:650;border-top:1px solid var(--line);padding-top:5px;width:100%;margin-top:auto}.lb .lb-board[data-view=list] .lb-tile{display:grid;grid-template-columns:auto minmax(0,1fr);gap:4px 12px;min-height:80px}.lb .lb-board[data-view=list] .lb-tile-top{grid-row:1/4;width:auto;align-self:center;display:flex;flex-direction:column}.lb .lb-board[data-view=list] .lb-tile-top .lb-icon{margin:0}.lb .lb-board[data-view=list] .lb-map-state{border:0;padding:0}.lb .lb-board[data-view=list] .lb-tile>small{grid-column:2}.lb .lb-project-heading{margin-top:28px}.lb .lb-project-card{padding:14px;border:2px solid var(--line);box-shadow:0 3px 0 var(--line);display:flex;flex-direction:column;gap:9px}.lb .lb-project-card[data-affordable=true]{border-color:var(--gold);box-shadow:0 3px 0 var(--gold)}.lb .lb-project-card[data-built=true]{background:var(--route-soft)}.lb .lb-project-state{font-weight:650;color:var(--gold);font-size:.85em}.lb .lb-project-card[data-built=true] .lb-project-state{color:var(--route)}.lb .lb-tokens{display:flex;flex-wrap:wrap;gap:6px}.lb .lb-token{display:inline-flex;align-items:baseline;gap:5px;background:var(--gold-soft);color:var(--gold);border:1px solid var(--gold);border-radius:20px;padding:3px 9px;font-size:.83em}.lb .lb-token>span:last-child{overflow-wrap:anywhere}.lb .lb-plan-marker{display:block;border:1px dashed var(--accent);border-radius:6px;padding:3px 7px;background:var(--soft);color:var(--accent);font-weight:650}.lb .lb-ready-choices{border-block:1px solid var(--line);padding:8px 0 14px;margin:14px 0}.lb .lb-small-choice{font-size:.83em;padding:6px 9px}.lb .lb-instruction{padding:14px;border-left:4px solid var(--accent);background:var(--soft);border-radius:0 10px 10px 0;font-weight:600}.lb .lb-current>h3{font-size:1.3em}.lb .lb-current{border-top:5px solid var(--accent)}.lb .lb-current fieldset{padding:12px;border:1px solid var(--line);border-radius:12px;margin-bottom:14px}.lb .lb-order li{background:var(--soft);border-radius:9px;padding:8px}.lb .lb-answer-review,.lb .lb-move-rewards{border:1px solid var(--line);border-radius:12px;padding:12px;margin:14px 0}.lb .lb-move-rewards{background:var(--gold-soft)}.lb .lb-answer-review{background:var(--soft)}.lb .lb-answer-review p:last-child{margin-bottom:0}.lb .lb-planner{padding:12px 14px;background:var(--panel);border:1px solid var(--line);border-radius:14px}.lb .lb-planner>summary{padding:0}.lb .lb-planning-goal{border-left:3px solid var(--gold);padding:10px;background:var(--gold-soft);margin:12px 0}.lb .lb-planning-goal>button{margin-top:12px}.lb .lb-plan-options{display:grid;gap:8px}.lb .lb-plan-options>button{display:flex;flex-direction:column;text-align:left;gap:6px}.lb .lb-route-list{padding-left:20px}.lb .lb-route-list li{margin:12px 0}.lb .lb-route-list small{display:block;margin-top:5px}.lb .lb-concept-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(180px,100%),1fr));gap:10px}.lb .lb-concept-list>div{display:grid;grid-template-columns:auto 1fr;gap:3px 8px;background:var(--soft);padding:12px;border-radius:10px}.lb .lb-concept-list small{grid-column:2}.lb .lb-timeline{padding-left:24px}.lb .lb-timeline li{padding:8px 12px;border-left:2px solid var(--accent)}.lb .lb-timeline small{display:block}.lb .lb-practice-activity{border:1px solid var(--line);border-radius:12px;padding:14px}.lb .lb-practice-activity>button{margin-top:14px}.lb [data-board-journal]{margin-top:24px;background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:14px}.lb [data-board-journal]>summary{font-size:1.1em}.lb [data-board-practice]{border:2px solid var(--accent);padding:12px;border-radius:12px}@media(max-width:900px){.lb .lb-board{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:700px){.lb .lb-dashboard{grid-template-columns:1fr}.lb .lb-supplies{grid-template-columns:repeat(2,minmax(0,1fr))}.lb .lb-supply{padding:10px;align-items:flex-start;gap:8px;flex-direction:column}.lb .lb-supply-symbol{width:36px;min-width:36px;height:36px;font-size:1.1em}.lb .lb-phase-strip li{display:block;padding:8px;font-size:.8em}.lb .lb-phase-strip li>span{margin-bottom:5px}.lb .lb-tabletop{padding:12px}.lb .lb-tile{min-height:155px;padding:9px}.lb .lb-board{gap:26px 12px}.lb .lb-phase-strip{gap:5px}.lb .lb-current{margin-top:3px}}@media(forced-colors:active){.lb .lb-tile[data-state=current]{outline:3px solid Highlight}.lb .lb-tile[data-state=ready]{border-style:solid}.lb .lb-tile[data-state=locked]{border-style:dashed}.lb .lb-token,.lb .lb-supply-symbol{border-color:CanvasText}.lb .lb-paths line[data-open=true]{stroke:Highlight}.lb .lb-tabletop{background-image:none}}
.lb .lb-workshop{margin-top:24px}.lb .lb-workshop .lb-project-heading{margin-top:0}.lb .lb-workshop .lb-projects{grid-template-columns:repeat(3,minmax(0,1fr));align-items:stretch}.lb .lb-workshop .lb-plan-options{grid-template-columns:repeat(3,minmax(0,1fr))}.lb .lb-workshop .lb-route-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0 24px}.lb .lb-workshop .lb-planning-goal{max-width:620px}@media(max-width:700px){.lb .lb-workshop .lb-projects,.lb .lb-workshop .lb-plan-options,.lb .lb-workshop .lb-route-list{grid-template-columns:1fr}}@media(forced-colors:active){.lb{--bg:Canvas!important;--panel:Canvas!important;--ink:CanvasText!important;--muted:CanvasText!important;--line:CanvasText!important;--accent:LinkText!important;--soft:Canvas!important;--route:LinkText!important;--route-soft:Canvas!important;--gold:CanvasText!important;--gold-soft:Canvas!important}.lb .lb-primary,.dark .lb .lb-primary,.lb button.lb-primary:hover:enabled,.dark .lb button.lb-primary:hover:enabled{background:ButtonFace;color:ButtonText}.lb .lb-phase-strip [aria-current=step]>span,.dark .lb .lb-phase-strip [aria-current=step]>span{background:Highlight;color:HighlightText}}`}</style>; }
export function Icon({ name }) {
  const paths = { leaf: 'M5 25C0 7 17 3 27 3C27 15 23 29 5 25ZM5 25L21 9M12 18V11M12 18H20', water: 'M16 3C12 9 5 15 5 21A11 11 0 0 0 27 21C27 15 20 9 16 3ZM10 22Q11 26 16 26', book: 'M16 7Q9 2 3 6V26Q9 22 16 27Q23 22 29 26V6Q23 2 16 7V27', gear: 'M4 7H28V26H4ZM10 7V3M22 7V3M4 14H28M10 20H15M21 20H24', star: 'M16 3L20 12L30 13L22 20L24 29L16 24L8 29L10 20L2 13L12 12Z', home: 'M2 15L16 3L30 15M6 12V29H26V12M13 29V20H20V29', bridge: 'M2 26V15Q16 0 30 15V26M2 19H30M7 11V19M16 8V19M25 11V19', flask: 'M12 3H20M13 3V13L5 26Q4 29 8 29H24Q28 29 27 26L19 13V3M9 21H23M13 25H15' };
  return <svg className="lb-icon" viewBox="0 0 32 32" aria-hidden="true"><path d={paths[name] || paths.book}/></svg>;
}
export function Activity({ node, value, onChange, disabled, support, showImages=true, t }) {
  const [movement, setMovement] = useState(''),activity=visualActivity(support,node),cue=id=>activity?.cues?.[id];
  const label=(text,id)=>cue(id)?text+'. '+cue(id).description:text;
  const prompt=<VisualCue cue={cue('prompt')} support={support} showImages={showImages} t={t}/>;
  if (node.kind === 'choice') return <><VisualStyles/>{prompt}<label>{tr(t, 'response', 'Your response')}<select data-board-choice value={value} disabled={disabled} onChange={event => onChange(event.target.value)}><option value="">{tr(t, 'select_response', 'Choose a response')}</option>{node.options.map((option, i) => <option key={i} value={String(i)}>{label(option,'option'+i)}</option>)}</select></label><VisualChoices options={node.options} prefix="option" activity={activity} support={support} showImages={showImages} value={value} onChange={onChange} disabled={disabled} t={t}/></>;
  if (node.kind === 'settings') return <><VisualStyles/>{prompt}<div>{node.controls.map((control, index) => {const update=next=>{const values=value.split(',');values[index]=next;onChange(values.join(','));};return <div key={index}><label>{control.label}<select data-board-control={index} value={value.split(',')[index] || ''} disabled={disabled} onChange={event => update(event.target.value)}><option value="">{tr(t, 'select_setting', 'Choose a setting')}</option>{control.options.map((option, i) => <option key={i} value={String(i)}>{label(option,'setting'+index+'_'+i)}</option>)}</select></label><VisualChoices options={control.options} prefix={'setting'+index+'_'} activity={activity} support={support} showImages={showImages} value={value.split(',')[index]||''} onChange={update} disabled={disabled} t={t}/></div>;})}</div></>;
  const order = value.split(',').map(Number), move = (index, delta) => { const next = order.slice(), destination = index + delta; if (destination < 0 || destination >= order.length || disabled) return; [next[index], next[destination]] = [next[destination], next[index]]; onChange(next.join(',')); setMovement(tr(t, 'moved', '{item}: position {position} of {total}', { item: node.items[order[index]], position: destination + 1, total: order.length })); };
  return <><VisualStyles/>{prompt}<ol className="lb-order">{order.map((item, index) => <li key={item}><div className="lb-visual-item"><span>{index + 1}. {node.items[item]}</span><VisualCue cue={cue('item'+item)} support={support} showImages={showImages} t={t}/></div><button type="button" data-board-up={item} disabled={disabled} aria-disabled={index === 0 || disabled} aria-label={tr(t, 'move_up', 'Move {item} up', { item: node.items[item] })} onClick={() => move(index, -1)}>↑</button><button type="button" disabled={disabled} aria-disabled={index === order.length - 1 || disabled} aria-label={tr(t, 'move_down', 'Move {item} down', { item: node.items[item] })} onClick={() => move(index, 1)}>↓</button></li>)}</ol><p className="lb-visually-hidden" role="status">{movement}</p></>;
}
export function BoardView({ board, run, support:rawSupport, role = 'solo', uid = 'solo', roster = {}, onMove, onAnswer, onResolve, onAdvance, onRetry, busy, paused, pending, onReviewPending, pendingResponses = [], onCheckResponses, notice, workspaceKey, workspaceSnapshot, onWorkspaceChange, classRoles, onToggleRoles, reportContext, t }) {
  // Parents key this view by the board/session scope. Read its tab preferences
  // before the first selection effect can save a new available location.
  const [initialWorkspace] = useState(() => {
    if (onWorkspaceChange || !workspaceKey) return {value:soloWorkspace(board,run,workspaceSnapshot)};
    try {
      const raw = sessionStorage.getItem(workspaceKey);
      if (raw && raw.length >= 50000) throw Error('Workspace exceeds the supported size');
      const saved = raw ? JSON.parse(raw) : null;
      return {value:soloWorkspace(board,run,saved?.board === JSON.stringify(board) ? saved : null)};
    } catch (_) { return {value:soloWorkspace(board,run,null),warning:true}; }
  });
  const support=React.useMemo(()=>{try{return prepareSupport(rawSupport,board);}catch(_){return null;}},[rawSupport,board]),[showImages,setShowImages]=useState(initialWorkspace.value.showImages!==false);
  const playRoot=useRef(null);
  const navigateCompletion=kind=>{const selector=kind==='finale'?'[data-board-finale]':kind==='practice'?'[data-adaptive-replay]':'[data-board-portfolio]',target=playRoot.current?.querySelector(selector);if(!target)return;for(let parent=target;parent&&parent!==playRoot.current;parent=parent.parentElement)if(parent.tagName==='DETAILS')parent.open=true;setTimeout(()=>{const focus=target.querySelector('h3,h4')||target.querySelector('button,summary');focus?.focus({preventScroll:true});target.scrollIntoView?.({block:'start'});},0);};
  const [replay,setReplay]=useState(initialWorkspace.value.replay||null);
  const [finale,setFinale]=useState(initialWorkspace.value.finale||null);
  const [guideMode,setGuideMode]=useState(initialWorkspace.value.guideMode||'');
  const [projectGoal,setProjectGoal] = useState(initialWorkspace.value.projectGoal || '');
  const rememberedPlan = React.useMemo(() => projectGoal ? projectPlan(board,run,projectGoal) : null,[board,run,projectGoal]), planned = rememberedPlan?.status === 'funded' ? rememberedPlan.locationIds : [];
  const step = engine.stepOf(run), progress = engine.derive(board, run), ready = engine.targets(board, run).map(n => n.id), heading = useRef(null), phaseRef = useRef(step.phase), [selected, setSelected] = useState(initialWorkspace.value.selected), [view, setView] = useState(initialWorkspace.value.view), [drafts, setDrafts] = useState(initialWorkspace.value.drafts), [savedWarning, setSavedWarning] = useState(initialWorkspace.warning ? tr(t,'restore_workspace_unavailable','Your view, construction goal, and unfinished response could not be restored in this tab.') : ''), [confirmEarly, setConfirmEarly] = useState(false), [inspectId, setInspectId] = useState(''), inspection = useRef(null), earlyCancel = useRef(null), resolveTrigger = useRef(null);
  useEffect(() => { if (confirmEarly) earlyCancel.current?.focus(); }, [confirmEarly]);
  const cancelEarly = () => { setConfirmEarly(false); resolveTrigger.current?.focus(); };
  const activeNode = board.locations.find(n => n.id === step.targetId), targetId = step.phase === 'choose' || progress.complete ? selected : step.targetId, target = [...board.locations, ...board.projects].find(n => n.id === targetId) || board.locations[0];
  const draftKey = run.turn + ':' + (step.retryRound ? step.retryRound + ':' : '') + (activeNode?.id || ''), draft = drafts[draftKey] ?? (activeNode ? engine.initialDraft(activeNode) : '');
  const persist = (nextDrafts, nextSelected = selected, nextView = view, nextGoal = projectGoal, nextImages = showImages, nextGuide = guideMode, nextFinale = finale, nextReplay = replay) => { if (onWorkspaceChange) { onWorkspaceChange({ drafts: nextDrafts, selected: nextSelected, view: nextView, ...(nextGoal ? {projectGoal:nextGoal} : {}), ...(nextImages ? {} : {showImages:false}), ...(nextGuide ? {guideMode:nextGuide} : {}), ...(nextFinale ? {finale:nextFinale} : {}), ...(nextReplay ? {replay:nextReplay} : {}) }); return; } if (!workspaceKey) return; try { sessionStorage.setItem(workspaceKey, JSON.stringify({ board: JSON.stringify(board), drafts: nextDrafts, selected: nextSelected, view: nextView, ...(nextGoal ? {projectGoal:nextGoal} : {}), ...(nextImages ? {} : {showImages:false}), ...(nextGuide ? {guideMode:nextGuide} : {}), ...(nextFinale ? {finale:nextFinale} : {}), ...(nextReplay ? {replay:nextReplay} : {}) })); } catch (_) { setSavedWarning(tr(t,'save_workspace_unavailable','Your view, construction goal, and unfinished response could not be saved in this tab.')); } };
  const chooseGuide=mode=>{setGuideMode(mode);persist(drafts,selected,view,projectGoal,showImages,mode);if(mode==='off')setTimeout(()=>heading.current?.focus(),0);};
  const chooseProjectGoal = id => { const next = board.projects.some(project => project.id === id) ? id : ''; setProjectGoal(next); persist(drafts,selected,view,next); };
  const planner = <RoutePlanner board={board} run={run} onSelect={id => select(id)} goal={projectGoal} onGoalChange={chooseProjectGoal} t={t}/>;
  const select = id => { setSelected(id); persist(drafts, id); if (step.phase !== 'choose' || progress.complete) { setInspectId(id); setTimeout(() => inspection.current?.focus(), 0); } else setTimeout(() => heading.current?.focus(), 0); };
  useEffect(() => { setConfirmEarly(false); setInspectId(''); if (step.phase === 'choose' && !progress.complete && !ready.includes(selected)) { const next = engine.targets(board, run).find(node => node.conceptId && !progress.concepts.includes(node.conceptId)) || engine.targets(board, run)[0]; if (next) { setSelected(next.id); persist(drafts, next.id); } } if (phaseRef.current !== step.phase && document.activeElement === document.body) heading.current?.focus(); phaseRef.current = step.phase; }, [run.turn, step.phase, step.retryRound]);
  const answers = Object.keys(step.answers || {}).filter(id => Object.prototype.hasOwnProperty.call(roster, id)), votes = Object.values(step.votes || {}), answered = !!step.answers?.[uid], result = step.result, personalResult = result?.marks?.[uid], locked = !!busy || !!paused || !!pending;
  const projectEffect = project => project.effect.kind === 'yield' ? tr(t, 'yield_effect', 'Future successful locations earn +1 {resource}.', { resource: board.resources[project.effect.resource] }) : tr(t, 'path_effect', 'Opens a direct path to {location}.', { location: board.locations.find(n => n.id === project.effect.targetId)?.name });
  const inspected = [...board.locations, ...board.projects].find(n => n.id === inspectId);
  const cost = values => values.map((v, i) => v + ' ' + board.resources[i]).join(' · ');
  const limit = engine.turnLimit(board, run);
  const construction = <section className="lb-workshop"><h3 className="lb-project-heading">{tr(t, 'projects', 'Construction projects')}</h3><div className="lb-projects">{board.projects.map(project => { const detail = moveDetails(board, run, project.id); return <button type="button" className="lb-project-card" key={project.id} data-project={project.id} data-affordable={detail.affordable} data-built={detail.built} aria-pressed={(inspectId || targetId) === project.id} onClick={() => select(project.id)}><span className="lb-row"><Icon name={project.icon}/><strong>{project.name}</strong></span>{showImages&&detail.built&&support?.art?.projects?.[project.id]&&<SupportImage src={support.assets[support.art.projects[project.id]]} t={t}/>}<span className="lb-project-state">{detail.built ? '✓ ' + tr(t, 'built', 'Built') : detail.affordable ? tr(t, 'project_ready', 'Ready to build') : tr(t, 'project_save', 'Gather resources')}</span>{projectGoal === project.id && !detail.built && <small className="lb-plan-marker" data-goal-project={project.id}>{tr(t,'your_construction_goal','Your construction goal')}</small>}<small>{tr(t, 'project_cost', 'Construction cost')}</small><Amounts board={board} values={project.cost}/><small>{projectEffect(project)}</small>{!detail.built && !detail.affordable && <small>{tr(t, 'missing_resources', 'Still needed: {resources}', { resources: cost(detail.shortfall) })}</small>}</button>; })}</div>{!progress.complete && planner}</section>;
  return <div ref={playRoot} data-board-play data-play-view={view}><SupportStyles/><header className="lb-expedition-header"><p className="lb-eyebrow">{role === 'solo' ? tr(t, 'solo_expedition', 'Your lesson expedition') : tr(t, 'class_expedition', 'A shared lesson expedition')}</p><h2>{board.title}</h2>{view !== 'focus' && <p>{board.mission}</p>}<MissionDashboard board={board} run={run} role={role} compact={view === 'focus'} t={t}/></header>
    <ClassroomRoles config={classRoles} roster={roster} run={run} uid={uid} teacher={role === 'teacher'} onToggle={onToggleRoles} busy={busy || paused} t={t}/><ol className="lb-phase-strip" aria-label={tr(t, 'turn_flow', 'Turn flow')} data-board-flow>{[['choose', tr(t, 'phase_choose', 'Choose a move')], ['answer', tr(t, 'phase_activity', 'Explore the lesson')], ['review', tr(t, 'phase_review', 'Review and plan')]].map(([phase,label], index) => <li key={phase} aria-current={!progress.complete && step.phase === phase ? 'step' : undefined}><span aria-hidden="true">{index + 1}</span>{label}</li>)}</ol>
    <p role="status" aria-live="polite" aria-atomic="true" data-board-phase>{progress.complete ? tr(t, 'complete', 'The shared objective is complete!') : step.phase === 'choose' ? tr(t, 'choose_phase', 'Choose the next location or project.') : step.phase === 'answer' ? role === 'solo' ? tr(t, 'solo_answer_phase', 'Activity open: {name}. Explore the evidence, then respond.', { name: activeNode?.name }) : tr(t, 'answer_phase', 'Activity open: {name}. Everyone can respond.', { name: activeNode?.name }) : tr(t, 'review_phase', 'Review the result before the next move.')}</p>
    {showImages&&support?.imageOmissions>0&&<p className="lb-notice" data-board-image-omissions>{tr(t,'shared_picture_omissions','{count} pictures could not be included in this shared board. Vocabulary and activities remain available as text.',{count:support.imageOmissions})}</p>}{notice && <p className="lb-notice" role="status">{notice}</p>}{savedWarning && <p className="lb-notice">{savedWarning}</p>}{paused && <p className="lb-notice" role="status">{tr(t, 'paused', 'Board paused. Your unfinished response is kept.')}</p>}
    <div className="lb-row" aria-label={tr(t, 'presentation', 'Board presentation')}>{Object.keys(support?.assets||{}).length>0&&<button type="button" data-board-images aria-pressed={showImages} onClick={()=>{const next=!showImages;setShowImages(next);persist(drafts,selected,view,projectGoal,next);}}>{showImages?tr(t,'hide_board_images','Hide pictures'):tr(t,'show_board_images','Show pictures')}</button>}<button type="button" aria-pressed={view === 'board'} onClick={() => { setView('board'); persist(drafts, selected, 'board'); }}>{tr(t, 'board_view', 'Board view')}</button><button type="button" aria-pressed={view === 'list'} onClick={() => { setView('list'); persist(drafts, selected, 'list'); }}>{tr(t, 'list_view', 'Location list')}</button><button type="button" data-board-focus aria-pressed={view === 'focus'} onClick={() => { setView('focus'); persist(drafts,selected,'focus'); setTimeout(() => heading.current?.focus(),0); }}>{tr(t, 'focus_view', 'Focus on current move')}</button><button type="button" onClick={() => heading.current?.focus()}>{tr(t, 'jump_move', 'Jump to current move')}</button></div>
    <CompletionSteps board={board} run={run} finale={finale} replay={replay} onNavigate={navigateCompletion} t={t}/>
    <FirstBuildGuide board={board} run={run} mode={guideMode} onMode={chooseGuide} onInspect={id=>{if(id)select(id);else heading.current?.focus();}} role={role} answered={answered} locked={locked} t={t}/>
    {role === 'teacher' && step.phase === 'choose' && !progress.complete && <ClassProposals board={board} run={run} roster={roster} onSelect={select} t={t}/>}
    {view === 'focus' && <p className="lb-muted" data-focus-help>{tr(t, 'focus_help', 'Use the current move and its available choices here. Switch to Board view or Location list whenever you want to explore the map and construction plans.')}</p>}
    <div className="lb-columns" style={view === 'focus' ? {gridTemplateColumns:'minmax(0,1fr)'} : undefined}>{view !== 'focus' && <div>{view==='board'&&<GrowingWorld board={board} run={run} support={support} showImages={showImages} onInspect={select} Icon={Icon} t={t}/>}<BoardMap run={run} Icon={Icon} support={support} showImages={showImages} planned={planned} board={board} progress={progress} ready={ready} selected={inspectId || targetId} onSelect={select} view={view} currentId={step.phase !== 'choose' && !progress.complete ? step.targetId : ''} t={t}/>{inspected && <section className="lb-panel" style={{ marginTop: 14 }} data-board-inspection><h3 ref={inspection} tabIndex={-1}>{inspected.name}</h3><p>{inspected.scene || inspected.description}</p><MovePreview board={board} run={run} target={inspected} t={t}/>{inspected.cost ? <p>{projectEffect(inspected)}</p> : progress.visited.includes(inspected.id) && <><p>{inspected.explanation}</p><blockquote>{inspected.sourceQuote}</blockquote></>}<button type="button" onClick={() => { setInspectId(''); heading.current?.focus(); }}>{tr(t, 'return_move', 'Return to current move')}</button></section>}</div>}
      <section className="lb-panel lb-current" data-phase={step.phase} aria-label={tr(t, 'current_move', 'Current move')}><h3 ref={heading} tabIndex={-1}>{progress.complete ? tr(t, 'well_done', 'Your board is complete') : target.name}</h3>
        {progress.complete ? <><p>{board.debrief}</p><section data-board-final-move><h4>{tr(t, 'final_move', 'Final move: {name}', { name: [...board.locations, ...board.projects].find(item => item.id === step.targetId)?.name || '' })}</h4><MoveRecap board={board} run={run} role={role} uid={uid} t={t}/></section><h4>{tr(t, 'built_choices', 'Your construction choices')}</h4><ul>{board.projects.filter(p => progress.built.includes(p.id)).map(p => <li key={p.id}>{p.name}: {projectEffect(p)}</li>)}</ul><BoardFinale preview={!!reportContext?.preview} board={board} run={run} value={finale} disabled={locked} role={role} t={t} onChange={next=>{setFinale(next);persist(drafts,selected,view,projectGoal,showImages,guideMode,next);}}/>{progress.performance[uid] && <p>{tr(t, 'your_learning', 'Your recorded responses: {correct} correct out of {answered}. Shared construction progress is separate.', progress.performance[uid])}</p>}</> : <><p>{target.scene || target.description}</p>
          {step.phase === 'choose' && <><div className="lb-ready-choices"><p className="lb-muted">{tr(t, 'ready_move_help', 'Select a move to inspect it, then use the action button below.')}</p><div className="lb-row">{engine.targets(board, run).map(node => <button type="button" key={node.id} className="lb-small-choice" data-ready-move={node.id} aria-pressed={node.id === target.id} onClick={() => select(node.id)}>{node.cost ? '◆ ' : '○ '}{node.name}</button>)}</div></div><MovePreview board={board} run={run} target={target} t={t}/>{target.cost && <p>{projectEffect(target)}</p>}{ready.includes(target.id) ? <button type="button" className="lb-primary" data-board-move={target.id} disabled={locked} onClick={() => onMove(target.id)}>{role === 'student' ? tr(t, 'propose', 'Propose this move') : target.cost ? tr(t, 'build', 'Build this project') : tr(t, 'explore', 'Explore this location')}</button> : <p>{target.cost ? progress.built.includes(target.id) ? tr(t, 'already_built', 'This project is already built.') : tr(t, 'need_resources', 'Explore more locations to earn the required resources.') : progress.visited.includes(target.id) ? tr(t, 'already_explored', 'Already explored. Its resources have been collected.') : tr(t, 'reach_help', 'Explore a connected location or build a shortcut to open this location.')}</p>}{role !== 'solo' && <p>{tr(t, 'votes', 'Confirmed proposals for this move: {count}.', { count: votes.filter(id => id === target.id).length })}</p>}{step.votes?.[uid] && role === 'student' && <p role="status">{tr(t, 'your_proposal', 'Your confirmed proposal: {name}', { name: [...board.locations, ...board.projects].find(n => n.id === step.votes[uid])?.name || '' })}</p>}{role === 'student' && pending && onReviewPending && <button type="button" data-board-pending-link onClick={onReviewPending}>{tr(t, 'review_delivery', 'Review delivery status')}</button>}{!target.cost && progress.visited.includes(target.id) && <><p>{target.explanation}</p><blockquote>{target.sourceQuote}</blockquote></>}</>}
          {step.phase === 'answer' && activeNode && <><p className="lb-instruction">{activeNode.instruction}</p><fieldset disabled={locked || answered || role === 'teacher'}><legend>{tr(t, 'activity', 'Lesson activity')}</legend><Activity key={run.turn + ':' + (step.retryRound || 0)} support={support} showImages={showImages} node={activeNode} value={answered ? step.answers[uid].value : draft} disabled={locked || answered || role === 'teacher'} t={t} onChange={value => { const next = { [draftKey]: value }; setDrafts(next); persist(next); }}/></fieldset>{role !== 'teacher' && <button type="button" className="lb-primary" data-board-submit disabled={locked || answered || !engine.validValue(activeNode, draft)} onClick={() => onAnswer(draft)}>{answered ? tr(t, 'confirmed', 'Response confirmed') : tr(t, 'submit', 'Submit response')}</button>}{role === 'student' && pending && onReviewPending && <button type="button" data-board-pending-link onClick={onReviewPending}>{tr(t, 'review_delivery', 'Review delivery status')}</button>}{answered && <p role="status">{tr(t, 'wait_review', 'Your response is confirmed. Review the lesson while the teacher gathers responses.')}</p>}<details><summary>{tr(t, 'hints', 'Hints and lesson evidence')}</summary><blockquote>{activeNode.sourceQuote}</blockquote>{activeNode.hints.map((hint, index) => <details key={index}><summary>{tr(t, 'hint', 'Hint {number}', { number: index + 1 })}</summary><p>{hint}</p></details>)}</details>{role === 'teacher' && <><p>{tr(t, 'response_count', '{count}/{total} learners have confirmed responses.', { count: answers.length, total: Object.keys(roster).length })}</p>{pendingResponses.length > 0 && <div className="lb-notice" data-board-received><p role="status">{tr(t, 'received_waiting', 'Received responses awaiting confirmation: {count}.', { count: pendingResponses.length })}</p><p>{paused ? tr(t, 'received_paused', 'These responses are kept while the board is paused. Resume to confirm them.') : tr(t, 'received_resolve_help', 'Confirm these received responses before resolving the activity.')} </p>{onCheckResponses && <button type="button" data-check-board-responses disabled={locked} onClick={onCheckResponses}>{tr(t, 'check_received', 'Retry confirmation')}</button>}</div>}{answers.length < Object.keys(roster).length && <details data-board-awaiting><summary>{tr(t, 'awaiting_count', 'Awaiting confirmed responses: {count}', { count: Object.keys(roster).length - answers.length })}</summary><ul>{Object.entries(roster).filter(([id]) => !answers.includes(id)).map(([id, learner]) => <li key={id}>{learner?.name || id}{pendingResponses.includes(id) && <span> · {tr(t, 'received_label', 'Received; awaiting confirmation')}</span>}</li>)}</ul><p>{tr(t, 'awaiting_help', 'Received responses must be confirmed before you resolve. Learners who have not responded are not marked incorrect.')}</p></details>}<p className="lb-muted">{tr(t, 'shared_rule', 'The location is explored when at least half of the submitted responses are correct. Missing responses are not marked wrong. An unsuccessful activity costs no resources and can be tried again.')}</p><button type="button" ref={resolveTrigger} data-board-resolve disabled={locked || !answers.length || pendingResponses.length > 0} onClick={() => answers.length < Object.keys(roster).length ? setConfirmEarly(true) : onResolve()}>{tr(t, 'review_responses', 'Review responses and resolve')}</button>{confirmEarly && <div className="lb-notice" role="group" onKeyDown={event => { if (event.key === 'Escape') { event.preventDefault(); event.stopPropagation(); cancelEarly(); } }} aria-label={tr(t, 'early_review', 'Resolve before everyone responds')}><p>{tr(t, 'early_notice', '{count} learners have not confirmed a response. Continue with the responses already received?', { count: Object.keys(roster).length - answers.length })}</p><button type="button" disabled={locked || pendingResponses.length > 0} onClick={() => { setConfirmEarly(false); onResolve(); }}>{tr(t, 'resolve_received', 'Resolve received responses')}</button><button type="button" ref={earlyCancel} disabled={locked} onClick={cancelEarly}>{tr(t, 'keep_waiting', 'Keep waiting')}</button></div>}<details><summary>{tr(t, 'teacher_responses', 'Teacher response review')}</summary><ul>{answers.map(id => <li key={id}><strong>{roster[id]?.name || id}: {step.answers[id].correct ? tr(t, 'correct', 'Correct') : tr(t, 'revisit', 'Needs review')}</strong><p>{responseText(activeNode, step.answers[id].value)}</p></li>)}</ul><p><strong>{tr(t, 'solution', 'Solution')}: </strong>{responseText(activeNode, engine.solution(activeNode))}</p><p>{activeNode.explanation}</p></details></>}</>}
          {step.phase === 'review' && <><MoveRecap board={board} run={run} role={role} uid={uid} t={t}/>{role !== 'student' ? <div className="lb-row">{!result?.success && onRetry && limit.canRetry && <button type="button" className="lb-primary" data-board-retry disabled={locked} onClick={onRetry}>{tr(t, 'retry_location', 'Retry this location')}</button>}<button type="button" className={result?.success ? 'lb-primary' : ''} data-board-next disabled={locked || !limit.canAdvance} onClick={onAdvance}>{tr(t, 'next_move', 'Choose the next move')}</button>{limit.reached && <p>{tr(t, 'move_limit', 'This board has reached its move limit. You can still retry this activity if it needs review. Restart the board to explore another route.')}</p>}</div> : <p>{tr(t, 'teacher_next', 'The teacher will open the next move after discussion.')}</p>}</>}
        </>}
      {showImages&&target.cost&&progress.built.includes(target.id)&&support?.art?.projects?.[target.id]&&<SupportImage src={support.assets[support.art.projects[target.id]]} t={t}/>}<VocabularyCards support={support} nodeId={target.id} showImages={showImages} reviewed={step.phase==='review'||progress.visited.includes(target.id)} teacher={role==='teacher'} t={t}/>
      </section>
    </div><GuidedTools collapsed={guideMode==='active'&&!progress.visited.length&&!progress.complete} label={tr(t,'guide_construction_tools','Construction and planning tools. These open after your first successful exploration.')}>{view !== 'focus' && construction}{view === 'focus' && !progress.complete && planner}</GuidedTools><PlanningSandbox board={board} run={run} onInspect={select} locked={locked} support={support} showImages={showImages} Icon={Icon} t={t}/>{role === 'teacher' && <TeacherLearningReview board={board} run={run} roster={roster} t={t}/>}{role !== 'student' && reportContext && <ReportActions board={board} run={run} roster={roster} context={reportContext} t={t}/>}<GuidedTools collapsed={guideMode==='active'&&!Object.values(run.steps||{}).some(step=>step.result)} label={tr(t,'guide_journal_tools','Learning trail. This opens after your first review.')}><LearningJournal onShare={()=>navigateCompletion('share')} replay={replay} onReplayChange={next=>{setReplay(next);persist(drafts,selected,view,projectGoal,showImages,guideMode,finale,next);}} disabled={locked} support={support} showImages={showImages} board={board} run={run} role={role} uid={uid} roster={roster} Activity={Activity} t={t}/></GuidedTools><LearningShare board={board} run={run} uid={uid} role={role} finale={finale} replay={replay} preview={!!reportContext?.preview} t={t}/>
  </div>;
}
