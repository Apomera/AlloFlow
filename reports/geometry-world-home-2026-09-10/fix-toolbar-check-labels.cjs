const fs=require('node:fs');const file=__dirname+'/verify-home.cjs';let s=fs.readFileSync(file,'utf8');for(const [from,to] of [["name:'Hide game bar',exact:true","name:'Hide the Geometry World game bar',exact:true"],["name:'Show game bar',exact:true","name:'Show the Geometry World game bar',exact:true"]]){if(s.split(from).length!==2)throw Error('Missing toolbar assertion');s=s.replace(from,to);}const fd=fs.openSync(file,'r+');fs.writeFileSync(fd,s);fs.ftruncateSync(fd,Buffer.byteLength(s));fs.closeSync(fd);console.log('Toolbar checks use their actual accessible names.');
