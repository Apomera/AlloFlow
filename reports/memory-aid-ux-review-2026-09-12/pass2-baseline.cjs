const fs=require('fs'),path=require('path'),{pathToFileURL}=require('url'),{chromium}=require('playwright');
(async()=>{const browser=await chromium.launch({headless:true});try{const page=await browser.newPage({viewport:{width:390,height:844}});await page.goto(pathToFileURL(path.join(__dirname,'current-preview.html')).href);await page.getByRole('button',{name:'Try recall',exact:true}).first().click();await page.getByRole('radio',{name:'Without hints',exact:true}).check();await page.getByRole('button',{name:'Start recall practice',exact:true}).click();const result={width:390,responseTop:await page.getByRole('textbox',{name:/Recall response for/}).evaluate(el=>Math.round(el.getBoundingClientRect().top+scrollY)),pageHeight:await page.evaluate(()=>document.documentElement.scrollHeight)};fs.writeFileSync(path.join(__dirname,'pass2-baseline.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result));}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
