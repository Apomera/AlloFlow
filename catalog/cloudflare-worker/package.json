{
  "name": "alloflow-catalog-submit-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that accepts AlloFlow catalog submissions and commits them to catalog/pending/ via the GitHub API.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
