const fs=require('fs'),file=__dirname+'/browser.cjs';let s=fs.readFileSync(file,'utf8');s=s.replace("await vocab.locator(':scope > summary').click();await vocab.locator('details > summary').click();","if(await vocab.getAttribute('open')===null)await vocab.locator(':scope > summary').click();if(await vocab.locator('details').getAttribute('open')===null)await vocab.locator('details > summary').click();");fs.writeFileSync(file,s);
