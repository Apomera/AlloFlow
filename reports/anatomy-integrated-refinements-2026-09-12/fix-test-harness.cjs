const fs=require('fs');for(const file of ['tests/anatomy_integrated_refinements.test.js','tests/anatomy_learning_reliability.test.js']){let s=fs.readFileSync(file,'utf8').replaceAll('.props.ref({})','.ref({})').replace("node('data-anatomy-quiz-panel',true)","node('data-anatomy-quiz-panel','true')");fs.writeFileSync(file,s);}
let s=fs.readFileSync('tests/anatomy_lab_science.test.js','utf8').replace("'var quizPatch={'","'if(!sameQuizQuestion(state)' ").replace("'_totalCorrect=priorCorrect+1'","'patch._totalCorrect=safeNonNegativeNumber(state._totalCorrect,0,true)+1'");fs.writeFileSync('tests/anatomy_lab_science.test.js',s);
s=fs.readFileSync('tests/anatomy_mobile_a11y_polish.test.js','utf8').replace('source.indexOf("function ", start + 10)','source.indexOf("\\n        function ", start + 10)');fs.writeFileSync('tests/anatomy_mobile_a11y_polish.test.js',s);
