const {session}=require('./science_refinement_session.cjs');exports.refine=p=>{const {E,get,replaceAll,finish}=session(p);
E('reading','data',`## When Doubling One Doubles the Other

Imagine every taco costs $3, with no fees or discounts. One costs $3, two cost $6, four cost $12. The cost is **proportional** to the number bought under those assumptions.

A proportional model has the rule **y = kx**. Here x is the input, y the output, and k the **constant of proportionality**. For tacos, k is $3 per taco. At every nonzero input, y divided by x equals 3. When x is zero, the rule gives y = 0; do not calculate 0 divided by 0.

The same multiplier connects the quantities throughout the model's domain. Scaling an input by a factor scales the output by that factor, as long as both inputs are allowed. Whole taco counts are whole numbers; their graph is a set of points on a line. A continuous quantity may fill the line or a segment.

To check an exact table, divide y by x for every nonzero x. Check any zero-input row separately: its output must be zero. Matching ratios show that the listed pairs fit a proportional rule. A few matching pairs alone do not prove an unobserved relationship will always follow it.

With the fixed-price assumption, nine tacos cost 9 times 3, or $27. A fee, discount or change in product would require checking the model again. For real measurements, small differences in ratios may reflect uncertainty or rounding. Report the range tested and whether a proportional model is a reasonable approximation.

A pool that already contains ten gallons and gains five per minute follows y = 5x + 10 while filling steadily and before it reaches capacity. At zero minutes it has ten gallons. It is not proportional to elapsed time. The amount ADDED since the start can be proportional, so define your output carefully.

On equally interpreted coordinate axes, y = kx lies on a straight nonvertical line through the **origin**, (0,0). Passing through the origin alone is not enough; curved graphs can do that too. The value k is the numerical slope, calculated from units on the axes. Changing the display scales changes the apparent angle, not the underlying rate.

A table, equation and graph can describe the same model. Name the input, output, units, constant and allowed values. Then explain which assumptions make your prediction reasonable.`);
E('glossary','data.2.def','The multiplier k in y = kx; for a nonzero input it equals output divided by input.');E('glossary','data.9.def','Use a stated model to estimate an unmeasured value within its assumptions and domain.');E('glossary','data.11.def','Slope describes change in output per input unit; the displayed angle also depends on axis scales.');
E('anchor','data.sections.0.bullets.0','For nonzero x, divide output by input');E('anchor','data.sections.0.bullets.1','Matching ratios: listed pairs fit y = kx');E('anchor','data.sections.0.bullets.2','At x = 0, check y = 0 without dividing');E('anchor','data.sections.1.bullets.2','Predict within the model assumptions and allowed inputs');E('anchor','data.sections.2.bullets.2','A nonzero value at x = 0 breaks proportionality');E('anchor','data.sections.3.bullets.0','Table: constant y/x for nonzero x');E('anchor','data.sections.3.bullets.2','Graph: points on a nonvertical line through the origin');
E('memory','data.sourceExcerpt','Check y/x only for nonzero x. Any zero-input row must have zero output. Matching exact pairs fit a proportional model; predictions beyond them require assumptions.');
const M=(i,k,v)=>E('memory',`data.cards.${i}.${k}`,v);M(0,'target','Test exact table entries without dividing by zero');M(0,'essentialFacts.0','Divide output by input for each nonzero input.');M(0,'essentialFacts.1','If all those ratios match and any zero-input row has zero output, the listed pairs fit a proportional model.');M(0,'essentialFacts.2','For measured data, assess uncertainty before interpreting small ratio differences.');M(0,'aiExample','NONZERO: divide. ZERO: inspect. MATCH: a model fits. Then check whether the situation supports that model beyond the table.');M(0,'mapping','The cue separates nonzero inputs from zero and separates a fit to listed pairs from a universal claim.');M(0,'scaffoldStarter','For nonzero inputs ____. At zero ____. The data fit ____.');M(1,'essentialFacts.2','A proportional function has points on a nonvertical straight line through the origin; a zero point alone is insufficient.');M(1,'mapping','A nonzero zero-input amount rules out proportionality. A zero amount passes only one necessary check, not the whole test.');M(2,'essentialFacts.0','Table: y/x agrees in every nonzero-input row; check zero separately.');M(2,'essentialFacts.2','Graph: y = kx lies on a straight nonvertical line through the origin; domain may restrict it to points or a segment.');
E('sort','data.items.4.content','Points on a nonvertical straight line through the origin');E('sort','data.items.8.content','Distance traveled from the start at a constant 60 miles per hour');
E('math','data.problems.4.steps.2.explanation','The pairs fit y = 3x + 5. At x = 0 the model gives 5, not zero. The first listed output 8 occurs at x = 1 and is not the intercept.');
const q=get('quiz').data.questions;q[0].question='Which checks show that every row of an exact table fits a proportional model?';q[0].options[3]=q[0].correctAnswer='Equal y/x for nonzero x; any x = 0 row must have y = 0';q[2].question='A proportional function with positive constant and all real inputs has which graph?';q[2].options[1]=q[2].correctAnswer='A rising straight line through the origin (0, 0)';
const faq=['For a proportional relationship, k is the unit rate with the chosen output-per-input order. Reversing the order changes the number and units.','The rule y = kx gives zero output when x is zero. But a zero point alone does not prove proportionality; the same k must work throughout the domain.','In y = kx + b, b is the output at x = 0. The rule is proportional when b = 0; a nonzero b makes it nonproportional.','Yes. For 0 < k < 1, the line rises more slowly in numerical units than y = x. A negative k also is below 1, but gives a falling line. Axis scales affect the visible angle.','For an exact table, check every nonzero-input ratio and inspect zero-input rows separately. Matching rows fit a proportional rule; measured data need uncertainty checks and do not prove behavior outside the tested range.','Proportional models help with fixed unit prices, scaling and conversions. Fees, changing rates, limits and different definitions of the quantities can make a model inappropriate.'];faq.forEach((v,i)=>E('faq',`data.${i}.answer`,v));
E('challenge','data.instructions','Collect at least five real data pairs and assess whether a proportional model fits the measured range. Check nonzero ratios, zero behavior, units and measurement uncertainty. Do not claim to prove behavior outside your observations.');E('challenge','data.brief.seedDirection','Record data with units and resolution. Compute y/x only when x is nonzero. Inspect a zero-input value separately if available, then check a graph and the context.');E('challenge','data.brief.lockedLessonFacts.0','A proportional rule has one k with y = kx. The ratio y/x is used only when x is nonzero.');E('challenge','data.brief.lockedLessonFacts.3','Proportional model points lie on a nonvertical line through the origin; available inputs may be discrete.');E('challenge','data.brief.criteria.1','Compute y/x for each nonzero input; mark zero-input ratios undefined and inspect y separately');E('challenge','data.brief.criteria.2','State whether the model fits approximately or exactly in the measured range, with uncertainty and limits');E('challenge','data.supports.parallelExample.move','He used an illustrative fare rule y = 2.50x + 3 and distances of 1, 2, 3, 4 and 6 miles. Costs were $5.50, $8, $10.50, $13 and $18. The ratios vary, but subtracting the $3 base fare before dividing gives $2.50 per mile each time. These are model numbers, not actual taxi receipts.');E('challenge','data.coachHint','Never divide by zero. Compare all nonzero ratios, assess measurement uncertainty, check the origin condition and limit the conclusion to the tested range.');E('challenge','data.sourceExcerpt','A proportional model has y = kx. Test nonzero ratios and zero behavior separately, and state what observations can and cannot establish.');
return finish();};
