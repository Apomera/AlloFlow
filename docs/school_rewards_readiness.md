# School Rewards pathway readiness

This report covers the local package and its automated checks. The integrated tests run the actual portal HTML and Apps Script code with simulated Google Sheets, Drive, identity, locks, triggers, and email. They do not certify a school's live Google Workspace deployment or a physical printer.

## Repeatable release check

Run `npm run verify:school-rewards` from the repository root.

Latest local verification (2026-09-04): all **491 pre-existing tests across 15 files passed** in the broader release run; that command reported a host timeout starting the new onboarding worker. After the final demo fixes and rebuild, the affected check passed **171 tests across 4 files**, including all **6 new onboarding regressions**, with exit code 0 (`scratch/school-rewards-demo-verified-results.json`). These runs cover 497 distinct cases; there was not one clean 497-test invocation. The final browser demo passed Staff → Student → Cashier → Administrator, with balances 9 → 29 → 14 and stock 20 → 19. Desktop and 390px mobile screenshots were inspected; no horizontal overflow or page errors were found, and the tour stays above mobile navigation. Spanish Portal coverage is 858/858 catalogue entries. Continuation (2026-09-04, Claude): after the award-notice period fix and practice rebuild, the four affected suites passed **172 tests** (`--pool=threads`); the onboarding suite's worker timed out at startup in that run and passed separately, **7 of 7**, with `--pool=forks`. Portal and practice mirrors were re-verified byte-identical.

The command includes the school rewards repository, portal, setup panel, language packs, manual, practice pages, Leadership Hub launcher, classroom reward boundary, Print Lab tool, and Geometry World handoff tests.

## Pathways covered

| Pathway | Automated evidence | Live check before school use |
| --- | --- | --- |
| Setup and deployment | Setup/bootstrap, role rules, schema migrations, launcher, copyable files and deployment mirrors | Run the setup/status check from the intended managed administrator account; deploy updated Code.gs and Portal.html |
| Student access | Own balances, recognition, goals/catalog, activity and role-specific navigation | Sign in as two test students and confirm each sees only their own records |
| Staff recognition | Single/group awards, explanations, corrections, undo, partial failures and retry recovery | Award and undo a small recognition using designated test accounts |
| Physical store | Open-window checks, inventory, reserved points, checkout, receipts, refund and integrity reconciliation | Complete one test purchase and refund; verify balances, stock and managed-email receipt |
| Print submission | Recipe and GLB/STL handoff validation, upload limits/hash checks, private review and linked revisions | Review a recipe and a sample imported file with the school's trained print operator |
| Print fulfillment | Quote, reservation, queue, print status, fulfillment, cancellation and print refund | Confirm the school’s material/printer profiles and hand-delivery procedure |
| Moderated model catalog | Consent, moderation, reporting, publication permissions and private recipe remix | Approve a test design and verify the allowed remix from another test student account |
| Guardian and student mail | Consent, statements/digests, quota handling, resumable runs and uncertain-delivery resolution | Verify consented test addresses, actual delivery and scheduled-trigger ownership |
| Administration | Members/roster, catalog and stock adjustments, windows, categories, levels, SIS preview/apply, reports, records, retention and year rollover | Preview the school's approved roster export; validate timezone, academic year, ownership and retention choices |
| Recovery and integrity | Interrupted writes, duplicate requests, inventory/audit integrity, receipt recovery and pending operations | Run the integrity report and verify the school backup/recovery procedure |
| Language and training | English/Spanish pack parity, setup help, manuals and practice pages | Have staff review the terminology and practice their assigned role |

## Small administrator demo

The fictional-data rewards/store demo is ready for a guided walkthrough. Use [the presenter guide](school_rewards_admin_demo.md). Live school deployment and advanced pathways still require the acceptance checks below. The continuation context is in [the Claude handoff](../CLAUDE_SCHOOL_REWARDS_HANDOFF.md).

## Improvements from this review

- Optional practice customization stays hidden until opened. The practice page explains the four roles and offers an opt-in tour and five-minute demo guide.
- Admin section links and unfinished checklist buttons open the relevant screen and preserve keyboard focus. Advanced sections start collapsed; catalog stock actions open the inventory section.
- Student search no longer rebuilds results when losing focus, preserving the first click on a student. Clicking an already selected single-student tile keeps it selected.
- Tour timers cancel cleanly on exit or superseded steps, and mobile tour positioning respects bottom navigation.
- **Built-in help (2026-09-04).** A Help button in the portal header opens a role-aware guide inside the portal: the two kinds of points, who does what, finding and verifying a student, the two-step checkout, and setup ownership, with links to the manual, quick cards, and practice page. Sections show only for the signed-in role and every string is in the Spanish pack (918/918). Screenshots: `scratch/school-rewards-help-{staff,staff-es,admin,student-phone}.png`.
- **Open is not always open right now.** The Store tab now says in words when a window is set to Open but its start time has not arrived (and when checkout opens) or its end time has passed. The first-week checklist hint explains Preview versus Open and the schedule. The Overview's "How this pilot works" gained a fourth line separating spendable balance from growth levels.
- **Manual and quick cards refreshed** (`school-rewards-manual.html`, `school-rewards-quick-cards.html`, both mirrored): finding a student, two-step checkout, Open-but-scheduled windows, the collapsed Admin layout with checklist buttons, the two numbers a student sees, the Demo guide, the Help button, and the English-only practice wrapper. Reviewed date moved to September 4, 2026.
- **Manual visuals (2026-09-04, second pass).** Section 1 gained an accessible inline SVG of how points move (award → available balance → checkout; award → growth level; title/desc for screen readers; theme-aware via CSS variables) and an "If you get lost, press Help" paragraph. Section 5 gained a three-column "Shopping day at a glance" box. Four figures were captured fresh from the practice page with `dev-tools/school_rewards_manual_capture.cjs` and converted to 1180px JPGs in `school-rewards-manual-assets/` (mirrored): the practice page with the demo guide open (replaces 09), the built-in Help panel (10), the Admin tab with the collapsed layout and a checklist action button (11, replaces the stale 07 figure in the text; the 07 file remains), and the Store tab for a window set to Open before its start time (12). Diagram and box were rendered in light and dark and inspected.
- **Print Lab default flipped to opt-in (2026-09-05, authorised).** `printLabEnabled_()` now returns true only for an explicit 'true', so a fresh deployment hides the tab until an administrator turns it on; a school that already saved the setting keeps its value. Repository test rewritten to pin the new default; the store pathways seed now opts in explicitly. Portal setting help, manual, and demo guide say 'hidden until turned on'.
- **check_stem_ctx made guard-aware (2026-09-06).** The gate flagged `ctx.X` reads the source explicitly guards, which cannot throw: `ctx.lang || 'en'`, `ctx.tryAward || function(){}`, `typeof ctx.addXP === 'function'`, `ctx.foo ? ctx.foo() : fallback()`. Of its 17 findings, 14 were that false positive, which is how a gate teaches people to ignore it. A reference is now exempt only when the source tests it in a **direct** shape, deliberately not following nesting, so `ctx.lang.toUpperCase() || 'en'` still reports because a missing `ctx.lang` still throws there. Calibrated with a temporary SEL fixture, since removed: it flagged the unguarded call and the nested dereference and stayed silent on the four guarded forms. Every one of the 21 suppressions was read in the real source and confirmed to be a guard with a fallback.
- **Reduced motion now reaches the tools that ask for it (2026-09-06).** The first of the gate's three real findings is fixed. `stem_lab_module.js` has computed `_reduceMotion` for its own modal animations since the reduced-motion work, reading both the app's header toggle and the operating system preference, but never put it on the tool context. Tree Lab asks for `ctx.reduceMotion`, received undefined, and so animated growth at three updates per second and passed `reduced: false` to its 3D viewer for a learner who had explicitly asked for less motion. One additive line on the context literal fixes it: no tool had a value before, so nothing can regress. Tree Lab's suite passes 183 of 183 and the gate no longer flags it.
- **The gate registry's other failures are not from this work (2026-09-06).** `tests/dev_tools_orphan_gates.test.js` has four red gates, and `check_stem_ctx` is not among them: they name a galaxy tool, a dissection tool, a plate-tectonics file that no longer parses, and two state scanners. None mentions Print Lab, Art Studio, or Architecture Studio, and all belong to other sessions' in-flight work in this shared tree.
- **Three real findings now visible instead of buried (2026-09-06).** With the noise gone the gate reports exactly three, all in other people's tools and all left alone: `stem_tool_eligibility.js` reads `ctx.focusSection` into a variable that is never provided; `stem_tool_gisstudio.js` falls back to `ctx.lang`, which does not exist, so its locale is undefined; and `stem_tool_treelab.js` computes `!!ctx.reduceMotion`, so a learner's reduced-motion preference is never honoured there. That last one is an accessibility gap worth someone's attention.
- **The repo's own STEM gates run clean over the three changed tools (2026-09-06).** `check_stem_ctx` (the loader really does provide the navigation callback the new buttons call, so they are not silent no-ops), `check_free_vars` (no new free variables, which is what proves `ctx` is genuinely in scope where Art Studio's hand-off uses it, rather than shadowed by one of the canvas contexts declared elsewhere in that file), and `check_stem_layout_defects` (zero findings for each of Print Lab, Art Studio, and Architecture Studio). **Correction (2026-09-06):** the entry I first reported here as a bug in `sel_tool_strengths.js` was not one. Its Back button reads `ctx.setStemLabTool ? ctx.setStemLabTool(null) : (ctx.setSelHubTool && ctx.setSelHubTool(null))`, and the SEL hub does provide `setSelHubTool`, so the button works. The defect was in the gate, which flagged guarded reads; it is fixed below.
- **New print labels entered the translation pipeline (2026-09-06).** The six Architecture Studio keys were missing from `ui_strings.js`, which the runtime pack builder diffs against the user's cached pack: a key absent there is never translated for anyone, in any language, however correctly its call site is wrapped. Registered with the repo's own tool (89,147 to 89,153 keys, file still parses). The two Art Studio keys were already present, added by the label tooling. The drift gate reports zero divergence for all eight, and its total is 7 both before and after this change, so the entries it does list are another session's in-flight work. The `lang/*.js` packs were deliberately not touched: another session is hand-fixing the Spanish and Esperanto packs today, and registration alone is what puts these strings in front of every language.
- **Art Studio sculpt toolbar photographed and unpacked (2026-09-06).** The last unverified visual claim is now checked: the shot harness gained a `--wait` flag so the CDN-loaded 3D engine can arrive, and the sculpt tab renders with the Print Lab button present and enabled. Looking found a real defect I had caused: adding that button made six labelled controls share a zero-basis flex row, compressing each to 51px with two-line labels. They now carry an explicit flex basis, so the row wraps to two lines with every control 40px tall and 115-147px wide. Inline style rather than a Tailwind class, because the shipped stylesheet is prebuilt and an absent class fails silently.
- **Architecture Studio print entry made findable (2026-09-05).** The toolbar's export group, Print Lab included, sits at the far right of a pill row that scrolls off-screen on ordinary displays. The always-visible left sidebar now has a Print & export section with the same two actions (Print Lab, STL file) wired to the same handlers, plus a one-line note that nothing there spends points. Pinned in `tests/archstudio_printlab_bridge.test.js` (two buttons per handler, section renders with a build present) and checked against the studio's own visual gate.
- **Bridges seen in a browser (2026-09-05).** `dev-tools/stem_tool_shot.cjs` gained a `--pre` hook so a Print Lab hand-off can be photographed. Inspected: the empty Print Lab with its three Start-in buttons; the From Art Studio card with the sculpture loaded into the Design tab; the From Architecture Studio card with counts, mesh envelope, physical size, block-scale presets, and printer-fit advice; and Architecture Studio's toolbar with the Print Lab button enabled beside STL at the sibling's exact size (`scratch/shot-*.png`). The shots exposed and fixed one defect: Art Studio leaves its landing screen only when `studioStarted` is set, so both hand-off paths now set it. Not photographed: Art Studio's sculpt toolbar, which renders only after three.js loads from the CDN; its button is covered by source assertions and the aria-label gate.
- **Bridges made bidirectional (2026-09-05).** Print Lab now offers Start in Geometry World / Architecture Studio / Art Studio when it opens empty, and Revise in Art Studio carries the current (possibly edited) primitive recipe back and opens the sculpt tab, with a download fallback; Art Studio consumes it once on mount and keeps the previous sculpture in undo. Geometry World already had a real return path; Architecture Studio's return is navigation only, by design, since Print Lab holds only its STL. Covered in `tests/artstudio_printlab_bridge.test.js`.
- **Architecture Studio joins as a Print Lab source (2026-09-05).** 'Architect' is one of Architecture Studio's editor styles; the tool builds real block geometry and already downloaded binary STL, but had no route into Print Lab except save-then-open. Its toolbar now has a Print Lab button that reuses the STL builder and hands the bytes over locally; Print Lab validates them exactly as it does Geometry World's, measures the mesh so physical size and printer fit work, shows a From Architecture Studio card with scale presets and a Revise button, and keeps no editable source (the studio keeps its own save). Covered by `tests/archstudio_printlab_bridge.test.js`.
- **Art Studio joins Geometry World as a Print Lab source (2026-09-05).** Question answered: four tools produce printable geometry (Print Lab, Geometry World, Architecture Studio, Art Studio); the earlier count of three predates the Architecture Studio bridge: Print Lab itself (primitive recipe, GLB, STL), Geometry World (STL handoff with editable block source), and now Art Studio sculpt mode. Its sculptures are already the Prim3D recipe Print Lab designs with, but the only way across was export-JSON-then-load. The sculpt toolbar gained a Print Lab button that hands the recipe over locally; Print Lab accepts a RECIPE handoff, opens on the Design tab with the recipe loaded, and shows a From Art Studio card with a Revise in Art Studio button. Covered by `tests/artstudio_printlab_bridge.test.js`; both tools mirrored to the desktop tree.
- **Print Lab default, stated rather than implied (2026-09-05).** The repository shows the 3D Print Lab tab unless the flag is explicitly false, so a fresh deployment ships with it visible to students and staff; `school_rewards_repository.test.js` pins that as intended. Every document, and the setting's own help text, read as though a school opts in. The portal setting, the manual, and the presenter guide now say plainly that a new deployment starts with the tab shown and that switching it off is a deliberate first-week step. A test holds the three documents to the behaviour in `Code.gs`. **Open question for the owner:** whether the default should instead be opt-in. That is a product decision, and flipping it would mean changing a deliberate, tested behaviour, so it was not changed here.
- **Presenter guide realigned (2026-09-05).** `docs/school_rewards_admin_demo.md` predated the Help panel, the bilingual practice page, and the tour's focus control, and no test covered it, so it could describe a screen that had moved. The route now opens with Help, notes the language switch, points at the three quick cards as leave-behinds, and keeps its five-minute timing. A gate in `school_rewards_manual.test.js` now holds it to the in-page guide: every role the in-page route visits must be named in both, both must mention Help, and the balance and stock figures the demo walkthrough asserts at runtime must appear in the document.
- **Full gate, one clean run (2026-09-05).** `npm run verify:school-rewards -- --pool=forks`: **16 files, 510 tests passed, exit code 0**, in 80 seconds. This covers the suites these passes did not touch as well, including the print portal, repository, integration contract, panel, and Print Lab bridges. The earlier note that a single clean invocation had never been achieved no longer applies; the threads pool was the cause.
- **Phone sweep (2026-09-05).** `dev-tools/school_rewards_mobile_sweep.cjs` measures every visible control at 390x844 in all four roles. It confirmed the claims in manual section 11: no horizontal overflow and the tab strip fixed to the bottom of the screen in every role, with the Help panel open and the tour running. It also found two touch targets under the 24px minimum: the built-in help links rendered 17-21px tall, and the checkbox rows, which are tapped through their label, rendered 16px. Both are fixed and pinned in `school_rewards_portal_features.test.js`; the sweep now reports zero controls under 24px. The sweep itself was corrected first, to skip screen-reader-only controls and to credit a checkbox its wrapping label.
- **Link and copy integrity (2026-09-05).** Every metadata claim on the manual was checked against the document: 13 sections is right, 5,189 words is about 26 minutes at normal reading speed so the 25-minute claim stands, and the reviewed date is refreshed to September 5. No internal anchor in the manual is dangling, the four Help-panel deep links all resolve to real manual sections, and the manual's link to the student quick card resolves. All of that is now a test.
- **Wrapper Spanish coverage gate (2026-09-05).** A second test walks the generated practice page and proves every wrapper string is in the Spanish dictionary: 58 markup strings, the attributes, the three scenario names, the 16 built-in tour strings, and the six runtime tour labels. Calibrated against deliberately broken copies, it catches an edited button label, a reworded tour title, and a newly added untranslated line, so it cannot pass blindly.
- **Print check (2026-09-05).** `dev-tools/school_rewards_print_check.cjs` generates real Chromium PDFs (Letter, half-inch margins) and reports page counts plus the computed break rule of every block. Quick cards: 3 pages for 3 cards, each card whole, so the third card did not disturb the one-card-per-page promise. Manual: the new diagram, the at-a-glance box, and tables computed to `break-inside:auto` and are now kept whole; tall screenshots stay breakable on purpose so a caption can follow to the next sheet. The manual's stated length was a guess (12, then 14, then a mis-measured 19) and is now the reproducible 29 pages. The first measurements were taken with the lazy-loaded figures still undecoded, which under-reports; the harness now forces every image to load and decode before pagination and returns the same count on three consecutive runs. Pinned in `school_rewards_manual.test.js`. Chromium's headless build downloads PDFs rather than rendering them, so this verifies page counts and break rules, not a visual read of each sheet.
- **Practice wrapper themes and keyboard (2026-09-05).** `dev-tools/school_rewards_wrapper_a11y.cjs` measures the contrast of every visible text element in the practice bar, introduction, demo guide, and tour box against its effective background: 37 elements checked in light, dark, and forced-colours modes, none below WCAG AA. Keyboard walk: Help opens with focus on the panel, Tab reaches Close and each link before leaving the panel, Escape returns focus to the button; the tour opens with focus on Next, Escape ends it and returns focus to Start the tour. One gap fixed: the tour box is last in the document, so tabbing past Exit wrapped to the top of the page; each step now has a "Go to the highlighted control" button that moves focus into the control the step is about, and the dialog is named by the step title (`aria-labelledby`).
- **Help panel dark theme (2026-09-05).** The panel's manual links kept the light-theme purple on the dark card (about 1.6:1). Dark mode now uses the portal's lavender accent and high-contrast mode its amber; both pinned in `school_rewards_portal_features.test.js`. Verified by computed-style probe and screenshots in dark and forced-colors modes.
- **Discoverability (2026-09-05).** The teacher guide's school-leaders chapter now has a School Rewards & Store section pointing at the manual and quick cards; the manuals index card links the quick cards and practice page; the package README opens with a Manuals and training section.
- **Practice page in Spanish (2026-09-05).** The practice wrapper now switches with the portal's language menu: bar, introduction, five-minute demo guide, customize panel labels and placeholders, scenario names, the built-in tour steps, and the tour box controls. Verified in jsdom (round trip es → en) and in a headless browser with screenshots (`scratch/school-rewards-practice-es*.png`).
- **Deep links, status table, student card (2026-09-05).** Each role section of the built-in Help panel ends with a "Read more in the manual" link to its own manual section (awarding, store, students, admin); four new catalogue strings, Spanish 922/922. Manual section 5 gained a table translating every Store tab note into what is true and what a cashier does. The quick cards gained a third one-page card for students and families, linked from manual sections 4 and 6.
- The award confirmation notice no longer doubles the period after an abbreviated surname (“Points recorded for Avery R.”). A seventh onboarding regression covers the notice text and its Undo button.


- Shopping-window saves send explicit UTC instants while their editors display local time. Status changes retain exact stored dates, including seconds and repeated clock hours. Invalid dates, reversed ranges and clock-change gaps are rejected locally.
- Saved print quotes retain their deadlines, override decisions, explanations and intentionally blank review fields. New quote defaults use the operator’s local time. Material/time estimates reject invalid values instead of silently rounding or substituting zero.

- The practice-page builder writes UTF-8 bytes without Windows newline conversion and avoids truncating synced files before opening them for updates. Both generated copies are checked for complete scripts, the full guided tour and deployment parity.

- Administrators can resume a private remix from its integrity-report row using only the original signed student intent. Altered or duplicate request records and missing, changed or duplicate files remain blocked; separate audit events record the administrator while preserving student attribution.
- Pending remix requests now contribute to the integrity report’s pending-operation count, and invalid remix signatures are reported explicitly.
- After administrator recovery, the original student can clear the stale retry state through a read-only status check on refresh, only after completion is confirmed and the recovered model is visible.
- A later coded rejection cannot discard an earlier uncertain request, including after a page reload. Known first-attempt validation errors release the form for correction; unknown outcomes retain the original key. Quote expiration and whole-number point limits are checked before submission.

- Private recipe remixes use a signed recovery record, a stable model ID and verified file reuse. Interrupted requests retain their original retry identity; ambiguous or conflicting files require administrator review.
- Print Lab, publication/moderation and guardian actions suppress overlapping submits and retain exact request keys after lost responses. Changed drafts cannot silently become new actions while the original is unresolved.
- Full and store refreshes stage results before rendering; older responses and errors are ignored. Checkout stops if another refresh supersedes its availability check.

- Confirmed changes throughout rewards, refunds, Print Lab, roster/member administration, school settings, windows, email scheduling and records workflows retain their completion message if the following refresh fails. Refresh saved changes performs only reads; genuinely uncertain requests keep their original retry behavior.
- Prize and stock editors immediately display the values returned by a confirmed save.

- Print handoff, binary-asset, and SIS file imports keep only the latest selection; stale reads and errors cannot overwrite it or restore a cleared selection.
- Partial group awards retain recorded/failed counts, warnings and the original retry key even if refresh fails or is retried.
- Roster preview/apply lock the selected import and suppress duplicate requests while running. Failed applies retain the original confirmation and idempotency key for retry.
- Print submission locks its draft during registration/upload/submission and suppresses duplicate clicks.
- A Print Lab loading failure no longer prevents core rewards and administration from rendering. An explicit retry control restores Print Lab; unavailable Print Lab content remains hidden.
- The catalog publication and private-remix pathway was verified end to end, including the existing permission normalization.
- Complete portal-to-repository tests now exercise role navigation, recognition-to-refund, recipe-to-print-refund, publication-to-remix, and administrative SIS/mail/report/year workflows.

## Deployment boundary

These are local source and packaged-file improvements. Updating this repository does not update an existing Apps Script deployment. Publish a new version of the school-owned deployment after copying the updated package files. Existing schema-v6 repositories need no schema migration for these changes; older repositories must follow the documented migration order.

SIS support is an approved JSON snapshot workflow, not a live vendor connection. Printer actions record workflow status; they do not send commands to a printer. Guardian consent and physical print review remain school responsibilities.
