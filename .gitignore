node_modules

.git-token*

# Security: PAT folder used for local gh auth — never commit the token
Claude Token/

# Archive: temp/debug/backup files moved here during cleanup
_archive/
# Dev scratch: one-off build scripts relocated out of source dirs (kept local)
_dev_scratch/

# Security: Never commit build artifacts (they contain baked-in API keys)
desktop/web-app/build/
desktop/web-app/.firebase/

# Security: Never commit environment files with secrets
desktop/web-app/.env
desktop/web-app/.env.local
desktop/web-app/.env.production
desktop/web-app/.env.maintainer-demo
*.env.local

# Security: Never commit API key files
api key.txt
*api*key*
*.key
*.secret

# Security: CSE API key config — deployed to Firebase only, never committed
desktop/web-app/public/cse-config.json
cse-config.json

# Large media files (too large for GitHub and for Cloudflare Pages 25 MiB cap)
*.m4a
*.mp4
*.mov
*.wav
# Exception: Kitchen Table audio is small (~3 MB) and embedded in index.html.
!Kitchen_Table_Scripts_and_Gamified_Learning.m4a
# TTS model files: Kokoro + Piper voices load from external CDNs at runtime
# (HuggingFace), not from this repo. Any local copies are dev-only.
*.onnx
word_audio_bank_part*.json
audio_bank.json
Sample audiobank.txt
profile-image.jpg

# Stale copies (source of truth is AlloFlowANTI.txt)
Firebase version/
svg/AlloFlowANTI.txt
src/App.jsx

# Prototype apps (kept locally, not in git)
3d game scaffold.txt
ConflictResolutionapp.txt
Migdas-2 app.txt
FBA-BIP Prototype.txt

# Personal & reference docs (kept locally)
Aaron Pomeranz Resume.txt
reflective_journal.md
Report Writer prompts.txt
*.pptx
GEIS protocol*/

# One-off scripts & scratch (kept in git history)
scripts/*.py
scripts/*.txt
scripts/*.js
scripts/archive/
stem_test*.js
search_results.txt
tour.txt

# Outreach & personal correspondence (emails, drafts, pitches)
outreach_*.md
*_draft.md
*_email_final.md

# Pilot study / research documents (consent forms, surveys, protocols)
pilot/

# Monolith backup files (kept locally, source of truth is AlloFlowANTI.txt)
AlloFlowANTI_backup*.txt
desktop/web-app/src/AlloFlowANTI_backup*.txt

# Build/deploy logs
desktop/web-app/_build_log.txt
desktop/web-app/_deploy_log.txt
desktop/web-app/_npm_log.txt

# One-off fix/extract scripts (not part of the build pipeline)
_fix_*.ps1
_fix_*.js
extract_*.ps1
desktop/web-app/public/stem_lab/_fix_*.js
desktop/web-app/public/stem_lab/extract_*.ps1
stem_lab/_build_p*.py

# Marketing / non-code docs
youtube_multilingual_description.md

# Graphify knowledge-graph outputs (developer index for AI coding sessions;
# regenerable via `/graphify` slash command — see https://graphify.net)
graphify-out/
.graphify/

# Local infrastructure scripts
setup-local-ai.bat

# Misc test files
nonshareabletest.txt

# Catalog staging area: lessons exported from AlloFlow, before placing in
# examples/ or submitting to the Cloudflare Worker
Lesson JSONs/

# Agent-generated temp/debug files (one-off scripts, logs, diffs, backups)
fix_*.py
fix_*.js
find_*.py
find_*.js
patch_*.py
replace_*.py
restore_*.py
compare_*.py
search_*.py
investigate*.js
investigation*.txt
hooks_*.txt
*.bak
# Date/phase-suffixed backups (e.g. acholi.js.bak.20260604, ui_strings.js.bak.handpurge-20260604)
*.bak.*
*.bak-*
# PDF-pipeline test artifacts (may contain report content — never commit)
High Impact Reports*.pdf
err.log
err.txt
error.txt
diff.txt
diff2.txt
found_lines.txt
search_output.txt
search_prose.txt
temp_*.txt
test_results.txt
trace_result.txt
build_output.txt
tmp_*.txt
tmp_*.py
tmp_*.js
deep_analysis.js
nest_track.js
star_code_map.txt
add_features.js
test_extract_query.js

# Playwright e2e artifacts
test-results/
playwright-report/
playwright/.cache/

# Vitest coverage reports (generated by `npm run test:coverage`)
coverage/

# AlloFlow Desktop local runtime state
desktop/.local/
desktop/app-build/
desktop/dist/

# Proposal drafts (same rationale as outreach_*.md — personal docs, not app code)
el_proposal*.md
el_proposal*.pdf

# veraPDF clause-diff artifacts (regenerated by the tag-tree golden on every run)
tests/e2e/artifacts/
help_gaps/
dev-tools/i18n/lang_staleness/
dev-tools/stem_i18n_report/user_facing.csv
dev-tools/stem_i18n_report/inventory.json
dev-tools/stem_i18n_report/ui_strings_stem_*.json
desktop/.local/
vitest-results.json

# Generated at build time by desktop/scripts/build-edition.cjs (deleted after build)
desktop/electron-builder.*.effective.json

# Transient desktop build/run logs
desktop-build.log
desktop-run.log
